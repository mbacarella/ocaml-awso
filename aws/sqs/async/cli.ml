(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String"
       and label = flag "label" (required string) ~doc:"STRING String"
       and aWSAccountIds =
         flag "a-w-s-account-ids" (required json_arg)
           ~doc:"JSON AWSAccountIdList"
       and actions =
         flag "actions" (required json_arg) ~doc:"JSON ActionNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_permission
           (Values.AddPermissionRequest.make ~queueUrl ~label
              ~aWSAccountIds:(Values.AWSAccountIdList.of_json aWSAccountIds)
              ~actions:(Values.ActionNameList.of_json actions) ()) None None])
let change_message_visibility =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String"
       and receiptHandle =
         flag "receipt-handle" (required string) ~doc:"STRING String"
       and visibilityTimeout =
         flag "visibility-timeout" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.change_message_visibility
           (Values.ChangeMessageVisibilityRequest.make ~queueUrl
              ~receiptHandle ~visibilityTimeout ()) None None])
let change_message_visibility_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String"
       and entries =
         flag "entries" (required json_arg)
           ~doc:"JSON ChangeMessageVisibilityBatchRequestEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.change_message_visibility_batch
           (Values.ChangeMessageVisibilityBatchRequest.make ~queueUrl
              ~entries:(Values.ChangeMessageVisibilityBatchRequestEntryList.of_json
                          entries) ())
           (Some Values.ChangeMessageVisibilityBatchResult.to_json)
           (Some Values.ChangeMessageVisibilityBatchResult.error_to_json)])
let create_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON QueueAttributeMap"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and queueName =
         flag "queue-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_queue
           (Values.CreateQueueRequest.make
              ?attributes:(Option.map ~f:Values.QueueAttributeMap.of_json
                             attributes)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~queueName ())
           (Some Values.CreateQueueResult.to_json)
           (Some Values.CreateQueueResult.error_to_json)])
let delete_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String"
       and receiptHandle =
         flag "receipt-handle" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_message
           (Values.DeleteMessageRequest.make ~queueUrl ~receiptHandle ())
           None None])
let delete_message_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String"
       and entries =
         flag "entries" (required json_arg)
           ~doc:"JSON DeleteMessageBatchRequestEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_message_batch
           (Values.DeleteMessageBatchRequest.make ~queueUrl
              ~entries:(Values.DeleteMessageBatchRequestEntryList.of_json
                          entries) ())
           (Some Values.DeleteMessageBatchResult.to_json)
           (Some Values.DeleteMessageBatchResult.error_to_json)])
let delete_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_queue (Values.DeleteQueueRequest.make ~queueUrl ()) None
           None])
let get_queue_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeNames =
         flag "attribute-names" (optional json_arg)
           ~doc:"JSON AttributeNameList"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_queue_attributes
           (Values.GetQueueAttributesRequest.make
              ?attributeNames:(Option.map ~f:Values.AttributeNameList.of_json
                                 attributeNames) ~queueUrl ())
           (Some Values.GetQueueAttributesResult.to_json)
           (Some Values.GetQueueAttributesResult.error_to_json)])
let get_queue_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueOwnerAWSAccountId =
         flag "queue-owner-a-w-s-account-id" (optional string)
           ~doc:"STRING String"
       and queueName =
         flag "queue-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_queue_url
           (Values.GetQueueUrlRequest.make ?queueOwnerAWSAccountId ~queueName
              ()) (Some Values.GetQueueUrlResult.to_json)
           (Some Values.GetQueueUrlResult.error_to_json)])
let list_dead_letter_source_queues =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxedInteger"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dead_letter_source_queues
           (Values.ListDeadLetterSourceQueuesRequest.make ?nextToken
              ?maxResults ~queueUrl ())
           (Some Values.ListDeadLetterSourceQueuesResult.to_json)
           (Some Values.ListDeadLetterSourceQueuesResult.error_to_json)])
let list_queue_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queue_tags (Values.ListQueueTagsRequest.make ~queueUrl ())
           (Some Values.ListQueueTagsResult.to_json)
           (Some Values.ListQueueTagsResult.error_to_json)])
let list_queues =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueNamePrefix =
         flag "queue-name-prefix" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxedInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queues
           (Values.ListQueuesRequest.make ?queueNamePrefix ?nextToken
              ?maxResults ()) (Some Values.ListQueuesResult.to_json)
           (Some Values.ListQueuesResult.error_to_json)])
let purge_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.purge_queue (Values.PurgeQueueRequest.make ~queueUrl ()) None
           None])
let receive_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeNames =
         flag "attribute-names" (optional json_arg)
           ~doc:"JSON AttributeNameList"
       and messageAttributeNames =
         flag "message-attribute-names" (optional json_arg)
           ~doc:"JSON MessageAttributeNameList"
       and maxNumberOfMessages =
         flag "max-number-of-messages" (optional int) ~doc:"INT Integer"
       and visibilityTimeout =
         flag "visibility-timeout" (optional int) ~doc:"INT Integer"
       and waitTimeSeconds =
         flag "wait-time-seconds" (optional int) ~doc:"INT Integer"
       and receiveRequestAttemptId =
         flag "receive-request-attempt-id" (optional string)
           ~doc:"STRING String"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.receive_message
           (Values.ReceiveMessageRequest.make
              ?attributeNames:(Option.map ~f:Values.AttributeNameList.of_json
                                 attributeNames)
              ?messageAttributeNames:(Option.map
                                        ~f:Values.MessageAttributeNameList.of_json
                                        messageAttributeNames)
              ?maxNumberOfMessages ?visibilityTimeout ?waitTimeSeconds
              ?receiveRequestAttemptId ~queueUrl ())
           (Some Values.ReceiveMessageResult.to_json)
           (Some Values.ReceiveMessageResult.error_to_json)])
let remove_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String"
       and label = flag "label" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_permission
           (Values.RemovePermissionRequest.make ~queueUrl ~label ()) None
           None])
let send_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and delaySeconds =
         flag "delay-seconds" (optional int) ~doc:"INT Integer"
       and messageAttributes =
         flag "message-attributes" (optional json_arg)
           ~doc:"JSON MessageBodyAttributeMap"
       and messageSystemAttributes =
         flag "message-system-attributes" (optional json_arg)
           ~doc:"JSON MessageBodySystemAttributeMap"
       and messageDeduplicationId =
         flag "message-deduplication-id" (optional string)
           ~doc:"STRING String"
       and messageGroupId =
         flag "message-group-id" (optional string) ~doc:"STRING String"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String"
       and messageBody =
         flag "message-body" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_message
           (Values.SendMessageRequest.make ?delaySeconds
              ?messageAttributes:(Option.map
                                    ~f:Values.MessageBodyAttributeMap.of_json
                                    messageAttributes)
              ?messageSystemAttributes:(Option.map
                                          ~f:Values.MessageBodySystemAttributeMap.of_json
                                          messageSystemAttributes)
              ?messageDeduplicationId ?messageGroupId ~queueUrl ~messageBody
              ()) (Some Values.SendMessageResult.to_json)
           (Some Values.SendMessageResult.error_to_json)])
let send_message_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String"
       and entries =
         flag "entries" (required json_arg)
           ~doc:"JSON SendMessageBatchRequestEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_message_batch
           (Values.SendMessageBatchRequest.make ~queueUrl
              ~entries:(Values.SendMessageBatchRequestEntryList.of_json
                          entries) ())
           (Some Values.SendMessageBatchResult.to_json)
           (Some Values.SendMessageBatchResult.error_to_json)])
let set_queue_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String"
       and attributes =
         flag "attributes" (required json_arg) ~doc:"JSON QueueAttributeMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_queue_attributes
           (Values.SetQueueAttributesRequest.make ~queueUrl
              ~attributes:(Values.QueueAttributeMap.of_json attributes) ())
           None None])
let tag_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_queue
           (Values.TagQueueRequest.make ~queueUrl
              ~tags:(Values.TagMap.of_json tags) ()) None None])
let untag_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queueUrl = flag "queue-url" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_queue
           (Values.UntagQueueRequest.make ~queueUrl
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-permission", add_permission);
    ("change-message-visibility", change_message_visibility);
    ("change-message-visibility-batch", change_message_visibility_batch);
    ("create-queue", create_queue);
    ("delete-message", delete_message);
    ("delete-message-batch", delete_message_batch);
    ("delete-queue", delete_queue);
    ("get-queue-attributes", get_queue_attributes);
    ("get-queue-url", get_queue_url);
    ("list-dead-letter-source-queues", list_dead_letter_source_queues);
    ("list-queue-tags", list_queue_tags);
    ("list-queues", list_queues);
    ("purge-queue", purge_queue);
    ("receive-message", receive_message);
    ("remove-permission", remove_permission);
    ("send-message", send_message);
    ("send-message-batch", send_message_batch);
    ("set-queue-attributes", set_queue_attributes);
    ("tag-queue", tag_queue);
    ("untag-queue", untag_queue)]
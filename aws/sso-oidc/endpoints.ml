(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateToken: (CreateTokenRequest.t, CreateTokenResponse.t,
  CreateTokenResponse.error) t 
  | RegisterClient: (RegisterClientRequest.t, RegisterClientResponse.t,
  RegisterClientResponse.error) t 
  | StartDeviceAuthorization: (StartDeviceAuthorizationRequest.t,
  StartDeviceAuthorizationResponse.t, StartDeviceAuthorizationResponse.error)
  t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateToken -> `POST
  | RegisterClient -> `POST
  | StartDeviceAuthorization -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateToken -> (Format.kasprintf Uri.of_string) "/token"
      | RegisterClient -> (Format.kasprintf Uri.of_string) "/client/register"
      | StartDeviceAuthorization ->
          (Format.kasprintf Uri.of_string) "/device_authorization")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateToken ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("clientId",
                           (ClientId.to_value req.CreateTokenRequest.clientId));
                      Some
                        ("clientSecret",
                          (ClientSecret.to_value
                             req.CreateTokenRequest.clientSecret));
                      Some
                        ("grantType",
                          (GrantType.to_value
                             req.CreateTokenRequest.grantType));
                      Some
                        ("deviceCode",
                          (DeviceCode.to_value
                             req.CreateTokenRequest.deviceCode));
                      Option.map req.CreateTokenRequest.code
                        ~f:(fun x -> ("code", (AuthCode.to_value x)));
                      Option.map req.CreateTokenRequest.refreshToken
                        ~f:(fun x ->
                              ("refreshToken", (RefreshToken.to_value x)));
                      Option.map req.CreateTokenRequest.scope
                        ~f:(fun x -> ("scope", (Scopes.to_value x)));
                      Option.map req.CreateTokenRequest.redirectUri
                        ~f:(fun x -> ("redirectUri", (URI.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | RegisterClient ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("clientName",
                           (ClientName.to_value
                              req.RegisterClientRequest.clientName));
                      Some
                        ("clientType",
                          (ClientType.to_value
                             req.RegisterClientRequest.clientType));
                      Option.map req.RegisterClientRequest.scopes
                        ~f:(fun x -> ("scopes", (Scopes.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartDeviceAuthorization ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("clientId",
                           (ClientId.to_value
                              req.StartDeviceAuthorizationRequest.clientId));
                      Some
                        ("clientSecret",
                          (ClientSecret.to_value
                             req.StartDeviceAuthorizationRequest.clientSecret));
                      Some
                        ("startUrl",
                          (URI.to_value
                             req.StartDeviceAuthorizationRequest.startUrl))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : (Awso.Http.Response.t, Awso.Http.Io.Error.call) result) :
  (o, [ `AWS of e  | `Transport of Awso.Http.Io.Error.call ]) result=
  let handle_error err error_of_json =
    match err with
    | `Too_many_redirects -> Error (`Transport `Too_many_redirects)
    | `Bad_response
        { Awso.Http.Io.Error.code = code; body; x_amzn_error_type } ->
        let generic_error () =
          Error
            (`Transport
               (`Bad_response
                  { Awso.Http.Io.Error.code = code; body; x_amzn_error_type })) in
        (match (x_amzn_error_type, error_of_json,
                 ((code >= 400) && (code <= 599)))
         with
         | (Some error_type, Some error_of_json, true) ->
             let json = Yojson.Safe.from_string body in
             Error (`AWS (error_of_json error_type json))
         | (None, Some error_of_json, true) ->
             (try
                let json = Yojson.Safe.from_string body in
                match json |> (Yojson.Safe.Util.member "__type") with
                | `String error_type ->
                    let error_type =
                      match String.lsplit2 error_type ~on:'#' with
                      | Some (_, s) -> s
                      | None -> error_type in
                    Error (`AWS (error_of_json error_type json))
                | `Null -> generic_error ()
                | _ ->
                    failwithf "Error '__type' did not have string type: %s"
                      body ()
              with | _ -> generic_error ())
         | (None, _, _) | (_, None, _) | (_, _, false) -> generic_error ()) in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = resp in
  let _ = handle_error in
  let _ = response_to_json in
  match endpoint with
  | CreateToken ->
      (match resp with
       | Error err ->
           handle_error err (Some CreateTokenResponse.error_of_json)
       | Ok resp -> Ok (CreateTokenResponse.of_json (response_to_json resp)))
  | RegisterClient ->
      (match resp with
       | Error err ->
           handle_error err (Some RegisterClientResponse.error_of_json)
       | Ok resp ->
           Ok (RegisterClientResponse.of_json (response_to_json resp)))
  | StartDeviceAuthorization ->
      (match resp with
       | Error err ->
           handle_error err
             (Some StartDeviceAuthorizationResponse.error_of_json)
       | Ok resp ->
           Ok
             (StartDeviceAuthorizationResponse.of_json
                (response_to_json resp)))
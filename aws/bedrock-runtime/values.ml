(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.bedrock_runtime
let apiVersion = "2023-09-30"
let endpointPrefix = "bedrock-runtime"
let serviceFullName = "Amazon Bedrock Runtime"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module GuardrailAutomatedReasoningStatementNaturalLanguageContent =
  struct
    type nonrec t = string
    let context_ =
      "GuardrailAutomatedReasoningStatementNaturalLanguageContent"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json
        ~kind:"GuardrailAutomatedReasoningStatementNaturalLanguageContent" j
    let to_json = simple_to_json to_value
  end
module GuardrailAutomatedReasoningStatementLogicContent =
  struct
    type nonrec t = string
    let context_ = "GuardrailAutomatedReasoningStatementLogicContent"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"GuardrailAutomatedReasoningStatementLogicContent"
        j
    let to_json = simple_to_json to_value
  end
module GuardrailAutomatedReasoningInputTextReference =
  struct
    type nonrec t =
      {
      text:
        GuardrailAutomatedReasoningStatementNaturalLanguageContent.t option
        [@ocaml.doc
          "The specific text from the original input that this reference points to."]}
    let make ?text = fun () -> { text }
    let to_value x =
      structure_to_value
        [("text",
           (Option.map x.text
              ~f:GuardrailAutomatedReasoningStatementNaturalLanguageContent.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text =
        (Option.map
           ~f:GuardrailAutomatedReasoningStatementNaturalLanguageContent.of_xml)
          (Xml.child xml_arg0 "text") in
      make ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text =
        field_map json__ "text"
          GuardrailAutomatedReasoningStatementNaturalLanguageContent.of_json in
      make ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "References a portion of the original input text that corresponds to logical elements."]
module GuardrailAutomatedReasoningStatement =
  struct
    type nonrec t =
      {
      logic: GuardrailAutomatedReasoningStatementLogicContent.t option
        [@ocaml.doc "The formal logical representation of the statement."];
      naturalLanguage:
        GuardrailAutomatedReasoningStatementNaturalLanguageContent.t option
        [@ocaml.doc
          "The natural language explanation of the logical statement."]}
    let make ?logic =
      fun ?naturalLanguage -> fun () -> { logic; naturalLanguage }
    let to_value x =
      structure_to_value
        [("logic",
           (Option.map x.logic
              ~f:GuardrailAutomatedReasoningStatementLogicContent.to_value));
        ("naturalLanguage",
          (Option.map x.naturalLanguage
             ~f:GuardrailAutomatedReasoningStatementNaturalLanguageContent.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let naturalLanguage =
        (Option.map
           ~f:GuardrailAutomatedReasoningStatementNaturalLanguageContent.of_xml)
          (Xml.child xml_arg0 "naturalLanguage") in
      let logic =
        (Option.map
           ~f:GuardrailAutomatedReasoningStatementLogicContent.of_xml)
          (Xml.child xml_arg0 "logic") in
      make ?naturalLanguage ?logic ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let naturalLanguage =
        field_map json__ "naturalLanguage"
          GuardrailAutomatedReasoningStatementNaturalLanguageContent.of_json in
      let logic =
        field_map json__ "logic"
          GuardrailAutomatedReasoningStatementLogicContent.of_json in
      make ?naturalLanguage ?logic ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A logical statement that includes both formal logic representation and natural language explanation."]
module GuardrailAutomatedReasoningInputTextReferenceList =
  struct
    type nonrec t = GuardrailAutomatedReasoningInputTextReference.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |>
         (List.map ~f:GuardrailAutomatedReasoningInputTextReference.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:GuardrailAutomatedReasoningInputTextReference.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailAutomatedReasoningInputTextReferenceList"
        ~of_json:GuardrailAutomatedReasoningInputTextReference.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailAutomatedReasoningStatementList =
  struct
    type nonrec t = GuardrailAutomatedReasoningStatement.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailAutomatedReasoningStatement.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:GuardrailAutomatedReasoningStatement.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailAutomatedReasoningStatementList"
        ~of_json:GuardrailAutomatedReasoningStatement.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailAutomatedReasoningTranslationConfidence =
  struct
    type nonrec t = float
    let make i =
      let open Result in
        ok_or_failwith
          ((check_float_min i ~min:1.) >>=
             (fun () -> check_float_min i ~min:0.));
        i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module GuardrailAutomatedReasoningTranslation =
  struct
    type nonrec t =
      {
      premises: GuardrailAutomatedReasoningStatementList.t option
        [@ocaml.doc
          "The logical statements that serve as the foundation or assumptions for the claims."];
      claims: GuardrailAutomatedReasoningStatementList.t option
        [@ocaml.doc
          "The logical statements that are being validated against the premises and policy rules."];
      untranslatedPremises:
        GuardrailAutomatedReasoningInputTextReferenceList.t option
        [@ocaml.doc
          "References to portions of the original input text that correspond to the premises but could not be fully translated."];
      untranslatedClaims:
        GuardrailAutomatedReasoningInputTextReferenceList.t option
        [@ocaml.doc
          "References to portions of the original input text that correspond to the claims but could not be fully translated."];
      confidence: GuardrailAutomatedReasoningTranslationConfidence.t option
        [@ocaml.doc
          "A confidence score between 0 and 1 indicating how certain the system is about the logical translation."]}
    let make ?premises =
      fun ?claims ->
        fun ?untranslatedPremises ->
          fun ?untranslatedClaims ->
            fun ?confidence ->
              fun () ->
                {
                  premises;
                  claims;
                  untranslatedPremises;
                  untranslatedClaims;
                  confidence
                }
    let to_value x =
      structure_to_value
        [("premises",
           (Option.map x.premises
              ~f:GuardrailAutomatedReasoningStatementList.to_value));
        ("claims",
          (Option.map x.claims
             ~f:GuardrailAutomatedReasoningStatementList.to_value));
        ("untranslatedPremises",
          (Option.map x.untranslatedPremises
             ~f:GuardrailAutomatedReasoningInputTextReferenceList.to_value));
        ("untranslatedClaims",
          (Option.map x.untranslatedClaims
             ~f:GuardrailAutomatedReasoningInputTextReferenceList.to_value));
        ("confidence",
          (Option.map x.confidence
             ~f:GuardrailAutomatedReasoningTranslationConfidence.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let confidence =
        (Option.map
           ~f:GuardrailAutomatedReasoningTranslationConfidence.of_xml)
          (Xml.child xml_arg0 "confidence") in
      let untranslatedClaims =
        (Option.map
           ~f:GuardrailAutomatedReasoningInputTextReferenceList.of_xml)
          (Xml.child xml_arg0 "untranslatedClaims") in
      let untranslatedPremises =
        (Option.map
           ~f:GuardrailAutomatedReasoningInputTextReferenceList.of_xml)
          (Xml.child xml_arg0 "untranslatedPremises") in
      let claims =
        (Option.map ~f:GuardrailAutomatedReasoningStatementList.of_xml)
          (Xml.child xml_arg0 "claims") in
      let premises =
        (Option.map ~f:GuardrailAutomatedReasoningStatementList.of_xml)
          (Xml.child xml_arg0 "premises") in
      make ?confidence ?untranslatedClaims ?untranslatedPremises ?claims
        ?premises ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let confidence =
        field_map json__ "confidence"
          GuardrailAutomatedReasoningTranslationConfidence.of_json in
      let untranslatedClaims =
        field_map json__ "untranslatedClaims"
          GuardrailAutomatedReasoningInputTextReferenceList.of_json in
      let untranslatedPremises =
        field_map json__ "untranslatedPremises"
          GuardrailAutomatedReasoningInputTextReferenceList.of_json in
      let claims =
        field_map json__ "claims"
          GuardrailAutomatedReasoningStatementList.of_json in
      let premises =
        field_map json__ "premises"
          GuardrailAutomatedReasoningStatementList.of_json in
      make ?confidence ?untranslatedClaims ?untranslatedPremises ?claims
        ?premises ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the logical translation of natural language input into formal logical statements, including premises, claims, and confidence scores."]
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module AutomatedReasoningRuleIdentifier =
  struct
    type nonrec t = string
    let context_ = "AutomatedReasoningRuleIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:12) >>=
                  (fun () -> check_pattern i ~pattern:"[a-z0-9]{12}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AutomatedReasoningRuleIdentifier" j
    let to_json = simple_to_json to_value
  end
module GuardrailAutomatedReasoningPolicyVersionArn =
  struct
    type nonrec t = string
    let context_ = "GuardrailAutomatedReasoningPolicyVersionArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:automated-reasoning-policy/[a-z0-9]{12}(:([1-9][0-9]{0,11}))?")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"GuardrailAutomatedReasoningPolicyVersionArn" j
    let to_json = simple_to_json to_value
  end
module GuardrailAutomatedReasoningTranslationList =
  struct
    type nonrec t = GuardrailAutomatedReasoningTranslation.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailAutomatedReasoningTranslation.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:GuardrailAutomatedReasoningTranslation.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailAutomatedReasoningTranslationList"
        ~of_json:GuardrailAutomatedReasoningTranslation.of_json j
    let to_json v = composed_to_json to_value v
  end
module DocumentContentBlock =
  struct
    type nonrec t =
      {
      text: String_.t option [@ocaml.doc "The text content of the document."]}
    let make ?text = fun () -> { text }
    let to_value x =
      structure_to_value [("text", (Option.map x.text ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" String_.of_json in make ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the actual content of a document that can be processed by the model and potentially cited in the response."]
module AccountId =
  struct
    type nonrec t = string
    let context_ = "AccountId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[0-9]{12}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AccountId" j
    let to_json = simple_to_json to_value
  end
module S3Uri =
  struct
    type nonrec t = string
    let context_ = "S3Uri"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"s3://[a-z0-9][\\.\\-a-z0-9]{1,61}[a-z0-9](/.*)?")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3Uri" j
    let to_json = simple_to_json to_value
  end
module GuardrailAutomatedReasoningLogicWarningType =
  struct
    type nonrec t =
      | ALWAYS_FALSE 
      | ALWAYS_TRUE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ALWAYS_FALSE -> "ALWAYS_FALSE"
      | ALWAYS_TRUE -> "ALWAYS_TRUE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ALWAYS_FALSE" -> ALWAYS_FALSE
      | "ALWAYS_TRUE" -> ALWAYS_TRUE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration GuardrailAutomatedReasoningLogicWarningType"
           xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"GuardrailAutomatedReasoningLogicWarningType" j)
    let to_json = simple_to_json to_value
  end
module GuardrailAutomatedReasoningRule =
  struct
    type nonrec t =
      {
      identifier: AutomatedReasoningRuleIdentifier.t option
        [@ocaml.doc "The unique identifier of the automated reasoning rule."];
      policyVersionArn: GuardrailAutomatedReasoningPolicyVersionArn.t option
        [@ocaml.doc
          "The ARN of the automated reasoning policy version that contains this rule."]}
    let make ?identifier =
      fun ?policyVersionArn -> fun () -> { identifier; policyVersionArn }
    let to_value x =
      structure_to_value
        [("identifier",
           (Option.map x.identifier
              ~f:AutomatedReasoningRuleIdentifier.to_value));
        ("policyVersionArn",
          (Option.map x.policyVersionArn
             ~f:GuardrailAutomatedReasoningPolicyVersionArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyVersionArn =
        (Option.map ~f:GuardrailAutomatedReasoningPolicyVersionArn.of_xml)
          (Xml.child xml_arg0 "policyVersionArn") in
      let identifier =
        (Option.map ~f:AutomatedReasoningRuleIdentifier.of_xml)
          (Xml.child xml_arg0 "identifier") in
      make ?policyVersionArn ?identifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policyVersionArn =
        field_map json__ "policyVersionArn"
          GuardrailAutomatedReasoningPolicyVersionArn.of_json in
      let identifier =
        field_map json__ "identifier"
          AutomatedReasoningRuleIdentifier.of_json in
      make ?policyVersionArn ?identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "References a specific automated reasoning policy rule that was applied during evaluation."]
module GuardrailAutomatedReasoningScenario =
  struct
    type nonrec t =
      {
      statements: GuardrailAutomatedReasoningStatementList.t option
        [@ocaml.doc
          "List of logical assignments and statements that define this scenario."]}
    let make ?statements = fun () -> { statements }
    let to_value x =
      structure_to_value
        [("statements",
           (Option.map x.statements
              ~f:GuardrailAutomatedReasoningStatementList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statements =
        (Option.map ~f:GuardrailAutomatedReasoningStatementList.of_xml)
          (Xml.child xml_arg0 "statements") in
      make ?statements ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let statements =
        field_map json__ "statements"
          GuardrailAutomatedReasoningStatementList.of_json in
      make ?statements ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a logical scenario where claims can be evaluated as true or false, containing specific logical assignments."]
module GuardrailAutomatedReasoningTranslationOption =
  struct
    type nonrec t =
      {
      translations: GuardrailAutomatedReasoningTranslationList.t option
        [@ocaml.doc
          "Example translations that provide this possible interpretation of the input."]}
    let make ?translations = fun () -> { translations }
    let to_value x =
      structure_to_value
        [("translations",
           (Option.map x.translations
              ~f:GuardrailAutomatedReasoningTranslationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let translations =
        (Option.map ~f:GuardrailAutomatedReasoningTranslationList.of_xml)
          (Xml.child xml_arg0 "translations") in
      make ?translations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let translations =
        field_map json__ "translations"
          GuardrailAutomatedReasoningTranslationList.of_json in
      make ?translations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents one possible logical interpretation of ambiguous input content."]
module DocumentCharLocationDocumentIndexInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for DocumentCharLocationDocumentIndexInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DocumentCharLocationEndInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for DocumentCharLocationEndInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DocumentCharLocationStartInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for DocumentCharLocationStartInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DocumentChunkLocationDocumentIndexInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for DocumentChunkLocationDocumentIndexInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DocumentChunkLocationEndInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for DocumentChunkLocationEndInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DocumentChunkLocationStartInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for DocumentChunkLocationStartInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DocumentPageLocationDocumentIndexInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for DocumentPageLocationDocumentIndexInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DocumentPageLocationEndInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for DocumentPageLocationEndInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DocumentPageLocationStartInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for DocumentPageLocationStartInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SearchResultLocationEndInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for SearchResultLocationEndInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SearchResultLocationSearchResultIndexInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for SearchResultLocationSearchResultIndexInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SearchResultLocationStartInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for SearchResultLocationStartInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module DocumentContentBlocks =
  struct
    type nonrec t = DocumentContentBlock.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DocumentContentBlock.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DocumentContentBlock.of_xml)
    let of_json j =
      list_of_json ~kind:"DocumentContentBlocks"
        ~of_json:DocumentContentBlock.of_json j
    let to_json v = composed_to_json to_value v
  end
module DocumentSourceBytesBlob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module S3Location =
  struct
    type nonrec t =
      {
      uri: S3Uri.t [@ocaml.doc "An object URI starting with s3://."];
      bucketOwner: AccountId.t option
        [@ocaml.doc
          "If the bucket belongs to another AWS account, specify that account's ID."]}
    let context_ = "S3Location"
    let make ?bucketOwner = fun ~uri -> fun () -> { bucketOwner; uri }
    let to_value x =
      structure_to_value
        [("uri", (Some (S3Uri.to_value x.uri)));
        ("bucketOwner", (Option.map x.bucketOwner ~f:AccountId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bucketOwner =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "bucketOwner") in
      let uri = S3Uri.of_xml (Xml.child_exn ~context:context_ xml_arg0 "uri") in
      make ?bucketOwner ~uri ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bucketOwner = field_map json__ "bucketOwner" AccountId.of_json in
      let uri = field_map_exn json__ "uri" S3Uri.of_json in
      make ?bucketOwner ~uri ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A storage location in an Amazon S3 bucket."]
module ImageSourceBytesBlob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module SearchResultContentBlock =
  struct
    type nonrec t = {
      text: String_.t [@ocaml.doc "The actual text content"]}
    let context_ = "SearchResultContentBlock"
    let make ~text = fun () -> { text }
    let to_value x =
      structure_to_value [("text", (Some (String_.to_value x.text)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "text") in
      make ~text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map_exn json__ "text" String_.of_json in make ~text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A block within a search result that contains the content."]
module VideoSourceBytesBlob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module GuardrailAutomatedReasoningLogicWarning =
  struct
    type nonrec t =
      {
      type_: GuardrailAutomatedReasoningLogicWarningType.t option
        [@ocaml.doc
          "The category of the detected logical issue, such as statements that are always true or always false."];
      premises: GuardrailAutomatedReasoningStatementList.t option
        [@ocaml.doc
          "The logical statements that serve as premises under which the claims are validated."];
      claims: GuardrailAutomatedReasoningStatementList.t option
        [@ocaml.doc
          "The logical statements that are validated while assuming the policy and premises."]}
    let make ?type_ =
      fun ?premises -> fun ?claims -> fun () -> { type_; premises; claims }
    let to_value x =
      structure_to_value
        [("type",
           (Option.map x.type_
              ~f:GuardrailAutomatedReasoningLogicWarningType.to_value));
        ("premises",
          (Option.map x.premises
             ~f:GuardrailAutomatedReasoningStatementList.to_value));
        ("claims",
          (Option.map x.claims
             ~f:GuardrailAutomatedReasoningStatementList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let claims =
        (Option.map ~f:GuardrailAutomatedReasoningStatementList.of_xml)
          (Xml.child xml_arg0 "claims") in
      let premises =
        (Option.map ~f:GuardrailAutomatedReasoningStatementList.of_xml)
          (Xml.child xml_arg0 "premises") in
      let type_ =
        (Option.map ~f:GuardrailAutomatedReasoningLogicWarningType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?claims ?premises ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let claims =
        field_map json__ "claims"
          GuardrailAutomatedReasoningStatementList.of_json in
      let premises =
        field_map json__ "premises"
          GuardrailAutomatedReasoningStatementList.of_json in
      let type_ =
        field_map json__ "type"
          GuardrailAutomatedReasoningLogicWarningType.of_json in
      make ?claims ?premises ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Identifies logical issues in the translated statements that exist independent of any policy rules, such as statements that are always true or always false."]
module GuardrailAutomatedReasoningRuleList =
  struct
    type nonrec t = GuardrailAutomatedReasoningRule.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailAutomatedReasoningRule.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailAutomatedReasoningRule.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailAutomatedReasoningRuleList"
        ~of_json:GuardrailAutomatedReasoningRule.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailAutomatedReasoningDifferenceScenarioList =
  struct
    type nonrec t = GuardrailAutomatedReasoningScenario.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:2) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailAutomatedReasoningScenario.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:GuardrailAutomatedReasoningScenario.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailAutomatedReasoningDifferenceScenarioList"
        ~of_json:GuardrailAutomatedReasoningScenario.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailAutomatedReasoningTranslationOptionList =
  struct
    type nonrec t = GuardrailAutomatedReasoningTranslationOption.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:2) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |>
         (List.map ~f:GuardrailAutomatedReasoningTranslationOption.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:GuardrailAutomatedReasoningTranslationOption.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailAutomatedReasoningTranslationOptionList"
        ~of_json:GuardrailAutomatedReasoningTranslationOption.of_json j
    let to_json v = composed_to_json to_value v
  end
module DocumentCharLocation =
  struct
    type nonrec t =
      {
      documentIndex: DocumentCharLocationDocumentIndexInteger.t option
        [@ocaml.doc
          "The index of the document within the array of documents provided in the request."];
      start: DocumentCharLocationStartInteger.t option
        [@ocaml.doc
          "The starting character position of the cited content within the document."];
      end_: DocumentCharLocationEndInteger.t option
        [@ocaml.doc
          "The ending character position of the cited content within the document."]}
    let make ?documentIndex =
      fun ?start -> fun ?end_ -> fun () -> { documentIndex; start; end_ }
    let to_value x =
      structure_to_value
        [("documentIndex",
           (Option.map x.documentIndex
              ~f:DocumentCharLocationDocumentIndexInteger.to_value));
        ("start",
          (Option.map x.start ~f:DocumentCharLocationStartInteger.to_value));
        ("end",
          (Option.map x.end_ ~f:DocumentCharLocationEndInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let end_ =
        (Option.map ~f:DocumentCharLocationEndInteger.of_xml)
          (Xml.child xml_arg0 "end") in
      let start =
        (Option.map ~f:DocumentCharLocationStartInteger.of_xml)
          (Xml.child xml_arg0 "start") in
      let documentIndex =
        (Option.map ~f:DocumentCharLocationDocumentIndexInteger.of_xml)
          (Xml.child xml_arg0 "documentIndex") in
      make ?end_ ?start ?documentIndex ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let end_ =
        field_map json__ "end" DocumentCharLocationEndInteger.of_json in
      let start =
        field_map json__ "start" DocumentCharLocationStartInteger.of_json in
      let documentIndex =
        field_map json__ "documentIndex"
          DocumentCharLocationDocumentIndexInteger.of_json in
      make ?end_ ?start ?documentIndex ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a character-level location within a document, providing precise positioning information for cited content using start and end character indices."]
module DocumentChunkLocation =
  struct
    type nonrec t =
      {
      documentIndex: DocumentChunkLocationDocumentIndexInteger.t option
        [@ocaml.doc
          "The index of the document within the array of documents provided in the request."];
      start: DocumentChunkLocationStartInteger.t option
        [@ocaml.doc
          "The starting chunk identifier or index of the cited content within the document."];
      end_: DocumentChunkLocationEndInteger.t option
        [@ocaml.doc
          "The ending chunk identifier or index of the cited content within the document."]}
    let make ?documentIndex =
      fun ?start -> fun ?end_ -> fun () -> { documentIndex; start; end_ }
    let to_value x =
      structure_to_value
        [("documentIndex",
           (Option.map x.documentIndex
              ~f:DocumentChunkLocationDocumentIndexInteger.to_value));
        ("start",
          (Option.map x.start ~f:DocumentChunkLocationStartInteger.to_value));
        ("end",
          (Option.map x.end_ ~f:DocumentChunkLocationEndInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let end_ =
        (Option.map ~f:DocumentChunkLocationEndInteger.of_xml)
          (Xml.child xml_arg0 "end") in
      let start =
        (Option.map ~f:DocumentChunkLocationStartInteger.of_xml)
          (Xml.child xml_arg0 "start") in
      let documentIndex =
        (Option.map ~f:DocumentChunkLocationDocumentIndexInteger.of_xml)
          (Xml.child xml_arg0 "documentIndex") in
      make ?end_ ?start ?documentIndex ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let end_ =
        field_map json__ "end" DocumentChunkLocationEndInteger.of_json in
      let start =
        field_map json__ "start" DocumentChunkLocationStartInteger.of_json in
      let documentIndex =
        field_map json__ "documentIndex"
          DocumentChunkLocationDocumentIndexInteger.of_json in
      make ?end_ ?start ?documentIndex ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a chunk-level location within a document, providing positioning information for cited content using logical document segments or chunks."]
module DocumentPageLocation =
  struct
    type nonrec t =
      {
      documentIndex: DocumentPageLocationDocumentIndexInteger.t option
        [@ocaml.doc
          "The index of the document within the array of documents provided in the request."];
      start: DocumentPageLocationStartInteger.t option
        [@ocaml.doc
          "The starting page number of the cited content within the document."];
      end_: DocumentPageLocationEndInteger.t option
        [@ocaml.doc
          "The ending page number of the cited content within the document."]}
    let make ?documentIndex =
      fun ?start -> fun ?end_ -> fun () -> { documentIndex; start; end_ }
    let to_value x =
      structure_to_value
        [("documentIndex",
           (Option.map x.documentIndex
              ~f:DocumentPageLocationDocumentIndexInteger.to_value));
        ("start",
          (Option.map x.start ~f:DocumentPageLocationStartInteger.to_value));
        ("end",
          (Option.map x.end_ ~f:DocumentPageLocationEndInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let end_ =
        (Option.map ~f:DocumentPageLocationEndInteger.of_xml)
          (Xml.child xml_arg0 "end") in
      let start =
        (Option.map ~f:DocumentPageLocationStartInteger.of_xml)
          (Xml.child xml_arg0 "start") in
      let documentIndex =
        (Option.map ~f:DocumentPageLocationDocumentIndexInteger.of_xml)
          (Xml.child xml_arg0 "documentIndex") in
      make ?end_ ?start ?documentIndex ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let end_ =
        field_map json__ "end" DocumentPageLocationEndInteger.of_json in
      let start =
        field_map json__ "start" DocumentPageLocationStartInteger.of_json in
      let documentIndex =
        field_map json__ "documentIndex"
          DocumentPageLocationDocumentIndexInteger.of_json in
      make ?end_ ?start ?documentIndex ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a page-level location within a document, providing positioning information for cited content using page numbers."]
module SearchResultLocation =
  struct
    type nonrec t =
      {
      searchResultIndex:
        SearchResultLocationSearchResultIndexInteger.t option
        [@ocaml.doc
          "The index of the search result content block where the cited content is found."];
      start: SearchResultLocationStartInteger.t option
        [@ocaml.doc
          "The starting position in the content array where the cited content begins."];
      end_: SearchResultLocationEndInteger.t option
        [@ocaml.doc
          "The ending position in the content array where the cited content ends."]}
    let make ?searchResultIndex =
      fun ?start -> fun ?end_ -> fun () -> { searchResultIndex; start; end_ }
    let to_value x =
      structure_to_value
        [("searchResultIndex",
           (Option.map x.searchResultIndex
              ~f:SearchResultLocationSearchResultIndexInteger.to_value));
        ("start",
          (Option.map x.start ~f:SearchResultLocationStartInteger.to_value));
        ("end",
          (Option.map x.end_ ~f:SearchResultLocationEndInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let end_ =
        (Option.map ~f:SearchResultLocationEndInteger.of_xml)
          (Xml.child xml_arg0 "end") in
      let start =
        (Option.map ~f:SearchResultLocationStartInteger.of_xml)
          (Xml.child xml_arg0 "start") in
      let searchResultIndex =
        (Option.map ~f:SearchResultLocationSearchResultIndexInteger.of_xml)
          (Xml.child xml_arg0 "searchResultIndex") in
      make ?end_ ?start ?searchResultIndex ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let end_ =
        field_map json__ "end" SearchResultLocationEndInteger.of_json in
      let start =
        field_map json__ "start" SearchResultLocationStartInteger.of_json in
      let searchResultIndex =
        field_map json__ "searchResultIndex"
          SearchResultLocationSearchResultIndexInteger.of_json in
      make ?end_ ?start ?searchResultIndex ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a search result location within the content array, providing positioning information for cited content using search result index and block positions."]
module WebLocation =
  struct
    type nonrec t =
      {
      url: String_.t option
        [@ocaml.doc "The URL that was cited when performing a web search."];
      domain: String_.t option
        [@ocaml.doc
          "The domain that was cited when performing a web search."]}
    let make ?url = fun ?domain -> fun () -> { url; domain }
    let to_value x =
      structure_to_value
        [("url", (Option.map x.url ~f:String_.to_value));
        ("domain", (Option.map x.domain ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "domain") in
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "url") in
      make ?domain ?url ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domain = field_map json__ "domain" String_.of_json in
      let url = field_map json__ "url" String_.of_json in
      make ?domain ?url ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides the URL and domain information for the website that was cited when performing a web search."]
module CitationSourceContent =
  struct
    type nonrec t =
      {
      text: String_.t option
        [@ocaml.doc
          "The text content from the source document that is being cited."]}
    let make ?text = fun () -> { text }
    let to_value x =
      structure_to_value [("text", (Option.map x.text ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" String_.of_json in make ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the actual text content from a source document that is being cited or referenced in the model's response."]
module CitationsConfig =
  struct
    type nonrec t =
      {
      enabled: Boolean.t
        [@ocaml.doc
          "Specifies whether citations from the selected document should be used in the model's response. When set to true, the model can generate citations that reference the source documents used to inform the response."]}
    let context_ = "CitationsConfig"
    let make ~enabled = fun () -> { enabled }
    let to_value x =
      structure_to_value [("enabled", (Some (Boolean.to_value x.enabled)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enabled =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "enabled") in
      make ~enabled ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let enabled = field_map_exn json__ "enabled" Boolean.of_json in
      make ~enabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration settings for enabling and controlling document citations in Converse API responses. When enabled, the model can include citation information that links generated content back to specific source documents."]
module DocumentBlockNameString =
  struct
    type nonrec t = string
    let context_ = "DocumentBlockNameString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:200) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DocumentBlockNameString" j
    let to_json = simple_to_json to_value
  end
module DocumentFormat =
  struct
    type nonrec t =
      | Pdf 
      | Csv 
      | Doc 
      | Docx 
      | Xls 
      | Xlsx 
      | Html 
      | Txt 
      | Md 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Pdf -> "pdf"
      | Csv -> "csv"
      | Doc -> "doc"
      | Docx -> "docx"
      | Xls -> "xls"
      | Xlsx -> "xlsx"
      | Html -> "html"
      | Txt -> "txt"
      | Md -> "md"
      | Non_static_id s -> s
    let of_string =
      function
      | "pdf" -> Pdf
      | "csv" -> Csv
      | "doc" -> Doc
      | "docx" -> Docx
      | "xls" -> Xls
      | "xlsx" -> Xlsx
      | "html" -> Html
      | "txt" -> Txt
      | "md" -> Md
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DocumentFormat" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DocumentFormat" j)
    let to_json = simple_to_json to_value
  end
module DocumentSource =
  struct
    type nonrec t =
      {
      bytes: DocumentSourceBytesBlob.t option
        [@ocaml.doc
          "The raw bytes for the document. If you use an Amazon Web Services SDK, you don't need to encode the bytes in base64."];
      s3Location: S3Location.t option
        [@ocaml.doc
          "The location of a document object in an Amazon S3 bucket. To see which models support S3 uploads, see Supported models and features for Converse."];
      text: String_.t option
        [@ocaml.doc "The text content of the document source."];
      content: DocumentContentBlocks.t option
        [@ocaml.doc
          "The structured content of the document source, which may include various content blocks such as text, images, or other document elements."]}
    let make ?bytes =
      fun ?s3Location ->
        fun ?text ->
          fun ?content -> fun () -> { bytes; s3Location; text; content }
    let to_value x =
      structure_to_value
        [("bytes", (Option.map x.bytes ~f:DocumentSourceBytesBlob.to_value));
        ("s3Location", (Option.map x.s3Location ~f:S3Location.to_value));
        ("text", (Option.map x.text ~f:String_.to_value));
        ("content", (Option.map x.content ~f:DocumentContentBlocks.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let content =
        (Option.map ~f:DocumentContentBlocks.of_xml)
          (Xml.child xml_arg0 "content") in
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      let s3Location =
        (Option.map ~f:S3Location.of_xml) (Xml.child xml_arg0 "s3Location") in
      let bytes =
        (Option.map ~f:DocumentSourceBytesBlob.of_xml)
          (Xml.child xml_arg0 "bytes") in
      make ?content ?text ?s3Location ?bytes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let content = field_map json__ "content" DocumentContentBlocks.of_json in
      let text = field_map json__ "text" String_.of_json in
      let s3Location = field_map json__ "s3Location" S3Location.of_json in
      let bytes = field_map json__ "bytes" DocumentSourceBytesBlob.of_json in
      make ?content ?text ?s3Location ?bytes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the content of a document."]
module ErrorBlock =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "A human-readable error message describing what went wrong during content processing."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A block containing error information when content processing fails."]
module ImageFormat =
  struct
    type nonrec t =
      | Png 
      | Jpeg 
      | Gif 
      | Webp 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Png -> "png"
      | Jpeg -> "jpeg"
      | Gif -> "gif"
      | Webp -> "webp"
      | Non_static_id s -> s
    let of_string =
      function
      | "png" -> Png
      | "jpeg" -> Jpeg
      | "gif" -> Gif
      | "webp" -> Webp
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ImageFormat" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ImageFormat" j)
    let to_json = simple_to_json to_value
  end
module ImageSource =
  struct
    type nonrec t =
      {
      bytes: ImageSourceBytesBlob.t option
        [@ocaml.doc
          "The raw image bytes for the image. If you use an AWS SDK, you don't need to encode the image bytes in base64."];
      s3Location: S3Location.t option
        [@ocaml.doc
          "The location of an image object in an Amazon S3 bucket. To see which models support S3 uploads, see Supported models and features for Converse."]}
    let make ?bytes = fun ?s3Location -> fun () -> { bytes; s3Location }
    let to_value x =
      structure_to_value
        [("bytes", (Option.map x.bytes ~f:ImageSourceBytesBlob.to_value));
        ("s3Location", (Option.map x.s3Location ~f:S3Location.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Location =
        (Option.map ~f:S3Location.of_xml) (Xml.child xml_arg0 "s3Location") in
      let bytes =
        (Option.map ~f:ImageSourceBytesBlob.of_xml)
          (Xml.child xml_arg0 "bytes") in
      make ?s3Location ?bytes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Location = field_map json__ "s3Location" S3Location.of_json in
      let bytes = field_map json__ "bytes" ImageSourceBytesBlob.of_json in
      make ?s3Location ?bytes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The source for an image."]
module SearchResultContentBlocks =
  struct
    type nonrec t = SearchResultContentBlock.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SearchResultContentBlock.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SearchResultContentBlock.of_xml)
    let of_json j =
      list_of_json ~kind:"SearchResultContentBlocks"
        ~of_json:SearchResultContentBlock.of_json j
    let to_json v = composed_to_json to_value v
  end
module VideoFormat =
  struct
    type nonrec t =
      | Mkv 
      | Mov 
      | Mp4 
      | Webm 
      | Flv 
      | Mpeg 
      | Mpg 
      | Wmv 
      | Three_gp 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Mkv -> "mkv"
      | Mov -> "mov"
      | Mp4 -> "mp4"
      | Webm -> "webm"
      | Flv -> "flv"
      | Mpeg -> "mpeg"
      | Mpg -> "mpg"
      | Wmv -> "wmv"
      | Three_gp -> "three_gp"
      | Non_static_id s -> s
    let of_string =
      function
      | "mkv" -> Mkv
      | "mov" -> Mov
      | "mp4" -> Mp4
      | "webm" -> Webm
      | "flv" -> Flv
      | "mpeg" -> Mpeg
      | "mpg" -> Mpg
      | "wmv" -> Wmv
      | "three_gp" -> Three_gp
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration VideoFormat" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"VideoFormat" j)
    let to_json = simple_to_json to_value
  end
module VideoSource =
  struct
    type nonrec t =
      {
      bytes: VideoSourceBytesBlob.t option
        [@ocaml.doc "Video content encoded in base64."];
      s3Location: S3Location.t option
        [@ocaml.doc
          "The location of a video object in an Amazon S3 bucket. To see which models support S3 uploads, see Supported models and features for Converse."]}
    let make ?bytes = fun ?s3Location -> fun () -> { bytes; s3Location }
    let to_value x =
      structure_to_value
        [("bytes", (Option.map x.bytes ~f:VideoSourceBytesBlob.to_value));
        ("s3Location", (Option.map x.s3Location ~f:S3Location.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Location =
        (Option.map ~f:S3Location.of_xml) (Xml.child xml_arg0 "s3Location") in
      let bytes =
        (Option.map ~f:VideoSourceBytesBlob.of_xml)
          (Xml.child xml_arg0 "bytes") in
      make ?s3Location ?bytes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Location = field_map json__ "s3Location" S3Location.of_json in
      let bytes = field_map json__ "bytes" VideoSourceBytesBlob.of_json in
      make ?s3Location ?bytes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A video source. You can upload a smaller video as a base64-encoded string as long as the encoded file is less than 25MB. You can also transfer videos up to 1GB in size from an S3 bucket."]
module GuardrailAutomatedReasoningImpossibleFinding =
  struct
    type nonrec t =
      {
      translation: GuardrailAutomatedReasoningTranslation.t option
        [@ocaml.doc
          "The logical translation of the input that this finding evaluates."];
      contradictingRules: GuardrailAutomatedReasoningRuleList.t option
        [@ocaml.doc
          "The automated reasoning policy rules that contradict the claims and/or premises in the input."];
      logicWarning: GuardrailAutomatedReasoningLogicWarning.t option
        [@ocaml.doc
          "Indication of a logic issue with the translation without needing to consider the automated reasoning policy rules."]}
    let make ?translation =
      fun ?contradictingRules ->
        fun ?logicWarning ->
          fun () -> { translation; contradictingRules; logicWarning }
    let to_value x =
      structure_to_value
        [("translation",
           (Option.map x.translation
              ~f:GuardrailAutomatedReasoningTranslation.to_value));
        ("contradictingRules",
          (Option.map x.contradictingRules
             ~f:GuardrailAutomatedReasoningRuleList.to_value));
        ("logicWarning",
          (Option.map x.logicWarning
             ~f:GuardrailAutomatedReasoningLogicWarning.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logicWarning =
        (Option.map ~f:GuardrailAutomatedReasoningLogicWarning.of_xml)
          (Xml.child xml_arg0 "logicWarning") in
      let contradictingRules =
        (Option.map ~f:GuardrailAutomatedReasoningRuleList.of_xml)
          (Xml.child xml_arg0 "contradictingRules") in
      let translation =
        (Option.map ~f:GuardrailAutomatedReasoningTranslation.of_xml)
          (Xml.child xml_arg0 "translation") in
      make ?logicWarning ?contradictingRules ?translation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logicWarning =
        field_map json__ "logicWarning"
          GuardrailAutomatedReasoningLogicWarning.of_json in
      let contradictingRules =
        field_map json__ "contradictingRules"
          GuardrailAutomatedReasoningRuleList.of_json in
      let translation =
        field_map json__ "translation"
          GuardrailAutomatedReasoningTranslation.of_json in
      make ?logicWarning ?contradictingRules ?translation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that no valid claims can be made due to logical contradictions in the premises or rules."]
module GuardrailAutomatedReasoningInvalidFinding =
  struct
    type nonrec t =
      {
      translation: GuardrailAutomatedReasoningTranslation.t option
        [@ocaml.doc
          "The logical translation of the input that this finding invalidates."];
      contradictingRules: GuardrailAutomatedReasoningRuleList.t option
        [@ocaml.doc
          "The automated reasoning policy rules that contradict the claims in the input."];
      logicWarning: GuardrailAutomatedReasoningLogicWarning.t option
        [@ocaml.doc
          "Indication of a logic issue with the translation without needing to consider the automated reasoning policy rules."]}
    let make ?translation =
      fun ?contradictingRules ->
        fun ?logicWarning ->
          fun () -> { translation; contradictingRules; logicWarning }
    let to_value x =
      structure_to_value
        [("translation",
           (Option.map x.translation
              ~f:GuardrailAutomatedReasoningTranslation.to_value));
        ("contradictingRules",
          (Option.map x.contradictingRules
             ~f:GuardrailAutomatedReasoningRuleList.to_value));
        ("logicWarning",
          (Option.map x.logicWarning
             ~f:GuardrailAutomatedReasoningLogicWarning.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logicWarning =
        (Option.map ~f:GuardrailAutomatedReasoningLogicWarning.of_xml)
          (Xml.child xml_arg0 "logicWarning") in
      let contradictingRules =
        (Option.map ~f:GuardrailAutomatedReasoningRuleList.of_xml)
          (Xml.child xml_arg0 "contradictingRules") in
      let translation =
        (Option.map ~f:GuardrailAutomatedReasoningTranslation.of_xml)
          (Xml.child xml_arg0 "translation") in
      make ?logicWarning ?contradictingRules ?translation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logicWarning =
        field_map json__ "logicWarning"
          GuardrailAutomatedReasoningLogicWarning.of_json in
      let contradictingRules =
        field_map json__ "contradictingRules"
          GuardrailAutomatedReasoningRuleList.of_json in
      let translation =
        field_map json__ "translation"
          GuardrailAutomatedReasoningTranslation.of_json in
      make ?logicWarning ?contradictingRules ?translation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the claims are logically false and contradictory to the established rules or premises."]
module GuardrailAutomatedReasoningNoTranslationsFinding =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that no relevant logical information could be extracted from the input for validation."]
module GuardrailAutomatedReasoningSatisfiableFinding =
  struct
    type nonrec t =
      {
      translation: GuardrailAutomatedReasoningTranslation.t option
        [@ocaml.doc
          "The logical translation of the input that this finding evaluates."];
      claimsTrueScenario: GuardrailAutomatedReasoningScenario.t option
        [@ocaml.doc
          "An example scenario demonstrating how the claims could be logically true."];
      claimsFalseScenario: GuardrailAutomatedReasoningScenario.t option
        [@ocaml.doc
          "An example scenario demonstrating how the claims could be logically false."];
      logicWarning: GuardrailAutomatedReasoningLogicWarning.t option
        [@ocaml.doc
          "Indication of a logic issue with the translation without needing to consider the automated reasoning policy rules."]}
    let make ?translation =
      fun ?claimsTrueScenario ->
        fun ?claimsFalseScenario ->
          fun ?logicWarning ->
            fun () ->
              {
                translation;
                claimsTrueScenario;
                claimsFalseScenario;
                logicWarning
              }
    let to_value x =
      structure_to_value
        [("translation",
           (Option.map x.translation
              ~f:GuardrailAutomatedReasoningTranslation.to_value));
        ("claimsTrueScenario",
          (Option.map x.claimsTrueScenario
             ~f:GuardrailAutomatedReasoningScenario.to_value));
        ("claimsFalseScenario",
          (Option.map x.claimsFalseScenario
             ~f:GuardrailAutomatedReasoningScenario.to_value));
        ("logicWarning",
          (Option.map x.logicWarning
             ~f:GuardrailAutomatedReasoningLogicWarning.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logicWarning =
        (Option.map ~f:GuardrailAutomatedReasoningLogicWarning.of_xml)
          (Xml.child xml_arg0 "logicWarning") in
      let claimsFalseScenario =
        (Option.map ~f:GuardrailAutomatedReasoningScenario.of_xml)
          (Xml.child xml_arg0 "claimsFalseScenario") in
      let claimsTrueScenario =
        (Option.map ~f:GuardrailAutomatedReasoningScenario.of_xml)
          (Xml.child xml_arg0 "claimsTrueScenario") in
      let translation =
        (Option.map ~f:GuardrailAutomatedReasoningTranslation.of_xml)
          (Xml.child xml_arg0 "translation") in
      make ?logicWarning ?claimsFalseScenario ?claimsTrueScenario
        ?translation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logicWarning =
        field_map json__ "logicWarning"
          GuardrailAutomatedReasoningLogicWarning.of_json in
      let claimsFalseScenario =
        field_map json__ "claimsFalseScenario"
          GuardrailAutomatedReasoningScenario.of_json in
      let claimsTrueScenario =
        field_map json__ "claimsTrueScenario"
          GuardrailAutomatedReasoningScenario.of_json in
      let translation =
        field_map json__ "translation"
          GuardrailAutomatedReasoningTranslation.of_json in
      make ?logicWarning ?claimsFalseScenario ?claimsTrueScenario
        ?translation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the claims could be either true or false depending on additional assumptions not provided in the input."]
module GuardrailAutomatedReasoningTooComplexFinding =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the input exceeds the processing capacity due to the volume or complexity of the logical information."]
module GuardrailAutomatedReasoningTranslationAmbiguousFinding =
  struct
    type nonrec t =
      {
      options: GuardrailAutomatedReasoningTranslationOptionList.t option
        [@ocaml.doc
          "Different logical interpretations that were detected during translation of the input."];
      differenceScenarios:
        GuardrailAutomatedReasoningDifferenceScenarioList.t option
        [@ocaml.doc
          "Scenarios showing how the different translation options differ in meaning."]}
    let make ?options =
      fun ?differenceScenarios -> fun () -> { options; differenceScenarios }
    let to_value x =
      structure_to_value
        [("options",
           (Option.map x.options
              ~f:GuardrailAutomatedReasoningTranslationOptionList.to_value));
        ("differenceScenarios",
          (Option.map x.differenceScenarios
             ~f:GuardrailAutomatedReasoningDifferenceScenarioList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let differenceScenarios =
        (Option.map
           ~f:GuardrailAutomatedReasoningDifferenceScenarioList.of_xml)
          (Xml.child xml_arg0 "differenceScenarios") in
      let options =
        (Option.map
           ~f:GuardrailAutomatedReasoningTranslationOptionList.of_xml)
          (Xml.child xml_arg0 "options") in
      make ?differenceScenarios ?options ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let differenceScenarios =
        field_map json__ "differenceScenarios"
          GuardrailAutomatedReasoningDifferenceScenarioList.of_json in
      let options =
        field_map json__ "options"
          GuardrailAutomatedReasoningTranslationOptionList.of_json in
      make ?differenceScenarios ?options ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the input has multiple valid logical interpretations, requiring additional context or clarification."]
module GuardrailAutomatedReasoningValidFinding =
  struct
    type nonrec t =
      {
      translation: GuardrailAutomatedReasoningTranslation.t option
        [@ocaml.doc
          "The logical translation of the input that this finding validates."];
      claimsTrueScenario: GuardrailAutomatedReasoningScenario.t option
        [@ocaml.doc
          "An example scenario demonstrating how the claims are logically true."];
      supportingRules: GuardrailAutomatedReasoningRuleList.t option
        [@ocaml.doc
          "The automated reasoning policy rules that support why this result is considered valid."];
      logicWarning: GuardrailAutomatedReasoningLogicWarning.t option
        [@ocaml.doc
          "Indication of a logic issue with the translation without needing to consider the automated reasoning policy rules."]}
    let make ?translation =
      fun ?claimsTrueScenario ->
        fun ?supportingRules ->
          fun ?logicWarning ->
            fun () ->
              {
                translation;
                claimsTrueScenario;
                supportingRules;
                logicWarning
              }
    let to_value x =
      structure_to_value
        [("translation",
           (Option.map x.translation
              ~f:GuardrailAutomatedReasoningTranslation.to_value));
        ("claimsTrueScenario",
          (Option.map x.claimsTrueScenario
             ~f:GuardrailAutomatedReasoningScenario.to_value));
        ("supportingRules",
          (Option.map x.supportingRules
             ~f:GuardrailAutomatedReasoningRuleList.to_value));
        ("logicWarning",
          (Option.map x.logicWarning
             ~f:GuardrailAutomatedReasoningLogicWarning.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logicWarning =
        (Option.map ~f:GuardrailAutomatedReasoningLogicWarning.of_xml)
          (Xml.child xml_arg0 "logicWarning") in
      let supportingRules =
        (Option.map ~f:GuardrailAutomatedReasoningRuleList.of_xml)
          (Xml.child xml_arg0 "supportingRules") in
      let claimsTrueScenario =
        (Option.map ~f:GuardrailAutomatedReasoningScenario.of_xml)
          (Xml.child xml_arg0 "claimsTrueScenario") in
      let translation =
        (Option.map ~f:GuardrailAutomatedReasoningTranslation.of_xml)
          (Xml.child xml_arg0 "translation") in
      make ?logicWarning ?supportingRules ?claimsTrueScenario ?translation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logicWarning =
        field_map json__ "logicWarning"
          GuardrailAutomatedReasoningLogicWarning.of_json in
      let supportingRules =
        field_map json__ "supportingRules"
          GuardrailAutomatedReasoningRuleList.of_json in
      let claimsTrueScenario =
        field_map json__ "claimsTrueScenario"
          GuardrailAutomatedReasoningScenario.of_json in
      let translation =
        field_map json__ "translation"
          GuardrailAutomatedReasoningTranslation.of_json in
      make ?logicWarning ?supportingRules ?claimsTrueScenario ?translation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that the claims are definitively true and logically implied by the premises, with no possible alternative interpretations."]
module GuardrailContentFilterConfidence =
  struct
    type nonrec t =
      | NONE 
      | LOW 
      | MEDIUM 
      | HIGH 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NONE -> "NONE"
      | LOW -> "LOW"
      | MEDIUM -> "MEDIUM"
      | HIGH -> "HIGH"
      | Non_static_id s -> s
    let of_string =
      function
      | "NONE" -> NONE
      | "LOW" -> LOW
      | "MEDIUM" -> MEDIUM
      | "HIGH" -> HIGH
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailContentFilterConfidence"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailContentFilterConfidence" j)
    let to_json = simple_to_json to_value
  end
module GuardrailContentFilterStrength =
  struct
    type nonrec t =
      | NONE 
      | LOW 
      | MEDIUM 
      | HIGH 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NONE -> "NONE"
      | LOW -> "LOW"
      | MEDIUM -> "MEDIUM"
      | HIGH -> "HIGH"
      | Non_static_id s -> s
    let of_string =
      function
      | "NONE" -> NONE
      | "LOW" -> LOW
      | "MEDIUM" -> MEDIUM
      | "HIGH" -> HIGH
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailContentFilterStrength"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailContentFilterStrength" j)
    let to_json = simple_to_json to_value
  end
module GuardrailContentFilterType =
  struct
    type nonrec t =
      | INSULTS 
      | HATE 
      | SEXUAL 
      | VIOLENCE 
      | MISCONDUCT 
      | PROMPT_ATTACK 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INSULTS -> "INSULTS"
      | HATE -> "HATE"
      | SEXUAL -> "SEXUAL"
      | VIOLENCE -> "VIOLENCE"
      | MISCONDUCT -> "MISCONDUCT"
      | PROMPT_ATTACK -> "PROMPT_ATTACK"
      | Non_static_id s -> s
    let of_string =
      function
      | "INSULTS" -> INSULTS
      | "HATE" -> HATE
      | "SEXUAL" -> SEXUAL
      | "VIOLENCE" -> VIOLENCE
      | "MISCONDUCT" -> MISCONDUCT
      | "PROMPT_ATTACK" -> PROMPT_ATTACK
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailContentFilterType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailContentFilterType" j)
    let to_json = simple_to_json to_value
  end
module GuardrailContentPolicyAction =
  struct
    type nonrec t =
      | BLOCKED 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | BLOCKED -> "BLOCKED" | NONE -> "NONE" | Non_static_id s -> s
    let of_string =
      function | "BLOCKED" -> BLOCKED | "NONE" -> NONE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailContentPolicyAction"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailContentPolicyAction" j)
    let to_json = simple_to_json to_value
  end
module GuardrailContextualGroundingFilterScoreDouble =
  struct
    type nonrec t = float
    let make i =
      let open Result in
        ok_or_failwith
          ((check_float_min i ~min:1.) >>=
             (fun () -> check_float_min i ~min:0.));
        i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module GuardrailContextualGroundingFilterThresholdDouble =
  struct
    type nonrec t = float
    let make i =
      let open Result in
        ok_or_failwith
          ((check_float_min i ~min:1.) >>=
             (fun () -> check_float_min i ~min:0.));
        i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module GuardrailContextualGroundingFilterType =
  struct
    type nonrec t =
      | GROUNDING 
      | RELEVANCE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GROUNDING -> "GROUNDING"
      | RELEVANCE -> "RELEVANCE"
      | Non_static_id s -> s
    let of_string =
      function
      | "GROUNDING" -> GROUNDING
      | "RELEVANCE" -> RELEVANCE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration GuardrailContextualGroundingFilterType"
           xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"GuardrailContextualGroundingFilterType" j)
    let to_json = simple_to_json to_value
  end
module GuardrailContextualGroundingPolicyAction =
  struct
    type nonrec t =
      | BLOCKED 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | BLOCKED -> "BLOCKED" | NONE -> "NONE" | Non_static_id s -> s
    let of_string =
      function | "BLOCKED" -> BLOCKED | "NONE" -> NONE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration GuardrailContextualGroundingPolicyAction"
           xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"GuardrailContextualGroundingPolicyAction" j)
    let to_json = simple_to_json to_value
  end
module ImagesGuarded =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ImagesGuarded" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ImagesTotal =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ImagesTotal" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TextCharactersGuarded =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for TextCharactersGuarded" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TextCharactersTotal =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for TextCharactersTotal" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GuardrailPiiEntityType =
  struct
    type nonrec t =
      | ADDRESS 
      | AGE 
      | AWS_ACCESS_KEY 
      | AWS_SECRET_KEY 
      | CA_HEALTH_NUMBER 
      | CA_SOCIAL_INSURANCE_NUMBER 
      | CREDIT_DEBIT_CARD_CVV 
      | CREDIT_DEBIT_CARD_EXPIRY 
      | CREDIT_DEBIT_CARD_NUMBER 
      | DRIVER_ID 
      | EMAIL 
      | INTERNATIONAL_BANK_ACCOUNT_NUMBER 
      | IP_ADDRESS 
      | LICENSE_PLATE 
      | MAC_ADDRESS 
      | NAME 
      | PASSWORD 
      | PHONE 
      | PIN 
      | SWIFT_CODE 
      | UK_NATIONAL_HEALTH_SERVICE_NUMBER 
      | UK_NATIONAL_INSURANCE_NUMBER 
      | UK_UNIQUE_TAXPAYER_REFERENCE_NUMBER 
      | URL 
      | USERNAME 
      | US_BANK_ACCOUNT_NUMBER 
      | US_BANK_ROUTING_NUMBER 
      | US_INDIVIDUAL_TAX_IDENTIFICATION_NUMBER 
      | US_PASSPORT_NUMBER 
      | US_SOCIAL_SECURITY_NUMBER 
      | VEHICLE_IDENTIFICATION_NUMBER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ADDRESS -> "ADDRESS"
      | AGE -> "AGE"
      | AWS_ACCESS_KEY -> "AWS_ACCESS_KEY"
      | AWS_SECRET_KEY -> "AWS_SECRET_KEY"
      | CA_HEALTH_NUMBER -> "CA_HEALTH_NUMBER"
      | CA_SOCIAL_INSURANCE_NUMBER -> "CA_SOCIAL_INSURANCE_NUMBER"
      | CREDIT_DEBIT_CARD_CVV -> "CREDIT_DEBIT_CARD_CVV"
      | CREDIT_DEBIT_CARD_EXPIRY -> "CREDIT_DEBIT_CARD_EXPIRY"
      | CREDIT_DEBIT_CARD_NUMBER -> "CREDIT_DEBIT_CARD_NUMBER"
      | DRIVER_ID -> "DRIVER_ID"
      | EMAIL -> "EMAIL"
      | INTERNATIONAL_BANK_ACCOUNT_NUMBER ->
          "INTERNATIONAL_BANK_ACCOUNT_NUMBER"
      | IP_ADDRESS -> "IP_ADDRESS"
      | LICENSE_PLATE -> "LICENSE_PLATE"
      | MAC_ADDRESS -> "MAC_ADDRESS"
      | NAME -> "NAME"
      | PASSWORD -> "PASSWORD"
      | PHONE -> "PHONE"
      | PIN -> "PIN"
      | SWIFT_CODE -> "SWIFT_CODE"
      | UK_NATIONAL_HEALTH_SERVICE_NUMBER ->
          "UK_NATIONAL_HEALTH_SERVICE_NUMBER"
      | UK_NATIONAL_INSURANCE_NUMBER -> "UK_NATIONAL_INSURANCE_NUMBER"
      | UK_UNIQUE_TAXPAYER_REFERENCE_NUMBER ->
          "UK_UNIQUE_TAXPAYER_REFERENCE_NUMBER"
      | URL -> "URL"
      | USERNAME -> "USERNAME"
      | US_BANK_ACCOUNT_NUMBER -> "US_BANK_ACCOUNT_NUMBER"
      | US_BANK_ROUTING_NUMBER -> "US_BANK_ROUTING_NUMBER"
      | US_INDIVIDUAL_TAX_IDENTIFICATION_NUMBER ->
          "US_INDIVIDUAL_TAX_IDENTIFICATION_NUMBER"
      | US_PASSPORT_NUMBER -> "US_PASSPORT_NUMBER"
      | US_SOCIAL_SECURITY_NUMBER -> "US_SOCIAL_SECURITY_NUMBER"
      | VEHICLE_IDENTIFICATION_NUMBER -> "VEHICLE_IDENTIFICATION_NUMBER"
      | Non_static_id s -> s
    let of_string =
      function
      | "ADDRESS" -> ADDRESS
      | "AGE" -> AGE
      | "AWS_ACCESS_KEY" -> AWS_ACCESS_KEY
      | "AWS_SECRET_KEY" -> AWS_SECRET_KEY
      | "CA_HEALTH_NUMBER" -> CA_HEALTH_NUMBER
      | "CA_SOCIAL_INSURANCE_NUMBER" -> CA_SOCIAL_INSURANCE_NUMBER
      | "CREDIT_DEBIT_CARD_CVV" -> CREDIT_DEBIT_CARD_CVV
      | "CREDIT_DEBIT_CARD_EXPIRY" -> CREDIT_DEBIT_CARD_EXPIRY
      | "CREDIT_DEBIT_CARD_NUMBER" -> CREDIT_DEBIT_CARD_NUMBER
      | "DRIVER_ID" -> DRIVER_ID
      | "EMAIL" -> EMAIL
      | "INTERNATIONAL_BANK_ACCOUNT_NUMBER" ->
          INTERNATIONAL_BANK_ACCOUNT_NUMBER
      | "IP_ADDRESS" -> IP_ADDRESS
      | "LICENSE_PLATE" -> LICENSE_PLATE
      | "MAC_ADDRESS" -> MAC_ADDRESS
      | "NAME" -> NAME
      | "PASSWORD" -> PASSWORD
      | "PHONE" -> PHONE
      | "PIN" -> PIN
      | "SWIFT_CODE" -> SWIFT_CODE
      | "UK_NATIONAL_HEALTH_SERVICE_NUMBER" ->
          UK_NATIONAL_HEALTH_SERVICE_NUMBER
      | "UK_NATIONAL_INSURANCE_NUMBER" -> UK_NATIONAL_INSURANCE_NUMBER
      | "UK_UNIQUE_TAXPAYER_REFERENCE_NUMBER" ->
          UK_UNIQUE_TAXPAYER_REFERENCE_NUMBER
      | "URL" -> URL
      | "USERNAME" -> USERNAME
      | "US_BANK_ACCOUNT_NUMBER" -> US_BANK_ACCOUNT_NUMBER
      | "US_BANK_ROUTING_NUMBER" -> US_BANK_ROUTING_NUMBER
      | "US_INDIVIDUAL_TAX_IDENTIFICATION_NUMBER" ->
          US_INDIVIDUAL_TAX_IDENTIFICATION_NUMBER
      | "US_PASSPORT_NUMBER" -> US_PASSPORT_NUMBER
      | "US_SOCIAL_SECURITY_NUMBER" -> US_SOCIAL_SECURITY_NUMBER
      | "VEHICLE_IDENTIFICATION_NUMBER" -> VEHICLE_IDENTIFICATION_NUMBER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailPiiEntityType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailPiiEntityType" j)
    let to_json = simple_to_json to_value
  end
module GuardrailSensitiveInformationPolicyAction =
  struct
    type nonrec t =
      | ANONYMIZED 
      | BLOCKED 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ANONYMIZED -> "ANONYMIZED"
      | BLOCKED -> "BLOCKED"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ANONYMIZED" -> ANONYMIZED
      | "BLOCKED" -> BLOCKED
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration GuardrailSensitiveInformationPolicyAction"
           xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"GuardrailSensitiveInformationPolicyAction" j)
    let to_json = simple_to_json to_value
  end
module GuardrailTopicPolicyAction =
  struct
    type nonrec t =
      | BLOCKED 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | BLOCKED -> "BLOCKED" | NONE -> "NONE" | Non_static_id s -> s
    let of_string =
      function | "BLOCKED" -> BLOCKED | "NONE" -> NONE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailTopicPolicyAction"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailTopicPolicyAction" j)
    let to_json = simple_to_json to_value
  end
module GuardrailTopicType =
  struct
    type nonrec t =
      | DENY 
      | Non_static_id of string 
    let make i = i
    let to_string = function | DENY -> "DENY" | Non_static_id s -> s
    let of_string = function | "DENY" -> DENY | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailTopicType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GuardrailTopicType" j)
    let to_json = simple_to_json to_value
  end
module GuardrailWordPolicyAction =
  struct
    type nonrec t =
      | BLOCKED 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | BLOCKED -> "BLOCKED" | NONE -> "NONE" | Non_static_id s -> s
    let of_string =
      function | "BLOCKED" -> BLOCKED | "NONE" -> NONE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailWordPolicyAction" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailWordPolicyAction" j)
    let to_json = simple_to_json to_value
  end
module GuardrailManagedWordType =
  struct
    type nonrec t =
      | PROFANITY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | PROFANITY -> "PROFANITY" | Non_static_id s -> s
    let of_string =
      function | "PROFANITY" -> PROFANITY | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailManagedWordType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailManagedWordType" j)
    let to_json = simple_to_json to_value
  end
module CitationLocation =
  struct
    type nonrec t =
      {
      web: WebLocation.t option
        [@ocaml.doc "The web URL that was cited for this reference."];
      documentChar: DocumentCharLocation.t option
        [@ocaml.doc
          "The character-level location within the document where the cited content is found."];
      documentPage: DocumentPageLocation.t option
        [@ocaml.doc
          "The page-level location within the document where the cited content is found."];
      documentChunk: DocumentChunkLocation.t option
        [@ocaml.doc
          "The chunk-level location within the document where the cited content is found, typically used for documents that have been segmented into logical chunks."];
      searchResultLocation: SearchResultLocation.t option
        [@ocaml.doc
          "The search result location where the cited content is found, including the search result index and block positions within the content array."]}
    let make ?web =
      fun ?documentChar ->
        fun ?documentPage ->
          fun ?documentChunk ->
            fun ?searchResultLocation ->
              fun () ->
                {
                  web;
                  documentChar;
                  documentPage;
                  documentChunk;
                  searchResultLocation
                }
    let to_value x =
      structure_to_value
        [("web", (Option.map x.web ~f:WebLocation.to_value));
        ("documentChar",
          (Option.map x.documentChar ~f:DocumentCharLocation.to_value));
        ("documentPage",
          (Option.map x.documentPage ~f:DocumentPageLocation.to_value));
        ("documentChunk",
          (Option.map x.documentChunk ~f:DocumentChunkLocation.to_value));
        ("searchResultLocation",
          (Option.map x.searchResultLocation ~f:SearchResultLocation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let searchResultLocation =
        (Option.map ~f:SearchResultLocation.of_xml)
          (Xml.child xml_arg0 "searchResultLocation") in
      let documentChunk =
        (Option.map ~f:DocumentChunkLocation.of_xml)
          (Xml.child xml_arg0 "documentChunk") in
      let documentPage =
        (Option.map ~f:DocumentPageLocation.of_xml)
          (Xml.child xml_arg0 "documentPage") in
      let documentChar =
        (Option.map ~f:DocumentCharLocation.of_xml)
          (Xml.child xml_arg0 "documentChar") in
      let web = (Option.map ~f:WebLocation.of_xml) (Xml.child xml_arg0 "web") in
      make ?searchResultLocation ?documentChunk ?documentPage ?documentChar
        ?web ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let searchResultLocation =
        field_map json__ "searchResultLocation" SearchResultLocation.of_json in
      let documentChunk =
        field_map json__ "documentChunk" DocumentChunkLocation.of_json in
      let documentPage =
        field_map json__ "documentPage" DocumentPageLocation.of_json in
      let documentChar =
        field_map json__ "documentChar" DocumentCharLocation.of_json in
      let web = field_map json__ "web" WebLocation.of_json in
      make ?searchResultLocation ?documentChunk ?documentPage ?documentChar
        ?web ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the precise location within a source document where cited content can be found. This can include character-level positions, page numbers, or document chunks depending on the document type and indexing method."]
module CitationSourceContentList =
  struct
    type nonrec t = CitationSourceContent.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CitationSourceContent.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CitationSourceContent.of_xml)
    let of_json j =
      list_of_json ~kind:"CitationSourceContentList"
        ~of_json:CitationSourceContent.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailConverseImageSourceBytesBlob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module GuardrailConverseContentQualifier =
  struct
    type nonrec t =
      | Grounding_source 
      | Query 
      | Guard_content 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Grounding_source -> "grounding_source"
      | Query -> "query"
      | Guard_content -> "guard_content"
      | Non_static_id s -> s
    let of_string =
      function
      | "grounding_source" -> Grounding_source
      | "query" -> Query
      | "guard_content" -> Guard_content
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailConverseContentQualifier"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailConverseContentQualifier" j)
    let to_json = simple_to_json to_value
  end
module Document =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DocumentBlock =
  struct
    type nonrec t =
      {
      format: DocumentFormat.t option
        [@ocaml.doc "The format of a document, or its extension."];
      name: DocumentBlockNameString.t
        [@ocaml.doc
          "A name for the document. The name can only contain the following characters: Alphanumeric characters Whitespace characters (no more than one in a row) Hyphens Parentheses Square brackets This field is vulnerable to prompt injections, because the model might inadvertently interpret it as instructions. Therefore, we recommend that you specify a neutral name."];
      source: DocumentSource.t
        [@ocaml.doc "Contains the content of the document."];
      context: String_.t option
        [@ocaml.doc
          "Contextual information about how the document should be processed or interpreted by the model when generating citations."];
      citations: CitationsConfig.t option
        [@ocaml.doc
          "Configuration settings that control how citations should be generated for this specific document."]}
    let context_ = "DocumentBlock"
    let make ?format =
      fun ?context ->
        fun ?citations ->
          fun ~name ->
            fun ~source ->
              fun () -> { format; context; citations; name; source }
    let to_value x =
      structure_to_value
        [("format", (Option.map x.format ~f:DocumentFormat.to_value));
        ("name", (Some (DocumentBlockNameString.to_value x.name)));
        ("source", (Some (DocumentSource.to_value x.source)));
        ("context", (Option.map x.context ~f:String_.to_value));
        ("citations", (Option.map x.citations ~f:CitationsConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let citations =
        (Option.map ~f:CitationsConfig.of_xml)
          (Xml.child xml_arg0 "citations") in
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "context") in
      let source =
        DocumentSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "source") in
      let name =
        DocumentBlockNameString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let format =
        (Option.map ~f:DocumentFormat.of_xml) (Xml.child xml_arg0 "format") in
      make ?citations ?context ~source ~name ?format ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let citations = field_map json__ "citations" CitationsConfig.of_json in
      let context = field_map json__ "context" String_.of_json in
      let source = field_map_exn json__ "source" DocumentSource.of_json in
      let name = field_map_exn json__ "name" DocumentBlockNameString.of_json in
      let format = field_map json__ "format" DocumentFormat.of_json in
      make ?citations ?context ~source ~name ?format ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A document to include in a message."]
module ImageBlock =
  struct
    type nonrec t =
      {
      format: ImageFormat.t [@ocaml.doc "The format of the image."];
      source: ImageSource.t [@ocaml.doc "The source for the image."];
      error: ErrorBlock.t option
        [@ocaml.doc
          "Error information if the image block could not be processed or contains invalid data."]}
    let context_ = "ImageBlock"
    let make ?error =
      fun ~format -> fun ~source -> fun () -> { error; format; source }
    let to_value x =
      structure_to_value
        [("format", (Some (ImageFormat.to_value x.format)));
        ("source", (Some (ImageSource.to_value x.source)));
        ("error", (Option.map x.error ~f:ErrorBlock.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let error =
        (Option.map ~f:ErrorBlock.of_xml) (Xml.child xml_arg0 "error") in
      let source =
        ImageSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "source") in
      let format =
        ImageFormat.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "format") in
      make ?error ~source ~format ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let error = field_map json__ "error" ErrorBlock.of_json in
      let source = field_map_exn json__ "source" ImageSource.of_json in
      let format = field_map_exn json__ "format" ImageFormat.of_json in
      make ?error ~source ~format ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Image content for a message."]
module SearchResultBlock =
  struct
    type nonrec t =
      {
      source: String_.t
        [@ocaml.doc "The source URL or identifier for the content."];
      title: String_.t
        [@ocaml.doc "A descriptive title for the search result."];
      content: SearchResultContentBlocks.t
        [@ocaml.doc "An array of search result content block."];
      citations: CitationsConfig.t option
        [@ocaml.doc "Configuration setting for citations"]}
    let context_ = "SearchResultBlock"
    let make ?citations =
      fun ~source ->
        fun ~title ->
          fun ~content -> fun () -> { citations; source; title; content }
    let to_value x =
      structure_to_value
        [("source", (Some (String_.to_value x.source)));
        ("title", (Some (String_.to_value x.title)));
        ("content", (Some (SearchResultContentBlocks.to_value x.content)));
        ("citations", (Option.map x.citations ~f:CitationsConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let citations =
        (Option.map ~f:CitationsConfig.of_xml)
          (Xml.child xml_arg0 "citations") in
      let content =
        SearchResultContentBlocks.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "content") in
      let title =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "title") in
      let source =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "source") in
      make ?citations ~content ~title ~source ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let citations = field_map json__ "citations" CitationsConfig.of_json in
      let content =
        field_map_exn json__ "content" SearchResultContentBlocks.of_json in
      let title = field_map_exn json__ "title" String_.of_json in
      let source = field_map_exn json__ "source" String_.of_json in
      make ?citations ~content ~title ~source ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A search result block that enables natural citations with proper source attribution for retrieved content. This field is only supported by Anthropic Claude Opus 4.1, Opus 4, Sonnet 4.5, Sonnet 4, Sonnet 3.7, and 3.5 Haiku models."]
module VideoBlock =
  struct
    type nonrec t =
      {
      format: VideoFormat.t [@ocaml.doc "The block's format."];
      source: VideoSource.t [@ocaml.doc "The block's source."]}
    let context_ = "VideoBlock"
    let make ~format = fun ~source -> fun () -> { format; source }
    let to_value x =
      structure_to_value
        [("format", (Some (VideoFormat.to_value x.format)));
        ("source", (Some (VideoSource.to_value x.source)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let source =
        VideoSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "source") in
      let format =
        VideoFormat.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "format") in
      make ~source ~format ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let source = field_map_exn json__ "source" VideoSource.of_json in
      let format = field_map_exn json__ "format" VideoFormat.of_json in
      make ~source ~format ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A video block."]
module GuardrailOrigin =
  struct
    type nonrec t =
      | REQUEST 
      | ACCOUNT_ENFORCED 
      | ORGANIZATION_ENFORCED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | REQUEST -> "REQUEST"
      | ACCOUNT_ENFORCED -> "ACCOUNT_ENFORCED"
      | ORGANIZATION_ENFORCED -> "ORGANIZATION_ENFORCED"
      | Non_static_id s -> s
    let of_string =
      function
      | "REQUEST" -> REQUEST
      | "ACCOUNT_ENFORCED" -> ACCOUNT_ENFORCED
      | "ORGANIZATION_ENFORCED" -> ORGANIZATION_ENFORCED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration GuardrailOrigin" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GuardrailOrigin" j)
    let to_json = simple_to_json to_value
  end
module GuardrailAutomatedReasoningFinding =
  struct
    type nonrec t =
      {
      valid: GuardrailAutomatedReasoningValidFinding.t option
        [@ocaml.doc
          "Contains the result when the automated reasoning evaluation determines that the claims in the input are logically valid and definitively true based on the provided premises and policy rules."];
      invalid: GuardrailAutomatedReasoningInvalidFinding.t option
        [@ocaml.doc
          "Contains the result when the automated reasoning evaluation determines that the claims in the input are logically invalid and contradict the established premises or policy rules."];
      satisfiable: GuardrailAutomatedReasoningSatisfiableFinding.t option
        [@ocaml.doc
          "Contains the result when the automated reasoning evaluation determines that the claims in the input could be either true or false depending on additional assumptions not provided in the input context."];
      impossible: GuardrailAutomatedReasoningImpossibleFinding.t option
        [@ocaml.doc
          "Contains the result when the automated reasoning evaluation determines that no valid logical conclusions can be drawn due to contradictions in the premises or policy rules themselves."];
      translationAmbiguous:
        GuardrailAutomatedReasoningTranslationAmbiguousFinding.t option
        [@ocaml.doc
          "Contains the result when the automated reasoning evaluation detects that the input has multiple valid logical interpretations, requiring additional context or clarification to proceed with validation."];
      tooComplex: GuardrailAutomatedReasoningTooComplexFinding.t option
        [@ocaml.doc
          "Contains the result when the automated reasoning evaluation cannot process the input due to its complexity or volume exceeding the system's processing capacity for logical analysis."];
      noTranslations:
        GuardrailAutomatedReasoningNoTranslationsFinding.t option
        [@ocaml.doc
          "Contains the result when the automated reasoning evaluation cannot extract any relevant logical information from the input that can be validated against the policy rules."]}
    let make ?valid =
      fun ?invalid ->
        fun ?satisfiable ->
          fun ?impossible ->
            fun ?translationAmbiguous ->
              fun ?tooComplex ->
                fun ?noTranslations ->
                  fun () ->
                    {
                      valid;
                      invalid;
                      satisfiable;
                      impossible;
                      translationAmbiguous;
                      tooComplex;
                      noTranslations
                    }
    let to_value x =
      structure_to_value
        [("valid",
           (Option.map x.valid
              ~f:GuardrailAutomatedReasoningValidFinding.to_value));
        ("invalid",
          (Option.map x.invalid
             ~f:GuardrailAutomatedReasoningInvalidFinding.to_value));
        ("satisfiable",
          (Option.map x.satisfiable
             ~f:GuardrailAutomatedReasoningSatisfiableFinding.to_value));
        ("impossible",
          (Option.map x.impossible
             ~f:GuardrailAutomatedReasoningImpossibleFinding.to_value));
        ("translationAmbiguous",
          (Option.map x.translationAmbiguous
             ~f:GuardrailAutomatedReasoningTranslationAmbiguousFinding.to_value));
        ("tooComplex",
          (Option.map x.tooComplex
             ~f:GuardrailAutomatedReasoningTooComplexFinding.to_value));
        ("noTranslations",
          (Option.map x.noTranslations
             ~f:GuardrailAutomatedReasoningNoTranslationsFinding.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let noTranslations =
        (Option.map
           ~f:GuardrailAutomatedReasoningNoTranslationsFinding.of_xml)
          (Xml.child xml_arg0 "noTranslations") in
      let tooComplex =
        (Option.map ~f:GuardrailAutomatedReasoningTooComplexFinding.of_xml)
          (Xml.child xml_arg0 "tooComplex") in
      let translationAmbiguous =
        (Option.map
           ~f:GuardrailAutomatedReasoningTranslationAmbiguousFinding.of_xml)
          (Xml.child xml_arg0 "translationAmbiguous") in
      let impossible =
        (Option.map ~f:GuardrailAutomatedReasoningImpossibleFinding.of_xml)
          (Xml.child xml_arg0 "impossible") in
      let satisfiable =
        (Option.map ~f:GuardrailAutomatedReasoningSatisfiableFinding.of_xml)
          (Xml.child xml_arg0 "satisfiable") in
      let invalid =
        (Option.map ~f:GuardrailAutomatedReasoningInvalidFinding.of_xml)
          (Xml.child xml_arg0 "invalid") in
      let valid =
        (Option.map ~f:GuardrailAutomatedReasoningValidFinding.of_xml)
          (Xml.child xml_arg0 "valid") in
      make ?noTranslations ?tooComplex ?translationAmbiguous ?impossible
        ?satisfiable ?invalid ?valid ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let noTranslations =
        field_map json__ "noTranslations"
          GuardrailAutomatedReasoningNoTranslationsFinding.of_json in
      let tooComplex =
        field_map json__ "tooComplex"
          GuardrailAutomatedReasoningTooComplexFinding.of_json in
      let translationAmbiguous =
        field_map json__ "translationAmbiguous"
          GuardrailAutomatedReasoningTranslationAmbiguousFinding.of_json in
      let impossible =
        field_map json__ "impossible"
          GuardrailAutomatedReasoningImpossibleFinding.of_json in
      let satisfiable =
        field_map json__ "satisfiable"
          GuardrailAutomatedReasoningSatisfiableFinding.of_json in
      let invalid =
        field_map json__ "invalid"
          GuardrailAutomatedReasoningInvalidFinding.of_json in
      let valid =
        field_map json__ "valid"
          GuardrailAutomatedReasoningValidFinding.of_json in
      make ?noTranslations ?tooComplex ?translationAmbiguous ?impossible
        ?satisfiable ?invalid ?valid ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a logical validation result from automated reasoning policy evaluation. The finding indicates whether claims in the input are logically valid, invalid, satisfiable, impossible, or have other logical issues."]
module GuardrailContentFilter =
  struct
    type nonrec t =
      {
      type_: GuardrailContentFilterType.t option
        [@ocaml.doc "The guardrail type."];
      confidence: GuardrailContentFilterConfidence.t option
        [@ocaml.doc "The guardrail confidence."];
      filterStrength: GuardrailContentFilterStrength.t option
        [@ocaml.doc
          "The filter strength setting for the guardrail content filter."];
      action: GuardrailContentPolicyAction.t option
        [@ocaml.doc "The guardrail action."];
      detected: Boolean.t option
        [@ocaml.doc
          "Indicates whether content that breaches the guardrail configuration is detected."]}
    let make ?type_ =
      fun ?confidence ->
        fun ?filterStrength ->
          fun ?action ->
            fun ?detected ->
              fun () ->
                { type_; confidence; filterStrength; action; detected }
    let to_value x =
      structure_to_value
        [("type",
           (Option.map x.type_ ~f:GuardrailContentFilterType.to_value));
        ("confidence",
          (Option.map x.confidence
             ~f:GuardrailContentFilterConfidence.to_value));
        ("filterStrength",
          (Option.map x.filterStrength
             ~f:GuardrailContentFilterStrength.to_value));
        ("action",
          (Option.map x.action ~f:GuardrailContentPolicyAction.to_value));
        ("detected", (Option.map x.detected ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let detected =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "detected") in
      let action =
        (Option.map ~f:GuardrailContentPolicyAction.of_xml)
          (Xml.child xml_arg0 "action") in
      let filterStrength =
        (Option.map ~f:GuardrailContentFilterStrength.of_xml)
          (Xml.child xml_arg0 "filterStrength") in
      let confidence =
        (Option.map ~f:GuardrailContentFilterConfidence.of_xml)
          (Xml.child xml_arg0 "confidence") in
      let type_ =
        (Option.map ~f:GuardrailContentFilterType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?detected ?action ?filterStrength ?confidence ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let detected = field_map json__ "detected" Boolean.of_json in
      let action =
        field_map json__ "action" GuardrailContentPolicyAction.of_json in
      let filterStrength =
        field_map json__ "filterStrength"
          GuardrailContentFilterStrength.of_json in
      let confidence =
        field_map json__ "confidence"
          GuardrailContentFilterConfidence.of_json in
      let type_ = field_map json__ "type" GuardrailContentFilterType.of_json in
      make ?detected ?action ?filterStrength ?confidence ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The content filter for a guardrail."]
module GuardrailContextualGroundingFilter =
  struct
    type nonrec t =
      {
      type_: GuardrailContextualGroundingFilterType.t option
        [@ocaml.doc "The contextual grounding filter type."];
      threshold: GuardrailContextualGroundingFilterThresholdDouble.t option
        [@ocaml.doc
          "The threshold used by contextual grounding filter to determine whether the content is grounded or not."];
      score: GuardrailContextualGroundingFilterScoreDouble.t option
        [@ocaml.doc "The score generated by contextual grounding filter."];
      action: GuardrailContextualGroundingPolicyAction.t option
        [@ocaml.doc
          "The action performed by the guardrails contextual grounding filter."];
      detected: Boolean.t option
        [@ocaml.doc
          "Indicates whether content that fails the contextual grounding evaluation (grounding or relevance score less than the corresponding threshold) was detected."]}
    let make ?type_ =
      fun ?threshold ->
        fun ?score ->
          fun ?action ->
            fun ?detected ->
              fun () -> { type_; threshold; score; action; detected }
    let to_value x =
      structure_to_value
        [("type",
           (Option.map x.type_
              ~f:GuardrailContextualGroundingFilterType.to_value));
        ("threshold",
          (Option.map x.threshold
             ~f:GuardrailContextualGroundingFilterThresholdDouble.to_value));
        ("score",
          (Option.map x.score
             ~f:GuardrailContextualGroundingFilterScoreDouble.to_value));
        ("action",
          (Option.map x.action
             ~f:GuardrailContextualGroundingPolicyAction.to_value));
        ("detected", (Option.map x.detected ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let detected =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "detected") in
      let action =
        (Option.map ~f:GuardrailContextualGroundingPolicyAction.of_xml)
          (Xml.child xml_arg0 "action") in
      let score =
        (Option.map ~f:GuardrailContextualGroundingFilterScoreDouble.of_xml)
          (Xml.child xml_arg0 "score") in
      let threshold =
        (Option.map
           ~f:GuardrailContextualGroundingFilterThresholdDouble.of_xml)
          (Xml.child xml_arg0 "threshold") in
      let type_ =
        (Option.map ~f:GuardrailContextualGroundingFilterType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?detected ?action ?score ?threshold ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let detected = field_map json__ "detected" Boolean.of_json in
      let action =
        field_map json__ "action"
          GuardrailContextualGroundingPolicyAction.of_json in
      let score =
        field_map json__ "score"
          GuardrailContextualGroundingFilterScoreDouble.of_json in
      let threshold =
        field_map json__ "threshold"
          GuardrailContextualGroundingFilterThresholdDouble.of_json in
      let type_ =
        field_map json__ "type"
          GuardrailContextualGroundingFilterType.of_json in
      make ?detected ?action ?score ?threshold ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details for the guardrails contextual grounding filter."]
module GuardrailImageCoverage =
  struct
    type nonrec t =
      {
      guarded: ImagesGuarded.t option
        [@ocaml.doc "The count (integer) of images guardrails guarded."];
      total: ImagesTotal.t option
        [@ocaml.doc
          "Represents the total number of images (integer) that were in the request (guarded and unguarded)."]}
    let make ?guarded = fun ?total -> fun () -> { guarded; total }
    let to_value x =
      structure_to_value
        [("guarded", (Option.map x.guarded ~f:ImagesGuarded.to_value));
        ("total", (Option.map x.total ~f:ImagesTotal.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let total =
        (Option.map ~f:ImagesTotal.of_xml) (Xml.child xml_arg0 "total") in
      let guarded =
        (Option.map ~f:ImagesGuarded.of_xml) (Xml.child xml_arg0 "guarded") in
      make ?total ?guarded ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let total = field_map json__ "total" ImagesTotal.of_json in
      let guarded = field_map json__ "guarded" ImagesGuarded.of_json in
      make ?total ?guarded ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The details of the guardrail image coverage."]
module GuardrailTextCharactersCoverage =
  struct
    type nonrec t =
      {
      guarded: TextCharactersGuarded.t option
        [@ocaml.doc
          "The text characters that were guarded by the guardrail coverage."];
      total: TextCharactersTotal.t option
        [@ocaml.doc "The total text characters by the guardrail coverage."]}
    let make ?guarded = fun ?total -> fun () -> { guarded; total }
    let to_value x =
      structure_to_value
        [("guarded",
           (Option.map x.guarded ~f:TextCharactersGuarded.to_value));
        ("total", (Option.map x.total ~f:TextCharactersTotal.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let total =
        (Option.map ~f:TextCharactersTotal.of_xml)
          (Xml.child xml_arg0 "total") in
      let guarded =
        (Option.map ~f:TextCharactersGuarded.of_xml)
          (Xml.child xml_arg0 "guarded") in
      make ?total ?guarded ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let total = field_map json__ "total" TextCharactersTotal.of_json in
      let guarded = field_map json__ "guarded" TextCharactersGuarded.of_json in
      make ?total ?guarded ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The guardrail coverage for the text characters."]
module GuardrailAutomatedReasoningPoliciesProcessed =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for GuardrailAutomatedReasoningPoliciesProcessed"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GuardrailAutomatedReasoningPolicyUnitsProcessed =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for GuardrailAutomatedReasoningPolicyUnitsProcessed"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GuardrailContentPolicyImageUnitsProcessed =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for GuardrailContentPolicyImageUnitsProcessed"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GuardrailContentPolicyUnitsProcessed =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for GuardrailContentPolicyUnitsProcessed"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GuardrailContextualGroundingPolicyUnitsProcessed =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for GuardrailContextualGroundingPolicyUnitsProcessed"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GuardrailSensitiveInformationPolicyFreeUnitsProcessed =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for GuardrailSensitiveInformationPolicyFreeUnitsProcessed"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GuardrailSensitiveInformationPolicyUnitsProcessed =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for GuardrailSensitiveInformationPolicyUnitsProcessed"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GuardrailTopicPolicyUnitsProcessed =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for GuardrailTopicPolicyUnitsProcessed" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GuardrailWordPolicyUnitsProcessed =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for GuardrailWordPolicyUnitsProcessed" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GuardrailPiiEntityFilter =
  struct
    type nonrec t =
      {
      match_: String_.t option [@ocaml.doc "The PII entity filter match."];
      type_: GuardrailPiiEntityType.t option
        [@ocaml.doc "The PII entity filter type."];
      action: GuardrailSensitiveInformationPolicyAction.t option
        [@ocaml.doc "The PII entity filter action."];
      detected: Boolean.t option
        [@ocaml.doc
          "Indicates whether personally identifiable information (PII) that breaches the guardrail configuration is detected."]}
    let make ?match_ =
      fun ?type_ ->
        fun ?action ->
          fun ?detected -> fun () -> { match_; type_; action; detected }
    let to_value x =
      structure_to_value
        [("match", (Option.map x.match_ ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:GuardrailPiiEntityType.to_value));
        ("action",
          (Option.map x.action
             ~f:GuardrailSensitiveInformationPolicyAction.to_value));
        ("detected", (Option.map x.detected ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let detected =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "detected") in
      let action =
        (Option.map ~f:GuardrailSensitiveInformationPolicyAction.of_xml)
          (Xml.child xml_arg0 "action") in
      let type_ =
        (Option.map ~f:GuardrailPiiEntityType.of_xml)
          (Xml.child xml_arg0 "type") in
      let match_ =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "match") in
      make ?detected ?action ?type_ ?match_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let detected = field_map json__ "detected" Boolean.of_json in
      let action =
        field_map json__ "action"
          GuardrailSensitiveInformationPolicyAction.of_json in
      let type_ = field_map json__ "type" GuardrailPiiEntityType.of_json in
      let match_ = field_map json__ "match" String_.of_json in
      make ?detected ?action ?type_ ?match_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A Personally Identifiable Information (PII) entity configured in a guardrail."]
module GuardrailRegexFilter =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The regex filter name."];
      match_: String_.t option [@ocaml.doc "The regesx filter match."];
      regex: String_.t option [@ocaml.doc "The regex query."];
      action: GuardrailSensitiveInformationPolicyAction.t option
        [@ocaml.doc "The region filter action."];
      detected: Boolean.t option
        [@ocaml.doc
          "Indicates whether custom regex entities that breach the guardrail configuration are detected."]}
    let make ?name =
      fun ?match_ ->
        fun ?regex ->
          fun ?action ->
            fun ?detected ->
              fun () -> { name; match_; regex; action; detected }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("match", (Option.map x.match_ ~f:String_.to_value));
        ("regex", (Option.map x.regex ~f:String_.to_value));
        ("action",
          (Option.map x.action
             ~f:GuardrailSensitiveInformationPolicyAction.to_value));
        ("detected", (Option.map x.detected ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let detected =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "detected") in
      let action =
        (Option.map ~f:GuardrailSensitiveInformationPolicyAction.of_xml)
          (Xml.child xml_arg0 "action") in
      let regex = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "regex") in
      let match_ =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "match") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?detected ?action ?regex ?match_ ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let detected = field_map json__ "detected" Boolean.of_json in
      let action =
        field_map json__ "action"
          GuardrailSensitiveInformationPolicyAction.of_json in
      let regex = field_map json__ "regex" String_.of_json in
      let match_ = field_map json__ "match" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?detected ?action ?regex ?match_ ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A Regex filter configured in a guardrail."]
module GuardrailTopic =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name for the guardrail."];
      type_: GuardrailTopicType.t option
        [@ocaml.doc
          "The type behavior that the guardrail should perform when the model detects the topic."];
      action: GuardrailTopicPolicyAction.t option
        [@ocaml.doc
          "The action the guardrail should take when it intervenes on a topic."];
      detected: Boolean.t option
        [@ocaml.doc
          "Indicates whether topic content that breaches the guardrail configuration is detected."]}
    let make ?name =
      fun ?type_ ->
        fun ?action ->
          fun ?detected -> fun () -> { name; type_; action; detected }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:GuardrailTopicType.to_value));
        ("action",
          (Option.map x.action ~f:GuardrailTopicPolicyAction.to_value));
        ("detected", (Option.map x.detected ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let detected =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "detected") in
      let action =
        (Option.map ~f:GuardrailTopicPolicyAction.of_xml)
          (Xml.child xml_arg0 "action") in
      let type_ =
        (Option.map ~f:GuardrailTopicType.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?detected ?action ?type_ ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let detected = field_map json__ "detected" Boolean.of_json in
      let action =
        field_map json__ "action" GuardrailTopicPolicyAction.of_json in
      let type_ = field_map json__ "type" GuardrailTopicType.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?detected ?action ?type_ ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a topic guardrail."]
module GuardrailCustomWord =
  struct
    type nonrec t =
      {
      match_: String_.t option [@ocaml.doc "The match for the custom word."];
      action: GuardrailWordPolicyAction.t option
        [@ocaml.doc "The action for the custom word."];
      detected: Boolean.t option
        [@ocaml.doc
          "Indicates whether custom word content that breaches the guardrail configuration is detected."]}
    let make ?match_ =
      fun ?action -> fun ?detected -> fun () -> { match_; action; detected }
    let to_value x =
      structure_to_value
        [("match", (Option.map x.match_ ~f:String_.to_value));
        ("action",
          (Option.map x.action ~f:GuardrailWordPolicyAction.to_value));
        ("detected", (Option.map x.detected ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let detected =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "detected") in
      let action =
        (Option.map ~f:GuardrailWordPolicyAction.of_xml)
          (Xml.child xml_arg0 "action") in
      let match_ =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "match") in
      make ?detected ?action ?match_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let detected = field_map json__ "detected" Boolean.of_json in
      let action =
        field_map json__ "action" GuardrailWordPolicyAction.of_json in
      let match_ = field_map json__ "match" String_.of_json in
      make ?detected ?action ?match_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A custom word configured in a guardrail."]
module GuardrailManagedWord =
  struct
    type nonrec t =
      {
      match_: String_.t option [@ocaml.doc "The match for the managed word."];
      type_: GuardrailManagedWordType.t option
        [@ocaml.doc "The type for the managed word."];
      action: GuardrailWordPolicyAction.t option
        [@ocaml.doc "The action for the managed word."];
      detected: Boolean.t option
        [@ocaml.doc
          "Indicates whether managed word content that breaches the guardrail configuration is detected."]}
    let make ?match_ =
      fun ?type_ ->
        fun ?action ->
          fun ?detected -> fun () -> { match_; type_; action; detected }
    let to_value x =
      structure_to_value
        [("match", (Option.map x.match_ ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:GuardrailManagedWordType.to_value));
        ("action",
          (Option.map x.action ~f:GuardrailWordPolicyAction.to_value));
        ("detected", (Option.map x.detected ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let detected =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "detected") in
      let action =
        (Option.map ~f:GuardrailWordPolicyAction.of_xml)
          (Xml.child xml_arg0 "action") in
      let type_ =
        (Option.map ~f:GuardrailManagedWordType.of_xml)
          (Xml.child xml_arg0 "type") in
      let match_ =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "match") in
      make ?detected ?action ?type_ ?match_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let detected = field_map json__ "detected" Boolean.of_json in
      let action =
        field_map json__ "action" GuardrailWordPolicyAction.of_json in
      let type_ = field_map json__ "type" GuardrailManagedWordType.of_json in
      let match_ = field_map json__ "match" String_.of_json in
      make ?detected ?action ?type_ ?match_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A managed word configured in a guardrail."]
module AudioSourceBytesBlob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module CitationGeneratedContent =
  struct
    type nonrec t =
      {
      text: String_.t option
        [@ocaml.doc
          "The text content that was generated by the model and is supported by the associated citation."]}
    let make ?text = fun () -> { text }
    let to_value x =
      structure_to_value [("text", (Option.map x.text ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" String_.of_json in make ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the generated text content that corresponds to or is supported by a citation from a source document."]
module Citation =
  struct
    type nonrec t =
      {
      title: String_.t option
        [@ocaml.doc
          "The title or identifier of the source document being cited."];
      source: String_.t option
        [@ocaml.doc
          "The source from the original search result that provided the cited content."];
      sourceContent: CitationSourceContentList.t option
        [@ocaml.doc
          "The specific content from the source document that was referenced or cited in the generated response."];
      location: CitationLocation.t option
        [@ocaml.doc
          "The precise location within the source document where the cited content can be found, including character positions, page numbers, or chunk identifiers."]}
    let make ?title =
      fun ?source ->
        fun ?sourceContent ->
          fun ?location ->
            fun () -> { title; source; sourceContent; location }
    let to_value x =
      structure_to_value
        [("title", (Option.map x.title ~f:String_.to_value));
        ("source", (Option.map x.source ~f:String_.to_value));
        ("sourceContent",
          (Option.map x.sourceContent ~f:CitationSourceContentList.to_value));
        ("location", (Option.map x.location ~f:CitationLocation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:CitationLocation.of_xml)
          (Xml.child xml_arg0 "location") in
      let sourceContent =
        (Option.map ~f:CitationSourceContentList.of_xml)
          (Xml.child xml_arg0 "sourceContent") in
      let source =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "source") in
      let title = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "title") in
      make ?location ?sourceContent ?source ?title ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "location" CitationLocation.of_json in
      let sourceContent =
        field_map json__ "sourceContent" CitationSourceContentList.of_json in
      let source = field_map json__ "source" String_.of_json in
      let title = field_map json__ "title" String_.of_json in
      make ?location ?sourceContent ?source ?title ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a citation that references a specific source document. Citations provide traceability between the model's generated response and the source documents that informed that response."]
module GuardrailConverseImageFormat =
  struct
    type nonrec t =
      | Png 
      | Jpeg 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Png -> "png" | Jpeg -> "jpeg" | Non_static_id s -> s
    let of_string =
      function | "png" -> Png | "jpeg" -> Jpeg | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailConverseImageFormat"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailConverseImageFormat" j)
    let to_json = simple_to_json to_value
  end
module GuardrailConverseImageSource =
  struct
    type nonrec t =
      {
      bytes: GuardrailConverseImageSourceBytesBlob.t option
        [@ocaml.doc "The raw image bytes for the image."]}
    let make ?bytes = fun () -> { bytes }
    let of_header_and_body = ((fun (xs, pipe) -> make ?bytes:(Some pipe) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("bytes",
           (Option.map x.bytes
              ~f:GuardrailConverseImageSourceBytesBlob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bytes =
        (Option.map ~f:GuardrailConverseImageSourceBytesBlob.of_xml)
          (Xml.child xml_arg0 "bytes") in
      make ?bytes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bytes =
        field_map json__ "bytes"
          GuardrailConverseImageSourceBytesBlob.of_json in
      make ?bytes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The image source (image bytes) of the guardrail converse image source."]
module GuardrailConverseContentQualifierList =
  struct
    type nonrec t = GuardrailConverseContentQualifier.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailConverseContentQualifier.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:GuardrailConverseContentQualifier.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailConverseContentQualifierList"
        ~of_json:GuardrailConverseContentQualifier.of_json j
    let to_json v = composed_to_json to_value v
  end
module ToolResultContentBlock =
  struct
    type nonrec t =
      {
      json: Document.t option
        [@ocaml.doc "A tool result that is JSON format data."];
      text: String_.t option [@ocaml.doc "A tool result that is text."];
      image: ImageBlock.t option
        [@ocaml.doc
          "A tool result that is an image. This field is only supported by Amazon Nova and Anthropic Claude 3 and 4 models."];
      document: DocumentBlock.t option
        [@ocaml.doc "A tool result that is a document."];
      video: VideoBlock.t option [@ocaml.doc "A tool result that is video."];
      searchResult: SearchResultBlock.t option
        [@ocaml.doc "A tool result that is a search result."]}
    let make ?json =
      fun ?text ->
        fun ?image ->
          fun ?document ->
            fun ?video ->
              fun ?searchResult ->
                fun () ->
                  { json; text; image; document; video; searchResult }
    let to_value x =
      structure_to_value
        [("json", (Option.map x.json ~f:Document.to_value));
        ("text", (Option.map x.text ~f:String_.to_value));
        ("image", (Option.map x.image ~f:ImageBlock.to_value));
        ("document", (Option.map x.document ~f:DocumentBlock.to_value));
        ("video", (Option.map x.video ~f:VideoBlock.to_value));
        ("searchResult",
          (Option.map x.searchResult ~f:SearchResultBlock.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let searchResult =
        (Option.map ~f:SearchResultBlock.of_xml)
          (Xml.child xml_arg0 "searchResult") in
      let video =
        (Option.map ~f:VideoBlock.of_xml) (Xml.child xml_arg0 "video") in
      let document =
        (Option.map ~f:DocumentBlock.of_xml) (Xml.child xml_arg0 "document") in
      let image =
        (Option.map ~f:ImageBlock.of_xml) (Xml.child xml_arg0 "image") in
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      let json = (Option.map ~f:Document.of_xml) (Xml.child xml_arg0 "json") in
      make ?searchResult ?video ?document ?image ?text ?json ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let searchResult =
        field_map json__ "searchResult" SearchResultBlock.of_json in
      let video = field_map json__ "video" VideoBlock.of_json in
      let document = field_map json__ "document" DocumentBlock.of_json in
      let image = field_map json__ "image" ImageBlock.of_json in
      let text = field_map json__ "text" String_.of_json in
      let json = field_map json__ "json" Document.of_json in
      make ?searchResult ?video ?document ?image ?text ?json ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The tool result content block. For more information, see Call a tool with the Converse API in the Amazon Bedrock User Guide."]
module GuardrailArn =
  struct
    type nonrec t = string
    let context_ = "GuardrailArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:guardrail/[a-z0-9]+)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GuardrailArn" j
    let to_json = simple_to_json to_value
  end
module GuardrailId =
  struct
    type nonrec t = string
    let context_ = "GuardrailId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"([a-z0-9]+)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GuardrailId" j
    let to_json = simple_to_json to_value
  end
module GuardrailOriginList =
  struct
    type nonrec t = GuardrailOrigin.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailOrigin.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailOrigin.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailOriginList"
        ~of_json:GuardrailOrigin.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailOwnership =
  struct
    type nonrec t =
      | SELF 
      | CROSS_ACCOUNT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SELF -> "SELF"
      | CROSS_ACCOUNT -> "CROSS_ACCOUNT"
      | Non_static_id s -> s
    let of_string =
      function
      | "SELF" -> SELF
      | "CROSS_ACCOUNT" -> CROSS_ACCOUNT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailOwnership" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GuardrailOwnership" j)
    let to_json = simple_to_json to_value
  end
module GuardrailVersion =
  struct
    type nonrec t = string
    let context_ = "GuardrailVersion"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"(|([1-9][0-9]{0,7})|(DRAFT))");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GuardrailVersion" j
    let to_json = simple_to_json to_value
  end
module GuardrailAutomatedReasoningFindingList =
  struct
    type nonrec t = GuardrailAutomatedReasoningFinding.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailAutomatedReasoningFinding.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:GuardrailAutomatedReasoningFinding.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailAutomatedReasoningFindingList"
        ~of_json:GuardrailAutomatedReasoningFinding.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailContentFilterList =
  struct
    type nonrec t = GuardrailContentFilter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailContentFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailContentFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailContentFilterList"
        ~of_json:GuardrailContentFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailContextualGroundingFilters =
  struct
    type nonrec t = GuardrailContextualGroundingFilter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailContextualGroundingFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:GuardrailContextualGroundingFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailContextualGroundingFilters"
        ~of_json:GuardrailContextualGroundingFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailCoverage =
  struct
    type nonrec t =
      {
      textCharacters: GuardrailTextCharactersCoverage.t option
        [@ocaml.doc "The text characters of the guardrail coverage details."];
      images: GuardrailImageCoverage.t option
        [@ocaml.doc
          "The guardrail coverage for images (the number of images that guardrails guarded)."]}
    let make ?textCharacters =
      fun ?images -> fun () -> { textCharacters; images }
    let to_value x =
      structure_to_value
        [("textCharacters",
           (Option.map x.textCharacters
              ~f:GuardrailTextCharactersCoverage.to_value));
        ("images", (Option.map x.images ~f:GuardrailImageCoverage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let images =
        (Option.map ~f:GuardrailImageCoverage.of_xml)
          (Xml.child xml_arg0 "images") in
      let textCharacters =
        (Option.map ~f:GuardrailTextCharactersCoverage.of_xml)
          (Xml.child xml_arg0 "textCharacters") in
      make ?images ?textCharacters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let images = field_map json__ "images" GuardrailImageCoverage.of_json in
      let textCharacters =
        field_map json__ "textCharacters"
          GuardrailTextCharactersCoverage.of_json in
      make ?images ?textCharacters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The action of the guardrail coverage details."]
module GuardrailProcessingLatency =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module GuardrailUsage =
  struct
    type nonrec t =
      {
      topicPolicyUnits: GuardrailTopicPolicyUnitsProcessed.t option
        [@ocaml.doc "The topic policy units processed by the guardrail."];
      contentPolicyUnits: GuardrailContentPolicyUnitsProcessed.t option
        [@ocaml.doc "The content policy units processed by the guardrail."];
      wordPolicyUnits: GuardrailWordPolicyUnitsProcessed.t option
        [@ocaml.doc "The word policy units processed by the guardrail."];
      sensitiveInformationPolicyUnits:
        GuardrailSensitiveInformationPolicyUnitsProcessed.t option
        [@ocaml.doc
          "The sensitive information policy units processed by the guardrail."];
      sensitiveInformationPolicyFreeUnits:
        GuardrailSensitiveInformationPolicyFreeUnitsProcessed.t option
        [@ocaml.doc
          "The sensitive information policy free units processed by the guardrail."];
      contextualGroundingPolicyUnits:
        GuardrailContextualGroundingPolicyUnitsProcessed.t option
        [@ocaml.doc
          "The contextual grounding policy units processed by the guardrail."];
      contentPolicyImageUnits:
        GuardrailContentPolicyImageUnitsProcessed.t option
        [@ocaml.doc
          "The content policy image units processed by the guardrail."];
      automatedReasoningPolicyUnits:
        GuardrailAutomatedReasoningPolicyUnitsProcessed.t option
        [@ocaml.doc
          "The number of text units processed by the automated reasoning policy."];
      automatedReasoningPolicies:
        GuardrailAutomatedReasoningPoliciesProcessed.t option
        [@ocaml.doc
          "The number of automated reasoning policies that were processed during the guardrail evaluation."]}
    let make ?topicPolicyUnits =
      fun ?contentPolicyUnits ->
        fun ?wordPolicyUnits ->
          fun ?sensitiveInformationPolicyUnits ->
            fun ?sensitiveInformationPolicyFreeUnits ->
              fun ?contextualGroundingPolicyUnits ->
                fun ?contentPolicyImageUnits ->
                  fun ?automatedReasoningPolicyUnits ->
                    fun ?automatedReasoningPolicies ->
                      fun () ->
                        {
                          topicPolicyUnits;
                          contentPolicyUnits;
                          wordPolicyUnits;
                          sensitiveInformationPolicyUnits;
                          sensitiveInformationPolicyFreeUnits;
                          contextualGroundingPolicyUnits;
                          contentPolicyImageUnits;
                          automatedReasoningPolicyUnits;
                          automatedReasoningPolicies
                        }
    let to_value x =
      structure_to_value
        [("topicPolicyUnits",
           (Option.map x.topicPolicyUnits
              ~f:GuardrailTopicPolicyUnitsProcessed.to_value));
        ("contentPolicyUnits",
          (Option.map x.contentPolicyUnits
             ~f:GuardrailContentPolicyUnitsProcessed.to_value));
        ("wordPolicyUnits",
          (Option.map x.wordPolicyUnits
             ~f:GuardrailWordPolicyUnitsProcessed.to_value));
        ("sensitiveInformationPolicyUnits",
          (Option.map x.sensitiveInformationPolicyUnits
             ~f:GuardrailSensitiveInformationPolicyUnitsProcessed.to_value));
        ("sensitiveInformationPolicyFreeUnits",
          (Option.map x.sensitiveInformationPolicyFreeUnits
             ~f:GuardrailSensitiveInformationPolicyFreeUnitsProcessed.to_value));
        ("contextualGroundingPolicyUnits",
          (Option.map x.contextualGroundingPolicyUnits
             ~f:GuardrailContextualGroundingPolicyUnitsProcessed.to_value));
        ("contentPolicyImageUnits",
          (Option.map x.contentPolicyImageUnits
             ~f:GuardrailContentPolicyImageUnitsProcessed.to_value));
        ("automatedReasoningPolicyUnits",
          (Option.map x.automatedReasoningPolicyUnits
             ~f:GuardrailAutomatedReasoningPolicyUnitsProcessed.to_value));
        ("automatedReasoningPolicies",
          (Option.map x.automatedReasoningPolicies
             ~f:GuardrailAutomatedReasoningPoliciesProcessed.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let automatedReasoningPolicies =
        (Option.map ~f:GuardrailAutomatedReasoningPoliciesProcessed.of_xml)
          (Xml.child xml_arg0 "automatedReasoningPolicies") in
      let automatedReasoningPolicyUnits =
        (Option.map ~f:GuardrailAutomatedReasoningPolicyUnitsProcessed.of_xml)
          (Xml.child xml_arg0 "automatedReasoningPolicyUnits") in
      let contentPolicyImageUnits =
        (Option.map ~f:GuardrailContentPolicyImageUnitsProcessed.of_xml)
          (Xml.child xml_arg0 "contentPolicyImageUnits") in
      let contextualGroundingPolicyUnits =
        (Option.map
           ~f:GuardrailContextualGroundingPolicyUnitsProcessed.of_xml)
          (Xml.child xml_arg0 "contextualGroundingPolicyUnits") in
      let sensitiveInformationPolicyFreeUnits =
        (Option.map
           ~f:GuardrailSensitiveInformationPolicyFreeUnitsProcessed.of_xml)
          (Xml.child xml_arg0 "sensitiveInformationPolicyFreeUnits") in
      let sensitiveInformationPolicyUnits =
        (Option.map
           ~f:GuardrailSensitiveInformationPolicyUnitsProcessed.of_xml)
          (Xml.child xml_arg0 "sensitiveInformationPolicyUnits") in
      let wordPolicyUnits =
        (Option.map ~f:GuardrailWordPolicyUnitsProcessed.of_xml)
          (Xml.child xml_arg0 "wordPolicyUnits") in
      let contentPolicyUnits =
        (Option.map ~f:GuardrailContentPolicyUnitsProcessed.of_xml)
          (Xml.child xml_arg0 "contentPolicyUnits") in
      let topicPolicyUnits =
        (Option.map ~f:GuardrailTopicPolicyUnitsProcessed.of_xml)
          (Xml.child xml_arg0 "topicPolicyUnits") in
      make ?automatedReasoningPolicies ?automatedReasoningPolicyUnits
        ?contentPolicyImageUnits ?contextualGroundingPolicyUnits
        ?sensitiveInformationPolicyFreeUnits ?sensitiveInformationPolicyUnits
        ?wordPolicyUnits ?contentPolicyUnits ?topicPolicyUnits ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let automatedReasoningPolicies =
        field_map json__ "automatedReasoningPolicies"
          GuardrailAutomatedReasoningPoliciesProcessed.of_json in
      let automatedReasoningPolicyUnits =
        field_map json__ "automatedReasoningPolicyUnits"
          GuardrailAutomatedReasoningPolicyUnitsProcessed.of_json in
      let contentPolicyImageUnits =
        field_map json__ "contentPolicyImageUnits"
          GuardrailContentPolicyImageUnitsProcessed.of_json in
      let contextualGroundingPolicyUnits =
        field_map json__ "contextualGroundingPolicyUnits"
          GuardrailContextualGroundingPolicyUnitsProcessed.of_json in
      let sensitiveInformationPolicyFreeUnits =
        field_map json__ "sensitiveInformationPolicyFreeUnits"
          GuardrailSensitiveInformationPolicyFreeUnitsProcessed.of_json in
      let sensitiveInformationPolicyUnits =
        field_map json__ "sensitiveInformationPolicyUnits"
          GuardrailSensitiveInformationPolicyUnitsProcessed.of_json in
      let wordPolicyUnits =
        field_map json__ "wordPolicyUnits"
          GuardrailWordPolicyUnitsProcessed.of_json in
      let contentPolicyUnits =
        field_map json__ "contentPolicyUnits"
          GuardrailContentPolicyUnitsProcessed.of_json in
      let topicPolicyUnits =
        field_map json__ "topicPolicyUnits"
          GuardrailTopicPolicyUnitsProcessed.of_json in
      make ?automatedReasoningPolicies ?automatedReasoningPolicyUnits
        ?contentPolicyImageUnits ?contextualGroundingPolicyUnits
        ?sensitiveInformationPolicyFreeUnits ?sensitiveInformationPolicyUnits
        ?wordPolicyUnits ?contentPolicyUnits ?topicPolicyUnits ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The details on the use of the guardrail."]
module GuardrailPiiEntityFilterList =
  struct
    type nonrec t = GuardrailPiiEntityFilter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailPiiEntityFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailPiiEntityFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailPiiEntityFilterList"
        ~of_json:GuardrailPiiEntityFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailRegexFilterList =
  struct
    type nonrec t = GuardrailRegexFilter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailRegexFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailRegexFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailRegexFilterList"
        ~of_json:GuardrailRegexFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailTopicList =
  struct
    type nonrec t = GuardrailTopic.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailTopic.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailTopic.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailTopicList" ~of_json:GuardrailTopic.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module GuardrailCustomWordList =
  struct
    type nonrec t = GuardrailCustomWord.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailCustomWord.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailCustomWord.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailCustomWordList"
        ~of_json:GuardrailCustomWord.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailManagedWordList =
  struct
    type nonrec t = GuardrailManagedWord.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailManagedWord.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailManagedWord.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailManagedWordList"
        ~of_json:GuardrailManagedWord.of_json j
    let to_json v = composed_to_json to_value v
  end
module AudioFormat =
  struct
    type nonrec t =
      | Mp3 
      | Opus 
      | Wav 
      | Aac 
      | Flac 
      | Mp4 
      | Ogg 
      | Mkv 
      | Mka 
      | X_aac 
      | M4a 
      | Mpeg 
      | Mpga 
      | Pcm 
      | Webm 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Mp3 -> "mp3"
      | Opus -> "opus"
      | Wav -> "wav"
      | Aac -> "aac"
      | Flac -> "flac"
      | Mp4 -> "mp4"
      | Ogg -> "ogg"
      | Mkv -> "mkv"
      | Mka -> "mka"
      | X_aac -> "x-aac"
      | M4a -> "m4a"
      | Mpeg -> "mpeg"
      | Mpga -> "mpga"
      | Pcm -> "pcm"
      | Webm -> "webm"
      | Non_static_id s -> s
    let of_string =
      function
      | "mp3" -> Mp3
      | "opus" -> Opus
      | "wav" -> Wav
      | "aac" -> Aac
      | "flac" -> Flac
      | "mp4" -> Mp4
      | "ogg" -> Ogg
      | "mkv" -> Mkv
      | "mka" -> Mka
      | "x-aac" -> X_aac
      | "m4a" -> M4a
      | "mpeg" -> Mpeg
      | "mpga" -> Mpga
      | "pcm" -> Pcm
      | "webm" -> Webm
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AudioFormat" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AudioFormat" j)
    let to_json = simple_to_json to_value
  end
module AudioSource =
  struct
    type nonrec t =
      {
      bytes: AudioSourceBytesBlob.t option
        [@ocaml.doc "Audio data encoded in base64."];
      s3Location: S3Location.t option
        [@ocaml.doc
          "A reference to audio data stored in an Amazon S3 bucket. To see which models support S3 uploads, see Supported models and features for Converse."]}
    let make ?bytes = fun ?s3Location -> fun () -> { bytes; s3Location }
    let to_value x =
      structure_to_value
        [("bytes", (Option.map x.bytes ~f:AudioSourceBytesBlob.to_value));
        ("s3Location", (Option.map x.s3Location ~f:S3Location.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Location =
        (Option.map ~f:S3Location.of_xml) (Xml.child xml_arg0 "s3Location") in
      let bytes =
        (Option.map ~f:AudioSourceBytesBlob.of_xml)
          (Xml.child xml_arg0 "bytes") in
      make ?s3Location ?bytes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Location = field_map json__ "s3Location" S3Location.of_json in
      let bytes = field_map json__ "bytes" AudioSourceBytesBlob.of_json in
      make ?s3Location ?bytes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The source of audio data, which can be provided either as raw bytes or a reference to an S3 location."]
module CachePointType =
  struct
    type nonrec t =
      | Default 
      | Non_static_id of string 
    let make i = i
    let to_string = function | Default -> "default" | Non_static_id s -> s
    let of_string = function | "default" -> Default | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CachePointType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CachePointType" j)
    let to_json = simple_to_json to_value
  end
module CacheTTL =
  struct
    type nonrec t =
      | Zz5m 
      | Zz1h 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Zz5m -> "5m" | Zz1h -> "1h" | Non_static_id s -> s
    let of_string =
      function | "5m" -> Zz5m | "1h" -> Zz1h | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CacheTTL" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CacheTTL" j)
    let to_json = simple_to_json to_value
  end
module CitationGeneratedContentList =
  struct
    type nonrec t = CitationGeneratedContent.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CitationGeneratedContent.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CitationGeneratedContent.of_xml)
    let of_json j =
      list_of_json ~kind:"CitationGeneratedContentList"
        ~of_json:CitationGeneratedContent.of_json j
    let to_json v = composed_to_json to_value v
  end
module Citations =
  struct
    type nonrec t = Citation.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Citation.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Citation.of_xml)
    let of_json j =
      list_of_json ~kind:"Citations" ~of_json:Citation.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailConverseImageBlock =
  struct
    type nonrec t =
      {
      format: GuardrailConverseImageFormat.t
        [@ocaml.doc
          "The format details for the image type of the guardrail converse image block."];
      source: GuardrailConverseImageSource.t
        [@ocaml.doc
          "The image source (image bytes) of the guardrail converse image block."]}
    let context_ = "GuardrailConverseImageBlock"
    let make ~format = fun ~source -> fun () -> { format; source }
    let to_value x =
      structure_to_value
        [("format", (Some (GuardrailConverseImageFormat.to_value x.format)));
        ("source", (Some (GuardrailConverseImageSource.to_value x.source)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let source =
        GuardrailConverseImageSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "source") in
      let format =
        GuardrailConverseImageFormat.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "format") in
      make ~source ~format ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let source =
        field_map_exn json__ "source" GuardrailConverseImageSource.of_json in
      let format =
        field_map_exn json__ "format" GuardrailConverseImageFormat.of_json in
      make ~source ~format ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An image block that contains images that you want to assess with a guardrail."]
module GuardrailConverseTextBlock =
  struct
    type nonrec t =
      {
      text: String_.t [@ocaml.doc "The text that you want to guard."];
      qualifiers: GuardrailConverseContentQualifierList.t option
        [@ocaml.doc
          "The qualifier details for the guardrails contextual grounding filter."]}
    let context_ = "GuardrailConverseTextBlock"
    let make ?qualifiers = fun ~text -> fun () -> { qualifiers; text }
    let to_value x =
      structure_to_value
        [("text", (Some (String_.to_value x.text)));
        ("qualifiers",
          (Option.map x.qualifiers
             ~f:GuardrailConverseContentQualifierList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let qualifiers =
        (Option.map ~f:GuardrailConverseContentQualifierList.of_xml)
          (Xml.child xml_arg0 "qualifiers") in
      let text =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "text") in
      make ?qualifiers ~text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let qualifiers =
        field_map json__ "qualifiers"
          GuardrailConverseContentQualifierList.of_json in
      let text = field_map_exn json__ "text" String_.of_json in
      make ?qualifiers ~text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A text block that contains text that you want to assess with a guardrail. For more information, see GuardrailConverseContentBlock."]
module Blob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module ReasoningTextBlock =
  struct
    type nonrec t =
      {
      text: String_.t
        [@ocaml.doc
          "The reasoning that the model used to return the output."];
      signature: String_.t option
        [@ocaml.doc
          "A token that verifies that the reasoning text was generated by the model. If you pass a reasoning block back to the API in a multi-turn conversation, include the text and its signature unmodified."]}
    let context_ = "ReasoningTextBlock"
    let make ?signature = fun ~text -> fun () -> { signature; text }
    let to_value x =
      structure_to_value
        [("text", (Some (String_.to_value x.text)));
        ("signature", (Option.map x.signature ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let signature =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "signature") in
      let text =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "text") in
      make ?signature ~text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let signature = field_map json__ "signature" String_.of_json in
      let text = field_map_exn json__ "text" String_.of_json in
      make ?signature ~text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the reasoning that the model used to return the output."]
module ToolResultContentBlocks =
  struct
    type nonrec t = ToolResultContentBlock.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ToolResultContentBlock.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ToolResultContentBlock.of_xml)
    let of_json j =
      list_of_json ~kind:"ToolResultContentBlocks"
        ~of_json:ToolResultContentBlock.of_json j
    let to_json v = composed_to_json to_value v
  end
module ToolResultStatus =
  struct
    type nonrec t =
      | Success 
      | Error 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Success -> "success"
      | Error -> "error"
      | Non_static_id s -> s
    let of_string =
      function
      | "success" -> Success
      | "error" -> Error
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ToolResultStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ToolResultStatus" j)
    let to_json = simple_to_json to_value
  end
module ToolUseId =
  struct
    type nonrec t = string
    let context_ = "ToolUseId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9_.:-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ToolUseId" j
    let to_json = simple_to_json to_value
  end
module ToolName =
  struct
    type nonrec t = string
    let context_ = "ToolName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ToolName" j
    let to_json = simple_to_json to_value
  end
module ToolUseType =
  struct
    type nonrec t =
      | Server_tool_use 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Server_tool_use -> "server_tool_use" | Non_static_id s -> s
    let of_string =
      function | "server_tool_use" -> Server_tool_use | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ToolUseType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ToolUseType" j)
    let to_json = simple_to_json to_value
  end
module AppliedGuardrailDetails =
  struct
    type nonrec t =
      {
      guardrailId: GuardrailId.t option
        [@ocaml.doc "The unique ID of the guardrail that was applied."];
      guardrailVersion: GuardrailVersion.t option
        [@ocaml.doc "The version of the guardrail that was applied."];
      guardrailArn: GuardrailArn.t option
        [@ocaml.doc "The ARN of the guardrail that was applied."];
      guardrailOrigin: GuardrailOriginList.t option
        [@ocaml.doc
          "The origin of how the guardrail was applied. This can be either requested at the API level or enforced at the account or organization level as a default guardrail."];
      guardrailOwnership: GuardrailOwnership.t option
        [@ocaml.doc
          "The ownership type of the guardrail, indicating whether it is owned by the requesting account or is a cross-account guardrail shared from another AWS account."]}
    let make ?guardrailId =
      fun ?guardrailVersion ->
        fun ?guardrailArn ->
          fun ?guardrailOrigin ->
            fun ?guardrailOwnership ->
              fun () ->
                {
                  guardrailId;
                  guardrailVersion;
                  guardrailArn;
                  guardrailOrigin;
                  guardrailOwnership
                }
    let to_value x =
      structure_to_value
        [("guardrailId", (Option.map x.guardrailId ~f:GuardrailId.to_value));
        ("guardrailVersion",
          (Option.map x.guardrailVersion ~f:GuardrailVersion.to_value));
        ("guardrailArn",
          (Option.map x.guardrailArn ~f:GuardrailArn.to_value));
        ("guardrailOrigin",
          (Option.map x.guardrailOrigin ~f:GuardrailOriginList.to_value));
        ("guardrailOwnership",
          (Option.map x.guardrailOwnership ~f:GuardrailOwnership.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let guardrailOwnership =
        (Option.map ~f:GuardrailOwnership.of_xml)
          (Xml.child xml_arg0 "guardrailOwnership") in
      let guardrailOrigin =
        (Option.map ~f:GuardrailOriginList.of_xml)
          (Xml.child xml_arg0 "guardrailOrigin") in
      let guardrailArn =
        (Option.map ~f:GuardrailArn.of_xml)
          (Xml.child xml_arg0 "guardrailArn") in
      let guardrailVersion =
        (Option.map ~f:GuardrailVersion.of_xml)
          (Xml.child xml_arg0 "guardrailVersion") in
      let guardrailId =
        (Option.map ~f:GuardrailId.of_xml) (Xml.child xml_arg0 "guardrailId") in
      make ?guardrailOwnership ?guardrailOrigin ?guardrailArn
        ?guardrailVersion ?guardrailId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let guardrailOwnership =
        field_map json__ "guardrailOwnership" GuardrailOwnership.of_json in
      let guardrailOrigin =
        field_map json__ "guardrailOrigin" GuardrailOriginList.of_json in
      let guardrailArn = field_map json__ "guardrailArn" GuardrailArn.of_json in
      let guardrailVersion =
        field_map json__ "guardrailVersion" GuardrailVersion.of_json in
      let guardrailId = field_map json__ "guardrailId" GuardrailId.of_json in
      make ?guardrailOwnership ?guardrailOrigin ?guardrailArn
        ?guardrailVersion ?guardrailId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about the specific guardrail that was applied during this assessment, including its identifier, version, ARN, origin, and ownership information."]
module GuardrailAutomatedReasoningPolicyAssessment =
  struct
    type nonrec t =
      {
      findings: GuardrailAutomatedReasoningFindingList.t option
        [@ocaml.doc
          "List of logical validation results produced by evaluating the input content against automated reasoning policies."]}
    let make ?findings = fun () -> { findings }
    let to_value x =
      structure_to_value
        [("findings",
           (Option.map x.findings
              ~f:GuardrailAutomatedReasoningFindingList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let findings =
        (Option.map ~f:GuardrailAutomatedReasoningFindingList.of_xml)
          (Xml.child xml_arg0 "findings") in
      make ?findings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let findings =
        field_map json__ "findings"
          GuardrailAutomatedReasoningFindingList.of_json in
      make ?findings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the results of automated reasoning policy evaluation, including logical findings about the validity of claims made in the input content."]
module GuardrailContentPolicyAssessment =
  struct
    type nonrec t =
      {
      filters: GuardrailContentFilterList.t option
        [@ocaml.doc "The content policy filters."]}
    let make ?filters = fun () -> { filters }
    let to_value x =
      structure_to_value
        [("filters",
           (Option.map x.filters ~f:GuardrailContentFilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:GuardrailContentFilterList.of_xml)
          (Xml.child xml_arg0 "filters") in
      make ?filters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters =
        field_map json__ "filters" GuardrailContentFilterList.of_json in
      make ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An assessment of a content policy for a guardrail."]
module GuardrailContextualGroundingPolicyAssessment =
  struct
    type nonrec t =
      {
      filters: GuardrailContextualGroundingFilters.t option
        [@ocaml.doc
          "The filter details for the guardrails contextual grounding filter."]}
    let make ?filters = fun () -> { filters }
    let to_value x =
      structure_to_value
        [("filters",
           (Option.map x.filters
              ~f:GuardrailContextualGroundingFilters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:GuardrailContextualGroundingFilters.of_xml)
          (Xml.child xml_arg0 "filters") in
      make ?filters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters =
        field_map json__ "filters"
          GuardrailContextualGroundingFilters.of_json in
      make ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The policy assessment details for the guardrails contextual grounding filter."]
module GuardrailInvocationMetrics =
  struct
    type nonrec t =
      {
      guardrailProcessingLatency: GuardrailProcessingLatency.t option
        [@ocaml.doc
          "The processing latency details for the guardrail invocation metrics."];
      usage: GuardrailUsage.t option
        [@ocaml.doc
          "The usage details for the guardrail invocation metrics."];
      guardrailCoverage: GuardrailCoverage.t option
        [@ocaml.doc
          "The coverage details for the guardrail invocation metrics."]}
    let make ?guardrailProcessingLatency =
      fun ?usage ->
        fun ?guardrailCoverage ->
          fun () -> { guardrailProcessingLatency; usage; guardrailCoverage }
    let to_value x =
      structure_to_value
        [("guardrailProcessingLatency",
           (Option.map x.guardrailProcessingLatency
              ~f:GuardrailProcessingLatency.to_value));
        ("usage", (Option.map x.usage ~f:GuardrailUsage.to_value));
        ("guardrailCoverage",
          (Option.map x.guardrailCoverage ~f:GuardrailCoverage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let guardrailCoverage =
        (Option.map ~f:GuardrailCoverage.of_xml)
          (Xml.child xml_arg0 "guardrailCoverage") in
      let usage =
        (Option.map ~f:GuardrailUsage.of_xml) (Xml.child xml_arg0 "usage") in
      let guardrailProcessingLatency =
        (Option.map ~f:GuardrailProcessingLatency.of_xml)
          (Xml.child xml_arg0 "guardrailProcessingLatency") in
      make ?guardrailCoverage ?usage ?guardrailProcessingLatency ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let guardrailCoverage =
        field_map json__ "guardrailCoverage" GuardrailCoverage.of_json in
      let usage = field_map json__ "usage" GuardrailUsage.of_json in
      let guardrailProcessingLatency =
        field_map json__ "guardrailProcessingLatency"
          GuardrailProcessingLatency.of_json in
      make ?guardrailCoverage ?usage ?guardrailProcessingLatency ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The invocation metrics for the guardrail."]
module GuardrailSensitiveInformationPolicyAssessment =
  struct
    type nonrec t =
      {
      piiEntities: GuardrailPiiEntityFilterList.t option
        [@ocaml.doc "The PII entities in the assessment."];
      regexes: GuardrailRegexFilterList.t option
        [@ocaml.doc "The regex queries in the assessment."]}
    let make ?piiEntities =
      fun ?regexes -> fun () -> { piiEntities; regexes }
    let to_value x =
      structure_to_value
        [("piiEntities",
           (Option.map x.piiEntities ~f:GuardrailPiiEntityFilterList.to_value));
        ("regexes",
          (Option.map x.regexes ~f:GuardrailRegexFilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let regexes =
        (Option.map ~f:GuardrailRegexFilterList.of_xml)
          (Xml.child xml_arg0 "regexes") in
      let piiEntities =
        (Option.map ~f:GuardrailPiiEntityFilterList.of_xml)
          (Xml.child xml_arg0 "piiEntities") in
      make ?regexes ?piiEntities ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let regexes =
        field_map json__ "regexes" GuardrailRegexFilterList.of_json in
      let piiEntities =
        field_map json__ "piiEntities" GuardrailPiiEntityFilterList.of_json in
      make ?regexes ?piiEntities ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The assessment for a Personally Identifiable Information (PII) policy."]
module GuardrailTopicPolicyAssessment =
  struct
    type nonrec t =
      {
      topics: GuardrailTopicList.t option
        [@ocaml.doc "The topics in the assessment."]}
    let make ?topics = fun () -> { topics }
    let to_value x =
      structure_to_value
        [("topics", (Option.map x.topics ~f:GuardrailTopicList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let topics =
        (Option.map ~f:GuardrailTopicList.of_xml)
          (Xml.child xml_arg0 "topics") in
      make ?topics ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let topics = field_map json__ "topics" GuardrailTopicList.of_json in
      make ?topics ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A behavior assessment of a topic policy."]
module GuardrailWordPolicyAssessment =
  struct
    type nonrec t =
      {
      customWords: GuardrailCustomWordList.t option
        [@ocaml.doc "Custom words in the assessment."];
      managedWordLists: GuardrailManagedWordList.t option
        [@ocaml.doc "Managed word lists in the assessment."]}
    let make ?customWords =
      fun ?managedWordLists -> fun () -> { customWords; managedWordLists }
    let to_value x =
      structure_to_value
        [("customWords",
           (Option.map x.customWords ~f:GuardrailCustomWordList.to_value));
        ("managedWordLists",
          (Option.map x.managedWordLists ~f:GuardrailManagedWordList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let managedWordLists =
        (Option.map ~f:GuardrailManagedWordList.of_xml)
          (Xml.child xml_arg0 "managedWordLists") in
      let customWords =
        (Option.map ~f:GuardrailCustomWordList.of_xml)
          (Xml.child xml_arg0 "customWords") in
      make ?managedWordLists ?customWords ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let managedWordLists =
        field_map json__ "managedWordLists" GuardrailManagedWordList.of_json in
      let customWords =
        field_map json__ "customWords" GuardrailCustomWordList.of_json in
      make ?managedWordLists ?customWords ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The word policy assessment."]
module AudioBlock =
  struct
    type nonrec t =
      {
      format: AudioFormat.t
        [@ocaml.doc
          "The format of the audio data, such as MP3, WAV, FLAC, or other supported audio formats."];
      source: AudioSource.t
        [@ocaml.doc
          "The source of the audio data, which can be provided as raw bytes or an S3 location."];
      error: ErrorBlock.t option
        [@ocaml.doc
          "Error information if the audio block could not be processed or contains invalid data."]}
    let context_ = "AudioBlock"
    let make ?error =
      fun ~format -> fun ~source -> fun () -> { error; format; source }
    let to_value x =
      structure_to_value
        [("format", (Some (AudioFormat.to_value x.format)));
        ("source", (Some (AudioSource.to_value x.source)));
        ("error", (Option.map x.error ~f:ErrorBlock.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let error =
        (Option.map ~f:ErrorBlock.of_xml) (Xml.child xml_arg0 "error") in
      let source =
        AudioSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "source") in
      let format =
        AudioFormat.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "format") in
      make ?error ~source ~format ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let error = field_map json__ "error" ErrorBlock.of_json in
      let source = field_map_exn json__ "source" AudioSource.of_json in
      let format = field_map_exn json__ "format" AudioFormat.of_json in
      make ?error ~source ~format ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An audio content block that contains audio data in various supported formats."]
module CachePointBlock =
  struct
    type nonrec t =
      {
      type_: CachePointType.t
        [@ocaml.doc
          "Specifies the type of cache point within the CachePointBlock."];
      ttl: CacheTTL.t option
        [@ocaml.doc
          "Optional TTL duration for cache entries. When specified, enables extended TTL caching with the specified duration. When omitted, uses type value for caching behavior."]}
    let context_ = "CachePointBlock"
    let make ?ttl = fun ~type_ -> fun () -> { ttl; type_ }
    let to_value x =
      structure_to_value
        [("type", (Some (CachePointType.to_value x.type_)));
        ("ttl", (Option.map x.ttl ~f:CacheTTL.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ttl = (Option.map ~f:CacheTTL.of_xml) (Xml.child xml_arg0 "ttl") in
      let type_ =
        CachePointType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ?ttl ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ttl = field_map json__ "ttl" CacheTTL.of_json in
      let type_ = field_map_exn json__ "type" CachePointType.of_json in
      make ?ttl ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines a section of content to be cached for reuse in subsequent API calls."]
module CitationsContentBlock =
  struct
    type nonrec t =
      {
      content: CitationGeneratedContentList.t option
        [@ocaml.doc
          "The generated content that is supported by the associated citations."];
      citations: Citations.t option
        [@ocaml.doc
          "An array of citations that reference the source documents used to generate the associated content."]}
    let make ?content = fun ?citations -> fun () -> { content; citations }
    let to_value x =
      structure_to_value
        [("content",
           (Option.map x.content ~f:CitationGeneratedContentList.to_value));
        ("citations", (Option.map x.citations ~f:Citations.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let citations =
        (Option.map ~f:Citations.of_xml) (Xml.child xml_arg0 "citations") in
      let content =
        (Option.map ~f:CitationGeneratedContentList.of_xml)
          (Xml.child xml_arg0 "content") in
      make ?citations ?content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let citations = field_map json__ "citations" Citations.of_json in
      let content =
        field_map json__ "content" CitationGeneratedContentList.of_json in
      make ?citations ?content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A content block that contains both generated text and associated citation information. This block type is returned when document citations are enabled, providing traceability between the generated content and the source documents that informed the response."]
module GuardrailConverseContentBlock =
  struct
    type nonrec t =
      {
      text: GuardrailConverseTextBlock.t option
        [@ocaml.doc "The text to guard."];
      image: GuardrailConverseImageBlock.t option
        [@ocaml.doc
          "Image within converse content block to be evaluated by the guardrail."]}
    let make ?text = fun ?image -> fun () -> { text; image }
    let to_value x =
      structure_to_value
        [("text", (Option.map x.text ~f:GuardrailConverseTextBlock.to_value));
        ("image",
          (Option.map x.image ~f:GuardrailConverseImageBlock.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let image =
        (Option.map ~f:GuardrailConverseImageBlock.of_xml)
          (Xml.child xml_arg0 "image") in
      let text =
        (Option.map ~f:GuardrailConverseTextBlock.of_xml)
          (Xml.child xml_arg0 "text") in
      make ?image ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let image =
        field_map json__ "image" GuardrailConverseImageBlock.of_json in
      let text = field_map json__ "text" GuardrailConverseTextBlock.of_json in
      make ?image ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A content block for selective guarding with the Converse or ConverseStream API operations."]
module ReasoningContentBlock =
  struct
    type nonrec t =
      {
      reasoningText: ReasoningTextBlock.t option
        [@ocaml.doc
          "The reasoning that the model used to return the output."];
      redactedContent: Blob.t option
        [@ocaml.doc
          "The content in the reasoning that was encrypted by the model provider for safety reasons. The encryption doesn't affect the quality of responses."]}
    let make ?reasoningText =
      fun ?redactedContent -> fun () -> { reasoningText; redactedContent }
    let to_value x =
      structure_to_value
        [("reasoningText",
           (Option.map x.reasoningText ~f:ReasoningTextBlock.to_value));
        ("redactedContent", (Option.map x.redactedContent ~f:Blob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let redactedContent =
        (Option.map ~f:Blob.of_xml) (Xml.child xml_arg0 "redactedContent") in
      let reasoningText =
        (Option.map ~f:ReasoningTextBlock.of_xml)
          (Xml.child xml_arg0 "reasoningText") in
      make ?redactedContent ?reasoningText ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let redactedContent = field_map json__ "redactedContent" Blob.of_json in
      let reasoningText =
        field_map json__ "reasoningText" ReasoningTextBlock.of_json in
      make ?redactedContent ?reasoningText ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains content regarding the reasoning that is carried out by the model with respect to the content in the content block. Reasoning refers to a Chain of Thought (CoT) that the model generates to enhance the accuracy of its final response."]
module ToolResultBlock =
  struct
    type nonrec t =
      {
      toolUseId: ToolUseId.t
        [@ocaml.doc
          "The ID of the tool request that this is the result for."];
      content: ToolResultContentBlocks.t
        [@ocaml.doc "The content for tool result content block."];
      status: ToolResultStatus.t option
        [@ocaml.doc
          "The status for the tool result content block. This field is only supported by Amazon Nova and Anthropic Claude 3 and 4 models."];
      type_: String_.t option
        [@ocaml.doc "The type for the tool result content block."]}
    let context_ = "ToolResultBlock"
    let make ?status =
      fun ?type_ ->
        fun ~toolUseId ->
          fun ~content -> fun () -> { status; type_; toolUseId; content }
    let to_value x =
      structure_to_value
        [("toolUseId", (Some (ToolUseId.to_value x.toolUseId)));
        ("content", (Some (ToolResultContentBlocks.to_value x.content)));
        ("status", (Option.map x.status ~f:ToolResultStatus.to_value));
        ("type", (Option.map x.type_ ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      let status =
        (Option.map ~f:ToolResultStatus.of_xml) (Xml.child xml_arg0 "status") in
      let content =
        ToolResultContentBlocks.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "content") in
      let toolUseId =
        ToolUseId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "toolUseId") in
      make ?type_ ?status ~content ~toolUseId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" String_.of_json in
      let status = field_map json__ "status" ToolResultStatus.of_json in
      let content =
        field_map_exn json__ "content" ToolResultContentBlocks.of_json in
      let toolUseId = field_map_exn json__ "toolUseId" ToolUseId.of_json in
      make ?type_ ?status ~content ~toolUseId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A tool result block that contains the results for a tool request that the model previously made. For more information, see Call a tool with the Converse API in the Amazon Bedrock User Guide."]
module ToolUseBlock =
  struct
    type nonrec t =
      {
      toolUseId: ToolUseId.t [@ocaml.doc "The ID for the tool request."];
      name: ToolName.t
        [@ocaml.doc "The name of the tool that the model wants to use."];
      input: Document.t [@ocaml.doc "The input to pass to the tool."];
      type_: ToolUseType.t option
        [@ocaml.doc "The type for the tool request."]}
    let context_ = "ToolUseBlock"
    let make ?type_ =
      fun ~toolUseId ->
        fun ~name ->
          fun ~input -> fun () -> { type_; toolUseId; name; input }
    let to_value x =
      structure_to_value
        [("toolUseId", (Some (ToolUseId.to_value x.toolUseId)));
        ("name", (Some (ToolName.to_value x.name)));
        ("input", (Some (Document.to_value x.input)));
        ("type", (Option.map x.type_ ~f:ToolUseType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:ToolUseType.of_xml) (Xml.child xml_arg0 "type") in
      let input =
        Document.of_xml (Xml.child_exn ~context:context_ xml_arg0 "input") in
      let name =
        ToolName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let toolUseId =
        ToolUseId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "toolUseId") in
      make ?type_ ~input ~name ~toolUseId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" ToolUseType.of_json in
      let input = field_map_exn json__ "input" Document.of_json in
      let name = field_map_exn json__ "name" ToolName.of_json in
      let toolUseId = field_map_exn json__ "toolUseId" ToolUseId.of_json in
      make ?type_ ~input ~name ~toolUseId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A tool use content block. Contains information about a tool that the model is requesting be run., The model uses the result from the tool to generate a response. For more information, see Call a tool with the Converse API in the Amazon Bedrock User Guide."]
module NonEmptyString =
  struct
    type nonrec t = string
    let context_ = "NonEmptyString"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NonEmptyString" j
    let to_json = simple_to_json to_value
  end
module ToolInputSchema =
  struct
    type nonrec t =
      {
      json: Document.t option
        [@ocaml.doc
          "The JSON schema for the tool. For more information, see JSON Schema Reference."]}
    let make ?json = fun () -> { json }
    let to_value x =
      structure_to_value [("json", (Option.map x.json ~f:Document.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let json = (Option.map ~f:Document.of_xml) (Xml.child xml_arg0 "json") in
      make ?json ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let json = field_map json__ "json" Document.of_json in make ?json ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The schema for the tool. The top level schema type must be object. For more information, see Call a tool with the Converse API in the Amazon Bedrock User Guide."]
module GuardrailAssessment =
  struct
    type nonrec t =
      {
      topicPolicy: GuardrailTopicPolicyAssessment.t option
        [@ocaml.doc "The topic policy."];
      contentPolicy: GuardrailContentPolicyAssessment.t option
        [@ocaml.doc "The content policy."];
      wordPolicy: GuardrailWordPolicyAssessment.t option
        [@ocaml.doc "The word policy."];
      sensitiveInformationPolicy:
        GuardrailSensitiveInformationPolicyAssessment.t option
        [@ocaml.doc "The sensitive information policy."];
      contextualGroundingPolicy:
        GuardrailContextualGroundingPolicyAssessment.t option
        [@ocaml.doc
          "The contextual grounding policy used for the guardrail assessment."];
      automatedReasoningPolicy:
        GuardrailAutomatedReasoningPolicyAssessment.t option
        [@ocaml.doc
          "The automated reasoning policy assessment results, including logical validation findings for the input content."];
      invocationMetrics: GuardrailInvocationMetrics.t option
        [@ocaml.doc "The invocation metrics for the guardrail assessment."];
      appliedGuardrailDetails: AppliedGuardrailDetails.t option }
    let make ?topicPolicy =
      fun ?contentPolicy ->
        fun ?wordPolicy ->
          fun ?sensitiveInformationPolicy ->
            fun ?contextualGroundingPolicy ->
              fun ?automatedReasoningPolicy ->
                fun ?invocationMetrics ->
                  fun ?appliedGuardrailDetails ->
                    fun () ->
                      {
                        topicPolicy;
                        contentPolicy;
                        wordPolicy;
                        sensitiveInformationPolicy;
                        contextualGroundingPolicy;
                        automatedReasoningPolicy;
                        invocationMetrics;
                        appliedGuardrailDetails
                      }
    let to_value x =
      structure_to_value
        [("topicPolicy",
           (Option.map x.topicPolicy
              ~f:GuardrailTopicPolicyAssessment.to_value));
        ("contentPolicy",
          (Option.map x.contentPolicy
             ~f:GuardrailContentPolicyAssessment.to_value));
        ("wordPolicy",
          (Option.map x.wordPolicy ~f:GuardrailWordPolicyAssessment.to_value));
        ("sensitiveInformationPolicy",
          (Option.map x.sensitiveInformationPolicy
             ~f:GuardrailSensitiveInformationPolicyAssessment.to_value));
        ("contextualGroundingPolicy",
          (Option.map x.contextualGroundingPolicy
             ~f:GuardrailContextualGroundingPolicyAssessment.to_value));
        ("automatedReasoningPolicy",
          (Option.map x.automatedReasoningPolicy
             ~f:GuardrailAutomatedReasoningPolicyAssessment.to_value));
        ("invocationMetrics",
          (Option.map x.invocationMetrics
             ~f:GuardrailInvocationMetrics.to_value));
        ("appliedGuardrailDetails",
          (Option.map x.appliedGuardrailDetails
             ~f:AppliedGuardrailDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let appliedGuardrailDetails =
        (Option.map ~f:AppliedGuardrailDetails.of_xml)
          (Xml.child xml_arg0 "appliedGuardrailDetails") in
      let invocationMetrics =
        (Option.map ~f:GuardrailInvocationMetrics.of_xml)
          (Xml.child xml_arg0 "invocationMetrics") in
      let automatedReasoningPolicy =
        (Option.map ~f:GuardrailAutomatedReasoningPolicyAssessment.of_xml)
          (Xml.child xml_arg0 "automatedReasoningPolicy") in
      let contextualGroundingPolicy =
        (Option.map ~f:GuardrailContextualGroundingPolicyAssessment.of_xml)
          (Xml.child xml_arg0 "contextualGroundingPolicy") in
      let sensitiveInformationPolicy =
        (Option.map ~f:GuardrailSensitiveInformationPolicyAssessment.of_xml)
          (Xml.child xml_arg0 "sensitiveInformationPolicy") in
      let wordPolicy =
        (Option.map ~f:GuardrailWordPolicyAssessment.of_xml)
          (Xml.child xml_arg0 "wordPolicy") in
      let contentPolicy =
        (Option.map ~f:GuardrailContentPolicyAssessment.of_xml)
          (Xml.child xml_arg0 "contentPolicy") in
      let topicPolicy =
        (Option.map ~f:GuardrailTopicPolicyAssessment.of_xml)
          (Xml.child xml_arg0 "topicPolicy") in
      make ?appliedGuardrailDetails ?invocationMetrics
        ?automatedReasoningPolicy ?contextualGroundingPolicy
        ?sensitiveInformationPolicy ?wordPolicy ?contentPolicy ?topicPolicy
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let appliedGuardrailDetails =
        field_map json__ "appliedGuardrailDetails"
          AppliedGuardrailDetails.of_json in
      let invocationMetrics =
        field_map json__ "invocationMetrics"
          GuardrailInvocationMetrics.of_json in
      let automatedReasoningPolicy =
        field_map json__ "automatedReasoningPolicy"
          GuardrailAutomatedReasoningPolicyAssessment.of_json in
      let contextualGroundingPolicy =
        field_map json__ "contextualGroundingPolicy"
          GuardrailContextualGroundingPolicyAssessment.of_json in
      let sensitiveInformationPolicy =
        field_map json__ "sensitiveInformationPolicy"
          GuardrailSensitiveInformationPolicyAssessment.of_json in
      let wordPolicy =
        field_map json__ "wordPolicy" GuardrailWordPolicyAssessment.of_json in
      let contentPolicy =
        field_map json__ "contentPolicy"
          GuardrailContentPolicyAssessment.of_json in
      let topicPolicy =
        field_map json__ "topicPolicy" GuardrailTopicPolicyAssessment.of_json in
      make ?appliedGuardrailDetails ?invocationMetrics
        ?automatedReasoningPolicy ?contextualGroundingPolicy
        ?sensitiveInformationPolicy ?wordPolicy ?contentPolicy ?topicPolicy
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A behavior assessment of the guardrail policies used in a call to the Converse API."]
module ContentBlock =
  struct
    type nonrec t =
      {
      text: String_.t option [@ocaml.doc "Text to include in the message."];
      image: ImageBlock.t option
        [@ocaml.doc
          "Image to include in the message. This field is only supported by Anthropic Claude 3 models."];
      document: DocumentBlock.t option
        [@ocaml.doc "A document to include in the message."];
      video: VideoBlock.t option
        [@ocaml.doc "Video to include in the message."];
      audio: AudioBlock.t option
        [@ocaml.doc
          "An audio content block containing audio data in the conversation."];
      toolUse: ToolUseBlock.t option
        [@ocaml.doc "Information about a tool use request from a model."];
      toolResult: ToolResultBlock.t option
        [@ocaml.doc "The result for a tool request that a model makes."];
      guardContent: GuardrailConverseContentBlock.t option
        [@ocaml.doc
          "Contains the content to assess with the guardrail. If you don't specify guardContent in a call to the Converse API, the guardrail (if passed in the Converse API) assesses the entire message. For more information, see Use a guardrail with the Converse API in the Amazon Bedrock User Guide."];
      cachePoint: CachePointBlock.t option
        [@ocaml.doc "CachePoint to include in the message."];
      reasoningContent: ReasoningContentBlock.t option
        [@ocaml.doc
          "Contains content regarding the reasoning that is carried out by the model. Reasoning refers to a Chain of Thought (CoT) that the model generates to enhance the accuracy of its final response."];
      citationsContent: CitationsContentBlock.t option
        [@ocaml.doc
          "A content block that contains both generated text and associated citation information, providing traceability between the response and source documents."];
      searchResult: SearchResultBlock.t option
        [@ocaml.doc "Search result to include in the message."]}
    let make ?text =
      fun ?image ->
        fun ?document ->
          fun ?video ->
            fun ?audio ->
              fun ?toolUse ->
                fun ?toolResult ->
                  fun ?guardContent ->
                    fun ?cachePoint ->
                      fun ?reasoningContent ->
                        fun ?citationsContent ->
                          fun ?searchResult ->
                            fun () ->
                              {
                                text;
                                image;
                                document;
                                video;
                                audio;
                                toolUse;
                                toolResult;
                                guardContent;
                                cachePoint;
                                reasoningContent;
                                citationsContent;
                                searchResult
                              }
    let to_value x =
      structure_to_value
        [("text", (Option.map x.text ~f:String_.to_value));
        ("image", (Option.map x.image ~f:ImageBlock.to_value));
        ("document", (Option.map x.document ~f:DocumentBlock.to_value));
        ("video", (Option.map x.video ~f:VideoBlock.to_value));
        ("audio", (Option.map x.audio ~f:AudioBlock.to_value));
        ("toolUse", (Option.map x.toolUse ~f:ToolUseBlock.to_value));
        ("toolResult", (Option.map x.toolResult ~f:ToolResultBlock.to_value));
        ("guardContent",
          (Option.map x.guardContent
             ~f:GuardrailConverseContentBlock.to_value));
        ("cachePoint", (Option.map x.cachePoint ~f:CachePointBlock.to_value));
        ("reasoningContent",
          (Option.map x.reasoningContent ~f:ReasoningContentBlock.to_value));
        ("citationsContent",
          (Option.map x.citationsContent ~f:CitationsContentBlock.to_value));
        ("searchResult",
          (Option.map x.searchResult ~f:SearchResultBlock.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let searchResult =
        (Option.map ~f:SearchResultBlock.of_xml)
          (Xml.child xml_arg0 "searchResult") in
      let citationsContent =
        (Option.map ~f:CitationsContentBlock.of_xml)
          (Xml.child xml_arg0 "citationsContent") in
      let reasoningContent =
        (Option.map ~f:ReasoningContentBlock.of_xml)
          (Xml.child xml_arg0 "reasoningContent") in
      let cachePoint =
        (Option.map ~f:CachePointBlock.of_xml)
          (Xml.child xml_arg0 "cachePoint") in
      let guardContent =
        (Option.map ~f:GuardrailConverseContentBlock.of_xml)
          (Xml.child xml_arg0 "guardContent") in
      let toolResult =
        (Option.map ~f:ToolResultBlock.of_xml)
          (Xml.child xml_arg0 "toolResult") in
      let toolUse =
        (Option.map ~f:ToolUseBlock.of_xml) (Xml.child xml_arg0 "toolUse") in
      let audio =
        (Option.map ~f:AudioBlock.of_xml) (Xml.child xml_arg0 "audio") in
      let video =
        (Option.map ~f:VideoBlock.of_xml) (Xml.child xml_arg0 "video") in
      let document =
        (Option.map ~f:DocumentBlock.of_xml) (Xml.child xml_arg0 "document") in
      let image =
        (Option.map ~f:ImageBlock.of_xml) (Xml.child xml_arg0 "image") in
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?searchResult ?citationsContent ?reasoningContent ?cachePoint
        ?guardContent ?toolResult ?toolUse ?audio ?video ?document ?image
        ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let searchResult =
        field_map json__ "searchResult" SearchResultBlock.of_json in
      let citationsContent =
        field_map json__ "citationsContent" CitationsContentBlock.of_json in
      let reasoningContent =
        field_map json__ "reasoningContent" ReasoningContentBlock.of_json in
      let cachePoint = field_map json__ "cachePoint" CachePointBlock.of_json in
      let guardContent =
        field_map json__ "guardContent" GuardrailConverseContentBlock.of_json in
      let toolResult = field_map json__ "toolResult" ToolResultBlock.of_json in
      let toolUse = field_map json__ "toolUse" ToolUseBlock.of_json in
      let audio = field_map json__ "audio" AudioBlock.of_json in
      let video = field_map json__ "video" VideoBlock.of_json in
      let document = field_map json__ "document" DocumentBlock.of_json in
      let image = field_map json__ "image" ImageBlock.of_json in
      let text = field_map json__ "text" String_.of_json in
      make ?searchResult ?citationsContent ?reasoningContent ?cachePoint
        ?guardContent ?toolResult ?toolUse ?audio ?video ?document ?image
        ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A block of content for a message that you pass to, or receive from, a model with the Converse or ConverseStream API operations."]
module SystemTool =
  struct
    type nonrec t =
      {
      name: ToolName.t
        [@ocaml.doc
          "The name of the system-defined tool that you want to call."]}
    let context_ = "SystemTool"
    let make ~name = fun () -> { name }
    let to_value x =
      structure_to_value [("name", (Some (ToolName.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        ToolName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map_exn json__ "name" ToolName.of_json in
      make ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a system-defined tool for the model to use. System-defined tools are tools that are created and provided by the model provider."]
module ToolSpecification =
  struct
    type nonrec t =
      {
      name: ToolName.t [@ocaml.doc "The name for the tool."];
      description: NonEmptyString.t option
        [@ocaml.doc "The description for the tool."];
      inputSchema: ToolInputSchema.t
        [@ocaml.doc "The input schema for the tool in JSON format."];
      strict: Boolean.t option
        [@ocaml.doc
          "Flag to enable structured output enforcement on a tool usage response."]}
    let context_ = "ToolSpecification"
    let make ?description =
      fun ?strict ->
        fun ~name ->
          fun ~inputSchema ->
            fun () -> { description; strict; name; inputSchema }
    let to_value x =
      structure_to_value
        [("name", (Some (ToolName.to_value x.name)));
        ("description",
          (Option.map x.description ~f:NonEmptyString.to_value));
        ("inputSchema", (Some (ToolInputSchema.to_value x.inputSchema)));
        ("strict", (Option.map x.strict ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let strict =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "strict") in
      let inputSchema =
        ToolInputSchema.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "inputSchema") in
      let description =
        (Option.map ~f:NonEmptyString.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        ToolName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?strict ~inputSchema ?description ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let strict = field_map json__ "strict" Boolean.of_json in
      let inputSchema =
        field_map_exn json__ "inputSchema" ToolInputSchema.of_json in
      let description = field_map json__ "description" NonEmptyString.of_json in
      let name = field_map_exn json__ "name" ToolName.of_json in
      make ?strict ~inputSchema ?description ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specification for the tool. For more information, see Call a tool with the Converse API in the Amazon Bedrock User Guide."]
module CitationSourceContentDelta =
  struct
    type nonrec t =
      {
      text: String_.t option
        [@ocaml.doc
          "An incremental update to the text content from the source document that is being cited."]}
    let make ?text = fun () -> { text }
    let to_value x =
      structure_to_value [("text", (Option.map x.text ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" String_.of_json in make ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains incremental updates to the source content text during streaming responses, allowing clients to build up the cited content progressively."]
module GuardrailAssessmentList =
  struct
    type nonrec t = GuardrailAssessment.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailAssessment.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailAssessment.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailAssessmentList"
        ~of_json:GuardrailAssessment.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailOutputText =
  struct
    type nonrec t = string
    let context_ = "GuardrailOutputText"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GuardrailOutputText" j
    let to_json = simple_to_json to_value
  end
module CacheDetailInputTokensInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for CacheDetailInputTokensInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module KmsKeyId =
  struct
    type nonrec t = string
    let context_ = "KmsKeyId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(-[^:]+)?:kms:[a-zA-Z0-9-]*:[0-9]{12}:((key/[a-zA-Z0-9-]{36})|(alias/[a-zA-Z0-9-_/]+))")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KmsKeyId" j
    let to_json = simple_to_json to_value
  end
module ContentBlocks =
  struct
    type nonrec t = ContentBlock.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContentBlock.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContentBlock.of_xml)
    let of_json j =
      list_of_json ~kind:"ContentBlocks" ~of_json:ContentBlock.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConversationRole =
  struct
    type nonrec t =
      | User 
      | Assistant 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | User -> "user"
      | Assistant -> "assistant"
      | Non_static_id s -> s
    let of_string =
      function
      | "user" -> User
      | "assistant" -> Assistant
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ConversationRole" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ConversationRole" j)
    let to_json = simple_to_json to_value
  end
module AnyToolChoice =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The model must request at least one tool (no text is generated). For example, \\{\"any\" : \\{\\}\\}. For more information, see Call a tool with the Converse API in the Amazon Bedrock User Guide."]
module AutoToolChoice =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Model automatically decides if a tool should be called or whether to generate text instead. For example, \\{\"auto\" : \\{\\}\\}. For more information, see Call a tool with the Converse API in the Amazon Bedrock User Guide"]
module SpecificToolChoice =
  struct
    type nonrec t =
      {
      name: ToolName.t
        [@ocaml.doc "The name of the tool that the model must request."]}
    let context_ = "SpecificToolChoice"
    let make ~name = fun () -> { name }
    let to_value x =
      structure_to_value [("name", (Some (ToolName.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        ToolName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map_exn json__ "name" ToolName.of_json in
      make ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The model must request a specific tool. For example, \\{\"tool\" : \\{\"name\" : \"Your tool name\"\\}\\}. For more information, see Call a tool with the Converse API in the Amazon Bedrock User Guide This field is only supported by Anthropic Claude 3 models."]
module Tool =
  struct
    type nonrec t =
      {
      toolSpec: ToolSpecification.t option
        [@ocaml.doc "The specfication for the tool."];
      systemTool: SystemTool.t option
        [@ocaml.doc "Specifies the system-defined tool that you want use."];
      cachePoint: CachePointBlock.t option
        [@ocaml.doc "CachePoint to include in the tool configuration."]}
    let make ?toolSpec =
      fun ?systemTool ->
        fun ?cachePoint -> fun () -> { toolSpec; systemTool; cachePoint }
    let to_value x =
      structure_to_value
        [("toolSpec", (Option.map x.toolSpec ~f:ToolSpecification.to_value));
        ("systemTool", (Option.map x.systemTool ~f:SystemTool.to_value));
        ("cachePoint", (Option.map x.cachePoint ~f:CachePointBlock.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cachePoint =
        (Option.map ~f:CachePointBlock.of_xml)
          (Xml.child xml_arg0 "cachePoint") in
      let systemTool =
        (Option.map ~f:SystemTool.of_xml) (Xml.child xml_arg0 "systemTool") in
      let toolSpec =
        (Option.map ~f:ToolSpecification.of_xml)
          (Xml.child xml_arg0 "toolSpec") in
      make ?cachePoint ?systemTool ?toolSpec ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cachePoint = field_map json__ "cachePoint" CachePointBlock.of_json in
      let systemTool = field_map json__ "systemTool" SystemTool.of_json in
      let toolSpec = field_map json__ "toolSpec" ToolSpecification.of_json in
      make ?cachePoint ?systemTool ?toolSpec ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a tool that you can use with the Converse API. For more information, see Call a tool with the Converse API in the Amazon Bedrock User Guide."]
module CitationSourceContentListDelta =
  struct
    type nonrec t = CitationSourceContentDelta.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CitationSourceContentDelta.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CitationSourceContentDelta.of_xml)
    let of_json j =
      list_of_json ~kind:"CitationSourceContentListDelta"
        ~of_json:CitationSourceContentDelta.of_json j
    let to_json v = composed_to_json to_value v
  end
module ToolResultBlockDelta =
  struct
    type nonrec t =
      {
      text: String_.t option
        [@ocaml.doc "The reasoning the model used to return the output."];
      json: Document.t option
        [@ocaml.doc
          "The JSON schema for the tool result content block. see JSON Schema Reference."]}
    let make ?text = fun ?json -> fun () -> { text; json }
    let to_value x =
      structure_to_value
        [("text", (Option.map x.text ~f:String_.to_value));
        ("json", (Option.map x.json ~f:Document.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let json = (Option.map ~f:Document.of_xml) (Xml.child xml_arg0 "json") in
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?json ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let json = field_map json__ "json" Document.of_json in
      let text = field_map json__ "text" String_.of_json in
      make ?json ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains incremental updates to tool results information during streaming responses. This allows clients to build up tool results data progressively as the response is generated."]
module GuardrailAssessmentListMap =
  struct
    type nonrec t = (String_.t * GuardrailAssessmentList.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types String GuardrailAssessmentList"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x ->
                       (GuardrailAssessmentList.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:GuardrailAssessmentList.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailAssessmentMap =
  struct
    type nonrec t = (String_.t * GuardrailAssessment.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types String GuardrailAssessment"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x ->
                       (GuardrailAssessment.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:GuardrailAssessment.of_json j
    let to_json v = composed_to_json to_value v
  end
module ModelOutputs =
  struct
    type nonrec t = GuardrailOutputText.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailOutputText.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailOutputText.of_xml)
    let of_json j =
      list_of_json ~kind:"ModelOutputs" ~of_json:GuardrailOutputText.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module InvokedModelId =
  struct
    type nonrec t = string
    let context_ = "InvokedModelId"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"(arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}::foundation-model/[a-z0-9-]{1,63}[.]{1}[a-z0-9-]{1,63}([a-z0-9-]{1,63}[.]){0,2}[a-z0-9-]{1,63}([:][a-z0-9-]{1,63}){0,2})|(arn:aws(|-us-gov|-cn|-iso|-iso-b):bedrock:(|[0-9a-z-]{1,20}):(|[0-9]{12}):inference-profile/[a-zA-Z0-9-:.]+)");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InvokedModelId" j
    let to_json = simple_to_json to_value
  end
module CacheDetail =
  struct
    type nonrec t =
      {
      ttl: CacheTTL.t option
        [@ocaml.doc "TTL duration for these cached tokens"];
      inputTokens: CacheDetailInputTokensInteger.t option
        [@ocaml.doc
          "Number of tokens written to cache with this TTL (cache creation tokens)"]}
    let make ?ttl = fun ?inputTokens -> fun () -> { ttl; inputTokens }
    let to_value x =
      structure_to_value
        [("ttl", (Option.map x.ttl ~f:CacheTTL.to_value));
        ("inputTokens",
          (Option.map x.inputTokens ~f:CacheDetailInputTokensInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let inputTokens =
        (Option.map ~f:CacheDetailInputTokensInteger.of_xml)
          (Xml.child xml_arg0 "inputTokens") in
      let ttl = (Option.map ~f:CacheTTL.of_xml) (Xml.child xml_arg0 "ttl") in
      make ?inputTokens ?ttl ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let inputTokens =
        field_map json__ "inputTokens" CacheDetailInputTokensInteger.of_json in
      let ttl = field_map json__ "ttl" CacheTTL.of_json in
      make ?inputTokens ?ttl ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Cache creation metrics for a specific TTL duration"]
module GuardrailImageSourceBytesBlob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module GuardrailContentQualifier =
  struct
    type nonrec t =
      | Grounding_source 
      | Query 
      | Guard_content 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Grounding_source -> "grounding_source"
      | Query -> "query"
      | Guard_content -> "guard_content"
      | Non_static_id s -> s
    let of_string =
      function
      | "grounding_source" -> Grounding_source
      | "query" -> Query
      | "guard_content" -> Guard_content
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailContentQualifier" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailContentQualifier" j)
    let to_json = simple_to_json to_value
  end
module AsyncInvokeS3OutputDataConfig =
  struct
    type nonrec t =
      {
      s3Uri: S3Uri.t [@ocaml.doc "An object URI starting with s3://."];
      kmsKeyId: KmsKeyId.t option [@ocaml.doc "A KMS encryption key ID."];
      bucketOwner: AccountId.t option
        [@ocaml.doc
          "If the bucket belongs to another AWS account, specify that account's ID."]}
    let context_ = "AsyncInvokeS3OutputDataConfig"
    let make ?kmsKeyId =
      fun ?bucketOwner ->
        fun ~s3Uri -> fun () -> { kmsKeyId; bucketOwner; s3Uri }
    let to_value x =
      structure_to_value
        [("s3Uri", (Some (S3Uri.to_value x.s3Uri)));
        ("kmsKeyId", (Option.map x.kmsKeyId ~f:KmsKeyId.to_value));
        ("bucketOwner", (Option.map x.bucketOwner ~f:AccountId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bucketOwner =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "bucketOwner") in
      let kmsKeyId =
        (Option.map ~f:KmsKeyId.of_xml) (Xml.child xml_arg0 "kmsKeyId") in
      let s3Uri =
        S3Uri.of_xml (Xml.child_exn ~context:context_ xml_arg0 "s3Uri") in
      make ?bucketOwner ?kmsKeyId ~s3Uri ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bucketOwner = field_map json__ "bucketOwner" AccountId.of_json in
      let kmsKeyId = field_map json__ "kmsKeyId" KmsKeyId.of_json in
      let s3Uri = field_map_exn json__ "s3Uri" S3Uri.of_json in
      make ?bucketOwner ?kmsKeyId ~s3Uri ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Asynchronous invocation output data settings."]
module Message =
  struct
    type nonrec t =
      {
      role: ConversationRole.t
        [@ocaml.doc "The role that the message plays in the message."];
      content: ContentBlocks.t
        [@ocaml.doc
          "The message content. Note the following restrictions: You can include up to 20 images. Each image's size, height, and width must be no more than 3.75 MB, 8000 px, and 8000 px, respectively. You can include up to five documents. Each document's size must be no more than 4.5 MB. If you include a ContentBlock with a document field in the array, you must also include a ContentBlock with a text field. You can only include images and documents if the role is user."]}
    let context_ = "Message"
    let make ~role = fun ~content -> fun () -> { role; content }
    let to_value x =
      structure_to_value
        [("role", (Some (ConversationRole.to_value x.role)));
        ("content", (Some (ContentBlocks.to_value x.content)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let content =
        ContentBlocks.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "content") in
      let role =
        ConversationRole.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "role") in
      make ~content ~role ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let content = field_map_exn json__ "content" ContentBlocks.of_json in
      let role = field_map_exn json__ "role" ConversationRole.of_json in
      make ~content ~role ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A message input, or returned from, a call to Converse or ConverseStream."]
module SystemContentBlock =
  struct
    type nonrec t =
      {
      text: NonEmptyString.t option
        [@ocaml.doc "A system prompt for the model."];
      guardContent: GuardrailConverseContentBlock.t option
        [@ocaml.doc
          "A content block to assess with the guardrail. Use with the Converse or ConverseStream API operations. For more information, see Use a guardrail with the Converse API in the Amazon Bedrock User Guide."];
      cachePoint: CachePointBlock.t option
        [@ocaml.doc "CachePoint to include in the system prompt."]}
    let make ?text =
      fun ?guardContent ->
        fun ?cachePoint -> fun () -> { text; guardContent; cachePoint }
    let to_value x =
      structure_to_value
        [("text", (Option.map x.text ~f:NonEmptyString.to_value));
        ("guardContent",
          (Option.map x.guardContent
             ~f:GuardrailConverseContentBlock.to_value));
        ("cachePoint", (Option.map x.cachePoint ~f:CachePointBlock.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cachePoint =
        (Option.map ~f:CachePointBlock.of_xml)
          (Xml.child xml_arg0 "cachePoint") in
      let guardContent =
        (Option.map ~f:GuardrailConverseContentBlock.of_xml)
          (Xml.child xml_arg0 "guardContent") in
      let text =
        (Option.map ~f:NonEmptyString.of_xml) (Xml.child xml_arg0 "text") in
      make ?cachePoint ?guardContent ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cachePoint = field_map json__ "cachePoint" CachePointBlock.of_json in
      let guardContent =
        field_map json__ "guardContent" GuardrailConverseContentBlock.of_json in
      let text = field_map json__ "text" NonEmptyString.of_json in
      make ?cachePoint ?guardContent ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations for instructions to provide the model for how to handle input. To learn more, see Using the Converse API."]
module ToolChoice =
  struct
    type nonrec t =
      {
      auto: AutoToolChoice.t option
        [@ocaml.doc
          "(Default). The Model automatically decides if a tool should be called or whether to generate text instead."];
      any: AnyToolChoice.t option
        [@ocaml.doc
          "The model must request at least one tool (no text is generated)."];
      tool: SpecificToolChoice.t option
        [@ocaml.doc
          "The Model must request the specified tool. Only supported by Anthropic Claude 3 and Amazon Nova models."]}
    let make ?auto = fun ?any -> fun ?tool -> fun () -> { auto; any; tool }
    let to_value x =
      structure_to_value
        [("auto", (Option.map x.auto ~f:AutoToolChoice.to_value));
        ("any", (Option.map x.any ~f:AnyToolChoice.to_value));
        ("tool", (Option.map x.tool ~f:SpecificToolChoice.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tool =
        (Option.map ~f:SpecificToolChoice.of_xml) (Xml.child xml_arg0 "tool") in
      let any =
        (Option.map ~f:AnyToolChoice.of_xml) (Xml.child xml_arg0 "any") in
      let auto =
        (Option.map ~f:AutoToolChoice.of_xml) (Xml.child xml_arg0 "auto") in
      make ?tool ?any ?auto ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tool = field_map json__ "tool" SpecificToolChoice.of_json in
      let any = field_map json__ "any" AnyToolChoice.of_json in
      let auto = field_map json__ "auto" AutoToolChoice.of_json in
      make ?tool ?any ?auto ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Determines which tools the model should request in a call to Converse or ConverseStream. For more information, see Call a tool with the Converse API in the Amazon Bedrock User Guide."]
module ToolConfigurationToolsList =
  struct
    type nonrec t = Tool.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Tool.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tool.of_xml)
    let of_json j =
      list_of_json ~kind:"ToolConfigurationToolsList" ~of_json:Tool.of_json j
    let to_json v = composed_to_json to_value v
  end
module CitationsDelta =
  struct
    type nonrec t =
      {
      title: String_.t option
        [@ocaml.doc
          "The title or identifier of the source document being cited."];
      source: String_.t option
        [@ocaml.doc
          "The source from the original search result that provided the cited content."];
      sourceContent: CitationSourceContentListDelta.t option
        [@ocaml.doc
          "The specific content from the source document that was referenced or cited in the generated response."];
      location: CitationLocation.t option
        [@ocaml.doc
          "Specifies the precise location within a source document where cited content can be found. This can include character-level positions, page numbers, or document chunks depending on the document type and indexing method."]}
    let make ?title =
      fun ?source ->
        fun ?sourceContent ->
          fun ?location ->
            fun () -> { title; source; sourceContent; location }
    let to_value x =
      structure_to_value
        [("title", (Option.map x.title ~f:String_.to_value));
        ("source", (Option.map x.source ~f:String_.to_value));
        ("sourceContent",
          (Option.map x.sourceContent
             ~f:CitationSourceContentListDelta.to_value));
        ("location", (Option.map x.location ~f:CitationLocation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:CitationLocation.of_xml)
          (Xml.child xml_arg0 "location") in
      let sourceContent =
        (Option.map ~f:CitationSourceContentListDelta.of_xml)
          (Xml.child xml_arg0 "sourceContent") in
      let source =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "source") in
      let title = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "title") in
      make ?location ?sourceContent ?source ?title ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location = field_map json__ "location" CitationLocation.of_json in
      let sourceContent =
        field_map json__ "sourceContent"
          CitationSourceContentListDelta.of_json in
      let source = field_map json__ "source" String_.of_json in
      let title = field_map json__ "title" String_.of_json in
      make ?location ?sourceContent ?source ?title ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains incremental updates to citation information during streaming responses. This allows clients to build up citation data progressively as the response is generated."]
module ImageBlockDelta =
  struct
    type nonrec t =
      {
      source: ImageSource.t option
        [@ocaml.doc
          "The incremental image source data for this delta event."];
      error: ErrorBlock.t option
        [@ocaml.doc
          "Error information if this image delta could not be processed."]}
    let make ?source = fun ?error -> fun () -> { source; error }
    let to_value x =
      structure_to_value
        [("source", (Option.map x.source ~f:ImageSource.to_value));
        ("error", (Option.map x.error ~f:ErrorBlock.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let error =
        (Option.map ~f:ErrorBlock.of_xml) (Xml.child xml_arg0 "error") in
      let source =
        (Option.map ~f:ImageSource.of_xml) (Xml.child xml_arg0 "source") in
      make ?error ?source ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let error = field_map json__ "error" ErrorBlock.of_json in
      let source = field_map json__ "source" ImageSource.of_json in
      make ?error ?source ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A streaming delta event that contains incremental image data during streaming responses."]
module ReasoningContentBlockDelta =
  struct
    type nonrec t =
      {
      text: String_.t option
        [@ocaml.doc
          "The reasoning that the model used to return the output."];
      redactedContent: Blob.t option
        [@ocaml.doc
          "The content in the reasoning that was encrypted by the model provider for safety reasons. The encryption doesn't affect the quality of responses."];
      signature: String_.t option
        [@ocaml.doc
          "A token that verifies that the reasoning text was generated by the model. If you pass a reasoning block back to the API in a multi-turn conversation, include the text and its signature unmodified."]}
    let make ?text =
      fun ?redactedContent ->
        fun ?signature -> fun () -> { text; redactedContent; signature }
    let to_value x =
      structure_to_value
        [("text", (Option.map x.text ~f:String_.to_value));
        ("redactedContent", (Option.map x.redactedContent ~f:Blob.to_value));
        ("signature", (Option.map x.signature ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let signature =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "signature") in
      let redactedContent =
        (Option.map ~f:Blob.of_xml) (Xml.child xml_arg0 "redactedContent") in
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?signature ?redactedContent ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let signature = field_map json__ "signature" String_.of_json in
      let redactedContent = field_map json__ "redactedContent" Blob.of_json in
      let text = field_map json__ "text" String_.of_json in
      make ?signature ?redactedContent ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains content regarding the reasoning that is carried out by the model with respect to the content in the content block. Reasoning refers to a Chain of Thought (CoT) that the model generates to enhance the accuracy of its final response."]
module ToolResultBlocksDelta =
  struct
    type nonrec t = ToolResultBlockDelta.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ToolResultBlockDelta.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ToolResultBlockDelta.of_xml)
    let of_json j =
      list_of_json ~kind:"ToolResultBlocksDelta"
        ~of_json:ToolResultBlockDelta.of_json j
    let to_json v = composed_to_json to_value v
  end
module ToolUseBlockDelta =
  struct
    type nonrec t =
      {
      input: String_.t option [@ocaml.doc "The input for a requested tool."]}
    let make ?input = fun () -> { input }
    let to_value x =
      structure_to_value
        [("input", (Option.map x.input ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let input = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "input") in
      make ?input ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let input = field_map json__ "input" String_.of_json in make ?input ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The delta for a tool use block."]
module ImageBlockStart =
  struct
    type nonrec t =
      {
      format: ImageFormat.t option
        [@ocaml.doc
          "The format of the image data that will be streamed in subsequent delta events."]}
    let make ?format = fun () -> { format }
    let to_value x =
      structure_to_value
        [("format", (Option.map x.format ~f:ImageFormat.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let format =
        (Option.map ~f:ImageFormat.of_xml) (Xml.child xml_arg0 "format") in
      make ?format ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let format = field_map json__ "format" ImageFormat.of_json in
      make ?format ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The initial event in a streaming image block that indicates the start of image content."]
module ToolResultBlockStart =
  struct
    type nonrec t =
      {
      toolUseId: ToolUseId.t option
        [@ocaml.doc
          "The ID of the tool that was used to generate this tool result block."];
      type_: String_.t option
        [@ocaml.doc
          "The type for the tool that was used to generate this tool result block."];
      status: ToolResultStatus.t option
        [@ocaml.doc "The status of the tool result block."]}
    let make ?toolUseId =
      fun ?type_ -> fun ?status -> fun () -> { toolUseId; type_; status }
    let to_value x =
      structure_to_value
        [("toolUseId", (Option.map x.toolUseId ~f:ToolUseId.to_value));
        ("type", (Option.map x.type_ ~f:String_.to_value));
        ("status", (Option.map x.status ~f:ToolResultStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ToolResultStatus.of_xml) (Xml.child xml_arg0 "status") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      let toolUseId =
        (Option.map ~f:ToolUseId.of_xml) (Xml.child xml_arg0 "toolUseId") in
      make ?status ?type_ ?toolUseId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" ToolResultStatus.of_json in
      let type_ = field_map json__ "type" String_.of_json in
      let toolUseId = field_map json__ "toolUseId" ToolUseId.of_json in
      make ?status ?type_ ?toolUseId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The start of a tool result block. For more information, see Call a tool with the Converse API in the Amazon Bedrock User Guide."]
module ToolUseBlockStart =
  struct
    type nonrec t =
      {
      toolUseId: ToolUseId.t option
        [@ocaml.doc "The ID for the tool request."];
      name: ToolName.t option
        [@ocaml.doc
          "The name of the tool that the model is requesting to use."];
      type_: ToolUseType.t option
        [@ocaml.doc "The type for the tool request."]}
    let make ?toolUseId =
      fun ?name -> fun ?type_ -> fun () -> { toolUseId; name; type_ }
    let to_value x =
      structure_to_value
        [("toolUseId", (Option.map x.toolUseId ~f:ToolUseId.to_value));
        ("name", (Option.map x.name ~f:ToolName.to_value));
        ("type", (Option.map x.type_ ~f:ToolUseType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:ToolUseType.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:ToolName.of_xml) (Xml.child xml_arg0 "name") in
      let toolUseId =
        (Option.map ~f:ToolUseId.of_xml) (Xml.child xml_arg0 "toolUseId") in
      make ?type_ ?name ?toolUseId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" ToolUseType.of_json in
      let name = field_map json__ "name" ToolName.of_json in
      let toolUseId = field_map json__ "toolUseId" ToolUseId.of_json in
      make ?type_ ?name ?toolUseId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The start of a tool use block. For more information, see Call a tool with the Converse API in the Amazon Bedrock User Guide."]
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module GuardrailTraceAssessment =
  struct
    type nonrec t =
      {
      modelOutput: ModelOutputs.t option
        [@ocaml.doc "The output from the model."];
      inputAssessment: GuardrailAssessmentMap.t option
        [@ocaml.doc "The input assessment."];
      outputAssessments: GuardrailAssessmentListMap.t option
        [@ocaml.doc "the output assessments."];
      actionReason: String_.t option
        [@ocaml.doc
          "Provides the reason for the action taken when harmful content is detected."]}
    let make ?modelOutput =
      fun ?inputAssessment ->
        fun ?outputAssessments ->
          fun ?actionReason ->
            fun () ->
              { modelOutput; inputAssessment; outputAssessments; actionReason
              }
    let to_value x =
      structure_to_value
        [("modelOutput", (Option.map x.modelOutput ~f:ModelOutputs.to_value));
        ("inputAssessment",
          (Option.map x.inputAssessment ~f:GuardrailAssessmentMap.to_value));
        ("outputAssessments",
          (Option.map x.outputAssessments
             ~f:GuardrailAssessmentListMap.to_value));
        ("actionReason", (Option.map x.actionReason ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let actionReason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "actionReason") in
      let outputAssessments =
        (Option.map ~f:GuardrailAssessmentListMap.of_xml)
          (Xml.child xml_arg0 "outputAssessments") in
      let inputAssessment =
        (Option.map ~f:GuardrailAssessmentMap.of_xml)
          (Xml.child xml_arg0 "inputAssessment") in
      let modelOutput =
        (Option.map ~f:ModelOutputs.of_xml)
          (Xml.child xml_arg0 "modelOutput") in
      make ?actionReason ?outputAssessments ?inputAssessment ?modelOutput ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let actionReason = field_map json__ "actionReason" String_.of_json in
      let outputAssessments =
        field_map json__ "outputAssessments"
          GuardrailAssessmentListMap.of_json in
      let inputAssessment =
        field_map json__ "inputAssessment" GuardrailAssessmentMap.of_json in
      let modelOutput = field_map json__ "modelOutput" ModelOutputs.of_json in
      make ?actionReason ?outputAssessments ?inputAssessment ?modelOutput ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A Top level guardrail trace object. For more information, see ConverseTrace."]
module PromptRouterTrace =
  struct
    type nonrec t =
      {
      invokedModelId: InvokedModelId.t option
        [@ocaml.doc "The ID of the invoked model."]}
    let make ?invokedModelId = fun () -> { invokedModelId }
    let to_value x =
      structure_to_value
        [("invokedModelId",
           (Option.map x.invokedModelId ~f:InvokedModelId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let invokedModelId =
        (Option.map ~f:InvokedModelId.of_xml)
          (Xml.child xml_arg0 "invokedModelId") in
      make ?invokedModelId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let invokedModelId =
        field_map json__ "invokedModelId" InvokedModelId.of_json in
      make ?invokedModelId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A prompt router trace."]
module PerformanceConfigLatency =
  struct
    type nonrec t =
      | Standard 
      | Optimized 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Standard -> "standard"
      | Optimized -> "optimized"
      | Non_static_id s -> s
    let of_string =
      function
      | "standard" -> Standard
      | "optimized" -> Optimized
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PerformanceConfigLatency" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PerformanceConfigLatency" j)
    let to_json = simple_to_json to_value
  end
module ServiceTierType =
  struct
    type nonrec t =
      | Priority 
      | Default 
      | Flex 
      | Reserved 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Priority -> "priority"
      | Default -> "default"
      | Flex -> "flex"
      | Reserved -> "reserved"
      | Non_static_id s -> s
    let of_string =
      function
      | "priority" -> Priority
      | "default" -> Default
      | "flex" -> Flex
      | "reserved" -> Reserved
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ServiceTierType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ServiceTierType" j)
    let to_json = simple_to_json to_value
  end
module CacheDetailsList =
  struct
    type nonrec t = CacheDetail.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CacheDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CacheDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"CacheDetailsList" ~of_json:CacheDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module TokenUsageCacheReadInputTokensInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for TokenUsageCacheReadInputTokensInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TokenUsageCacheWriteInputTokensInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for TokenUsageCacheWriteInputTokensInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TokenUsageInputTokensInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for TokenUsageInputTokensInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TokenUsageOutputTokensInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for TokenUsageOutputTokensInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TokenUsageTotalTokensInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for TokenUsageTotalTokensInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module JsonSchemaDefinition =
  struct
    type nonrec t =
      {
      schema: String_.t
        [@ocaml.doc
          "The JSON schema to constrain the model's output. For more information, see JSON Schema Reference."];
      name: String_.t option [@ocaml.doc "The name of the JSON schema."];
      description: String_.t option
        [@ocaml.doc "A description of the JSON schema."]}
    let context_ = "JsonSchemaDefinition"
    let make ?name =
      fun ?description ->
        fun ~schema -> fun () -> { name; description; schema }
    let to_value x =
      structure_to_value
        [("schema", (Some (String_.to_value x.schema)));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let schema =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "schema") in
      make ?description ?name ~schema ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description = field_map json__ "description" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let schema = field_map_exn json__ "schema" String_.of_json in
      make ?description ?name ~schema ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "JSON schema structured output format options."]
module GuardrailImageFormat =
  struct
    type nonrec t =
      | Png 
      | Jpeg 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Png -> "png" | Jpeg -> "jpeg" | Non_static_id s -> s
    let of_string =
      function | "png" -> Png | "jpeg" -> Jpeg | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailImageFormat" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GuardrailImageFormat" j)
    let to_json = simple_to_json to_value
  end
module GuardrailImageSource =
  struct
    type nonrec t =
      {
      bytes: GuardrailImageSourceBytesBlob.t option
        [@ocaml.doc
          "The bytes details of the guardrail image source. Object used in independent api."]}
    let make ?bytes = fun () -> { bytes }
    let of_header_and_body = ((fun (xs, pipe) -> make ?bytes:(Some pipe) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("bytes",
           (Option.map x.bytes ~f:GuardrailImageSourceBytesBlob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bytes =
        (Option.map ~f:GuardrailImageSourceBytesBlob.of_xml)
          (Xml.child xml_arg0 "bytes") in
      make ?bytes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bytes =
        field_map json__ "bytes" GuardrailImageSourceBytesBlob.of_json in
      make ?bytes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The image source (image bytes) of the guardrail image source. Object used in independent api."]
module GuardrailContentQualifierList =
  struct
    type nonrec t = GuardrailContentQualifier.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailContentQualifier.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailContentQualifier.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailContentQualifierList"
        ~of_json:GuardrailContentQualifier.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9\\s._:/=+@-]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9\\s._:/=+@-]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module AsyncInvokeArn =
  struct
    type nonrec t = string
    let context_ = "AsyncInvokeArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:[a-z0-9\\-]+:bedrock:[a-z0-9\\-]*:[0-9]*:(provisioned-model|foundation-model)/.+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AsyncInvokeArn" j
    let to_json = simple_to_json to_value
  end
module AsyncInvokeIdempotencyToken =
  struct
    type nonrec t = string
    let context_ = "AsyncInvokeIdempotencyToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[!-~]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AsyncInvokeIdempotencyToken" j
    let to_json = simple_to_json to_value
  end
module AsyncInvokeMessage =
  struct
    type nonrec t = string
    let context_ = "AsyncInvokeMessage"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AsyncInvokeMessage" j
    let to_json = simple_to_json to_value
  end
module AsyncInvokeOutputDataConfig =
  struct
    type nonrec t =
      {
      s3OutputDataConfig: AsyncInvokeS3OutputDataConfig.t option
        [@ocaml.doc "A storage location for the output data in an S3 bucket"]}
    let make ?s3OutputDataConfig = fun () -> { s3OutputDataConfig }
    let to_value x =
      structure_to_value
        [("s3OutputDataConfig",
           (Option.map x.s3OutputDataConfig
              ~f:AsyncInvokeS3OutputDataConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3OutputDataConfig =
        (Option.map ~f:AsyncInvokeS3OutputDataConfig.of_xml)
          (Xml.child xml_arg0 "s3OutputDataConfig") in
      make ?s3OutputDataConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3OutputDataConfig =
        field_map json__ "s3OutputDataConfig"
          AsyncInvokeS3OutputDataConfig.of_json in
      make ?s3OutputDataConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Asynchronous invocation output data settings."]
module AsyncInvokeStatus =
  struct
    type nonrec t =
      | InProgress 
      | Completed 
      | Failed 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | InProgress -> "InProgress"
      | Completed -> "Completed"
      | Failed -> "Failed"
      | Non_static_id s -> s
    let of_string =
      function
      | "InProgress" -> InProgress
      | "Completed" -> Completed
      | "Failed" -> Failed
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AsyncInvokeStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AsyncInvokeStatus" j)
    let to_json = simple_to_json to_value
  end
module InvocationArn =
  struct
    type nonrec t = string
    let context_ = "InvocationArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:async-invoke/[a-z0-9]{12}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InvocationArn" j
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module NonBlankString =
  struct
    type nonrec t = string
    let context_ = "NonBlankString"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[\\s\\S]*"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NonBlankString" j
    let to_json = simple_to_json to_value
  end
module StatusCode =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:599) >>=
             (fun () -> check_int_min i ~min:100));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for StatusCode" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module PartBody =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module Messages =
  struct
    type nonrec t = Message.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Message.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Message.of_xml)
    let of_json j = list_of_json ~kind:"Messages" ~of_json:Message.of_json j
    let to_json v = composed_to_json to_value v
  end
module SystemContentBlocks =
  struct
    type nonrec t = SystemContentBlock.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SystemContentBlock.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SystemContentBlock.of_xml)
    let of_json j =
      list_of_json ~kind:"SystemContentBlocks"
        ~of_json:SystemContentBlock.of_json j
    let to_json v = composed_to_json to_value v
  end
module ToolConfiguration =
  struct
    type nonrec t =
      {
      tools: ToolConfigurationToolsList.t
        [@ocaml.doc "An array of tools that you want to pass to a model."];
      toolChoice: ToolChoice.t option
        [@ocaml.doc
          "If supported by model, forces the model to request a tool."]}
    let context_ = "ToolConfiguration"
    let make ?toolChoice = fun ~tools -> fun () -> { toolChoice; tools }
    let to_value x =
      structure_to_value
        [("tools", (Some (ToolConfigurationToolsList.to_value x.tools)));
        ("toolChoice", (Option.map x.toolChoice ~f:ToolChoice.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let toolChoice =
        (Option.map ~f:ToolChoice.of_xml) (Xml.child xml_arg0 "toolChoice") in
      let tools =
        ToolConfigurationToolsList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tools") in
      make ?toolChoice ~tools ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let toolChoice = field_map json__ "toolChoice" ToolChoice.of_json in
      let tools =
        field_map_exn json__ "tools" ToolConfigurationToolsList.of_json in
      make ?toolChoice ~tools ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration information for the tools that you pass to a model. For more information, see Tool use (function calling) in the Amazon Bedrock User Guide."]
module Body =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module ContentBlockDelta =
  struct
    type nonrec t =
      {
      text: String_.t option [@ocaml.doc "The content text."];
      toolUse: ToolUseBlockDelta.t option
        [@ocaml.doc
          "Information about a tool that the model is requesting to use."];
      toolResult: ToolResultBlocksDelta.t option
        [@ocaml.doc
          "An incremental update that contains the results from a tool call."];
      reasoningContent: ReasoningContentBlockDelta.t option
        [@ocaml.doc
          "Contains content regarding the reasoning that is carried out by the model. Reasoning refers to a Chain of Thought (CoT) that the model generates to enhance the accuracy of its final response."];
      citation: CitationsDelta.t option
        [@ocaml.doc
          "Incremental citation information that is streamed as part of the response generation process."];
      image: ImageBlockDelta.t option
        [@ocaml.doc
          "A streaming delta event containing incremental image data."]}
    let make ?text =
      fun ?toolUse ->
        fun ?toolResult ->
          fun ?reasoningContent ->
            fun ?citation ->
              fun ?image ->
                fun () ->
                  {
                    text;
                    toolUse;
                    toolResult;
                    reasoningContent;
                    citation;
                    image
                  }
    let to_value x =
      structure_to_value
        [("text", (Option.map x.text ~f:String_.to_value));
        ("toolUse", (Option.map x.toolUse ~f:ToolUseBlockDelta.to_value));
        ("toolResult",
          (Option.map x.toolResult ~f:ToolResultBlocksDelta.to_value));
        ("reasoningContent",
          (Option.map x.reasoningContent
             ~f:ReasoningContentBlockDelta.to_value));
        ("citation", (Option.map x.citation ~f:CitationsDelta.to_value));
        ("image", (Option.map x.image ~f:ImageBlockDelta.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let image =
        (Option.map ~f:ImageBlockDelta.of_xml) (Xml.child xml_arg0 "image") in
      let citation =
        (Option.map ~f:CitationsDelta.of_xml) (Xml.child xml_arg0 "citation") in
      let reasoningContent =
        (Option.map ~f:ReasoningContentBlockDelta.of_xml)
          (Xml.child xml_arg0 "reasoningContent") in
      let toolResult =
        (Option.map ~f:ToolResultBlocksDelta.of_xml)
          (Xml.child xml_arg0 "toolResult") in
      let toolUse =
        (Option.map ~f:ToolUseBlockDelta.of_xml)
          (Xml.child xml_arg0 "toolUse") in
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?image ?citation ?reasoningContent ?toolResult ?toolUse ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let image = field_map json__ "image" ImageBlockDelta.of_json in
      let citation = field_map json__ "citation" CitationsDelta.of_json in
      let reasoningContent =
        field_map json__ "reasoningContent"
          ReasoningContentBlockDelta.of_json in
      let toolResult =
        field_map json__ "toolResult" ToolResultBlocksDelta.of_json in
      let toolUse = field_map json__ "toolUse" ToolUseBlockDelta.of_json in
      let text = field_map json__ "text" String_.of_json in
      make ?image ?citation ?reasoningContent ?toolResult ?toolUse ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A block of content in a streaming response."]
module NonNegativeInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for NonNegativeInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ContentBlockStart =
  struct
    type nonrec t =
      {
      toolUse: ToolUseBlockStart.t option
        [@ocaml.doc
          "Information about a tool that the model is requesting to use."];
      toolResult: ToolResultBlockStart.t option [@ocaml.doc "The"];
      image: ImageBlockStart.t option
        [@ocaml.doc
          "The initial event indicating the start of a streaming image block."]}
    let make ?toolUse =
      fun ?toolResult ->
        fun ?image -> fun () -> { toolUse; toolResult; image }
    let to_value x =
      structure_to_value
        [("toolUse", (Option.map x.toolUse ~f:ToolUseBlockStart.to_value));
        ("toolResult",
          (Option.map x.toolResult ~f:ToolResultBlockStart.to_value));
        ("image", (Option.map x.image ~f:ImageBlockStart.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let image =
        (Option.map ~f:ImageBlockStart.of_xml) (Xml.child xml_arg0 "image") in
      let toolResult =
        (Option.map ~f:ToolResultBlockStart.of_xml)
          (Xml.child xml_arg0 "toolResult") in
      let toolUse =
        (Option.map ~f:ToolUseBlockStart.of_xml)
          (Xml.child xml_arg0 "toolUse") in
      make ?image ?toolResult ?toolUse ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let image = field_map json__ "image" ImageBlockStart.of_json in
      let toolResult =
        field_map json__ "toolResult" ToolResultBlockStart.of_json in
      let toolUse = field_map json__ "toolUse" ToolUseBlockStart.of_json in
      make ?image ?toolResult ?toolUse ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Content block start information."]
module ConverseStreamMetrics =
  struct
    type nonrec t =
      {
      latencyMs: Long.t option
        [@ocaml.doc
          "The latency for the streaming request, in milliseconds."]}
    let make ?latencyMs = fun () -> { latencyMs }
    let to_value x =
      structure_to_value
        [("latencyMs", (Option.map x.latencyMs ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let latencyMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "latencyMs") in
      make ?latencyMs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let latencyMs = field_map json__ "latencyMs" Long.of_json in
      make ?latencyMs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Metrics for the stream."]
module ConverseStreamTrace =
  struct
    type nonrec t =
      {
      guardrail: GuardrailTraceAssessment.t option
        [@ocaml.doc "The guardrail trace object."];
      promptRouter: PromptRouterTrace.t option
        [@ocaml.doc "The request's prompt router."]}
    let make ?guardrail =
      fun ?promptRouter -> fun () -> { guardrail; promptRouter }
    let to_value x =
      structure_to_value
        [("guardrail",
           (Option.map x.guardrail ~f:GuardrailTraceAssessment.to_value));
        ("promptRouter",
          (Option.map x.promptRouter ~f:PromptRouterTrace.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let promptRouter =
        (Option.map ~f:PromptRouterTrace.of_xml)
          (Xml.child xml_arg0 "promptRouter") in
      let guardrail =
        (Option.map ~f:GuardrailTraceAssessment.of_xml)
          (Xml.child xml_arg0 "guardrail") in
      make ?promptRouter ?guardrail ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let promptRouter =
        field_map json__ "promptRouter" PromptRouterTrace.of_json in
      let guardrail =
        field_map json__ "guardrail" GuardrailTraceAssessment.of_json in
      make ?promptRouter ?guardrail ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The trace object in a response from ConverseStream."]
module PerformanceConfiguration =
  struct
    type nonrec t =
      {
      latency: PerformanceConfigLatency.t option
        [@ocaml.doc
          "To use a latency-optimized version of the model, set to optimized."]}
    let make ?latency = fun () -> { latency }
    let to_value x =
      structure_to_value
        [("latency",
           (Option.map x.latency ~f:PerformanceConfigLatency.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let latency =
        (Option.map ~f:PerformanceConfigLatency.of_xml)
          (Xml.child xml_arg0 "latency") in
      make ?latency ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let latency =
        field_map json__ "latency" PerformanceConfigLatency.of_json in
      make ?latency ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Performance settings for a model."]
module ServiceTier =
  struct
    type nonrec t =
      {
      type_: ServiceTierType.t
        [@ocaml.doc
          "Specifies the processing tier type used for serving the request."]}
    let context_ = "ServiceTier"
    let make ~type_ = fun () -> { type_ }
    let to_value x =
      structure_to_value
        [("type", (Some (ServiceTierType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        ServiceTierType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map_exn json__ "type" ServiceTierType.of_json in
      make ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the processing tier configuration used for serving the request."]
module TokenUsage =
  struct
    type nonrec t =
      {
      inputTokens: TokenUsageInputTokensInteger.t option
        [@ocaml.doc "The number of tokens sent in the request to the model."];
      outputTokens: TokenUsageOutputTokensInteger.t option
        [@ocaml.doc
          "The number of tokens that the model generated for the request."];
      totalTokens: TokenUsageTotalTokensInteger.t option
        [@ocaml.doc
          "The total of input tokens and tokens generated by the model."];
      cacheReadInputTokens: TokenUsageCacheReadInputTokensInteger.t option
        [@ocaml.doc
          "The number of input tokens read from the cache for the request."];
      cacheWriteInputTokens: TokenUsageCacheWriteInputTokensInteger.t option
        [@ocaml.doc
          "The number of input tokens written to the cache for the request."];
      cacheDetails: CacheDetailsList.t option
        [@ocaml.doc
          "Detailed breakdown of cache writes by TTL. Empty if no cache creation occurred. Sorted by TTL duration (1h before 5m)."]}
    let make ?inputTokens =
      fun ?outputTokens ->
        fun ?totalTokens ->
          fun ?cacheReadInputTokens ->
            fun ?cacheWriteInputTokens ->
              fun ?cacheDetails ->
                fun () ->
                  {
                    inputTokens;
                    outputTokens;
                    totalTokens;
                    cacheReadInputTokens;
                    cacheWriteInputTokens;
                    cacheDetails
                  }
    let to_value x =
      structure_to_value
        [("inputTokens",
           (Option.map x.inputTokens ~f:TokenUsageInputTokensInteger.to_value));
        ("outputTokens",
          (Option.map x.outputTokens
             ~f:TokenUsageOutputTokensInteger.to_value));
        ("totalTokens",
          (Option.map x.totalTokens ~f:TokenUsageTotalTokensInteger.to_value));
        ("cacheReadInputTokens",
          (Option.map x.cacheReadInputTokens
             ~f:TokenUsageCacheReadInputTokensInteger.to_value));
        ("cacheWriteInputTokens",
          (Option.map x.cacheWriteInputTokens
             ~f:TokenUsageCacheWriteInputTokensInteger.to_value));
        ("cacheDetails",
          (Option.map x.cacheDetails ~f:CacheDetailsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cacheDetails =
        (Option.map ~f:CacheDetailsList.of_xml)
          (Xml.child xml_arg0 "cacheDetails") in
      let cacheWriteInputTokens =
        (Option.map ~f:TokenUsageCacheWriteInputTokensInteger.of_xml)
          (Xml.child xml_arg0 "cacheWriteInputTokens") in
      let cacheReadInputTokens =
        (Option.map ~f:TokenUsageCacheReadInputTokensInteger.of_xml)
          (Xml.child xml_arg0 "cacheReadInputTokens") in
      let totalTokens =
        (Option.map ~f:TokenUsageTotalTokensInteger.of_xml)
          (Xml.child xml_arg0 "totalTokens") in
      let outputTokens =
        (Option.map ~f:TokenUsageOutputTokensInteger.of_xml)
          (Xml.child xml_arg0 "outputTokens") in
      let inputTokens =
        (Option.map ~f:TokenUsageInputTokensInteger.of_xml)
          (Xml.child xml_arg0 "inputTokens") in
      make ?cacheDetails ?cacheWriteInputTokens ?cacheReadInputTokens
        ?totalTokens ?outputTokens ?inputTokens ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cacheDetails =
        field_map json__ "cacheDetails" CacheDetailsList.of_json in
      let cacheWriteInputTokens =
        field_map json__ "cacheWriteInputTokens"
          TokenUsageCacheWriteInputTokensInteger.of_json in
      let cacheReadInputTokens =
        field_map json__ "cacheReadInputTokens"
          TokenUsageCacheReadInputTokensInteger.of_json in
      let totalTokens =
        field_map json__ "totalTokens" TokenUsageTotalTokensInteger.of_json in
      let outputTokens =
        field_map json__ "outputTokens" TokenUsageOutputTokensInteger.of_json in
      let inputTokens =
        field_map json__ "inputTokens" TokenUsageInputTokensInteger.of_json in
      make ?cacheDetails ?cacheWriteInputTokens ?cacheReadInputTokens
        ?totalTokens ?outputTokens ?inputTokens ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The tokens used in a message API inference call."]
module StopReason =
  struct
    type nonrec t =
      | End_turn 
      | Tool_use 
      | Max_tokens 
      | Stop_sequence 
      | Guardrail_intervened 
      | Content_filtered 
      | Malformed_model_output 
      | Malformed_tool_use 
      | Model_context_window_exceeded 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | End_turn -> "end_turn"
      | Tool_use -> "tool_use"
      | Max_tokens -> "max_tokens"
      | Stop_sequence -> "stop_sequence"
      | Guardrail_intervened -> "guardrail_intervened"
      | Content_filtered -> "content_filtered"
      | Malformed_model_output -> "malformed_model_output"
      | Malformed_tool_use -> "malformed_tool_use"
      | Model_context_window_exceeded -> "model_context_window_exceeded"
      | Non_static_id s -> s
    let of_string =
      function
      | "end_turn" -> End_turn
      | "tool_use" -> Tool_use
      | "max_tokens" -> Max_tokens
      | "stop_sequence" -> Stop_sequence
      | "guardrail_intervened" -> Guardrail_intervened
      | "content_filtered" -> Content_filtered
      | "malformed_model_output" -> Malformed_model_output
      | "malformed_tool_use" -> Malformed_tool_use
      | "model_context_window_exceeded" -> Model_context_window_exceeded
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StopReason" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StopReason" j)
    let to_json = simple_to_json to_value
  end
module OutputFormatStructure =
  struct
    type nonrec t =
      {
      jsonSchema: JsonSchemaDefinition.t option
        [@ocaml.doc
          "A JSON schema structure that the model's output must adhere to."]}
    let make ?jsonSchema = fun () -> { jsonSchema }
    let to_value x =
      structure_to_value
        [("jsonSchema",
           (Option.map x.jsonSchema ~f:JsonSchemaDefinition.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let jsonSchema =
        (Option.map ~f:JsonSchemaDefinition.of_xml)
          (Xml.child xml_arg0 "jsonSchema") in
      make ?jsonSchema ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let jsonSchema =
        field_map json__ "jsonSchema" JsonSchemaDefinition.of_json in
      make ?jsonSchema ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The structure that the model's output must adhere to."]
module OutputFormatType =
  struct
    type nonrec t =
      | Json_schema 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Json_schema -> "json_schema" | Non_static_id s -> s
    let of_string =
      function | "json_schema" -> Json_schema | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration OutputFormatType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"OutputFormatType" j)
    let to_json = simple_to_json to_value
  end
module GuardrailImageBlock =
  struct
    type nonrec t =
      {
      format: GuardrailImageFormat.t
        [@ocaml.doc
          "The format details for the file type of the image blocked by the guardrail."];
      source: GuardrailImageSource.t
        [@ocaml.doc
          "The image source (image bytes) details of the image blocked by the guardrail."]}
    let context_ = "GuardrailImageBlock"
    let make ~format = fun ~source -> fun () -> { format; source }
    let to_value x =
      structure_to_value
        [("format", (Some (GuardrailImageFormat.to_value x.format)));
        ("source", (Some (GuardrailImageSource.to_value x.source)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let source =
        GuardrailImageSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "source") in
      let format =
        GuardrailImageFormat.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "format") in
      make ~source ~format ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let source = field_map_exn json__ "source" GuardrailImageSource.of_json in
      let format = field_map_exn json__ "format" GuardrailImageFormat.of_json in
      make ~source ~format ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contain an image which user wants guarded. This block is accepted by the guardrails independent API."]
module GuardrailTextBlock =
  struct
    type nonrec t =
      {
      text: String_.t
        [@ocaml.doc
          "The input text details to be evaluated by the guardrail."];
      qualifiers: GuardrailContentQualifierList.t option
        [@ocaml.doc "The qualifiers describing the text block."]}
    let context_ = "GuardrailTextBlock"
    let make ?qualifiers = fun ~text -> fun () -> { qualifiers; text }
    let to_value x =
      structure_to_value
        [("text", (Some (String_.to_value x.text)));
        ("qualifiers",
          (Option.map x.qualifiers ~f:GuardrailContentQualifierList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let qualifiers =
        (Option.map ~f:GuardrailContentQualifierList.of_xml)
          (Xml.child xml_arg0 "qualifiers") in
      let text =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "text") in
      make ?qualifiers ~text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let qualifiers =
        field_map json__ "qualifiers" GuardrailContentQualifierList.of_json in
      let text = field_map_exn json__ "text" String_.of_json in
      make ?qualifiers ~text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The text block to be evaluated by the guardrail."]
module Tag =
  struct
    type nonrec t =
      {
      key: TagKey.t [@ocaml.doc "The tag's key."];
      value: TagValue.t [@ocaml.doc "The tag's value."]}
    let context_ = "Tag"
    let make ~key = fun ~value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("key", (Some (TagKey.to_value x.key)));
        ("value", (Some (TagValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        TagValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "value") in
      let key =
        TagKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "key") in
      make ~value ~key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map_exn json__ "value" TagValue.of_json in
      let key = field_map_exn json__ "key" TagKey.of_json in
      make ~value ~key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A tag."]
module AsyncInvokeSummary =
  struct
    type nonrec t =
      {
      invocationArn: InvocationArn.t option
        [@ocaml.doc "The invocation's ARN."];
      modelArn: AsyncInvokeArn.t option
        [@ocaml.doc "The invoked model's ARN."];
      clientRequestToken: AsyncInvokeIdempotencyToken.t option
        [@ocaml.doc "The invocation's idempotency token."];
      status: AsyncInvokeStatus.t option
        [@ocaml.doc "The invocation's status."];
      failureMessage: AsyncInvokeMessage.t option
        [@ocaml.doc "An error message."];
      submitTime: Timestamp.t option
        [@ocaml.doc "When the invocation was submitted."];
      lastModifiedTime: Timestamp.t option
        [@ocaml.doc "When the invocation was last modified."];
      endTime: Timestamp.t option [@ocaml.doc "When the invocation ended."];
      outputDataConfig: AsyncInvokeOutputDataConfig.t option
        [@ocaml.doc "The invocation's output data settings."]}
    let make ?invocationArn =
      fun ?modelArn ->
        fun ?clientRequestToken ->
          fun ?status ->
            fun ?failureMessage ->
              fun ?submitTime ->
                fun ?lastModifiedTime ->
                  fun ?endTime ->
                    fun ?outputDataConfig ->
                      fun () ->
                        {
                          invocationArn;
                          modelArn;
                          clientRequestToken;
                          status;
                          failureMessage;
                          submitTime;
                          lastModifiedTime;
                          endTime;
                          outputDataConfig
                        }
    let to_value x =
      structure_to_value
        [("invocationArn",
           (Option.map x.invocationArn ~f:InvocationArn.to_value));
        ("modelArn", (Option.map x.modelArn ~f:AsyncInvokeArn.to_value));
        ("clientRequestToken",
          (Option.map x.clientRequestToken
             ~f:AsyncInvokeIdempotencyToken.to_value));
        ("status", (Option.map x.status ~f:AsyncInvokeStatus.to_value));
        ("failureMessage",
          (Option.map x.failureMessage ~f:AsyncInvokeMessage.to_value));
        ("submitTime", (Option.map x.submitTime ~f:Timestamp.to_value));
        ("lastModifiedTime",
          (Option.map x.lastModifiedTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("outputDataConfig",
          (Option.map x.outputDataConfig
             ~f:AsyncInvokeOutputDataConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputDataConfig =
        (Option.map ~f:AsyncInvokeOutputDataConfig.of_xml)
          (Xml.child xml_arg0 "outputDataConfig") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let lastModifiedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastModifiedTime") in
      let submitTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "submitTime") in
      let failureMessage =
        (Option.map ~f:AsyncInvokeMessage.of_xml)
          (Xml.child xml_arg0 "failureMessage") in
      let status =
        (Option.map ~f:AsyncInvokeStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let clientRequestToken =
        (Option.map ~f:AsyncInvokeIdempotencyToken.of_xml)
          (Xml.child xml_arg0 "clientRequestToken") in
      let modelArn =
        (Option.map ~f:AsyncInvokeArn.of_xml) (Xml.child xml_arg0 "modelArn") in
      let invocationArn =
        (Option.map ~f:InvocationArn.of_xml)
          (Xml.child xml_arg0 "invocationArn") in
      make ?outputDataConfig ?endTime ?lastModifiedTime ?submitTime
        ?failureMessage ?status ?clientRequestToken ?modelArn ?invocationArn
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputDataConfig =
        field_map json__ "outputDataConfig"
          AsyncInvokeOutputDataConfig.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let lastModifiedTime =
        field_map json__ "lastModifiedTime" Timestamp.of_json in
      let submitTime = field_map json__ "submitTime" Timestamp.of_json in
      let failureMessage =
        field_map json__ "failureMessage" AsyncInvokeMessage.of_json in
      let status = field_map json__ "status" AsyncInvokeStatus.of_json in
      let clientRequestToken =
        field_map json__ "clientRequestToken"
          AsyncInvokeIdempotencyToken.of_json in
      let modelArn = field_map json__ "modelArn" AsyncInvokeArn.of_json in
      let invocationArn =
        field_map json__ "invocationArn" InvocationArn.of_json in
      make ?outputDataConfig ?endTime ?lastModifiedTime ?submitTime
        ?failureMessage ?status ?clientRequestToken ?modelArn ?invocationArn
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A summary of an asynchronous invocation."]
module InternalServerException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An internal server error occurred. For troubleshooting this error, see InternalFailure in the Amazon Bedrock User Guide"]
module ModelStreamErrorException =
  struct
    type nonrec t =
      {
      message: NonBlankString.t option ;
      originalStatusCode: StatusCode.t option
        [@ocaml.doc "The original status code."];
      originalMessage: NonBlankString.t option
        [@ocaml.doc "The original message."]}
    let make ?message =
      fun ?originalStatusCode ->
        fun ?originalMessage ->
          fun () -> { message; originalStatusCode; originalMessage }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value));
        ("originalStatusCode",
          (Option.map x.originalStatusCode ~f:StatusCode.to_value));
        ("originalMessage",
          (Option.map x.originalMessage ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let originalMessage =
        (Option.map ~f:NonBlankString.of_xml)
          (Xml.child xml_arg0 "originalMessage") in
      let originalStatusCode =
        (Option.map ~f:StatusCode.of_xml)
          (Xml.child xml_arg0 "originalStatusCode") in
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?originalMessage ?originalStatusCode ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let originalMessage =
        field_map json__ "originalMessage" NonBlankString.of_json in
      let originalStatusCode =
        field_map json__ "originalStatusCode" StatusCode.of_json in
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?originalMessage ?originalStatusCode ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An error occurred while streaming the response. Retry your request."]
module ModelTimeoutException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request took too long to process. Processing time exceeded the model timeout length."]
module PayloadPart =
  struct
    type nonrec t =
      {
      bytes: PartBody.t option
        [@ocaml.doc "Base64-encoded bytes of payload data."]}
    let make ?bytes = fun () -> { bytes }
    let of_header_and_body = ((fun (xs, pipe) -> make ?bytes:(Some pipe) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("bytes", (Option.map x.bytes ~f:PartBody.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bytes =
        (Option.map ~f:PartBody.of_xml) (Xml.child xml_arg0 "bytes") in
      make ?bytes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bytes = field_map json__ "bytes" PartBody.of_json in make ?bytes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Payload content included in the response."]
module ServiceUnavailableException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The service isn't currently available. For troubleshooting this error, see ServiceUnavailable in the Amazon Bedrock User Guide"]
module ThrottlingException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Your request was denied due to exceeding the account quotas for Amazon Bedrock. For troubleshooting this error, see ThrottlingException in the Amazon Bedrock User Guide"]
module ValidationException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The input fails to satisfy the constraints specified by Amazon Bedrock. For troubleshooting this error, see ValidationError in the Amazon Bedrock User Guide"]
module BidirectionalOutputPayloadPart =
  struct
    type nonrec t =
      {
      bytes: PartBody.t option
        [@ocaml.doc "The speech output of the bidirectional stream."]}
    let make ?bytes = fun () -> { bytes }
    let of_header_and_body = ((fun (xs, pipe) -> make ?bytes:(Some pipe) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("bytes", (Option.map x.bytes ~f:PartBody.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bytes =
        (Option.map ~f:PartBody.of_xml) (Xml.child xml_arg0 "bytes") in
      make ?bytes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bytes = field_map json__ "bytes" PartBody.of_json in make ?bytes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Output from the bidirectional stream. The output is speech and a text transcription."]
module BidirectionalInputPayloadPart =
  struct
    type nonrec t =
      {
      bytes: PartBody.t option
        [@ocaml.doc "The audio content for the bidirectional input."]}
    let make ?bytes = fun () -> { bytes }
    let of_header_and_body = ((fun (xs, pipe) -> make ?bytes:(Some pipe) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("bytes", (Option.map x.bytes ~f:PartBody.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bytes =
        (Option.map ~f:PartBody.of_xml) (Xml.child xml_arg0 "bytes") in
      make ?bytes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bytes = field_map json__ "bytes" PartBody.of_json in make ?bytes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Payload content for the bidirectional input. The input is an audio stream."]
module ConverseTokensRequest =
  struct
    type nonrec t =
      {
      messages: Messages.t option
        [@ocaml.doc "An array of messages to count tokens for."];
      system: SystemContentBlocks.t option
        [@ocaml.doc
          "The system content blocks to count tokens for. System content provides instructions or context to the model about how it should behave or respond. The token count will include any system content provided."];
      toolConfig: ToolConfiguration.t option
        [@ocaml.doc
          "The toolConfig of Converse input request to count tokens for. Configuration information for the tools that the model can use when generating a response."];
      additionalModelRequestFields: Document.t option
        [@ocaml.doc
          "The additionalModelRequestFields of Converse input request to count tokens for. Use this field when you want to pass additional parameters that the model supports."]}
    let make ?messages =
      fun ?system ->
        fun ?toolConfig ->
          fun ?additionalModelRequestFields ->
            fun () ->
              { messages; system; toolConfig; additionalModelRequestFields }
    let to_value x =
      structure_to_value
        [("messages", (Option.map x.messages ~f:Messages.to_value));
        ("system", (Option.map x.system ~f:SystemContentBlocks.to_value));
        ("toolConfig",
          (Option.map x.toolConfig ~f:ToolConfiguration.to_value));
        ("additionalModelRequestFields",
          (Option.map x.additionalModelRequestFields ~f:Document.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let additionalModelRequestFields =
        (Option.map ~f:Document.of_xml)
          (Xml.child xml_arg0 "additionalModelRequestFields") in
      let toolConfig =
        (Option.map ~f:ToolConfiguration.of_xml)
          (Xml.child xml_arg0 "toolConfig") in
      let system =
        (Option.map ~f:SystemContentBlocks.of_xml)
          (Xml.child xml_arg0 "system") in
      let messages =
        (Option.map ~f:Messages.of_xml) (Xml.child xml_arg0 "messages") in
      make ?additionalModelRequestFields ?toolConfig ?system ?messages ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let additionalModelRequestFields =
        field_map json__ "additionalModelRequestFields" Document.of_json in
      let toolConfig =
        field_map json__ "toolConfig" ToolConfiguration.of_json in
      let system = field_map json__ "system" SystemContentBlocks.of_json in
      let messages = field_map json__ "messages" Messages.of_json in
      make ?additionalModelRequestFields ?toolConfig ?system ?messages ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The inputs from a Converse API request for token counting. This structure mirrors the input format for the Converse operation, allowing you to count tokens for conversation-based inference requests."]
module InvokeModelTokensRequest =
  struct
    type nonrec t =
      {
      body: Body.t
        [@ocaml.doc
          "The request body to count tokens for, formatted according to the model's expected input format. To learn about the input format for different models, see Model inference parameters and responses."]}
    let context_ = "InvokeModelTokensRequest"
    let make ~body = fun () -> { body }
    let of_header_and_body = ((fun (xs, pipe) -> make ~body:pipe ())
      [@warning "-27"])
    let to_value x =
      structure_to_value [("body", (Some (Body.to_value x.body)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let body =
        Body.of_xml (Xml.child_exn ~context:context_ xml_arg0 "body") in
      make ~body ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let body = field_map_exn json__ "body" Body.of_json in make ~body ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The body of an InvokeModel API request for token counting. This structure mirrors the input format for the InvokeModel operation, allowing you to count tokens for raw text inference requests."]
module ContentBlockDeltaEvent =
  struct
    type nonrec t =
      {
      delta: ContentBlockDelta.t option
        [@ocaml.doc "The delta for a content block delta event."];
      contentBlockIndex: NonNegativeInteger.t option
        [@ocaml.doc "The block index for a content block delta event."]}
    let make ?delta =
      fun ?contentBlockIndex -> fun () -> { delta; contentBlockIndex }
    let to_value x =
      structure_to_value
        [("delta", (Option.map x.delta ~f:ContentBlockDelta.to_value));
        ("contentBlockIndex",
          (Option.map x.contentBlockIndex ~f:NonNegativeInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let contentBlockIndex =
        (Option.map ~f:NonNegativeInteger.of_xml)
          (Xml.child xml_arg0 "contentBlockIndex") in
      let delta =
        (Option.map ~f:ContentBlockDelta.of_xml) (Xml.child xml_arg0 "delta") in
      make ?contentBlockIndex ?delta ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let contentBlockIndex =
        field_map json__ "contentBlockIndex" NonNegativeInteger.of_json in
      let delta = field_map json__ "delta" ContentBlockDelta.of_json in
      make ?contentBlockIndex ?delta ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The content block delta event."]
module ContentBlockStartEvent =
  struct
    type nonrec t =
      {
      start: ContentBlockStart.t option
        [@ocaml.doc "Start information about a content block start event."];
      contentBlockIndex: NonNegativeInteger.t option
        [@ocaml.doc "The index for a content block start event."]}
    let make ?start =
      fun ?contentBlockIndex -> fun () -> { start; contentBlockIndex }
    let to_value x =
      structure_to_value
        [("start", (Option.map x.start ~f:ContentBlockStart.to_value));
        ("contentBlockIndex",
          (Option.map x.contentBlockIndex ~f:NonNegativeInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let contentBlockIndex =
        (Option.map ~f:NonNegativeInteger.of_xml)
          (Xml.child xml_arg0 "contentBlockIndex") in
      let start =
        (Option.map ~f:ContentBlockStart.of_xml) (Xml.child xml_arg0 "start") in
      make ?contentBlockIndex ?start ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let contentBlockIndex =
        field_map json__ "contentBlockIndex" NonNegativeInteger.of_json in
      let start = field_map json__ "start" ContentBlockStart.of_json in
      make ?contentBlockIndex ?start ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Content block start event."]
module ContentBlockStopEvent =
  struct
    type nonrec t =
      {
      contentBlockIndex: NonNegativeInteger.t option
        [@ocaml.doc "The index for a content block."]}
    let make ?contentBlockIndex = fun () -> { contentBlockIndex }
    let to_value x =
      structure_to_value
        [("contentBlockIndex",
           (Option.map x.contentBlockIndex ~f:NonNegativeInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let contentBlockIndex =
        (Option.map ~f:NonNegativeInteger.of_xml)
          (Xml.child xml_arg0 "contentBlockIndex") in
      make ?contentBlockIndex ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let contentBlockIndex =
        field_map json__ "contentBlockIndex" NonNegativeInteger.of_json in
      make ?contentBlockIndex ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A content block stop event."]
module ConverseStreamMetadataEvent =
  struct
    type nonrec t =
      {
      usage: TokenUsage.t option
        [@ocaml.doc "Usage information for the conversation stream event."];
      metrics: ConverseStreamMetrics.t option
        [@ocaml.doc
          "The metrics for the conversation stream metadata event."];
      trace: ConverseStreamTrace.t option
        [@ocaml.doc
          "The trace object in the response from ConverseStream that contains information about the guardrail behavior."];
      performanceConfig: PerformanceConfiguration.t option
        [@ocaml.doc
          "Model performance configuration metadata for the conversation stream event."];
      serviceTier: ServiceTier.t option
        [@ocaml.doc
          "Specifies the processing tier configuration used for serving the request."]}
    let make ?usage =
      fun ?metrics ->
        fun ?trace ->
          fun ?performanceConfig ->
            fun ?serviceTier ->
              fun () ->
                { usage; metrics; trace; performanceConfig; serviceTier }
    let to_value x =
      structure_to_value
        [("usage", (Option.map x.usage ~f:TokenUsage.to_value));
        ("metrics", (Option.map x.metrics ~f:ConverseStreamMetrics.to_value));
        ("trace", (Option.map x.trace ~f:ConverseStreamTrace.to_value));
        ("performanceConfig",
          (Option.map x.performanceConfig
             ~f:PerformanceConfiguration.to_value));
        ("serviceTier", (Option.map x.serviceTier ~f:ServiceTier.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceTier =
        (Option.map ~f:ServiceTier.of_xml) (Xml.child xml_arg0 "serviceTier") in
      let performanceConfig =
        (Option.map ~f:PerformanceConfiguration.of_xml)
          (Xml.child xml_arg0 "performanceConfig") in
      let trace =
        (Option.map ~f:ConverseStreamTrace.of_xml)
          (Xml.child xml_arg0 "trace") in
      let metrics =
        (Option.map ~f:ConverseStreamMetrics.of_xml)
          (Xml.child xml_arg0 "metrics") in
      let usage =
        (Option.map ~f:TokenUsage.of_xml) (Xml.child xml_arg0 "usage") in
      make ?serviceTier ?performanceConfig ?trace ?metrics ?usage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serviceTier = field_map json__ "serviceTier" ServiceTier.of_json in
      let performanceConfig =
        field_map json__ "performanceConfig" PerformanceConfiguration.of_json in
      let trace = field_map json__ "trace" ConverseStreamTrace.of_json in
      let metrics = field_map json__ "metrics" ConverseStreamMetrics.of_json in
      let usage = field_map json__ "usage" TokenUsage.of_json in
      make ?serviceTier ?performanceConfig ?trace ?metrics ?usage ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A conversation stream metadata event."]
module MessageStartEvent =
  struct
    type nonrec t =
      {
      role: ConversationRole.t option
        [@ocaml.doc "The role for the message."]}
    let make ?role = fun () -> { role }
    let to_value x =
      structure_to_value
        [("role", (Option.map x.role ~f:ConversationRole.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let role =
        (Option.map ~f:ConversationRole.of_xml) (Xml.child xml_arg0 "role") in
      make ?role ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let role = field_map json__ "role" ConversationRole.of_json in
      make ?role ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The start of a message."]
module MessageStopEvent =
  struct
    type nonrec t =
      {
      stopReason: StopReason.t option
        [@ocaml.doc "The reason why the model stopped generating output."];
      additionalModelResponseFields: Document.t option
        [@ocaml.doc "The additional model response fields."]}
    let make ?stopReason =
      fun ?additionalModelResponseFields ->
        fun () -> { stopReason; additionalModelResponseFields }
    let to_value x =
      structure_to_value
        [("stopReason", (Option.map x.stopReason ~f:StopReason.to_value));
        ("additionalModelResponseFields",
          (Option.map x.additionalModelResponseFields ~f:Document.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let additionalModelResponseFields =
        (Option.map ~f:Document.of_xml)
          (Xml.child xml_arg0 "additionalModelResponseFields") in
      let stopReason =
        (Option.map ~f:StopReason.of_xml) (Xml.child xml_arg0 "stopReason") in
      make ?additionalModelResponseFields ?stopReason ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let additionalModelResponseFields =
        field_map json__ "additionalModelResponseFields" Document.of_json in
      let stopReason = field_map json__ "stopReason" StopReason.of_json in
      make ?additionalModelResponseFields ?stopReason ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The stop event for a message."]
module ConverseStreamRequestAdditionalModelResponseFieldPathsListMemberString =
  struct
    type nonrec t = string
    let context_ =
      "ConverseStreamRequestAdditionalModelResponseFieldPathsListMemberString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json
        ~kind:"ConverseStreamRequestAdditionalModelResponseFieldPathsListMemberString"
        j
    let to_json = simple_to_json to_value
  end
module GuardrailIdentifier =
  struct
    type nonrec t = string
    let context_ = "GuardrailIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(|([a-z0-9]+)|(arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:guardrail/[a-z0-9]+))")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GuardrailIdentifier" j
    let to_json = simple_to_json to_value
  end
module GuardrailStreamProcessingMode =
  struct
    type nonrec t =
      | Sync 
      | Async 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Sync -> "sync" | Async -> "async" | Non_static_id s -> s
    let of_string =
      function | "sync" -> Sync | "async" -> Async | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailStreamProcessingMode"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailStreamProcessingMode" j)
    let to_json = simple_to_json to_value
  end
module GuardrailTrace =
  struct
    type nonrec t =
      | Enabled 
      | Disabled 
      | Enabled_full 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Enabled -> "enabled"
      | Disabled -> "disabled"
      | Enabled_full -> "enabled_full"
      | Non_static_id s -> s
    let of_string =
      function
      | "enabled" -> Enabled
      | "disabled" -> Disabled
      | "enabled_full" -> Enabled_full
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration GuardrailTrace" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GuardrailTrace" j)
    let to_json = simple_to_json to_value
  end
module InferenceConfigurationMaxTokensInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for InferenceConfigurationMaxTokensInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module InferenceConfigurationStopSequencesList =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:2500) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"InferenceConfigurationStopSequencesList"
        ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module InferenceConfigurationTemperatureFloat =
  struct
    type nonrec t = float
    let make i =
      let open Result in
        ok_or_failwith
          ((check_float_min i ~min:1.) >>=
             (fun () -> check_float_min i ~min:0.));
        i
    let of_string = Float.of_string
    let to_value x = `Float x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a float" xml_arg0)
    let of_json j = float_of_json ~kind:"a float" j
    let to_json = simple_to_json to_value
  end
module InferenceConfigurationTopPFloat =
  struct
    type nonrec t = float
    let make i =
      let open Result in
        ok_or_failwith
          ((check_float_min i ~min:1.) >>=
             (fun () -> check_float_min i ~min:0.));
        i
    let of_string = Float.of_string
    let to_value x = `Float x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a float" xml_arg0)
    let of_json j = float_of_json ~kind:"a float" j
    let to_json = simple_to_json to_value
  end
module OutputFormat =
  struct
    type nonrec t =
      {
      type_: OutputFormatType.t
        [@ocaml.doc "The type of structured output format."];
      structure: OutputFormatStructure.t
        [@ocaml.doc "The structure that the model's output must adhere to."]}
    let context_ = "OutputFormat"
    let make ~type_ = fun ~structure -> fun () -> { type_; structure }
    let to_value x =
      structure_to_value
        [("type", (Some (OutputFormatType.to_value x.type_)));
        ("structure", (Some (OutputFormatStructure.to_value x.structure)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let structure =
        OutputFormatStructure.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "structure") in
      let type_ =
        OutputFormatType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ~structure ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let structure =
        field_map_exn json__ "structure" OutputFormatStructure.of_json in
      let type_ = field_map_exn json__ "type" OutputFormatType.of_json in
      make ~structure ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Structured output parameters to control the model's response."]
module PromptVariableValues =
  struct
    type nonrec t =
      {
      text: String_.t option
        [@ocaml.doc "The text value that the variable maps to."]}
    let make ?text = fun () -> { text }
    let to_value x =
      structure_to_value [("text", (Option.map x.text ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" String_.of_json in make ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains a map of variables in a prompt from Prompt management to an object containing the values to fill in for them when running model invocation. For more information, see How Prompt management works."]
module RequestMetadataKeyString =
  struct
    type nonrec t = string
    let context_ = "RequestMetadataKeyString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[a-zA-Z0-9\\s:_@$#=/+,-.]{1,256}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RequestMetadataKeyString" j
    let to_json = simple_to_json to_value
  end
module RequestMetadataValueString =
  struct
    type nonrec t = string
    let context_ = "RequestMetadataValueString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[a-zA-Z0-9\\s:_@$#=/+,-.]{0,256}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RequestMetadataValueString" j
    let to_json = simple_to_json to_value
  end
module ConverseRequestAdditionalModelResponseFieldPathsListMemberString =
  struct
    type nonrec t = string
    let context_ =
      "ConverseRequestAdditionalModelResponseFieldPathsListMemberString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json
        ~kind:"ConverseRequestAdditionalModelResponseFieldPathsListMemberString"
        j
    let to_json = simple_to_json to_value
  end
module GuardrailOutputContent =
  struct
    type nonrec t =
      {
      text: GuardrailOutputText.t option
        [@ocaml.doc
          "The specific text for the output content produced by the guardrail."]}
    let make ?text = fun () -> { text }
    let to_value x =
      structure_to_value
        [("text", (Option.map x.text ~f:GuardrailOutputText.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text =
        (Option.map ~f:GuardrailOutputText.of_xml)
          (Xml.child xml_arg0 "text") in
      make ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" GuardrailOutputText.of_json in
      make ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output content produced by the guardrail."]
module GuardrailContentBlock =
  struct
    type nonrec t =
      {
      text: GuardrailTextBlock.t option
        [@ocaml.doc
          "Text within content block to be evaluated by the guardrail."];
      image: GuardrailImageBlock.t option
        [@ocaml.doc
          "Image within guardrail content block to be evaluated by the guardrail."]}
    let make ?text = fun ?image -> fun () -> { text; image }
    let to_value x =
      structure_to_value
        [("text", (Option.map x.text ~f:GuardrailTextBlock.to_value));
        ("image", (Option.map x.image ~f:GuardrailImageBlock.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let image =
        (Option.map ~f:GuardrailImageBlock.of_xml)
          (Xml.child xml_arg0 "image") in
      let text =
        (Option.map ~f:GuardrailTextBlock.of_xml) (Xml.child xml_arg0 "text") in
      make ?image ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let image = field_map json__ "image" GuardrailImageBlock.of_json in
      let text = field_map json__ "text" GuardrailTextBlock.of_json in
      make ?image ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The content block to be evaluated by the guardrail."]
module AccessDeniedException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request is denied because you do not have sufficient permissions to perform the requested action. For troubleshooting this error, see AccessDeniedException in the Amazon Bedrock User Guide"]
module ConflictException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Error occurred because of a conflict while performing an operation."]
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified resource ARN was not found. For troubleshooting this error, see ResourceNotFound in the Amazon Bedrock User Guide"]
module ServiceQuotaExceededException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Your request exceeds the service quota for your account. You can view your quotas at Viewing service quotas. You can resubmit your request later."]
module AsyncInvokeIdentifier =
  struct
    type nonrec t = string
    let context_ = "AsyncInvokeIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z_\\.\\-/0-9:]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AsyncInvokeIdentifier" j
    let to_json = simple_to_json to_value
  end
module ModelInputPayload =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module AsyncInvokeSummaries =
  struct
    type nonrec t = AsyncInvokeSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AsyncInvokeSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AsyncInvokeSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"AsyncInvokeSummaries"
        ~of_json:AsyncInvokeSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module PaginationToken =
  struct
    type nonrec t = string
    let context_ = "PaginationToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"\\S*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PaginationToken" j
    let to_json = simple_to_json to_value
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SortAsyncInvocationBy =
  struct
    type nonrec t =
      | SubmissionTime 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | SubmissionTime -> "SubmissionTime" | Non_static_id s -> s
    let of_string =
      function | "SubmissionTime" -> SubmissionTime | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SortAsyncInvocationBy" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SortAsyncInvocationBy" j)
    let to_json = simple_to_json to_value
  end
module SortOrder =
  struct
    type nonrec t =
      | Ascending 
      | Descending 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Ascending -> "Ascending"
      | Descending -> "Descending"
      | Non_static_id s -> s
    let of_string =
      function
      | "Ascending" -> Ascending
      | "Descending" -> Descending
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SortOrder" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SortOrder" j)
    let to_json = simple_to_json to_value
  end
module MimeType =
  struct
    type nonrec t = string
    let context_ = "MimeType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MimeType" j
    let to_json = simple_to_json to_value
  end
module ModelErrorException =
  struct
    type nonrec t =
      {
      message: NonBlankString.t option ;
      originalStatusCode: StatusCode.t option
        [@ocaml.doc "The original status code."];
      resourceName: NonBlankString.t option [@ocaml.doc "The resource name."]}
    let make ?message =
      fun ?originalStatusCode ->
        fun ?resourceName ->
          fun () -> { message; originalStatusCode; resourceName }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value));
        ("originalStatusCode",
          (Option.map x.originalStatusCode ~f:StatusCode.to_value));
        ("resourceName",
          (Option.map x.resourceName ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceName =
        (Option.map ~f:NonBlankString.of_xml)
          (Xml.child xml_arg0 "resourceName") in
      let originalStatusCode =
        (Option.map ~f:StatusCode.of_xml)
          (Xml.child xml_arg0 "originalStatusCode") in
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?resourceName ?originalStatusCode ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceName =
        field_map json__ "resourceName" NonBlankString.of_json in
      let originalStatusCode =
        field_map json__ "originalStatusCode" StatusCode.of_json in
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?resourceName ?originalStatusCode ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request failed due to an error while processing the model."]
module ModelNotReadyException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The model specified in the request is not ready to serve inference requests. The AWS SDK will automatically retry the operation up to 5 times. For information about configuring automatic retries, see Retry behavior in the AWS SDKs and Tools reference guide."]
module ResponseStream =
  struct
    type nonrec t =
      {
      chunk: PayloadPart.t option
        [@ocaml.doc "Content included in the response."];
      internalServerException: InternalServerException.t option
        [@ocaml.doc "An internal server error occurred. Retry your request."];
      modelStreamErrorException: ModelStreamErrorException.t option
        [@ocaml.doc
          "An error occurred while streaming the response. Retry your request."];
      validationException: ValidationException.t option
        [@ocaml.doc
          "Input validation failed. Check your request parameters and retry the request."];
      throttlingException: ThrottlingException.t option
        [@ocaml.doc
          "Your request was throttled because of service-wide limitations. Resubmit your request later or in a different region. You can also purchase Provisioned Throughput to increase the rate or number of tokens you can process."];
      modelTimeoutException: ModelTimeoutException.t option
        [@ocaml.doc
          "The request took too long to process. Processing time exceeded the model timeout length."];
      serviceUnavailableException: ServiceUnavailableException.t option
        [@ocaml.doc "The service isn't available. Try again later."]}
    let make ?chunk =
      fun ?internalServerException ->
        fun ?modelStreamErrorException ->
          fun ?validationException ->
            fun ?throttlingException ->
              fun ?modelTimeoutException ->
                fun ?serviceUnavailableException ->
                  fun () ->
                    {
                      chunk;
                      internalServerException;
                      modelStreamErrorException;
                      validationException;
                      throttlingException;
                      modelTimeoutException;
                      serviceUnavailableException
                    }
    let to_value x =
      structure_to_value
        [("chunk", (Option.map x.chunk ~f:PayloadPart.to_value));
        ("internalServerException",
          (Option.map x.internalServerException
             ~f:InternalServerException.to_value));
        ("modelStreamErrorException",
          (Option.map x.modelStreamErrorException
             ~f:ModelStreamErrorException.to_value));
        ("validationException",
          (Option.map x.validationException ~f:ValidationException.to_value));
        ("throttlingException",
          (Option.map x.throttlingException ~f:ThrottlingException.to_value));
        ("modelTimeoutException",
          (Option.map x.modelTimeoutException
             ~f:ModelTimeoutException.to_value));
        ("serviceUnavailableException",
          (Option.map x.serviceUnavailableException
             ~f:ServiceUnavailableException.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceUnavailableException =
        (Option.map ~f:ServiceUnavailableException.of_xml)
          (Xml.child xml_arg0 "serviceUnavailableException") in
      let modelTimeoutException =
        (Option.map ~f:ModelTimeoutException.of_xml)
          (Xml.child xml_arg0 "modelTimeoutException") in
      let throttlingException =
        (Option.map ~f:ThrottlingException.of_xml)
          (Xml.child xml_arg0 "throttlingException") in
      let validationException =
        (Option.map ~f:ValidationException.of_xml)
          (Xml.child xml_arg0 "validationException") in
      let modelStreamErrorException =
        (Option.map ~f:ModelStreamErrorException.of_xml)
          (Xml.child xml_arg0 "modelStreamErrorException") in
      let internalServerException =
        (Option.map ~f:InternalServerException.of_xml)
          (Xml.child xml_arg0 "internalServerException") in
      let chunk =
        (Option.map ~f:PayloadPart.of_xml) (Xml.child xml_arg0 "chunk") in
      make ?serviceUnavailableException ?modelTimeoutException
        ?throttlingException ?validationException ?modelStreamErrorException
        ?internalServerException ?chunk ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serviceUnavailableException =
        field_map json__ "serviceUnavailableException"
          ServiceUnavailableException.of_json in
      let modelTimeoutException =
        field_map json__ "modelTimeoutException"
          ModelTimeoutException.of_json in
      let throttlingException =
        field_map json__ "throttlingException" ThrottlingException.of_json in
      let validationException =
        field_map json__ "validationException" ValidationException.of_json in
      let modelStreamErrorException =
        field_map json__ "modelStreamErrorException"
          ModelStreamErrorException.of_json in
      let internalServerException =
        field_map json__ "internalServerException"
          InternalServerException.of_json in
      let chunk = field_map json__ "chunk" PayloadPart.of_json in
      make ?serviceUnavailableException ?modelTimeoutException
        ?throttlingException ?validationException ?modelStreamErrorException
        ?internalServerException ?chunk ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Definition of content in the response stream."]
module InvokeModelIdentifier =
  struct
    type nonrec t = string
    let context_ = "InvokeModelIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:(([0-9]{12}:custom-model/[a-z0-9-]{1,63}[.]{1}[a-z0-9-]{1,63}/[a-z0-9]{12})|(:foundation-model/[a-z0-9-]{1,63}[.]{1}[a-z0-9-]{1,63}([.:]?[a-z0-9-]{1,63}))|([0-9]{12}:imported-model/[a-z0-9]{12})|([0-9]{12}:provisioned-model/[a-z0-9]{12})|([0-9]{12}:custom-model-deployment/[a-z0-9]{12})|([0-9]{12}:(inference-profile|application-inference-profile)/[a-zA-Z0-9-:.]+)))|([a-z0-9-]{1,63}[.]{1}[a-z0-9-]{1,63}([.:]?[a-z0-9-]{1,63}))|(([0-9a-zA-Z][_-]?)+)|([a-zA-Z0-9-:.]+)$|(^(arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:prompt/[0-9a-zA-Z]{10}(?::[0-9]{1,5})?))$|(^arn:aws:sagemaker:[a-z0-9-]+:[0-9]{12}:endpoint/[a-zA-Z0-9-]+$)|(^arn:aws(-[^:]+)?:bedrock:([0-9a-z-]{1,20}):([0-9]{12}):(default-)?prompt-router/[a-zA-Z0-9-:.]+$)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InvokeModelIdentifier" j
    let to_json = simple_to_json to_value
  end
module Trace =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | ENABLED_FULL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | ENABLED_FULL -> "ENABLED_FULL"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | "ENABLED_FULL" -> ENABLED_FULL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Trace" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Trace" j)
    let to_json = simple_to_json to_value
  end
module InvokeModelWithBidirectionalStreamOutput =
  struct
    type nonrec t =
      {
      chunk: BidirectionalOutputPayloadPart.t option
        [@ocaml.doc
          "The speech chunk that was provided as output from the invocation step."];
      internalServerException: InternalServerException.t option
        [@ocaml.doc "The request encountered an unknown internal error."];
      modelStreamErrorException: ModelStreamErrorException.t option
        [@ocaml.doc
          "The request encountered an error with the model stream."];
      validationException: ValidationException.t option
        [@ocaml.doc
          "The input fails to satisfy the constraints specified by an Amazon Web Services service."];
      throttlingException: ThrottlingException.t option
        [@ocaml.doc "The request was denied due to request throttling."];
      modelTimeoutException: ModelTimeoutException.t option
        [@ocaml.doc
          "The connection was closed because a request was not received within the timeout period."];
      serviceUnavailableException: ServiceUnavailableException.t option
        [@ocaml.doc
          "The request has failed due to a temporary failure of the server."]}
    let make ?chunk =
      fun ?internalServerException ->
        fun ?modelStreamErrorException ->
          fun ?validationException ->
            fun ?throttlingException ->
              fun ?modelTimeoutException ->
                fun ?serviceUnavailableException ->
                  fun () ->
                    {
                      chunk;
                      internalServerException;
                      modelStreamErrorException;
                      validationException;
                      throttlingException;
                      modelTimeoutException;
                      serviceUnavailableException
                    }
    let to_value x =
      structure_to_value
        [("chunk",
           (Option.map x.chunk ~f:BidirectionalOutputPayloadPart.to_value));
        ("internalServerException",
          (Option.map x.internalServerException
             ~f:InternalServerException.to_value));
        ("modelStreamErrorException",
          (Option.map x.modelStreamErrorException
             ~f:ModelStreamErrorException.to_value));
        ("validationException",
          (Option.map x.validationException ~f:ValidationException.to_value));
        ("throttlingException",
          (Option.map x.throttlingException ~f:ThrottlingException.to_value));
        ("modelTimeoutException",
          (Option.map x.modelTimeoutException
             ~f:ModelTimeoutException.to_value));
        ("serviceUnavailableException",
          (Option.map x.serviceUnavailableException
             ~f:ServiceUnavailableException.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceUnavailableException =
        (Option.map ~f:ServiceUnavailableException.of_xml)
          (Xml.child xml_arg0 "serviceUnavailableException") in
      let modelTimeoutException =
        (Option.map ~f:ModelTimeoutException.of_xml)
          (Xml.child xml_arg0 "modelTimeoutException") in
      let throttlingException =
        (Option.map ~f:ThrottlingException.of_xml)
          (Xml.child xml_arg0 "throttlingException") in
      let validationException =
        (Option.map ~f:ValidationException.of_xml)
          (Xml.child xml_arg0 "validationException") in
      let modelStreamErrorException =
        (Option.map ~f:ModelStreamErrorException.of_xml)
          (Xml.child xml_arg0 "modelStreamErrorException") in
      let internalServerException =
        (Option.map ~f:InternalServerException.of_xml)
          (Xml.child xml_arg0 "internalServerException") in
      let chunk =
        (Option.map ~f:BidirectionalOutputPayloadPart.of_xml)
          (Xml.child xml_arg0 "chunk") in
      make ?serviceUnavailableException ?modelTimeoutException
        ?throttlingException ?validationException ?modelStreamErrorException
        ?internalServerException ?chunk ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serviceUnavailableException =
        field_map json__ "serviceUnavailableException"
          ServiceUnavailableException.of_json in
      let modelTimeoutException =
        field_map json__ "modelTimeoutException"
          ModelTimeoutException.of_json in
      let throttlingException =
        field_map json__ "throttlingException" ThrottlingException.of_json in
      let validationException =
        field_map json__ "validationException" ValidationException.of_json in
      let modelStreamErrorException =
        field_map json__ "modelStreamErrorException"
          ModelStreamErrorException.of_json in
      let internalServerException =
        field_map json__ "internalServerException"
          InternalServerException.of_json in
      let chunk =
        field_map json__ "chunk" BidirectionalOutputPayloadPart.of_json in
      make ?serviceUnavailableException ?modelTimeoutException
        ?throttlingException ?validationException ?modelStreamErrorException
        ?internalServerException ?chunk ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Output from the bidirectional stream that was used for model invocation."]
module InvokeModelWithBidirectionalStreamInput =
  struct
    type nonrec t =
      {
      chunk: BidirectionalInputPayloadPart.t option
        [@ocaml.doc
          "The audio chunk that is used as input for the invocation step."]}
    let make ?chunk = fun () -> { chunk }
    let to_value x =
      structure_to_value
        [("chunk",
           (Option.map x.chunk ~f:BidirectionalInputPayloadPart.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let chunk =
        (Option.map ~f:BidirectionalInputPayloadPart.of_xml)
          (Xml.child xml_arg0 "chunk") in
      make ?chunk ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let chunk =
        field_map json__ "chunk" BidirectionalInputPayloadPart.of_json in
      make ?chunk ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Payload content, the speech chunk, for the bidirectional input of the invocation step."]
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CountTokensInput =
  struct
    type nonrec t =
      {
      invokeModel: InvokeModelTokensRequest.t option
        [@ocaml.doc
          "An InvokeModel request for which to count tokens. Use this field when you want to count tokens for a raw text input that would be sent to the InvokeModel operation."];
      converse: ConverseTokensRequest.t option
        [@ocaml.doc
          "A Converse request for which to count tokens. Use this field when you want to count tokens for a conversation-based input that would be sent to the Converse operation."]}
    let make ?invokeModel =
      fun ?converse -> fun () -> { invokeModel; converse }
    let to_value x =
      structure_to_value
        [("invokeModel",
           (Option.map x.invokeModel ~f:InvokeModelTokensRequest.to_value));
        ("converse",
          (Option.map x.converse ~f:ConverseTokensRequest.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let converse =
        (Option.map ~f:ConverseTokensRequest.of_xml)
          (Xml.child xml_arg0 "converse") in
      let invokeModel =
        (Option.map ~f:InvokeModelTokensRequest.of_xml)
          (Xml.child xml_arg0 "invokeModel") in
      make ?converse ?invokeModel ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let converse =
        field_map json__ "converse" ConverseTokensRequest.of_json in
      let invokeModel =
        field_map json__ "invokeModel" InvokeModelTokensRequest.of_json in
      make ?converse ?invokeModel ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The input value for token counting. The value should be either an InvokeModel or Converse request body."]
module FoundationModelVersionIdentifier =
  struct
    type nonrec t = string[@@ocaml.doc "ARN or ID of a Bedrock model"]
    let context_ = "FoundationModelVersionIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z_\\.\\-/0-9:]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FoundationModelVersionIdentifier" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "ARN or ID of a Bedrock model"]
module ConverseStreamOutput =
  struct
    type nonrec t =
      {
      messageStart: MessageStartEvent.t option
        [@ocaml.doc "Message start information."];
      contentBlockStart: ContentBlockStartEvent.t option
        [@ocaml.doc "Start information for a content block."];
      contentBlockDelta: ContentBlockDeltaEvent.t option
        [@ocaml.doc "The messages output content block delta."];
      contentBlockStop: ContentBlockStopEvent.t option
        [@ocaml.doc "Stop information for a content block."];
      messageStop: MessageStopEvent.t option
        [@ocaml.doc "Message stop information."];
      metadata: ConverseStreamMetadataEvent.t option
        [@ocaml.doc "Metadata for the converse output stream."];
      internalServerException: InternalServerException.t option
        [@ocaml.doc "An internal server error occurred. Retry your request."];
      modelStreamErrorException: ModelStreamErrorException.t option
        [@ocaml.doc "A streaming error occurred. Retry your request."];
      validationException: ValidationException.t option
        [@ocaml.doc
          "The input fails to satisfy the constraints specified by Amazon Bedrock. For troubleshooting this error, see ValidationError in the Amazon Bedrock User Guide."];
      throttlingException: ThrottlingException.t option
        [@ocaml.doc
          "Your request was denied due to exceeding the account quotas for Amazon Bedrock. For troubleshooting this error, see ThrottlingException in the Amazon Bedrock User Guide."];
      serviceUnavailableException: ServiceUnavailableException.t option
        [@ocaml.doc
          "The service isn't currently available. For troubleshooting this error, see ServiceUnavailable in the Amazon Bedrock User Guide"]}
    let make ?messageStart =
      fun ?contentBlockStart ->
        fun ?contentBlockDelta ->
          fun ?contentBlockStop ->
            fun ?messageStop ->
              fun ?metadata ->
                fun ?internalServerException ->
                  fun ?modelStreamErrorException ->
                    fun ?validationException ->
                      fun ?throttlingException ->
                        fun ?serviceUnavailableException ->
                          fun () ->
                            {
                              messageStart;
                              contentBlockStart;
                              contentBlockDelta;
                              contentBlockStop;
                              messageStop;
                              metadata;
                              internalServerException;
                              modelStreamErrorException;
                              validationException;
                              throttlingException;
                              serviceUnavailableException
                            }
    let to_value x =
      structure_to_value
        [("messageStart",
           (Option.map x.messageStart ~f:MessageStartEvent.to_value));
        ("contentBlockStart",
          (Option.map x.contentBlockStart ~f:ContentBlockStartEvent.to_value));
        ("contentBlockDelta",
          (Option.map x.contentBlockDelta ~f:ContentBlockDeltaEvent.to_value));
        ("contentBlockStop",
          (Option.map x.contentBlockStop ~f:ContentBlockStopEvent.to_value));
        ("messageStop",
          (Option.map x.messageStop ~f:MessageStopEvent.to_value));
        ("metadata",
          (Option.map x.metadata ~f:ConverseStreamMetadataEvent.to_value));
        ("internalServerException",
          (Option.map x.internalServerException
             ~f:InternalServerException.to_value));
        ("modelStreamErrorException",
          (Option.map x.modelStreamErrorException
             ~f:ModelStreamErrorException.to_value));
        ("validationException",
          (Option.map x.validationException ~f:ValidationException.to_value));
        ("throttlingException",
          (Option.map x.throttlingException ~f:ThrottlingException.to_value));
        ("serviceUnavailableException",
          (Option.map x.serviceUnavailableException
             ~f:ServiceUnavailableException.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceUnavailableException =
        (Option.map ~f:ServiceUnavailableException.of_xml)
          (Xml.child xml_arg0 "serviceUnavailableException") in
      let throttlingException =
        (Option.map ~f:ThrottlingException.of_xml)
          (Xml.child xml_arg0 "throttlingException") in
      let validationException =
        (Option.map ~f:ValidationException.of_xml)
          (Xml.child xml_arg0 "validationException") in
      let modelStreamErrorException =
        (Option.map ~f:ModelStreamErrorException.of_xml)
          (Xml.child xml_arg0 "modelStreamErrorException") in
      let internalServerException =
        (Option.map ~f:InternalServerException.of_xml)
          (Xml.child xml_arg0 "internalServerException") in
      let metadata =
        (Option.map ~f:ConverseStreamMetadataEvent.of_xml)
          (Xml.child xml_arg0 "metadata") in
      let messageStop =
        (Option.map ~f:MessageStopEvent.of_xml)
          (Xml.child xml_arg0 "messageStop") in
      let contentBlockStop =
        (Option.map ~f:ContentBlockStopEvent.of_xml)
          (Xml.child xml_arg0 "contentBlockStop") in
      let contentBlockDelta =
        (Option.map ~f:ContentBlockDeltaEvent.of_xml)
          (Xml.child xml_arg0 "contentBlockDelta") in
      let contentBlockStart =
        (Option.map ~f:ContentBlockStartEvent.of_xml)
          (Xml.child xml_arg0 "contentBlockStart") in
      let messageStart =
        (Option.map ~f:MessageStartEvent.of_xml)
          (Xml.child xml_arg0 "messageStart") in
      make ?serviceUnavailableException ?throttlingException
        ?validationException ?modelStreamErrorException
        ?internalServerException ?metadata ?messageStop ?contentBlockStop
        ?contentBlockDelta ?contentBlockStart ?messageStart ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serviceUnavailableException =
        field_map json__ "serviceUnavailableException"
          ServiceUnavailableException.of_json in
      let throttlingException =
        field_map json__ "throttlingException" ThrottlingException.of_json in
      let validationException =
        field_map json__ "validationException" ValidationException.of_json in
      let modelStreamErrorException =
        field_map json__ "modelStreamErrorException"
          ModelStreamErrorException.of_json in
      let internalServerException =
        field_map json__ "internalServerException"
          InternalServerException.of_json in
      let metadata =
        field_map json__ "metadata" ConverseStreamMetadataEvent.of_json in
      let messageStop =
        field_map json__ "messageStop" MessageStopEvent.of_json in
      let contentBlockStop =
        field_map json__ "contentBlockStop" ContentBlockStopEvent.of_json in
      let contentBlockDelta =
        field_map json__ "contentBlockDelta" ContentBlockDeltaEvent.of_json in
      let contentBlockStart =
        field_map json__ "contentBlockStart" ContentBlockStartEvent.of_json in
      let messageStart =
        field_map json__ "messageStart" MessageStartEvent.of_json in
      make ?serviceUnavailableException ?throttlingException
        ?validationException ?modelStreamErrorException
        ?internalServerException ?metadata ?messageStop ?contentBlockStop
        ?contentBlockDelta ?contentBlockStart ?messageStart ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The messages output stream"]
module ConversationalModelId =
  struct
    type nonrec t = string
    let context_ = "ConversationalModelId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:(([0-9]{12}:custom-model/[a-z0-9-]{1,63}[.]{1}[a-z0-9-]{1,63}/[a-z0-9]{12})|(:foundation-model/[a-z0-9-]{1,63}[.]{1}[a-z0-9-]{1,63}([.:]?[a-z0-9-]{1,63}))|([0-9]{12}:imported-model/[a-z0-9]{12})|([0-9]{12}:provisioned-model/[a-z0-9]{12})|([0-9]{12}:custom-model-deployment/[a-z0-9]{12})|([0-9]{12}:(inference-profile|application-inference-profile)/[a-zA-Z0-9-:.]+)))|([a-z0-9-]{1,63}[.]{1}[a-z0-9-]{1,63}([.:]?[a-z0-9-]{1,63}))|(([0-9a-zA-Z][_-]?)+)|([a-zA-Z0-9-:.]+)|(^(arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:prompt/[0-9a-zA-Z]{10}(?::[0-9]{1,5})?))$|(^arn:aws:sagemaker:[a-z0-9-]+:[0-9]{12}:endpoint/[a-zA-Z0-9-]+$)|(^arn:aws(-[^:]+)?:bedrock:([0-9a-z-]{1,20}):([0-9]{12}):(default-)?prompt-router/[a-zA-Z0-9-:.]+$)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ConversationalModelId" j
    let to_json = simple_to_json to_value
  end
module ConverseStreamRequestAdditionalModelResponseFieldPathsList =
  struct
    type nonrec t =
      ConverseStreamRequestAdditionalModelResponseFieldPathsListMemberString.t
        list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |>
         (List.map
            ~f:ConverseStreamRequestAdditionalModelResponseFieldPathsListMemberString.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ConverseStreamRequestAdditionalModelResponseFieldPathsListMemberString.of_xml)
    let of_json j =
      list_of_json
        ~kind:"ConverseStreamRequestAdditionalModelResponseFieldPathsList"
        ~of_json:ConverseStreamRequestAdditionalModelResponseFieldPathsListMemberString.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module GuardrailStreamConfiguration =
  struct
    type nonrec t =
      {
      guardrailIdentifier: GuardrailIdentifier.t option
        [@ocaml.doc "The identifier for the guardrail."];
      guardrailVersion: GuardrailVersion.t option
        [@ocaml.doc "The version of the guardrail."];
      trace: GuardrailTrace.t option
        [@ocaml.doc "The trace behavior for the guardrail."];
      streamProcessingMode: GuardrailStreamProcessingMode.t option
        [@ocaml.doc
          "The processing mode. The processing mode. For more information, see Configure streaming response behavior in the Amazon Bedrock User Guide."]}
    let make ?guardrailIdentifier =
      fun ?guardrailVersion ->
        fun ?trace ->
          fun ?streamProcessingMode ->
            fun () ->
              {
                guardrailIdentifier;
                guardrailVersion;
                trace;
                streamProcessingMode
              }
    let to_value x =
      structure_to_value
        [("guardrailIdentifier",
           (Option.map x.guardrailIdentifier ~f:GuardrailIdentifier.to_value));
        ("guardrailVersion",
          (Option.map x.guardrailVersion ~f:GuardrailVersion.to_value));
        ("trace", (Option.map x.trace ~f:GuardrailTrace.to_value));
        ("streamProcessingMode",
          (Option.map x.streamProcessingMode
             ~f:GuardrailStreamProcessingMode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamProcessingMode =
        (Option.map ~f:GuardrailStreamProcessingMode.of_xml)
          (Xml.child xml_arg0 "streamProcessingMode") in
      let trace =
        (Option.map ~f:GuardrailTrace.of_xml) (Xml.child xml_arg0 "trace") in
      let guardrailVersion =
        (Option.map ~f:GuardrailVersion.of_xml)
          (Xml.child xml_arg0 "guardrailVersion") in
      let guardrailIdentifier =
        (Option.map ~f:GuardrailIdentifier.of_xml)
          (Xml.child xml_arg0 "guardrailIdentifier") in
      make ?streamProcessingMode ?trace ?guardrailVersion
        ?guardrailIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let streamProcessingMode =
        field_map json__ "streamProcessingMode"
          GuardrailStreamProcessingMode.of_json in
      let trace = field_map json__ "trace" GuardrailTrace.of_json in
      let guardrailVersion =
        field_map json__ "guardrailVersion" GuardrailVersion.of_json in
      let guardrailIdentifier =
        field_map json__ "guardrailIdentifier" GuardrailIdentifier.of_json in
      make ?streamProcessingMode ?trace ?guardrailVersion
        ?guardrailIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration information for a guardrail that you use with the ConverseStream action."]
module InferenceConfiguration =
  struct
    type nonrec t =
      {
      maxTokens: InferenceConfigurationMaxTokensInteger.t option
        [@ocaml.doc
          "The maximum number of tokens to allow in the generated response. The default value is the maximum allowed value for the model that you are using. For more information, see Inference parameters for foundation models."];
      temperature: InferenceConfigurationTemperatureFloat.t option
        [@ocaml.doc
          "The likelihood of the model selecting higher-probability options while generating a response. A lower value makes the model more likely to choose higher-probability options, while a higher value makes the model more likely to choose lower-probability options. The default value is the default value for the model that you are using. For more information, see Inference parameters for foundation models."];
      topP: InferenceConfigurationTopPFloat.t option
        [@ocaml.doc
          "The percentage of most-likely candidates that the model considers for the next token. For example, if you choose a value of 0.8 for topP, the model selects from the top 80% of the probability distribution of tokens that could be next in the sequence. The default value is the default value for the model that you are using. For more information, see Inference parameters for foundation models."];
      stopSequences: InferenceConfigurationStopSequencesList.t option
        [@ocaml.doc
          "A list of stop sequences. A stop sequence is a sequence of characters that causes the model to stop generating the response."]}
    let make ?maxTokens =
      fun ?temperature ->
        fun ?topP ->
          fun ?stopSequences ->
            fun () -> { maxTokens; temperature; topP; stopSequences }
    let to_value x =
      structure_to_value
        [("maxTokens",
           (Option.map x.maxTokens
              ~f:InferenceConfigurationMaxTokensInteger.to_value));
        ("temperature",
          (Option.map x.temperature
             ~f:InferenceConfigurationTemperatureFloat.to_value));
        ("topP",
          (Option.map x.topP ~f:InferenceConfigurationTopPFloat.to_value));
        ("stopSequences",
          (Option.map x.stopSequences
             ~f:InferenceConfigurationStopSequencesList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stopSequences =
        (Option.map ~f:InferenceConfigurationStopSequencesList.of_xml)
          (Xml.child xml_arg0 "stopSequences") in
      let topP =
        (Option.map ~f:InferenceConfigurationTopPFloat.of_xml)
          (Xml.child xml_arg0 "topP") in
      let temperature =
        (Option.map ~f:InferenceConfigurationTemperatureFloat.of_xml)
          (Xml.child xml_arg0 "temperature") in
      let maxTokens =
        (Option.map ~f:InferenceConfigurationMaxTokensInteger.of_xml)
          (Xml.child xml_arg0 "maxTokens") in
      make ?stopSequences ?topP ?temperature ?maxTokens ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let stopSequences =
        field_map json__ "stopSequences"
          InferenceConfigurationStopSequencesList.of_json in
      let topP =
        field_map json__ "topP" InferenceConfigurationTopPFloat.of_json in
      let temperature =
        field_map json__ "temperature"
          InferenceConfigurationTemperatureFloat.of_json in
      let maxTokens =
        field_map json__ "maxTokens"
          InferenceConfigurationMaxTokensInteger.of_json in
      make ?stopSequences ?topP ?temperature ?maxTokens ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Base inference parameters to pass to a model in a call to Converse or ConverseStream. For more information, see Inference parameters for foundation models. If you need to pass additional parameters that the model supports, use the additionalModelRequestFields request field in the call to Converse or ConverseStream. For more information, see Model parameters."]
module OutputConfig =
  struct
    type nonrec t =
      {
      textFormat: OutputFormat.t option
        [@ocaml.doc
          "Structured output parameters to control the model's text response."]}
    let make ?textFormat = fun () -> { textFormat }
    let to_value x =
      structure_to_value
        [("textFormat", (Option.map x.textFormat ~f:OutputFormat.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let textFormat =
        (Option.map ~f:OutputFormat.of_xml) (Xml.child xml_arg0 "textFormat") in
      make ?textFormat ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let textFormat = field_map json__ "textFormat" OutputFormat.of_json in
      make ?textFormat ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Output configuration for a model response in a call to Converse or ConverseStream."]
module PromptVariableMap =
  struct
    type nonrec t = (String_.t * PromptVariableValues.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types String PromptVariableValues"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x ->
                       (PromptVariableValues.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:PromptVariableValues.of_json j
    let to_json v = composed_to_json to_value v
  end
module RequestMetadata =
  struct
    type nonrec t =
      (RequestMetadataKeyString.t * RequestMetadataValueString.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:16) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((RequestMetadataKeyString.of_string chopped),
                              (RequestMetadataValueString.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (RequestMetadataKeyString.to_value x) |>
                    (fun x ->
                       (RequestMetadataValueString.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:RequestMetadataKeyString.of_string
        ~of_json:RequestMetadataValueString.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConverseMetrics =
  struct
    type nonrec t =
      {
      latencyMs: Long.t option
        [@ocaml.doc "The latency of the call to Converse, in milliseconds."]}
    let make ?latencyMs = fun () -> { latencyMs }
    let to_value x =
      structure_to_value
        [("latencyMs", (Option.map x.latencyMs ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let latencyMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "latencyMs") in
      make ?latencyMs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let latencyMs = field_map json__ "latencyMs" Long.of_json in
      make ?latencyMs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Metrics for a call to Converse."]
module ConverseOutput =
  struct
    type nonrec t =
      {
      message: Message.t option
        [@ocaml.doc "The message that the model generates."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:Message.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" Message.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output from a call to Converse."]
module ConverseTrace =
  struct
    type nonrec t =
      {
      guardrail: GuardrailTraceAssessment.t option
        [@ocaml.doc "The guardrail trace object."];
      promptRouter: PromptRouterTrace.t option
        [@ocaml.doc "The request's prompt router."]}
    let make ?guardrail =
      fun ?promptRouter -> fun () -> { guardrail; promptRouter }
    let to_value x =
      structure_to_value
        [("guardrail",
           (Option.map x.guardrail ~f:GuardrailTraceAssessment.to_value));
        ("promptRouter",
          (Option.map x.promptRouter ~f:PromptRouterTrace.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let promptRouter =
        (Option.map ~f:PromptRouterTrace.of_xml)
          (Xml.child xml_arg0 "promptRouter") in
      let guardrail =
        (Option.map ~f:GuardrailTraceAssessment.of_xml)
          (Xml.child xml_arg0 "guardrail") in
      make ?promptRouter ?guardrail ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let promptRouter =
        field_map json__ "promptRouter" PromptRouterTrace.of_json in
      let guardrail =
        field_map json__ "guardrail" GuardrailTraceAssessment.of_json in
      make ?promptRouter ?guardrail ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The trace object in a response from Converse."]
module ConverseRequestAdditionalModelResponseFieldPathsList =
  struct
    type nonrec t =
      ConverseRequestAdditionalModelResponseFieldPathsListMemberString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |>
         (List.map
            ~f:ConverseRequestAdditionalModelResponseFieldPathsListMemberString.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ConverseRequestAdditionalModelResponseFieldPathsListMemberString.of_xml)
    let of_json j =
      list_of_json
        ~kind:"ConverseRequestAdditionalModelResponseFieldPathsList"
        ~of_json:ConverseRequestAdditionalModelResponseFieldPathsListMemberString.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module GuardrailConfiguration =
  struct
    type nonrec t =
      {
      guardrailIdentifier: GuardrailIdentifier.t option
        [@ocaml.doc "The identifier for the guardrail."];
      guardrailVersion: GuardrailVersion.t option
        [@ocaml.doc "The version of the guardrail."];
      trace: GuardrailTrace.t option
        [@ocaml.doc "The trace behavior for the guardrail."]}
    let make ?guardrailIdentifier =
      fun ?guardrailVersion ->
        fun ?trace ->
          fun () -> { guardrailIdentifier; guardrailVersion; trace }
    let to_value x =
      structure_to_value
        [("guardrailIdentifier",
           (Option.map x.guardrailIdentifier ~f:GuardrailIdentifier.to_value));
        ("guardrailVersion",
          (Option.map x.guardrailVersion ~f:GuardrailVersion.to_value));
        ("trace", (Option.map x.trace ~f:GuardrailTrace.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trace =
        (Option.map ~f:GuardrailTrace.of_xml) (Xml.child xml_arg0 "trace") in
      let guardrailVersion =
        (Option.map ~f:GuardrailVersion.of_xml)
          (Xml.child xml_arg0 "guardrailVersion") in
      let guardrailIdentifier =
        (Option.map ~f:GuardrailIdentifier.of_xml)
          (Xml.child xml_arg0 "guardrailIdentifier") in
      make ?trace ?guardrailVersion ?guardrailIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trace = field_map json__ "trace" GuardrailTrace.of_json in
      let guardrailVersion =
        field_map json__ "guardrailVersion" GuardrailVersion.of_json in
      let guardrailIdentifier =
        field_map json__ "guardrailIdentifier" GuardrailIdentifier.of_json in
      make ?trace ?guardrailVersion ?guardrailIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration information for a guardrail that you use with the Converse operation."]
module GuardrailAction =
  struct
    type nonrec t =
      | NONE 
      | GUARDRAIL_INTERVENED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NONE -> "NONE"
      | GUARDRAIL_INTERVENED -> "GUARDRAIL_INTERVENED"
      | Non_static_id s -> s
    let of_string =
      function
      | "NONE" -> NONE
      | "GUARDRAIL_INTERVENED" -> GUARDRAIL_INTERVENED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration GuardrailAction" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GuardrailAction" j)
    let to_json = simple_to_json to_value
  end
module GuardrailOutputContentList =
  struct
    type nonrec t = GuardrailOutputContent.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailOutputContent.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailOutputContent.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailOutputContentList"
        ~of_json:GuardrailOutputContent.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailContentBlockList =
  struct
    type nonrec t = GuardrailContentBlock.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailContentBlock.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailContentBlock.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailContentBlockList"
        ~of_json:GuardrailContentBlock.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailContentSource =
  struct
    type nonrec t =
      | INPUT 
      | OUTPUT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | INPUT -> "INPUT" | OUTPUT -> "OUTPUT" | Non_static_id s -> s
    let of_string =
      function | "INPUT" -> INPUT | "OUTPUT" -> OUTPUT | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailContentSource" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailContentSource" j)
    let to_json = simple_to_json to_value
  end
module GuardrailOutputScope =
  struct
    type nonrec t =
      | INTERVENTIONS 
      | FULL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INTERVENTIONS -> "INTERVENTIONS"
      | FULL -> "FULL"
      | Non_static_id s -> s
    let of_string =
      function
      | "INTERVENTIONS" -> INTERVENTIONS
      | "FULL" -> FULL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailOutputScope" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GuardrailOutputScope" j)
    let to_json = simple_to_json to_value
  end
module StartAsyncInvokeResponse =
  struct
    type nonrec t =
      {
      invocationArn: InvocationArn.t option
        [@ocaml.doc "The ARN of the invocation."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?invocationArn = fun () -> { invocationArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("invocationArn",
           (Option.map x.invocationArn ~f:InvocationArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let invocationArn =
        (Option.map ~f:InvocationArn.of_xml)
          (Xml.child xml_arg0 "invocationArn") in
      make ?invocationArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let invocationArn =
        field_map json__ "invocationArn" InvocationArn.of_json in
      make ?invocationArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts an asynchronous invocation. This operation requires permission for the bedrock:InvokeModel action. To deny all inference access to resources that you specify in the modelId field, you need to deny access to the bedrock:InvokeModel and bedrock:InvokeModelWithResponseStream actions. Doing this also denies access to the resource through the Converse API actions (Converse and ConverseStream). For more information see Deny access for inference on specific models."]
module StartAsyncInvokeRequest =
  struct
    type nonrec t =
      {
      clientRequestToken: AsyncInvokeIdempotencyToken.t option
        [@ocaml.doc
          "Specify idempotency token to ensure that requests are not duplicated."];
      modelId: AsyncInvokeIdentifier.t [@ocaml.doc "The model to invoke."];
      modelInput: ModelInputPayload.t
        [@ocaml.doc "Input to send to the model."];
      outputDataConfig: AsyncInvokeOutputDataConfig.t
        [@ocaml.doc "Where to store the output."];
      tags: TagList.t option [@ocaml.doc "Tags to apply to the invocation."]}
    let context_ = "StartAsyncInvokeRequest"
    let make ?clientRequestToken =
      fun ?tags ->
        fun ~modelId ->
          fun ~modelInput ->
            fun ~outputDataConfig ->
              fun () ->
                {
                  clientRequestToken;
                  tags;
                  modelId;
                  modelInput;
                  outputDataConfig
                }
    let to_value x =
      structure_to_value
        [("clientRequestToken",
           (Option.map x.clientRequestToken
              ~f:AsyncInvokeIdempotencyToken.to_value));
        ("modelId", (Some (AsyncInvokeIdentifier.to_value x.modelId)));
        ("modelInput", (Some (ModelInputPayload.to_value x.modelInput)));
        ("outputDataConfig",
          (Some (AsyncInvokeOutputDataConfig.to_value x.outputDataConfig)));
        ("tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let outputDataConfig =
        AsyncInvokeOutputDataConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "outputDataConfig") in
      let modelInput =
        ModelInputPayload.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelInput") in
      let modelId =
        AsyncInvokeIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelId") in
      let clientRequestToken =
        (Option.map ~f:AsyncInvokeIdempotencyToken.of_xml)
          (Xml.child xml_arg0 "clientRequestToken") in
      make ?tags ~outputDataConfig ~modelInput ~modelId ?clientRequestToken
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagList.of_json in
      let outputDataConfig =
        field_map_exn json__ "outputDataConfig"
          AsyncInvokeOutputDataConfig.of_json in
      let modelInput =
        field_map_exn json__ "modelInput" ModelInputPayload.of_json in
      let modelId =
        field_map_exn json__ "modelId" AsyncInvokeIdentifier.of_json in
      let clientRequestToken =
        field_map json__ "clientRequestToken"
          AsyncInvokeIdempotencyToken.of_json in
      make ?tags ~outputDataConfig ~modelInput ~modelId ?clientRequestToken
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts an asynchronous invocation. This operation requires permission for the bedrock:InvokeModel action. To deny all inference access to resources that you specify in the modelId field, you need to deny access to the bedrock:InvokeModel and bedrock:InvokeModelWithResponseStream actions. Doing this also denies access to the resource through the Converse API actions (Converse and ConverseStream). For more information see Deny access for inference on specific models."]
module ListAsyncInvokesResponse =
  struct
    type nonrec t =
      {
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "Specify the pagination token from a previous request to retrieve the next page of results."];
      asyncInvokeSummaries: AsyncInvokeSummaries.t option
        [@ocaml.doc "A list of invocation summaries."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?asyncInvokeSummaries ->
        fun () -> { nextToken; asyncInvokeSummaries }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("asyncInvokeSummaries",
          (Option.map x.asyncInvokeSummaries ~f:AsyncInvokeSummaries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let asyncInvokeSummaries =
        (Option.map ~f:AsyncInvokeSummaries.of_xml)
          (Xml.child xml_arg0 "asyncInvokeSummaries") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ?asyncInvokeSummaries ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let asyncInvokeSummaries =
        field_map json__ "asyncInvokeSummaries" AsyncInvokeSummaries.of_json in
      let nextToken = field_map json__ "nextToken" PaginationToken.of_json in
      make ?asyncInvokeSummaries ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists asynchronous invocations."]
module ListAsyncInvokesRequest =
  struct
    type nonrec t =
      {
      submitTimeAfter: Timestamp.t option
        [@ocaml.doc "Include invocations submitted after this time."];
      submitTimeBefore: Timestamp.t option
        [@ocaml.doc "Include invocations submitted before this time."];
      statusEquals: AsyncInvokeStatus.t option
        [@ocaml.doc "Filter invocations by status."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of invocations to return in one page of results."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "Specify the pagination token from a previous request to retrieve the next page of results."];
      sortBy: SortAsyncInvocationBy.t option
        [@ocaml.doc "How to sort the response."];
      sortOrder: SortOrder.t option
        [@ocaml.doc "The sorting order for the response."]}
    let make ?submitTimeAfter =
      fun ?submitTimeBefore ->
        fun ?statusEquals ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun ?sortBy ->
                fun ?sortOrder ->
                  fun () ->
                    {
                      submitTimeAfter;
                      submitTimeBefore;
                      statusEquals;
                      maxResults;
                      nextToken;
                      sortBy;
                      sortOrder
                    }
    let to_value x =
      structure_to_value
        [("submitTimeAfter",
           (Option.map x.submitTimeAfter ~f:Timestamp.to_value));
        ("submitTimeBefore",
          (Option.map x.submitTimeBefore ~f:Timestamp.to_value));
        ("statusEquals",
          (Option.map x.statusEquals ~f:AsyncInvokeStatus.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("sortBy", (Option.map x.sortBy ~f:SortAsyncInvocationBy.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrder.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortOrder =
        (Option.map ~f:SortOrder.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let sortBy =
        (Option.map ~f:SortAsyncInvocationBy.of_xml)
          (Xml.child xml_arg0 "sortBy") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let statusEquals =
        (Option.map ~f:AsyncInvokeStatus.of_xml)
          (Xml.child xml_arg0 "statusEquals") in
      let submitTimeBefore =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "submitTimeBefore") in
      let submitTimeAfter =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "submitTimeAfter") in
      make ?sortOrder ?sortBy ?nextToken ?maxResults ?statusEquals
        ?submitTimeBefore ?submitTimeAfter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortOrder = field_map json__ "sortOrder" SortOrder.of_json in
      let sortBy = field_map json__ "sortBy" SortAsyncInvocationBy.of_json in
      let nextToken = field_map json__ "nextToken" PaginationToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let statusEquals =
        field_map json__ "statusEquals" AsyncInvokeStatus.of_json in
      let submitTimeBefore =
        field_map json__ "submitTimeBefore" Timestamp.of_json in
      let submitTimeAfter =
        field_map json__ "submitTimeAfter" Timestamp.of_json in
      make ?sortOrder ?sortBy ?nextToken ?maxResults ?statusEquals
        ?submitTimeBefore ?submitTimeAfter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists asynchronous invocations."]
module InvokeModelWithResponseStreamResponse =
  struct
    type nonrec t =
      {
      body: ResponseStream.t option
        [@ocaml.doc
          "Inference response from the model in the format specified by the contentType header. To see the format and content of this field for different models, refer to Inference parameters."];
      contentType: MimeType.t option
        [@ocaml.doc "The MIME type of the inference result."];
      performanceConfigLatency: PerformanceConfigLatency.t option
        [@ocaml.doc "Model performance settings for the request."];
      serviceTier: ServiceTierType.t option
        [@ocaml.doc
          "Specifies the processing tier type used for serving the request."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ModelErrorException of ModelErrorException.t 
      | `ModelNotReadyException of ModelNotReadyException.t 
      | `ModelStreamErrorException of ModelStreamErrorException.t 
      | `ModelTimeoutException of ModelTimeoutException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?body =
      fun ?contentType ->
        fun ?performanceConfigLatency ->
          fun ?serviceTier ->
            fun () ->
              { body; contentType; performanceConfigLatency; serviceTier }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ModelErrorException" ->
          `ModelErrorException (ModelErrorException.of_json json)
      | "ModelNotReadyException" ->
          `ModelNotReadyException (ModelNotReadyException.of_json json)
      | "ModelStreamErrorException" ->
          `ModelStreamErrorException (ModelStreamErrorException.of_json json)
      | "ModelTimeoutException" ->
          `ModelTimeoutException (ModelTimeoutException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ModelErrorException" ->
          `ModelErrorException (ModelErrorException.of_xml xml)
      | "ModelNotReadyException" ->
          `ModelNotReadyException (ModelNotReadyException.of_xml xml)
      | "ModelStreamErrorException" ->
          `ModelStreamErrorException (ModelStreamErrorException.of_xml xml)
      | "ModelTimeoutException" ->
          `ModelTimeoutException (ModelTimeoutException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ModelErrorException e ->
          `Assoc
            [("error", (`String "ModelErrorException"));
            ("details", (ModelErrorException.to_json e))]
      | `ModelNotReadyException e ->
          `Assoc
            [("error", (`String "ModelNotReadyException"));
            ("details", (ModelNotReadyException.to_json e))]
      | `ModelStreamErrorException e ->
          `Assoc
            [("error", (`String "ModelStreamErrorException"));
            ("details", (ModelStreamErrorException.to_json e))]
      | `ModelTimeoutException e ->
          `Assoc
            [("error", (`String "ModelTimeoutException"));
            ("details", (ModelTimeoutException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make ?body:(Some pipe)
            ?contentType:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "X-Amzn-Bedrock-Content-Type")
                            ~f:MimeType.of_string)
            ?performanceConfigLatency:(Option.map
                                         ((List.Assoc.find
                                             ~equal:String.Caseless.equal) xs
                                            "X-Amzn-Bedrock-PerformanceConfig-Latency")
                                         ~f:PerformanceConfigLatency.of_string)
            ?serviceTier:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "X-Amzn-Bedrock-Service-Tier")
                            ~f:ServiceTierType.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("body", (Option.map x.body ~f:ResponseStream.to_value));
        ("X-Amzn-Bedrock-Content-Type",
          (Option.map x.contentType ~f:MimeType.to_value));
        ("X-Amzn-Bedrock-PerformanceConfig-Latency",
          (Option.map x.performanceConfigLatency
             ~f:PerformanceConfigLatency.to_value));
        ("X-Amzn-Bedrock-Service-Tier",
          (Option.map x.serviceTier ~f:ServiceTierType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceTier =
        (Option.map ~f:ServiceTierType.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-Service-Tier") in
      let performanceConfigLatency =
        (Option.map ~f:PerformanceConfigLatency.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-PerformanceConfig-Latency") in
      let contentType =
        (Option.map ~f:MimeType.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-Content-Type") in
      let body =
        (Option.map ~f:ResponseStream.of_xml) (Xml.child xml_arg0 "body") in
      make ?serviceTier ?performanceConfigLatency ?contentType ?body ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serviceTier =
        field_map json__ "serviceTier" ServiceTierType.of_json in
      let performanceConfigLatency =
        field_map json__ "performanceConfigLatency"
          PerformanceConfigLatency.of_json in
      let contentType = field_map json__ "contentType" MimeType.of_json in
      let body = field_map json__ "body" ResponseStream.of_json in
      make ?serviceTier ?performanceConfigLatency ?contentType ?body ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Invoke the specified Amazon Bedrock model to run inference using the prompt and inference parameters provided in the request body. The response is returned in a stream. To see if a model supports streaming, call GetFoundationModel and check the responseStreamingSupported field in the response. The CLI doesn't support streaming operations in Amazon Bedrock, including InvokeModelWithResponseStream. For example code, see Invoke model with streaming code example in the Amazon Bedrock User Guide. This operation requires permissions to perform the bedrock:InvokeModelWithResponseStream action. To deny all inference access to resources that you specify in the modelId field, you need to deny access to the bedrock:InvokeModel and bedrock:InvokeModelWithResponseStream actions. Doing this also denies access to the resource through the Converse API actions (Converse and ConverseStream). For more information see Deny access for inference on specific models. For troubleshooting some of the common errors you might encounter when using the InvokeModelWithResponseStream API, see Troubleshooting Amazon Bedrock API Error Codes in the Amazon Bedrock User Guide"]
module InvokeModelWithResponseStreamRequest =
  struct
    type nonrec t =
      {
      body: Body.t option
        [@ocaml.doc
          "The prompt and inference parameters in the format specified in the contentType in the header. You must provide the body in JSON format. To see the format and content of the request and response bodies for different models, refer to Inference parameters. For more information, see Run inference in the Bedrock User Guide."];
      contentType: MimeType.t option
        [@ocaml.doc
          "The MIME type of the input data in the request. You must specify application/json."];
      accept: MimeType.t option
        [@ocaml.doc
          "The desired MIME type of the inference body in the response. The default value is application/json."];
      modelId: InvokeModelIdentifier.t
        [@ocaml.doc
          "The unique identifier of the model to invoke to run inference. The modelId to provide depends on the type of model or throughput that you use: If you use a base model, specify the model ID or its ARN. For a list of model IDs for base models, see Amazon Bedrock base model IDs (on-demand throughput) in the Amazon Bedrock User Guide. If you use an inference profile, specify the inference profile ID or its ARN. For a list of inference profile IDs, see Supported Regions and models for cross-region inference in the Amazon Bedrock User Guide. If you use a provisioned model, specify the ARN of the Provisioned Throughput. For more information, see Run inference using a Provisioned Throughput in the Amazon Bedrock User Guide. If you use a custom model, specify the ARN of the custom model deployment (for on-demand inference) or the ARN of your provisioned model (for Provisioned Throughput). For more information, see Use a custom model in Amazon Bedrock in the Amazon Bedrock User Guide. If you use an imported model, specify the ARN of the imported model. You can get the model ARN from a successful call to CreateModelImportJob or from the Imported models page in the Amazon Bedrock console."];
      trace: Trace.t option
        [@ocaml.doc
          "Specifies whether to enable or disable the Bedrock trace. If enabled, you can see the full Bedrock trace."];
      guardrailIdentifier: GuardrailIdentifier.t option
        [@ocaml.doc
          "The unique identifier of the guardrail that you want to use. If you don't provide a value, no guardrail is applied to the invocation. An error is thrown in the following situations. You don't provide a guardrail identifier but you specify the amazon-bedrock-guardrailConfig field in the request body. You enable the guardrail but the contentType isn't application/json. You provide a guardrail identifier, but guardrailVersion isn't specified."];
      guardrailVersion: GuardrailVersion.t option
        [@ocaml.doc
          "The version number for the guardrail. The value can also be DRAFT."];
      performanceConfigLatency: PerformanceConfigLatency.t option
        [@ocaml.doc "Model performance settings for the request."];
      serviceTier: ServiceTierType.t option
        [@ocaml.doc
          "Specifies the processing tier type used for serving the request."]}
    let context_ = "InvokeModelWithResponseStreamRequest"
    let make ?body =
      fun ?contentType ->
        fun ?accept ->
          fun ?trace ->
            fun ?guardrailIdentifier ->
              fun ?guardrailVersion ->
                fun ?performanceConfigLatency ->
                  fun ?serviceTier ->
                    fun ~modelId ->
                      fun () ->
                        {
                          body;
                          contentType;
                          accept;
                          trace;
                          guardrailIdentifier;
                          guardrailVersion;
                          performanceConfigLatency;
                          serviceTier;
                          modelId
                        }
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make ?body:(Some pipe)
            ?contentType:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "Content-Type") ~f:MimeType.of_string)
            ?accept:(Option.map
                       ((List.Assoc.find ~equal:String.Caseless.equal) xs
                          "X-Amzn-Bedrock-Accept") ~f:MimeType.of_string)
            ~modelId:(InvokeModelIdentifier.of_string
                        ((List.Assoc.find_exn ~equal:String.Caseless.equal)
                           xs "modelId"))
            ?trace:(Option.map
                      ((List.Assoc.find ~equal:String.Caseless.equal) xs
                         "X-Amzn-Bedrock-Trace") ~f:Trace.of_string)
            ?guardrailIdentifier:(Option.map
                                    ((List.Assoc.find
                                        ~equal:String.Caseless.equal) xs
                                       "X-Amzn-Bedrock-GuardrailIdentifier")
                                    ~f:GuardrailIdentifier.of_string)
            ?guardrailVersion:(Option.map
                                 ((List.Assoc.find
                                     ~equal:String.Caseless.equal) xs
                                    "X-Amzn-Bedrock-GuardrailVersion")
                                 ~f:GuardrailVersion.of_string)
            ?performanceConfigLatency:(Option.map
                                         ((List.Assoc.find
                                             ~equal:String.Caseless.equal) xs
                                            "X-Amzn-Bedrock-PerformanceConfig-Latency")
                                         ~f:PerformanceConfigLatency.of_string)
            ?serviceTier:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "X-Amzn-Bedrock-Service-Tier")
                            ~f:ServiceTierType.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("body", (Option.map x.body ~f:Body.to_value));
        ("Content-Type", (Option.map x.contentType ~f:MimeType.to_value));
        ("X-Amzn-Bedrock-Accept", (Option.map x.accept ~f:MimeType.to_value));
        ("modelId", (Some (InvokeModelIdentifier.to_value x.modelId)));
        ("X-Amzn-Bedrock-Trace", (Option.map x.trace ~f:Trace.to_value));
        ("X-Amzn-Bedrock-GuardrailIdentifier",
          (Option.map x.guardrailIdentifier ~f:GuardrailIdentifier.to_value));
        ("X-Amzn-Bedrock-GuardrailVersion",
          (Option.map x.guardrailVersion ~f:GuardrailVersion.to_value));
        ("X-Amzn-Bedrock-PerformanceConfig-Latency",
          (Option.map x.performanceConfigLatency
             ~f:PerformanceConfigLatency.to_value));
        ("X-Amzn-Bedrock-Service-Tier",
          (Option.map x.serviceTier ~f:ServiceTierType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceTier =
        (Option.map ~f:ServiceTierType.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-Service-Tier") in
      let performanceConfigLatency =
        (Option.map ~f:PerformanceConfigLatency.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-PerformanceConfig-Latency") in
      let guardrailVersion =
        (Option.map ~f:GuardrailVersion.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-GuardrailVersion") in
      let guardrailIdentifier =
        (Option.map ~f:GuardrailIdentifier.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-GuardrailIdentifier") in
      let trace =
        (Option.map ~f:Trace.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-Trace") in
      let modelId =
        InvokeModelIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelId") in
      let accept =
        (Option.map ~f:MimeType.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-Accept") in
      let contentType =
        (Option.map ~f:MimeType.of_xml) (Xml.child xml_arg0 "Content-Type") in
      let body = (Option.map ~f:Body.of_xml) (Xml.child xml_arg0 "body") in
      make ?serviceTier ?performanceConfigLatency ?guardrailVersion
        ?guardrailIdentifier ?trace ~modelId ?accept ?contentType ?body ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serviceTier =
        field_map json__ "serviceTier" ServiceTierType.of_json in
      let performanceConfigLatency =
        field_map json__ "performanceConfigLatency"
          PerformanceConfigLatency.of_json in
      let guardrailVersion =
        field_map json__ "guardrailVersion" GuardrailVersion.of_json in
      let guardrailIdentifier =
        field_map json__ "guardrailIdentifier" GuardrailIdentifier.of_json in
      let trace = field_map json__ "trace" Trace.of_json in
      let modelId =
        field_map_exn json__ "modelId" InvokeModelIdentifier.of_json in
      let accept = field_map json__ "accept" MimeType.of_json in
      let contentType = field_map json__ "contentType" MimeType.of_json in
      let body = field_map json__ "body" Body.of_json in
      make ?serviceTier ?performanceConfigLatency ?guardrailVersion
        ?guardrailIdentifier ?trace ~modelId ?accept ?contentType ?body ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Invoke the specified Amazon Bedrock model to run inference using the prompt and inference parameters provided in the request body. The response is returned in a stream. To see if a model supports streaming, call GetFoundationModel and check the responseStreamingSupported field in the response. The CLI doesn't support streaming operations in Amazon Bedrock, including InvokeModelWithResponseStream. For example code, see Invoke model with streaming code example in the Amazon Bedrock User Guide. This operation requires permissions to perform the bedrock:InvokeModelWithResponseStream action. To deny all inference access to resources that you specify in the modelId field, you need to deny access to the bedrock:InvokeModel and bedrock:InvokeModelWithResponseStream actions. Doing this also denies access to the resource through the Converse API actions (Converse and ConverseStream). For more information see Deny access for inference on specific models. For troubleshooting some of the common errors you might encounter when using the InvokeModelWithResponseStream API, see Troubleshooting Amazon Bedrock API Error Codes in the Amazon Bedrock User Guide"]
module InvokeModelWithBidirectionalStreamResponse =
  struct
    type nonrec t =
      {
      body: InvokeModelWithBidirectionalStreamOutput.t option
        [@ocaml.doc
          "Streaming response from the model in the format specified by the BidirectionalOutputPayloadPart header."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ModelErrorException of ModelErrorException.t 
      | `ModelNotReadyException of ModelNotReadyException.t 
      | `ModelStreamErrorException of ModelStreamErrorException.t 
      | `ModelTimeoutException of ModelTimeoutException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?body = fun () -> { body }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ModelErrorException" ->
          `ModelErrorException (ModelErrorException.of_json json)
      | "ModelNotReadyException" ->
          `ModelNotReadyException (ModelNotReadyException.of_json json)
      | "ModelStreamErrorException" ->
          `ModelStreamErrorException (ModelStreamErrorException.of_json json)
      | "ModelTimeoutException" ->
          `ModelTimeoutException (ModelTimeoutException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ModelErrorException" ->
          `ModelErrorException (ModelErrorException.of_xml xml)
      | "ModelNotReadyException" ->
          `ModelNotReadyException (ModelNotReadyException.of_xml xml)
      | "ModelStreamErrorException" ->
          `ModelStreamErrorException (ModelStreamErrorException.of_xml xml)
      | "ModelTimeoutException" ->
          `ModelTimeoutException (ModelTimeoutException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ModelErrorException e ->
          `Assoc
            [("error", (`String "ModelErrorException"));
            ("details", (ModelErrorException.to_json e))]
      | `ModelNotReadyException e ->
          `Assoc
            [("error", (`String "ModelNotReadyException"));
            ("details", (ModelNotReadyException.to_json e))]
      | `ModelStreamErrorException e ->
          `Assoc
            [("error", (`String "ModelStreamErrorException"));
            ("details", (ModelStreamErrorException.to_json e))]
      | `ModelTimeoutException e ->
          `Assoc
            [("error", (`String "ModelTimeoutException"));
            ("details", (ModelTimeoutException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ?body:(Some pipe) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("body",
           (Option.map x.body
              ~f:InvokeModelWithBidirectionalStreamOutput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let body =
        (Option.map ~f:InvokeModelWithBidirectionalStreamOutput.of_xml)
          (Xml.child xml_arg0 "body") in
      make ?body ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let body =
        field_map json__ "body"
          InvokeModelWithBidirectionalStreamOutput.of_json in
      make ?body ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Invoke the specified Amazon Bedrock model to run inference using the bidirectional stream. The response is returned in a stream that remains open for 8 minutes. A single session can contain multiple prompts and responses from the model. The prompts to the model are provided as audio files and the model's responses are spoken back to the user and transcribed. It is possible for users to interrupt the model's response with a new prompt, which will halt the response speech. The model will retain contextual awareness of the conversation while pivoting to respond to the new prompt."]
module InvokeModelWithBidirectionalStreamRequest =
  struct
    type nonrec t =
      {
      modelId: InvokeModelIdentifier.t
        [@ocaml.doc
          "The model ID or ARN of the model ID to use. Currently, only amazon.nova-sonic-v1:0 is supported."];
      body: InvokeModelWithBidirectionalStreamInput.t
        [@ocaml.doc
          "The prompt and inference parameters in the format specified in the BidirectionalInputPayloadPart in the header. You must provide the body in JSON format. To see the format and content of the request and response bodies for different models, refer to Inference parameters. For more information, see Run inference in the Bedrock User Guide."]}
    let context_ = "InvokeModelWithBidirectionalStreamRequest"
    let make ~modelId = fun ~body -> fun () -> { modelId; body }
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ~modelId:(InvokeModelIdentifier.of_string
                        ((List.Assoc.find_exn ~equal:String.Caseless.equal)
                           xs "modelId")) ~body:pipe ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("modelId", (Some (InvokeModelIdentifier.to_value x.modelId)));
        ("body",
          (Some (InvokeModelWithBidirectionalStreamInput.to_value x.body)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let body =
        InvokeModelWithBidirectionalStreamInput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "body") in
      let modelId =
        InvokeModelIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelId") in
      make ~body ~modelId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let body =
        field_map_exn json__ "body"
          InvokeModelWithBidirectionalStreamInput.of_json in
      let modelId =
        field_map_exn json__ "modelId" InvokeModelIdentifier.of_json in
      make ~body ~modelId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Invoke the specified Amazon Bedrock model to run inference using the bidirectional stream. The response is returned in a stream that remains open for 8 minutes. A single session can contain multiple prompts and responses from the model. The prompts to the model are provided as audio files and the model's responses are spoken back to the user and transcribed. It is possible for users to interrupt the model's response with a new prompt, which will halt the response speech. The model will retain contextual awareness of the conversation while pivoting to respond to the new prompt."]
module InvokeModelResponse =
  struct
    type nonrec t =
      {
      body: Body.t option
        [@ocaml.doc
          "Inference response from the model in the format specified in the contentType header. To see the format and content of the request and response bodies for different models, refer to Inference parameters."];
      contentType: MimeType.t option
        [@ocaml.doc "The MIME type of the inference result."];
      performanceConfigLatency: PerformanceConfigLatency.t option
        [@ocaml.doc "Model performance settings for the request."];
      serviceTier: ServiceTierType.t option
        [@ocaml.doc
          "Specifies the processing tier type used for serving the request."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ModelErrorException of ModelErrorException.t 
      | `ModelNotReadyException of ModelNotReadyException.t 
      | `ModelTimeoutException of ModelTimeoutException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?body =
      fun ?contentType ->
        fun ?performanceConfigLatency ->
          fun ?serviceTier ->
            fun () ->
              { body; contentType; performanceConfigLatency; serviceTier }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ModelErrorException" ->
          `ModelErrorException (ModelErrorException.of_json json)
      | "ModelNotReadyException" ->
          `ModelNotReadyException (ModelNotReadyException.of_json json)
      | "ModelTimeoutException" ->
          `ModelTimeoutException (ModelTimeoutException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ModelErrorException" ->
          `ModelErrorException (ModelErrorException.of_xml xml)
      | "ModelNotReadyException" ->
          `ModelNotReadyException (ModelNotReadyException.of_xml xml)
      | "ModelTimeoutException" ->
          `ModelTimeoutException (ModelTimeoutException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ModelErrorException e ->
          `Assoc
            [("error", (`String "ModelErrorException"));
            ("details", (ModelErrorException.to_json e))]
      | `ModelNotReadyException e ->
          `Assoc
            [("error", (`String "ModelNotReadyException"));
            ("details", (ModelNotReadyException.to_json e))]
      | `ModelTimeoutException e ->
          `Assoc
            [("error", (`String "ModelTimeoutException"));
            ("details", (ModelTimeoutException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make ?body:(Some pipe)
            ?contentType:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "Content-Type") ~f:MimeType.of_string)
            ?performanceConfigLatency:(Option.map
                                         ((List.Assoc.find
                                             ~equal:String.Caseless.equal) xs
                                            "X-Amzn-Bedrock-PerformanceConfig-Latency")
                                         ~f:PerformanceConfigLatency.of_string)
            ?serviceTier:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "X-Amzn-Bedrock-Service-Tier")
                            ~f:ServiceTierType.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("body", (Option.map x.body ~f:Body.to_value));
        ("Content-Type", (Option.map x.contentType ~f:MimeType.to_value));
        ("X-Amzn-Bedrock-PerformanceConfig-Latency",
          (Option.map x.performanceConfigLatency
             ~f:PerformanceConfigLatency.to_value));
        ("X-Amzn-Bedrock-Service-Tier",
          (Option.map x.serviceTier ~f:ServiceTierType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceTier =
        (Option.map ~f:ServiceTierType.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-Service-Tier") in
      let performanceConfigLatency =
        (Option.map ~f:PerformanceConfigLatency.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-PerformanceConfig-Latency") in
      let contentType =
        (Option.map ~f:MimeType.of_xml) (Xml.child xml_arg0 "Content-Type") in
      let body = (Option.map ~f:Body.of_xml) (Xml.child xml_arg0 "body") in
      make ?serviceTier ?performanceConfigLatency ?contentType ?body ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serviceTier =
        field_map json__ "serviceTier" ServiceTierType.of_json in
      let performanceConfigLatency =
        field_map json__ "performanceConfigLatency"
          PerformanceConfigLatency.of_json in
      let contentType = field_map json__ "contentType" MimeType.of_json in
      let body = field_map json__ "body" Body.of_json in
      make ?serviceTier ?performanceConfigLatency ?contentType ?body ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Invokes the specified Amazon Bedrock model to run inference using the prompt and inference parameters provided in the request body. You use model inference to generate text, images, and embeddings. For example code, see Invoke model code examples in the Amazon Bedrock User Guide. This operation requires permission for the bedrock:InvokeModel action. To deny all inference access to resources that you specify in the modelId field, you need to deny access to the bedrock:InvokeModel and bedrock:InvokeModelWithResponseStream actions. Doing this also denies access to the resource through the Converse API actions (Converse and ConverseStream). For more information see Deny access for inference on specific models. For troubleshooting some of the common errors you might encounter when using the InvokeModel API, see Troubleshooting Amazon Bedrock API Error Codes in the Amazon Bedrock User Guide"]
module InvokeModelRequest =
  struct
    type nonrec t =
      {
      body: Body.t option
        [@ocaml.doc
          "The prompt and inference parameters in the format specified in the contentType in the header. You must provide the body in JSON format. To see the format and content of the request and response bodies for different models, refer to Inference parameters. For more information, see Run inference in the Bedrock User Guide."];
      contentType: MimeType.t option
        [@ocaml.doc
          "The MIME type of the input data in the request. You must specify application/json."];
      accept: MimeType.t option
        [@ocaml.doc
          "The desired MIME type of the inference body in the response. The default value is application/json."];
      modelId: InvokeModelIdentifier.t
        [@ocaml.doc
          "The unique identifier of the model to invoke to run inference. The modelId to provide depends on the type of model or throughput that you use: If you use a base model, specify the model ID or its ARN. For a list of model IDs for base models, see Amazon Bedrock base model IDs (on-demand throughput) in the Amazon Bedrock User Guide. If you use an inference profile, specify the inference profile ID or its ARN. For a list of inference profile IDs, see Supported Regions and models for cross-region inference in the Amazon Bedrock User Guide. If you use a provisioned model, specify the ARN of the Provisioned Throughput. For more information, see Run inference using a Provisioned Throughput in the Amazon Bedrock User Guide. If you use a custom model, specify the ARN of the custom model deployment (for on-demand inference) or the ARN of your provisioned model (for Provisioned Throughput). For more information, see Use a custom model in Amazon Bedrock in the Amazon Bedrock User Guide. If you use an imported model, specify the ARN of the imported model. You can get the model ARN from a successful call to CreateModelImportJob or from the Imported models page in the Amazon Bedrock console."];
      trace: Trace.t option
        [@ocaml.doc
          "Specifies whether to enable or disable the Bedrock trace. If enabled, you can see the full Bedrock trace."];
      guardrailIdentifier: GuardrailIdentifier.t option
        [@ocaml.doc
          "The unique identifier of the guardrail that you want to use. If you don't provide a value, no guardrail is applied to the invocation. An error will be thrown in the following situations. You don't provide a guardrail identifier but you specify the amazon-bedrock-guardrailConfig field in the request body. You enable the guardrail but the contentType isn't application/json. You provide a guardrail identifier, but guardrailVersion isn't specified."];
      guardrailVersion: GuardrailVersion.t option
        [@ocaml.doc
          "The version number for the guardrail. The value can also be DRAFT."];
      performanceConfigLatency: PerformanceConfigLatency.t option
        [@ocaml.doc "Model performance settings for the request."];
      serviceTier: ServiceTierType.t option
        [@ocaml.doc
          "Specifies the processing tier type used for serving the request."]}
    let context_ = "InvokeModelRequest"
    let make ?body =
      fun ?contentType ->
        fun ?accept ->
          fun ?trace ->
            fun ?guardrailIdentifier ->
              fun ?guardrailVersion ->
                fun ?performanceConfigLatency ->
                  fun ?serviceTier ->
                    fun ~modelId ->
                      fun () ->
                        {
                          body;
                          contentType;
                          accept;
                          trace;
                          guardrailIdentifier;
                          guardrailVersion;
                          performanceConfigLatency;
                          serviceTier;
                          modelId
                        }
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make ?body:(Some pipe)
            ?contentType:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "Content-Type") ~f:MimeType.of_string)
            ?accept:(Option.map
                       ((List.Assoc.find ~equal:String.Caseless.equal) xs
                          "Accept") ~f:MimeType.of_string)
            ~modelId:(InvokeModelIdentifier.of_string
                        ((List.Assoc.find_exn ~equal:String.Caseless.equal)
                           xs "modelId"))
            ?trace:(Option.map
                      ((List.Assoc.find ~equal:String.Caseless.equal) xs
                         "X-Amzn-Bedrock-Trace") ~f:Trace.of_string)
            ?guardrailIdentifier:(Option.map
                                    ((List.Assoc.find
                                        ~equal:String.Caseless.equal) xs
                                       "X-Amzn-Bedrock-GuardrailIdentifier")
                                    ~f:GuardrailIdentifier.of_string)
            ?guardrailVersion:(Option.map
                                 ((List.Assoc.find
                                     ~equal:String.Caseless.equal) xs
                                    "X-Amzn-Bedrock-GuardrailVersion")
                                 ~f:GuardrailVersion.of_string)
            ?performanceConfigLatency:(Option.map
                                         ((List.Assoc.find
                                             ~equal:String.Caseless.equal) xs
                                            "X-Amzn-Bedrock-PerformanceConfig-Latency")
                                         ~f:PerformanceConfigLatency.of_string)
            ?serviceTier:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "X-Amzn-Bedrock-Service-Tier")
                            ~f:ServiceTierType.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("body", (Option.map x.body ~f:Body.to_value));
        ("Content-Type", (Option.map x.contentType ~f:MimeType.to_value));
        ("Accept", (Option.map x.accept ~f:MimeType.to_value));
        ("modelId", (Some (InvokeModelIdentifier.to_value x.modelId)));
        ("X-Amzn-Bedrock-Trace", (Option.map x.trace ~f:Trace.to_value));
        ("X-Amzn-Bedrock-GuardrailIdentifier",
          (Option.map x.guardrailIdentifier ~f:GuardrailIdentifier.to_value));
        ("X-Amzn-Bedrock-GuardrailVersion",
          (Option.map x.guardrailVersion ~f:GuardrailVersion.to_value));
        ("X-Amzn-Bedrock-PerformanceConfig-Latency",
          (Option.map x.performanceConfigLatency
             ~f:PerformanceConfigLatency.to_value));
        ("X-Amzn-Bedrock-Service-Tier",
          (Option.map x.serviceTier ~f:ServiceTierType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceTier =
        (Option.map ~f:ServiceTierType.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-Service-Tier") in
      let performanceConfigLatency =
        (Option.map ~f:PerformanceConfigLatency.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-PerformanceConfig-Latency") in
      let guardrailVersion =
        (Option.map ~f:GuardrailVersion.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-GuardrailVersion") in
      let guardrailIdentifier =
        (Option.map ~f:GuardrailIdentifier.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-GuardrailIdentifier") in
      let trace =
        (Option.map ~f:Trace.of_xml)
          (Xml.child xml_arg0 "X-Amzn-Bedrock-Trace") in
      let modelId =
        InvokeModelIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelId") in
      let accept =
        (Option.map ~f:MimeType.of_xml) (Xml.child xml_arg0 "Accept") in
      let contentType =
        (Option.map ~f:MimeType.of_xml) (Xml.child xml_arg0 "Content-Type") in
      let body = (Option.map ~f:Body.of_xml) (Xml.child xml_arg0 "body") in
      make ?serviceTier ?performanceConfigLatency ?guardrailVersion
        ?guardrailIdentifier ?trace ~modelId ?accept ?contentType ?body ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serviceTier =
        field_map json__ "serviceTier" ServiceTierType.of_json in
      let performanceConfigLatency =
        field_map json__ "performanceConfigLatency"
          PerformanceConfigLatency.of_json in
      let guardrailVersion =
        field_map json__ "guardrailVersion" GuardrailVersion.of_json in
      let guardrailIdentifier =
        field_map json__ "guardrailIdentifier" GuardrailIdentifier.of_json in
      let trace = field_map json__ "trace" Trace.of_json in
      let modelId =
        field_map_exn json__ "modelId" InvokeModelIdentifier.of_json in
      let accept = field_map json__ "accept" MimeType.of_json in
      let contentType = field_map json__ "contentType" MimeType.of_json in
      let body = field_map json__ "body" Body.of_json in
      make ?serviceTier ?performanceConfigLatency ?guardrailVersion
        ?guardrailIdentifier ?trace ~modelId ?accept ?contentType ?body ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Invokes the specified Amazon Bedrock model to run inference using the prompt and inference parameters provided in the request body. You use model inference to generate text, images, and embeddings. For example code, see Invoke model code examples in the Amazon Bedrock User Guide. This operation requires permission for the bedrock:InvokeModel action. To deny all inference access to resources that you specify in the modelId field, you need to deny access to the bedrock:InvokeModel and bedrock:InvokeModelWithResponseStream actions. Doing this also denies access to the resource through the Converse API actions (Converse and ConverseStream). For more information see Deny access for inference on specific models. For troubleshooting some of the common errors you might encounter when using the InvokeModel API, see Troubleshooting Amazon Bedrock API Error Codes in the Amazon Bedrock User Guide"]
module GetAsyncInvokeResponse =
  struct
    type nonrec t =
      {
      invocationArn: InvocationArn.t option
        [@ocaml.doc "The invocation's ARN."];
      modelArn: AsyncInvokeArn.t option
        [@ocaml.doc "The invocation's model ARN."];
      clientRequestToken: AsyncInvokeIdempotencyToken.t option
        [@ocaml.doc "The invocation's idempotency token."];
      status: AsyncInvokeStatus.t option
        [@ocaml.doc "The invocation's status."];
      failureMessage: AsyncInvokeMessage.t option
        [@ocaml.doc "An error message."];
      submitTime: Timestamp.t option
        [@ocaml.doc "When the invocation request was submitted."];
      lastModifiedTime: Timestamp.t option
        [@ocaml.doc "The invocation's last modified time."];
      endTime: Timestamp.t option [@ocaml.doc "When the invocation ended."];
      outputDataConfig: AsyncInvokeOutputDataConfig.t option
        [@ocaml.doc "Output data settings."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?invocationArn =
      fun ?modelArn ->
        fun ?clientRequestToken ->
          fun ?status ->
            fun ?failureMessage ->
              fun ?submitTime ->
                fun ?lastModifiedTime ->
                  fun ?endTime ->
                    fun ?outputDataConfig ->
                      fun () ->
                        {
                          invocationArn;
                          modelArn;
                          clientRequestToken;
                          status;
                          failureMessage;
                          submitTime;
                          lastModifiedTime;
                          endTime;
                          outputDataConfig
                        }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("invocationArn",
           (Option.map x.invocationArn ~f:InvocationArn.to_value));
        ("modelArn", (Option.map x.modelArn ~f:AsyncInvokeArn.to_value));
        ("clientRequestToken",
          (Option.map x.clientRequestToken
             ~f:AsyncInvokeIdempotencyToken.to_value));
        ("status", (Option.map x.status ~f:AsyncInvokeStatus.to_value));
        ("failureMessage",
          (Option.map x.failureMessage ~f:AsyncInvokeMessage.to_value));
        ("submitTime", (Option.map x.submitTime ~f:Timestamp.to_value));
        ("lastModifiedTime",
          (Option.map x.lastModifiedTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("outputDataConfig",
          (Option.map x.outputDataConfig
             ~f:AsyncInvokeOutputDataConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputDataConfig =
        (Option.map ~f:AsyncInvokeOutputDataConfig.of_xml)
          (Xml.child xml_arg0 "outputDataConfig") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let lastModifiedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastModifiedTime") in
      let submitTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "submitTime") in
      let failureMessage =
        (Option.map ~f:AsyncInvokeMessage.of_xml)
          (Xml.child xml_arg0 "failureMessage") in
      let status =
        (Option.map ~f:AsyncInvokeStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let clientRequestToken =
        (Option.map ~f:AsyncInvokeIdempotencyToken.of_xml)
          (Xml.child xml_arg0 "clientRequestToken") in
      let modelArn =
        (Option.map ~f:AsyncInvokeArn.of_xml) (Xml.child xml_arg0 "modelArn") in
      let invocationArn =
        (Option.map ~f:InvocationArn.of_xml)
          (Xml.child xml_arg0 "invocationArn") in
      make ?outputDataConfig ?endTime ?lastModifiedTime ?submitTime
        ?failureMessage ?status ?clientRequestToken ?modelArn ?invocationArn
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputDataConfig =
        field_map json__ "outputDataConfig"
          AsyncInvokeOutputDataConfig.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let lastModifiedTime =
        field_map json__ "lastModifiedTime" Timestamp.of_json in
      let submitTime = field_map json__ "submitTime" Timestamp.of_json in
      let failureMessage =
        field_map json__ "failureMessage" AsyncInvokeMessage.of_json in
      let status = field_map json__ "status" AsyncInvokeStatus.of_json in
      let clientRequestToken =
        field_map json__ "clientRequestToken"
          AsyncInvokeIdempotencyToken.of_json in
      let modelArn = field_map json__ "modelArn" AsyncInvokeArn.of_json in
      let invocationArn =
        field_map json__ "invocationArn" InvocationArn.of_json in
      make ?outputDataConfig ?endTime ?lastModifiedTime ?submitTime
        ?failureMessage ?status ?clientRequestToken ?modelArn ?invocationArn
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieve information about an asynchronous invocation."]
module GetAsyncInvokeRequest =
  struct
    type nonrec t =
      {
      invocationArn: InvocationArn.t [@ocaml.doc "The invocation's ARN."]}
    let context_ = "GetAsyncInvokeRequest"
    let make ~invocationArn = fun () -> { invocationArn }
    let to_value x =
      structure_to_value
        [("invocationArn", (Some (InvocationArn.to_value x.invocationArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let invocationArn =
        InvocationArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "invocationArn") in
      make ~invocationArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let invocationArn =
        field_map_exn json__ "invocationArn" InvocationArn.of_json in
      make ~invocationArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieve information about an asynchronous invocation."]
module CountTokensResponse =
  struct
    type nonrec t =
      {
      inputTokens: Integer.t option
        [@ocaml.doc
          "The number of tokens in the provided input according to the specified model's tokenization rules. This count represents the number of input tokens that would be processed if the same input were sent to the model in an inference request. Use this value to estimate costs and ensure your inputs stay within model token limits."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?inputTokens = fun () -> { inputTokens }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("inputTokens", (Option.map x.inputTokens ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let inputTokens =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "inputTokens") in
      make ?inputTokens ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let inputTokens = field_map json__ "inputTokens" Integer.of_json in
      make ?inputTokens ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the token count for a given inference request. This operation helps you estimate token usage before sending requests to foundation models by returning the token count that would be used if the same input were sent to the model in an inference request. Token counting is model-specific because different models use different tokenization strategies. The token count returned by this operation will match the token count that would be charged if the same input were sent to the model in an InvokeModel or Converse request. You can use this operation to: Estimate costs before sending inference requests. Optimize prompts to fit within token limits. Plan for token usage in your applications. This operation accepts the same input formats as InvokeModel and Converse, allowing you to count tokens for both raw text inputs and structured conversation formats. The following operations are related to CountTokens: InvokeModel - Sends inference requests to foundation models Converse - Sends conversation-based inference requests to foundation models"]
module CountTokensRequest =
  struct
    type nonrec t =
      {
      modelId: FoundationModelVersionIdentifier.t
        [@ocaml.doc
          "The unique identifier or ARN of the foundation model to use for token counting. Each model processes tokens differently, so the token count is specific to the model you specify."];
      input: CountTokensInput.t
        [@ocaml.doc
          "The input for which to count tokens. The structure of this parameter depends on whether you're counting tokens for an InvokeModel or Converse request: For InvokeModel requests, provide the request body in the invokeModel field For Converse requests, provide the messages and system content in the converse field The input format must be compatible with the model specified in the modelId parameter."]}
    let context_ = "CountTokensRequest"
    let make ~modelId = fun ~input -> fun () -> { modelId; input }
    let to_value x =
      structure_to_value
        [("modelId",
           (Some (FoundationModelVersionIdentifier.to_value x.modelId)));
        ("input", (Some (CountTokensInput.to_value x.input)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let input =
        CountTokensInput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "input") in
      let modelId =
        FoundationModelVersionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelId") in
      make ~input ~modelId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let input = field_map_exn json__ "input" CountTokensInput.of_json in
      let modelId =
        field_map_exn json__ "modelId"
          FoundationModelVersionIdentifier.of_json in
      make ~input ~modelId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the token count for a given inference request. This operation helps you estimate token usage before sending requests to foundation models by returning the token count that would be used if the same input were sent to the model in an inference request. Token counting is model-specific because different models use different tokenization strategies. The token count returned by this operation will match the token count that would be charged if the same input were sent to the model in an InvokeModel or Converse request. You can use this operation to: Estimate costs before sending inference requests. Optimize prompts to fit within token limits. Plan for token usage in your applications. This operation accepts the same input formats as InvokeModel and Converse, allowing you to count tokens for both raw text inputs and structured conversation formats. The following operations are related to CountTokens: InvokeModel - Sends inference requests to foundation models Converse - Sends conversation-based inference requests to foundation models"]
module ConverseStreamResponse =
  struct
    type nonrec t =
      {
      stream: ConverseStreamOutput.t option
        [@ocaml.doc "The output stream that the model generated."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ModelErrorException of ModelErrorException.t 
      | `ModelNotReadyException of ModelNotReadyException.t 
      | `ModelTimeoutException of ModelTimeoutException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?stream = fun () -> { stream }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ModelErrorException" ->
          `ModelErrorException (ModelErrorException.of_json json)
      | "ModelNotReadyException" ->
          `ModelNotReadyException (ModelNotReadyException.of_json json)
      | "ModelTimeoutException" ->
          `ModelTimeoutException (ModelTimeoutException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ModelErrorException" ->
          `ModelErrorException (ModelErrorException.of_xml xml)
      | "ModelNotReadyException" ->
          `ModelNotReadyException (ModelNotReadyException.of_xml xml)
      | "ModelTimeoutException" ->
          `ModelTimeoutException (ModelTimeoutException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ModelErrorException e ->
          `Assoc
            [("error", (`String "ModelErrorException"));
            ("details", (ModelErrorException.to_json e))]
      | `ModelNotReadyException e ->
          `Assoc
            [("error", (`String "ModelNotReadyException"));
            ("details", (ModelNotReadyException.to_json e))]
      | `ModelTimeoutException e ->
          `Assoc
            [("error", (`String "ModelTimeoutException"));
            ("details", (ModelTimeoutException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ?stream:(Some pipe) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("stream", (Option.map x.stream ~f:ConverseStreamOutput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stream =
        (Option.map ~f:ConverseStreamOutput.of_xml)
          (Xml.child xml_arg0 "stream") in
      make ?stream ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let stream = field_map json__ "stream" ConverseStreamOutput.of_json in
      make ?stream ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends messages to the specified Amazon Bedrock model and returns the response in a stream. ConverseStream provides a consistent API that works with all Amazon Bedrock models that support messages. This allows you to write code once and use it with different models. Should a model have unique inference parameters, you can also pass those unique parameters to the model. To find out if a model supports streaming, call GetFoundationModel and check the responseStreamingSupported field in the response. The CLI doesn't support streaming operations in Amazon Bedrock, including ConverseStream. Amazon Bedrock doesn't store any text, images, or documents that you provide as content. The data is only used to generate the response. You can submit a prompt by including it in the messages field, specifying the modelId of a foundation model or inference profile to run inference on it, and including any other fields that are relevant to your use case. You can also submit a prompt from Prompt management by specifying the ARN of the prompt version and including a map of variables to values in the promptVariables field. You can append more messages to the prompt by using the messages field. If you use a prompt from Prompt management, you can't include the following fields in the request: additionalModelRequestFields, inferenceConfig, system, or toolConfig. Instead, these fields must be defined through Prompt management. For more information, see Use a prompt from Prompt management. For information about the Converse API, see Use the Converse API in the Amazon Bedrock User Guide. To use a guardrail, see Use a guardrail with the Converse API in the Amazon Bedrock User Guide. To use a tool with a model, see Tool use (Function calling) in the Amazon Bedrock User Guide For example code, see Conversation streaming example in the Amazon Bedrock User Guide. This operation requires permission for the bedrock:InvokeModelWithResponseStream action. To deny all inference access to resources that you specify in the modelId field, you need to deny access to the bedrock:InvokeModel and bedrock:InvokeModelWithResponseStream actions. Doing this also denies access to the resource through the base inference actions (InvokeModel and InvokeModelWithResponseStream). For more information see Deny access for inference on specific models. For troubleshooting some of the common errors you might encounter when using the ConverseStream API, see Troubleshooting Amazon Bedrock API Error Codes in the Amazon Bedrock User Guide"]
module ConverseStreamRequest =
  struct
    type nonrec t =
      {
      modelId: ConversationalModelId.t
        [@ocaml.doc
          "Specifies the model or throughput with which to run inference, or the prompt resource to use in inference. The value depends on the resource that you use: If you use a base model, specify the model ID or its ARN. For a list of model IDs for base models, see Amazon Bedrock base model IDs (on-demand throughput) in the Amazon Bedrock User Guide. If you use an inference profile, specify the inference profile ID or its ARN. For a list of inference profile IDs, see Supported Regions and models for cross-region inference in the Amazon Bedrock User Guide. If you use a provisioned model, specify the ARN of the Provisioned Throughput. For more information, see Run inference using a Provisioned Throughput in the Amazon Bedrock User Guide. If you use a custom model, first purchase Provisioned Throughput for it. Then specify the ARN of the resulting provisioned model. For more information, see Use a custom model in Amazon Bedrock in the Amazon Bedrock User Guide. To include a prompt that was defined in Prompt management, specify the ARN of the prompt version to use. The Converse API doesn't support imported models."];
      messages: Messages.t option
        [@ocaml.doc "The messages that you want to send to the model."];
      system: SystemContentBlocks.t option
        [@ocaml.doc
          "A prompt that provides instructions or context to the model about the task it should perform, or the persona it should adopt during the conversation."];
      inferenceConfig: InferenceConfiguration.t option
        [@ocaml.doc
          "Inference parameters to pass to the model. Converse and ConverseStream support a base set of inference parameters. If you need to pass additional parameters that the model supports, use the additionalModelRequestFields request field."];
      toolConfig: ToolConfiguration.t option
        [@ocaml.doc
          "Configuration information for the tools that the model can use when generating a response. For information about models that support streaming tool use, see Supported models and model features."];
      guardrailConfig: GuardrailStreamConfiguration.t option
        [@ocaml.doc
          "Configuration information for a guardrail that you want to use in the request. If you include guardContent blocks in the content field in the messages field, the guardrail operates only on those messages. If you include no guardContent blocks, the guardrail operates on all messages in the request body and in any included prompt resource."];
      additionalModelRequestFields: Document.t option
        [@ocaml.doc
          "Additional inference parameters that the model supports, beyond the base set of inference parameters that Converse and ConverseStream support in the inferenceConfig field. For more information, see Model parameters."];
      promptVariables: PromptVariableMap.t option
        [@ocaml.doc
          "Contains a map of variables in a prompt from Prompt management to objects containing the values to fill in for them when running model invocation. This field is ignored if you don't specify a prompt resource in the modelId field."];
      additionalModelResponseFieldPaths:
        ConverseStreamRequestAdditionalModelResponseFieldPathsList.t option
        [@ocaml.doc
          "Additional model parameters field paths to return in the response. Converse and ConverseStream return the requested fields as a JSON Pointer object in the additionalModelResponseFields field. The following is example JSON for additionalModelResponseFieldPaths. \\[ \"/stop_sequence\" \\] For information about the JSON Pointer syntax, see the Internet Engineering Task Force (IETF) documentation. Converse and ConverseStream reject an empty JSON Pointer or incorrectly structured JSON Pointer with a 400 error code. if the JSON Pointer is valid, but the requested field is not in the model response, it is ignored by Converse."];
      requestMetadata: RequestMetadata.t option
        [@ocaml.doc
          "Key-value pairs that you can use to filter invocation logs."];
      performanceConfig: PerformanceConfiguration.t option
        [@ocaml.doc "Model performance settings for the request."];
      serviceTier: ServiceTier.t option
        [@ocaml.doc
          "Specifies the processing tier configuration used for serving the request."];
      outputConfig: OutputConfig.t option
        [@ocaml.doc "Output configuration for a model response."]}
    let context_ = "ConverseStreamRequest"
    let make ?messages =
      fun ?system ->
        fun ?inferenceConfig ->
          fun ?toolConfig ->
            fun ?guardrailConfig ->
              fun ?additionalModelRequestFields ->
                fun ?promptVariables ->
                  fun ?additionalModelResponseFieldPaths ->
                    fun ?requestMetadata ->
                      fun ?performanceConfig ->
                        fun ?serviceTier ->
                          fun ?outputConfig ->
                            fun ~modelId ->
                              fun () ->
                                {
                                  messages;
                                  system;
                                  inferenceConfig;
                                  toolConfig;
                                  guardrailConfig;
                                  additionalModelRequestFields;
                                  promptVariables;
                                  additionalModelResponseFieldPaths;
                                  requestMetadata;
                                  performanceConfig;
                                  serviceTier;
                                  outputConfig;
                                  modelId
                                }
    let to_value x =
      structure_to_value
        [("modelId", (Some (ConversationalModelId.to_value x.modelId)));
        ("messages", (Option.map x.messages ~f:Messages.to_value));
        ("system", (Option.map x.system ~f:SystemContentBlocks.to_value));
        ("inferenceConfig",
          (Option.map x.inferenceConfig ~f:InferenceConfiguration.to_value));
        ("toolConfig",
          (Option.map x.toolConfig ~f:ToolConfiguration.to_value));
        ("guardrailConfig",
          (Option.map x.guardrailConfig
             ~f:GuardrailStreamConfiguration.to_value));
        ("additionalModelRequestFields",
          (Option.map x.additionalModelRequestFields ~f:Document.to_value));
        ("promptVariables",
          (Option.map x.promptVariables ~f:PromptVariableMap.to_value));
        ("additionalModelResponseFieldPaths",
          (Option.map x.additionalModelResponseFieldPaths
             ~f:ConverseStreamRequestAdditionalModelResponseFieldPathsList.to_value));
        ("requestMetadata",
          (Option.map x.requestMetadata ~f:RequestMetadata.to_value));
        ("performanceConfig",
          (Option.map x.performanceConfig
             ~f:PerformanceConfiguration.to_value));
        ("serviceTier", (Option.map x.serviceTier ~f:ServiceTier.to_value));
        ("outputConfig",
          (Option.map x.outputConfig ~f:OutputConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputConfig =
        (Option.map ~f:OutputConfig.of_xml)
          (Xml.child xml_arg0 "outputConfig") in
      let serviceTier =
        (Option.map ~f:ServiceTier.of_xml) (Xml.child xml_arg0 "serviceTier") in
      let performanceConfig =
        (Option.map ~f:PerformanceConfiguration.of_xml)
          (Xml.child xml_arg0 "performanceConfig") in
      let requestMetadata =
        (Option.map ~f:RequestMetadata.of_xml)
          (Xml.child xml_arg0 "requestMetadata") in
      let additionalModelResponseFieldPaths =
        (Option.map
           ~f:ConverseStreamRequestAdditionalModelResponseFieldPathsList.of_xml)
          (Xml.child xml_arg0 "additionalModelResponseFieldPaths") in
      let promptVariables =
        (Option.map ~f:PromptVariableMap.of_xml)
          (Xml.child xml_arg0 "promptVariables") in
      let additionalModelRequestFields =
        (Option.map ~f:Document.of_xml)
          (Xml.child xml_arg0 "additionalModelRequestFields") in
      let guardrailConfig =
        (Option.map ~f:GuardrailStreamConfiguration.of_xml)
          (Xml.child xml_arg0 "guardrailConfig") in
      let toolConfig =
        (Option.map ~f:ToolConfiguration.of_xml)
          (Xml.child xml_arg0 "toolConfig") in
      let inferenceConfig =
        (Option.map ~f:InferenceConfiguration.of_xml)
          (Xml.child xml_arg0 "inferenceConfig") in
      let system =
        (Option.map ~f:SystemContentBlocks.of_xml)
          (Xml.child xml_arg0 "system") in
      let messages =
        (Option.map ~f:Messages.of_xml) (Xml.child xml_arg0 "messages") in
      let modelId =
        ConversationalModelId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelId") in
      make ?outputConfig ?serviceTier ?performanceConfig ?requestMetadata
        ?additionalModelResponseFieldPaths ?promptVariables
        ?additionalModelRequestFields ?guardrailConfig ?toolConfig
        ?inferenceConfig ?system ?messages ~modelId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputConfig = field_map json__ "outputConfig" OutputConfig.of_json in
      let serviceTier = field_map json__ "serviceTier" ServiceTier.of_json in
      let performanceConfig =
        field_map json__ "performanceConfig" PerformanceConfiguration.of_json in
      let requestMetadata =
        field_map json__ "requestMetadata" RequestMetadata.of_json in
      let additionalModelResponseFieldPaths =
        field_map json__ "additionalModelResponseFieldPaths"
          ConverseStreamRequestAdditionalModelResponseFieldPathsList.of_json in
      let promptVariables =
        field_map json__ "promptVariables" PromptVariableMap.of_json in
      let additionalModelRequestFields =
        field_map json__ "additionalModelRequestFields" Document.of_json in
      let guardrailConfig =
        field_map json__ "guardrailConfig"
          GuardrailStreamConfiguration.of_json in
      let toolConfig =
        field_map json__ "toolConfig" ToolConfiguration.of_json in
      let inferenceConfig =
        field_map json__ "inferenceConfig" InferenceConfiguration.of_json in
      let system = field_map json__ "system" SystemContentBlocks.of_json in
      let messages = field_map json__ "messages" Messages.of_json in
      let modelId =
        field_map_exn json__ "modelId" ConversationalModelId.of_json in
      make ?outputConfig ?serviceTier ?performanceConfig ?requestMetadata
        ?additionalModelResponseFieldPaths ?promptVariables
        ?additionalModelRequestFields ?guardrailConfig ?toolConfig
        ?inferenceConfig ?system ?messages ~modelId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends messages to the specified Amazon Bedrock model and returns the response in a stream. ConverseStream provides a consistent API that works with all Amazon Bedrock models that support messages. This allows you to write code once and use it with different models. Should a model have unique inference parameters, you can also pass those unique parameters to the model. To find out if a model supports streaming, call GetFoundationModel and check the responseStreamingSupported field in the response. The CLI doesn't support streaming operations in Amazon Bedrock, including ConverseStream. Amazon Bedrock doesn't store any text, images, or documents that you provide as content. The data is only used to generate the response. You can submit a prompt by including it in the messages field, specifying the modelId of a foundation model or inference profile to run inference on it, and including any other fields that are relevant to your use case. You can also submit a prompt from Prompt management by specifying the ARN of the prompt version and including a map of variables to values in the promptVariables field. You can append more messages to the prompt by using the messages field. If you use a prompt from Prompt management, you can't include the following fields in the request: additionalModelRequestFields, inferenceConfig, system, or toolConfig. Instead, these fields must be defined through Prompt management. For more information, see Use a prompt from Prompt management. For information about the Converse API, see Use the Converse API in the Amazon Bedrock User Guide. To use a guardrail, see Use a guardrail with the Converse API in the Amazon Bedrock User Guide. To use a tool with a model, see Tool use (Function calling) in the Amazon Bedrock User Guide For example code, see Conversation streaming example in the Amazon Bedrock User Guide. This operation requires permission for the bedrock:InvokeModelWithResponseStream action. To deny all inference access to resources that you specify in the modelId field, you need to deny access to the bedrock:InvokeModel and bedrock:InvokeModelWithResponseStream actions. Doing this also denies access to the resource through the base inference actions (InvokeModel and InvokeModelWithResponseStream). For more information see Deny access for inference on specific models. For troubleshooting some of the common errors you might encounter when using the ConverseStream API, see Troubleshooting Amazon Bedrock API Error Codes in the Amazon Bedrock User Guide"]
module ConverseResponse =
  struct
    type nonrec t =
      {
      output: ConverseOutput.t option
        [@ocaml.doc "The result from the call to Converse."];
      stopReason: StopReason.t option
        [@ocaml.doc "The reason why the model stopped generating output."];
      usage: TokenUsage.t option
        [@ocaml.doc
          "The total number of tokens used in the call to Converse. The total includes the tokens input to the model and the tokens generated by the model."];
      metrics: ConverseMetrics.t option
        [@ocaml.doc "Metrics for the call to Converse."];
      additionalModelResponseFields: Document.t option
        [@ocaml.doc
          "Additional fields in the response that are unique to the model."];
      trace: ConverseTrace.t option
        [@ocaml.doc
          "A trace object that contains information about the Guardrail behavior."];
      performanceConfig: PerformanceConfiguration.t option
        [@ocaml.doc "Model performance settings for the request."];
      serviceTier: ServiceTier.t option
        [@ocaml.doc
          "Specifies the processing tier configuration used for serving the request."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ModelErrorException of ModelErrorException.t 
      | `ModelNotReadyException of ModelNotReadyException.t 
      | `ModelTimeoutException of ModelTimeoutException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?output =
      fun ?stopReason ->
        fun ?usage ->
          fun ?metrics ->
            fun ?additionalModelResponseFields ->
              fun ?trace ->
                fun ?performanceConfig ->
                  fun ?serviceTier ->
                    fun () ->
                      {
                        output;
                        stopReason;
                        usage;
                        metrics;
                        additionalModelResponseFields;
                        trace;
                        performanceConfig;
                        serviceTier
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ModelErrorException" ->
          `ModelErrorException (ModelErrorException.of_json json)
      | "ModelNotReadyException" ->
          `ModelNotReadyException (ModelNotReadyException.of_json json)
      | "ModelTimeoutException" ->
          `ModelTimeoutException (ModelTimeoutException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ModelErrorException" ->
          `ModelErrorException (ModelErrorException.of_xml xml)
      | "ModelNotReadyException" ->
          `ModelNotReadyException (ModelNotReadyException.of_xml xml)
      | "ModelTimeoutException" ->
          `ModelTimeoutException (ModelTimeoutException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ModelErrorException e ->
          `Assoc
            [("error", (`String "ModelErrorException"));
            ("details", (ModelErrorException.to_json e))]
      | `ModelNotReadyException e ->
          `Assoc
            [("error", (`String "ModelNotReadyException"));
            ("details", (ModelNotReadyException.to_json e))]
      | `ModelTimeoutException e ->
          `Assoc
            [("error", (`String "ModelTimeoutException"));
            ("details", (ModelTimeoutException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("output", (Option.map x.output ~f:ConverseOutput.to_value));
        ("stopReason", (Option.map x.stopReason ~f:StopReason.to_value));
        ("usage", (Option.map x.usage ~f:TokenUsage.to_value));
        ("metrics", (Option.map x.metrics ~f:ConverseMetrics.to_value));
        ("additionalModelResponseFields",
          (Option.map x.additionalModelResponseFields ~f:Document.to_value));
        ("trace", (Option.map x.trace ~f:ConverseTrace.to_value));
        ("performanceConfig",
          (Option.map x.performanceConfig
             ~f:PerformanceConfiguration.to_value));
        ("serviceTier", (Option.map x.serviceTier ~f:ServiceTier.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceTier =
        (Option.map ~f:ServiceTier.of_xml) (Xml.child xml_arg0 "serviceTier") in
      let performanceConfig =
        (Option.map ~f:PerformanceConfiguration.of_xml)
          (Xml.child xml_arg0 "performanceConfig") in
      let trace =
        (Option.map ~f:ConverseTrace.of_xml) (Xml.child xml_arg0 "trace") in
      let additionalModelResponseFields =
        (Option.map ~f:Document.of_xml)
          (Xml.child xml_arg0 "additionalModelResponseFields") in
      let metrics =
        (Option.map ~f:ConverseMetrics.of_xml) (Xml.child xml_arg0 "metrics") in
      let usage =
        (Option.map ~f:TokenUsage.of_xml) (Xml.child xml_arg0 "usage") in
      let stopReason =
        (Option.map ~f:StopReason.of_xml) (Xml.child xml_arg0 "stopReason") in
      let output =
        (Option.map ~f:ConverseOutput.of_xml) (Xml.child xml_arg0 "output") in
      make ?serviceTier ?performanceConfig ?trace
        ?additionalModelResponseFields ?metrics ?usage ?stopReason ?output ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let serviceTier = field_map json__ "serviceTier" ServiceTier.of_json in
      let performanceConfig =
        field_map json__ "performanceConfig" PerformanceConfiguration.of_json in
      let trace = field_map json__ "trace" ConverseTrace.of_json in
      let additionalModelResponseFields =
        field_map json__ "additionalModelResponseFields" Document.of_json in
      let metrics = field_map json__ "metrics" ConverseMetrics.of_json in
      let usage = field_map json__ "usage" TokenUsage.of_json in
      let stopReason = field_map json__ "stopReason" StopReason.of_json in
      let output = field_map json__ "output" ConverseOutput.of_json in
      make ?serviceTier ?performanceConfig ?trace
        ?additionalModelResponseFields ?metrics ?usage ?stopReason ?output ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends messages to the specified Amazon Bedrock model. Converse provides a consistent interface that works with all models that support messages. This allows you to write code once and use it with different models. If a model has unique inference parameters, you can also pass those unique parameters to the model. Amazon Bedrock doesn't store any text, images, or documents that you provide as content. The data is only used to generate the response. You can submit a prompt by including it in the messages field, specifying the modelId of a foundation model or inference profile to run inference on it, and including any other fields that are relevant to your use case. You can also submit a prompt from Prompt management by specifying the ARN of the prompt version and including a map of variables to values in the promptVariables field. You can append more messages to the prompt by using the messages field. If you use a prompt from Prompt management, you can't include the following fields in the request: additionalModelRequestFields, inferenceConfig, system, or toolConfig. Instead, these fields must be defined through Prompt management. For more information, see Use a prompt from Prompt management. For information about the Converse API, see Use the Converse API in the Amazon Bedrock User Guide. To use a guardrail, see Use a guardrail with the Converse API in the Amazon Bedrock User Guide. To use a tool with a model, see Tool use (Function calling) in the Amazon Bedrock User Guide For example code, see Converse API examples in the Amazon Bedrock User Guide. This operation requires permission for the bedrock:InvokeModel action. To deny all inference access to resources that you specify in the modelId field, you need to deny access to the bedrock:InvokeModel and bedrock:InvokeModelWithResponseStream actions. Doing this also denies access to the resource through the base inference actions (InvokeModel and InvokeModelWithResponseStream). For more information see Deny access for inference on specific models. For troubleshooting some of the common errors you might encounter when using the Converse API, see Troubleshooting Amazon Bedrock API Error Codes in the Amazon Bedrock User Guide"]
module ConverseRequest =
  struct
    type nonrec t =
      {
      modelId: ConversationalModelId.t
        [@ocaml.doc
          "Specifies the model or throughput with which to run inference, or the prompt resource to use in inference. The value depends on the resource that you use: If you use a base model, specify the model ID or its ARN. For a list of model IDs for base models, see Amazon Bedrock base model IDs (on-demand throughput) in the Amazon Bedrock User Guide. If you use an inference profile, specify the inference profile ID or its ARN. For a list of inference profile IDs, see Supported Regions and models for cross-region inference in the Amazon Bedrock User Guide. If you use a provisioned model, specify the ARN of the Provisioned Throughput. For more information, see Run inference using a Provisioned Throughput in the Amazon Bedrock User Guide. If you use a custom model, first purchase Provisioned Throughput for it. Then specify the ARN of the resulting provisioned model. For more information, see Use a custom model in Amazon Bedrock in the Amazon Bedrock User Guide. To include a prompt that was defined in Prompt management, specify the ARN of the prompt version to use. The Converse API doesn't support imported models."];
      messages: Messages.t option
        [@ocaml.doc "The messages that you want to send to the model."];
      system: SystemContentBlocks.t option
        [@ocaml.doc
          "A prompt that provides instructions or context to the model about the task it should perform, or the persona it should adopt during the conversation."];
      inferenceConfig: InferenceConfiguration.t option
        [@ocaml.doc
          "Inference parameters to pass to the model. Converse and ConverseStream support a base set of inference parameters. If you need to pass additional parameters that the model supports, use the additionalModelRequestFields request field."];
      toolConfig: ToolConfiguration.t option
        [@ocaml.doc
          "Configuration information for the tools that the model can use when generating a response. For information about models that support tool use, see Supported models and model features."];
      guardrailConfig: GuardrailConfiguration.t option
        [@ocaml.doc
          "Configuration information for a guardrail that you want to use in the request. If you include guardContent blocks in the content field in the messages field, the guardrail operates only on those messages. If you include no guardContent blocks, the guardrail operates on all messages in the request body and in any included prompt resource."];
      additionalModelRequestFields: Document.t option
        [@ocaml.doc
          "Additional inference parameters that the model supports, beyond the base set of inference parameters that Converse and ConverseStream support in the inferenceConfig field. For more information, see Model parameters."];
      promptVariables: PromptVariableMap.t option
        [@ocaml.doc
          "Contains a map of variables in a prompt from Prompt management to objects containing the values to fill in for them when running model invocation. This field is ignored if you don't specify a prompt resource in the modelId field."];
      additionalModelResponseFieldPaths:
        ConverseRequestAdditionalModelResponseFieldPathsList.t option
        [@ocaml.doc
          "Additional model parameters field paths to return in the response. Converse and ConverseStream return the requested fields as a JSON Pointer object in the additionalModelResponseFields field. The following is example JSON for additionalModelResponseFieldPaths. \\[ \"/stop_sequence\" \\] For information about the JSON Pointer syntax, see the Internet Engineering Task Force (IETF) documentation. Converse and ConverseStream reject an empty JSON Pointer or incorrectly structured JSON Pointer with a 400 error code. if the JSON Pointer is valid, but the requested field is not in the model response, it is ignored by Converse."];
      requestMetadata: RequestMetadata.t option
        [@ocaml.doc
          "Key-value pairs that you can use to filter invocation logs."];
      performanceConfig: PerformanceConfiguration.t option
        [@ocaml.doc "Model performance settings for the request."];
      serviceTier: ServiceTier.t option
        [@ocaml.doc
          "Specifies the processing tier configuration used for serving the request."];
      outputConfig: OutputConfig.t option
        [@ocaml.doc "Output configuration for a model response."]}
    let context_ = "ConverseRequest"
    let make ?messages =
      fun ?system ->
        fun ?inferenceConfig ->
          fun ?toolConfig ->
            fun ?guardrailConfig ->
              fun ?additionalModelRequestFields ->
                fun ?promptVariables ->
                  fun ?additionalModelResponseFieldPaths ->
                    fun ?requestMetadata ->
                      fun ?performanceConfig ->
                        fun ?serviceTier ->
                          fun ?outputConfig ->
                            fun ~modelId ->
                              fun () ->
                                {
                                  messages;
                                  system;
                                  inferenceConfig;
                                  toolConfig;
                                  guardrailConfig;
                                  additionalModelRequestFields;
                                  promptVariables;
                                  additionalModelResponseFieldPaths;
                                  requestMetadata;
                                  performanceConfig;
                                  serviceTier;
                                  outputConfig;
                                  modelId
                                }
    let to_value x =
      structure_to_value
        [("modelId", (Some (ConversationalModelId.to_value x.modelId)));
        ("messages", (Option.map x.messages ~f:Messages.to_value));
        ("system", (Option.map x.system ~f:SystemContentBlocks.to_value));
        ("inferenceConfig",
          (Option.map x.inferenceConfig ~f:InferenceConfiguration.to_value));
        ("toolConfig",
          (Option.map x.toolConfig ~f:ToolConfiguration.to_value));
        ("guardrailConfig",
          (Option.map x.guardrailConfig ~f:GuardrailConfiguration.to_value));
        ("additionalModelRequestFields",
          (Option.map x.additionalModelRequestFields ~f:Document.to_value));
        ("promptVariables",
          (Option.map x.promptVariables ~f:PromptVariableMap.to_value));
        ("additionalModelResponseFieldPaths",
          (Option.map x.additionalModelResponseFieldPaths
             ~f:ConverseRequestAdditionalModelResponseFieldPathsList.to_value));
        ("requestMetadata",
          (Option.map x.requestMetadata ~f:RequestMetadata.to_value));
        ("performanceConfig",
          (Option.map x.performanceConfig
             ~f:PerformanceConfiguration.to_value));
        ("serviceTier", (Option.map x.serviceTier ~f:ServiceTier.to_value));
        ("outputConfig",
          (Option.map x.outputConfig ~f:OutputConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputConfig =
        (Option.map ~f:OutputConfig.of_xml)
          (Xml.child xml_arg0 "outputConfig") in
      let serviceTier =
        (Option.map ~f:ServiceTier.of_xml) (Xml.child xml_arg0 "serviceTier") in
      let performanceConfig =
        (Option.map ~f:PerformanceConfiguration.of_xml)
          (Xml.child xml_arg0 "performanceConfig") in
      let requestMetadata =
        (Option.map ~f:RequestMetadata.of_xml)
          (Xml.child xml_arg0 "requestMetadata") in
      let additionalModelResponseFieldPaths =
        (Option.map
           ~f:ConverseRequestAdditionalModelResponseFieldPathsList.of_xml)
          (Xml.child xml_arg0 "additionalModelResponseFieldPaths") in
      let promptVariables =
        (Option.map ~f:PromptVariableMap.of_xml)
          (Xml.child xml_arg0 "promptVariables") in
      let additionalModelRequestFields =
        (Option.map ~f:Document.of_xml)
          (Xml.child xml_arg0 "additionalModelRequestFields") in
      let guardrailConfig =
        (Option.map ~f:GuardrailConfiguration.of_xml)
          (Xml.child xml_arg0 "guardrailConfig") in
      let toolConfig =
        (Option.map ~f:ToolConfiguration.of_xml)
          (Xml.child xml_arg0 "toolConfig") in
      let inferenceConfig =
        (Option.map ~f:InferenceConfiguration.of_xml)
          (Xml.child xml_arg0 "inferenceConfig") in
      let system =
        (Option.map ~f:SystemContentBlocks.of_xml)
          (Xml.child xml_arg0 "system") in
      let messages =
        (Option.map ~f:Messages.of_xml) (Xml.child xml_arg0 "messages") in
      let modelId =
        ConversationalModelId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelId") in
      make ?outputConfig ?serviceTier ?performanceConfig ?requestMetadata
        ?additionalModelResponseFieldPaths ?promptVariables
        ?additionalModelRequestFields ?guardrailConfig ?toolConfig
        ?inferenceConfig ?system ?messages ~modelId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputConfig = field_map json__ "outputConfig" OutputConfig.of_json in
      let serviceTier = field_map json__ "serviceTier" ServiceTier.of_json in
      let performanceConfig =
        field_map json__ "performanceConfig" PerformanceConfiguration.of_json in
      let requestMetadata =
        field_map json__ "requestMetadata" RequestMetadata.of_json in
      let additionalModelResponseFieldPaths =
        field_map json__ "additionalModelResponseFieldPaths"
          ConverseRequestAdditionalModelResponseFieldPathsList.of_json in
      let promptVariables =
        field_map json__ "promptVariables" PromptVariableMap.of_json in
      let additionalModelRequestFields =
        field_map json__ "additionalModelRequestFields" Document.of_json in
      let guardrailConfig =
        field_map json__ "guardrailConfig" GuardrailConfiguration.of_json in
      let toolConfig =
        field_map json__ "toolConfig" ToolConfiguration.of_json in
      let inferenceConfig =
        field_map json__ "inferenceConfig" InferenceConfiguration.of_json in
      let system = field_map json__ "system" SystemContentBlocks.of_json in
      let messages = field_map json__ "messages" Messages.of_json in
      let modelId =
        field_map_exn json__ "modelId" ConversationalModelId.of_json in
      make ?outputConfig ?serviceTier ?performanceConfig ?requestMetadata
        ?additionalModelResponseFieldPaths ?promptVariables
        ?additionalModelRequestFields ?guardrailConfig ?toolConfig
        ?inferenceConfig ?system ?messages ~modelId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends messages to the specified Amazon Bedrock model. Converse provides a consistent interface that works with all models that support messages. This allows you to write code once and use it with different models. If a model has unique inference parameters, you can also pass those unique parameters to the model. Amazon Bedrock doesn't store any text, images, or documents that you provide as content. The data is only used to generate the response. You can submit a prompt by including it in the messages field, specifying the modelId of a foundation model or inference profile to run inference on it, and including any other fields that are relevant to your use case. You can also submit a prompt from Prompt management by specifying the ARN of the prompt version and including a map of variables to values in the promptVariables field. You can append more messages to the prompt by using the messages field. If you use a prompt from Prompt management, you can't include the following fields in the request: additionalModelRequestFields, inferenceConfig, system, or toolConfig. Instead, these fields must be defined through Prompt management. For more information, see Use a prompt from Prompt management. For information about the Converse API, see Use the Converse API in the Amazon Bedrock User Guide. To use a guardrail, see Use a guardrail with the Converse API in the Amazon Bedrock User Guide. To use a tool with a model, see Tool use (Function calling) in the Amazon Bedrock User Guide For example code, see Converse API examples in the Amazon Bedrock User Guide. This operation requires permission for the bedrock:InvokeModel action. To deny all inference access to resources that you specify in the modelId field, you need to deny access to the bedrock:InvokeModel and bedrock:InvokeModelWithResponseStream actions. Doing this also denies access to the resource through the base inference actions (InvokeModel and InvokeModelWithResponseStream). For more information see Deny access for inference on specific models. For troubleshooting some of the common errors you might encounter when using the Converse API, see Troubleshooting Amazon Bedrock API Error Codes in the Amazon Bedrock User Guide"]
module ApplyGuardrailResponse =
  struct
    type nonrec t =
      {
      usage: GuardrailUsage.t option
        [@ocaml.doc "The usage details in the response from the guardrail."];
      action: GuardrailAction.t option
        [@ocaml.doc "The action taken in the response from the guardrail."];
      actionReason: String_.t option
        [@ocaml.doc
          "The reason for the action taken when harmful content is detected."];
      outputs: GuardrailOutputContentList.t option
        [@ocaml.doc "The output details in the response from the guardrail."];
      assessments: GuardrailAssessmentList.t option
        [@ocaml.doc
          "The assessment details in the response from the guardrail."];
      guardrailCoverage: GuardrailCoverage.t option
        [@ocaml.doc
          "The guardrail coverage details in the apply guardrail response."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?usage =
      fun ?action ->
        fun ?actionReason ->
          fun ?outputs ->
            fun ?assessments ->
              fun ?guardrailCoverage ->
                fun () ->
                  {
                    usage;
                    action;
                    actionReason;
                    outputs;
                    assessments;
                    guardrailCoverage
                  }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("usage", (Option.map x.usage ~f:GuardrailUsage.to_value));
        ("action", (Option.map x.action ~f:GuardrailAction.to_value));
        ("actionReason", (Option.map x.actionReason ~f:String_.to_value));
        ("outputs",
          (Option.map x.outputs ~f:GuardrailOutputContentList.to_value));
        ("assessments",
          (Option.map x.assessments ~f:GuardrailAssessmentList.to_value));
        ("guardrailCoverage",
          (Option.map x.guardrailCoverage ~f:GuardrailCoverage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let guardrailCoverage =
        (Option.map ~f:GuardrailCoverage.of_xml)
          (Xml.child xml_arg0 "guardrailCoverage") in
      let assessments =
        (Option.map ~f:GuardrailAssessmentList.of_xml)
          (Xml.child xml_arg0 "assessments") in
      let outputs =
        (Option.map ~f:GuardrailOutputContentList.of_xml)
          (Xml.child xml_arg0 "outputs") in
      let actionReason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "actionReason") in
      let action =
        (Option.map ~f:GuardrailAction.of_xml) (Xml.child xml_arg0 "action") in
      let usage =
        (Option.map ~f:GuardrailUsage.of_xml) (Xml.child xml_arg0 "usage") in
      make ?guardrailCoverage ?assessments ?outputs ?actionReason ?action
        ?usage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let guardrailCoverage =
        field_map json__ "guardrailCoverage" GuardrailCoverage.of_json in
      let assessments =
        field_map json__ "assessments" GuardrailAssessmentList.of_json in
      let outputs =
        field_map json__ "outputs" GuardrailOutputContentList.of_json in
      let actionReason = field_map json__ "actionReason" String_.of_json in
      let action = field_map json__ "action" GuardrailAction.of_json in
      let usage = field_map json__ "usage" GuardrailUsage.of_json in
      make ?guardrailCoverage ?assessments ?outputs ?actionReason ?action
        ?usage ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The action to apply a guardrail. For troubleshooting some of the common errors you might encounter when using the ApplyGuardrail API, see Troubleshooting Amazon Bedrock API Error Codes in the Amazon Bedrock User Guide"]
module ApplyGuardrailRequest =
  struct
    type nonrec t =
      {
      guardrailIdentifier: GuardrailIdentifier.t
        [@ocaml.doc
          "The guardrail identifier used in the request to apply the guardrail."];
      guardrailVersion: GuardrailVersion.t
        [@ocaml.doc
          "The guardrail version used in the request to apply the guardrail."];
      source: GuardrailContentSource.t
        [@ocaml.doc
          "The source of data used in the request to apply the guardrail."];
      content: GuardrailContentBlockList.t
        [@ocaml.doc
          "The content details used in the request to apply the guardrail."];
      outputScope: GuardrailOutputScope.t option
        [@ocaml.doc
          "Specifies the scope of the output that you get in the response. Set to FULL to return the entire output, including any detected and non-detected entries in the response for enhanced debugging. Note that the full output scope doesn't apply to word filters or regex in sensitive information filters. It does apply to all other filtering policies, including sensitive information with filters that can detect personally identifiable information (PII)."]}
    let context_ = "ApplyGuardrailRequest"
    let make ?outputScope =
      fun ~guardrailIdentifier ->
        fun ~guardrailVersion ->
          fun ~source ->
            fun ~content ->
              fun () ->
                {
                  outputScope;
                  guardrailIdentifier;
                  guardrailVersion;
                  source;
                  content
                }
    let to_value x =
      structure_to_value
        [("guardrailIdentifier",
           (Some (GuardrailIdentifier.to_value x.guardrailIdentifier)));
        ("guardrailVersion",
          (Some (GuardrailVersion.to_value x.guardrailVersion)));
        ("source", (Some (GuardrailContentSource.to_value x.source)));
        ("content", (Some (GuardrailContentBlockList.to_value x.content)));
        ("outputScope",
          (Option.map x.outputScope ~f:GuardrailOutputScope.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputScope =
        (Option.map ~f:GuardrailOutputScope.of_xml)
          (Xml.child xml_arg0 "outputScope") in
      let content =
        GuardrailContentBlockList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "content") in
      let source =
        GuardrailContentSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "source") in
      let guardrailVersion =
        GuardrailVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "guardrailVersion") in
      let guardrailIdentifier =
        GuardrailIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "guardrailIdentifier") in
      make ?outputScope ~content ~source ~guardrailVersion
        ~guardrailIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputScope =
        field_map json__ "outputScope" GuardrailOutputScope.of_json in
      let content =
        field_map_exn json__ "content" GuardrailContentBlockList.of_json in
      let source =
        field_map_exn json__ "source" GuardrailContentSource.of_json in
      let guardrailVersion =
        field_map_exn json__ "guardrailVersion" GuardrailVersion.of_json in
      let guardrailIdentifier =
        field_map_exn json__ "guardrailIdentifier"
          GuardrailIdentifier.of_json in
      make ?outputScope ~content ~source ~guardrailVersion
        ~guardrailIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The action to apply a guardrail. For troubleshooting some of the common errors you might encounter when using the ApplyGuardrail API, see Troubleshooting Amazon Bedrock API Error Codes in the Amazon Bedrock User Guide"]
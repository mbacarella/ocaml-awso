(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let apply_guardrail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and outputScope =
         flag "output-scope" (optional json_arg)
           ~doc:"JSON GuardrailOutputScope"
       and guardrailIdentifier =
         flag "guardrail-identifier" (required string)
           ~doc:"STRING GuardrailIdentifier"
       and guardrailVersion =
         flag "guardrail-version" (required string)
           ~doc:"STRING GuardrailVersion"
       and source =
         flag "source" (required json_arg) ~doc:"JSON GuardrailContentSource"
       and content =
         flag "content" (required json_arg)
           ~doc:"JSON GuardrailContentBlockList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.apply_guardrail
           (Values.ApplyGuardrailRequest.make
              ?outputScope:(Option.map ~f:Values.GuardrailOutputScope.of_json
                              outputScope) ~guardrailIdentifier
              ~guardrailVersion
              ~source:(Values.GuardrailContentSource.of_json source)
              ~content:(Values.GuardrailContentBlockList.of_json content) ())
           (Some Values.ApplyGuardrailResponse.to_json)
           (Some Values.ApplyGuardrailResponse.error_to_json)])
let converse =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and messages =
         flag "messages" (optional json_arg) ~doc:"JSON Messages"
       and system =
         flag "system" (optional json_arg) ~doc:"JSON SystemContentBlocks"
       and inferenceConfig =
         flag "inference-config" (optional json_arg)
           ~doc:"JSON InferenceConfiguration"
       and toolConfig =
         flag "tool-config" (optional json_arg) ~doc:"JSON ToolConfiguration"
       and guardrailConfig =
         flag "guardrail-config" (optional json_arg)
           ~doc:"JSON GuardrailConfiguration"
       and additionalModelRequestFields =
         flag "additional-model-request-fields" (optional json_arg)
           ~doc:"JSON Document"
       and promptVariables =
         flag "prompt-variables" (optional json_arg)
           ~doc:"JSON PromptVariableMap"
       and additionalModelResponseFieldPaths =
         flag "additional-model-response-field-paths" (optional json_arg)
           ~doc:"JSON ConverseRequestAdditionalModelResponseFieldPathsList"
       and requestMetadata =
         flag "request-metadata" (optional json_arg)
           ~doc:"JSON RequestMetadata"
       and performanceConfig =
         flag "performance-config" (optional json_arg)
           ~doc:"JSON PerformanceConfiguration"
       and serviceTier =
         flag "service-tier" (optional json_arg) ~doc:"JSON ServiceTier"
       and outputConfig =
         flag "output-config" (optional json_arg) ~doc:"JSON OutputConfig"
       and modelId =
         flag "model-id" (required string)
           ~doc:"STRING ConversationalModelId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.converse
           (Values.ConverseRequest.make
              ?messages:(Option.map ~f:Values.Messages.of_json messages)
              ?system:(Option.map ~f:Values.SystemContentBlocks.of_json
                         system)
              ?inferenceConfig:(Option.map
                                  ~f:Values.InferenceConfiguration.of_json
                                  inferenceConfig)
              ?toolConfig:(Option.map ~f:Values.ToolConfiguration.of_json
                             toolConfig)
              ?guardrailConfig:(Option.map
                                  ~f:Values.GuardrailConfiguration.of_json
                                  guardrailConfig)
              ?additionalModelRequestFields:(Option.map
                                               ~f:Values.Document.of_json
                                               additionalModelRequestFields)
              ?promptVariables:(Option.map
                                  ~f:Values.PromptVariableMap.of_json
                                  promptVariables)
              ?additionalModelResponseFieldPaths:(Option.map
                                                    ~f:Values.ConverseRequestAdditionalModelResponseFieldPathsList.of_json
                                                    additionalModelResponseFieldPaths)
              ?requestMetadata:(Option.map ~f:Values.RequestMetadata.of_json
                                  requestMetadata)
              ?performanceConfig:(Option.map
                                    ~f:Values.PerformanceConfiguration.of_json
                                    performanceConfig)
              ?serviceTier:(Option.map ~f:Values.ServiceTier.of_json
                              serviceTier)
              ?outputConfig:(Option.map ~f:Values.OutputConfig.of_json
                               outputConfig) ~modelId ())
           (Some Values.ConverseResponse.to_json)
           (Some Values.ConverseResponse.error_to_json)])
let converse_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and messages =
         flag "messages" (optional json_arg) ~doc:"JSON Messages"
       and system =
         flag "system" (optional json_arg) ~doc:"JSON SystemContentBlocks"
       and inferenceConfig =
         flag "inference-config" (optional json_arg)
           ~doc:"JSON InferenceConfiguration"
       and toolConfig =
         flag "tool-config" (optional json_arg) ~doc:"JSON ToolConfiguration"
       and guardrailConfig =
         flag "guardrail-config" (optional json_arg)
           ~doc:"JSON GuardrailStreamConfiguration"
       and additionalModelRequestFields =
         flag "additional-model-request-fields" (optional json_arg)
           ~doc:"JSON Document"
       and promptVariables =
         flag "prompt-variables" (optional json_arg)
           ~doc:"JSON PromptVariableMap"
       and additionalModelResponseFieldPaths =
         flag "additional-model-response-field-paths" (optional json_arg)
           ~doc:"JSON ConverseStreamRequestAdditionalModelResponseFieldPathsList"
       and requestMetadata =
         flag "request-metadata" (optional json_arg)
           ~doc:"JSON RequestMetadata"
       and performanceConfig =
         flag "performance-config" (optional json_arg)
           ~doc:"JSON PerformanceConfiguration"
       and serviceTier =
         flag "service-tier" (optional json_arg) ~doc:"JSON ServiceTier"
       and outputConfig =
         flag "output-config" (optional json_arg) ~doc:"JSON OutputConfig"
       and modelId =
         flag "model-id" (required string)
           ~doc:"STRING ConversationalModelId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.converse_stream
           (Values.ConverseStreamRequest.make
              ?messages:(Option.map ~f:Values.Messages.of_json messages)
              ?system:(Option.map ~f:Values.SystemContentBlocks.of_json
                         system)
              ?inferenceConfig:(Option.map
                                  ~f:Values.InferenceConfiguration.of_json
                                  inferenceConfig)
              ?toolConfig:(Option.map ~f:Values.ToolConfiguration.of_json
                             toolConfig)
              ?guardrailConfig:(Option.map
                                  ~f:Values.GuardrailStreamConfiguration.of_json
                                  guardrailConfig)
              ?additionalModelRequestFields:(Option.map
                                               ~f:Values.Document.of_json
                                               additionalModelRequestFields)
              ?promptVariables:(Option.map
                                  ~f:Values.PromptVariableMap.of_json
                                  promptVariables)
              ?additionalModelResponseFieldPaths:(Option.map
                                                    ~f:Values.ConverseStreamRequestAdditionalModelResponseFieldPathsList.of_json
                                                    additionalModelResponseFieldPaths)
              ?requestMetadata:(Option.map ~f:Values.RequestMetadata.of_json
                                  requestMetadata)
              ?performanceConfig:(Option.map
                                    ~f:Values.PerformanceConfiguration.of_json
                                    performanceConfig)
              ?serviceTier:(Option.map ~f:Values.ServiceTier.of_json
                              serviceTier)
              ?outputConfig:(Option.map ~f:Values.OutputConfig.of_json
                               outputConfig) ~modelId ())
           (Some Values.ConverseStreamResponse.to_json)
           (Some Values.ConverseStreamResponse.error_to_json)])
let count_tokens =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelId =
         flag "model-id" (required string)
           ~doc:"STRING FoundationModelVersionIdentifier"
       and input =
         flag "input" (required json_arg) ~doc:"JSON CountTokensInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.count_tokens
           (Values.CountTokensRequest.make ~modelId
              ~input:(Values.CountTokensInput.of_json input) ())
           (Some Values.CountTokensResponse.to_json)
           (Some Values.CountTokensResponse.error_to_json)])
let get_async_invoke =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and invocationArn =
         flag "invocation-arn" (required string) ~doc:"STRING InvocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_async_invoke
           (Values.GetAsyncInvokeRequest.make ~invocationArn ())
           (Some Values.GetAsyncInvokeResponse.to_json)
           (Some Values.GetAsyncInvokeResponse.error_to_json)])
let invoke_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and body = flag "body" (optional json_arg) ~doc:"JSON Body"
       and contentType =
         flag "content-type" (optional string) ~doc:"STRING MimeType"
       and accept = flag "accept" (optional string) ~doc:"STRING MimeType"
       and trace = flag "trace" (optional json_arg) ~doc:"JSON Trace"
       and guardrailIdentifier =
         flag "guardrail-identifier" (optional string)
           ~doc:"STRING GuardrailIdentifier"
       and guardrailVersion =
         flag "guardrail-version" (optional string)
           ~doc:"STRING GuardrailVersion"
       and performanceConfigLatency =
         flag "performance-config-latency" (optional json_arg)
           ~doc:"JSON PerformanceConfigLatency"
       and serviceTier =
         flag "service-tier" (optional json_arg) ~doc:"JSON ServiceTierType"
       and modelId =
         flag "model-id" (required string)
           ~doc:"STRING InvokeModelIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invoke_model
           (Values.InvokeModelRequest.make
              ?body:(Option.map ~f:Values.Body.of_json body) ?contentType
              ?accept ?trace:(Option.map ~f:Values.Trace.of_json trace)
              ?guardrailIdentifier ?guardrailVersion
              ?performanceConfigLatency:(Option.map
                                           ~f:Values.PerformanceConfigLatency.of_json
                                           performanceConfigLatency)
              ?serviceTier:(Option.map ~f:Values.ServiceTierType.of_json
                              serviceTier) ~modelId ())
           (Some Values.InvokeModelResponse.to_json)
           (Some Values.InvokeModelResponse.error_to_json)])
let invoke_model_with_bidirectional_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelId =
         flag "model-id" (required string)
           ~doc:"STRING InvokeModelIdentifier"
       and body =
         flag "body" (required json_arg)
           ~doc:"JSON InvokeModelWithBidirectionalStreamInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invoke_model_with_bidirectional_stream
           (Values.InvokeModelWithBidirectionalStreamRequest.make ~modelId
              ~body:(Values.InvokeModelWithBidirectionalStreamInput.of_json
                       body) ())
           (Some Values.InvokeModelWithBidirectionalStreamResponse.to_json)
           (Some
              Values.InvokeModelWithBidirectionalStreamResponse.error_to_json)])
let invoke_model_with_response_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and body = flag "body" (optional json_arg) ~doc:"JSON Body"
       and contentType =
         flag "content-type" (optional string) ~doc:"STRING MimeType"
       and accept = flag "accept" (optional string) ~doc:"STRING MimeType"
       and trace = flag "trace" (optional json_arg) ~doc:"JSON Trace"
       and guardrailIdentifier =
         flag "guardrail-identifier" (optional string)
           ~doc:"STRING GuardrailIdentifier"
       and guardrailVersion =
         flag "guardrail-version" (optional string)
           ~doc:"STRING GuardrailVersion"
       and performanceConfigLatency =
         flag "performance-config-latency" (optional json_arg)
           ~doc:"JSON PerformanceConfigLatency"
       and serviceTier =
         flag "service-tier" (optional json_arg) ~doc:"JSON ServiceTierType"
       and modelId =
         flag "model-id" (required string)
           ~doc:"STRING InvokeModelIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invoke_model_with_response_stream
           (Values.InvokeModelWithResponseStreamRequest.make
              ?body:(Option.map ~f:Values.Body.of_json body) ?contentType
              ?accept ?trace:(Option.map ~f:Values.Trace.of_json trace)
              ?guardrailIdentifier ?guardrailVersion
              ?performanceConfigLatency:(Option.map
                                           ~f:Values.PerformanceConfigLatency.of_json
                                           performanceConfigLatency)
              ?serviceTier:(Option.map ~f:Values.ServiceTierType.of_json
                              serviceTier) ~modelId ())
           (Some Values.InvokeModelWithResponseStreamResponse.to_json)
           (Some Values.InvokeModelWithResponseStreamResponse.error_to_json)])
let list_async_invokes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and submitTimeAfter =
         flag "submit-time-after" (optional json_arg) ~doc:"JSON Timestamp"
       and submitTimeBefore =
         flag "submit-time-before" (optional json_arg) ~doc:"JSON Timestamp"
       and statusEquals =
         flag "status-equals" (optional json_arg)
           ~doc:"JSON AsyncInvokeStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortAsyncInvocationBy"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_async_invokes
           (Values.ListAsyncInvokesRequest.make
              ?submitTimeAfter:(Option.map ~f:Values.Timestamp.of_json
                                  submitTimeAfter)
              ?submitTimeBefore:(Option.map ~f:Values.Timestamp.of_json
                                   submitTimeBefore)
              ?statusEquals:(Option.map ~f:Values.AsyncInvokeStatus.of_json
                               statusEquals) ?maxResults ?nextToken
              ?sortBy:(Option.map ~f:Values.SortAsyncInvocationBy.of_json
                         sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ()) (Some Values.ListAsyncInvokesResponse.to_json)
           (Some Values.ListAsyncInvokesResponse.error_to_json)])
let start_async_invoke =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING AsyncInvokeIdempotencyToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and modelId =
         flag "model-id" (required string)
           ~doc:"STRING AsyncInvokeIdentifier"
       and modelInput =
         flag "model-input" (required json_arg) ~doc:"JSON ModelInputPayload"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON AsyncInvokeOutputDataConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_async_invoke
           (Values.StartAsyncInvokeRequest.make ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~modelId
              ~modelInput:(Values.ModelInputPayload.of_json modelInput)
              ~outputDataConfig:(Values.AsyncInvokeOutputDataConfig.of_json
                                   outputDataConfig) ())
           (Some Values.StartAsyncInvokeResponse.to_json)
           (Some Values.StartAsyncInvokeResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("apply-guardrail", apply_guardrail);
    ("converse", converse);
    ("converse-stream", converse_stream);
    ("count-tokens", count_tokens);
    ("get-async-invoke", get_async_invoke);
    ("invoke-model", invoke_model);
    ("invoke-model-with-bidirectional-stream",
      invoke_model_with_bidirectional_stream);
    ("invoke-model-with-response-stream", invoke_model_with_response_stream);
    ("list-async-invokes", list_async_invokes);
    ("start-async-invoke", start_async_invoke)]
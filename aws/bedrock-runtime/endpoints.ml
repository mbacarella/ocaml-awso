(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | ApplyGuardrail: (ApplyGuardrailRequest.t, ApplyGuardrailResponse.t,
  ApplyGuardrailResponse.error) t 
  | Converse: (ConverseRequest.t, ConverseResponse.t, ConverseResponse.error)
  t 
  | ConverseStream: (ConverseStreamRequest.t, ConverseStreamResponse.t,
  ConverseStreamResponse.error) t 
  | CountTokens: (CountTokensRequest.t, CountTokensResponse.t,
  CountTokensResponse.error) t 
  | GetAsyncInvoke: (GetAsyncInvokeRequest.t, GetAsyncInvokeResponse.t,
  GetAsyncInvokeResponse.error) t 
  | InvokeModel: (InvokeModelRequest.t, InvokeModelResponse.t,
  InvokeModelResponse.error) t 
  | InvokeModelWithBidirectionalStream:
  (InvokeModelWithBidirectionalStreamRequest.t,
  InvokeModelWithBidirectionalStreamResponse.t,
  InvokeModelWithBidirectionalStreamResponse.error) t 
  | InvokeModelWithResponseStream: (InvokeModelWithResponseStreamRequest.t,
  InvokeModelWithResponseStreamResponse.t,
  InvokeModelWithResponseStreamResponse.error) t 
  | ListAsyncInvokes: (ListAsyncInvokesRequest.t, ListAsyncInvokesResponse.t,
  ListAsyncInvokesResponse.error) t 
  | StartAsyncInvoke: (StartAsyncInvokeRequest.t, StartAsyncInvokeResponse.t,
  StartAsyncInvokeResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | ApplyGuardrail -> `POST
  | Converse -> `POST
  | ConverseStream -> `POST
  | CountTokens -> `POST
  | GetAsyncInvoke -> `GET
  | InvokeModel -> `POST
  | InvokeModelWithBidirectionalStream -> `POST
  | InvokeModelWithResponseStream -> `POST
  | ListAsyncInvokes -> `GET
  | StartAsyncInvoke -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | ApplyGuardrail ->
          (Format.kasprintf Uri.of_string) "/guardrail/%s/version/%s/apply"
            (GuardrailIdentifier.to_header
               x.ApplyGuardrailRequest.guardrailIdentifier)
            (GuardrailVersion.to_header
               x.ApplyGuardrailRequest.guardrailVersion)
      | Converse ->
          (Format.kasprintf Uri.of_string) "/model/%s/converse"
            (ConversationalModelId.to_header x.ConverseRequest.modelId)
      | ConverseStream ->
          (Format.kasprintf Uri.of_string) "/model/%s/converse-stream"
            (ConversationalModelId.to_header x.ConverseStreamRequest.modelId)
      | CountTokens ->
          (Format.kasprintf Uri.of_string) "/model/%s/count-tokens"
            (FoundationModelVersionIdentifier.to_header
               x.CountTokensRequest.modelId)
      | GetAsyncInvoke ->
          (Format.kasprintf Uri.of_string) "/async-invoke/%s"
            (InvocationArn.to_header x.GetAsyncInvokeRequest.invocationArn)
      | InvokeModel ->
          (Format.kasprintf Uri.of_string) "/model/%s/invoke"
            (InvokeModelIdentifier.to_header x.InvokeModelRequest.modelId)
      | InvokeModelWithBidirectionalStream ->
          (Format.kasprintf Uri.of_string)
            "/model/%s/invoke-with-bidirectional-stream"
            (InvokeModelIdentifier.to_header
               x.InvokeModelWithBidirectionalStreamRequest.modelId)
      | InvokeModelWithResponseStream ->
          (Format.kasprintf Uri.of_string)
            "/model/%s/invoke-with-response-stream"
            (InvokeModelIdentifier.to_header
               x.InvokeModelWithResponseStreamRequest.modelId)
      | ListAsyncInvokes ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/async-invoke")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("submitTimeAfter", (Timestamp.to_header v)))
                  x.submitTimeAfter;
               Option.map
                 ~f:(fun v -> ("submitTimeBefore", (Timestamp.to_header v)))
                 x.submitTimeBefore;
               Option.map
                 ~f:(fun v ->
                       ("statusEquals", (AsyncInvokeStatus.to_header v)))
                 x.statusEquals;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (PaginationToken.to_header v)))
                 x.nextToken;
               Option.map
                 ~f:(fun v -> ("sortBy", (SortAsyncInvocationBy.to_header v)))
                 x.sortBy;
               Option.map
                 ~f:(fun v -> ("sortOrder", (SortOrder.to_header v)))
                 x.sortOrder])
      | StartAsyncInvoke -> (Format.kasprintf Uri.of_string) "/async-invoke")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | ApplyGuardrail ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("source",
                           (GuardrailContentSource.to_value
                              req.ApplyGuardrailRequest.source));
                      Some
                        ("content",
                          (GuardrailContentBlockList.to_value
                             req.ApplyGuardrailRequest.content));
                      Option.map req.ApplyGuardrailRequest.outputScope
                        ~f:(fun x ->
                              ("outputScope",
                                (GuardrailOutputScope.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | Converse ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ConverseRequest.messages
                         ~f:(fun x -> ("messages", (Messages.to_value x)));
                      Option.map req.ConverseRequest.system
                        ~f:(fun x ->
                              ("system", (SystemContentBlocks.to_value x)));
                      Option.map req.ConverseRequest.inferenceConfig
                        ~f:(fun x ->
                              ("inferenceConfig",
                                (InferenceConfiguration.to_value x)));
                      Option.map req.ConverseRequest.toolConfig
                        ~f:(fun x ->
                              ("toolConfig", (ToolConfiguration.to_value x)));
                      Option.map req.ConverseRequest.guardrailConfig
                        ~f:(fun x ->
                              ("guardrailConfig",
                                (GuardrailConfiguration.to_value x)));
                      Option.map
                        req.ConverseRequest.additionalModelRequestFields
                        ~f:(fun x ->
                              ("additionalModelRequestFields",
                                (Document.to_value x)));
                      Option.map req.ConverseRequest.promptVariables
                        ~f:(fun x ->
                              ("promptVariables",
                                (PromptVariableMap.to_value x)));
                      Option.map
                        req.ConverseRequest.additionalModelResponseFieldPaths
                        ~f:(fun x ->
                              ("additionalModelResponseFieldPaths",
                                (ConverseRequestAdditionalModelResponseFieldPathsList.to_value
                                   x)));
                      Option.map req.ConverseRequest.requestMetadata
                        ~f:(fun x ->
                              ("requestMetadata",
                                (RequestMetadata.to_value x)));
                      Option.map req.ConverseRequest.performanceConfig
                        ~f:(fun x ->
                              ("performanceConfig",
                                (PerformanceConfiguration.to_value x)));
                      Option.map req.ConverseRequest.serviceTier
                        ~f:(fun x ->
                              ("serviceTier", (ServiceTier.to_value x)));
                      Option.map req.ConverseRequest.outputConfig
                        ~f:(fun x ->
                              ("outputConfig", (OutputConfig.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ConverseStream ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ConverseStreamRequest.messages
                         ~f:(fun x -> ("messages", (Messages.to_value x)));
                      Option.map req.ConverseStreamRequest.system
                        ~f:(fun x ->
                              ("system", (SystemContentBlocks.to_value x)));
                      Option.map req.ConverseStreamRequest.inferenceConfig
                        ~f:(fun x ->
                              ("inferenceConfig",
                                (InferenceConfiguration.to_value x)));
                      Option.map req.ConverseStreamRequest.toolConfig
                        ~f:(fun x ->
                              ("toolConfig", (ToolConfiguration.to_value x)));
                      Option.map req.ConverseStreamRequest.guardrailConfig
                        ~f:(fun x ->
                              ("guardrailConfig",
                                (GuardrailStreamConfiguration.to_value x)));
                      Option.map
                        req.ConverseStreamRequest.additionalModelRequestFields
                        ~f:(fun x ->
                              ("additionalModelRequestFields",
                                (Document.to_value x)));
                      Option.map req.ConverseStreamRequest.promptVariables
                        ~f:(fun x ->
                              ("promptVariables",
                                (PromptVariableMap.to_value x)));
                      Option.map
                        req.ConverseStreamRequest.additionalModelResponseFieldPaths
                        ~f:(fun x ->
                              ("additionalModelResponseFieldPaths",
                                (ConverseStreamRequestAdditionalModelResponseFieldPathsList.to_value
                                   x)));
                      Option.map req.ConverseStreamRequest.requestMetadata
                        ~f:(fun x ->
                              ("requestMetadata",
                                (RequestMetadata.to_value x)));
                      Option.map req.ConverseStreamRequest.performanceConfig
                        ~f:(fun x ->
                              ("performanceConfig",
                                (PerformanceConfiguration.to_value x)));
                      Option.map req.ConverseStreamRequest.serviceTier
                        ~f:(fun x ->
                              ("serviceTier", (ServiceTier.to_value x)));
                      Option.map req.ConverseStreamRequest.outputConfig
                        ~f:(fun x ->
                              ("outputConfig", (OutputConfig.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CountTokens ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("input",
                           (CountTokensInput.to_value
                              req.CountTokensRequest.input))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetAsyncInvoke ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | InvokeModel ->
      let headers =
        Some
          ((List.filter_opt
              [Option.map req.InvokeModelRequest.contentType
                 ~f:(fun x -> ("Content-Type", (MimeType.to_header x)));
              Option.map req.InvokeModelRequest.accept
                ~f:(fun x -> ("Accept", (MimeType.to_header x)));
              Option.map req.InvokeModelRequest.trace
                ~f:(fun x -> ("X-Amzn-Bedrock-Trace", (Trace.to_header x)));
              Option.map req.InvokeModelRequest.guardrailIdentifier
                ~f:(fun x ->
                      ("X-Amzn-Bedrock-GuardrailIdentifier",
                        (GuardrailIdentifier.to_header x)));
              Option.map req.InvokeModelRequest.guardrailVersion
                ~f:(fun x ->
                      ("X-Amzn-Bedrock-GuardrailVersion",
                        (GuardrailVersion.to_header x)));
              Option.map req.InvokeModelRequest.performanceConfigLatency
                ~f:(fun x ->
                      ("X-Amzn-Bedrock-PerformanceConfig-Latency",
                        (PerformanceConfigLatency.to_header x)));
              Option.map req.InvokeModelRequest.serviceTier
                ~f:(fun x ->
                      ("X-Amzn-Bedrock-Service-Tier",
                        (ServiceTierType.to_header x)))])
             |> Awso.Http.Headers.of_list) in
      let body = Option.map req.body ~f:Body.to_header in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | InvokeModelWithBidirectionalStream ->
      let headers = Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
      let body =
        (fun param ->
           ((param |> InvokeModelWithBidirectionalStreamInput.to_value) |>
              Awso.Botodata.Json.value_to_json)
             |> Yojson.Safe.to_string) req.body in
      Awso.Http.Request.make ?headers ~body (method_of_endpoint endp)
  | InvokeModelWithResponseStream ->
      let headers =
        Some
          ((List.filter_opt
              [Option.map
                 req.InvokeModelWithResponseStreamRequest.contentType
                 ~f:(fun x -> ("Content-Type", (MimeType.to_header x)));
              Option.map req.InvokeModelWithResponseStreamRequest.accept
                ~f:(fun x ->
                      ("X-Amzn-Bedrock-Accept", (MimeType.to_header x)));
              Option.map req.InvokeModelWithResponseStreamRequest.trace
                ~f:(fun x -> ("X-Amzn-Bedrock-Trace", (Trace.to_header x)));
              Option.map
                req.InvokeModelWithResponseStreamRequest.guardrailIdentifier
                ~f:(fun x ->
                      ("X-Amzn-Bedrock-GuardrailIdentifier",
                        (GuardrailIdentifier.to_header x)));
              Option.map
                req.InvokeModelWithResponseStreamRequest.guardrailVersion
                ~f:(fun x ->
                      ("X-Amzn-Bedrock-GuardrailVersion",
                        (GuardrailVersion.to_header x)));
              Option.map
                req.InvokeModelWithResponseStreamRequest.performanceConfigLatency
                ~f:(fun x ->
                      ("X-Amzn-Bedrock-PerformanceConfig-Latency",
                        (PerformanceConfigLatency.to_header x)));
              Option.map req.InvokeModelWithResponseStreamRequest.serviceTier
                ~f:(fun x ->
                      ("X-Amzn-Bedrock-Service-Tier",
                        (ServiceTierType.to_header x)))])
             |> Awso.Http.Headers.of_list) in
      let body = Option.map req.body ~f:Body.to_header in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListAsyncInvokes ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartAsyncInvoke ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.StartAsyncInvokeRequest.clientRequestToken
                         ~f:(fun x ->
                               ("clientRequestToken",
                                 (AsyncInvokeIdempotencyToken.to_value x)));
                      Some
                        ("modelId",
                          (AsyncInvokeIdentifier.to_value
                             req.StartAsyncInvokeRequest.modelId));
                      Some
                        ("modelInput",
                          (ModelInputPayload.to_value
                             req.StartAsyncInvokeRequest.modelInput));
                      Some
                        ("outputDataConfig",
                          (AsyncInvokeOutputDataConfig.to_value
                             req.StartAsyncInvokeRequest.outputDataConfig));
                      Option.map req.StartAsyncInvokeRequest.tags
                        ~f:(fun x -> ("tags", (TagList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | ApplyGuardrail ->
      if is_success
      then Ok (ApplyGuardrailResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ApplyGuardrailResponse.error_of_json))
  | Converse ->
      if is_success
      then Ok (ConverseResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ConverseResponse.error_of_json))
  | ConverseStream ->
      if is_success
      then
        let body =
          ConverseStreamOutput.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (ConverseStreamResponse.of_header_and_body (headers, body))
      else
        Error (parse_aws_error (Some ConverseStreamResponse.error_of_json))
  | CountTokens ->
      if is_success
      then Ok (CountTokensResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CountTokensResponse.error_of_json))
  | GetAsyncInvoke ->
      if is_success
      then Ok (GetAsyncInvokeResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some GetAsyncInvokeResponse.error_of_json))
  | InvokeModel ->
      if is_success
      then
        let body = Body.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (InvokeModelResponse.of_header_and_body (headers, body))
      else Error (parse_aws_error (Some InvokeModelResponse.error_of_json))
  | InvokeModelWithBidirectionalStream ->
      if is_success
      then
        let body =
          InvokeModelWithBidirectionalStreamOutput.of_string
            (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok
          (InvokeModelWithBidirectionalStreamResponse.of_header_and_body
             (headers, body))
      else
        Error
          (parse_aws_error
             (Some InvokeModelWithBidirectionalStreamResponse.error_of_json))
  | InvokeModelWithResponseStream ->
      if is_success
      then
        let body = ResponseStream.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok
          (InvokeModelWithResponseStreamResponse.of_header_and_body
             (headers, body))
      else
        Error
          (parse_aws_error
             (Some InvokeModelWithResponseStreamResponse.error_of_json))
  | ListAsyncInvokes ->
      if is_success
      then Ok (ListAsyncInvokesResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListAsyncInvokesResponse.error_of_json))
  | StartAsyncInvoke ->
      if is_success
      then Ok (StartAsyncInvokeResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some StartAsyncInvokeResponse.error_of_json))
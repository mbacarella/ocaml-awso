(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_input_device_transfer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputDeviceId =
         flag "input-device-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_input_device_transfer
           (Values.AcceptInputDeviceTransferRequest.make ~inputDeviceId ())
           (Some Values.AcceptInputDeviceTransferResponse.to_json)
           (Some Values.AcceptInputDeviceTransferResponse.error_to_json)])
let batch_delete =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelIds =
         flag "channel-ids" (optional json_arg) ~doc:"JSON __listOf__string"
       and inputIds =
         flag "input-ids" (optional json_arg) ~doc:"JSON __listOf__string"
       and inputSecurityGroupIds =
         flag "input-security-group-ids" (optional json_arg)
           ~doc:"JSON __listOf__string"
       and multiplexIds =
         flag "multiplex-ids" (optional json_arg)
           ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete
           (Values.BatchDeleteRequest.make
              ?channelIds:(Option.map ~f:Values.Zz__listOf__string.of_json
                             channelIds)
              ?inputIds:(Option.map ~f:Values.Zz__listOf__string.of_json
                           inputIds)
              ?inputSecurityGroupIds:(Option.map
                                        ~f:Values.Zz__listOf__string.of_json
                                        inputSecurityGroupIds)
              ?multiplexIds:(Option.map ~f:Values.Zz__listOf__string.of_json
                               multiplexIds) ())
           (Some Values.BatchDeleteResponse.to_json)
           (Some Values.BatchDeleteResponse.error_to_json)])
let batch_start =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelIds =
         flag "channel-ids" (optional json_arg) ~doc:"JSON __listOf__string"
       and multiplexIds =
         flag "multiplex-ids" (optional json_arg)
           ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_start
           (Values.BatchStartRequest.make
              ?channelIds:(Option.map ~f:Values.Zz__listOf__string.of_json
                             channelIds)
              ?multiplexIds:(Option.map ~f:Values.Zz__listOf__string.of_json
                               multiplexIds) ())
           (Some Values.BatchStartResponse.to_json)
           (Some Values.BatchStartResponse.error_to_json)])
let batch_stop =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelIds =
         flag "channel-ids" (optional json_arg) ~doc:"JSON __listOf__string"
       and multiplexIds =
         flag "multiplex-ids" (optional json_arg)
           ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_stop
           (Values.BatchStopRequest.make
              ?channelIds:(Option.map ~f:Values.Zz__listOf__string.of_json
                             channelIds)
              ?multiplexIds:(Option.map ~f:Values.Zz__listOf__string.of_json
                               multiplexIds) ())
           (Some Values.BatchStopResponse.to_json)
           (Some Values.BatchStopResponse.error_to_json)])
let batch_update_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and creates =
         flag "creates" (optional json_arg)
           ~doc:"JSON BatchScheduleActionCreateRequest"
       and deletes =
         flag "deletes" (optional json_arg)
           ~doc:"JSON BatchScheduleActionDeleteRequest"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_schedule
           (Values.BatchUpdateScheduleRequest.make
              ?creates:(Option.map
                          ~f:Values.BatchScheduleActionCreateRequest.of_json
                          creates)
              ?deletes:(Option.map
                          ~f:Values.BatchScheduleActionDeleteRequest.of_json
                          deletes) ~channelId ())
           (Some Values.BatchUpdateScheduleResponse.to_json)
           (Some Values.BatchUpdateScheduleResponse.error_to_json)])
let cancel_input_device_transfer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputDeviceId =
         flag "input-device-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_input_device_transfer
           (Values.CancelInputDeviceTransferRequest.make ~inputDeviceId ())
           (Some Values.CancelInputDeviceTransferResponse.to_json)
           (Some Values.CancelInputDeviceTransferResponse.error_to_json)])
let claim_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.claim_device (Values.ClaimDeviceRequest.make ?id ())
           (Some Values.ClaimDeviceResponse.to_json)
           (Some Values.ClaimDeviceResponse.error_to_json)])
let create_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cdiInputSpecification =
         flag "cdi-input-specification" (optional json_arg)
           ~doc:"JSON CdiInputSpecification"
       and channelClass =
         flag "channel-class" (optional json_arg) ~doc:"JSON ChannelClass"
       and destinations =
         flag "destinations" (optional json_arg)
           ~doc:"JSON __listOfOutputDestination"
       and encoderSettings =
         flag "encoder-settings" (optional json_arg)
           ~doc:"JSON EncoderSettings"
       and inputAttachments =
         flag "input-attachments" (optional json_arg)
           ~doc:"JSON __listOfInputAttachment"
       and inputSpecification =
         flag "input-specification" (optional json_arg)
           ~doc:"JSON InputSpecification"
       and logLevel =
         flag "log-level" (optional json_arg) ~doc:"JSON LogLevel"
       and maintenance =
         flag "maintenance" (optional json_arg)
           ~doc:"JSON MaintenanceCreateSettings"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and requestId =
         flag "request-id" (optional string) ~doc:"STRING __string"
       and reserved =
         flag "reserved" (optional string) ~doc:"STRING __string"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and vpc = flag "vpc" (optional json_arg) ~doc:"JSON VpcOutputSettings"
       and anywhereSettings =
         flag "anywhere-settings" (optional json_arg)
           ~doc:"JSON AnywhereSettings"
       and channelEngineVersion =
         flag "channel-engine-version" (optional json_arg)
           ~doc:"JSON ChannelEngineVersionRequest"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL __boolean"
       and linkedChannelSettings =
         flag "linked-channel-settings" (optional json_arg)
           ~doc:"JSON LinkedChannelSettings"
       and channelSecurityGroups =
         flag "channel-security-groups" (optional json_arg)
           ~doc:"JSON __listOf__string"
       and inferenceSettings =
         flag "inference-settings" (optional json_arg)
           ~doc:"JSON InferenceSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel
           (Values.CreateChannelRequest.make
              ?cdiInputSpecification:(Option.map
                                        ~f:Values.CdiInputSpecification.of_json
                                        cdiInputSpecification)
              ?channelClass:(Option.map ~f:Values.ChannelClass.of_json
                               channelClass)
              ?destinations:(Option.map
                               ~f:Values.Zz__listOfOutputDestination.of_json
                               destinations)
              ?encoderSettings:(Option.map ~f:Values.EncoderSettings.of_json
                                  encoderSettings)
              ?inputAttachments:(Option.map
                                   ~f:Values.Zz__listOfInputAttachment.of_json
                                   inputAttachments)
              ?inputSpecification:(Option.map
                                     ~f:Values.InputSpecification.of_json
                                     inputSpecification)
              ?logLevel:(Option.map ~f:Values.LogLevel.of_json logLevel)
              ?maintenance:(Option.map
                              ~f:Values.MaintenanceCreateSettings.of_json
                              maintenance) ?name ?requestId ?reserved
              ?roleArn ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?vpc:(Option.map ~f:Values.VpcOutputSettings.of_json vpc)
              ?anywhereSettings:(Option.map
                                   ~f:Values.AnywhereSettings.of_json
                                   anywhereSettings)
              ?channelEngineVersion:(Option.map
                                       ~f:Values.ChannelEngineVersionRequest.of_json
                                       channelEngineVersion) ?dryRun
              ?linkedChannelSettings:(Option.map
                                        ~f:Values.LinkedChannelSettings.of_json
                                        linkedChannelSettings)
              ?channelSecurityGroups:(Option.map
                                        ~f:Values.Zz__listOf__string.of_json
                                        channelSecurityGroups)
              ?inferenceSettings:(Option.map
                                    ~f:Values.InferenceSettings.of_json
                                    inferenceSettings) ())
           (Some Values.CreateChannelResponse.to_json)
           (Some Values.CreateChannelResponse.error_to_json)])
let create_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinations =
         flag "destinations" (optional json_arg)
           ~doc:"JSON __listOfInputDestinationRequest"
       and inputDevices =
         flag "input-devices" (optional json_arg)
           ~doc:"JSON __listOfInputDeviceSettings"
       and inputSecurityGroups =
         flag "input-security-groups" (optional json_arg)
           ~doc:"JSON __listOf__string"
       and mediaConnectFlows =
         flag "media-connect-flows" (optional json_arg)
           ~doc:"JSON __listOfMediaConnectFlowRequest"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and requestId =
         flag "request-id" (optional string) ~doc:"STRING __string"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING __string"
       and sources =
         flag "sources" (optional json_arg)
           ~doc:"JSON __listOfInputSourceRequest"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON InputType"
       and vpc = flag "vpc" (optional json_arg) ~doc:"JSON InputVpcRequest"
       and srtSettings =
         flag "srt-settings" (optional json_arg)
           ~doc:"JSON SrtSettingsRequest"
       and inputNetworkLocation =
         flag "input-network-location" (optional json_arg)
           ~doc:"JSON InputNetworkLocation"
       and multicastSettings =
         flag "multicast-settings" (optional json_arg)
           ~doc:"JSON MulticastSettingsCreateRequest"
       and smpte2110ReceiverGroupSettings =
         flag "smpte2110-receiver-group-settings" (optional json_arg)
           ~doc:"JSON Smpte2110ReceiverGroupSettings"
       and sdiSources =
         flag "sdi-sources" (optional json_arg) ~doc:"JSON InputSdiSources"
       and routerSettings =
         flag "router-settings" (optional json_arg)
           ~doc:"JSON RouterSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_input
           (Values.CreateInputRequest.make
              ?destinations:(Option.map
                               ~f:Values.Zz__listOfInputDestinationRequest.of_json
                               destinations)
              ?inputDevices:(Option.map
                               ~f:Values.Zz__listOfInputDeviceSettings.of_json
                               inputDevices)
              ?inputSecurityGroups:(Option.map
                                      ~f:Values.Zz__listOf__string.of_json
                                      inputSecurityGroups)
              ?mediaConnectFlows:(Option.map
                                    ~f:Values.Zz__listOfMediaConnectFlowRequest.of_json
                                    mediaConnectFlows) ?name ?requestId
              ?roleArn
              ?sources:(Option.map
                          ~f:Values.Zz__listOfInputSourceRequest.of_json
                          sources)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?type_:(Option.map ~f:Values.InputType.of_json type_)
              ?vpc:(Option.map ~f:Values.InputVpcRequest.of_json vpc)
              ?srtSettings:(Option.map ~f:Values.SrtSettingsRequest.of_json
                              srtSettings)
              ?inputNetworkLocation:(Option.map
                                       ~f:Values.InputNetworkLocation.of_json
                                       inputNetworkLocation)
              ?multicastSettings:(Option.map
                                    ~f:Values.MulticastSettingsCreateRequest.of_json
                                    multicastSettings)
              ?smpte2110ReceiverGroupSettings:(Option.map
                                                 ~f:Values.Smpte2110ReceiverGroupSettings.of_json
                                                 smpte2110ReceiverGroupSettings)
              ?sdiSources:(Option.map ~f:Values.InputSdiSources.of_json
                             sdiSources)
              ?routerSettings:(Option.map ~f:Values.RouterSettings.of_json
                                 routerSettings) ())
           (Some Values.CreateInputResponse.to_json)
           (Some Values.CreateInputResponse.error_to_json)])
let create_input_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and whitelistRules =
         flag "whitelist-rules" (optional json_arg)
           ~doc:"JSON __listOfInputWhitelistRuleCidr" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_input_security_group
           (Values.CreateInputSecurityGroupRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?whitelistRules:(Option.map
                                 ~f:Values.Zz__listOfInputWhitelistRuleCidr.of_json
                                 whitelistRules) ())
           (Some Values.CreateInputSecurityGroupResponse.to_json)
           (Some Values.CreateInputSecurityGroupResponse.error_to_json)])
let create_multiplex =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and availabilityZones =
         flag "availability-zones" (required json_arg)
           ~doc:"JSON __listOf__string"
       and multiplexSettings =
         flag "multiplex-settings" (required json_arg)
           ~doc:"JSON MultiplexSettings"
       and name = flag "name" (required string) ~doc:"STRING __string"
       and requestId =
         flag "request-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_multiplex
           (Values.CreateMultiplexRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~availabilityZones:(Values.Zz__listOf__string.of_json
                                    availabilityZones)
              ~multiplexSettings:(Values.MultiplexSettings.of_json
                                    multiplexSettings) ~name ~requestId ())
           (Some Values.CreateMultiplexResponse.to_json)
           (Some Values.CreateMultiplexResponse.error_to_json)])
let create_multiplex_program =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and multiplexId =
         flag "multiplex-id" (required string) ~doc:"STRING __string"
       and multiplexProgramSettings =
         flag "multiplex-program-settings" (required json_arg)
           ~doc:"JSON MultiplexProgramSettings"
       and programName =
         flag "program-name" (required string) ~doc:"STRING __string"
       and requestId =
         flag "request-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_multiplex_program
           (Values.CreateMultiplexProgramRequest.make ~multiplexId
              ~multiplexProgramSettings:(Values.MultiplexProgramSettings.of_json
                                           multiplexProgramSettings)
              ~programName ~requestId ())
           (Some Values.CreateMultiplexProgramResponse.to_json)
           (Some Values.CreateMultiplexProgramResponse.error_to_json)])
let create_partner_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requestId =
         flag "request-id" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and inputId = flag "input-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_partner_input
           (Values.CreatePartnerInputRequest.make ?requestId
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~inputId ())
           (Some Values.CreatePartnerInputResponse.to_json)
           (Some Values.CreatePartnerInputResponse.error_to_json)])
let create_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_tags
           (Values.CreateTagsRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~resourceArn ())
           None None])
let delete_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel (Values.DeleteChannelRequest.make ~channelId ())
           (Some Values.DeleteChannelResponse.to_json)
           (Some Values.DeleteChannelResponse.error_to_json)])
let delete_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputId = flag "input-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_input (Values.DeleteInputRequest.make ~inputId ())
           (Some Values.DeleteInputResponse.to_json)
           (Some Values.DeleteInputResponse.error_to_json)])
let delete_input_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputSecurityGroupId =
         flag "input-security-group-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_input_security_group
           (Values.DeleteInputSecurityGroupRequest.make ~inputSecurityGroupId
              ()) (Some Values.DeleteInputSecurityGroupResponse.to_json)
           (Some Values.DeleteInputSecurityGroupResponse.error_to_json)])
let delete_multiplex =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and multiplexId =
         flag "multiplex-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_multiplex
           (Values.DeleteMultiplexRequest.make ~multiplexId ())
           (Some Values.DeleteMultiplexResponse.to_json)
           (Some Values.DeleteMultiplexResponse.error_to_json)])
let delete_multiplex_program =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and multiplexId =
         flag "multiplex-id" (required string) ~doc:"STRING __string"
       and programName =
         flag "program-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_multiplex_program
           (Values.DeleteMultiplexProgramRequest.make ~multiplexId
              ~programName ())
           (Some Values.DeleteMultiplexProgramResponse.to_json)
           (Some Values.DeleteMultiplexProgramResponse.error_to_json)])
let delete_reservation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservationId =
         flag "reservation-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_reservation
           (Values.DeleteReservationRequest.make ~reservationId ())
           (Some Values.DeleteReservationResponse.to_json)
           (Some Values.DeleteReservationResponse.error_to_json)])
let delete_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_schedule
           (Values.DeleteScheduleRequest.make ~channelId ())
           (Some Values.DeleteScheduleResponse.to_json)
           (Some Values.DeleteScheduleResponse.error_to_json)])
let delete_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tags
           (Values.DeleteTagsRequest.make ~resourceArn
              ~tagKeys:(Values.Zz__listOf__string.of_json tagKeys) ()) None
           None])
let describe_account_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_configuration
           (Values.DescribeAccountConfigurationRequest.make ())
           (Some Values.DescribeAccountConfigurationResponse.to_json)
           (Some Values.DescribeAccountConfigurationResponse.error_to_json)])
let describe_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel
           (Values.DescribeChannelRequest.make ~channelId ())
           (Some Values.DescribeChannelResponse.to_json)
           (Some Values.DescribeChannelResponse.error_to_json)])
let describe_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputId = flag "input-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_input (Values.DescribeInputRequest.make ~inputId ())
           (Some Values.DescribeInputResponse.to_json)
           (Some Values.DescribeInputResponse.error_to_json)])
let describe_input_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputDeviceId =
         flag "input-device-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_input_device
           (Values.DescribeInputDeviceRequest.make ~inputDeviceId ())
           (Some Values.DescribeInputDeviceResponse.to_json)
           (Some Values.DescribeInputDeviceResponse.error_to_json)])
let describe_input_device_thumbnail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputDeviceId =
         flag "input-device-id" (required string) ~doc:"STRING __string"
       and accept =
         flag "accept" (required json_arg) ~doc:"JSON AcceptHeader" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_input_device_thumbnail
           (Values.DescribeInputDeviceThumbnailRequest.make ~inputDeviceId
              ~accept:(Values.AcceptHeader.of_json accept) ())
           (Some Values.DescribeInputDeviceThumbnailResponse.to_json)
           (Some Values.DescribeInputDeviceThumbnailResponse.error_to_json)])
let describe_input_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputSecurityGroupId =
         flag "input-security-group-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_input_security_group
           (Values.DescribeInputSecurityGroupRequest.make
              ~inputSecurityGroupId ())
           (Some Values.DescribeInputSecurityGroupResponse.to_json)
           (Some Values.DescribeInputSecurityGroupResponse.error_to_json)])
let describe_multiplex =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and multiplexId =
         flag "multiplex-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_multiplex
           (Values.DescribeMultiplexRequest.make ~multiplexId ())
           (Some Values.DescribeMultiplexResponse.to_json)
           (Some Values.DescribeMultiplexResponse.error_to_json)])
let describe_multiplex_program =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and multiplexId =
         flag "multiplex-id" (required string) ~doc:"STRING __string"
       and programName =
         flag "program-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_multiplex_program
           (Values.DescribeMultiplexProgramRequest.make ~multiplexId
              ~programName ())
           (Some Values.DescribeMultiplexProgramResponse.to_json)
           (Some Values.DescribeMultiplexProgramResponse.error_to_json)])
let describe_offering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and offeringId =
         flag "offering-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_offering
           (Values.DescribeOfferingRequest.make ~offeringId ())
           (Some Values.DescribeOfferingResponse.to_json)
           (Some Values.DescribeOfferingResponse.error_to_json)])
let describe_reservation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservationId =
         flag "reservation-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reservation
           (Values.DescribeReservationRequest.make ~reservationId ())
           (Some Values.DescribeReservationResponse.to_json)
           (Some Values.DescribeReservationResponse.error_to_json)])
let describe_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_schedule
           (Values.DescribeScheduleRequest.make ?maxResults ?nextToken
              ~channelId ()) (Some Values.DescribeScheduleResponse.to_json)
           (Some Values.DescribeScheduleResponse.error_to_json)])
let describe_thumbnails =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING __string"
       and pipelineId =
         flag "pipeline-id" (required string) ~doc:"STRING __string"
       and thumbnailType =
         flag "thumbnail-type" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_thumbnails
           (Values.DescribeThumbnailsRequest.make ~channelId ~pipelineId
              ~thumbnailType ())
           (Some Values.DescribeThumbnailsResponse.to_json)
           (Some Values.DescribeThumbnailsResponse.error_to_json)])
let list_channels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channels
           (Values.ListChannelsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListChannelsResponse.to_json)
           (Some Values.ListChannelsResponse.error_to_json)])
let list_input_device_transfers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and transferType =
         flag "transfer-type" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_input_device_transfers
           (Values.ListInputDeviceTransfersRequest.make ?maxResults
              ?nextToken ~transferType ())
           (Some Values.ListInputDeviceTransfersResponse.to_json)
           (Some Values.ListInputDeviceTransfersResponse.error_to_json)])
let list_input_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_input_devices
           (Values.ListInputDevicesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListInputDevicesResponse.to_json)
           (Some Values.ListInputDevicesResponse.error_to_json)])
let list_input_security_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_input_security_groups
           (Values.ListInputSecurityGroupsRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListInputSecurityGroupsResponse.to_json)
           (Some Values.ListInputSecurityGroupsResponse.error_to_json)])
let list_inputs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_inputs
           (Values.ListInputsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListInputsResponse.to_json)
           (Some Values.ListInputsResponse.error_to_json)])
let list_multiplex_programs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and multiplexId =
         flag "multiplex-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_multiplex_programs
           (Values.ListMultiplexProgramsRequest.make ?maxResults ?nextToken
              ~multiplexId ())
           (Some Values.ListMultiplexProgramsResponse.to_json)
           (Some Values.ListMultiplexProgramsResponse.error_to_json)])
let list_multiplexes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_multiplexes
           (Values.ListMultiplexesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListMultiplexesResponse.to_json)
           (Some Values.ListMultiplexesResponse.error_to_json)])
let list_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelClass =
         flag "channel-class" (optional string) ~doc:"STRING __string"
       and channelConfiguration =
         flag "channel-configuration" (optional string)
           ~doc:"STRING __string"
       and codec = flag "codec" (optional string) ~doc:"STRING __string"
       and duration =
         flag "duration" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and maximumBitrate =
         flag "maximum-bitrate" (optional string) ~doc:"STRING __string"
       and maximumFramerate =
         flag "maximum-framerate" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and resolution =
         flag "resolution" (optional string) ~doc:"STRING __string"
       and resourceType =
         flag "resource-type" (optional string) ~doc:"STRING __string"
       and specialFeature =
         flag "special-feature" (optional string) ~doc:"STRING __string"
       and videoQuality =
         flag "video-quality" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_offerings
           (Values.ListOfferingsRequest.make ?channelClass
              ?channelConfiguration ?codec ?duration ?maxResults
              ?maximumBitrate ?maximumFramerate ?nextToken ?resolution
              ?resourceType ?specialFeature ?videoQuality ())
           (Some Values.ListOfferingsResponse.to_json)
           (Some Values.ListOfferingsResponse.error_to_json)])
let list_reservations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelClass =
         flag "channel-class" (optional string) ~doc:"STRING __string"
       and codec = flag "codec" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and maximumBitrate =
         flag "maximum-bitrate" (optional string) ~doc:"STRING __string"
       and maximumFramerate =
         flag "maximum-framerate" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and resolution =
         flag "resolution" (optional string) ~doc:"STRING __string"
       and resourceType =
         flag "resource-type" (optional string) ~doc:"STRING __string"
       and specialFeature =
         flag "special-feature" (optional string) ~doc:"STRING __string"
       and videoQuality =
         flag "video-quality" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_reservations
           (Values.ListReservationsRequest.make ?channelClass ?codec
              ?maxResults ?maximumBitrate ?maximumFramerate ?nextToken
              ?resolution ?resourceType ?specialFeature ?videoQuality ())
           (Some Values.ListReservationsResponse.to_json)
           (Some Values.ListReservationsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let purchase_offering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and renewalSettings =
         flag "renewal-settings" (optional json_arg)
           ~doc:"JSON RenewalSettings"
       and requestId =
         flag "request-id" (optional string) ~doc:"STRING __string"
       and start = flag "start" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and count = flag "count" (required int) ~doc:"INT __integerMin1"
       and offeringId =
         flag "offering-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.purchase_offering
           (Values.PurchaseOfferingRequest.make ?name
              ?renewalSettings:(Option.map ~f:Values.RenewalSettings.of_json
                                  renewalSettings) ?requestId ?start
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~count
              ~offeringId ()) (Some Values.PurchaseOfferingResponse.to_json)
           (Some Values.PurchaseOfferingResponse.error_to_json)])
let reboot_input_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force =
         flag "force" (optional json_arg) ~doc:"JSON RebootInputDeviceForce"
       and inputDeviceId =
         flag "input-device-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_input_device
           (Values.RebootInputDeviceRequest.make
              ?force:(Option.map ~f:Values.RebootInputDeviceForce.of_json
                        force) ~inputDeviceId ())
           (Some Values.RebootInputDeviceResponse.to_json)
           (Some Values.RebootInputDeviceResponse.error_to_json)])
let reject_input_device_transfer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputDeviceId =
         flag "input-device-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_input_device_transfer
           (Values.RejectInputDeviceTransferRequest.make ~inputDeviceId ())
           (Some Values.RejectInputDeviceTransferResponse.to_json)
           (Some Values.RejectInputDeviceTransferResponse.error_to_json)])
let start_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_channel (Values.StartChannelRequest.make ~channelId ())
           (Some Values.StartChannelResponse.to_json)
           (Some Values.StartChannelResponse.error_to_json)])
let start_input_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputDeviceId =
         flag "input-device-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_input_device
           (Values.StartInputDeviceRequest.make ~inputDeviceId ())
           (Some Values.StartInputDeviceResponse.to_json)
           (Some Values.StartInputDeviceResponse.error_to_json)])
let start_input_device_maintenance_window =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputDeviceId =
         flag "input-device-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_input_device_maintenance_window
           (Values.StartInputDeviceMaintenanceWindowRequest.make
              ~inputDeviceId ())
           (Some Values.StartInputDeviceMaintenanceWindowResponse.to_json)
           (Some
              Values.StartInputDeviceMaintenanceWindowResponse.error_to_json)])
let start_multiplex =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and multiplexId =
         flag "multiplex-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_multiplex
           (Values.StartMultiplexRequest.make ~multiplexId ())
           (Some Values.StartMultiplexResponse.to_json)
           (Some Values.StartMultiplexResponse.error_to_json)])
let stop_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_channel (Values.StopChannelRequest.make ~channelId ())
           (Some Values.StopChannelResponse.to_json)
           (Some Values.StopChannelResponse.error_to_json)])
let stop_input_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputDeviceId =
         flag "input-device-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_input_device
           (Values.StopInputDeviceRequest.make ~inputDeviceId ())
           (Some Values.StopInputDeviceResponse.to_json)
           (Some Values.StopInputDeviceResponse.error_to_json)])
let stop_multiplex =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and multiplexId =
         flag "multiplex-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_multiplex
           (Values.StopMultiplexRequest.make ~multiplexId ())
           (Some Values.StopMultiplexResponse.to_json)
           (Some Values.StopMultiplexResponse.error_to_json)])
let transfer_input_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetCustomerId =
         flag "target-customer-id" (optional string) ~doc:"STRING __string"
       and targetRegion =
         flag "target-region" (optional string) ~doc:"STRING __string"
       and transferMessage =
         flag "transfer-message" (optional string) ~doc:"STRING __string"
       and inputDeviceId =
         flag "input-device-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.transfer_input_device
           (Values.TransferInputDeviceRequest.make ?targetCustomerId
              ?targetRegion ?transferMessage ~inputDeviceId ())
           (Some Values.TransferInputDeviceResponse.to_json)
           (Some Values.TransferInputDeviceResponse.error_to_json)])
let update_account_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountConfiguration =
         flag "account-configuration" (optional json_arg)
           ~doc:"JSON AccountConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account_configuration
           (Values.UpdateAccountConfigurationRequest.make
              ?accountConfiguration:(Option.map
                                       ~f:Values.AccountConfiguration.of_json
                                       accountConfiguration) ())
           (Some Values.UpdateAccountConfigurationResponse.to_json)
           (Some Values.UpdateAccountConfigurationResponse.error_to_json)])
let update_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cdiInputSpecification =
         flag "cdi-input-specification" (optional json_arg)
           ~doc:"JSON CdiInputSpecification"
       and destinations =
         flag "destinations" (optional json_arg)
           ~doc:"JSON __listOfOutputDestination"
       and encoderSettings =
         flag "encoder-settings" (optional json_arg)
           ~doc:"JSON EncoderSettings"
       and inputAttachments =
         flag "input-attachments" (optional json_arg)
           ~doc:"JSON __listOfInputAttachment"
       and inputSpecification =
         flag "input-specification" (optional json_arg)
           ~doc:"JSON InputSpecification"
       and logLevel =
         flag "log-level" (optional json_arg) ~doc:"JSON LogLevel"
       and maintenance =
         flag "maintenance" (optional json_arg)
           ~doc:"JSON MaintenanceUpdateSettings"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING __string"
       and channelEngineVersion =
         flag "channel-engine-version" (optional json_arg)
           ~doc:"JSON ChannelEngineVersionRequest"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL __boolean"
       and anywhereSettings =
         flag "anywhere-settings" (optional json_arg)
           ~doc:"JSON AnywhereSettings"
       and linkedChannelSettings =
         flag "linked-channel-settings" (optional json_arg)
           ~doc:"JSON LinkedChannelSettings"
       and channelSecurityGroups =
         flag "channel-security-groups" (optional json_arg)
           ~doc:"JSON __listOf__string"
       and inferenceSettings =
         flag "inference-settings" (optional json_arg)
           ~doc:"JSON InferenceSettings"
       and specialRouterSettings =
         flag "special-router-settings" (optional json_arg)
           ~doc:"JSON SpecialRouterSettings"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel
           (Values.UpdateChannelRequest.make
              ?cdiInputSpecification:(Option.map
                                        ~f:Values.CdiInputSpecification.of_json
                                        cdiInputSpecification)
              ?destinations:(Option.map
                               ~f:Values.Zz__listOfOutputDestination.of_json
                               destinations)
              ?encoderSettings:(Option.map ~f:Values.EncoderSettings.of_json
                                  encoderSettings)
              ?inputAttachments:(Option.map
                                   ~f:Values.Zz__listOfInputAttachment.of_json
                                   inputAttachments)
              ?inputSpecification:(Option.map
                                     ~f:Values.InputSpecification.of_json
                                     inputSpecification)
              ?logLevel:(Option.map ~f:Values.LogLevel.of_json logLevel)
              ?maintenance:(Option.map
                              ~f:Values.MaintenanceUpdateSettings.of_json
                              maintenance) ?name ?roleArn
              ?channelEngineVersion:(Option.map
                                       ~f:Values.ChannelEngineVersionRequest.of_json
                                       channelEngineVersion) ?dryRun
              ?anywhereSettings:(Option.map
                                   ~f:Values.AnywhereSettings.of_json
                                   anywhereSettings)
              ?linkedChannelSettings:(Option.map
                                        ~f:Values.LinkedChannelSettings.of_json
                                        linkedChannelSettings)
              ?channelSecurityGroups:(Option.map
                                        ~f:Values.Zz__listOf__string.of_json
                                        channelSecurityGroups)
              ?inferenceSettings:(Option.map
                                    ~f:Values.InferenceSettings.of_json
                                    inferenceSettings)
              ?specialRouterSettings:(Option.map
                                        ~f:Values.SpecialRouterSettings.of_json
                                        specialRouterSettings) ~channelId ())
           (Some Values.UpdateChannelResponse.to_json)
           (Some Values.UpdateChannelResponse.error_to_json)])
let update_channel_class =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinations =
         flag "destinations" (optional json_arg)
           ~doc:"JSON __listOfOutputDestination"
       and channelClass =
         flag "channel-class" (required json_arg) ~doc:"JSON ChannelClass"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel_class
           (Values.UpdateChannelClassRequest.make
              ?destinations:(Option.map
                               ~f:Values.Zz__listOfOutputDestination.of_json
                               destinations)
              ~channelClass:(Values.ChannelClass.of_json channelClass)
              ~channelId ()) (Some Values.UpdateChannelClassResponse.to_json)
           (Some Values.UpdateChannelClassResponse.error_to_json)])
let update_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinations =
         flag "destinations" (optional json_arg)
           ~doc:"JSON __listOfInputDestinationRequest"
       and inputDevices =
         flag "input-devices" (optional json_arg)
           ~doc:"JSON __listOfInputDeviceRequest"
       and inputSecurityGroups =
         flag "input-security-groups" (optional json_arg)
           ~doc:"JSON __listOf__string"
       and mediaConnectFlows =
         flag "media-connect-flows" (optional json_arg)
           ~doc:"JSON __listOfMediaConnectFlowRequest"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING __string"
       and sources =
         flag "sources" (optional json_arg)
           ~doc:"JSON __listOfInputSourceRequest"
       and srtSettings =
         flag "srt-settings" (optional json_arg)
           ~doc:"JSON SrtSettingsRequest"
       and multicastSettings =
         flag "multicast-settings" (optional json_arg)
           ~doc:"JSON MulticastSettingsUpdateRequest"
       and smpte2110ReceiverGroupSettings =
         flag "smpte2110-receiver-group-settings" (optional json_arg)
           ~doc:"JSON Smpte2110ReceiverGroupSettings"
       and sdiSources =
         flag "sdi-sources" (optional json_arg) ~doc:"JSON InputSdiSources"
       and specialRouterSettings =
         flag "special-router-settings" (optional json_arg)
           ~doc:"JSON SpecialRouterSettings"
       and inputId = flag "input-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_input
           (Values.UpdateInputRequest.make
              ?destinations:(Option.map
                               ~f:Values.Zz__listOfInputDestinationRequest.of_json
                               destinations)
              ?inputDevices:(Option.map
                               ~f:Values.Zz__listOfInputDeviceRequest.of_json
                               inputDevices)
              ?inputSecurityGroups:(Option.map
                                      ~f:Values.Zz__listOf__string.of_json
                                      inputSecurityGroups)
              ?mediaConnectFlows:(Option.map
                                    ~f:Values.Zz__listOfMediaConnectFlowRequest.of_json
                                    mediaConnectFlows) ?name ?roleArn
              ?sources:(Option.map
                          ~f:Values.Zz__listOfInputSourceRequest.of_json
                          sources)
              ?srtSettings:(Option.map ~f:Values.SrtSettingsRequest.of_json
                              srtSettings)
              ?multicastSettings:(Option.map
                                    ~f:Values.MulticastSettingsUpdateRequest.of_json
                                    multicastSettings)
              ?smpte2110ReceiverGroupSettings:(Option.map
                                                 ~f:Values.Smpte2110ReceiverGroupSettings.of_json
                                                 smpte2110ReceiverGroupSettings)
              ?sdiSources:(Option.map ~f:Values.InputSdiSources.of_json
                             sdiSources)
              ?specialRouterSettings:(Option.map
                                        ~f:Values.SpecialRouterSettings.of_json
                                        specialRouterSettings) ~inputId ())
           (Some Values.UpdateInputResponse.to_json)
           (Some Values.UpdateInputResponse.error_to_json)])
let update_input_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hdDeviceSettings =
         flag "hd-device-settings" (optional json_arg)
           ~doc:"JSON InputDeviceConfigurableSettings"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and uhdDeviceSettings =
         flag "uhd-device-settings" (optional json_arg)
           ~doc:"JSON InputDeviceConfigurableSettings"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING __string"
       and inputDeviceId =
         flag "input-device-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_input_device
           (Values.UpdateInputDeviceRequest.make
              ?hdDeviceSettings:(Option.map
                                   ~f:Values.InputDeviceConfigurableSettings.of_json
                                   hdDeviceSettings) ?name
              ?uhdDeviceSettings:(Option.map
                                    ~f:Values.InputDeviceConfigurableSettings.of_json
                                    uhdDeviceSettings) ?availabilityZone
              ~inputDeviceId ())
           (Some Values.UpdateInputDeviceResponse.to_json)
           (Some Values.UpdateInputDeviceResponse.error_to_json)])
let update_input_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and whitelistRules =
         flag "whitelist-rules" (optional json_arg)
           ~doc:"JSON __listOfInputWhitelistRuleCidr"
       and inputSecurityGroupId =
         flag "input-security-group-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_input_security_group
           (Values.UpdateInputSecurityGroupRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?whitelistRules:(Option.map
                                 ~f:Values.Zz__listOfInputWhitelistRuleCidr.of_json
                                 whitelistRules) ~inputSecurityGroupId ())
           (Some Values.UpdateInputSecurityGroupResponse.to_json)
           (Some Values.UpdateInputSecurityGroupResponse.error_to_json)])
let update_multiplex =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and multiplexSettings =
         flag "multiplex-settings" (optional json_arg)
           ~doc:"JSON MultiplexSettings"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and packetIdentifiersMapping =
         flag "packet-identifiers-mapping" (optional json_arg)
           ~doc:"JSON MultiplexPacketIdentifiersMapping"
       and multiplexId =
         flag "multiplex-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_multiplex
           (Values.UpdateMultiplexRequest.make
              ?multiplexSettings:(Option.map
                                    ~f:Values.MultiplexSettings.of_json
                                    multiplexSettings) ?name
              ?packetIdentifiersMapping:(Option.map
                                           ~f:Values.MultiplexPacketIdentifiersMapping.of_json
                                           packetIdentifiersMapping)
              ~multiplexId ()) (Some Values.UpdateMultiplexResponse.to_json)
           (Some Values.UpdateMultiplexResponse.error_to_json)])
let update_multiplex_program =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and multiplexProgramSettings =
         flag "multiplex-program-settings" (optional json_arg)
           ~doc:"JSON MultiplexProgramSettings"
       and multiplexId =
         flag "multiplex-id" (required string) ~doc:"STRING __string"
       and programName =
         flag "program-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_multiplex_program
           (Values.UpdateMultiplexProgramRequest.make
              ?multiplexProgramSettings:(Option.map
                                           ~f:Values.MultiplexProgramSettings.of_json
                                           multiplexProgramSettings)
              ~multiplexId ~programName ())
           (Some Values.UpdateMultiplexProgramResponse.to_json)
           (Some Values.UpdateMultiplexProgramResponse.error_to_json)])
let update_reservation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and renewalSettings =
         flag "renewal-settings" (optional json_arg)
           ~doc:"JSON RenewalSettings"
       and reservationId =
         flag "reservation-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_reservation
           (Values.UpdateReservationRequest.make ?name
              ?renewalSettings:(Option.map ~f:Values.RenewalSettings.of_json
                                  renewalSettings) ~reservationId ())
           (Some Values.UpdateReservationResponse.to_json)
           (Some Values.UpdateReservationResponse.error_to_json)])
let restart_channel_pipelines =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineIds =
         flag "pipeline-ids" (optional json_arg)
           ~doc:"JSON __listOfChannelPipelineIdToRestart"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restart_channel_pipelines
           (Values.RestartChannelPipelinesRequest.make
              ?pipelineIds:(Option.map
                              ~f:Values.Zz__listOfChannelPipelineIdToRestart.of_json
                              pipelineIds) ~channelId ())
           (Some Values.RestartChannelPipelinesResponse.to_json)
           (Some Values.RestartChannelPipelinesResponse.error_to_json)])
let create_cloud_watch_alarm_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datapointsToAlarm =
         flag "datapoints-to-alarm" (optional int) ~doc:"INT __integerMin1"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max1024"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and requestId =
         flag "request-id" (optional string)
           ~doc:"STRING __stringMin1Max256PatternS"
       and comparisonOperator =
         flag "comparison-operator" (required json_arg)
           ~doc:"JSON CloudWatchAlarmTemplateComparisonOperator"
       and evaluationPeriods =
         flag "evaluation-periods" (required int) ~doc:"INT __integerMin1"
       and groupIdentifier =
         flag "group-identifier" (required string)
           ~doc:"STRING __stringPatternS"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING __stringMax64"
       and name =
         flag "name" (required string)
           ~doc:"STRING __stringMin1Max255PatternS"
       and period =
         flag "period" (required int) ~doc:"INT __integerMin10Max86400"
       and statistic =
         flag "statistic" (required json_arg)
           ~doc:"JSON CloudWatchAlarmTemplateStatistic"
       and targetResourceType =
         flag "target-resource-type" (required json_arg)
           ~doc:"JSON CloudWatchAlarmTemplateTargetResourceType"
       and threshold =
         flag "threshold" (required float) ~doc:"FLOAT __double"
       and treatMissingData =
         flag "treat-missing-data" (required json_arg)
           ~doc:"JSON CloudWatchAlarmTemplateTreatMissingData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cloud_watch_alarm_template
           (Values.CreateCloudWatchAlarmTemplateRequest.make
              ?datapointsToAlarm ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?requestId
              ~comparisonOperator:(Values.CloudWatchAlarmTemplateComparisonOperator.of_json
                                     comparisonOperator) ~evaluationPeriods
              ~groupIdentifier ~metricName ~name ~period
              ~statistic:(Values.CloudWatchAlarmTemplateStatistic.of_json
                            statistic)
              ~targetResourceType:(Values.CloudWatchAlarmTemplateTargetResourceType.of_json
                                     targetResourceType) ~threshold
              ~treatMissingData:(Values.CloudWatchAlarmTemplateTreatMissingData.of_json
                                   treatMissingData) ())
           (Some Values.CreateCloudWatchAlarmTemplateResponse.to_json)
           (Some Values.CreateCloudWatchAlarmTemplateResponse.error_to_json)])
let create_cloud_watch_alarm_template_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max1024"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and requestId =
         flag "request-id" (optional string)
           ~doc:"STRING __stringMin1Max256PatternS"
       and name =
         flag "name" (required string)
           ~doc:"STRING __stringMin1Max255PatternS" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cloud_watch_alarm_template_group
           (Values.CreateCloudWatchAlarmTemplateGroupRequest.make
              ?description ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?requestId ~name ())
           (Some Values.CreateCloudWatchAlarmTemplateGroupResponse.to_json)
           (Some
              Values.CreateCloudWatchAlarmTemplateGroupResponse.error_to_json)])
let create_event_bridge_rule_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max1024"
       and eventTargets =
         flag "event-targets" (optional json_arg)
           ~doc:"JSON __listOfEventBridgeRuleTemplateTarget"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and requestId =
         flag "request-id" (optional string)
           ~doc:"STRING __stringMin1Max256PatternS"
       and eventType =
         flag "event-type" (required json_arg)
           ~doc:"JSON EventBridgeRuleTemplateEventType"
       and groupIdentifier =
         flag "group-identifier" (required string)
           ~doc:"STRING __stringPatternS"
       and name =
         flag "name" (required string)
           ~doc:"STRING __stringMin1Max255PatternS" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_event_bridge_rule_template
           (Values.CreateEventBridgeRuleTemplateRequest.make ?description
              ?eventTargets:(Option.map
                               ~f:Values.Zz__listOfEventBridgeRuleTemplateTarget.of_json
                               eventTargets)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?requestId
              ~eventType:(Values.EventBridgeRuleTemplateEventType.of_json
                            eventType) ~groupIdentifier ~name ())
           (Some Values.CreateEventBridgeRuleTemplateResponse.to_json)
           (Some Values.CreateEventBridgeRuleTemplateResponse.error_to_json)])
let create_event_bridge_rule_template_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max1024"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and requestId =
         flag "request-id" (optional string)
           ~doc:"STRING __stringMin1Max256PatternS"
       and name =
         flag "name" (required string)
           ~doc:"STRING __stringMin1Max255PatternS" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_event_bridge_rule_template_group
           (Values.CreateEventBridgeRuleTemplateGroupRequest.make
              ?description ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?requestId ~name ())
           (Some Values.CreateEventBridgeRuleTemplateGroupResponse.to_json)
           (Some
              Values.CreateEventBridgeRuleTemplateGroupResponse.error_to_json)])
let create_signal_map =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cloudWatchAlarmTemplateGroupIdentifiers =
         flag "cloud-watch-alarm-template-group-identifiers"
           (optional json_arg) ~doc:"JSON __listOf__stringPatternS"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max1024"
       and eventBridgeRuleTemplateGroupIdentifiers =
         flag "event-bridge-rule-template-group-identifiers"
           (optional json_arg) ~doc:"JSON __listOf__stringPatternS"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and requestId =
         flag "request-id" (optional string)
           ~doc:"STRING __stringMin1Max256PatternS"
       and discoveryEntryPointArn =
         flag "discovery-entry-point-arn" (required string)
           ~doc:"STRING __stringMin1Max2048"
       and name =
         flag "name" (required string)
           ~doc:"STRING __stringMin1Max255PatternS" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_signal_map
           (Values.CreateSignalMapRequest.make
              ?cloudWatchAlarmTemplateGroupIdentifiers:(Option.map
                                                          ~f:Values.Zz__listOf__stringPatternS.of_json
                                                          cloudWatchAlarmTemplateGroupIdentifiers)
              ?description
              ?eventBridgeRuleTemplateGroupIdentifiers:(Option.map
                                                          ~f:Values.Zz__listOf__stringPatternS.of_json
                                                          eventBridgeRuleTemplateGroupIdentifiers)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?requestId
              ~discoveryEntryPointArn ~name ())
           (Some Values.CreateSignalMapResponse.to_json)
           (Some Values.CreateSignalMapResponse.error_to_json)])
let delete_cloud_watch_alarm_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cloud_watch_alarm_template
           (Values.DeleteCloudWatchAlarmTemplateRequest.make ~identifier ())
           None None])
let delete_cloud_watch_alarm_template_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cloud_watch_alarm_template_group
           (Values.DeleteCloudWatchAlarmTemplateGroupRequest.make ~identifier
              ()) None None])
let delete_event_bridge_rule_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event_bridge_rule_template
           (Values.DeleteEventBridgeRuleTemplateRequest.make ~identifier ())
           None None])
let delete_event_bridge_rule_template_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event_bridge_rule_template_group
           (Values.DeleteEventBridgeRuleTemplateGroupRequest.make ~identifier
              ()) None None])
let delete_signal_map =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_signal_map
           (Values.DeleteSignalMapRequest.make ~identifier ()) None None])
let get_cloud_watch_alarm_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cloud_watch_alarm_template
           (Values.GetCloudWatchAlarmTemplateRequest.make ~identifier ())
           (Some Values.GetCloudWatchAlarmTemplateResponse.to_json)
           (Some Values.GetCloudWatchAlarmTemplateResponse.error_to_json)])
let get_cloud_watch_alarm_template_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cloud_watch_alarm_template_group
           (Values.GetCloudWatchAlarmTemplateGroupRequest.make ~identifier ())
           (Some Values.GetCloudWatchAlarmTemplateGroupResponse.to_json)
           (Some Values.GetCloudWatchAlarmTemplateGroupResponse.error_to_json)])
let get_event_bridge_rule_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_event_bridge_rule_template
           (Values.GetEventBridgeRuleTemplateRequest.make ~identifier ())
           (Some Values.GetEventBridgeRuleTemplateResponse.to_json)
           (Some Values.GetEventBridgeRuleTemplateResponse.error_to_json)])
let get_event_bridge_rule_template_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_event_bridge_rule_template_group
           (Values.GetEventBridgeRuleTemplateGroupRequest.make ~identifier ())
           (Some Values.GetEventBridgeRuleTemplateGroupResponse.to_json)
           (Some Values.GetEventBridgeRuleTemplateGroupResponse.error_to_json)])
let get_signal_map =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_signal_map (Values.GetSignalMapRequest.make ~identifier ())
           (Some Values.GetSignalMapResponse.to_json)
           (Some Values.GetSignalMapResponse.error_to_json)])
let list_cloud_watch_alarm_template_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and scope = flag "scope" (optional string) ~doc:"STRING __string"
       and signalMapIdentifier =
         flag "signal-map-identifier" (optional string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_cloud_watch_alarm_template_groups
           (Values.ListCloudWatchAlarmTemplateGroupsRequest.make ?maxResults
              ?nextToken ?scope ?signalMapIdentifier ())
           (Some Values.ListCloudWatchAlarmTemplateGroupsResponse.to_json)
           (Some
              Values.ListCloudWatchAlarmTemplateGroupsResponse.error_to_json)])
let list_cloud_watch_alarm_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupIdentifier =
         flag "group-identifier" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and scope = flag "scope" (optional string) ~doc:"STRING __string"
       and signalMapIdentifier =
         flag "signal-map-identifier" (optional string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_cloud_watch_alarm_templates
           (Values.ListCloudWatchAlarmTemplatesRequest.make ?groupIdentifier
              ?maxResults ?nextToken ?scope ?signalMapIdentifier ())
           (Some Values.ListCloudWatchAlarmTemplatesResponse.to_json)
           (Some Values.ListCloudWatchAlarmTemplatesResponse.error_to_json)])
let list_event_bridge_rule_template_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and signalMapIdentifier =
         flag "signal-map-identifier" (optional string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_event_bridge_rule_template_groups
           (Values.ListEventBridgeRuleTemplateGroupsRequest.make ?maxResults
              ?nextToken ?signalMapIdentifier ())
           (Some Values.ListEventBridgeRuleTemplateGroupsResponse.to_json)
           (Some
              Values.ListEventBridgeRuleTemplateGroupsResponse.error_to_json)])
let list_event_bridge_rule_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupIdentifier =
         flag "group-identifier" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and signalMapIdentifier =
         flag "signal-map-identifier" (optional string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_event_bridge_rule_templates
           (Values.ListEventBridgeRuleTemplatesRequest.make ?groupIdentifier
              ?maxResults ?nextToken ?signalMapIdentifier ())
           (Some Values.ListEventBridgeRuleTemplatesResponse.to_json)
           (Some Values.ListEventBridgeRuleTemplatesResponse.error_to_json)])
let list_signal_maps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cloudWatchAlarmTemplateGroupIdentifier =
         flag "cloud-watch-alarm-template-group-identifier" (optional string)
           ~doc:"STRING __string"
       and eventBridgeRuleTemplateGroupIdentifier =
         flag "event-bridge-rule-template-group-identifier" (optional string)
           ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_signal_maps
           (Values.ListSignalMapsRequest.make
              ?cloudWatchAlarmTemplateGroupIdentifier
              ?eventBridgeRuleTemplateGroupIdentifier ?maxResults ?nextToken
              ()) (Some Values.ListSignalMapsResponse.to_json)
           (Some Values.ListSignalMapsResponse.error_to_json)])
let start_delete_monitor_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_delete_monitor_deployment
           (Values.StartDeleteMonitorDeploymentRequest.make ~identifier ())
           (Some Values.StartDeleteMonitorDeploymentResponse.to_json)
           (Some Values.StartDeleteMonitorDeploymentResponse.error_to_json)])
let start_monitor_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL __boolean"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_monitor_deployment
           (Values.StartMonitorDeploymentRequest.make ?dryRun ~identifier ())
           (Some Values.StartMonitorDeploymentResponse.to_json)
           (Some Values.StartMonitorDeploymentResponse.error_to_json)])
let start_update_signal_map =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cloudWatchAlarmTemplateGroupIdentifiers =
         flag "cloud-watch-alarm-template-group-identifiers"
           (optional json_arg) ~doc:"JSON __listOf__stringPatternS"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max1024"
       and discoveryEntryPointArn =
         flag "discovery-entry-point-arn" (optional string)
           ~doc:"STRING __stringMin1Max2048"
       and eventBridgeRuleTemplateGroupIdentifiers =
         flag "event-bridge-rule-template-group-identifiers"
           (optional json_arg) ~doc:"JSON __listOf__stringPatternS"
       and forceRediscovery =
         flag "force-rediscovery" (optional bool) ~doc:"BOOL __boolean"
       and name =
         flag "name" (optional string)
           ~doc:"STRING __stringMin1Max255PatternS"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_update_signal_map
           (Values.StartUpdateSignalMapRequest.make
              ?cloudWatchAlarmTemplateGroupIdentifiers:(Option.map
                                                          ~f:Values.Zz__listOf__stringPatternS.of_json
                                                          cloudWatchAlarmTemplateGroupIdentifiers)
              ?description ?discoveryEntryPointArn
              ?eventBridgeRuleTemplateGroupIdentifiers:(Option.map
                                                          ~f:Values.Zz__listOf__stringPatternS.of_json
                                                          eventBridgeRuleTemplateGroupIdentifiers)
              ?forceRediscovery ?name ~identifier ())
           (Some Values.StartUpdateSignalMapResponse.to_json)
           (Some Values.StartUpdateSignalMapResponse.error_to_json)])
let update_cloud_watch_alarm_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comparisonOperator =
         flag "comparison-operator" (optional json_arg)
           ~doc:"JSON CloudWatchAlarmTemplateComparisonOperator"
       and datapointsToAlarm =
         flag "datapoints-to-alarm" (optional int) ~doc:"INT __integerMin1"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max1024"
       and evaluationPeriods =
         flag "evaluation-periods" (optional int) ~doc:"INT __integerMin1"
       and groupIdentifier =
         flag "group-identifier" (optional string)
           ~doc:"STRING __stringPatternS"
       and metricName =
         flag "metric-name" (optional string) ~doc:"STRING __stringMax64"
       and name =
         flag "name" (optional string)
           ~doc:"STRING __stringMin1Max255PatternS"
       and period =
         flag "period" (optional int) ~doc:"INT __integerMin10Max86400"
       and statistic =
         flag "statistic" (optional json_arg)
           ~doc:"JSON CloudWatchAlarmTemplateStatistic"
       and targetResourceType =
         flag "target-resource-type" (optional json_arg)
           ~doc:"JSON CloudWatchAlarmTemplateTargetResourceType"
       and threshold =
         flag "threshold" (optional float) ~doc:"FLOAT __double"
       and treatMissingData =
         flag "treat-missing-data" (optional json_arg)
           ~doc:"JSON CloudWatchAlarmTemplateTreatMissingData"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cloud_watch_alarm_template
           (Values.UpdateCloudWatchAlarmTemplateRequest.make
              ?comparisonOperator:(Option.map
                                     ~f:Values.CloudWatchAlarmTemplateComparisonOperator.of_json
                                     comparisonOperator) ?datapointsToAlarm
              ?description ?evaluationPeriods ?groupIdentifier ?metricName
              ?name ?period
              ?statistic:(Option.map
                            ~f:Values.CloudWatchAlarmTemplateStatistic.of_json
                            statistic)
              ?targetResourceType:(Option.map
                                     ~f:Values.CloudWatchAlarmTemplateTargetResourceType.of_json
                                     targetResourceType) ?threshold
              ?treatMissingData:(Option.map
                                   ~f:Values.CloudWatchAlarmTemplateTreatMissingData.of_json
                                   treatMissingData) ~identifier ())
           (Some Values.UpdateCloudWatchAlarmTemplateResponse.to_json)
           (Some Values.UpdateCloudWatchAlarmTemplateResponse.error_to_json)])
let update_cloud_watch_alarm_template_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max1024"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cloud_watch_alarm_template_group
           (Values.UpdateCloudWatchAlarmTemplateGroupRequest.make
              ?description ~identifier ())
           (Some Values.UpdateCloudWatchAlarmTemplateGroupResponse.to_json)
           (Some
              Values.UpdateCloudWatchAlarmTemplateGroupResponse.error_to_json)])
let update_event_bridge_rule_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max1024"
       and eventTargets =
         flag "event-targets" (optional json_arg)
           ~doc:"JSON __listOfEventBridgeRuleTemplateTarget"
       and eventType =
         flag "event-type" (optional json_arg)
           ~doc:"JSON EventBridgeRuleTemplateEventType"
       and groupIdentifier =
         flag "group-identifier" (optional string)
           ~doc:"STRING __stringPatternS"
       and name =
         flag "name" (optional string)
           ~doc:"STRING __stringMin1Max255PatternS"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_event_bridge_rule_template
           (Values.UpdateEventBridgeRuleTemplateRequest.make ?description
              ?eventTargets:(Option.map
                               ~f:Values.Zz__listOfEventBridgeRuleTemplateTarget.of_json
                               eventTargets)
              ?eventType:(Option.map
                            ~f:Values.EventBridgeRuleTemplateEventType.of_json
                            eventType) ?groupIdentifier ?name ~identifier ())
           (Some Values.UpdateEventBridgeRuleTemplateResponse.to_json)
           (Some Values.UpdateEventBridgeRuleTemplateResponse.error_to_json)])
let update_event_bridge_rule_template_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max1024"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_event_bridge_rule_template_group
           (Values.UpdateEventBridgeRuleTemplateGroupRequest.make
              ?description ~identifier ())
           (Some Values.UpdateEventBridgeRuleTemplateGroupResponse.to_json)
           (Some
              Values.UpdateEventBridgeRuleTemplateGroupResponse.error_to_json)])
let create_channel_placement_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and nodes =
         flag "nodes" (optional json_arg) ~doc:"JSON __listOf__string"
       and requestId =
         flag "request-id" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel_placement_group
           (Values.CreateChannelPlacementGroupRequest.make ?name
              ?nodes:(Option.map ~f:Values.Zz__listOf__string.of_json nodes)
              ?requestId ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~clusterId ())
           (Some Values.CreateChannelPlacementGroupResponse.to_json)
           (Some Values.CreateChannelPlacementGroupResponse.error_to_json)])
let create_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterType =
         flag "cluster-type" (optional json_arg) ~doc:"JSON ClusterType"
       and instanceRoleArn =
         flag "instance-role-arn" (optional string) ~doc:"STRING __string"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and networkSettings =
         flag "network-settings" (optional json_arg)
           ~doc:"JSON ClusterNetworkSettingsCreateRequest"
       and requestId =
         flag "request-id" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster
           (Values.CreateClusterRequest.make
              ?clusterType:(Option.map ~f:Values.ClusterType.of_json
                              clusterType) ?instanceRoleArn ?name
              ?networkSettings:(Option.map
                                  ~f:Values.ClusterNetworkSettingsCreateRequest.of_json
                                  networkSettings) ?requestId
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ())
           (Some Values.CreateClusterResponse.to_json)
           (Some Values.CreateClusterResponse.error_to_json)])
let create_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ipPools =
         flag "ip-pools" (optional json_arg)
           ~doc:"JSON __listOfIpPoolCreateRequest"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and requestId =
         flag "request-id" (optional string) ~doc:"STRING __string"
       and routes =
         flag "routes" (optional json_arg)
           ~doc:"JSON __listOfRouteCreateRequest"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_network
           (Values.CreateNetworkRequest.make
              ?ipPools:(Option.map
                          ~f:Values.Zz__listOfIpPoolCreateRequest.of_json
                          ipPools) ?name ?requestId
              ?routes:(Option.map
                         ~f:Values.Zz__listOfRouteCreateRequest.of_json
                         routes)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ())
           (Some Values.CreateNetworkResponse.to_json)
           (Some Values.CreateNetworkResponse.error_to_json)])
let create_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and nodeInterfaceMappings =
         flag "node-interface-mappings" (optional json_arg)
           ~doc:"JSON __listOfNodeInterfaceMappingCreateRequest"
       and requestId =
         flag "request-id" (optional string) ~doc:"STRING __string"
       and role = flag "role" (optional json_arg) ~doc:"JSON NodeRole"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_node
           (Values.CreateNodeRequest.make ?name
              ?nodeInterfaceMappings:(Option.map
                                        ~f:Values.Zz__listOfNodeInterfaceMappingCreateRequest.of_json
                                        nodeInterfaceMappings) ?requestId
              ?role:(Option.map ~f:Values.NodeRole.of_json role)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~clusterId ())
           (Some Values.CreateNodeResponse.to_json)
           (Some Values.CreateNodeResponse.error_to_json)])
let create_node_registration_script =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (optional string) ~doc:"STRING __string"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and nodeInterfaceMappings =
         flag "node-interface-mappings" (optional json_arg)
           ~doc:"JSON __listOfNodeInterfaceMapping"
       and requestId =
         flag "request-id" (optional string) ~doc:"STRING __string"
       and role = flag "role" (optional json_arg) ~doc:"JSON NodeRole"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_node_registration_script
           (Values.CreateNodeRegistrationScriptRequest.make ?id ?name
              ?nodeInterfaceMappings:(Option.map
                                        ~f:Values.Zz__listOfNodeInterfaceMapping.of_json
                                        nodeInterfaceMappings) ?requestId
              ?role:(Option.map ~f:Values.NodeRole.of_json role) ~clusterId
              ()) (Some Values.CreateNodeRegistrationScriptResponse.to_json)
           (Some Values.CreateNodeRegistrationScriptResponse.error_to_json)])
let delete_channel_placement_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelPlacementGroupId =
         flag "channel-placement-group-id" (required string)
           ~doc:"STRING __string"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel_placement_group
           (Values.DeleteChannelPlacementGroupRequest.make
              ~channelPlacementGroupId ~clusterId ())
           (Some Values.DeleteChannelPlacementGroupResponse.to_json)
           (Some Values.DeleteChannelPlacementGroupResponse.error_to_json)])
let delete_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster (Values.DeleteClusterRequest.make ~clusterId ())
           (Some Values.DeleteClusterResponse.to_json)
           (Some Values.DeleteClusterResponse.error_to_json)])
let delete_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network (Values.DeleteNetworkRequest.make ~networkId ())
           (Some Values.DeleteNetworkResponse.to_json)
           (Some Values.DeleteNetworkResponse.error_to_json)])
let delete_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string"
       and nodeId = flag "node-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_node
           (Values.DeleteNodeRequest.make ~clusterId ~nodeId ())
           (Some Values.DeleteNodeResponse.to_json)
           (Some Values.DeleteNodeResponse.error_to_json)])
let describe_channel_placement_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelPlacementGroupId =
         flag "channel-placement-group-id" (required string)
           ~doc:"STRING __string"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel_placement_group
           (Values.DescribeChannelPlacementGroupRequest.make
              ~channelPlacementGroupId ~clusterId ())
           (Some Values.DescribeChannelPlacementGroupResponse.to_json)
           (Some Values.DescribeChannelPlacementGroupResponse.error_to_json)])
let describe_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster
           (Values.DescribeClusterRequest.make ~clusterId ())
           (Some Values.DescribeClusterResponse.to_json)
           (Some Values.DescribeClusterResponse.error_to_json)])
let describe_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_network
           (Values.DescribeNetworkRequest.make ~networkId ())
           (Some Values.DescribeNetworkResponse.to_json)
           (Some Values.DescribeNetworkResponse.error_to_json)])
let describe_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string"
       and nodeId = flag "node-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_node
           (Values.DescribeNodeRequest.make ~clusterId ~nodeId ())
           (Some Values.DescribeNodeResponse.to_json)
           (Some Values.DescribeNodeResponse.error_to_json)])
let list_channel_placement_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_placement_groups
           (Values.ListChannelPlacementGroupsRequest.make ?maxResults
              ?nextToken ~clusterId ())
           (Some Values.ListChannelPlacementGroupsResponse.to_json)
           (Some Values.ListChannelPlacementGroupsResponse.error_to_json)])
let list_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_clusters
           (Values.ListClustersRequest.make ?maxResults ?nextToken ())
           (Some Values.ListClustersResponse.to_json)
           (Some Values.ListClustersResponse.error_to_json)])
let list_networks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_networks
           (Values.ListNetworksRequest.make ?maxResults ?nextToken ())
           (Some Values.ListNetworksResponse.to_json)
           (Some Values.ListNetworksResponse.error_to_json)])
let list_nodes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_nodes
           (Values.ListNodesRequest.make ?maxResults ?nextToken ~clusterId ())
           (Some Values.ListNodesResponse.to_json)
           (Some Values.ListNodesResponse.error_to_json)])
let update_channel_placement_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and nodes =
         flag "nodes" (optional json_arg) ~doc:"JSON __listOf__string"
       and channelPlacementGroupId =
         flag "channel-placement-group-id" (required string)
           ~doc:"STRING __string"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel_placement_group
           (Values.UpdateChannelPlacementGroupRequest.make ?name
              ?nodes:(Option.map ~f:Values.Zz__listOf__string.of_json nodes)
              ~channelPlacementGroupId ~clusterId ())
           (Some Values.UpdateChannelPlacementGroupResponse.to_json)
           (Some Values.UpdateChannelPlacementGroupResponse.error_to_json)])
let update_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and networkSettings =
         flag "network-settings" (optional json_arg)
           ~doc:"JSON ClusterNetworkSettingsUpdateRequest"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cluster
           (Values.UpdateClusterRequest.make ?name
              ?networkSettings:(Option.map
                                  ~f:Values.ClusterNetworkSettingsUpdateRequest.of_json
                                  networkSettings) ~clusterId ())
           (Some Values.UpdateClusterResponse.to_json)
           (Some Values.UpdateClusterResponse.error_to_json)])
let update_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ipPools =
         flag "ip-pools" (optional json_arg)
           ~doc:"JSON __listOfIpPoolUpdateRequest"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and routes =
         flag "routes" (optional json_arg)
           ~doc:"JSON __listOfRouteUpdateRequest"
       and networkId =
         flag "network-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_network
           (Values.UpdateNetworkRequest.make
              ?ipPools:(Option.map
                          ~f:Values.Zz__listOfIpPoolUpdateRequest.of_json
                          ipPools) ?name
              ?routes:(Option.map
                         ~f:Values.Zz__listOfRouteUpdateRequest.of_json
                         routes) ~networkId ())
           (Some Values.UpdateNetworkResponse.to_json)
           (Some Values.UpdateNetworkResponse.error_to_json)])
let update_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and role = flag "role" (optional json_arg) ~doc:"JSON NodeRole"
       and sdiSourceMappings =
         flag "sdi-source-mappings" (optional json_arg)
           ~doc:"JSON SdiSourceMappingsUpdateRequest"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string"
       and nodeId = flag "node-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_node
           (Values.UpdateNodeRequest.make ?name
              ?role:(Option.map ~f:Values.NodeRole.of_json role)
              ?sdiSourceMappings:(Option.map
                                    ~f:Values.SdiSourceMappingsUpdateRequest.of_json
                                    sdiSourceMappings) ~clusterId ~nodeId ())
           (Some Values.UpdateNodeResponse.to_json)
           (Some Values.UpdateNodeResponse.error_to_json)])
let update_node_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and state =
         flag "state" (optional json_arg) ~doc:"JSON UpdateNodeState"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string"
       and nodeId = flag "node-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_node_state
           (Values.UpdateNodeStateRequest.make
              ?state:(Option.map ~f:Values.UpdateNodeState.of_json state)
              ~clusterId ~nodeId ())
           (Some Values.UpdateNodeStateResponse.to_json)
           (Some Values.UpdateNodeStateResponse.error_to_json)])
let list_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_versions (Values.ListVersionsRequest.make ())
           (Some Values.ListVersionsResponse.to_json)
           (Some Values.ListVersionsResponse.error_to_json)])
let create_sdi_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mode = flag "mode" (optional json_arg) ~doc:"JSON SdiSourceMode"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and requestId =
         flag "request-id" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON SdiSourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sdi_source
           (Values.CreateSdiSourceRequest.make
              ?mode:(Option.map ~f:Values.SdiSourceMode.of_json mode) ?name
              ?requestId ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?type_:(Option.map ~f:Values.SdiSourceType.of_json type_) ())
           (Some Values.CreateSdiSourceResponse.to_json)
           (Some Values.CreateSdiSourceResponse.error_to_json)])
let delete_sdi_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sdiSourceId =
         flag "sdi-source-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_sdi_source
           (Values.DeleteSdiSourceRequest.make ~sdiSourceId ())
           (Some Values.DeleteSdiSourceResponse.to_json)
           (Some Values.DeleteSdiSourceResponse.error_to_json)])
let describe_sdi_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sdiSourceId =
         flag "sdi-source-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_sdi_source
           (Values.DescribeSdiSourceRequest.make ~sdiSourceId ())
           (Some Values.DescribeSdiSourceResponse.to_json)
           (Some Values.DescribeSdiSourceResponse.error_to_json)])
let list_sdi_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sdi_sources
           (Values.ListSdiSourcesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListSdiSourcesResponse.to_json)
           (Some Values.ListSdiSourcesResponse.error_to_json)])
let update_sdi_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mode = flag "mode" (optional json_arg) ~doc:"JSON SdiSourceMode"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON SdiSourceType"
       and sdiSourceId =
         flag "sdi-source-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_sdi_source
           (Values.UpdateSdiSourceRequest.make
              ?mode:(Option.map ~f:Values.SdiSourceMode.of_json mode) ?name
              ?type_:(Option.map ~f:Values.SdiSourceType.of_json type_)
              ~sdiSourceId ()) (Some Values.UpdateSdiSourceResponse.to_json)
           (Some Values.UpdateSdiSourceResponse.error_to_json)])
let list_alerts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and stateFilter =
         flag "state-filter" (optional string) ~doc:"STRING __string"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_alerts
           (Values.ListAlertsRequest.make ?maxResults ?nextToken ?stateFilter
              ~channelId ()) (Some Values.ListAlertsResponse.to_json)
           (Some Values.ListAlertsResponse.error_to_json)])
let list_cluster_alerts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and stateFilter =
         flag "state-filter" (optional string) ~doc:"STRING __string"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_cluster_alerts
           (Values.ListClusterAlertsRequest.make ?maxResults ?nextToken
              ?stateFilter ~clusterId ())
           (Some Values.ListClusterAlertsResponse.to_json)
           (Some Values.ListClusterAlertsResponse.error_to_json)])
let list_multiplex_alerts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and stateFilter =
         flag "state-filter" (optional string) ~doc:"STRING __string"
       and multiplexId =
         flag "multiplex-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_multiplex_alerts
           (Values.ListMultiplexAlertsRequest.make ?maxResults ?nextToken
              ?stateFilter ~multiplexId ())
           (Some Values.ListMultiplexAlertsResponse.to_json)
           (Some Values.ListMultiplexAlertsResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-input-device-transfer", accept_input_device_transfer);
    ("batch-delete", batch_delete);
    ("batch-start", batch_start);
    ("batch-stop", batch_stop);
    ("batch-update-schedule", batch_update_schedule);
    ("cancel-input-device-transfer", cancel_input_device_transfer);
    ("claim-device", claim_device);
    ("create-channel", create_channel);
    ("create-input", create_input);
    ("create-input-security-group", create_input_security_group);
    ("create-multiplex", create_multiplex);
    ("create-multiplex-program", create_multiplex_program);
    ("create-partner-input", create_partner_input);
    ("create-tags", create_tags);
    ("delete-channel", delete_channel);
    ("delete-input", delete_input);
    ("delete-input-security-group", delete_input_security_group);
    ("delete-multiplex", delete_multiplex);
    ("delete-multiplex-program", delete_multiplex_program);
    ("delete-reservation", delete_reservation);
    ("delete-schedule", delete_schedule);
    ("delete-tags", delete_tags);
    ("describe-account-configuration", describe_account_configuration);
    ("describe-channel", describe_channel);
    ("describe-input", describe_input);
    ("describe-input-device", describe_input_device);
    ("describe-input-device-thumbnail", describe_input_device_thumbnail);
    ("describe-input-security-group", describe_input_security_group);
    ("describe-multiplex", describe_multiplex);
    ("describe-multiplex-program", describe_multiplex_program);
    ("describe-offering", describe_offering);
    ("describe-reservation", describe_reservation);
    ("describe-schedule", describe_schedule);
    ("describe-thumbnails", describe_thumbnails);
    ("list-channels", list_channels);
    ("list-input-device-transfers", list_input_device_transfers);
    ("list-input-devices", list_input_devices);
    ("list-input-security-groups", list_input_security_groups);
    ("list-inputs", list_inputs);
    ("list-multiplex-programs", list_multiplex_programs);
    ("list-multiplexes", list_multiplexes);
    ("list-offerings", list_offerings);
    ("list-reservations", list_reservations);
    ("list-tags-for-resource", list_tags_for_resource);
    ("purchase-offering", purchase_offering);
    ("reboot-input-device", reboot_input_device);
    ("reject-input-device-transfer", reject_input_device_transfer);
    ("start-channel", start_channel);
    ("start-input-device", start_input_device);
    ("start-input-device-maintenance-window",
      start_input_device_maintenance_window);
    ("start-multiplex", start_multiplex);
    ("stop-channel", stop_channel);
    ("stop-input-device", stop_input_device);
    ("stop-multiplex", stop_multiplex);
    ("transfer-input-device", transfer_input_device);
    ("update-account-configuration", update_account_configuration);
    ("update-channel", update_channel);
    ("update-channel-class", update_channel_class);
    ("update-input", update_input);
    ("update-input-device", update_input_device);
    ("update-input-security-group", update_input_security_group);
    ("update-multiplex", update_multiplex);
    ("update-multiplex-program", update_multiplex_program);
    ("update-reservation", update_reservation);
    ("restart-channel-pipelines", restart_channel_pipelines);
    ("create-cloud-watch-alarm-template", create_cloud_watch_alarm_template);
    ("create-cloud-watch-alarm-template-group",
      create_cloud_watch_alarm_template_group);
    ("create-event-bridge-rule-template", create_event_bridge_rule_template);
    ("create-event-bridge-rule-template-group",
      create_event_bridge_rule_template_group);
    ("create-signal-map", create_signal_map);
    ("delete-cloud-watch-alarm-template", delete_cloud_watch_alarm_template);
    ("delete-cloud-watch-alarm-template-group",
      delete_cloud_watch_alarm_template_group);
    ("delete-event-bridge-rule-template", delete_event_bridge_rule_template);
    ("delete-event-bridge-rule-template-group",
      delete_event_bridge_rule_template_group);
    ("delete-signal-map", delete_signal_map);
    ("get-cloud-watch-alarm-template", get_cloud_watch_alarm_template);
    ("get-cloud-watch-alarm-template-group",
      get_cloud_watch_alarm_template_group);
    ("get-event-bridge-rule-template", get_event_bridge_rule_template);
    ("get-event-bridge-rule-template-group",
      get_event_bridge_rule_template_group);
    ("get-signal-map", get_signal_map);
    ("list-cloud-watch-alarm-template-groups",
      list_cloud_watch_alarm_template_groups);
    ("list-cloud-watch-alarm-templates", list_cloud_watch_alarm_templates);
    ("list-event-bridge-rule-template-groups",
      list_event_bridge_rule_template_groups);
    ("list-event-bridge-rule-templates", list_event_bridge_rule_templates);
    ("list-signal-maps", list_signal_maps);
    ("start-delete-monitor-deployment", start_delete_monitor_deployment);
    ("start-monitor-deployment", start_monitor_deployment);
    ("start-update-signal-map", start_update_signal_map);
    ("update-cloud-watch-alarm-template", update_cloud_watch_alarm_template);
    ("update-cloud-watch-alarm-template-group",
      update_cloud_watch_alarm_template_group);
    ("update-event-bridge-rule-template", update_event_bridge_rule_template);
    ("update-event-bridge-rule-template-group",
      update_event_bridge_rule_template_group);
    ("create-channel-placement-group", create_channel_placement_group);
    ("create-cluster", create_cluster);
    ("create-network", create_network);
    ("create-node", create_node);
    ("create-node-registration-script", create_node_registration_script);
    ("delete-channel-placement-group", delete_channel_placement_group);
    ("delete-cluster", delete_cluster);
    ("delete-network", delete_network);
    ("delete-node", delete_node);
    ("describe-channel-placement-group", describe_channel_placement_group);
    ("describe-cluster", describe_cluster);
    ("describe-network", describe_network);
    ("describe-node", describe_node);
    ("list-channel-placement-groups", list_channel_placement_groups);
    ("list-clusters", list_clusters);
    ("list-networks", list_networks);
    ("list-nodes", list_nodes);
    ("update-channel-placement-group", update_channel_placement_group);
    ("update-cluster", update_cluster);
    ("update-network", update_network);
    ("update-node", update_node);
    ("update-node-state", update_node_state);
    ("list-versions", list_versions);
    ("create-sdi-source", create_sdi_source);
    ("delete-sdi-source", delete_sdi_source);
    ("describe-sdi-source", describe_sdi_source);
    ("list-sdi-sources", list_sdi_sources);
    ("update-sdi-source", update_sdi_source);
    ("list-alerts", list_alerts);
    ("list-cluster-alerts", list_cluster_alerts);
    ("list-multiplex-alerts", list_multiplex_alerts)]
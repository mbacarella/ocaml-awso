(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_http_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and creatorRequestId =
         flag "creator-request-id" (optional string) ~doc:"STRING ResourceId"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string) ~doc:"STRING NamespaceNameHttp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_http_namespace
           (Values.CreateHttpNamespaceRequest.make ?creatorRequestId
              ?description ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~name ()) (Some Values.CreateHttpNamespaceResponse.to_json)
           (Some Values.CreateHttpNamespaceResponse.error_to_json)])
let create_private_dns_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and creatorRequestId =
         flag "creator-request-id" (optional string) ~doc:"STRING ResourceId"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and properties =
         flag "properties" (optional json_arg)
           ~doc:"JSON PrivateDnsNamespaceProperties"
       and name =
         flag "name" (required string) ~doc:"STRING NamespaceNamePrivate"
       and vpc = flag "vpc" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_private_dns_namespace
           (Values.CreatePrivateDnsNamespaceRequest.make ?creatorRequestId
              ?description ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?properties:(Option.map
                             ~f:Values.PrivateDnsNamespaceProperties.of_json
                             properties) ~name ~vpc ())
           (Some Values.CreatePrivateDnsNamespaceResponse.to_json)
           (Some Values.CreatePrivateDnsNamespaceResponse.error_to_json)])
let create_public_dns_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and creatorRequestId =
         flag "creator-request-id" (optional string) ~doc:"STRING ResourceId"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and properties =
         flag "properties" (optional json_arg)
           ~doc:"JSON PublicDnsNamespaceProperties"
       and name =
         flag "name" (required string) ~doc:"STRING NamespaceNamePublic" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_public_dns_namespace
           (Values.CreatePublicDnsNamespaceRequest.make ?creatorRequestId
              ?description ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?properties:(Option.map
                             ~f:Values.PublicDnsNamespaceProperties.of_json
                             properties) ~name ())
           (Some Values.CreatePublicDnsNamespaceResponse.to_json)
           (Some Values.CreatePublicDnsNamespaceResponse.error_to_json)])
let create_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespaceId =
         flag "namespace-id" (optional string) ~doc:"STRING ResourceId"
       and creatorRequestId =
         flag "creator-request-id" (optional string) ~doc:"STRING ResourceId"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and dnsConfig =
         flag "dns-config" (optional json_arg) ~doc:"JSON DnsConfig"
       and healthCheckConfig =
         flag "health-check-config" (optional json_arg)
           ~doc:"JSON HealthCheckConfig"
       and healthCheckCustomConfig =
         flag "health-check-custom-config" (optional json_arg)
           ~doc:"JSON HealthCheckCustomConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON ServiceTypeOption"
       and name = flag "name" (required string) ~doc:"STRING ServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service
           (Values.CreateServiceRequest.make ?namespaceId ?creatorRequestId
              ?description
              ?dnsConfig:(Option.map ~f:Values.DnsConfig.of_json dnsConfig)
              ?healthCheckConfig:(Option.map
                                    ~f:Values.HealthCheckConfig.of_json
                                    healthCheckConfig)
              ?healthCheckCustomConfig:(Option.map
                                          ~f:Values.HealthCheckCustomConfig.of_json
                                          healthCheckCustomConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?type_:(Option.map ~f:Values.ServiceTypeOption.of_json type_)
              ~name ()) (Some Values.CreateServiceResponse.to_json)
           (Some Values.CreateServiceResponse.error_to_json)])
let delete_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_namespace (Values.DeleteNamespaceRequest.make ~id ())
           (Some Values.DeleteNamespaceResponse.to_json)
           (Some Values.DeleteNamespaceResponse.error_to_json)])
let delete_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service (Values.DeleteServiceRequest.make ~id ())
           (Some Values.DeleteServiceResponse.to_json)
           (Some Values.DeleteServiceResponse.error_to_json)])
let deregister_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceId =
         flag "service-id" (required string) ~doc:"STRING ResourceId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_instance
           (Values.DeregisterInstanceRequest.make ~serviceId ~instanceId ())
           (Some Values.DeregisterInstanceResponse.to_json)
           (Some Values.DeregisterInstanceResponse.error_to_json)])
let discover_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT DiscoverMaxResults"
       and queryParameters =
         flag "query-parameters" (optional json_arg) ~doc:"JSON Attributes"
       and optionalParameters =
         flag "optional-parameters" (optional json_arg)
           ~doc:"JSON Attributes"
       and healthStatus =
         flag "health-status" (optional json_arg)
           ~doc:"JSON HealthStatusFilter"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING NamespaceName"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING ServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.discover_instances
           (Values.DiscoverInstancesRequest.make ?maxResults
              ?queryParameters:(Option.map ~f:Values.Attributes.of_json
                                  queryParameters)
              ?optionalParameters:(Option.map ~f:Values.Attributes.of_json
                                     optionalParameters)
              ?healthStatus:(Option.map ~f:Values.HealthStatusFilter.of_json
                               healthStatus) ~namespaceName ~serviceName ())
           (Some Values.DiscoverInstancesResponse.to_json)
           (Some Values.DiscoverInstancesResponse.error_to_json)])
let get_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceId =
         flag "service-id" (required string) ~doc:"STRING ResourceId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance
           (Values.GetInstanceRequest.make ~serviceId ~instanceId ())
           (Some Values.GetInstanceResponse.to_json)
           (Some Values.GetInstanceResponse.error_to_json)])
let get_instances_health_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instances =
         flag "instances" (optional json_arg) ~doc:"JSON InstanceIdList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and serviceId =
         flag "service-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instances_health_status
           (Values.GetInstancesHealthStatusRequest.make
              ?instances:(Option.map ~f:Values.InstanceIdList.of_json
                            instances) ?maxResults ?nextToken ~serviceId ())
           (Some Values.GetInstancesHealthStatusResponse.to_json)
           (Some Values.GetInstancesHealthStatusResponse.error_to_json)])
let get_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_namespace (Values.GetNamespaceRequest.make ~id ())
           (Some Values.GetNamespaceResponse.to_json)
           (Some Values.GetNamespaceResponse.error_to_json)])
let get_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operationId =
         flag "operation-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_operation (Values.GetOperationRequest.make ~operationId ())
           (Some Values.GetOperationResponse.to_json)
           (Some Values.GetOperationResponse.error_to_json)])
let get_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service (Values.GetServiceRequest.make ~id ())
           (Some Values.GetServiceResponse.to_json)
           (Some Values.GetServiceResponse.error_to_json)])
let list_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and serviceId =
         flag "service-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_instances
           (Values.ListInstancesRequest.make ?nextToken ?maxResults
              ~serviceId ()) (Some Values.ListInstancesResponse.to_json)
           (Some Values.ListInstancesResponse.error_to_json)])
let list_namespaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON NamespaceFilters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_namespaces
           (Values.ListNamespacesRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.NamespaceFilters.of_json filters)
              ()) (Some Values.ListNamespacesResponse.to_json)
           (Some Values.ListNamespacesResponse.error_to_json)])
let list_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON OperationFilters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_operations
           (Values.ListOperationsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.OperationFilters.of_json filters)
              ()) (Some Values.ListOperationsResponse.to_json)
           (Some Values.ListOperationsResponse.error_to_json)])
let list_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ServiceFilters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_services
           (Values.ListServicesRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.ServiceFilters.of_json filters)
              ()) (Some Values.ListServicesResponse.to_json)
           (Some Values.ListServicesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let register_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and creatorRequestId =
         flag "creator-request-id" (optional string) ~doc:"STRING ResourceId"
       and serviceId =
         flag "service-id" (required string) ~doc:"STRING ResourceId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and attributes =
         flag "attributes" (required json_arg) ~doc:"JSON Attributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_instance
           (Values.RegisterInstanceRequest.make ?creatorRequestId ~serviceId
              ~instanceId ~attributes:(Values.Attributes.of_json attributes)
              ()) (Some Values.RegisterInstanceResponse.to_json)
           (Some Values.RegisterInstanceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_http_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updaterRequestId =
         flag "updater-request-id" (optional string) ~doc:"STRING ResourceId"
       and id = flag "id" (required string) ~doc:"STRING ResourceId"
       and namespace =
         flag "namespace" (required json_arg) ~doc:"JSON HttpNamespaceChange" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_http_namespace
           (Values.UpdateHttpNamespaceRequest.make ?updaterRequestId ~id
              ~namespace:(Values.HttpNamespaceChange.of_json namespace) ())
           (Some Values.UpdateHttpNamespaceResponse.to_json)
           (Some Values.UpdateHttpNamespaceResponse.error_to_json)])
let update_instance_custom_health_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceId =
         flag "service-id" (required string) ~doc:"STRING ResourceId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING ResourceId"
       and status =
         flag "status" (required json_arg) ~doc:"JSON CustomHealthStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_instance_custom_health_status
           (Values.UpdateInstanceCustomHealthStatusRequest.make ~serviceId
              ~instanceId ~status:(Values.CustomHealthStatus.of_json status)
              ()) None None])
let update_private_dns_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updaterRequestId =
         flag "updater-request-id" (optional string) ~doc:"STRING ResourceId"
       and id = flag "id" (required string) ~doc:"STRING ResourceId"
       and namespace =
         flag "namespace" (required json_arg)
           ~doc:"JSON PrivateDnsNamespaceChange" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_private_dns_namespace
           (Values.UpdatePrivateDnsNamespaceRequest.make ?updaterRequestId
              ~id
              ~namespace:(Values.PrivateDnsNamespaceChange.of_json namespace)
              ()) (Some Values.UpdatePrivateDnsNamespaceResponse.to_json)
           (Some Values.UpdatePrivateDnsNamespaceResponse.error_to_json)])
let update_public_dns_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updaterRequestId =
         flag "updater-request-id" (optional string) ~doc:"STRING ResourceId"
       and id = flag "id" (required string) ~doc:"STRING ResourceId"
       and namespace =
         flag "namespace" (required json_arg)
           ~doc:"JSON PublicDnsNamespaceChange" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_public_dns_namespace
           (Values.UpdatePublicDnsNamespaceRequest.make ?updaterRequestId ~id
              ~namespace:(Values.PublicDnsNamespaceChange.of_json namespace)
              ()) (Some Values.UpdatePublicDnsNamespaceResponse.to_json)
           (Some Values.UpdatePublicDnsNamespaceResponse.error_to_json)])
let update_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceId"
       and service =
         flag "service" (required json_arg) ~doc:"JSON ServiceChange" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service
           (Values.UpdateServiceRequest.make ~id
              ~service:(Values.ServiceChange.of_json service) ())
           (Some Values.UpdateServiceResponse.to_json)
           (Some Values.UpdateServiceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-http-namespace", create_http_namespace);
    ("create-private-dns-namespace", create_private_dns_namespace);
    ("create-public-dns-namespace", create_public_dns_namespace);
    ("create-service", create_service);
    ("delete-namespace", delete_namespace);
    ("delete-service", delete_service);
    ("deregister-instance", deregister_instance);
    ("discover-instances", discover_instances);
    ("get-instance", get_instance);
    ("get-instances-health-status", get_instances_health_status);
    ("get-namespace", get_namespace);
    ("get-operation", get_operation);
    ("get-service", get_service);
    ("list-instances", list_instances);
    ("list-namespaces", list_namespaces);
    ("list-operations", list_operations);
    ("list-services", list_services);
    ("list-tags-for-resource", list_tags_for_resource);
    ("register-instance", register_instance);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-http-namespace", update_http_namespace);
    ("update-instance-custom-health-status",
      update_instance_custom_health_status);
    ("update-private-dns-namespace", update_private_dns_namespace);
    ("update-public-dns-namespace", update_public_dns_namespace);
    ("update-service", update_service)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | BatchGetPolicy: (BatchGetPolicyInput.t, BatchGetPolicyOutput.t,
  BatchGetPolicyOutput.error) t 
  | BatchIsAuthorized: (BatchIsAuthorizedInput.t, BatchIsAuthorizedOutput.t,
  BatchIsAuthorizedOutput.error) t 
  | BatchIsAuthorizedWithToken: (BatchIsAuthorizedWithTokenInput.t,
  BatchIsAuthorizedWithTokenOutput.t, BatchIsAuthorizedWithTokenOutput.error)
  t 
  | CreateIdentitySource: (CreateIdentitySourceInput.t,
  CreateIdentitySourceOutput.t, CreateIdentitySourceOutput.error) t 
  | CreatePolicy: (CreatePolicyInput.t, CreatePolicyOutput.t,
  CreatePolicyOutput.error) t 
  | CreatePolicyStore: (CreatePolicyStoreInput.t, CreatePolicyStoreOutput.t,
  CreatePolicyStoreOutput.error) t 
  | CreatePolicyStoreAlias: (CreatePolicyStoreAliasInput.t,
  CreatePolicyStoreAliasOutput.t, CreatePolicyStoreAliasOutput.error) t 
  | CreatePolicyTemplate: (CreatePolicyTemplateInput.t,
  CreatePolicyTemplateOutput.t, CreatePolicyTemplateOutput.error) t 
  | DeleteIdentitySource: (DeleteIdentitySourceInput.t,
  DeleteIdentitySourceOutput.t, DeleteIdentitySourceOutput.error) t 
  | DeletePolicy: (DeletePolicyInput.t, DeletePolicyOutput.t,
  DeletePolicyOutput.error) t 
  | DeletePolicyStore: (DeletePolicyStoreInput.t, DeletePolicyStoreOutput.t,
  DeletePolicyStoreOutput.error) t 
  | DeletePolicyStoreAlias: (DeletePolicyStoreAliasInput.t,
  DeletePolicyStoreAliasOutput.t, DeletePolicyStoreAliasOutput.error) t 
  | DeletePolicyTemplate: (DeletePolicyTemplateInput.t,
  DeletePolicyTemplateOutput.t, DeletePolicyTemplateOutput.error) t 
  | GetIdentitySource: (GetIdentitySourceInput.t, GetIdentitySourceOutput.t,
  GetIdentitySourceOutput.error) t 
  | GetPolicy: (GetPolicyInput.t, GetPolicyOutput.t, GetPolicyOutput.error) t
  
  | GetPolicyStore: (GetPolicyStoreInput.t, GetPolicyStoreOutput.t,
  GetPolicyStoreOutput.error) t 
  | GetPolicyStoreAlias: (GetPolicyStoreAliasInput.t,
  GetPolicyStoreAliasOutput.t, GetPolicyStoreAliasOutput.error) t 
  | GetPolicyTemplate: (GetPolicyTemplateInput.t, GetPolicyTemplateOutput.t,
  GetPolicyTemplateOutput.error) t 
  | GetSchema: (GetSchemaInput.t, GetSchemaOutput.t, GetSchemaOutput.error) t
  
  | IsAuthorized: (IsAuthorizedInput.t, IsAuthorizedOutput.t,
  IsAuthorizedOutput.error) t 
  | IsAuthorizedWithToken: (IsAuthorizedWithTokenInput.t,
  IsAuthorizedWithTokenOutput.t, IsAuthorizedWithTokenOutput.error) t 
  | ListIdentitySources: (ListIdentitySourcesInput.t,
  ListIdentitySourcesOutput.t, ListIdentitySourcesOutput.error) t 
  | ListPolicies: (ListPoliciesInput.t, ListPoliciesOutput.t,
  ListPoliciesOutput.error) t 
  | ListPolicyStoreAliases: (ListPolicyStoreAliasesInput.t,
  ListPolicyStoreAliasesOutput.t, ListPolicyStoreAliasesOutput.error) t 
  | ListPolicyStores: (ListPolicyStoresInput.t, ListPolicyStoresOutput.t,
  ListPolicyStoresOutput.error) t 
  | ListPolicyTemplates: (ListPolicyTemplatesInput.t,
  ListPolicyTemplatesOutput.t, ListPolicyTemplatesOutput.error) t 
  | ListTagsForResource: (ListTagsForResourceInput.t,
  ListTagsForResourceOutput.t, ListTagsForResourceOutput.error) t 
  | PutSchema: (PutSchemaInput.t, PutSchemaOutput.t, PutSchemaOutput.error) t
  
  | TagResource: (TagResourceInput.t, TagResourceOutput.t,
  TagResourceOutput.error) t 
  | UntagResource: (UntagResourceInput.t, UntagResourceOutput.t,
  UntagResourceOutput.error) t 
  | UpdateIdentitySource: (UpdateIdentitySourceInput.t,
  UpdateIdentitySourceOutput.t, UpdateIdentitySourceOutput.error) t 
  | UpdatePolicy: (UpdatePolicyInput.t, UpdatePolicyOutput.t,
  UpdatePolicyOutput.error) t 
  | UpdatePolicyStore: (UpdatePolicyStoreInput.t, UpdatePolicyStoreOutput.t,
  UpdatePolicyStoreOutput.error) t 
  | UpdatePolicyTemplate: (UpdatePolicyTemplateInput.t,
  UpdatePolicyTemplateOutput.t, UpdatePolicyTemplateOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | BatchGetPolicy -> `POST
  | BatchIsAuthorized -> `POST
  | BatchIsAuthorizedWithToken -> `POST
  | CreateIdentitySource -> `POST
  | CreatePolicy -> `POST
  | CreatePolicyStore -> `POST
  | CreatePolicyStoreAlias -> `POST
  | CreatePolicyTemplate -> `POST
  | DeleteIdentitySource -> `POST
  | DeletePolicy -> `POST
  | DeletePolicyStore -> `POST
  | DeletePolicyStoreAlias -> `POST
  | DeletePolicyTemplate -> `POST
  | GetIdentitySource -> `POST
  | GetPolicy -> `POST
  | GetPolicyStore -> `POST
  | GetPolicyStoreAlias -> `POST
  | GetPolicyTemplate -> `POST
  | GetSchema -> `POST
  | IsAuthorized -> `POST
  | IsAuthorizedWithToken -> `POST
  | ListIdentitySources -> `POST
  | ListPolicies -> `POST
  | ListPolicyStoreAliases -> `POST
  | ListPolicyStores -> `POST
  | ListPolicyTemplates -> `POST
  | ListTagsForResource -> `POST
  | PutSchema -> `POST
  | TagResource -> `POST
  | UntagResource -> `POST
  | UpdateIdentitySource -> `POST
  | UpdatePolicy -> `POST
  | UpdatePolicyStore -> `POST
  | UpdatePolicyTemplate -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | BatchGetPolicy -> (Format.kasprintf Uri.of_string) "/"
      | BatchIsAuthorized -> (Format.kasprintf Uri.of_string) "/"
      | BatchIsAuthorizedWithToken -> (Format.kasprintf Uri.of_string) "/"
      | CreateIdentitySource -> (Format.kasprintf Uri.of_string) "/"
      | CreatePolicy -> (Format.kasprintf Uri.of_string) "/"
      | CreatePolicyStore -> (Format.kasprintf Uri.of_string) "/"
      | CreatePolicyStoreAlias -> (Format.kasprintf Uri.of_string) "/"
      | CreatePolicyTemplate -> (Format.kasprintf Uri.of_string) "/"
      | DeleteIdentitySource -> (Format.kasprintf Uri.of_string) "/"
      | DeletePolicy -> (Format.kasprintf Uri.of_string) "/"
      | DeletePolicyStore -> (Format.kasprintf Uri.of_string) "/"
      | DeletePolicyStoreAlias -> (Format.kasprintf Uri.of_string) "/"
      | DeletePolicyTemplate -> (Format.kasprintf Uri.of_string) "/"
      | GetIdentitySource -> (Format.kasprintf Uri.of_string) "/"
      | GetPolicy -> (Format.kasprintf Uri.of_string) "/"
      | GetPolicyStore -> (Format.kasprintf Uri.of_string) "/"
      | GetPolicyStoreAlias -> (Format.kasprintf Uri.of_string) "/"
      | GetPolicyTemplate -> (Format.kasprintf Uri.of_string) "/"
      | GetSchema -> (Format.kasprintf Uri.of_string) "/"
      | IsAuthorized -> (Format.kasprintf Uri.of_string) "/"
      | IsAuthorizedWithToken -> (Format.kasprintf Uri.of_string) "/"
      | ListIdentitySources -> (Format.kasprintf Uri.of_string) "/"
      | ListPolicies -> (Format.kasprintf Uri.of_string) "/"
      | ListPolicyStoreAliases -> (Format.kasprintf Uri.of_string) "/"
      | ListPolicyStores -> (Format.kasprintf Uri.of_string) "/"
      | ListPolicyTemplates -> (Format.kasprintf Uri.of_string) "/"
      | ListTagsForResource -> (Format.kasprintf Uri.of_string) "/"
      | PutSchema -> (Format.kasprintf Uri.of_string) "/"
      | TagResource -> (Format.kasprintf Uri.of_string) "/"
      | UntagResource -> (Format.kasprintf Uri.of_string) "/"
      | UpdateIdentitySource -> (Format.kasprintf Uri.of_string) "/"
      | UpdatePolicy -> (Format.kasprintf Uri.of_string) "/"
      | UpdatePolicyStore -> (Format.kasprintf Uri.of_string) "/"
      | UpdatePolicyTemplate -> (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  match endp with
  | BatchGetPolicy ->
      let json = BatchGetPolicyInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.BatchGetPolicy")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | BatchIsAuthorized ->
      let json = BatchIsAuthorizedInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.BatchIsAuthorized")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | BatchIsAuthorizedWithToken ->
      let json = BatchIsAuthorizedWithTokenInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.BatchIsAuthorizedWithToken")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | CreateIdentitySource ->
      let json = CreateIdentitySourceInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.CreateIdentitySource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | CreatePolicy ->
      let json = CreatePolicyInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.CreatePolicy")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | CreatePolicyStore ->
      let json = CreatePolicyStoreInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.CreatePolicyStore")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | CreatePolicyStoreAlias ->
      let json = CreatePolicyStoreAliasInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.CreatePolicyStoreAlias")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | CreatePolicyTemplate ->
      let json = CreatePolicyTemplateInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.CreatePolicyTemplate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeleteIdentitySource ->
      let json = DeleteIdentitySourceInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.DeleteIdentitySource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeletePolicy ->
      let json = DeletePolicyInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.DeletePolicy")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeletePolicyStore ->
      let json = DeletePolicyStoreInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.DeletePolicyStore")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeletePolicyStoreAlias ->
      let json = DeletePolicyStoreAliasInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.DeletePolicyStoreAlias")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeletePolicyTemplate ->
      let json = DeletePolicyTemplateInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.DeletePolicyTemplate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetIdentitySource ->
      let json = GetIdentitySourceInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.GetIdentitySource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetPolicy ->
      let json = GetPolicyInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.GetPolicy")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetPolicyStore ->
      let json = GetPolicyStoreInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.GetPolicyStore")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetPolicyStoreAlias ->
      let json = GetPolicyStoreAliasInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.GetPolicyStoreAlias")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetPolicyTemplate ->
      let json = GetPolicyTemplateInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.GetPolicyTemplate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetSchema ->
      let json = GetSchemaInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.GetSchema")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | IsAuthorized ->
      let json = IsAuthorizedInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.IsAuthorized")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | IsAuthorizedWithToken ->
      let json = IsAuthorizedWithTokenInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.IsAuthorizedWithToken")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListIdentitySources ->
      let json = ListIdentitySourcesInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.ListIdentitySources")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListPolicies ->
      let json = ListPoliciesInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.ListPolicies")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListPolicyStoreAliases ->
      let json = ListPolicyStoreAliasesInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.ListPolicyStoreAliases")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListPolicyStores ->
      let json = ListPolicyStoresInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.ListPolicyStores")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListPolicyTemplates ->
      let json = ListPolicyTemplatesInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.ListPolicyTemplates")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListTagsForResource ->
      let json = ListTagsForResourceInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.ListTagsForResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | PutSchema ->
      let json = PutSchemaInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.PutSchema")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | TagResource ->
      let json = TagResourceInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.TagResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UntagResource ->
      let json = UntagResourceInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.UntagResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdateIdentitySource ->
      let json = UpdateIdentitySourceInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.UpdateIdentitySource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdatePolicy ->
      let json = UpdatePolicyInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.UpdatePolicy")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdatePolicyStore ->
      let json = UpdatePolicyStoreInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.UpdatePolicyStore")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdatePolicyTemplate ->
      let json = UpdatePolicyTemplateInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "VerifiedPermissions.UpdatePolicyTemplate")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = None }) in
    match (error_of_json, ((code >= 400) && (code <= 599))) with
    | (Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        (match json |> (Yojson.Safe.Util.member "__type") with
         | `String error_type -> error_of_json error_type json
         | `Null -> bail ()
         | _ ->
             failwith
               (sprintf "Error '__type' did not have string type: %s" body))
    | (None, _) | (_, false) -> bail () in
  let _ = parse_aws_error in
  let _ = resp in
  match endpoint with
  | BatchGetPolicy ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (BatchGetPolicyOutput.of_json json)
      else Error (parse_aws_error (Some BatchGetPolicyOutput.error_of_json))
  | BatchIsAuthorized ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (BatchIsAuthorizedOutput.of_json json)
      else
        Error (parse_aws_error (Some BatchIsAuthorizedOutput.error_of_json))
  | BatchIsAuthorizedWithToken ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (BatchIsAuthorizedWithTokenOutput.of_json json)
      else
        Error
          (parse_aws_error
             (Some BatchIsAuthorizedWithTokenOutput.error_of_json))
  | CreateIdentitySource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreateIdentitySourceOutput.of_json json)
      else
        Error
          (parse_aws_error (Some CreateIdentitySourceOutput.error_of_json))
  | CreatePolicy ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreatePolicyOutput.of_json json)
      else Error (parse_aws_error (Some CreatePolicyOutput.error_of_json))
  | CreatePolicyStore ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreatePolicyStoreOutput.of_json json)
      else
        Error (parse_aws_error (Some CreatePolicyStoreOutput.error_of_json))
  | CreatePolicyStoreAlias ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreatePolicyStoreAliasOutput.of_json json)
      else
        Error
          (parse_aws_error (Some CreatePolicyStoreAliasOutput.error_of_json))
  | CreatePolicyTemplate ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreatePolicyTemplateOutput.of_json json)
      else
        Error
          (parse_aws_error (Some CreatePolicyTemplateOutput.error_of_json))
  | DeleteIdentitySource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DeleteIdentitySourceOutput.of_json json)
      else
        Error
          (parse_aws_error (Some DeleteIdentitySourceOutput.error_of_json))
  | DeletePolicy ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DeletePolicyOutput.of_json json)
      else Error (parse_aws_error (Some DeletePolicyOutput.error_of_json))
  | DeletePolicyStore ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DeletePolicyStoreOutput.of_json json)
      else
        Error (parse_aws_error (Some DeletePolicyStoreOutput.error_of_json))
  | DeletePolicyStoreAlias ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DeletePolicyStoreAliasOutput.of_json json)
      else
        Error
          (parse_aws_error (Some DeletePolicyStoreAliasOutput.error_of_json))
  | DeletePolicyTemplate ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DeletePolicyTemplateOutput.of_json json)
      else
        Error
          (parse_aws_error (Some DeletePolicyTemplateOutput.error_of_json))
  | GetIdentitySource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetIdentitySourceOutput.of_json json)
      else
        Error (parse_aws_error (Some GetIdentitySourceOutput.error_of_json))
  | GetPolicy ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetPolicyOutput.of_json json)
      else Error (parse_aws_error (Some GetPolicyOutput.error_of_json))
  | GetPolicyStore ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetPolicyStoreOutput.of_json json)
      else Error (parse_aws_error (Some GetPolicyStoreOutput.error_of_json))
  | GetPolicyStoreAlias ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetPolicyStoreAliasOutput.of_json json)
      else
        Error
          (parse_aws_error (Some GetPolicyStoreAliasOutput.error_of_json))
  | GetPolicyTemplate ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetPolicyTemplateOutput.of_json json)
      else
        Error (parse_aws_error (Some GetPolicyTemplateOutput.error_of_json))
  | GetSchema ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetSchemaOutput.of_json json)
      else Error (parse_aws_error (Some GetSchemaOutput.error_of_json))
  | IsAuthorized ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (IsAuthorizedOutput.of_json json)
      else Error (parse_aws_error (Some IsAuthorizedOutput.error_of_json))
  | IsAuthorizedWithToken ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (IsAuthorizedWithTokenOutput.of_json json)
      else
        Error
          (parse_aws_error (Some IsAuthorizedWithTokenOutput.error_of_json))
  | ListIdentitySources ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListIdentitySourcesOutput.of_json json)
      else
        Error
          (parse_aws_error (Some ListIdentitySourcesOutput.error_of_json))
  | ListPolicies ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListPoliciesOutput.of_json json)
      else Error (parse_aws_error (Some ListPoliciesOutput.error_of_json))
  | ListPolicyStoreAliases ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListPolicyStoreAliasesOutput.of_json json)
      else
        Error
          (parse_aws_error (Some ListPolicyStoreAliasesOutput.error_of_json))
  | ListPolicyStores ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListPolicyStoresOutput.of_json json)
      else
        Error (parse_aws_error (Some ListPolicyStoresOutput.error_of_json))
  | ListPolicyTemplates ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListPolicyTemplatesOutput.of_json json)
      else
        Error
          (parse_aws_error (Some ListPolicyTemplatesOutput.error_of_json))
  | ListTagsForResource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListTagsForResourceOutput.of_json json)
      else
        Error
          (parse_aws_error (Some ListTagsForResourceOutput.error_of_json))
  | PutSchema ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (PutSchemaOutput.of_json json)
      else Error (parse_aws_error (Some PutSchemaOutput.error_of_json))
  | TagResource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (TagResourceOutput.of_json json)
      else Error (parse_aws_error (Some TagResourceOutput.error_of_json))
  | UntagResource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UntagResourceOutput.of_json json)
      else Error (parse_aws_error (Some UntagResourceOutput.error_of_json))
  | UpdateIdentitySource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UpdateIdentitySourceOutput.of_json json)
      else
        Error
          (parse_aws_error (Some UpdateIdentitySourceOutput.error_of_json))
  | UpdatePolicy ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UpdatePolicyOutput.of_json json)
      else Error (parse_aws_error (Some UpdatePolicyOutput.error_of_json))
  | UpdatePolicyStore ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UpdatePolicyStoreOutput.of_json json)
      else
        Error (parse_aws_error (Some UpdatePolicyStoreOutput.error_of_json))
  | UpdatePolicyTemplate ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UpdatePolicyTemplateOutput.of_json json)
      else
        Error
          (parse_aws_error (Some UpdatePolicyTemplateOutput.error_of_json))
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_get_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requests =
         flag "requests" (required json_arg)
           ~doc:"JSON BatchGetPolicyInputList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_policy
           (Values.BatchGetPolicyInput.make
              ~requests:(Values.BatchGetPolicyInputList.of_json requests) ())
           (Some Values.BatchGetPolicyOutput.to_json)
           (Some Values.BatchGetPolicyOutput.error_to_json)])
let batch_is_authorized =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entities =
         flag "entities" (optional json_arg) ~doc:"JSON EntitiesDefinition"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and requests =
         flag "requests" (required json_arg)
           ~doc:"JSON BatchIsAuthorizedInputList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_is_authorized
           (Values.BatchIsAuthorizedInput.make
              ?entities:(Option.map ~f:Values.EntitiesDefinition.of_json
                           entities) ~policyStoreId
              ~requests:(Values.BatchIsAuthorizedInputList.of_json requests)
              ()) (Some Values.BatchIsAuthorizedOutput.to_json)
           (Some Values.BatchIsAuthorizedOutput.error_to_json)])
let batch_is_authorized_with_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityToken =
         flag "identity-token" (optional string) ~doc:"STRING Token"
       and accessToken =
         flag "access-token" (optional string) ~doc:"STRING Token"
       and entities =
         flag "entities" (optional json_arg) ~doc:"JSON EntitiesDefinition"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and requests =
         flag "requests" (required json_arg)
           ~doc:"JSON BatchIsAuthorizedWithTokenInputList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_is_authorized_with_token
           (Values.BatchIsAuthorizedWithTokenInput.make ?identityToken
              ?accessToken
              ?entities:(Option.map ~f:Values.EntitiesDefinition.of_json
                           entities) ~policyStoreId
              ~requests:(Values.BatchIsAuthorizedWithTokenInputList.of_json
                           requests) ())
           (Some Values.BatchIsAuthorizedWithTokenOutput.to_json)
           (Some Values.BatchIsAuthorizedWithTokenOutput.error_to_json)])
let create_identity_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and principalEntityType =
         flag "principal-entity-type" (optional string)
           ~doc:"STRING PrincipalEntityType"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and configuration =
         flag "configuration" (required json_arg) ~doc:"JSON Configuration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_identity_source
           (Values.CreateIdentitySourceInput.make ?clientToken
              ?principalEntityType ~policyStoreId
              ~configuration:(Values.Configuration.of_json configuration) ())
           (Some Values.CreateIdentitySourceOutput.to_json)
           (Some Values.CreateIdentitySourceOutput.error_to_json)])
let create_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and name = flag "name" (optional string) ~doc:"STRING PolicyName"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and definition =
         flag "definition" (required json_arg) ~doc:"JSON PolicyDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_policy
           (Values.CreatePolicyInput.make ?clientToken ?name ~policyStoreId
              ~definition:(Values.PolicyDefinition.of_json definition) ())
           (Some Values.CreatePolicyOutput.to_json)
           (Some Values.CreatePolicyOutput.error_to_json)])
let create_policy_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PolicyStoreDescription"
       and deletionProtection =
         flag "deletion-protection" (optional json_arg)
           ~doc:"JSON DeletionProtection"
       and encryptionSettings =
         flag "encryption-settings" (optional json_arg)
           ~doc:"JSON EncryptionSettings"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and validationSettings =
         flag "validation-settings" (required json_arg)
           ~doc:"JSON ValidationSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_policy_store
           (Values.CreatePolicyStoreInput.make ?clientToken ?description
              ?deletionProtection:(Option.map
                                     ~f:Values.DeletionProtection.of_json
                                     deletionProtection)
              ?encryptionSettings:(Option.map
                                     ~f:Values.EncryptionSettings.of_json
                                     encryptionSettings)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~validationSettings:(Values.ValidationSettings.of_json
                                     validationSettings) ())
           (Some Values.CreatePolicyStoreOutput.to_json)
           (Some Values.CreatePolicyStoreOutput.error_to_json)])
let create_policy_store_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING Alias"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_policy_store_alias
           (Values.CreatePolicyStoreAliasInput.make ~aliasName ~policyStoreId
              ()) (Some Values.CreatePolicyStoreAliasOutput.to_json)
           (Some Values.CreatePolicyStoreAliasOutput.error_to_json)])
let create_policy_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PolicyTemplateDescription"
       and name =
         flag "name" (optional string) ~doc:"STRING PolicyTemplateName"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and statement =
         flag "statement" (required string) ~doc:"STRING PolicyStatement" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_policy_template
           (Values.CreatePolicyTemplateInput.make ?clientToken ?description
              ?name ~policyStoreId ~statement ())
           (Some Values.CreatePolicyTemplateOutput.to_json)
           (Some Values.CreatePolicyTemplateOutput.error_to_json)])
let delete_identity_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and identitySourceId =
         flag "identity-source-id" (required string)
           ~doc:"STRING IdentitySourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_identity_source
           (Values.DeleteIdentitySourceInput.make ~policyStoreId
              ~identitySourceId ())
           (Some Values.DeleteIdentitySourceOutput.to_json)
           (Some Values.DeleteIdentitySourceOutput.error_to_json)])
let delete_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy
           (Values.DeletePolicyInput.make ~policyStoreId ~policyId ())
           (Some Values.DeletePolicyOutput.to_json)
           (Some Values.DeletePolicyOutput.error_to_json)])
let delete_policy_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy_store
           (Values.DeletePolicyStoreInput.make ~policyStoreId ())
           (Some Values.DeletePolicyStoreOutput.to_json)
           (Some Values.DeletePolicyStoreOutput.error_to_json)])
let delete_policy_store_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING Alias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy_store_alias
           (Values.DeletePolicyStoreAliasInput.make ~aliasName ())
           (Some Values.DeletePolicyStoreAliasOutput.to_json)
           (Some Values.DeletePolicyStoreAliasOutput.error_to_json)])
let delete_policy_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and policyTemplateId =
         flag "policy-template-id" (required string)
           ~doc:"STRING PolicyTemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy_template
           (Values.DeletePolicyTemplateInput.make ~policyStoreId
              ~policyTemplateId ())
           (Some Values.DeletePolicyTemplateOutput.to_json)
           (Some Values.DeletePolicyTemplateOutput.error_to_json)])
let get_identity_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and identitySourceId =
         flag "identity-source-id" (required string)
           ~doc:"STRING IdentitySourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_identity_source
           (Values.GetIdentitySourceInput.make ~policyStoreId
              ~identitySourceId ())
           (Some Values.GetIdentitySourceOutput.to_json)
           (Some Values.GetIdentitySourceOutput.error_to_json)])
let get_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy
           (Values.GetPolicyInput.make ~policyStoreId ~policyId ())
           (Some Values.GetPolicyOutput.to_json)
           (Some Values.GetPolicyOutput.error_to_json)])
let get_policy_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional bool) ~doc:"BOOL Boolean"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy_store
           (Values.GetPolicyStoreInput.make ?tags ~policyStoreId ())
           (Some Values.GetPolicyStoreOutput.to_json)
           (Some Values.GetPolicyStoreOutput.error_to_json)])
let get_policy_store_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING Alias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy_store_alias
           (Values.GetPolicyStoreAliasInput.make ~aliasName ())
           (Some Values.GetPolicyStoreAliasOutput.to_json)
           (Some Values.GetPolicyStoreAliasOutput.error_to_json)])
let get_policy_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and policyTemplateId =
         flag "policy-template-id" (required string)
           ~doc:"STRING PolicyTemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy_template
           (Values.GetPolicyTemplateInput.make ~policyStoreId
              ~policyTemplateId ())
           (Some Values.GetPolicyTemplateOutput.to_json)
           (Some Values.GetPolicyTemplateOutput.error_to_json)])
let get_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_schema (Values.GetSchemaInput.make ~policyStoreId ())
           (Some Values.GetSchemaOutput.to_json)
           (Some Values.GetSchemaOutput.error_to_json)])
let is_authorized =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and principal =
         flag "principal" (optional json_arg) ~doc:"JSON EntityIdentifier"
       and action =
         flag "action" (optional json_arg) ~doc:"JSON ActionIdentifier"
       and resource =
         flag "resource" (optional json_arg) ~doc:"JSON EntityIdentifier"
       and context =
         flag "context" (optional json_arg) ~doc:"JSON ContextDefinition"
       and entities =
         flag "entities" (optional json_arg) ~doc:"JSON EntitiesDefinition"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.is_authorized
           (Values.IsAuthorizedInput.make
              ?principal:(Option.map ~f:Values.EntityIdentifier.of_json
                            principal)
              ?action:(Option.map ~f:Values.ActionIdentifier.of_json action)
              ?resource:(Option.map ~f:Values.EntityIdentifier.of_json
                           resource)
              ?context:(Option.map ~f:Values.ContextDefinition.of_json
                          context)
              ?entities:(Option.map ~f:Values.EntitiesDefinition.of_json
                           entities) ~policyStoreId ())
           (Some Values.IsAuthorizedOutput.to_json)
           (Some Values.IsAuthorizedOutput.error_to_json)])
let is_authorized_with_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityToken =
         flag "identity-token" (optional string) ~doc:"STRING Token"
       and accessToken =
         flag "access-token" (optional string) ~doc:"STRING Token"
       and action =
         flag "action" (optional json_arg) ~doc:"JSON ActionIdentifier"
       and resource =
         flag "resource" (optional json_arg) ~doc:"JSON EntityIdentifier"
       and context =
         flag "context" (optional json_arg) ~doc:"JSON ContextDefinition"
       and entities =
         flag "entities" (optional json_arg) ~doc:"JSON EntitiesDefinition"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.is_authorized_with_token
           (Values.IsAuthorizedWithTokenInput.make ?identityToken
              ?accessToken
              ?action:(Option.map ~f:Values.ActionIdentifier.of_json action)
              ?resource:(Option.map ~f:Values.EntityIdentifier.of_json
                           resource)
              ?context:(Option.map ~f:Values.ContextDefinition.of_json
                          context)
              ?entities:(Option.map ~f:Values.EntitiesDefinition.of_json
                           entities) ~policyStoreId ())
           (Some Values.IsAuthorizedWithTokenOutput.to_json)
           (Some Values.IsAuthorizedWithTokenOutput.error_to_json)])
let list_identity_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListIdentitySourcesMaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON IdentitySourceFilters"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_identity_sources
           (Values.ListIdentitySourcesInput.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.IdentitySourceFilters.of_json
                          filters) ~policyStoreId ())
           (Some Values.ListIdentitySourcesOutput.to_json)
           (Some Values.ListIdentitySourcesOutput.error_to_json)])
let list_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON PolicyFilter"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policies
           (Values.ListPoliciesInput.make ?nextToken ?maxResults
              ?filter:(Option.map ~f:Values.PolicyFilter.of_json filter)
              ~policyStoreId ()) (Some Values.ListPoliciesOutput.to_json)
           (Some Values.ListPoliciesOutput.error_to_json)])
let list_policy_store_aliases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON PolicyStoreAliasFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_store_aliases
           (Values.ListPolicyStoreAliasesInput.make ?nextToken ?maxResults
              ?filter:(Option.map ~f:Values.PolicyStoreAliasFilter.of_json
                         filter) ())
           (Some Values.ListPolicyStoreAliasesOutput.to_json)
           (Some Values.ListPolicyStoreAliasesOutput.error_to_json)])
let list_policy_stores =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_stores
           (Values.ListPolicyStoresInput.make ?nextToken ?maxResults ())
           (Some Values.ListPolicyStoresOutput.to_json)
           (Some Values.ListPolicyStoresOutput.error_to_json)])
let list_policy_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_templates
           (Values.ListPolicyTemplatesInput.make ?nextToken ?maxResults
              ~policyStoreId ())
           (Some Values.ListPolicyTemplatesOutput.to_json)
           (Some Values.ListPolicyTemplatesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let put_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and definition =
         flag "definition" (required json_arg) ~doc:"JSON SchemaDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_schema
           (Values.PutSchemaInput.make ~policyStoreId
              ~definition:(Values.SchemaDefinition.of_json definition) ())
           (Some Values.PutSchemaOutput.to_json)
           (Some Values.PutSchemaOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_identity_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and principalEntityType =
         flag "principal-entity-type" (optional string)
           ~doc:"STRING PrincipalEntityType"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and identitySourceId =
         flag "identity-source-id" (required string)
           ~doc:"STRING IdentitySourceId"
       and updateConfiguration =
         flag "update-configuration" (required json_arg)
           ~doc:"JSON UpdateConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_identity_source
           (Values.UpdateIdentitySourceInput.make ?principalEntityType
              ~policyStoreId ~identitySourceId
              ~updateConfiguration:(Values.UpdateConfiguration.of_json
                                      updateConfiguration) ())
           (Some Values.UpdateIdentitySourceOutput.to_json)
           (Some Values.UpdateIdentitySourceOutput.error_to_json)])
let update_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and definition =
         flag "definition" (optional json_arg)
           ~doc:"JSON UpdatePolicyDefinition"
       and name = flag "name" (optional string) ~doc:"STRING PolicyName"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_policy
           (Values.UpdatePolicyInput.make
              ?definition:(Option.map
                             ~f:Values.UpdatePolicyDefinition.of_json
                             definition) ?name ~policyStoreId ~policyId ())
           (Some Values.UpdatePolicyOutput.to_json)
           (Some Values.UpdatePolicyOutput.error_to_json)])
let update_policy_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deletionProtection =
         flag "deletion-protection" (optional json_arg)
           ~doc:"JSON DeletionProtection"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PolicyStoreDescription"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and validationSettings =
         flag "validation-settings" (required json_arg)
           ~doc:"JSON ValidationSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_policy_store
           (Values.UpdatePolicyStoreInput.make
              ?deletionProtection:(Option.map
                                     ~f:Values.DeletionProtection.of_json
                                     deletionProtection) ?description
              ~policyStoreId
              ~validationSettings:(Values.ValidationSettings.of_json
                                     validationSettings) ())
           (Some Values.UpdatePolicyStoreOutput.to_json)
           (Some Values.UpdatePolicyStoreOutput.error_to_json)])
let update_policy_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PolicyTemplateDescription"
       and name =
         flag "name" (optional string) ~doc:"STRING PolicyTemplateName"
       and policyStoreId =
         flag "policy-store-id" (required string) ~doc:"STRING PolicyStoreId"
       and policyTemplateId =
         flag "policy-template-id" (required string)
           ~doc:"STRING PolicyTemplateId"
       and statement =
         flag "statement" (required string) ~doc:"STRING PolicyStatement" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_policy_template
           (Values.UpdatePolicyTemplateInput.make ?description ?name
              ~policyStoreId ~policyTemplateId ~statement ())
           (Some Values.UpdatePolicyTemplateOutput.to_json)
           (Some Values.UpdatePolicyTemplateOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-get-policy", batch_get_policy);
    ("batch-is-authorized", batch_is_authorized);
    ("batch-is-authorized-with-token", batch_is_authorized_with_token);
    ("create-identity-source", create_identity_source);
    ("create-policy", create_policy);
    ("create-policy-store", create_policy_store);
    ("create-policy-store-alias", create_policy_store_alias);
    ("create-policy-template", create_policy_template);
    ("delete-identity-source", delete_identity_source);
    ("delete-policy", delete_policy);
    ("delete-policy-store", delete_policy_store);
    ("delete-policy-store-alias", delete_policy_store_alias);
    ("delete-policy-template", delete_policy_template);
    ("get-identity-source", get_identity_source);
    ("get-policy", get_policy);
    ("get-policy-store", get_policy_store);
    ("get-policy-store-alias", get_policy_store_alias);
    ("get-policy-template", get_policy_template);
    ("get-schema", get_schema);
    ("is-authorized", is_authorized);
    ("is-authorized-with-token", is_authorized_with_token);
    ("list-identity-sources", list_identity_sources);
    ("list-policies", list_policies);
    ("list-policy-store-aliases", list_policy_store_aliases);
    ("list-policy-stores", list_policy_stores);
    ("list-policy-templates", list_policy_templates);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-schema", put_schema);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-identity-source", update_identity_source);
    ("update-policy", update_policy);
    ("update-policy-store", update_policy_store);
    ("update-policy-template", update_policy_template)]
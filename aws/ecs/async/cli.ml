(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_capacity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING String"
       and autoScalingGroupProvider =
         flag "auto-scaling-group-provider" (required json_arg)
           ~doc:"JSON AutoScalingGroupProvider" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_capacity_provider
           (Values.CreateCapacityProviderRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~autoScalingGroupProvider:(Values.AutoScalingGroupProvider.of_json
                                           autoScalingGroupProvider) ())
           (Some Values.CreateCapacityProviderResponse.to_json)
           (Some Values.CreateCapacityProviderResponse.error_to_json)])
let create_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterName =
         flag "cluster-name" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and settings =
         flag "settings" (optional json_arg) ~doc:"JSON ClusterSettings"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON ClusterConfiguration"
       and capacityProviders =
         flag "capacity-providers" (optional json_arg) ~doc:"JSON StringList"
       and defaultCapacityProviderStrategy =
         flag "default-capacity-provider-strategy" (optional json_arg)
           ~doc:"JSON CapacityProviderStrategy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster
           (Values.CreateClusterRequest.make ?clusterName
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?settings:(Option.map ~f:Values.ClusterSettings.of_json
                           settings)
              ?configuration:(Option.map
                                ~f:Values.ClusterConfiguration.of_json
                                configuration)
              ?capacityProviders:(Option.map ~f:Values.StringList.of_json
                                    capacityProviders)
              ?defaultCapacityProviderStrategy:(Option.map
                                                  ~f:Values.CapacityProviderStrategy.of_json
                                                  defaultCapacityProviderStrategy)
              ()) (Some Values.CreateClusterResponse.to_json)
           (Some Values.CreateClusterResponse.error_to_json)])
let create_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and taskDefinition =
         flag "task-definition" (optional string) ~doc:"STRING String"
       and loadBalancers =
         flag "load-balancers" (optional json_arg) ~doc:"JSON LoadBalancers"
       and serviceRegistries =
         flag "service-registries" (optional json_arg)
           ~doc:"JSON ServiceRegistries"
       and desiredCount =
         flag "desired-count" (optional int) ~doc:"INT BoxedInteger"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and launchType =
         flag "launch-type" (optional json_arg) ~doc:"JSON LaunchType"
       and capacityProviderStrategy =
         flag "capacity-provider-strategy" (optional json_arg)
           ~doc:"JSON CapacityProviderStrategy"
       and platformVersion =
         flag "platform-version" (optional string) ~doc:"STRING String"
       and role = flag "role" (optional string) ~doc:"STRING String"
       and deploymentConfiguration =
         flag "deployment-configuration" (optional json_arg)
           ~doc:"JSON DeploymentConfiguration"
       and placementConstraints =
         flag "placement-constraints" (optional json_arg)
           ~doc:"JSON PlacementConstraints"
       and placementStrategy =
         flag "placement-strategy" (optional json_arg)
           ~doc:"JSON PlacementStrategies"
       and networkConfiguration =
         flag "network-configuration" (optional json_arg)
           ~doc:"JSON NetworkConfiguration"
       and healthCheckGracePeriodSeconds =
         flag "health-check-grace-period-seconds" (optional int)
           ~doc:"INT BoxedInteger"
       and schedulingStrategy =
         flag "scheduling-strategy" (optional json_arg)
           ~doc:"JSON SchedulingStrategy"
       and deploymentController =
         flag "deployment-controller" (optional json_arg)
           ~doc:"JSON DeploymentController"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and enableECSManagedTags =
         flag "enable-e-c-s-managed-tags" (optional bool) ~doc:"BOOL Boolean"
       and propagateTags =
         flag "propagate-tags" (optional json_arg) ~doc:"JSON PropagateTags"
       and enableExecuteCommand =
         flag "enable-execute-command" (optional bool) ~doc:"BOOL Boolean"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service
           (Values.CreateServiceRequest.make ?cluster ?taskDefinition
              ?loadBalancers:(Option.map ~f:Values.LoadBalancers.of_json
                                loadBalancers)
              ?serviceRegistries:(Option.map
                                    ~f:Values.ServiceRegistries.of_json
                                    serviceRegistries) ?desiredCount
              ?clientToken
              ?launchType:(Option.map ~f:Values.LaunchType.of_json launchType)
              ?capacityProviderStrategy:(Option.map
                                           ~f:Values.CapacityProviderStrategy.of_json
                                           capacityProviderStrategy)
              ?platformVersion ?role
              ?deploymentConfiguration:(Option.map
                                          ~f:Values.DeploymentConfiguration.of_json
                                          deploymentConfiguration)
              ?placementConstraints:(Option.map
                                       ~f:Values.PlacementConstraints.of_json
                                       placementConstraints)
              ?placementStrategy:(Option.map
                                    ~f:Values.PlacementStrategies.of_json
                                    placementStrategy)
              ?networkConfiguration:(Option.map
                                       ~f:Values.NetworkConfiguration.of_json
                                       networkConfiguration)
              ?healthCheckGracePeriodSeconds
              ?schedulingStrategy:(Option.map
                                     ~f:Values.SchedulingStrategy.of_json
                                     schedulingStrategy)
              ?deploymentController:(Option.map
                                       ~f:Values.DeploymentController.of_json
                                       deploymentController)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?enableECSManagedTags
              ?propagateTags:(Option.map ~f:Values.PropagateTags.of_json
                                propagateTags) ?enableExecuteCommand
              ~serviceName ()) (Some Values.CreateServiceResponse.to_json)
           (Some Values.CreateServiceResponse.error_to_json)])
let create_task_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and externalId =
         flag "external-id" (optional string) ~doc:"STRING String"
       and networkConfiguration =
         flag "network-configuration" (optional json_arg)
           ~doc:"JSON NetworkConfiguration"
       and loadBalancers =
         flag "load-balancers" (optional json_arg) ~doc:"JSON LoadBalancers"
       and serviceRegistries =
         flag "service-registries" (optional json_arg)
           ~doc:"JSON ServiceRegistries"
       and launchType =
         flag "launch-type" (optional json_arg) ~doc:"JSON LaunchType"
       and capacityProviderStrategy =
         flag "capacity-provider-strategy" (optional json_arg)
           ~doc:"JSON CapacityProviderStrategy"
       and platformVersion =
         flag "platform-version" (optional string) ~doc:"STRING String"
       and scale = flag "scale" (optional json_arg) ~doc:"JSON Scale"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and service = flag "service" (required string) ~doc:"STRING String"
       and cluster = flag "cluster" (required string) ~doc:"STRING String"
       and taskDefinition =
         flag "task-definition" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_task_set
           (Values.CreateTaskSetRequest.make ?externalId
              ?networkConfiguration:(Option.map
                                       ~f:Values.NetworkConfiguration.of_json
                                       networkConfiguration)
              ?loadBalancers:(Option.map ~f:Values.LoadBalancers.of_json
                                loadBalancers)
              ?serviceRegistries:(Option.map
                                    ~f:Values.ServiceRegistries.of_json
                                    serviceRegistries)
              ?launchType:(Option.map ~f:Values.LaunchType.of_json launchType)
              ?capacityProviderStrategy:(Option.map
                                           ~f:Values.CapacityProviderStrategy.of_json
                                           capacityProviderStrategy)
              ?platformVersion
              ?scale:(Option.map ~f:Values.Scale.of_json scale) ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~service
              ~cluster ~taskDefinition ())
           (Some Values.CreateTaskSetResponse.to_json)
           (Some Values.CreateTaskSetResponse.error_to_json)])
let delete_account_setting =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and principalArn =
         flag "principal-arn" (optional string) ~doc:"STRING String"
       and name = flag "name" (required json_arg) ~doc:"JSON SettingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_account_setting
           (Values.DeleteAccountSettingRequest.make ?principalArn
              ~name:(Values.SettingName.of_json name) ())
           (Some Values.DeleteAccountSettingResponse.to_json)
           (Some Values.DeleteAccountSettingResponse.error_to_json)])
let delete_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and attributes =
         flag "attributes" (required json_arg) ~doc:"JSON Attributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_attributes
           (Values.DeleteAttributesRequest.make ?cluster
              ~attributes:(Values.Attributes.of_json attributes) ())
           (Some Values.DeleteAttributesResponse.to_json)
           (Some Values.DeleteAttributesResponse.error_to_json)])
let delete_capacity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityProvider =
         flag "capacity-provider" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_capacity_provider
           (Values.DeleteCapacityProviderRequest.make ~capacityProvider ())
           (Some Values.DeleteCapacityProviderResponse.to_json)
           (Some Values.DeleteCapacityProviderResponse.error_to_json)])
let delete_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster (Values.DeleteClusterRequest.make ~cluster ())
           (Some Values.DeleteClusterResponse.to_json)
           (Some Values.DeleteClusterResponse.error_to_json)])
let delete_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and force = flag "force" (optional bool) ~doc:"BOOL BoxedBoolean"
       and service = flag "service" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service
           (Values.DeleteServiceRequest.make ?cluster ?force ~service ())
           (Some Values.DeleteServiceResponse.to_json)
           (Some Values.DeleteServiceResponse.error_to_json)])
let delete_task_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL BoxedBoolean"
       and cluster = flag "cluster" (required string) ~doc:"STRING String"
       and service = flag "service" (required string) ~doc:"STRING String"
       and taskSet = flag "task-set" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_task_set
           (Values.DeleteTaskSetRequest.make ?force ~cluster ~service
              ~taskSet ()) (Some Values.DeleteTaskSetResponse.to_json)
           (Some Values.DeleteTaskSetResponse.error_to_json)])
let deregister_container_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and force = flag "force" (optional bool) ~doc:"BOOL BoxedBoolean"
       and containerInstance =
         flag "container-instance" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_container_instance
           (Values.DeregisterContainerInstanceRequest.make ?cluster ?force
              ~containerInstance ())
           (Some Values.DeregisterContainerInstanceResponse.to_json)
           (Some Values.DeregisterContainerInstanceResponse.error_to_json)])
let deregister_task_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskDefinition =
         flag "task-definition" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_task_definition
           (Values.DeregisterTaskDefinitionRequest.make ~taskDefinition ())
           (Some Values.DeregisterTaskDefinitionResponse.to_json)
           (Some Values.DeregisterTaskDefinitionResponse.error_to_json)])
let describe_capacity_providers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityProviders =
         flag "capacity-providers" (optional json_arg) ~doc:"JSON StringList"
       and include_ =
         flag "include-" (optional json_arg)
           ~doc:"JSON CapacityProviderFieldList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxedInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_capacity_providers
           (Values.DescribeCapacityProvidersRequest.make
              ?capacityProviders:(Option.map ~f:Values.StringList.of_json
                                    capacityProviders)
              ?include_:(Option.map
                           ~f:Values.CapacityProviderFieldList.of_json
                           include_) ?maxResults ?nextToken ())
           (Some Values.DescribeCapacityProvidersResponse.to_json)
           (Some Values.DescribeCapacityProvidersResponse.error_to_json)])
let describe_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusters =
         flag "clusters" (optional json_arg) ~doc:"JSON StringList"
       and include_ =
         flag "include-" (optional json_arg) ~doc:"JSON ClusterFieldList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_clusters
           (Values.DescribeClustersRequest.make
              ?clusters:(Option.map ~f:Values.StringList.of_json clusters)
              ?include_:(Option.map ~f:Values.ClusterFieldList.of_json
                           include_) ())
           (Some Values.DescribeClustersResponse.to_json)
           (Some Values.DescribeClustersResponse.error_to_json)])
let describe_container_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and include_ =
         flag "include-" (optional json_arg)
           ~doc:"JSON ContainerInstanceFieldList"
       and containerInstances =
         flag "container-instances" (required json_arg)
           ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_container_instances
           (Values.DescribeContainerInstancesRequest.make ?cluster
              ?include_:(Option.map
                           ~f:Values.ContainerInstanceFieldList.of_json
                           include_)
              ~containerInstances:(Values.StringList.of_json
                                     containerInstances) ())
           (Some Values.DescribeContainerInstancesResponse.to_json)
           (Some Values.DescribeContainerInstancesResponse.error_to_json)])
let describe_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and include_ =
         flag "include-" (optional json_arg) ~doc:"JSON ServiceFieldList"
       and services =
         flag "services" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_services
           (Values.DescribeServicesRequest.make ?cluster
              ?include_:(Option.map ~f:Values.ServiceFieldList.of_json
                           include_)
              ~services:(Values.StringList.of_json services) ())
           (Some Values.DescribeServicesResponse.to_json)
           (Some Values.DescribeServicesResponse.error_to_json)])
let describe_task_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and include_ =
         flag "include-" (optional json_arg)
           ~doc:"JSON TaskDefinitionFieldList"
       and taskDefinition =
         flag "task-definition" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_task_definition
           (Values.DescribeTaskDefinitionRequest.make
              ?include_:(Option.map ~f:Values.TaskDefinitionFieldList.of_json
                           include_) ~taskDefinition ())
           (Some Values.DescribeTaskDefinitionResponse.to_json)
           (Some Values.DescribeTaskDefinitionResponse.error_to_json)])
let describe_task_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskSets =
         flag "task-sets" (optional json_arg) ~doc:"JSON StringList"
       and include_ =
         flag "include-" (optional json_arg) ~doc:"JSON TaskSetFieldList"
       and cluster = flag "cluster" (required string) ~doc:"STRING String"
       and service = flag "service" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_task_sets
           (Values.DescribeTaskSetsRequest.make
              ?taskSets:(Option.map ~f:Values.StringList.of_json taskSets)
              ?include_:(Option.map ~f:Values.TaskSetFieldList.of_json
                           include_) ~cluster ~service ())
           (Some Values.DescribeTaskSetsResponse.to_json)
           (Some Values.DescribeTaskSetsResponse.error_to_json)])
let describe_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and include_ =
         flag "include-" (optional json_arg) ~doc:"JSON TaskFieldList"
       and tasks = flag "tasks" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_tasks
           (Values.DescribeTasksRequest.make ?cluster
              ?include_:(Option.map ~f:Values.TaskFieldList.of_json include_)
              ~tasks:(Values.StringList.of_json tasks) ())
           (Some Values.DescribeTasksResponse.to_json)
           (Some Values.DescribeTasksResponse.error_to_json)])
let discover_poll_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerInstance =
         flag "container-instance" (optional string) ~doc:"STRING String"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.discover_poll_endpoint
           (Values.DiscoverPollEndpointRequest.make ?containerInstance
              ?cluster ()) (Some Values.DiscoverPollEndpointResponse.to_json)
           (Some Values.DiscoverPollEndpointResponse.error_to_json)])
let execute_command =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and container =
         flag "container" (optional string) ~doc:"STRING String"
       and command = flag "command" (required string) ~doc:"STRING String"
       and interactive =
         flag "interactive" (required bool) ~doc:"BOOL Boolean"
       and task = flag "task" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_command
           (Values.ExecuteCommandRequest.make ?cluster ?container ~command
              ~interactive ~task ())
           (Some Values.ExecuteCommandResponse.to_json)
           (Some Values.ExecuteCommandResponse.error_to_json)])
let list_account_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional json_arg) ~doc:"JSON SettingName"
       and value = flag "value" (optional string) ~doc:"STRING String"
       and principalArn =
         flag "principal-arn" (optional string) ~doc:"STRING String"
       and effectiveSettings =
         flag "effective-settings" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_account_settings
           (Values.ListAccountSettingsRequest.make
              ?name:(Option.map ~f:Values.SettingName.of_json name) ?value
              ?principalArn ?effectiveSettings ?nextToken ?maxResults ())
           (Some Values.ListAccountSettingsResponse.to_json)
           (Some Values.ListAccountSettingsResponse.error_to_json)])
let list_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and attributeName =
         flag "attribute-name" (optional string) ~doc:"STRING String"
       and attributeValue =
         flag "attribute-value" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxedInteger"
       and targetType =
         flag "target-type" (required json_arg) ~doc:"JSON TargetType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attributes
           (Values.ListAttributesRequest.make ?cluster ?attributeName
              ?attributeValue ?nextToken ?maxResults
              ~targetType:(Values.TargetType.of_json targetType) ())
           (Some Values.ListAttributesResponse.to_json)
           (Some Values.ListAttributesResponse.error_to_json)])
let list_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxedInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_clusters
           (Values.ListClustersRequest.make ?nextToken ?maxResults ())
           (Some Values.ListClustersResponse.to_json)
           (Some Values.ListClustersResponse.error_to_json)])
let list_container_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and filter = flag "filter" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxedInteger"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON ContainerInstanceStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_container_instances
           (Values.ListContainerInstancesRequest.make ?cluster ?filter
              ?nextToken ?maxResults
              ?status:(Option.map ~f:Values.ContainerInstanceStatus.of_json
                         status) ())
           (Some Values.ListContainerInstancesResponse.to_json)
           (Some Values.ListContainerInstancesResponse.error_to_json)])
let list_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxedInteger"
       and launchType =
         flag "launch-type" (optional json_arg) ~doc:"JSON LaunchType"
       and schedulingStrategy =
         flag "scheduling-strategy" (optional json_arg)
           ~doc:"JSON SchedulingStrategy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_services
           (Values.ListServicesRequest.make ?cluster ?nextToken ?maxResults
              ?launchType:(Option.map ~f:Values.LaunchType.of_json launchType)
              ?schedulingStrategy:(Option.map
                                     ~f:Values.SchedulingStrategy.of_json
                                     schedulingStrategy) ())
           (Some Values.ListServicesResponse.to_json)
           (Some Values.ListServicesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_task_definition_families =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and familyPrefix =
         flag "family-prefix" (optional string) ~doc:"STRING String"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON TaskDefinitionFamilyStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxedInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_task_definition_families
           (Values.ListTaskDefinitionFamiliesRequest.make ?familyPrefix
              ?status:(Option.map
                         ~f:Values.TaskDefinitionFamilyStatus.of_json status)
              ?nextToken ?maxResults ())
           (Some Values.ListTaskDefinitionFamiliesResponse.to_json)
           (Some Values.ListTaskDefinitionFamiliesResponse.error_to_json)])
let list_task_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and familyPrefix =
         flag "family-prefix" (optional string) ~doc:"STRING String"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON TaskDefinitionStatus"
       and sort = flag "sort" (optional json_arg) ~doc:"JSON SortOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxedInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_task_definitions
           (Values.ListTaskDefinitionsRequest.make ?familyPrefix
              ?status:(Option.map ~f:Values.TaskDefinitionStatus.of_json
                         status)
              ?sort:(Option.map ~f:Values.SortOrder.of_json sort) ?nextToken
              ?maxResults ())
           (Some Values.ListTaskDefinitionsResponse.to_json)
           (Some Values.ListTaskDefinitionsResponse.error_to_json)])
let list_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and containerInstance =
         flag "container-instance" (optional string) ~doc:"STRING String"
       and family = flag "family" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxedInteger"
       and startedBy =
         flag "started-by" (optional string) ~doc:"STRING String"
       and serviceName =
         flag "service-name" (optional string) ~doc:"STRING String"
       and desiredStatus =
         flag "desired-status" (optional json_arg) ~doc:"JSON DesiredStatus"
       and launchType =
         flag "launch-type" (optional json_arg) ~doc:"JSON LaunchType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tasks
           (Values.ListTasksRequest.make ?cluster ?containerInstance ?family
              ?nextToken ?maxResults ?startedBy ?serviceName
              ?desiredStatus:(Option.map ~f:Values.DesiredStatus.of_json
                                desiredStatus)
              ?launchType:(Option.map ~f:Values.LaunchType.of_json launchType)
              ()) (Some Values.ListTasksResponse.to_json)
           (Some Values.ListTasksResponse.error_to_json)])
let put_account_setting =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and principalArn =
         flag "principal-arn" (optional string) ~doc:"STRING String"
       and name = flag "name" (required json_arg) ~doc:"JSON SettingName"
       and value = flag "value" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_account_setting
           (Values.PutAccountSettingRequest.make ?principalArn
              ~name:(Values.SettingName.of_json name) ~value ())
           (Some Values.PutAccountSettingResponse.to_json)
           (Some Values.PutAccountSettingResponse.error_to_json)])
let put_account_setting_default =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required json_arg) ~doc:"JSON SettingName"
       and value = flag "value" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_account_setting_default
           (Values.PutAccountSettingDefaultRequest.make
              ~name:(Values.SettingName.of_json name) ~value ())
           (Some Values.PutAccountSettingDefaultResponse.to_json)
           (Some Values.PutAccountSettingDefaultResponse.error_to_json)])
let put_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and attributes =
         flag "attributes" (required json_arg) ~doc:"JSON Attributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_attributes
           (Values.PutAttributesRequest.make ?cluster
              ~attributes:(Values.Attributes.of_json attributes) ())
           (Some Values.PutAttributesResponse.to_json)
           (Some Values.PutAttributesResponse.error_to_json)])
let put_cluster_capacity_providers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (required string) ~doc:"STRING String"
       and capacityProviders =
         flag "capacity-providers" (required json_arg) ~doc:"JSON StringList"
       and defaultCapacityProviderStrategy =
         flag "default-capacity-provider-strategy" (required json_arg)
           ~doc:"JSON CapacityProviderStrategy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_cluster_capacity_providers
           (Values.PutClusterCapacityProvidersRequest.make ~cluster
              ~capacityProviders:(Values.StringList.of_json capacityProviders)
              ~defaultCapacityProviderStrategy:(Values.CapacityProviderStrategy.of_json
                                                  defaultCapacityProviderStrategy)
              ()) (Some Values.PutClusterCapacityProvidersResponse.to_json)
           (Some Values.PutClusterCapacityProvidersResponse.error_to_json)])
let register_container_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and instanceIdentityDocument =
         flag "instance-identity-document" (optional string)
           ~doc:"STRING String"
       and instanceIdentityDocumentSignature =
         flag "instance-identity-document-signature" (optional string)
           ~doc:"STRING String"
       and totalResources =
         flag "total-resources" (optional json_arg) ~doc:"JSON Resources"
       and versionInfo =
         flag "version-info" (optional json_arg) ~doc:"JSON VersionInfo"
       and containerInstanceArn =
         flag "container-instance-arn" (optional string) ~doc:"STRING String"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON Attributes"
       and platformDevices =
         flag "platform-devices" (optional json_arg)
           ~doc:"JSON PlatformDevices"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_container_instance
           (Values.RegisterContainerInstanceRequest.make ?cluster
              ?instanceIdentityDocument ?instanceIdentityDocumentSignature
              ?totalResources:(Option.map ~f:Values.Resources.of_json
                                 totalResources)
              ?versionInfo:(Option.map ~f:Values.VersionInfo.of_json
                              versionInfo) ?containerInstanceArn
              ?attributes:(Option.map ~f:Values.Attributes.of_json attributes)
              ?platformDevices:(Option.map ~f:Values.PlatformDevices.of_json
                                  platformDevices)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ())
           (Some Values.RegisterContainerInstanceResponse.to_json)
           (Some Values.RegisterContainerInstanceResponse.error_to_json)])
let register_task_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskRoleArn =
         flag "task-role-arn" (optional string) ~doc:"STRING String"
       and executionRoleArn =
         flag "execution-role-arn" (optional string) ~doc:"STRING String"
       and networkMode =
         flag "network-mode" (optional json_arg) ~doc:"JSON NetworkMode"
       and volumes =
         flag "volumes" (optional json_arg) ~doc:"JSON VolumeList"
       and placementConstraints =
         flag "placement-constraints" (optional json_arg)
           ~doc:"JSON TaskDefinitionPlacementConstraints"
       and requiresCompatibilities =
         flag "requires-compatibilities" (optional json_arg)
           ~doc:"JSON CompatibilityList"
       and cpu = flag "cpu" (optional string) ~doc:"STRING String"
       and memory = flag "memory" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and pidMode = flag "pid-mode" (optional json_arg) ~doc:"JSON PidMode"
       and ipcMode = flag "ipc-mode" (optional json_arg) ~doc:"JSON IpcMode"
       and proxyConfiguration =
         flag "proxy-configuration" (optional json_arg)
           ~doc:"JSON ProxyConfiguration"
       and inferenceAccelerators =
         flag "inference-accelerators" (optional json_arg)
           ~doc:"JSON InferenceAccelerators"
       and ephemeralStorage =
         flag "ephemeral-storage" (optional json_arg)
           ~doc:"JSON EphemeralStorage"
       and runtimePlatform =
         flag "runtime-platform" (optional json_arg)
           ~doc:"JSON RuntimePlatform"
       and family = flag "family" (required string) ~doc:"STRING String"
       and containerDefinitions =
         flag "container-definitions" (required json_arg)
           ~doc:"JSON ContainerDefinitions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_task_definition
           (Values.RegisterTaskDefinitionRequest.make ?taskRoleArn
              ?executionRoleArn
              ?networkMode:(Option.map ~f:Values.NetworkMode.of_json
                              networkMode)
              ?volumes:(Option.map ~f:Values.VolumeList.of_json volumes)
              ?placementConstraints:(Option.map
                                       ~f:Values.TaskDefinitionPlacementConstraints.of_json
                                       placementConstraints)
              ?requiresCompatibilities:(Option.map
                                          ~f:Values.CompatibilityList.of_json
                                          requiresCompatibilities) ?cpu
              ?memory ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?pidMode:(Option.map ~f:Values.PidMode.of_json pidMode)
              ?ipcMode:(Option.map ~f:Values.IpcMode.of_json ipcMode)
              ?proxyConfiguration:(Option.map
                                     ~f:Values.ProxyConfiguration.of_json
                                     proxyConfiguration)
              ?inferenceAccelerators:(Option.map
                                        ~f:Values.InferenceAccelerators.of_json
                                        inferenceAccelerators)
              ?ephemeralStorage:(Option.map
                                   ~f:Values.EphemeralStorage.of_json
                                   ephemeralStorage)
              ?runtimePlatform:(Option.map ~f:Values.RuntimePlatform.of_json
                                  runtimePlatform) ~family
              ~containerDefinitions:(Values.ContainerDefinitions.of_json
                                       containerDefinitions) ())
           (Some Values.RegisterTaskDefinitionResponse.to_json)
           (Some Values.RegisterTaskDefinitionResponse.error_to_json)])
let run_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityProviderStrategy =
         flag "capacity-provider-strategy" (optional json_arg)
           ~doc:"JSON CapacityProviderStrategy"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and count = flag "count" (optional int) ~doc:"INT BoxedInteger"
       and enableECSManagedTags =
         flag "enable-e-c-s-managed-tags" (optional bool) ~doc:"BOOL Boolean"
       and enableExecuteCommand =
         flag "enable-execute-command" (optional bool) ~doc:"BOOL Boolean"
       and group = flag "group" (optional string) ~doc:"STRING String"
       and launchType =
         flag "launch-type" (optional json_arg) ~doc:"JSON LaunchType"
       and networkConfiguration =
         flag "network-configuration" (optional json_arg)
           ~doc:"JSON NetworkConfiguration"
       and overrides =
         flag "overrides" (optional json_arg) ~doc:"JSON TaskOverride"
       and placementConstraints =
         flag "placement-constraints" (optional json_arg)
           ~doc:"JSON PlacementConstraints"
       and placementStrategy =
         flag "placement-strategy" (optional json_arg)
           ~doc:"JSON PlacementStrategies"
       and platformVersion =
         flag "platform-version" (optional string) ~doc:"STRING String"
       and propagateTags =
         flag "propagate-tags" (optional json_arg) ~doc:"JSON PropagateTags"
       and referenceId =
         flag "reference-id" (optional string) ~doc:"STRING String"
       and startedBy =
         flag "started-by" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and taskDefinition =
         flag "task-definition" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.run_task
           (Values.RunTaskRequest.make
              ?capacityProviderStrategy:(Option.map
                                           ~f:Values.CapacityProviderStrategy.of_json
                                           capacityProviderStrategy) ?cluster
              ?count ?enableECSManagedTags ?enableExecuteCommand ?group
              ?launchType:(Option.map ~f:Values.LaunchType.of_json launchType)
              ?networkConfiguration:(Option.map
                                       ~f:Values.NetworkConfiguration.of_json
                                       networkConfiguration)
              ?overrides:(Option.map ~f:Values.TaskOverride.of_json overrides)
              ?placementConstraints:(Option.map
                                       ~f:Values.PlacementConstraints.of_json
                                       placementConstraints)
              ?placementStrategy:(Option.map
                                    ~f:Values.PlacementStrategies.of_json
                                    placementStrategy) ?platformVersion
              ?propagateTags:(Option.map ~f:Values.PropagateTags.of_json
                                propagateTags) ?referenceId ?startedBy
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~taskDefinition
              ()) (Some Values.RunTaskResponse.to_json)
           (Some Values.RunTaskResponse.error_to_json)])
let start_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and enableECSManagedTags =
         flag "enable-e-c-s-managed-tags" (optional bool) ~doc:"BOOL Boolean"
       and enableExecuteCommand =
         flag "enable-execute-command" (optional bool) ~doc:"BOOL Boolean"
       and group = flag "group" (optional string) ~doc:"STRING String"
       and networkConfiguration =
         flag "network-configuration" (optional json_arg)
           ~doc:"JSON NetworkConfiguration"
       and overrides =
         flag "overrides" (optional json_arg) ~doc:"JSON TaskOverride"
       and propagateTags =
         flag "propagate-tags" (optional json_arg) ~doc:"JSON PropagateTags"
       and referenceId =
         flag "reference-id" (optional string) ~doc:"STRING String"
       and startedBy =
         flag "started-by" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and containerInstances =
         flag "container-instances" (required json_arg)
           ~doc:"JSON StringList"
       and taskDefinition =
         flag "task-definition" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_task
           (Values.StartTaskRequest.make ?cluster ?enableECSManagedTags
              ?enableExecuteCommand ?group
              ?networkConfiguration:(Option.map
                                       ~f:Values.NetworkConfiguration.of_json
                                       networkConfiguration)
              ?overrides:(Option.map ~f:Values.TaskOverride.of_json overrides)
              ?propagateTags:(Option.map ~f:Values.PropagateTags.of_json
                                propagateTags) ?referenceId ?startedBy
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~containerInstances:(Values.StringList.of_json
                                     containerInstances) ~taskDefinition ())
           (Some Values.StartTaskResponse.to_json)
           (Some Values.StartTaskResponse.error_to_json)])
let stop_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and reason = flag "reason" (optional string) ~doc:"STRING String"
       and task = flag "task" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_task
           (Values.StopTaskRequest.make ?cluster ?reason ~task ())
           (Some Values.StopTaskResponse.to_json)
           (Some Values.StopTaskResponse.error_to_json)])
let submit_attachment_state_changes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and attachments =
         flag "attachments" (required json_arg)
           ~doc:"JSON AttachmentStateChanges" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.submit_attachment_state_changes
           (Values.SubmitAttachmentStateChangesRequest.make ?cluster
              ~attachments:(Values.AttachmentStateChanges.of_json attachments)
              ()) (Some Values.SubmitAttachmentStateChangesResponse.to_json)
           (Some Values.SubmitAttachmentStateChangesResponse.error_to_json)])
let submit_container_state_change =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and task = flag "task" (optional string) ~doc:"STRING String"
       and containerName =
         flag "container-name" (optional string) ~doc:"STRING String"
       and runtimeId =
         flag "runtime-id" (optional string) ~doc:"STRING String"
       and status = flag "status" (optional string) ~doc:"STRING String"
       and exitCode = flag "exit-code" (optional int) ~doc:"INT BoxedInteger"
       and reason = flag "reason" (optional string) ~doc:"STRING String"
       and networkBindings =
         flag "network-bindings" (optional json_arg)
           ~doc:"JSON NetworkBindings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.submit_container_state_change
           (Values.SubmitContainerStateChangeRequest.make ?cluster ?task
              ?containerName ?runtimeId ?status ?exitCode ?reason
              ?networkBindings:(Option.map ~f:Values.NetworkBindings.of_json
                                  networkBindings) ())
           (Some Values.SubmitContainerStateChangeResponse.to_json)
           (Some Values.SubmitContainerStateChangeResponse.error_to_json)])
let submit_task_state_change =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and task = flag "task" (optional string) ~doc:"STRING String"
       and status = flag "status" (optional string) ~doc:"STRING String"
       and reason = flag "reason" (optional string) ~doc:"STRING String"
       and containers =
         flag "containers" (optional json_arg)
           ~doc:"JSON ContainerStateChanges"
       and attachments =
         flag "attachments" (optional json_arg)
           ~doc:"JSON AttachmentStateChanges"
       and managedAgents =
         flag "managed-agents" (optional json_arg)
           ~doc:"JSON ManagedAgentStateChanges"
       and pullStartedAt =
         flag "pull-started-at" (optional json_arg) ~doc:"JSON Timestamp"
       and pullStoppedAt =
         flag "pull-stopped-at" (optional json_arg) ~doc:"JSON Timestamp"
       and executionStoppedAt =
         flag "execution-stopped-at" (optional json_arg)
           ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.submit_task_state_change
           (Values.SubmitTaskStateChangeRequest.make ?cluster ?task ?status
              ?reason
              ?containers:(Option.map ~f:Values.ContainerStateChanges.of_json
                             containers)
              ?attachments:(Option.map
                              ~f:Values.AttachmentStateChanges.of_json
                              attachments)
              ?managedAgents:(Option.map
                                ~f:Values.ManagedAgentStateChanges.of_json
                                managedAgents)
              ?pullStartedAt:(Option.map ~f:Values.Timestamp.of_json
                                pullStartedAt)
              ?pullStoppedAt:(Option.map ~f:Values.Timestamp.of_json
                                pullStoppedAt)
              ?executionStoppedAt:(Option.map ~f:Values.Timestamp.of_json
                                     executionStoppedAt) ())
           (Some Values.SubmitTaskStateChangeResponse.to_json)
           (Some Values.SubmitTaskStateChangeResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_capacity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String"
       and autoScalingGroupProvider =
         flag "auto-scaling-group-provider" (required json_arg)
           ~doc:"JSON AutoScalingGroupProviderUpdate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_capacity_provider
           (Values.UpdateCapacityProviderRequest.make ~name
              ~autoScalingGroupProvider:(Values.AutoScalingGroupProviderUpdate.of_json
                                           autoScalingGroupProvider) ())
           (Some Values.UpdateCapacityProviderResponse.to_json)
           (Some Values.UpdateCapacityProviderResponse.error_to_json)])
let update_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and settings =
         flag "settings" (optional json_arg) ~doc:"JSON ClusterSettings"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON ClusterConfiguration"
       and cluster = flag "cluster" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cluster
           (Values.UpdateClusterRequest.make
              ?settings:(Option.map ~f:Values.ClusterSettings.of_json
                           settings)
              ?configuration:(Option.map
                                ~f:Values.ClusterConfiguration.of_json
                                configuration) ~cluster ())
           (Some Values.UpdateClusterResponse.to_json)
           (Some Values.UpdateClusterResponse.error_to_json)])
let update_cluster_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (required string) ~doc:"STRING String"
       and settings =
         flag "settings" (required json_arg) ~doc:"JSON ClusterSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cluster_settings
           (Values.UpdateClusterSettingsRequest.make ~cluster
              ~settings:(Values.ClusterSettings.of_json settings) ())
           (Some Values.UpdateClusterSettingsResponse.to_json)
           (Some Values.UpdateClusterSettingsResponse.error_to_json)])
let update_container_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and containerInstance =
         flag "container-instance" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_container_agent
           (Values.UpdateContainerAgentRequest.make ?cluster
              ~containerInstance ())
           (Some Values.UpdateContainerAgentResponse.to_json)
           (Some Values.UpdateContainerAgentResponse.error_to_json)])
let update_container_instances_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and containerInstances =
         flag "container-instances" (required json_arg)
           ~doc:"JSON StringList"
       and status =
         flag "status" (required json_arg)
           ~doc:"JSON ContainerInstanceStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_container_instances_state
           (Values.UpdateContainerInstancesStateRequest.make ?cluster
              ~containerInstances:(Values.StringList.of_json
                                     containerInstances)
              ~status:(Values.ContainerInstanceStatus.of_json status) ())
           (Some Values.UpdateContainerInstancesStateResponse.to_json)
           (Some Values.UpdateContainerInstancesStateResponse.error_to_json)])
let update_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (optional string) ~doc:"STRING String"
       and desiredCount =
         flag "desired-count" (optional int) ~doc:"INT BoxedInteger"
       and taskDefinition =
         flag "task-definition" (optional string) ~doc:"STRING String"
       and capacityProviderStrategy =
         flag "capacity-provider-strategy" (optional json_arg)
           ~doc:"JSON CapacityProviderStrategy"
       and deploymentConfiguration =
         flag "deployment-configuration" (optional json_arg)
           ~doc:"JSON DeploymentConfiguration"
       and networkConfiguration =
         flag "network-configuration" (optional json_arg)
           ~doc:"JSON NetworkConfiguration"
       and placementConstraints =
         flag "placement-constraints" (optional json_arg)
           ~doc:"JSON PlacementConstraints"
       and placementStrategy =
         flag "placement-strategy" (optional json_arg)
           ~doc:"JSON PlacementStrategies"
       and platformVersion =
         flag "platform-version" (optional string) ~doc:"STRING String"
       and forceNewDeployment =
         flag "force-new-deployment" (optional bool) ~doc:"BOOL Boolean"
       and healthCheckGracePeriodSeconds =
         flag "health-check-grace-period-seconds" (optional int)
           ~doc:"INT BoxedInteger"
       and enableExecuteCommand =
         flag "enable-execute-command" (optional bool)
           ~doc:"BOOL BoxedBoolean"
       and enableECSManagedTags =
         flag "enable-e-c-s-managed-tags" (optional bool)
           ~doc:"BOOL BoxedBoolean"
       and loadBalancers =
         flag "load-balancers" (optional json_arg) ~doc:"JSON LoadBalancers"
       and propagateTags =
         flag "propagate-tags" (optional json_arg) ~doc:"JSON PropagateTags"
       and serviceRegistries =
         flag "service-registries" (optional json_arg)
           ~doc:"JSON ServiceRegistries"
       and service = flag "service" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service
           (Values.UpdateServiceRequest.make ?cluster ?desiredCount
              ?taskDefinition
              ?capacityProviderStrategy:(Option.map
                                           ~f:Values.CapacityProviderStrategy.of_json
                                           capacityProviderStrategy)
              ?deploymentConfiguration:(Option.map
                                          ~f:Values.DeploymentConfiguration.of_json
                                          deploymentConfiguration)
              ?networkConfiguration:(Option.map
                                       ~f:Values.NetworkConfiguration.of_json
                                       networkConfiguration)
              ?placementConstraints:(Option.map
                                       ~f:Values.PlacementConstraints.of_json
                                       placementConstraints)
              ?placementStrategy:(Option.map
                                    ~f:Values.PlacementStrategies.of_json
                                    placementStrategy) ?platformVersion
              ?forceNewDeployment ?healthCheckGracePeriodSeconds
              ?enableExecuteCommand ?enableECSManagedTags
              ?loadBalancers:(Option.map ~f:Values.LoadBalancers.of_json
                                loadBalancers)
              ?propagateTags:(Option.map ~f:Values.PropagateTags.of_json
                                propagateTags)
              ?serviceRegistries:(Option.map
                                    ~f:Values.ServiceRegistries.of_json
                                    serviceRegistries) ~service ())
           (Some Values.UpdateServiceResponse.to_json)
           (Some Values.UpdateServiceResponse.error_to_json)])
let update_service_primary_task_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (required string) ~doc:"STRING String"
       and service = flag "service" (required string) ~doc:"STRING String"
       and primaryTaskSet =
         flag "primary-task-set" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_primary_task_set
           (Values.UpdateServicePrimaryTaskSetRequest.make ~cluster ~service
              ~primaryTaskSet ())
           (Some Values.UpdateServicePrimaryTaskSetResponse.to_json)
           (Some Values.UpdateServicePrimaryTaskSetResponse.error_to_json)])
let update_task_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cluster = flag "cluster" (required string) ~doc:"STRING String"
       and service = flag "service" (required string) ~doc:"STRING String"
       and taskSet = flag "task-set" (required string) ~doc:"STRING String"
       and scale = flag "scale" (required json_arg) ~doc:"JSON Scale" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_task_set
           (Values.UpdateTaskSetRequest.make ~cluster ~service ~taskSet
              ~scale:(Values.Scale.of_json scale) ())
           (Some Values.UpdateTaskSetResponse.to_json)
           (Some Values.UpdateTaskSetResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-capacity-provider", create_capacity_provider);
    ("create-cluster", create_cluster);
    ("create-service", create_service);
    ("create-task-set", create_task_set);
    ("delete-account-setting", delete_account_setting);
    ("delete-attributes", delete_attributes);
    ("delete-capacity-provider", delete_capacity_provider);
    ("delete-cluster", delete_cluster);
    ("delete-service", delete_service);
    ("delete-task-set", delete_task_set);
    ("deregister-container-instance", deregister_container_instance);
    ("deregister-task-definition", deregister_task_definition);
    ("describe-capacity-providers", describe_capacity_providers);
    ("describe-clusters", describe_clusters);
    ("describe-container-instances", describe_container_instances);
    ("describe-services", describe_services);
    ("describe-task-definition", describe_task_definition);
    ("describe-task-sets", describe_task_sets);
    ("describe-tasks", describe_tasks);
    ("discover-poll-endpoint", discover_poll_endpoint);
    ("execute-command", execute_command);
    ("list-account-settings", list_account_settings);
    ("list-attributes", list_attributes);
    ("list-clusters", list_clusters);
    ("list-container-instances", list_container_instances);
    ("list-services", list_services);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-task-definition-families", list_task_definition_families);
    ("list-task-definitions", list_task_definitions);
    ("list-tasks", list_tasks);
    ("put-account-setting", put_account_setting);
    ("put-account-setting-default", put_account_setting_default);
    ("put-attributes", put_attributes);
    ("put-cluster-capacity-providers", put_cluster_capacity_providers);
    ("register-container-instance", register_container_instance);
    ("register-task-definition", register_task_definition);
    ("run-task", run_task);
    ("start-task", start_task);
    ("stop-task", stop_task);
    ("submit-attachment-state-changes", submit_attachment_state_changes);
    ("submit-container-state-change", submit_container_state_change);
    ("submit-task-state-change", submit_task_state_change);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-capacity-provider", update_capacity_provider);
    ("update-cluster", update_cluster);
    ("update-cluster-settings", update_cluster_settings);
    ("update-container-agent", update_container_agent);
    ("update-container-instances-state", update_container_instances_state);
    ("update-service", update_service);
    ("update-service-primary-task-set", update_service_primary_task_set);
    ("update-task-set", update_task_set)]
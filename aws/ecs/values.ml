(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.ecs
let apiVersion = "2014-11-13"
let endpointPrefix = "ecs"
let serviceFullName = "Amazon EC2 Container Service"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "Amazon ECS"
let targetPrefix = "AmazonEC2ContainerServiceV20141113"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module EnvironmentFileType =
  struct
    type nonrec t =
      | S3 
      | Non_static_id of string 
    let make i = i
    let to_string = function | S3 -> "s3" | Non_static_id s -> s
    let of_string = function | "s3" -> S3 | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration EnvironmentFileType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EnvironmentFileType" j)
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module ResourceType =
  struct
    type nonrec t =
      | GPU 
      | InferenceAccelerator 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GPU -> "GPU"
      | InferenceAccelerator -> "InferenceAccelerator"
      | Non_static_id s -> s
    let of_string =
      function
      | "GPU" -> GPU
      | "InferenceAccelerator" -> InferenceAccelerator
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ResourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ResourceType" j)
    let to_json = simple_to_json to_value
  end
module DeviceCgroupPermission =
  struct
    type nonrec t =
      | Read 
      | Write 
      | Mknod 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Read -> "read"
      | Write -> "write"
      | Mknod -> "mknod"
      | Non_static_id s -> s
    let of_string =
      function
      | "read" -> Read
      | "write" -> Write
      | "mknod" -> Mknod
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DeviceCgroupPermission" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"DeviceCgroupPermission" j)
    let to_json = simple_to_json to_value
  end
module ManagedAgentName =
  struct
    type nonrec t =
      | ExecuteCommandAgent 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ExecuteCommandAgent -> "ExecuteCommandAgent"
      | Non_static_id s -> s
    let of_string =
      function
      | "ExecuteCommandAgent" -> ExecuteCommandAgent
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ManagedAgentName" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ManagedAgentName" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module BoxedInteger =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for BoxedInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TransportProtocol =
  struct
    type nonrec t =
      | Tcp 
      | Udp 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Tcp -> "tcp" | Udp -> "udp" | Non_static_id s -> s
    let of_string =
      function | "tcp" -> Tcp | "udp" -> Udp | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration TransportProtocol" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TransportProtocol" j)
    let to_json = simple_to_json to_value
  end
module EnvironmentFile =
  struct
    type nonrec t =
      {
      value: String_.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Amazon S3 object containing the environment variable file."];
      type_: EnvironmentFileType.t
        [@ocaml.doc "The file type to use. The only supported value is s3."]}
    let context_ = "EnvironmentFile"
    let make ~value = fun ~type_ -> fun () -> { value; type_ }
    let to_value x =
      structure_to_value
        [("value", (Some (String_.to_value x.value)));
        ("type", (Some (EnvironmentFileType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        EnvironmentFileType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let value =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "value") in
      make ~type_ ~value ()
    let of_json json =
      let type_ = field_map_exn json "type" EnvironmentFileType.of_json in
      let value = field_map_exn json "value" String_.of_json in
      make ~type_ ~value ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of files containing the environment variables to pass to a container. You can specify up to ten environment files. The file must have a .env file extension. Each line in an environment file should contain an environment variable in VARIABLE=VALUE format. Lines beginning with # are treated as comments and are ignored. For more information about the environment variable file syntax, see Declare default environment variables in file. If there are environment variables specified using the environment parameter in a container definition, they take precedence over the variables contained within an environment file. If multiple environment files are specified that contain the same variable, they're processed from the top down. We recommend that you use unique variable names. For more information, see Specifying environment variables in the Amazon Elastic Container Service Developer Guide. This parameter is only supported for tasks hosted on Fargate using the following platform versions: Linux platform version 1.4.0 or later. Windows platform version 1.0.0 or later."]
module KeyValuePair =
  struct
    type nonrec t =
      {
      name: String_.t option
        [@ocaml.doc
          "The name of the key-value pair. For environment variables, this is the name of the environment variable."];
      value: String_.t option
        [@ocaml.doc
          "The value of the key-value pair. For environment variables, this is the value of the environment variable."]}
    let make ?name = fun ?value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("value", (Option.map x.value ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?value ?name ()
    let of_json json =
      let value = field_map json "value" String_.of_json in
      let name = field_map json "name" String_.of_json in
      make ?value ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A key-value pair object."]
module ResourceRequirement =
  struct
    type nonrec t =
      {
      value: String_.t
        [@ocaml.doc
          "The value for the specified resource type. If the GPU type is used, the value is the number of physical GPUs the Amazon ECS container agent reserves for the container. The number of GPUs that's reserved for all containers in a task can't exceed the number of available GPUs on the container instance that the task is launched on. If the InferenceAccelerator type is used, the value matches the deviceName for an InferenceAccelerator specified in a task definition."];
      type_: ResourceType.t
        [@ocaml.doc
          "The type of resource to assign to a container. The supported values are GPU or InferenceAccelerator."]}
    let context_ = "ResourceRequirement"
    let make ~value = fun ~type_ -> fun () -> { value; type_ }
    let to_value x =
      structure_to_value
        [("value", (Some (String_.to_value x.value)));
        ("type", (Some (ResourceType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        ResourceType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let value =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "value") in
      make ~type_ ~value ()
    let of_json json =
      let type_ = field_map_exn json "type" ResourceType.of_json in
      let value = field_map_exn json "value" String_.of_json in
      make ~type_ ~value ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The type and amount of a resource to assign to a container. The supported resource types are GPUs and Elastic Inference accelerators. For more information, see Working with GPUs on Amazon ECS or Working with Amazon Elastic Inference on Amazon ECS in the Amazon Elastic Container Service Developer Guide"]
module DeviceCgroupPermissions =
  struct
    type nonrec t = DeviceCgroupPermission.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:DeviceCgroupPermission.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DeviceCgroupPermission.of_xml)
    let of_json j =
      list_of_json ~kind:"DeviceCgroupPermissions"
        ~of_json:DeviceCgroupPermission.of_json j
    let to_json v = composed_to_json to_value v
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module StringList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"StringList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module CapacityProviderStrategyItemBase =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100000) >>=
             (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for CapacityProviderStrategyItemBase" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CapacityProviderStrategyItemWeight =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for CapacityProviderStrategyItemWeight" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AssignPublicIp =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AssignPublicIp" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AssignPublicIp" j)
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module InstanceHealthCheckState =
  struct
    type nonrec t =
      | OK 
      | IMPAIRED 
      | INSUFFICIENT_DATA 
      | INITIALIZING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | OK -> "OK"
      | IMPAIRED -> "IMPAIRED"
      | INSUFFICIENT_DATA -> "INSUFFICIENT_DATA"
      | INITIALIZING -> "INITIALIZING"
      | Non_static_id s -> s
    let of_string =
      function
      | "OK" -> OK
      | "IMPAIRED" -> IMPAIRED
      | "INSUFFICIENT_DATA" -> INSUFFICIENT_DATA
      | "INITIALIZING" -> INITIALIZING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceHealthCheckState" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"InstanceHealthCheckState" j)
    let to_json = simple_to_json to_value
  end
module InstanceHealthCheckType =
  struct
    type nonrec t =
      | CONTAINER_RUNTIME 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CONTAINER_RUNTIME -> "CONTAINER_RUNTIME"
      | Non_static_id s -> s
    let of_string =
      function
      | "CONTAINER_RUNTIME" -> CONTAINER_RUNTIME
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceHealthCheckType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"InstanceHealthCheckType" j)
    let to_json = simple_to_json to_value
  end
module ManagedAgent =
  struct
    type nonrec t =
      {
      lastStartedAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the managed agent was last started."];
      name: ManagedAgentName.t option
        [@ocaml.doc
          "The name of the managed agent. When the execute command feature is enabled, the managed agent name is ExecuteCommandAgent."];
      reason: String_.t option
        [@ocaml.doc
          "The reason for why the managed agent is in the state it is in."];
      lastStatus: String_.t option
        [@ocaml.doc "The last known status of the managed agent."]}
    let make ?lastStartedAt =
      fun ?name ->
        fun ?reason ->
          fun ?lastStatus ->
            fun () -> { lastStartedAt; name; reason; lastStatus }
    let to_value x =
      structure_to_value
        [("lastStartedAt",
           (Option.map x.lastStartedAt ~f:Timestamp.to_value));
        ("name", (Option.map x.name ~f:ManagedAgentName.to_value));
        ("reason", (Option.map x.reason ~f:String_.to_value));
        ("lastStatus", (Option.map x.lastStatus ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastStatus =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "lastStatus") in
      let reason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "reason") in
      let name =
        (Option.map ~f:ManagedAgentName.of_xml) (Xml.child xml_arg0 "name") in
      let lastStartedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastStartedAt") in
      make ?lastStatus ?reason ?name ?lastStartedAt ()
    let of_json json =
      let lastStatus = field_map json "lastStatus" String_.of_json in
      let reason = field_map json "reason" String_.of_json in
      let name = field_map json "name" ManagedAgentName.of_json in
      let lastStartedAt = field_map json "lastStartedAt" Timestamp.of_json in
      make ?lastStatus ?reason ?name ?lastStartedAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about the managed agent status for the container."]
module NetworkBinding =
  struct
    type nonrec t =
      {
      bindIP: String_.t option
        [@ocaml.doc
          "The IP address that the container is bound to on the container instance."];
      containerPort: BoxedInteger.t option
        [@ocaml.doc
          "The port number on the container that's used with the network binding."];
      hostPort: BoxedInteger.t option
        [@ocaml.doc
          "The port number on the host that's used with the network binding."];
      protocol: TransportProtocol.t option
        [@ocaml.doc "The protocol used for the network binding."]}
    let make ?bindIP =
      fun ?containerPort ->
        fun ?hostPort ->
          fun ?protocol ->
            fun () -> { bindIP; containerPort; hostPort; protocol }
    let to_value x =
      structure_to_value
        [("bindIP", (Option.map x.bindIP ~f:String_.to_value));
        ("containerPort",
          (Option.map x.containerPort ~f:BoxedInteger.to_value));
        ("hostPort", (Option.map x.hostPort ~f:BoxedInteger.to_value));
        ("protocol", (Option.map x.protocol ~f:TransportProtocol.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protocol =
        (Option.map ~f:TransportProtocol.of_xml)
          (Xml.child xml_arg0 "protocol") in
      let hostPort =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "hostPort") in
      let containerPort =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "containerPort") in
      let bindIP =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "bindIP") in
      make ?protocol ?hostPort ?containerPort ?bindIP ()
    let of_json json =
      let protocol = field_map json "protocol" TransportProtocol.of_json in
      let hostPort = field_map json "hostPort" BoxedInteger.of_json in
      let containerPort = field_map json "containerPort" BoxedInteger.of_json in
      let bindIP = field_map json "bindIP" String_.of_json in
      make ?protocol ?hostPort ?containerPort ?bindIP ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details on the network bindings between a container and its host container instance. After a task reaches the RUNNING status, manual and automatic host and container port assignments are visible in the networkBindings section of DescribeTasks API responses."]
module NetworkInterface =
  struct
    type nonrec t =
      {
      attachmentId: String_.t option
        [@ocaml.doc "The attachment ID for the network interface."];
      privateIpv4Address: String_.t option
        [@ocaml.doc "The private IPv4 address for the network interface."];
      ipv6Address: String_.t option
        [@ocaml.doc "The private IPv6 address for the network interface."]}
    let make ?attachmentId =
      fun ?privateIpv4Address ->
        fun ?ipv6Address ->
          fun () -> { attachmentId; privateIpv4Address; ipv6Address }
    let to_value x =
      structure_to_value
        [("attachmentId", (Option.map x.attachmentId ~f:String_.to_value));
        ("privateIpv4Address",
          (Option.map x.privateIpv4Address ~f:String_.to_value));
        ("ipv6Address", (Option.map x.ipv6Address ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipv6Address =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ipv6Address") in
      let privateIpv4Address =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "privateIpv4Address") in
      let attachmentId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "attachmentId") in
      make ?ipv6Address ?privateIpv4Address ?attachmentId ()
    let of_json json =
      let ipv6Address = field_map json "ipv6Address" String_.of_json in
      let privateIpv4Address =
        field_map json "privateIpv4Address" String_.of_json in
      let attachmentId = field_map json "attachmentId" String_.of_json in
      make ?ipv6Address ?privateIpv4Address ?attachmentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing the elastic network interface for tasks that use the awsvpc network mode."]
module EnvironmentFiles =
  struct
    type nonrec t = EnvironmentFile.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:EnvironmentFile.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EnvironmentFile.of_xml)
    let of_json j =
      list_of_json ~kind:"EnvironmentFiles" ~of_json:EnvironmentFile.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module EnvironmentVariables =
  struct
    type nonrec t = KeyValuePair.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:KeyValuePair.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:KeyValuePair.of_xml)
    let of_json j =
      list_of_json ~kind:"EnvironmentVariables" ~of_json:KeyValuePair.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ResourceRequirements =
  struct
    type nonrec t = ResourceRequirement.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceRequirement.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceRequirement.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceRequirements"
        ~of_json:ResourceRequirement.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerCondition =
  struct
    type nonrec t =
      | START 
      | COMPLETE 
      | SUCCESS 
      | HEALTHY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | START -> "START"
      | COMPLETE -> "COMPLETE"
      | SUCCESS -> "SUCCESS"
      | HEALTHY -> "HEALTHY"
      | Non_static_id s -> s
    let of_string =
      function
      | "START" -> START
      | "COMPLETE" -> COMPLETE
      | "SUCCESS" -> SUCCESS
      | "HEALTHY" -> HEALTHY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ContainerCondition" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ContainerCondition" j)
    let to_json = simple_to_json to_value
  end
module Device =
  struct
    type nonrec t =
      {
      hostPath: String_.t
        [@ocaml.doc
          "The path for the device on the host container instance."];
      containerPath: String_.t option
        [@ocaml.doc
          "The path inside the container at which to expose the host device."];
      permissions: DeviceCgroupPermissions.t option
        [@ocaml.doc
          "The explicit permissions to provide to the container for the device. By default, the container has permissions for read, write, and mknod for the device."]}
    let context_ = "Device"
    let make ?containerPath =
      fun ?permissions ->
        fun ~hostPath -> fun () -> { containerPath; permissions; hostPath }
    let to_value x =
      structure_to_value
        [("hostPath", (Some (String_.to_value x.hostPath)));
        ("containerPath", (Option.map x.containerPath ~f:String_.to_value));
        ("permissions",
          (Option.map x.permissions ~f:DeviceCgroupPermissions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let permissions =
        (Option.map ~f:DeviceCgroupPermissions.of_xml)
          (Xml.child xml_arg0 "permissions") in
      let containerPath =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "containerPath") in
      let hostPath =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "hostPath") in
      make ?permissions ?containerPath ~hostPath ()
    let of_json json =
      let permissions =
        field_map json "permissions" DeviceCgroupPermissions.of_json in
      let containerPath = field_map json "containerPath" String_.of_json in
      let hostPath = field_map_exn json "hostPath" String_.of_json in
      make ?permissions ?containerPath ~hostPath ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An object representing a container instance host device."]
module Tmpfs =
  struct
    type nonrec t =
      {
      containerPath: String_.t
        [@ocaml.doc
          "The absolute file path where the tmpfs volume is to be mounted."];
      size: Integer.t
        [@ocaml.doc "The maximum size (in MiB) of the tmpfs volume."];
      mountOptions: StringList.t option
        [@ocaml.doc
          "The list of tmpfs volume mount options. Valid values: \"defaults\" | \"ro\" | \"rw\" | \"suid\" | \"nosuid\" | \"dev\" | \"nodev\" | \"exec\" | \"noexec\" | \"sync\" | \"async\" | \"dirsync\" | \"remount\" | \"mand\" | \"nomand\" | \"atime\" | \"noatime\" | \"diratime\" | \"nodiratime\" | \"bind\" | \"rbind\" | \"unbindable\" | \"runbindable\" | \"private\" | \"rprivate\" | \"shared\" | \"rshared\" | \"slave\" | \"rslave\" | \"relatime\" | \"norelatime\" | \"strictatime\" | \"nostrictatime\" | \"mode\" | \"uid\" | \"gid\" | \"nr_inodes\" | \"nr_blocks\" | \"mpol\""]}
    let context_ = "Tmpfs"
    let make ?mountOptions =
      fun ~containerPath ->
        fun ~size -> fun () -> { mountOptions; containerPath; size }
    let to_value x =
      structure_to_value
        [("containerPath", (Some (String_.to_value x.containerPath)));
        ("size", (Some (Integer.to_value x.size)));
        ("mountOptions", (Option.map x.mountOptions ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let mountOptions =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "mountOptions") in
      let size =
        Integer.of_xml (Xml.child_exn ~context:context_ xml_arg0 "size") in
      let containerPath =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "containerPath") in
      make ?mountOptions ~size ~containerPath ()
    let of_json json =
      let mountOptions = field_map json "mountOptions" StringList.of_json in
      let size = field_map_exn json "size" Integer.of_json in
      let containerPath = field_map_exn json "containerPath" String_.of_json in
      make ?mountOptions ~size ~containerPath ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The container path, mount options, and size of the tmpfs mount."]
module Secret =
  struct
    type nonrec t =
      {
      name: String_.t [@ocaml.doc "The name of the secret."];
      valueFrom: String_.t
        [@ocaml.doc
          "The secret to expose to the container. The supported values are either the full ARN of the Secrets Manager secret or the full ARN of the parameter in the SSM Parameter Store. For information about the require Identity and Access Management permissions, see Required IAM permissions for Amazon ECS secrets (for Secrets Manager) or Required IAM permissions for Amazon ECS secrets (for Systems Manager Parameter store) in the Amazon Elastic Container Service Developer Guide. If the SSM Parameter Store parameter exists in the same Region as the task you're launching, then you can use either the full ARN or name of the parameter. If the parameter exists in a different Region, then the full ARN must be specified."]}
    let context_ = "Secret"
    let make ~name = fun ~valueFrom -> fun () -> { name; valueFrom }
    let to_value x =
      structure_to_value
        [("name", (Some (String_.to_value x.name)));
        ("valueFrom", (Some (String_.to_value x.valueFrom)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let valueFrom =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "valueFrom") in
      let name =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~valueFrom ~name ()
    let of_json json =
      let valueFrom = field_map_exn json "valueFrom" String_.of_json in
      let name = field_map_exn json "name" String_.of_json in
      make ~valueFrom ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing the secret to expose to your container. Secrets can be exposed to a container in the following ways: To inject sensitive data into your containers as environment variables, use the secrets container definition parameter. To reference sensitive information in the log configuration of a container, use the secretOptions container definition parameter. For more information, see Specifying Sensitive Data in the Amazon Elastic Container Service Developer Guide."]
module BoxedBoolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module UlimitName =
  struct
    type nonrec t =
      | Core_ 
      | Cpu 
      | Data 
      | Fsize 
      | Locks 
      | Memlock 
      | Msgqueue 
      | Nice 
      | Nofile 
      | Nproc 
      | Rss 
      | Rtprio 
      | Rttime 
      | Sigpending 
      | Stack 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Core_ -> "core"
      | Cpu -> "cpu"
      | Data -> "data"
      | Fsize -> "fsize"
      | Locks -> "locks"
      | Memlock -> "memlock"
      | Msgqueue -> "msgqueue"
      | Nice -> "nice"
      | Nofile -> "nofile"
      | Nproc -> "nproc"
      | Rss -> "rss"
      | Rtprio -> "rtprio"
      | Rttime -> "rttime"
      | Sigpending -> "sigpending"
      | Stack -> "stack"
      | Non_static_id s -> s
    let of_string =
      function
      | "core" -> Core_
      | "cpu" -> Cpu
      | "data" -> Data
      | "fsize" -> Fsize
      | "locks" -> Locks
      | "memlock" -> Memlock
      | "msgqueue" -> Msgqueue
      | "nice" -> Nice
      | "nofile" -> Nofile
      | "nproc" -> Nproc
      | "rss" -> Rss
      | "rtprio" -> Rtprio
      | "rttime" -> Rttime
      | "sigpending" -> Sigpending
      | "stack" -> Stack
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration UlimitName" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"UlimitName" j)
    let to_json = simple_to_json to_value
  end
module EFSAuthorizationConfigIAM =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration EFSAuthorizationConfigIAM" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"EFSAuthorizationConfigIAM" j)
    let to_json = simple_to_json to_value
  end
module CapacityProviderStrategyItem =
  struct
    type nonrec t =
      {
      capacityProvider: String_.t
        [@ocaml.doc "The short name of the capacity provider."];
      weight: CapacityProviderStrategyItemWeight.t option
        [@ocaml.doc
          "The weight value designates the relative percentage of the total number of tasks launched that should use the specified capacity provider. The weight value is taken into consideration after the base value, if defined, is satisfied. If no weight value is specified, the default value of 0 is used. When multiple capacity providers are specified within a capacity provider strategy, at least one of the capacity providers must have a weight value greater than zero and any capacity providers with a weight of 0 can't be used to place tasks. If you specify multiple capacity providers in a strategy that all have a weight of 0, any RunTask or CreateService actions using the capacity provider strategy will fail. An example scenario for using weights is defining a strategy that contains two capacity providers and both have a weight of 1, then when the base is satisfied, the tasks will be split evenly across the two capacity providers. Using that same logic, if you specify a weight of 1 for capacityProviderA and a weight of 4 for capacityProviderB, then for every one task that's run using capacityProviderA, four tasks would use capacityProviderB."];
      base: CapacityProviderStrategyItemBase.t option
        [@ocaml.doc
          "The base value designates how many tasks, at a minimum, to run on the specified capacity provider. Only one capacity provider in a capacity provider strategy can have a base defined. If no value is specified, the default value of 0 is used."]}
    let context_ = "CapacityProviderStrategyItem"
    let make ?weight =
      fun ?base ->
        fun ~capacityProvider -> fun () -> { weight; base; capacityProvider }
    let to_value x =
      structure_to_value
        [("capacityProvider", (Some (String_.to_value x.capacityProvider)));
        ("weight",
          (Option.map x.weight ~f:CapacityProviderStrategyItemWeight.to_value));
        ("base",
          (Option.map x.base ~f:CapacityProviderStrategyItemBase.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let base =
        (Option.map ~f:CapacityProviderStrategyItemBase.of_xml)
          (Xml.child xml_arg0 "base") in
      let weight =
        (Option.map ~f:CapacityProviderStrategyItemWeight.of_xml)
          (Xml.child xml_arg0 "weight") in
      let capacityProvider =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "capacityProvider") in
      make ?base ?weight ~capacityProvider ()
    let of_json json =
      let base =
        field_map json "base" CapacityProviderStrategyItemBase.of_json in
      let weight =
        field_map json "weight" CapacityProviderStrategyItemWeight.of_json in
      let capacityProvider =
        field_map_exn json "capacityProvider" String_.of_json in
      make ?base ?weight ~capacityProvider ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details of a capacity provider strategy. A capacity provider strategy can be set when using the RunTask or CreateCluster APIs or as the default capacity provider strategy for a cluster with the CreateCluster API. Only capacity providers that are already associated with a cluster and have an ACTIVE or UPDATING status can be used in a capacity provider strategy. The PutClusterCapacityProviders API is used to associate a capacity provider with a cluster. If specifying a capacity provider that uses an Auto Scaling group, the capacity provider must already be created. New Auto Scaling group capacity providers can be created with the CreateCapacityProvider API operation. To use a Fargate capacity provider, specify either the FARGATE or FARGATE_SPOT capacity providers. The Fargate capacity providers are available to all accounts and only need to be associated with a cluster to be used in a capacity provider strategy. A capacity provider strategy may contain a maximum of 6 capacity providers."]
module AwsVpcConfiguration =
  struct
    type nonrec t =
      {
      subnets: StringList.t
        [@ocaml.doc
          "The IDs of the subnets associated with the task or service. There's a limit of 16 subnets that can be specified per AwsVpcConfiguration. All specified subnets must be from the same VPC."];
      securityGroups: StringList.t option
        [@ocaml.doc
          "The IDs of the security groups associated with the task or service. If you don't specify a security group, the default security group for the VPC is used. There's a limit of 5 security groups that can be specified per AwsVpcConfiguration. All specified security groups must be from the same VPC."];
      assignPublicIp: AssignPublicIp.t option
        [@ocaml.doc
          "Whether the task's elastic network interface receives a public IP address. The default value is DISABLED."]}
    let context_ = "AwsVpcConfiguration"
    let make ?securityGroups =
      fun ?assignPublicIp ->
        fun ~subnets -> fun () -> { securityGroups; assignPublicIp; subnets }
    let to_value x =
      structure_to_value
        [("subnets", (Some (StringList.to_value x.subnets)));
        ("securityGroups",
          (Option.map x.securityGroups ~f:StringList.to_value));
        ("assignPublicIp",
          (Option.map x.assignPublicIp ~f:AssignPublicIp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assignPublicIp =
        (Option.map ~f:AssignPublicIp.of_xml)
          (Xml.child xml_arg0 "assignPublicIp") in
      let securityGroups =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "securityGroups") in
      let subnets =
        StringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "subnets") in
      make ?assignPublicIp ?securityGroups ~subnets ()
    let of_json json =
      let assignPublicIp =
        field_map json "assignPublicIp" AssignPublicIp.of_json in
      let securityGroups = field_map json "securityGroups" StringList.of_json in
      let subnets = field_map_exn json "subnets" StringList.of_json in
      make ?assignPublicIp ?securityGroups ~subnets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing the networking details for a task or service."]
module LoadBalancer =
  struct
    type nonrec t =
      {
      targetGroupArn: String_.t option
        [@ocaml.doc
          "The full Amazon Resource Name (ARN) of the Elastic Load Balancing target group or groups associated with a service or task set. A target group ARN is only specified when using an Application Load Balancer or Network Load Balancer. If you're using a Classic Load Balancer, omit the target group ARN. For services using the ECS deployment controller, you can specify one or multiple target groups. For more information, see Registering Multiple Target Groups with a Service in the Amazon Elastic Container Service Developer Guide. For services using the CODE_DEPLOY deployment controller, you're required to define two target groups for the load balancer. For more information, see Blue/Green Deployment with CodeDeploy in the Amazon Elastic Container Service Developer Guide. If your service's task definition uses the awsvpc network mode, you must choose ip as the target type, not instance. Do this when creating your target groups because tasks that use the awsvpc network mode are associated with an elastic network interface, not an Amazon EC2 instance. This network mode is required for the Fargate launch type."];
      loadBalancerName: String_.t option
        [@ocaml.doc
          "The name of the load balancer to associate with the Amazon ECS service or task set. A load balancer name is only specified when using a Classic Load Balancer. If you are using an Application Load Balancer or a Network Load Balancer the load balancer name parameter should be omitted."];
      containerName: String_.t option
        [@ocaml.doc
          "The name of the container (as it appears in a container definition) to associate with the load balancer."];
      containerPort: BoxedInteger.t option
        [@ocaml.doc
          "The port on the container to associate with the load balancer. This port must correspond to a containerPort in the task definition the tasks in the service are using. For tasks that use the EC2 launch type, the container instance they're launched on must allow ingress traffic on the hostPort of the port mapping."]}
    let make ?targetGroupArn =
      fun ?loadBalancerName ->
        fun ?containerName ->
          fun ?containerPort ->
            fun () ->
              {
                targetGroupArn;
                loadBalancerName;
                containerName;
                containerPort
              }
    let to_value x =
      structure_to_value
        [("targetGroupArn",
           (Option.map x.targetGroupArn ~f:String_.to_value));
        ("loadBalancerName",
          (Option.map x.loadBalancerName ~f:String_.to_value));
        ("containerName", (Option.map x.containerName ~f:String_.to_value));
        ("containerPort",
          (Option.map x.containerPort ~f:BoxedInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerPort =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "containerPort") in
      let containerName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "containerName") in
      let loadBalancerName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "loadBalancerName") in
      let targetGroupArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "targetGroupArn") in
      make ?containerPort ?containerName ?loadBalancerName ?targetGroupArn ()
    let of_json json =
      let containerPort = field_map json "containerPort" BoxedInteger.of_json in
      let containerName = field_map json "containerName" String_.of_json in
      let loadBalancerName =
        field_map json "loadBalancerName" String_.of_json in
      let targetGroupArn = field_map json "targetGroupArn" String_.of_json in
      make ?containerPort ?containerName ?loadBalancerName ?targetGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The load balancer configuration to use with a service or task set. For specific notes and restrictions regarding the use of load balancers with services and task sets, see the CreateService and CreateTaskSet actions. When you add, update, or remove a load balancer configuration, Amazon ECS starts a new deployment with the updated Elastic Load Balancing configuration. This causes tasks to register to and deregister from load balancers. We recommend that you verify this on a test environment before you update the Elastic Load Balancing configuration. A service-linked role is required for services that use multiple target groups. For more information, see Service-linked roles in the Amazon Elastic Container Service Developer Guide."]
module Double =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module ScaleUnit =
  struct
    type nonrec t =
      | PERCENT 
      | Non_static_id of string 
    let make i = i
    let to_string = function | PERCENT -> "PERCENT" | Non_static_id s -> s
    let of_string = function | "PERCENT" -> PERCENT | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ScaleUnit" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ScaleUnit" j)
    let to_json = simple_to_json to_value
  end
module ServiceRegistry =
  struct
    type nonrec t =
      {
      registryArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the service registry. The currently supported service registry is Cloud Map. For more information, see CreateService."];
      port: BoxedInteger.t option
        [@ocaml.doc
          "The port value used if your service discovery service specified an SRV record. This field might be used if both the awsvpc network mode and SRV records are used."];
      containerName: String_.t option
        [@ocaml.doc
          "The container name value to be used for your service discovery service. It's already specified in the task definition. If the task definition that your service task specifies uses the bridge or host network mode, you must specify a containerName and containerPort combination from the task definition. If the task definition that your service task specifies uses the awsvpc network mode and a type SRV DNS record is used, you must specify either a containerName and containerPort combination or a port value. However, you can't specify both."];
      containerPort: BoxedInteger.t option
        [@ocaml.doc
          "The port value to be used for your service discovery service. It's already specified in the task definition. If the task definition your service task specifies uses the bridge or host network mode, you must specify a containerName and containerPort combination from the task definition. If the task definition your service task specifies uses the awsvpc network mode and a type SRV DNS record is used, you must specify either a containerName and containerPort combination or a port value. However, you can't specify both."]}
    let make ?registryArn =
      fun ?port ->
        fun ?containerName ->
          fun ?containerPort ->
            fun () -> { registryArn; port; containerName; containerPort }
    let to_value x =
      structure_to_value
        [("registryArn", (Option.map x.registryArn ~f:String_.to_value));
        ("port", (Option.map x.port ~f:BoxedInteger.to_value));
        ("containerName", (Option.map x.containerName ~f:String_.to_value));
        ("containerPort",
          (Option.map x.containerPort ~f:BoxedInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerPort =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "containerPort") in
      let containerName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "containerName") in
      let port =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "port") in
      let registryArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "registryArn") in
      make ?containerPort ?containerName ?port ?registryArn ()
    let of_json json =
      let containerPort = field_map json "containerPort" BoxedInteger.of_json in
      let containerName = field_map json "containerName" String_.of_json in
      let port = field_map json "port" BoxedInteger.of_json in
      let registryArn = field_map json "registryArn" String_.of_json in
      make ?containerPort ?containerName ?port ?registryArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details for the service registry. Each service may be associated with one service registry. Multiple service registries for each service are not supported. When you add, update, or remove the service registries configuration, Amazon ECS starts a new deployment. New tasks are registered and deregistered to the updated service registry configuration."]
module Tag =
  struct
    type nonrec t =
      {
      key: TagKey.t option
        [@ocaml.doc
          "One part of a key-value pair that make up a tag. A key is a general label that acts like a category for more specific tag values."];
      value: TagValue.t option
        [@ocaml.doc
          "The optional part of a key-value pair that make up a tag. A value acts as a descriptor within a tag category (key)."]}
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("key", (Option.map x.key ~f:TagKey.to_value));
        ("value", (Option.map x.value ~f:TagValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:TagValue.of_xml) (Xml.child xml_arg0 "value") in
      let key = (Option.map ~f:TagKey.of_xml) (Xml.child xml_arg0 "key") in
      make ?value ?key ()
    let of_json json =
      let value = field_map json "value" TagValue.of_json in
      let key = field_map json "key" TagKey.of_json in make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The metadata that you apply to a resource to help you categorize and organize them. Each tag consists of a key and an optional value. You define them. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."]
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module AttachmentDetails =
  struct
    type nonrec t = KeyValuePair.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:KeyValuePair.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:KeyValuePair.of_xml)
    let of_json j =
      list_of_json ~kind:"AttachmentDetails" ~of_json:KeyValuePair.of_json j
    let to_json v = composed_to_json to_value v
  end
module TargetType =
  struct
    type nonrec t =
      | Container_instance 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Container_instance -> "container-instance"
      | Non_static_id s -> s
    let of_string =
      function
      | "container-instance" -> Container_instance
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TargetType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TargetType" j)
    let to_json = simple_to_json to_value
  end
module InstanceHealthCheckResult =
  struct
    type nonrec t =
      {
      type_: InstanceHealthCheckType.t option
        [@ocaml.doc
          "The type of container instance health status that was verified."];
      status: InstanceHealthCheckState.t option
        [@ocaml.doc "The container instance health status."];
      lastUpdated: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for when the container instance health status was last updated."];
      lastStatusChange: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for when the container instance health status last changed."]}
    let make ?type_ =
      fun ?status ->
        fun ?lastUpdated ->
          fun ?lastStatusChange ->
            fun () -> { type_; status; lastUpdated; lastStatusChange }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:InstanceHealthCheckType.to_value));
        ("status",
          (Option.map x.status ~f:InstanceHealthCheckState.to_value));
        ("lastUpdated", (Option.map x.lastUpdated ~f:Timestamp.to_value));
        ("lastStatusChange",
          (Option.map x.lastStatusChange ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastStatusChange =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastStatusChange") in
      let lastUpdated =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdated") in
      let status =
        (Option.map ~f:InstanceHealthCheckState.of_xml)
          (Xml.child xml_arg0 "status") in
      let type_ =
        (Option.map ~f:InstanceHealthCheckType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?lastStatusChange ?lastUpdated ?status ?type_ ()
    let of_json json =
      let lastStatusChange =
        field_map json "lastStatusChange" Timestamp.of_json in
      let lastUpdated = field_map json "lastUpdated" Timestamp.of_json in
      let status = field_map json "status" InstanceHealthCheckState.of_json in
      let type_ = field_map json "type" InstanceHealthCheckType.of_json in
      make ?lastStatusChange ?lastUpdated ?status ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing the result of a container instance health status check."]
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module GpuIds =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j = list_of_json ~kind:"GpuIds" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module HealthStatus =
  struct
    type nonrec t =
      | HEALTHY 
      | UNHEALTHY 
      | UNKNOWN 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | HEALTHY -> "HEALTHY"
      | UNHEALTHY -> "UNHEALTHY"
      | UNKNOWN -> "UNKNOWN"
      | Non_static_id s -> s
    let of_string =
      function
      | "HEALTHY" -> HEALTHY
      | "UNHEALTHY" -> UNHEALTHY
      | "UNKNOWN" -> UNKNOWN
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration HealthStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"HealthStatus" j)
    let to_json = simple_to_json to_value
  end
module ManagedAgents =
  struct
    type nonrec t = ManagedAgent.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ManagedAgent.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ManagedAgent.of_xml)
    let of_json j =
      list_of_json ~kind:"ManagedAgents" ~of_json:ManagedAgent.of_json j
    let to_json v = composed_to_json to_value v
  end
module NetworkBindings =
  struct
    type nonrec t = NetworkBinding.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:NetworkBinding.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NetworkBinding.of_xml)
    let of_json j =
      list_of_json ~kind:"NetworkBindings" ~of_json:NetworkBinding.of_json j
    let to_json v = composed_to_json to_value v
  end
module NetworkInterfaces =
  struct
    type nonrec t = NetworkInterface.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:NetworkInterface.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NetworkInterface.of_xml)
    let of_json j =
      list_of_json ~kind:"NetworkInterfaces"
        ~of_json:NetworkInterface.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerOverride =
  struct
    type nonrec t =
      {
      name: String_.t option
        [@ocaml.doc
          "The name of the container that receives the override. This parameter is required if any override is specified."];
      command: StringList.t option
        [@ocaml.doc
          "The command to send to the container that overrides the default command from the Docker image or the task definition. You must also specify a container name."];
      environment: EnvironmentVariables.t option
        [@ocaml.doc
          "The environment variables to send to the container. You can add new environment variables, which are added to the container at launch, or you can override the existing environment variables from the Docker image or the task definition. You must also specify a container name."];
      environmentFiles: EnvironmentFiles.t option
        [@ocaml.doc
          "A list of files containing the environment variables to pass to a container, instead of the value from the container definition."];
      cpu: BoxedInteger.t option
        [@ocaml.doc
          "The number of cpu units reserved for the container, instead of the default value from the task definition. You must also specify a container name."];
      memory: BoxedInteger.t option
        [@ocaml.doc
          "The hard limit (in MiB) of memory to present to the container, instead of the default value from the task definition. If your container attempts to exceed the memory specified here, the container is killed. You must also specify a container name."];
      memoryReservation: BoxedInteger.t option
        [@ocaml.doc
          "The soft limit (in MiB) of memory to reserve for the container, instead of the default value from the task definition. You must also specify a container name."];
      resourceRequirements: ResourceRequirements.t option
        [@ocaml.doc
          "The type and amount of a resource to assign to a container, instead of the default value from the task definition. The only supported resource is a GPU."]}
    let make ?name =
      fun ?command ->
        fun ?environment ->
          fun ?environmentFiles ->
            fun ?cpu ->
              fun ?memory ->
                fun ?memoryReservation ->
                  fun ?resourceRequirements ->
                    fun () ->
                      {
                        name;
                        command;
                        environment;
                        environmentFiles;
                        cpu;
                        memory;
                        memoryReservation;
                        resourceRequirements
                      }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("command", (Option.map x.command ~f:StringList.to_value));
        ("environment",
          (Option.map x.environment ~f:EnvironmentVariables.to_value));
        ("environmentFiles",
          (Option.map x.environmentFiles ~f:EnvironmentFiles.to_value));
        ("cpu", (Option.map x.cpu ~f:BoxedInteger.to_value));
        ("memory", (Option.map x.memory ~f:BoxedInteger.to_value));
        ("memoryReservation",
          (Option.map x.memoryReservation ~f:BoxedInteger.to_value));
        ("resourceRequirements",
          (Option.map x.resourceRequirements ~f:ResourceRequirements.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceRequirements =
        (Option.map ~f:ResourceRequirements.of_xml)
          (Xml.child xml_arg0 "resourceRequirements") in
      let memoryReservation =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "memoryReservation") in
      let memory =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "memory") in
      let cpu =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "cpu") in
      let environmentFiles =
        (Option.map ~f:EnvironmentFiles.of_xml)
          (Xml.child xml_arg0 "environmentFiles") in
      let environment =
        (Option.map ~f:EnvironmentVariables.of_xml)
          (Xml.child xml_arg0 "environment") in
      let command =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "command") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?resourceRequirements ?memoryReservation ?memory ?cpu
        ?environmentFiles ?environment ?command ?name ()
    let of_json json =
      let resourceRequirements =
        field_map json "resourceRequirements" ResourceRequirements.of_json in
      let memoryReservation =
        field_map json "memoryReservation" BoxedInteger.of_json in
      let memory = field_map json "memory" BoxedInteger.of_json in
      let cpu = field_map json "cpu" BoxedInteger.of_json in
      let environmentFiles =
        field_map json "environmentFiles" EnvironmentFiles.of_json in
      let environment =
        field_map json "environment" EnvironmentVariables.of_json in
      let command = field_map json "command" StringList.of_json in
      let name = field_map json "name" String_.of_json in
      make ?resourceRequirements ?memoryReservation ?memory ?cpu
        ?environmentFiles ?environment ?command ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The overrides that are sent to a container. An empty container override can be passed in. An example of an empty container override is \\{\"containerOverrides\": \\[ \\] \\}. If a non-empty container override is specified, the name parameter must be included."]
module InferenceAcceleratorOverride =
  struct
    type nonrec t =
      {
      deviceName: String_.t option
        [@ocaml.doc
          "The Elastic Inference accelerator device name to override for the task. This parameter must match a deviceName specified in the task definition."];
      deviceType: String_.t option
        [@ocaml.doc "The Elastic Inference accelerator type to use."]}
    let make ?deviceName =
      fun ?deviceType -> fun () -> { deviceName; deviceType }
    let to_value x =
      structure_to_value
        [("deviceName", (Option.map x.deviceName ~f:String_.to_value));
        ("deviceType", (Option.map x.deviceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deviceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "deviceType") in
      let deviceName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "deviceName") in
      make ?deviceType ?deviceName ()
    let of_json json =
      let deviceType = field_map json "deviceType" String_.of_json in
      let deviceName = field_map json "deviceName" String_.of_json in
      make ?deviceType ?deviceName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details on an Elastic Inference accelerator task override. This parameter is used to override the Elastic Inference accelerator specified in the task definition. For more information, see Working with Amazon Elastic Inference on Amazon ECS in the Amazon Elastic Container Service Developer Guide."]
module ContainerDependency =
  struct
    type nonrec t =
      {
      containerName: String_.t [@ocaml.doc "The name of a container."];
      condition: ContainerCondition.t
        [@ocaml.doc
          "The dependency condition of the container. The following are the available conditions and their behavior: START - This condition emulates the behavior of links and volumes today. It validates that a dependent container is started before permitting other containers to start. COMPLETE - This condition validates that a dependent container runs to completion (exits) before permitting other containers to start. This can be useful for nonessential containers that run a script and then exit. This condition can't be set on an essential container. SUCCESS - This condition is the same as COMPLETE, but it also requires that the container exits with a zero status. This condition can't be set on an essential container. HEALTHY - This condition validates that the dependent container passes its Docker health check before permitting other containers to start. This requires that the dependent container has health checks configured. This condition is confirmed only at task startup."]}
    let context_ = "ContainerDependency"
    let make ~containerName =
      fun ~condition -> fun () -> { containerName; condition }
    let to_value x =
      structure_to_value
        [("containerName", (Some (String_.to_value x.containerName)));
        ("condition", (Some (ContainerCondition.to_value x.condition)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let condition =
        ContainerCondition.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "condition") in
      let containerName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "containerName") in
      make ~condition ~containerName ()
    let of_json json =
      let condition =
        field_map_exn json "condition" ContainerCondition.of_json in
      let containerName = field_map_exn json "containerName" String_.of_json in
      make ~condition ~containerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The dependencies defined for container startup and shutdown. A container can contain multiple dependencies. When a dependency is defined for container startup, for container shutdown it is reversed. Your Amazon ECS container instances require at least version 1.26.0 of the container agent to use container dependencies. However, we recommend using the latest container agent version. For information about checking your agent version and updating to the latest version, see Updating the Amazon ECS Container Agent in the Amazon Elastic Container Service Developer Guide. If you're using an Amazon ECS-optimized Linux AMI, your instance needs at least version 1.26.0-1 of the ecs-init package. If your container instances are launched from version 20190301 or later, then they contain the required versions of the container agent and ecs-init. For more information, see Amazon ECS-optimized Linux AMI in the Amazon Elastic Container Service Developer Guide. For tasks that use the Fargate launch type, the task or service requires the following platforms: Linux platform version 1.3.0 or later. Windows platform version 1.0.0 or later."]
module FirelensConfigurationOptionsMap =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module FirelensConfigurationType =
  struct
    type nonrec t =
      | Fluentd 
      | Fluentbit 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Fluentd -> "fluentd"
      | Fluentbit -> "fluentbit"
      | Non_static_id s -> s
    let of_string =
      function
      | "fluentd" -> Fluentd
      | "fluentbit" -> Fluentbit
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FirelensConfigurationType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"FirelensConfigurationType" j)
    let to_json = simple_to_json to_value
  end
module HostEntry =
  struct
    type nonrec t =
      {
      hostname: String_.t
        [@ocaml.doc "The hostname to use in the /etc/hosts entry."];
      ipAddress: String_.t
        [@ocaml.doc "The IP address to use in the /etc/hosts entry."]}
    let context_ = "HostEntry"
    let make ~hostname = fun ~ipAddress -> fun () -> { hostname; ipAddress }
    let to_value x =
      structure_to_value
        [("hostname", (Some (String_.to_value x.hostname)));
        ("ipAddress", (Some (String_.to_value x.ipAddress)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ipAddress =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ipAddress") in
      let hostname =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "hostname") in
      make ~ipAddress ~hostname ()
    let of_json json =
      let ipAddress = field_map_exn json "ipAddress" String_.of_json in
      let hostname = field_map_exn json "hostname" String_.of_json in
      make ~ipAddress ~hostname ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Hostnames and IP address entries that are added to the /etc/hosts file of a container via the extraHosts parameter of its ContainerDefinition."]
module DevicesList =
  struct
    type nonrec t = Device.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Device.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Device.of_xml)
    let of_json j =
      list_of_json ~kind:"DevicesList" ~of_json:Device.of_json j
    let to_json v = composed_to_json to_value v
  end
module KernelCapabilities =
  struct
    type nonrec t =
      {
      add: StringList.t option
        [@ocaml.doc
          "The Linux capabilities for the container that have been added to the default configuration provided by Docker. This parameter maps to CapAdd in the Create a container section of the Docker Remote API and the --cap-add option to docker run. Tasks launched on Fargate only support adding the SYS_PTRACE kernel capability. Valid values: \"ALL\" | \"AUDIT_CONTROL\" | \"AUDIT_WRITE\" | \"BLOCK_SUSPEND\" | \"CHOWN\" | \"DAC_OVERRIDE\" | \"DAC_READ_SEARCH\" | \"FOWNER\" | \"FSETID\" | \"IPC_LOCK\" | \"IPC_OWNER\" | \"KILL\" | \"LEASE\" | \"LINUX_IMMUTABLE\" | \"MAC_ADMIN\" | \"MAC_OVERRIDE\" | \"MKNOD\" | \"NET_ADMIN\" | \"NET_BIND_SERVICE\" | \"NET_BROADCAST\" | \"NET_RAW\" | \"SETFCAP\" | \"SETGID\" | \"SETPCAP\" | \"SETUID\" | \"SYS_ADMIN\" | \"SYS_BOOT\" | \"SYS_CHROOT\" | \"SYS_MODULE\" | \"SYS_NICE\" | \"SYS_PACCT\" | \"SYS_PTRACE\" | \"SYS_RAWIO\" | \"SYS_RESOURCE\" | \"SYS_TIME\" | \"SYS_TTY_CONFIG\" | \"SYSLOG\" | \"WAKE_ALARM\""];
      drop: StringList.t option
        [@ocaml.doc
          "The Linux capabilities for the container that have been removed from the default configuration provided by Docker. This parameter maps to CapDrop in the Create a container section of the Docker Remote API and the --cap-drop option to docker run. Valid values: \"ALL\" | \"AUDIT_CONTROL\" | \"AUDIT_WRITE\" | \"BLOCK_SUSPEND\" | \"CHOWN\" | \"DAC_OVERRIDE\" | \"DAC_READ_SEARCH\" | \"FOWNER\" | \"FSETID\" | \"IPC_LOCK\" | \"IPC_OWNER\" | \"KILL\" | \"LEASE\" | \"LINUX_IMMUTABLE\" | \"MAC_ADMIN\" | \"MAC_OVERRIDE\" | \"MKNOD\" | \"NET_ADMIN\" | \"NET_BIND_SERVICE\" | \"NET_BROADCAST\" | \"NET_RAW\" | \"SETFCAP\" | \"SETGID\" | \"SETPCAP\" | \"SETUID\" | \"SYS_ADMIN\" | \"SYS_BOOT\" | \"SYS_CHROOT\" | \"SYS_MODULE\" | \"SYS_NICE\" | \"SYS_PACCT\" | \"SYS_PTRACE\" | \"SYS_RAWIO\" | \"SYS_RESOURCE\" | \"SYS_TIME\" | \"SYS_TTY_CONFIG\" | \"SYSLOG\" | \"WAKE_ALARM\""]}
    let make ?add = fun ?drop -> fun () -> { add; drop }
    let to_value x =
      structure_to_value
        [("add", (Option.map x.add ~f:StringList.to_value));
        ("drop", (Option.map x.drop ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let drop =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "drop") in
      let add = (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "add") in
      make ?drop ?add ()
    let of_json json =
      let drop = field_map json "drop" StringList.of_json in
      let add = field_map json "add" StringList.of_json in make ?drop ?add ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Linux capabilities for the container that are added to or dropped from the default configuration provided by Docker. For more information about the default capabilities and the non-default available capabilities, see Runtime privilege and Linux capabilities in the Docker run reference. For more detailed information about these Linux capabilities, see the capabilities(7) Linux manual page."]
module TmpfsList =
  struct
    type nonrec t = Tmpfs.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Tmpfs.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tmpfs.of_xml)
    let of_json j = list_of_json ~kind:"TmpfsList" ~of_json:Tmpfs.of_json j
    let to_json v = composed_to_json to_value v
  end
module LogConfigurationOptionsMap =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module LogDriver =
  struct
    type nonrec t =
      | Json_file 
      | Syslog 
      | Journald 
      | Gelf 
      | Fluentd 
      | Awslogs 
      | Splunk 
      | Awsfirelens 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Json_file -> "json-file"
      | Syslog -> "syslog"
      | Journald -> "journald"
      | Gelf -> "gelf"
      | Fluentd -> "fluentd"
      | Awslogs -> "awslogs"
      | Splunk -> "splunk"
      | Awsfirelens -> "awsfirelens"
      | Non_static_id s -> s
    let of_string =
      function
      | "json-file" -> Json_file
      | "syslog" -> Syslog
      | "journald" -> Journald
      | "gelf" -> Gelf
      | "fluentd" -> Fluentd
      | "awslogs" -> Awslogs
      | "splunk" -> Splunk
      | "awsfirelens" -> Awsfirelens
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LogDriver" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LogDriver" j)
    let to_json = simple_to_json to_value
  end
module SecretList =
  struct
    type nonrec t = Secret.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Secret.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Secret.of_xml)
    let of_json j = list_of_json ~kind:"SecretList" ~of_json:Secret.of_json j
    let to_json v = composed_to_json to_value v
  end
module MountPoint =
  struct
    type nonrec t =
      {
      sourceVolume: String_.t option
        [@ocaml.doc
          "The name of the volume to mount. Must be a volume name referenced in the name parameter of task definition volume."];
      containerPath: String_.t option
        [@ocaml.doc "The path on the container to mount the host volume at."];
      readOnly: BoxedBoolean.t option
        [@ocaml.doc
          "If this value is true, the container has read-only access to the volume. If this value is false, then the container can write to the volume. The default value is false."]}
    let make ?sourceVolume =
      fun ?containerPath ->
        fun ?readOnly -> fun () -> { sourceVolume; containerPath; readOnly }
    let to_value x =
      structure_to_value
        [("sourceVolume", (Option.map x.sourceVolume ~f:String_.to_value));
        ("containerPath", (Option.map x.containerPath ~f:String_.to_value));
        ("readOnly", (Option.map x.readOnly ~f:BoxedBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let readOnly =
        (Option.map ~f:BoxedBoolean.of_xml) (Xml.child xml_arg0 "readOnly") in
      let containerPath =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "containerPath") in
      let sourceVolume =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sourceVolume") in
      make ?readOnly ?containerPath ?sourceVolume ()
    let of_json json =
      let readOnly = field_map json "readOnly" BoxedBoolean.of_json in
      let containerPath = field_map json "containerPath" String_.of_json in
      let sourceVolume = field_map json "sourceVolume" String_.of_json in
      make ?readOnly ?containerPath ?sourceVolume ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details for a volume mount point that's used in a container definition."]
module PortMapping =
  struct
    type nonrec t =
      {
      containerPort: BoxedInteger.t option
        [@ocaml.doc
          "The port number on the container that's bound to the user-specified or automatically assigned host port. If you use containers in a task with the awsvpc or host network mode, specify the exposed ports using containerPort. If you use containers in a task with the bridge network mode and you specify a container port and not a host port, your container automatically receives a host port in the ephemeral port range. For more information, see hostPort. Port mappings that are automatically assigned in this way do not count toward the 100 reserved ports limit of a container instance."];
      hostPort: BoxedInteger.t option
        [@ocaml.doc
          "The port number on the container instance to reserve for your container. If you use containers in a task with the awsvpc or host network mode, the hostPort can either be left blank or set to the same value as the containerPort. If you use containers in a task with the bridge network mode, you can specify a non-reserved host port for your container port mapping, or you can omit the hostPort (or set it to 0) while specifying a containerPort and your container automatically receives a port in the ephemeral port range for your container instance operating system and Docker version. The default ephemeral port range for Docker version 1.6.0 and later is listed on the instance under /proc/sys/net/ipv4/ip_local_port_range. If this kernel parameter is unavailable, the default ephemeral port range from 49153 through 65535 is used. Do not attempt to specify a host port in the ephemeral port range as these are reserved for automatic assignment. In general, ports below 32768 are outside of the ephemeral port range. The default ephemeral port range from 49153 through 65535 is always used for Docker versions before 1.6.0. The default reserved ports are 22 for SSH, the Docker ports 2375 and 2376, and the Amazon ECS container agent ports 51678-51680. Any host port that was previously specified in a running task is also reserved while the task is running. That is, after a task stops, the host port is released. The current reserved ports are displayed in the remainingResources of DescribeContainerInstances output. A container instance can have up to 100 reserved ports at a time. This number includes the default reserved ports. Automatically assigned ports aren't included in the 100 reserved ports quota."];
      protocol: TransportProtocol.t option
        [@ocaml.doc
          "The protocol used for the port mapping. Valid values are tcp and udp. The default is tcp."]}
    let make ?containerPort =
      fun ?hostPort ->
        fun ?protocol -> fun () -> { containerPort; hostPort; protocol }
    let to_value x =
      structure_to_value
        [("containerPort",
           (Option.map x.containerPort ~f:BoxedInteger.to_value));
        ("hostPort", (Option.map x.hostPort ~f:BoxedInteger.to_value));
        ("protocol", (Option.map x.protocol ~f:TransportProtocol.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protocol =
        (Option.map ~f:TransportProtocol.of_xml)
          (Xml.child xml_arg0 "protocol") in
      let hostPort =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "hostPort") in
      let containerPort =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "containerPort") in
      make ?protocol ?hostPort ?containerPort ()
    let of_json json =
      let protocol = field_map json "protocol" TransportProtocol.of_json in
      let hostPort = field_map json "hostPort" BoxedInteger.of_json in
      let containerPort = field_map json "containerPort" BoxedInteger.of_json in
      make ?protocol ?hostPort ?containerPort ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Port mappings allow containers to access ports on the host container instance to send or receive traffic. Port mappings are specified as part of the container definition. If you use containers in a task with the awsvpc or host network mode, specify the exposed ports using containerPort. The hostPort can be left blank or it must be the same value as the containerPort. You can't expose the same container port for multiple protocols. If you attempt this, an error is returned. After a task reaches the RUNNING status, manual and automatic host and container port assignments are visible in the networkBindings section of DescribeTasks API responses."]
module SystemControl =
  struct
    type nonrec t =
      {
      namespace: String_.t option
        [@ocaml.doc "The namespaced kernel parameter to set a value for."];
      value: String_.t option
        [@ocaml.doc
          "The value for the namespaced kernel parameter that's specified in namespace."]}
    let make ?namespace = fun ?value -> fun () -> { namespace; value }
    let to_value x =
      structure_to_value
        [("namespace", (Option.map x.namespace ~f:String_.to_value));
        ("value", (Option.map x.value ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let namespace =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "namespace") in
      make ?value ?namespace ()
    let of_json json =
      let value = field_map json "value" String_.of_json in
      let namespace = field_map json "namespace" String_.of_json in
      make ?value ?namespace ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of namespaced kernel parameters to set in the container. This parameter maps to Sysctls in the Create a container section of the Docker Remote API and the --sysctl option to docker run. We don't recommend that you specify network-related systemControls parameters for multiple containers in a single task. This task also uses either the awsvpc or host network mode. It does it for the following reasons. For tasks that use the awsvpc network mode, if you set systemControls for any container, it applies to all containers in the task. If you set different systemControls for multiple containers in a single task, the container that's started last determines which systemControls take effect. For tasks that use the host network mode, the systemControls parameter applies to the container instance's kernel parameter and that of all containers of any tasks running on that container instance."]
module Ulimit =
  struct
    type nonrec t =
      {
      name: UlimitName.t [@ocaml.doc "The type of the ulimit."];
      softLimit: Integer.t [@ocaml.doc "The soft limit for the ulimit type."];
      hardLimit: Integer.t [@ocaml.doc "The hard limit for the ulimit type."]}
    let context_ = "Ulimit"
    let make ~name =
      fun ~softLimit ->
        fun ~hardLimit -> fun () -> { name; softLimit; hardLimit }
    let to_value x =
      structure_to_value
        [("name", (Some (UlimitName.to_value x.name)));
        ("softLimit", (Some (Integer.to_value x.softLimit)));
        ("hardLimit", (Some (Integer.to_value x.hardLimit)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hardLimit =
        Integer.of_xml (Xml.child_exn ~context:context_ xml_arg0 "hardLimit") in
      let softLimit =
        Integer.of_xml (Xml.child_exn ~context:context_ xml_arg0 "softLimit") in
      let name =
        UlimitName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~hardLimit ~softLimit ~name ()
    let of_json json =
      let hardLimit = field_map_exn json "hardLimit" Integer.of_json in
      let softLimit = field_map_exn json "softLimit" Integer.of_json in
      let name = field_map_exn json "name" UlimitName.of_json in
      make ~hardLimit ~softLimit ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The ulimit settings to pass to the container. Amazon ECS tasks hosted on Fargate use the default resource limit values set by the operating system with the exception of the nofile resource limit parameter which Fargate overrides. The nofile resource limit sets a restriction on the number of open files that a container can use. The default nofile soft limit is 1024 and hard limit is 4096."]
module VolumeFrom =
  struct
    type nonrec t =
      {
      sourceContainer: String_.t option
        [@ocaml.doc
          "The name of another container within the same task definition to mount volumes from."];
      readOnly: BoxedBoolean.t option
        [@ocaml.doc
          "If this value is true, the container has read-only access to the volume. If this value is false, then the container can write to the volume. The default value is false."]}
    let make ?sourceContainer =
      fun ?readOnly -> fun () -> { sourceContainer; readOnly }
    let to_value x =
      structure_to_value
        [("sourceContainer",
           (Option.map x.sourceContainer ~f:String_.to_value));
        ("readOnly", (Option.map x.readOnly ~f:BoxedBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let readOnly =
        (Option.map ~f:BoxedBoolean.of_xml) (Xml.child xml_arg0 "readOnly") in
      let sourceContainer =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sourceContainer") in
      make ?readOnly ?sourceContainer ()
    let of_json json =
      let readOnly = field_map json "readOnly" BoxedBoolean.of_json in
      let sourceContainer = field_map json "sourceContainer" String_.of_json in
      make ?readOnly ?sourceContainer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details on a data volume from another container in the same task definition."]
module Scope =
  struct
    type nonrec t =
      | Task 
      | Shared 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Task -> "task" | Shared -> "shared" | Non_static_id s -> s
    let of_string =
      function | "task" -> Task | "shared" -> Shared | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Scope" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Scope" j)
    let to_json = simple_to_json to_value
  end
module StringMap =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module EFSAuthorizationConfig =
  struct
    type nonrec t =
      {
      accessPointId: String_.t option
        [@ocaml.doc
          "The Amazon EFS access point ID to use. If an access point is specified, the root directory value specified in the EFSVolumeConfiguration must either be omitted or set to / which will enforce the path set on the EFS access point. If an access point is used, transit encryption must be enabled in the EFSVolumeConfiguration. For more information, see Working with Amazon EFS Access Points in the Amazon Elastic File System User Guide."];
      iam: EFSAuthorizationConfigIAM.t option
        [@ocaml.doc
          "Determines whether to use the Amazon ECS task IAM role defined in a task definition when mounting the Amazon EFS file system. If enabled, transit encryption must be enabled in the EFSVolumeConfiguration. If this parameter is omitted, the default value of DISABLED is used. For more information, see Using Amazon EFS Access Points in the Amazon Elastic Container Service Developer Guide."]}
    let make ?accessPointId = fun ?iam -> fun () -> { accessPointId; iam }
    let to_value x =
      structure_to_value
        [("accessPointId", (Option.map x.accessPointId ~f:String_.to_value));
        ("iam", (Option.map x.iam ~f:EFSAuthorizationConfigIAM.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let iam =
        (Option.map ~f:EFSAuthorizationConfigIAM.of_xml)
          (Xml.child xml_arg0 "iam") in
      let accessPointId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "accessPointId") in
      make ?iam ?accessPointId ()
    let of_json json =
      let iam = field_map json "iam" EFSAuthorizationConfigIAM.of_json in
      let accessPointId = field_map json "accessPointId" String_.of_json in
      make ?iam ?accessPointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The authorization configuration details for the Amazon EFS file system."]
module EFSTransitEncryption =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration EFSTransitEncryption" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EFSTransitEncryption" j)
    let to_json = simple_to_json to_value
  end
module FSxWindowsFileServerAuthorizationConfig =
  struct
    type nonrec t =
      {
      credentialsParameter: String_.t
        [@ocaml.doc
          "The authorization credential option to use. The authorization credential options can be provided using either the Amazon Resource Name (ARN) of an Secrets Manager secret or SSM Parameter Store parameter. The ARNs refer to the stored credentials."];
      domain: String_.t
        [@ocaml.doc
          "A fully qualified domain name hosted by an Directory Service Managed Microsoft AD (Active Directory) or self-hosted AD on Amazon EC2."]}
    let context_ = "FSxWindowsFileServerAuthorizationConfig"
    let make ~credentialsParameter =
      fun ~domain -> fun () -> { credentialsParameter; domain }
    let to_value x =
      structure_to_value
        [("credentialsParameter",
           (Some (String_.to_value x.credentialsParameter)));
        ("domain", (Some (String_.to_value x.domain)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "domain") in
      let credentialsParameter =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "credentialsParameter") in
      make ~domain ~credentialsParameter ()
    let of_json json =
      let domain = field_map_exn json "domain" String_.of_json in
      let credentialsParameter =
        field_map_exn json "credentialsParameter" String_.of_json in
      make ~domain ~credentialsParameter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The authorization configuration details for Amazon FSx for Windows File Server file system. See FSxWindowsFileServerVolumeConfiguration in the Amazon Elastic Container Service API Reference. For more information and the input format, see Amazon FSx for Windows File Server Volumes in the Amazon Elastic Container Service Developer Guide."]
module CapacityProviderStrategy =
  struct
    type nonrec t = CapacityProviderStrategyItem.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CapacityProviderStrategyItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CapacityProviderStrategyItem.of_xml)
    let of_json j =
      list_of_json ~kind:"CapacityProviderStrategy"
        ~of_json:CapacityProviderStrategyItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module DeploymentRolloutState =
  struct
    type nonrec t =
      | COMPLETED 
      | FAILED 
      | IN_PROGRESS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | COMPLETED -> "COMPLETED"
      | FAILED -> "FAILED"
      | IN_PROGRESS -> "IN_PROGRESS"
      | Non_static_id s -> s
    let of_string =
      function
      | "COMPLETED" -> COMPLETED
      | "FAILED" -> FAILED
      | "IN_PROGRESS" -> IN_PROGRESS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DeploymentRolloutState" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"DeploymentRolloutState" j)
    let to_json = simple_to_json to_value
  end
module LaunchType =
  struct
    type nonrec t =
      | EC2 
      | FARGATE 
      | EXTERNAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | EC2 -> "EC2"
      | FARGATE -> "FARGATE"
      | EXTERNAL -> "EXTERNAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "EC2" -> EC2
      | "FARGATE" -> FARGATE
      | "EXTERNAL" -> EXTERNAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LaunchType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LaunchType" j)
    let to_json = simple_to_json to_value
  end
module NetworkConfiguration =
  struct
    type nonrec t =
      {
      awsvpcConfiguration: AwsVpcConfiguration.t option
        [@ocaml.doc
          "The VPC subnets and security groups that are associated with a task. All specified subnets and security groups must be from the same VPC."]}
    let make ?awsvpcConfiguration = fun () -> { awsvpcConfiguration }
    let to_value x =
      structure_to_value
        [("awsvpcConfiguration",
           (Option.map x.awsvpcConfiguration ~f:AwsVpcConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let awsvpcConfiguration =
        (Option.map ~f:AwsVpcConfiguration.of_xml)
          (Xml.child xml_arg0 "awsvpcConfiguration") in
      make ?awsvpcConfiguration ()
    let of_json json =
      let awsvpcConfiguration =
        field_map json "awsvpcConfiguration" AwsVpcConfiguration.of_json in
      make ?awsvpcConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing the network configuration for a task or service."]
module PlacementConstraintType =
  struct
    type nonrec t =
      | DistinctInstance 
      | MemberOf 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DistinctInstance -> "distinctInstance"
      | MemberOf -> "memberOf"
      | Non_static_id s -> s
    let of_string =
      function
      | "distinctInstance" -> DistinctInstance
      | "memberOf" -> MemberOf
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PlacementConstraintType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PlacementConstraintType" j)
    let to_json = simple_to_json to_value
  end
module PlacementStrategyType =
  struct
    type nonrec t =
      | Random 
      | Spread 
      | Binpack 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Random -> "random"
      | Spread -> "spread"
      | Binpack -> "binpack"
      | Non_static_id s -> s
    let of_string =
      function
      | "random" -> Random
      | "spread" -> Spread
      | "binpack" -> Binpack
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PlacementStrategyType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PlacementStrategyType" j)
    let to_json = simple_to_json to_value
  end
module LoadBalancers =
  struct
    type nonrec t = LoadBalancer.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LoadBalancer.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LoadBalancer.of_xml)
    let of_json j =
      list_of_json ~kind:"LoadBalancers" ~of_json:LoadBalancer.of_json j
    let to_json v = composed_to_json to_value v
  end
module Scale =
  struct
    type nonrec t =
      {
      value: Double.t option
        [@ocaml.doc
          "The value, specified as a percent total of a service's desiredCount, to scale the task set. Accepted values are numbers between 0 and 100."];
      unit: ScaleUnit.t option
        [@ocaml.doc "The unit of measure for the scale value."]}
    let make ?value = fun ?unit -> fun () -> { value; unit }
    let to_value x =
      structure_to_value
        [("value", (Option.map x.value ~f:Double.to_value));
        ("unit", (Option.map x.unit ~f:ScaleUnit.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unit = (Option.map ~f:ScaleUnit.of_xml) (Xml.child xml_arg0 "unit") in
      let value = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "value") in
      make ?unit ?value ()
    let of_json json =
      let unit = field_map json "unit" ScaleUnit.of_json in
      let value = field_map json "value" Double.of_json in
      make ?unit ?value ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A floating-point percentage of the desired number of tasks to place and keep running in the task set."]
module ServiceRegistries =
  struct
    type nonrec t = ServiceRegistry.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ServiceRegistry.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ServiceRegistry.of_xml)
    let of_json j =
      list_of_json ~kind:"ServiceRegistries" ~of_json:ServiceRegistry.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module StabilityStatus =
  struct
    type nonrec t =
      | STEADY_STATE 
      | STABILIZING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | STEADY_STATE -> "STEADY_STATE"
      | STABILIZING -> "STABILIZING"
      | Non_static_id s -> s
    let of_string =
      function
      | "STEADY_STATE" -> STEADY_STATE
      | "STABILIZING" -> STABILIZING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StabilityStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StabilityStatus" j)
    let to_json = simple_to_json to_value
  end
module Tags =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"Tags" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExecuteCommandLogConfiguration =
  struct
    type nonrec t =
      {
      cloudWatchLogGroupName: String_.t option
        [@ocaml.doc
          "The name of the CloudWatch log group to send logs to. The CloudWatch log group must already be created."];
      cloudWatchEncryptionEnabled: Boolean.t option
        [@ocaml.doc
          "Determines whether to use encryption on the CloudWatch logs. If not specified, encryption will be disabled."];
      s3BucketName: String_.t option
        [@ocaml.doc
          "The name of the S3 bucket to send logs to. The S3 bucket must already be created."];
      s3EncryptionEnabled: Boolean.t option
        [@ocaml.doc
          "Determines whether to use encryption on the S3 logs. If not specified, encryption is not used."];
      s3KeyPrefix: String_.t option
        [@ocaml.doc "An optional folder in the S3 bucket to place logs in."]}
    let make ?cloudWatchLogGroupName =
      fun ?cloudWatchEncryptionEnabled ->
        fun ?s3BucketName ->
          fun ?s3EncryptionEnabled ->
            fun ?s3KeyPrefix ->
              fun () ->
                {
                  cloudWatchLogGroupName;
                  cloudWatchEncryptionEnabled;
                  s3BucketName;
                  s3EncryptionEnabled;
                  s3KeyPrefix
                }
    let to_value x =
      structure_to_value
        [("cloudWatchLogGroupName",
           (Option.map x.cloudWatchLogGroupName ~f:String_.to_value));
        ("cloudWatchEncryptionEnabled",
          (Option.map x.cloudWatchEncryptionEnabled ~f:Boolean.to_value));
        ("s3BucketName", (Option.map x.s3BucketName ~f:String_.to_value));
        ("s3EncryptionEnabled",
          (Option.map x.s3EncryptionEnabled ~f:Boolean.to_value));
        ("s3KeyPrefix", (Option.map x.s3KeyPrefix ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3KeyPrefix =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "s3KeyPrefix") in
      let s3EncryptionEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "s3EncryptionEnabled") in
      let s3BucketName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "s3BucketName") in
      let cloudWatchEncryptionEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "cloudWatchEncryptionEnabled") in
      let cloudWatchLogGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "cloudWatchLogGroupName") in
      make ?s3KeyPrefix ?s3EncryptionEnabled ?s3BucketName
        ?cloudWatchEncryptionEnabled ?cloudWatchLogGroupName ()
    let of_json json =
      let s3KeyPrefix = field_map json "s3KeyPrefix" String_.of_json in
      let s3EncryptionEnabled =
        field_map json "s3EncryptionEnabled" Boolean.of_json in
      let s3BucketName = field_map json "s3BucketName" String_.of_json in
      let cloudWatchEncryptionEnabled =
        field_map json "cloudWatchEncryptionEnabled" Boolean.of_json in
      let cloudWatchLogGroupName =
        field_map json "cloudWatchLogGroupName" String_.of_json in
      make ?s3KeyPrefix ?s3EncryptionEnabled ?s3BucketName
        ?cloudWatchEncryptionEnabled ?cloudWatchLogGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The log configuration for the results of the execute command actions. The logs can be sent to CloudWatch Logs or an Amazon S3 bucket."]
module ExecuteCommandLogging =
  struct
    type nonrec t =
      | NONE 
      | DEFAULT 
      | OVERRIDE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NONE -> "NONE"
      | DEFAULT -> "DEFAULT"
      | OVERRIDE -> "OVERRIDE"
      | Non_static_id s -> s
    let of_string =
      function
      | "NONE" -> NONE
      | "DEFAULT" -> DEFAULT
      | "OVERRIDE" -> OVERRIDE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ExecuteCommandLogging" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ExecuteCommandLogging" j)
    let to_json = simple_to_json to_value
  end
module ClusterSettingName =
  struct
    type nonrec t =
      | ContainerInsights 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ContainerInsights -> "containerInsights"
      | Non_static_id s -> s
    let of_string =
      function
      | "containerInsights" -> ContainerInsights
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ClusterSettingName" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ClusterSettingName" j)
    let to_json = simple_to_json to_value
  end
module ManagedScalingInstanceWarmupPeriod =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:10000) >>=
             (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ManagedScalingInstanceWarmupPeriod" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ManagedScalingStatus =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ManagedScalingStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ManagedScalingStatus" j)
    let to_json = simple_to_json to_value
  end
module ManagedScalingStepSize =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:10000) >>=
             (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ManagedScalingStepSize" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ManagedScalingTargetCapacity =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ManagedScalingTargetCapacity"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Attachment =
  struct
    type nonrec t =
      {
      id: String_.t option
        [@ocaml.doc "The unique identifier for the attachment."];
      type_: String_.t option
        [@ocaml.doc
          "The type of the attachment, such as ElasticNetworkInterface."];
      status: String_.t option
        [@ocaml.doc
          "The status of the attachment. Valid values are PRECREATED, CREATED, ATTACHING, ATTACHED, DETACHING, DETACHED, DELETED, and FAILED."];
      details: AttachmentDetails.t option
        [@ocaml.doc
          "Details of the attachment. For elastic network interfaces, this includes the network interface ID, the MAC address, the subnet ID, and the private IPv4 address."]}
    let make ?id =
      fun ?type_ ->
        fun ?status ->
          fun ?details -> fun () -> { id; type_; status; details }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:String_.to_value));
        ("status", (Option.map x.status ~f:String_.to_value));
        ("details", (Option.map x.details ~f:AttachmentDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let details =
        (Option.map ~f:AttachmentDetails.of_xml)
          (Xml.child xml_arg0 "details") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      make ?details ?status ?type_ ?id ()
    let of_json json =
      let details = field_map json "details" AttachmentDetails.of_json in
      let status = field_map json "status" String_.of_json in
      let type_ = field_map json "type" String_.of_json in
      let id = field_map json "id" String_.of_json in
      make ?details ?status ?type_ ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing a container instance or task attachment."]
module Attribute =
  struct
    type nonrec t =
      {
      name: String_.t
        [@ocaml.doc
          "The name of the attribute. The name must contain between 1 and 128 characters. The name may contain letters (uppercase and lowercase), numbers, hyphens (-), underscores (_), forward slashes (/), back slashes (\\), or periods (.)."];
      value: String_.t option
        [@ocaml.doc
          "The value of the attribute. The value must contain between 1 and 128 characters. It can contain letters (uppercase and lowercase), numbers, hyphens (-), underscores (_), periods (.), at signs (\\@), forward slashes (/), back slashes (\\), colons (:), or spaces. The value can't can't start or end with a space."];
      targetType: TargetType.t option
        [@ocaml.doc
          "The type of the target to attach the attribute with. This parameter is required if you use the short form ID for a resource instead of the full ARN."];
      targetId: String_.t option
        [@ocaml.doc
          "The ID of the target. You can specify the short form ID for a resource or the full Amazon Resource Name (ARN)."]}
    let context_ = "Attribute"
    let make ?value =
      fun ?targetType ->
        fun ?targetId ->
          fun ~name -> fun () -> { value; targetType; targetId; name }
    let to_value x =
      structure_to_value
        [("name", (Some (String_.to_value x.name)));
        ("value", (Option.map x.value ~f:String_.to_value));
        ("targetType", (Option.map x.targetType ~f:TargetType.to_value));
        ("targetId", (Option.map x.targetId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "targetId") in
      let targetType =
        (Option.map ~f:TargetType.of_xml) (Xml.child xml_arg0 "targetType") in
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let name =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?targetId ?targetType ?value ~name ()
    let of_json json =
      let targetId = field_map json "targetId" String_.of_json in
      let targetType = field_map json "targetType" TargetType.of_json in
      let value = field_map json "value" String_.of_json in
      let name = field_map_exn json "name" String_.of_json in
      make ?targetId ?targetType ?value ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An attribute is a name-value pair that's associated with an Amazon ECS object. Use attributes to extend the Amazon ECS data model by adding custom metadata to your resources. For more information, see Attributes in the Amazon Elastic Container Service Developer Guide."]
module InstanceHealthCheckResultList =
  struct
    type nonrec t = InstanceHealthCheckResult.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceHealthCheckResult.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceHealthCheckResult.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceHealthCheckResultList"
        ~of_json:InstanceHealthCheckResult.of_json j
    let to_json v = composed_to_json to_value v
  end
module Resource =
  struct
    type nonrec t =
      {
      name: String_.t option
        [@ocaml.doc
          "The name of the resource, such as CPU, MEMORY, PORTS, PORTS_UDP, or a user-defined resource."];
      type_: String_.t option
        [@ocaml.doc
          "The type of the resource. Valid values: INTEGER, DOUBLE, LONG, or STRINGSET."];
      doubleValue: Double.t option
        [@ocaml.doc
          "When the doubleValue type is set, the value of the resource must be a double precision floating-point type."];
      longValue: Long.t option
        [@ocaml.doc
          "When the longValue type is set, the value of the resource must be an extended precision floating-point type."];
      integerValue: Integer.t option
        [@ocaml.doc
          "When the integerValue type is set, the value of the resource must be an integer."];
      stringSetValue: StringList.t option
        [@ocaml.doc
          "When the stringSetValue type is set, the value of the resource must be a string type."]}
    let make ?name =
      fun ?type_ ->
        fun ?doubleValue ->
          fun ?longValue ->
            fun ?integerValue ->
              fun ?stringSetValue ->
                fun () ->
                  {
                    name;
                    type_;
                    doubleValue;
                    longValue;
                    integerValue;
                    stringSetValue
                  }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:String_.to_value));
        ("doubleValue", (Option.map x.doubleValue ~f:Double.to_value));
        ("longValue", (Option.map x.longValue ~f:Long.to_value));
        ("integerValue", (Option.map x.integerValue ~f:Integer.to_value));
        ("stringSetValue",
          (Option.map x.stringSetValue ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stringSetValue =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "stringSetValue") in
      let integerValue =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "integerValue") in
      let longValue =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "longValue") in
      let doubleValue =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "doubleValue") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?stringSetValue ?integerValue ?longValue ?doubleValue ?type_ ?name
        ()
    let of_json json =
      let stringSetValue = field_map json "stringSetValue" StringList.of_json in
      let integerValue = field_map json "integerValue" Integer.of_json in
      let longValue = field_map json "longValue" Long.of_json in
      let doubleValue = field_map json "doubleValue" Double.of_json in
      let type_ = field_map json "type" String_.of_json in
      let name = field_map json "name" String_.of_json in
      make ?stringSetValue ?integerValue ?longValue ?doubleValue ?type_ ?name
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the resources available for a container instance."]
module Container =
  struct
    type nonrec t =
      {
      containerArn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the container."];
      taskArn: String_.t option [@ocaml.doc "The ARN of the task."];
      name: String_.t option [@ocaml.doc "The name of the container."];
      image: String_.t option
        [@ocaml.doc "The image used for the container."];
      imageDigest: String_.t option
        [@ocaml.doc
          "The container image manifest digest. The imageDigest is only returned if the container is using an image hosted in Amazon ECR, otherwise it is omitted."];
      runtimeId: String_.t option
        [@ocaml.doc "The ID of the Docker container."];
      lastStatus: String_.t option
        [@ocaml.doc "The last known status of the container."];
      exitCode: BoxedInteger.t option
        [@ocaml.doc "The exit code returned from the container."];
      reason: String_.t option
        [@ocaml.doc
          "A short (255 max characters) human-readable string to provide additional details about a running or stopped container."];
      networkBindings: NetworkBindings.t option
        [@ocaml.doc "The network bindings associated with the container."];
      networkInterfaces: NetworkInterfaces.t option
        [@ocaml.doc "The network interfaces associated with the container."];
      healthStatus: HealthStatus.t option
        [@ocaml.doc
          "The health status of the container. If health checks aren't configured for this container in its task definition, then it reports the health status as UNKNOWN."];
      managedAgents: ManagedAgents.t option
        [@ocaml.doc
          "The details of any Amazon ECS managed agents associated with the container."];
      cpu: String_.t option
        [@ocaml.doc
          "The number of CPU units set for the container. The value is 0 if no value was specified in the container definition when the task definition was registered."];
      memory: String_.t option
        [@ocaml.doc
          "The hard limit (in MiB) of memory set for the container."];
      memoryReservation: String_.t option
        [@ocaml.doc
          "The soft limit (in MiB) of memory set for the container."];
      gpuIds: GpuIds.t option
        [@ocaml.doc "The IDs of each GPU assigned to the container."]}
    let make ?containerArn =
      fun ?taskArn ->
        fun ?name ->
          fun ?image ->
            fun ?imageDigest ->
              fun ?runtimeId ->
                fun ?lastStatus ->
                  fun ?exitCode ->
                    fun ?reason ->
                      fun ?networkBindings ->
                        fun ?networkInterfaces ->
                          fun ?healthStatus ->
                            fun ?managedAgents ->
                              fun ?cpu ->
                                fun ?memory ->
                                  fun ?memoryReservation ->
                                    fun ?gpuIds ->
                                      fun () ->
                                        {
                                          containerArn;
                                          taskArn;
                                          name;
                                          image;
                                          imageDigest;
                                          runtimeId;
                                          lastStatus;
                                          exitCode;
                                          reason;
                                          networkBindings;
                                          networkInterfaces;
                                          healthStatus;
                                          managedAgents;
                                          cpu;
                                          memory;
                                          memoryReservation;
                                          gpuIds
                                        }
    let to_value x =
      structure_to_value
        [("containerArn", (Option.map x.containerArn ~f:String_.to_value));
        ("taskArn", (Option.map x.taskArn ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("image", (Option.map x.image ~f:String_.to_value));
        ("imageDigest", (Option.map x.imageDigest ~f:String_.to_value));
        ("runtimeId", (Option.map x.runtimeId ~f:String_.to_value));
        ("lastStatus", (Option.map x.lastStatus ~f:String_.to_value));
        ("exitCode", (Option.map x.exitCode ~f:BoxedInteger.to_value));
        ("reason", (Option.map x.reason ~f:String_.to_value));
        ("networkBindings",
          (Option.map x.networkBindings ~f:NetworkBindings.to_value));
        ("networkInterfaces",
          (Option.map x.networkInterfaces ~f:NetworkInterfaces.to_value));
        ("healthStatus",
          (Option.map x.healthStatus ~f:HealthStatus.to_value));
        ("managedAgents",
          (Option.map x.managedAgents ~f:ManagedAgents.to_value));
        ("cpu", (Option.map x.cpu ~f:String_.to_value));
        ("memory", (Option.map x.memory ~f:String_.to_value));
        ("memoryReservation",
          (Option.map x.memoryReservation ~f:String_.to_value));
        ("gpuIds", (Option.map x.gpuIds ~f:GpuIds.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gpuIds =
        (Option.map ~f:GpuIds.of_xml) (Xml.child xml_arg0 "gpuIds") in
      let memoryReservation =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "memoryReservation") in
      let memory =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "memory") in
      let cpu = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cpu") in
      let managedAgents =
        (Option.map ~f:ManagedAgents.of_xml)
          (Xml.child xml_arg0 "managedAgents") in
      let healthStatus =
        (Option.map ~f:HealthStatus.of_xml)
          (Xml.child xml_arg0 "healthStatus") in
      let networkInterfaces =
        (Option.map ~f:NetworkInterfaces.of_xml)
          (Xml.child xml_arg0 "networkInterfaces") in
      let networkBindings =
        (Option.map ~f:NetworkBindings.of_xml)
          (Xml.child xml_arg0 "networkBindings") in
      let reason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "reason") in
      let exitCode =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "exitCode") in
      let lastStatus =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "lastStatus") in
      let runtimeId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "runtimeId") in
      let imageDigest =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageDigest") in
      let image = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "image") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let taskArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskArn") in
      let containerArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "containerArn") in
      make ?gpuIds ?memoryReservation ?memory ?cpu ?managedAgents
        ?healthStatus ?networkInterfaces ?networkBindings ?reason ?exitCode
        ?lastStatus ?runtimeId ?imageDigest ?image ?name ?taskArn
        ?containerArn ()
    let of_json json =
      let gpuIds = field_map json "gpuIds" GpuIds.of_json in
      let memoryReservation =
        field_map json "memoryReservation" String_.of_json in
      let memory = field_map json "memory" String_.of_json in
      let cpu = field_map json "cpu" String_.of_json in
      let managedAgents =
        field_map json "managedAgents" ManagedAgents.of_json in
      let healthStatus = field_map json "healthStatus" HealthStatus.of_json in
      let networkInterfaces =
        field_map json "networkInterfaces" NetworkInterfaces.of_json in
      let networkBindings =
        field_map json "networkBindings" NetworkBindings.of_json in
      let reason = field_map json "reason" String_.of_json in
      let exitCode = field_map json "exitCode" BoxedInteger.of_json in
      let lastStatus = field_map json "lastStatus" String_.of_json in
      let runtimeId = field_map json "runtimeId" String_.of_json in
      let imageDigest = field_map json "imageDigest" String_.of_json in
      let image = field_map json "image" String_.of_json in
      let name = field_map json "name" String_.of_json in
      let taskArn = field_map json "taskArn" String_.of_json in
      let containerArn = field_map json "containerArn" String_.of_json in
      make ?gpuIds ?memoryReservation ?memory ?cpu ?managedAgents
        ?healthStatus ?networkInterfaces ?networkBindings ?reason ?exitCode
        ?lastStatus ?runtimeId ?imageDigest ?image ?name ?taskArn
        ?containerArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A Docker container that's part of a task."]
module InferenceAccelerator =
  struct
    type nonrec t =
      {
      deviceName: String_.t
        [@ocaml.doc
          "The Elastic Inference accelerator device name. The deviceName must also be referenced in a container definition as a ResourceRequirement."];
      deviceType: String_.t
        [@ocaml.doc "The Elastic Inference accelerator type to use."]}
    let context_ = "InferenceAccelerator"
    let make ~deviceName =
      fun ~deviceType -> fun () -> { deviceName; deviceType }
    let to_value x =
      structure_to_value
        [("deviceName", (Some (String_.to_value x.deviceName)));
        ("deviceType", (Some (String_.to_value x.deviceType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deviceType =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "deviceType") in
      let deviceName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "deviceName") in
      make ~deviceType ~deviceName ()
    let of_json json =
      let deviceType = field_map_exn json "deviceType" String_.of_json in
      let deviceName = field_map_exn json "deviceName" String_.of_json in
      make ~deviceType ~deviceName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details on an Elastic Inference accelerator. For more information, see Working with Amazon Elastic Inference on Amazon ECS in the Amazon Elastic Container Service Developer Guide."]
module ContainerOverrides =
  struct
    type nonrec t = ContainerOverride.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ContainerOverride.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerOverride.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerOverrides"
        ~of_json:ContainerOverride.of_json j
    let to_json v = composed_to_json to_value v
  end
module EphemeralStorage =
  struct
    type nonrec t =
      {
      sizeInGiB: Integer.t
        [@ocaml.doc
          "The total amount, in GiB, of ephemeral storage to set for the task. The minimum supported value is 21 GiB and the maximum supported value is 200 GiB."]}
    let context_ = "EphemeralStorage"
    let make ~sizeInGiB = fun () -> { sizeInGiB }
    let to_value x =
      structure_to_value
        [("sizeInGiB", (Some (Integer.to_value x.sizeInGiB)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sizeInGiB =
        Integer.of_xml (Xml.child_exn ~context:context_ xml_arg0 "sizeInGiB") in
      make ~sizeInGiB ()
    let of_json json =
      let sizeInGiB = field_map_exn json "sizeInGiB" Integer.of_json in
      make ~sizeInGiB ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The amount of ephemeral storage to allocate for the task. This parameter is used to expand the total amount of ephemeral storage available, beyond the default amount, for tasks hosted on Fargate. For more information, see Fargate task storage in the Amazon ECS User Guide for Fargate. This parameter is only supported for tasks hosted on Fargate using Linux platform version 1.4.0 or later. This parameter is not supported for Windows containers on Fargate."]
module InferenceAcceleratorOverrides =
  struct
    type nonrec t = InferenceAcceleratorOverride.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InferenceAcceleratorOverride.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InferenceAcceleratorOverride.of_xml)
    let of_json j =
      list_of_json ~kind:"InferenceAcceleratorOverrides"
        ~of_json:InferenceAcceleratorOverride.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerDependencies =
  struct
    type nonrec t = ContainerDependency.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ContainerDependency.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerDependency.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerDependencies"
        ~of_json:ContainerDependency.of_json j
    let to_json v = composed_to_json to_value v
  end
module DockerLabelsMap =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module FirelensConfiguration =
  struct
    type nonrec t =
      {
      type_: FirelensConfigurationType.t
        [@ocaml.doc
          "The log router to use. The valid values are fluentd or fluentbit."];
      options: FirelensConfigurationOptionsMap.t option
        [@ocaml.doc
          "The options to use when configuring the log router. This field is optional and can be used to specify a custom configuration file or to add additional metadata, such as the task, task definition, cluster, and container instance details to the log event. If specified, the syntax to use is \"options\":\\{\"enable-ecs-log-metadata\":\"true|false\",\"config-file-type:\"s3|file\",\"config-file-value\":\"arn:aws:s3:::mybucket/fluent.conf|filepath\"\\}. For more information, see Creating a Task Definition that Uses a FireLens Configuration in the Amazon Elastic Container Service Developer Guide. Tasks hosted on Fargate only support the file configuration file type."]}
    let context_ = "FirelensConfiguration"
    let make ?options = fun ~type_ -> fun () -> { options; type_ }
    let to_value x =
      structure_to_value
        [("type", (Some (FirelensConfigurationType.to_value x.type_)));
        ("options",
          (Option.map x.options ~f:FirelensConfigurationOptionsMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let options =
        (Option.map ~f:FirelensConfigurationOptionsMap.of_xml)
          (Xml.child xml_arg0 "options") in
      let type_ =
        FirelensConfigurationType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ?options ~type_ ()
    let of_json json =
      let options =
        field_map json "options" FirelensConfigurationOptionsMap.of_json in
      let type_ = field_map_exn json "type" FirelensConfigurationType.of_json in
      make ?options ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The FireLens configuration for the container. This is used to specify and configure a log router for container logs. For more information, see Custom Log Routing in the Amazon Elastic Container Service Developer Guide."]
module HealthCheck =
  struct
    type nonrec t =
      {
      command: StringList.t
        [@ocaml.doc
          "A string array representing the command that the container runs to determine if it is healthy. The string array must start with CMD to execute the command arguments directly, or CMD-SHELL to run the command with the container's default shell. When you use the Amazon Web Services Management Console JSON panel, the Command Line Interface, or the APIs, enclose the list of commands in brackets. \\[ \"CMD-SHELL\", \"curl -f http://localhost/ || exit 1\" \\] You don't need to include the brackets when you use the Amazon Web Services Management Console. \"CMD-SHELL\", \"curl -f http://localhost/ || exit 1\" An exit code of 0 indicates success, and non-zero exit code indicates failure. For more information, see HealthCheck in the Create a container section of the Docker Remote API."];
      interval: BoxedInteger.t option
        [@ocaml.doc
          "The time period in seconds between each health check execution. You may specify between 5 and 300 seconds. The default value is 30 seconds."];
      timeout: BoxedInteger.t option
        [@ocaml.doc
          "The time period in seconds to wait for a health check to succeed before it is considered a failure. You may specify between 2 and 60 seconds. The default value is 5."];
      retries: BoxedInteger.t option
        [@ocaml.doc
          "The number of times to retry a failed health check before the container is considered unhealthy. You may specify between 1 and 10 retries. The default value is 3."];
      startPeriod: BoxedInteger.t option
        [@ocaml.doc
          "The optional grace period to provide containers time to bootstrap before failed health checks count towards the maximum number of retries. You can specify between 0 and 300 seconds. By default, the startPeriod is disabled. If a health check succeeds within the startPeriod, then the container is considered healthy and any subsequent failures count toward the maximum number of retries."]}
    let context_ = "HealthCheck"
    let make ?interval =
      fun ?timeout ->
        fun ?retries ->
          fun ?startPeriod ->
            fun ~command ->
              fun () -> { interval; timeout; retries; startPeriod; command }
    let to_value x =
      structure_to_value
        [("command", (Some (StringList.to_value x.command)));
        ("interval", (Option.map x.interval ~f:BoxedInteger.to_value));
        ("timeout", (Option.map x.timeout ~f:BoxedInteger.to_value));
        ("retries", (Option.map x.retries ~f:BoxedInteger.to_value));
        ("startPeriod", (Option.map x.startPeriod ~f:BoxedInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startPeriod =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "startPeriod") in
      let retries =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "retries") in
      let timeout =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "timeout") in
      let interval =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "interval") in
      let command =
        StringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "command") in
      make ?startPeriod ?retries ?timeout ?interval ~command ()
    let of_json json =
      let startPeriod = field_map json "startPeriod" BoxedInteger.of_json in
      let retries = field_map json "retries" BoxedInteger.of_json in
      let timeout = field_map json "timeout" BoxedInteger.of_json in
      let interval = field_map json "interval" BoxedInteger.of_json in
      let command = field_map_exn json "command" StringList.of_json in
      make ?startPeriod ?retries ?timeout ?interval ~command ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing a container health check. Health check parameters that are specified in a container definition override any Docker health checks that exist in the container image (such as those specified in a parent image or from the image's Dockerfile). The Amazon ECS container agent only monitors and reports on the health checks specified in the task definition. Amazon ECS does not monitor Docker health checks that are embedded in a container image and not specified in the container definition. Health check parameters that are specified in a container definition override any Docker health checks that exist in the container image. You can view the health status of both individual containers and a task with the DescribeTasks API operation or when viewing the task details in the console. The following describes the possible healthStatus values for a container: HEALTHY-The container health check has passed successfully. UNHEALTHY-The container health check has failed. UNKNOWN-The container health check is being evaluated or there's no container health check defined. The following describes the possible healthStatus values for a task. The container health check status of nonessential containers do not have an effect on the health status of a task. HEALTHY-All essential containers within the task have passed their health checks. UNHEALTHY-One or more essential containers have failed their health check. UNKNOWN-The essential containers within the task are still having their health checks evaluated or there are no container health checks defined. If a task is run manually, and not as part of a service, the task will continue its lifecycle regardless of its health status. For tasks that are part of a service, if the task reports as unhealthy then the task will be stopped and the service scheduler will replace it. The following are notes about container health check support: Container health checks require version 1.17.0 or greater of the Amazon ECS container agent. For more information, see Updating the Amazon ECS Container Agent. Container health checks are supported for Fargate tasks if you're using platform version 1.1.0 or greater. For more information, see Fargate Platform Versions. Container health checks aren't supported for tasks that are part of a service that's configured to use a Classic Load Balancer."]
module HostEntryList =
  struct
    type nonrec t = HostEntry.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:HostEntry.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:HostEntry.of_xml)
    let of_json j =
      list_of_json ~kind:"HostEntryList" ~of_json:HostEntry.of_json j
    let to_json v = composed_to_json to_value v
  end
module LinuxParameters =
  struct
    type nonrec t =
      {
      capabilities: KernelCapabilities.t option
        [@ocaml.doc
          "The Linux capabilities for the container that are added to or dropped from the default configuration provided by Docker. For tasks that use the Fargate launch type, capabilities is supported for all platform versions but the add parameter is only supported if using platform version 1.4.0 or later."];
      devices: DevicesList.t option
        [@ocaml.doc
          "Any host devices to expose to the container. This parameter maps to Devices in the Create a container section of the Docker Remote API and the --device option to docker run. If you're using tasks that use the Fargate launch type, the devices parameter isn't supported."];
      initProcessEnabled: BoxedBoolean.t option
        [@ocaml.doc
          "Run an init process inside the container that forwards signals and reaps processes. This parameter maps to the --init option to docker run. This parameter requires version 1.25 of the Docker Remote API or greater on your container instance. To check the Docker Remote API version on your container instance, log in to your container instance and run the following command: sudo docker version --format '\\{\\{.Server.APIVersion\\}\\}'"];
      sharedMemorySize: BoxedInteger.t option
        [@ocaml.doc
          "The value for the size (in MiB) of the /dev/shm volume. This parameter maps to the --shm-size option to docker run. If you are using tasks that use the Fargate launch type, the sharedMemorySize parameter is not supported."];
      tmpfs: TmpfsList.t option
        [@ocaml.doc
          "The container path, mount options, and size (in MiB) of the tmpfs mount. This parameter maps to the --tmpfs option to docker run. If you're using tasks that use the Fargate launch type, the tmpfs parameter isn't supported."];
      maxSwap: BoxedInteger.t option
        [@ocaml.doc
          "The total amount of swap memory (in MiB) a container can use. This parameter will be translated to the --memory-swap option to docker run where the value would be the sum of the container memory plus the maxSwap value. If a maxSwap value of 0 is specified, the container will not use swap. Accepted values are 0 or any positive integer. If the maxSwap parameter is omitted, the container will use the swap configuration for the container instance it is running on. A maxSwap value must be set for the swappiness parameter to be used. If you're using tasks that use the Fargate launch type, the maxSwap parameter isn't supported."];
      swappiness: BoxedInteger.t option
        [@ocaml.doc
          "This allows you to tune a container's memory swappiness behavior. A swappiness value of 0 will cause swapping to not happen unless absolutely necessary. A swappiness value of 100 will cause pages to be swapped very aggressively. Accepted values are whole numbers between 0 and 100. If the swappiness parameter is not specified, a default value of 60 is used. If a value is not specified for maxSwap then this parameter is ignored. This parameter maps to the --memory-swappiness option to docker run. If you're using tasks that use the Fargate launch type, the swappiness parameter isn't supported."]}
    let make ?capabilities =
      fun ?devices ->
        fun ?initProcessEnabled ->
          fun ?sharedMemorySize ->
            fun ?tmpfs ->
              fun ?maxSwap ->
                fun ?swappiness ->
                  fun () ->
                    {
                      capabilities;
                      devices;
                      initProcessEnabled;
                      sharedMemorySize;
                      tmpfs;
                      maxSwap;
                      swappiness
                    }
    let to_value x =
      structure_to_value
        [("capabilities",
           (Option.map x.capabilities ~f:KernelCapabilities.to_value));
        ("devices", (Option.map x.devices ~f:DevicesList.to_value));
        ("initProcessEnabled",
          (Option.map x.initProcessEnabled ~f:BoxedBoolean.to_value));
        ("sharedMemorySize",
          (Option.map x.sharedMemorySize ~f:BoxedInteger.to_value));
        ("tmpfs", (Option.map x.tmpfs ~f:TmpfsList.to_value));
        ("maxSwap", (Option.map x.maxSwap ~f:BoxedInteger.to_value));
        ("swappiness", (Option.map x.swappiness ~f:BoxedInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let swappiness =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "swappiness") in
      let maxSwap =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "maxSwap") in
      let tmpfs =
        (Option.map ~f:TmpfsList.of_xml) (Xml.child xml_arg0 "tmpfs") in
      let sharedMemorySize =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "sharedMemorySize") in
      let initProcessEnabled =
        (Option.map ~f:BoxedBoolean.of_xml)
          (Xml.child xml_arg0 "initProcessEnabled") in
      let devices =
        (Option.map ~f:DevicesList.of_xml) (Xml.child xml_arg0 "devices") in
      let capabilities =
        (Option.map ~f:KernelCapabilities.of_xml)
          (Xml.child xml_arg0 "capabilities") in
      make ?swappiness ?maxSwap ?tmpfs ?sharedMemorySize ?initProcessEnabled
        ?devices ?capabilities ()
    let of_json json =
      let swappiness = field_map json "swappiness" BoxedInteger.of_json in
      let maxSwap = field_map json "maxSwap" BoxedInteger.of_json in
      let tmpfs = field_map json "tmpfs" TmpfsList.of_json in
      let sharedMemorySize =
        field_map json "sharedMemorySize" BoxedInteger.of_json in
      let initProcessEnabled =
        field_map json "initProcessEnabled" BoxedBoolean.of_json in
      let devices = field_map json "devices" DevicesList.of_json in
      let capabilities =
        field_map json "capabilities" KernelCapabilities.of_json in
      make ?swappiness ?maxSwap ?tmpfs ?sharedMemorySize ?initProcessEnabled
        ?devices ?capabilities ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Linux-specific options that are applied to the container, such as Linux KernelCapabilities."]
module LogConfiguration =
  struct
    type nonrec t =
      {
      logDriver: LogDriver.t
        [@ocaml.doc
          "The log driver to use for the container. For tasks on Fargate, the supported log drivers are awslogs, splunk, and awsfirelens. For tasks hosted on Amazon EC2 instances, the supported log drivers are awslogs, fluentd, gelf, json-file, journald, logentries,syslog, splunk, and awsfirelens. For more information about using the awslogs log driver, see Using the awslogs log driver in the Amazon Elastic Container Service Developer Guide. For more information about using the awsfirelens log driver, see Custom log routing in the Amazon Elastic Container Service Developer Guide. If you have a custom driver that isn't listed, you can fork the Amazon ECS container agent project that's available on GitHub and customize it to work with that driver. We encourage you to submit pull requests for changes that you would like to have included. However, we don't currently provide support for running modified copies of this software."];
      options: LogConfigurationOptionsMap.t option
        [@ocaml.doc
          "The configuration options to send to the log driver. This parameter requires version 1.19 of the Docker Remote API or greater on your container instance. To check the Docker Remote API version on your container instance, log in to your container instance and run the following command: sudo docker version --format '\\{\\{.Server.APIVersion\\}\\}'"];
      secretOptions: SecretList.t option
        [@ocaml.doc
          "The secrets to pass to the log configuration. For more information, see Specifying Sensitive Data in the Amazon Elastic Container Service Developer Guide."]}
    let context_ = "LogConfiguration"
    let make ?options =
      fun ?secretOptions ->
        fun ~logDriver -> fun () -> { options; secretOptions; logDriver }
    let to_value x =
      structure_to_value
        [("logDriver", (Some (LogDriver.to_value x.logDriver)));
        ("options",
          (Option.map x.options ~f:LogConfigurationOptionsMap.to_value));
        ("secretOptions",
          (Option.map x.secretOptions ~f:SecretList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let secretOptions =
        (Option.map ~f:SecretList.of_xml)
          (Xml.child xml_arg0 "secretOptions") in
      let options =
        (Option.map ~f:LogConfigurationOptionsMap.of_xml)
          (Xml.child xml_arg0 "options") in
      let logDriver =
        LogDriver.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "logDriver") in
      make ?secretOptions ?options ~logDriver ()
    let of_json json =
      let secretOptions = field_map json "secretOptions" SecretList.of_json in
      let options =
        field_map json "options" LogConfigurationOptionsMap.of_json in
      let logDriver = field_map_exn json "logDriver" LogDriver.of_json in
      make ?secretOptions ?options ~logDriver ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The log configuration for the container. This parameter maps to LogConfig in the Create a container section of the Docker Remote API and the --log-driver option to docker run . By default, containers use the same logging driver that the Docker daemon uses. However, the container might use a different logging driver than the Docker daemon by specifying a log driver configuration in the container definition. For more information about the options for different supported log drivers, see Configure logging drivers in the Docker documentation. Understand the following when specifying a log configuration for your containers. Amazon ECS currently supports a subset of the logging drivers available to the Docker daemon (shown in the valid values below). Additional log drivers may be available in future releases of the Amazon ECS container agent. This parameter requires version 1.18 of the Docker Remote API or greater on your container instance. For tasks that are hosted on Amazon EC2 instances, the Amazon ECS container agent must register the available logging drivers with the ECS_AVAILABLE_LOGGING_DRIVERS environment variable before containers placed on that instance can use these log configuration options. For more information, see Amazon ECS container agent configuration in the Amazon Elastic Container Service Developer Guide. For tasks that are on Fargate, because you don't have access to the underlying infrastructure your tasks are hosted on, any additional software needed must be installed outside of the task. For example, the Fluentd output aggregators or a remote host running Logstash to send Gelf logs to."]
module MountPointList =
  struct
    type nonrec t = MountPoint.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:MountPoint.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MountPoint.of_xml)
    let of_json j =
      list_of_json ~kind:"MountPointList" ~of_json:MountPoint.of_json j
    let to_json v = composed_to_json to_value v
  end
module PortMappingList =
  struct
    type nonrec t = PortMapping.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PortMapping.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PortMapping.of_xml)
    let of_json j =
      list_of_json ~kind:"PortMappingList" ~of_json:PortMapping.of_json j
    let to_json v = composed_to_json to_value v
  end
module RepositoryCredentials =
  struct
    type nonrec t =
      {
      credentialsParameter: String_.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the secret containing the private repository credentials. When you use the Amazon ECS API, CLI, or Amazon Web Services SDK, if the secret exists in the same Region as the task that you're launching then you can use either the full ARN or the name of the secret. When you use the Amazon Web Services Management Console, you must specify the full ARN of the secret."]}
    let context_ = "RepositoryCredentials"
    let make ~credentialsParameter = fun () -> { credentialsParameter }
    let to_value x =
      structure_to_value
        [("credentialsParameter",
           (Some (String_.to_value x.credentialsParameter)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let credentialsParameter =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "credentialsParameter") in
      make ~credentialsParameter ()
    let of_json json =
      let credentialsParameter =
        field_map_exn json "credentialsParameter" String_.of_json in
      make ~credentialsParameter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The repository credentials for private registry authentication."]
module SystemControls =
  struct
    type nonrec t = SystemControl.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SystemControl.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SystemControl.of_xml)
    let of_json j =
      list_of_json ~kind:"SystemControls" ~of_json:SystemControl.of_json j
    let to_json v = composed_to_json to_value v
  end
module UlimitList =
  struct
    type nonrec t = Ulimit.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Ulimit.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Ulimit.of_xml)
    let of_json j = list_of_json ~kind:"UlimitList" ~of_json:Ulimit.of_json j
    let to_json v = composed_to_json to_value v
  end
module VolumeFromList =
  struct
    type nonrec t = VolumeFrom.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:VolumeFrom.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:VolumeFrom.of_xml)
    let of_json j =
      list_of_json ~kind:"VolumeFromList" ~of_json:VolumeFrom.of_json j
    let to_json v = composed_to_json to_value v
  end
module TaskDefinitionPlacementConstraintType =
  struct
    type nonrec t =
      | MemberOf 
      | Non_static_id of string 
    let make i = i
    let to_string = function | MemberOf -> "memberOf" | Non_static_id s -> s
    let of_string = function | "memberOf" -> MemberOf | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration TaskDefinitionPlacementConstraintType" xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"TaskDefinitionPlacementConstraintType" j)
    let to_json = simple_to_json to_value
  end
module DockerVolumeConfiguration =
  struct
    type nonrec t =
      {
      scope: Scope.t option
        [@ocaml.doc
          "The scope for the Docker volume that determines its lifecycle. Docker volumes that are scoped to a task are automatically provisioned when the task starts and destroyed when the task stops. Docker volumes that are scoped as shared persist after the task stops."];
      autoprovision: BoxedBoolean.t option
        [@ocaml.doc
          "If this value is true, the Docker volume is created if it doesn't already exist. This field is only used if the scope is shared."];
      driver: String_.t option
        [@ocaml.doc
          "The Docker volume driver to use. The driver value must match the driver name provided by Docker because it is used for task placement. If the driver was installed using the Docker plugin CLI, use docker plugin ls to retrieve the driver name from your container instance. If the driver was installed using another method, use Docker plugin discovery to retrieve the driver name. For more information, see Docker plugin discovery. This parameter maps to Driver in the Create a volume section of the Docker Remote API and the xxdriver option to docker volume create."];
      driverOpts: StringMap.t option
        [@ocaml.doc
          "A map of Docker driver-specific options passed through. This parameter maps to DriverOpts in the Create a volume section of the Docker Remote API and the xxopt option to docker volume create."];
      labels: StringMap.t option
        [@ocaml.doc
          "Custom metadata to add to your Docker volume. This parameter maps to Labels in the Create a volume section of the Docker Remote API and the xxlabel option to docker volume create."]}
    let make ?scope =
      fun ?autoprovision ->
        fun ?driver ->
          fun ?driverOpts ->
            fun ?labels ->
              fun () -> { scope; autoprovision; driver; driverOpts; labels }
    let to_value x =
      structure_to_value
        [("scope", (Option.map x.scope ~f:Scope.to_value));
        ("autoprovision",
          (Option.map x.autoprovision ~f:BoxedBoolean.to_value));
        ("driver", (Option.map x.driver ~f:String_.to_value));
        ("driverOpts", (Option.map x.driverOpts ~f:StringMap.to_value));
        ("labels", (Option.map x.labels ~f:StringMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let labels =
        (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "labels") in
      let driverOpts =
        (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "driverOpts") in
      let driver =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "driver") in
      let autoprovision =
        (Option.map ~f:BoxedBoolean.of_xml)
          (Xml.child xml_arg0 "autoprovision") in
      let scope = (Option.map ~f:Scope.of_xml) (Xml.child xml_arg0 "scope") in
      make ?labels ?driverOpts ?driver ?autoprovision ?scope ()
    let of_json json =
      let labels = field_map json "labels" StringMap.of_json in
      let driverOpts = field_map json "driverOpts" StringMap.of_json in
      let driver = field_map json "driver" String_.of_json in
      let autoprovision = field_map json "autoprovision" BoxedBoolean.of_json in
      let scope = field_map json "scope" Scope.of_json in
      make ?labels ?driverOpts ?driver ?autoprovision ?scope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This parameter is specified when you're using Docker volumes. Docker volumes are only supported when you're using the EC2 launch type. Windows containers only support the use of the local driver. To use bind mounts, specify a host instead."]
module EFSVolumeConfiguration =
  struct
    type nonrec t =
      {
      fileSystemId: String_.t
        [@ocaml.doc "The Amazon EFS file system ID to use."];
      rootDirectory: String_.t option
        [@ocaml.doc
          "The directory within the Amazon EFS file system to mount as the root directory inside the host. If this parameter is omitted, the root of the Amazon EFS volume will be used. Specifying / will have the same effect as omitting this parameter. If an EFS access point is specified in the authorizationConfig, the root directory parameter must either be omitted or set to / which will enforce the path set on the EFS access point."];
      transitEncryption: EFSTransitEncryption.t option
        [@ocaml.doc
          "Determines whether to use encryption for Amazon EFS data in transit between the Amazon ECS host and the Amazon EFS server. Transit encryption must be enabled if Amazon EFS IAM authorization is used. If this parameter is omitted, the default value of DISABLED is used. For more information, see Encrypting Data in Transit in the Amazon Elastic File System User Guide."];
      transitEncryptionPort: BoxedInteger.t option
        [@ocaml.doc
          "The port to use when sending encrypted data between the Amazon ECS host and the Amazon EFS server. If you do not specify a transit encryption port, it will use the port selection strategy that the Amazon EFS mount helper uses. For more information, see EFS Mount Helper in the Amazon Elastic File System User Guide."];
      authorizationConfig: EFSAuthorizationConfig.t option
        [@ocaml.doc
          "The authorization configuration details for the Amazon EFS file system."]}
    let context_ = "EFSVolumeConfiguration"
    let make ?rootDirectory =
      fun ?transitEncryption ->
        fun ?transitEncryptionPort ->
          fun ?authorizationConfig ->
            fun ~fileSystemId ->
              fun () ->
                {
                  rootDirectory;
                  transitEncryption;
                  transitEncryptionPort;
                  authorizationConfig;
                  fileSystemId
                }
    let to_value x =
      structure_to_value
        [("fileSystemId", (Some (String_.to_value x.fileSystemId)));
        ("rootDirectory", (Option.map x.rootDirectory ~f:String_.to_value));
        ("transitEncryption",
          (Option.map x.transitEncryption ~f:EFSTransitEncryption.to_value));
        ("transitEncryptionPort",
          (Option.map x.transitEncryptionPort ~f:BoxedInteger.to_value));
        ("authorizationConfig",
          (Option.map x.authorizationConfig
             ~f:EFSAuthorizationConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let authorizationConfig =
        (Option.map ~f:EFSAuthorizationConfig.of_xml)
          (Xml.child xml_arg0 "authorizationConfig") in
      let transitEncryptionPort =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "transitEncryptionPort") in
      let transitEncryption =
        (Option.map ~f:EFSTransitEncryption.of_xml)
          (Xml.child xml_arg0 "transitEncryption") in
      let rootDirectory =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "rootDirectory") in
      let fileSystemId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "fileSystemId") in
      make ?authorizationConfig ?transitEncryptionPort ?transitEncryption
        ?rootDirectory ~fileSystemId ()
    let of_json json =
      let authorizationConfig =
        field_map json "authorizationConfig" EFSAuthorizationConfig.of_json in
      let transitEncryptionPort =
        field_map json "transitEncryptionPort" BoxedInteger.of_json in
      let transitEncryption =
        field_map json "transitEncryption" EFSTransitEncryption.of_json in
      let rootDirectory = field_map json "rootDirectory" String_.of_json in
      let fileSystemId = field_map_exn json "fileSystemId" String_.of_json in
      make ?authorizationConfig ?transitEncryptionPort ?transitEncryption
        ?rootDirectory ~fileSystemId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This parameter is specified when you're using an Amazon Elastic File System file system for task storage. For more information, see Amazon EFS Volumes in the Amazon Elastic Container Service Developer Guide."]
module FSxWindowsFileServerVolumeConfiguration =
  struct
    type nonrec t =
      {
      fileSystemId: String_.t
        [@ocaml.doc
          "The Amazon FSx for Windows File Server file system ID to use."];
      rootDirectory: String_.t
        [@ocaml.doc
          "The directory within the Amazon FSx for Windows File Server file system to mount as the root directory inside the host."];
      authorizationConfig: FSxWindowsFileServerAuthorizationConfig.t
        [@ocaml.doc
          "The authorization configuration details for the Amazon FSx for Windows File Server file system."]}
    let context_ = "FSxWindowsFileServerVolumeConfiguration"
    let make ~fileSystemId =
      fun ~rootDirectory ->
        fun ~authorizationConfig ->
          fun () -> { fileSystemId; rootDirectory; authorizationConfig }
    let to_value x =
      structure_to_value
        [("fileSystemId", (Some (String_.to_value x.fileSystemId)));
        ("rootDirectory", (Some (String_.to_value x.rootDirectory)));
        ("authorizationConfig",
          (Some
             (FSxWindowsFileServerAuthorizationConfig.to_value
                x.authorizationConfig)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let authorizationConfig =
        FSxWindowsFileServerAuthorizationConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "authorizationConfig") in
      let rootDirectory =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "rootDirectory") in
      let fileSystemId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "fileSystemId") in
      make ~authorizationConfig ~rootDirectory ~fileSystemId ()
    let of_json json =
      let authorizationConfig =
        field_map_exn json "authorizationConfig"
          FSxWindowsFileServerAuthorizationConfig.of_json in
      let rootDirectory = field_map_exn json "rootDirectory" String_.of_json in
      let fileSystemId = field_map_exn json "fileSystemId" String_.of_json in
      make ~authorizationConfig ~rootDirectory ~fileSystemId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This parameter is specified when you're using Amazon FSx for Windows File Server file system for task storage. For more information and the input format, see Amazon FSx for Windows File Server Volumes in the Amazon Elastic Container Service Developer Guide."]
module HostVolumeProperties =
  struct
    type nonrec t =
      {
      sourcePath: String_.t option
        [@ocaml.doc
          "When the host parameter is used, specify a sourcePath to declare the path on the host container instance that's presented to the container. If this parameter is empty, then the Docker daemon has assigned a host path for you. If the host parameter contains a sourcePath file location, then the data volume persists at the specified location on the host container instance until you delete it manually. If the sourcePath value doesn't exist on the host container instance, the Docker daemon creates it. If the location does exist, the contents of the source path folder are exported. If you're using the Fargate launch type, the sourcePath parameter is not supported."]}
    let make ?sourcePath = fun () -> { sourcePath }
    let to_value x =
      structure_to_value
        [("sourcePath", (Option.map x.sourcePath ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourcePath =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sourcePath") in
      make ?sourcePath ()
    let of_json json =
      let sourcePath = field_map json "sourcePath" String_.of_json in
      make ?sourcePath ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Details on a container instance bind mount host volume."]
module DeploymentCircuitBreaker =
  struct
    type nonrec t =
      {
      enable: Boolean.t
        [@ocaml.doc
          "Determines whether to use the deployment circuit breaker logic for the service."];
      rollback: Boolean.t
        [@ocaml.doc
          "Determines whether to configure Amazon ECS to roll back the service if a service deployment fails. If rollback is enabled, when a service deployment fails, the service is rolled back to the last deployment that completed successfully."]}
    let context_ = "DeploymentCircuitBreaker"
    let make ~enable = fun ~rollback -> fun () -> { enable; rollback }
    let to_value x =
      structure_to_value
        [("enable", (Some (Boolean.to_value x.enable)));
        ("rollback", (Some (Boolean.to_value x.rollback)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rollback =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "rollback") in
      let enable =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "enable") in
      make ~rollback ~enable ()
    let of_json json =
      let rollback = field_map_exn json "rollback" Boolean.of_json in
      let enable = field_map_exn json "enable" Boolean.of_json in
      make ~rollback ~enable ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The deployment circuit breaker can only be used for services using the rolling update (ECS) deployment type that aren't behind a Classic Load Balancer. The deployment circuit breaker determines whether a service deployment will fail if the service can't reach a steady state. If enabled, a service deployment will transition to a failed state and stop launching new tasks. You can also configure Amazon ECS to roll back your service to the last completed deployment after a failure. For more information, see Rolling update in the Amazon Elastic Container Service Developer Guide."]
module DeploymentControllerType =
  struct
    type nonrec t =
      | ECS 
      | CODE_DEPLOY 
      | EXTERNAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ECS -> "ECS"
      | CODE_DEPLOY -> "CODE_DEPLOY"
      | EXTERNAL -> "EXTERNAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "ECS" -> ECS
      | "CODE_DEPLOY" -> CODE_DEPLOY
      | "EXTERNAL" -> EXTERNAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DeploymentControllerType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"DeploymentControllerType" j)
    let to_json = simple_to_json to_value
  end
module Deployment =
  struct
    type nonrec t =
      {
      id: String_.t option [@ocaml.doc "The ID of the deployment."];
      status: String_.t option
        [@ocaml.doc
          "The status of the deployment. The following describes each state. PRIMARY The most recent deployment of a service. ACTIVE A service deployment that still has running tasks, but are in the process of being replaced with a new PRIMARY deployment. INACTIVE A deployment that has been completely replaced."];
      taskDefinition: String_.t option
        [@ocaml.doc
          "The most recent task definition that was specified for the tasks in the service to use."];
      desiredCount: Integer.t option
        [@ocaml.doc
          "The most recent desired count of tasks that was specified for the service to deploy or maintain."];
      pendingCount: Integer.t option
        [@ocaml.doc
          "The number of tasks in the deployment that are in the PENDING status."];
      runningCount: Integer.t option
        [@ocaml.doc
          "The number of tasks in the deployment that are in the RUNNING status."];
      failedTasks: Integer.t option
        [@ocaml.doc
          "The number of consecutively failed tasks in the deployment. A task is considered a failure if the service scheduler can't launch the task, the task doesn't transition to a RUNNING state, or if it fails any of its defined health checks and is stopped. Once a service deployment has one or more successfully running tasks, the failed task count resets to zero and stops being evaluated."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the service deployment was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the service deployment was last updated."];
      capacityProviderStrategy: CapacityProviderStrategy.t option
        [@ocaml.doc
          "The capacity provider strategy that the deployment is using."];
      launchType: LaunchType.t option
        [@ocaml.doc
          "The launch type the tasks in the service are using. For more information, see Amazon ECS Launch Types in the Amazon Elastic Container Service Developer Guide."];
      platformVersion: String_.t option
        [@ocaml.doc
          "The platform version that your tasks in the service run on. A platform version is only specified for tasks using the Fargate launch type. If one isn't specified, the LATEST platform version is used. For more information, see Fargate Platform Versions in the Amazon Elastic Container Service Developer Guide."];
      platformFamily: String_.t option
        [@ocaml.doc
          "The operating system that your tasks in the service, or tasks are running on. A platform family is specified only for tasks using the Fargate launch type. All tasks that run as part of this service must use the same platformFamily value as the service, for example, LINUX.."];
      networkConfiguration: NetworkConfiguration.t option
        [@ocaml.doc
          "The VPC subnet and security group configuration for tasks that receive their own elastic network interface by using the awsvpc networking mode."];
      rolloutState: DeploymentRolloutState.t option
        [@ocaml.doc
          "The rolloutState of a service is only returned for services that use the rolling update (ECS) deployment type that aren't behind a Classic Load Balancer. The rollout state of the deployment. When a service deployment is started, it begins in an IN_PROGRESS state. When the service reaches a steady state, the deployment transitions to a COMPLETED state. If the service fails to reach a steady state and circuit breaker is enabled, the deployment transitions to a FAILED state. A deployment in FAILED state doesn't launch any new tasks. For more information, see DeploymentCircuitBreaker."];
      rolloutStateReason: String_.t option
        [@ocaml.doc "A description of the rollout state of a deployment."]}
    let make ?id =
      fun ?status ->
        fun ?taskDefinition ->
          fun ?desiredCount ->
            fun ?pendingCount ->
              fun ?runningCount ->
                fun ?failedTasks ->
                  fun ?createdAt ->
                    fun ?updatedAt ->
                      fun ?capacityProviderStrategy ->
                        fun ?launchType ->
                          fun ?platformVersion ->
                            fun ?platformFamily ->
                              fun ?networkConfiguration ->
                                fun ?rolloutState ->
                                  fun ?rolloutStateReason ->
                                    fun () ->
                                      {
                                        id;
                                        status;
                                        taskDefinition;
                                        desiredCount;
                                        pendingCount;
                                        runningCount;
                                        failedTasks;
                                        createdAt;
                                        updatedAt;
                                        capacityProviderStrategy;
                                        launchType;
                                        platformVersion;
                                        platformFamily;
                                        networkConfiguration;
                                        rolloutState;
                                        rolloutStateReason
                                      }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:String_.to_value));
        ("status", (Option.map x.status ~f:String_.to_value));
        ("taskDefinition", (Option.map x.taskDefinition ~f:String_.to_value));
        ("desiredCount", (Option.map x.desiredCount ~f:Integer.to_value));
        ("pendingCount", (Option.map x.pendingCount ~f:Integer.to_value));
        ("runningCount", (Option.map x.runningCount ~f:Integer.to_value));
        ("failedTasks", (Option.map x.failedTasks ~f:Integer.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("capacityProviderStrategy",
          (Option.map x.capacityProviderStrategy
             ~f:CapacityProviderStrategy.to_value));
        ("launchType", (Option.map x.launchType ~f:LaunchType.to_value));
        ("platformVersion",
          (Option.map x.platformVersion ~f:String_.to_value));
        ("platformFamily", (Option.map x.platformFamily ~f:String_.to_value));
        ("networkConfiguration",
          (Option.map x.networkConfiguration ~f:NetworkConfiguration.to_value));
        ("rolloutState",
          (Option.map x.rolloutState ~f:DeploymentRolloutState.to_value));
        ("rolloutStateReason",
          (Option.map x.rolloutStateReason ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rolloutStateReason =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "rolloutStateReason") in
      let rolloutState =
        (Option.map ~f:DeploymentRolloutState.of_xml)
          (Xml.child xml_arg0 "rolloutState") in
      let networkConfiguration =
        (Option.map ~f:NetworkConfiguration.of_xml)
          (Xml.child xml_arg0 "networkConfiguration") in
      let platformFamily =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platformFamily") in
      let platformVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platformVersion") in
      let launchType =
        (Option.map ~f:LaunchType.of_xml) (Xml.child xml_arg0 "launchType") in
      let capacityProviderStrategy =
        (Option.map ~f:CapacityProviderStrategy.of_xml)
          (Xml.child xml_arg0 "capacityProviderStrategy") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let failedTasks =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "failedTasks") in
      let runningCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "runningCount") in
      let pendingCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "pendingCount") in
      let desiredCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "desiredCount") in
      let taskDefinition =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskDefinition") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      make ?rolloutStateReason ?rolloutState ?networkConfiguration
        ?platformFamily ?platformVersion ?launchType
        ?capacityProviderStrategy ?updatedAt ?createdAt ?failedTasks
        ?runningCount ?pendingCount ?desiredCount ?taskDefinition ?status ?id
        ()
    let of_json json =
      let rolloutStateReason =
        field_map json "rolloutStateReason" String_.of_json in
      let rolloutState =
        field_map json "rolloutState" DeploymentRolloutState.of_json in
      let networkConfiguration =
        field_map json "networkConfiguration" NetworkConfiguration.of_json in
      let platformFamily = field_map json "platformFamily" String_.of_json in
      let platformVersion = field_map json "platformVersion" String_.of_json in
      let launchType = field_map json "launchType" LaunchType.of_json in
      let capacityProviderStrategy =
        field_map json "capacityProviderStrategy"
          CapacityProviderStrategy.of_json in
      let updatedAt = field_map json "updatedAt" Timestamp.of_json in
      let createdAt = field_map json "createdAt" Timestamp.of_json in
      let failedTasks = field_map json "failedTasks" Integer.of_json in
      let runningCount = field_map json "runningCount" Integer.of_json in
      let pendingCount = field_map json "pendingCount" Integer.of_json in
      let desiredCount = field_map json "desiredCount" Integer.of_json in
      let taskDefinition = field_map json "taskDefinition" String_.of_json in
      let status = field_map json "status" String_.of_json in
      let id = field_map json "id" String_.of_json in
      make ?rolloutStateReason ?rolloutState ?networkConfiguration
        ?platformFamily ?platformVersion ?launchType
        ?capacityProviderStrategy ?updatedAt ?createdAt ?failedTasks
        ?runningCount ?pendingCount ?desiredCount ?taskDefinition ?status ?id
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details of an Amazon ECS service deployment. This is used only when a service uses the ECS deployment controller type."]
module PlacementConstraint =
  struct
    type nonrec t =
      {
      type_: PlacementConstraintType.t option
        [@ocaml.doc
          "The type of constraint. Use distinctInstance to ensure that each task in a particular group is running on a different container instance. Use memberOf to restrict the selection to a group of valid candidates."];
      expression: String_.t option
        [@ocaml.doc
          "A cluster query language expression to apply to the constraint. The expression can have a maximum length of 2000 characters. You can't specify an expression if the constraint type is distinctInstance. For more information, see Cluster query language in the Amazon Elastic Container Service Developer Guide."]}
    let make ?type_ = fun ?expression -> fun () -> { type_; expression }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:PlacementConstraintType.to_value));
        ("expression", (Option.map x.expression ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expression =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "expression") in
      let type_ =
        (Option.map ~f:PlacementConstraintType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?expression ?type_ ()
    let of_json json =
      let expression = field_map json "expression" String_.of_json in
      let type_ = field_map json "type" PlacementConstraintType.of_json in
      make ?expression ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing a constraint on task placement. For more information, see Task Placement Constraints in the Amazon Elastic Container Service Developer Guide. If you're using the Fargate launch type, task placement constraints aren't supported."]
module PlacementStrategy =
  struct
    type nonrec t =
      {
      type_: PlacementStrategyType.t option
        [@ocaml.doc
          "The type of placement strategy. The random placement strategy randomly places tasks on available candidates. The spread placement strategy spreads placement across available candidates evenly based on the field parameter. The binpack strategy places tasks on available candidates that have the least available amount of the resource that's specified with the field parameter. For example, if you binpack on memory, a task is placed on the instance with the least amount of remaining memory but still enough to run the task."];
      field: String_.t option
        [@ocaml.doc
          "The field to apply the placement strategy against. For the spread placement strategy, valid values are instanceId (or host, which has the same effect), or any platform or custom attribute that's applied to a container instance, such as attribute:ecs.availability-zone. For the binpack placement strategy, valid values are cpu and memory. For the random placement strategy, this field is not used."]}
    let make ?type_ = fun ?field -> fun () -> { type_; field }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:PlacementStrategyType.to_value));
        ("field", (Option.map x.field ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let field = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "field") in
      let type_ =
        (Option.map ~f:PlacementStrategyType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?field ?type_ ()
    let of_json json =
      let field = field_map json "field" String_.of_json in
      let type_ = field_map json "type" PlacementStrategyType.of_json in
      make ?field ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The task placement strategy for a task or service. For more information, see Task Placement Strategies in the Amazon Elastic Container Service Developer Guide."]
module ServiceEvent =
  struct
    type nonrec t =
      {
      id: String_.t option [@ocaml.doc "The ID string for the event."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the event was triggered."];
      message: String_.t option [@ocaml.doc "The event message."]}
    let make ?id =
      fun ?createdAt -> fun ?message -> fun () -> { id; createdAt; message }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:String_.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      make ?message ?createdAt ?id ()
    let of_json json =
      let message = field_map json "message" String_.of_json in
      let createdAt = field_map json "createdAt" Timestamp.of_json in
      let id = field_map json "id" String_.of_json in
      make ?message ?createdAt ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details for an event that's associated with a service."]
module TaskSet =
  struct
    type nonrec t =
      {
      id: String_.t option [@ocaml.doc "The ID of the task set."];
      taskSetArn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the task set."];
      serviceArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the service the task set exists in."];
      clusterArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the cluster that the service that hosts the task set exists in."];
      startedBy: String_.t option
        [@ocaml.doc
          "The tag specified when a task set is started. If an CodeDeploy deployment created the task set, the startedBy parameter is CODE_DEPLOY. If an external deployment created the task set, the startedBy field isn't used."];
      externalId: String_.t option
        [@ocaml.doc
          "The external ID associated with the task set. If an CodeDeploy deployment created a task set, the externalId parameter contains the CodeDeploy deployment ID. If a task set is created for an external deployment and is associated with a service discovery registry, the externalId parameter contains the ECS_TASK_SET_EXTERNAL_ID Cloud Map attribute."];
      status: String_.t option
        [@ocaml.doc
          "The status of the task set. The following describes each state. PRIMARY The task set is serving production traffic. ACTIVE The task set isn't serving production traffic. DRAINING The tasks in the task set are being stopped, and their corresponding targets are being deregistered from their target group."];
      taskDefinition: String_.t option
        [@ocaml.doc "The task definition that the task set is using."];
      computedDesiredCount: Integer.t option
        [@ocaml.doc
          "The computed desired count for the task set. This is calculated by multiplying the service's desiredCount by the task set's scale percentage. The result is always rounded up. For example, if the computed desired count is 1.2, it rounds up to 2 tasks."];
      pendingCount: Integer.t option
        [@ocaml.doc
          "The number of tasks in the task set that are in the PENDING status during a deployment. A task in the PENDING state is preparing to enter the RUNNING state. A task set enters the PENDING status when it launches for the first time or when it's restarted after being in the STOPPED state."];
      runningCount: Integer.t option
        [@ocaml.doc
          "The number of tasks in the task set that are in the RUNNING status during a deployment. A task in the RUNNING state is running and ready for use."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the task set was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the task set was last updated."];
      launchType: LaunchType.t option
        [@ocaml.doc
          "The launch type the tasks in the task set are using. For more information, see Amazon ECS launch types in the Amazon Elastic Container Service Developer Guide."];
      capacityProviderStrategy: CapacityProviderStrategy.t option
        [@ocaml.doc
          "The capacity provider strategy that are associated with the task set."];
      platformVersion: String_.t option
        [@ocaml.doc
          "The Fargate platform version where the tasks in the task set are running. A platform version is only specified for tasks run on Fargate. For more information, see Fargate platform versions in the Amazon Elastic Container Service Developer Guide."];
      platformFamily: String_.t option
        [@ocaml.doc
          "The operating system that your tasks in the set are running on. A platform family is specified only for tasks that use the Fargate launch type. All tasks in the set must have the same value."];
      networkConfiguration: NetworkConfiguration.t option
        [@ocaml.doc "The network configuration for the task set."];
      loadBalancers: LoadBalancers.t option
        [@ocaml.doc
          "Details on a load balancer that are used with a task set."];
      serviceRegistries: ServiceRegistries.t option
        [@ocaml.doc
          "The details for the service discovery registries to assign to this task set. For more information, see Service discovery."];
      scale: Scale.t option
        [@ocaml.doc
          "A floating-point percentage of your desired number of tasks to place and keep running in the task set."];
      stabilityStatus: StabilityStatus.t option
        [@ocaml.doc
          "The stability status. This indicates whether the task set has reached a steady state. If the following conditions are met, the task set sre in STEADY_STATE: The task runningCount is equal to the computedDesiredCount. The pendingCount is 0. There are no tasks that are running on container instances in the DRAINING status. All tasks are reporting a healthy status from the load balancers, service discovery, and container health checks. If any of those conditions aren't met, the stability status returns STABILIZING."];
      stabilityStatusAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the task set stability status was retrieved."];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the task set to help you categorize and organize them. Each tag consists of a key and an optional value. You define both. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."]}
    let make ?id =
      fun ?taskSetArn ->
        fun ?serviceArn ->
          fun ?clusterArn ->
            fun ?startedBy ->
              fun ?externalId ->
                fun ?status ->
                  fun ?taskDefinition ->
                    fun ?computedDesiredCount ->
                      fun ?pendingCount ->
                        fun ?runningCount ->
                          fun ?createdAt ->
                            fun ?updatedAt ->
                              fun ?launchType ->
                                fun ?capacityProviderStrategy ->
                                  fun ?platformVersion ->
                                    fun ?platformFamily ->
                                      fun ?networkConfiguration ->
                                        fun ?loadBalancers ->
                                          fun ?serviceRegistries ->
                                            fun ?scale ->
                                              fun ?stabilityStatus ->
                                                fun ?stabilityStatusAt ->
                                                  fun ?tags ->
                                                    fun () ->
                                                      {
                                                        id;
                                                        taskSetArn;
                                                        serviceArn;
                                                        clusterArn;
                                                        startedBy;
                                                        externalId;
                                                        status;
                                                        taskDefinition;
                                                        computedDesiredCount;
                                                        pendingCount;
                                                        runningCount;
                                                        createdAt;
                                                        updatedAt;
                                                        launchType;
                                                        capacityProviderStrategy;
                                                        platformVersion;
                                                        platformFamily;
                                                        networkConfiguration;
                                                        loadBalancers;
                                                        serviceRegistries;
                                                        scale;
                                                        stabilityStatus;
                                                        stabilityStatusAt;
                                                        tags
                                                      }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:String_.to_value));
        ("taskSetArn", (Option.map x.taskSetArn ~f:String_.to_value));
        ("serviceArn", (Option.map x.serviceArn ~f:String_.to_value));
        ("clusterArn", (Option.map x.clusterArn ~f:String_.to_value));
        ("startedBy", (Option.map x.startedBy ~f:String_.to_value));
        ("externalId", (Option.map x.externalId ~f:String_.to_value));
        ("status", (Option.map x.status ~f:String_.to_value));
        ("taskDefinition", (Option.map x.taskDefinition ~f:String_.to_value));
        ("computedDesiredCount",
          (Option.map x.computedDesiredCount ~f:Integer.to_value));
        ("pendingCount", (Option.map x.pendingCount ~f:Integer.to_value));
        ("runningCount", (Option.map x.runningCount ~f:Integer.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("launchType", (Option.map x.launchType ~f:LaunchType.to_value));
        ("capacityProviderStrategy",
          (Option.map x.capacityProviderStrategy
             ~f:CapacityProviderStrategy.to_value));
        ("platformVersion",
          (Option.map x.platformVersion ~f:String_.to_value));
        ("platformFamily", (Option.map x.platformFamily ~f:String_.to_value));
        ("networkConfiguration",
          (Option.map x.networkConfiguration ~f:NetworkConfiguration.to_value));
        ("loadBalancers",
          (Option.map x.loadBalancers ~f:LoadBalancers.to_value));
        ("serviceRegistries",
          (Option.map x.serviceRegistries ~f:ServiceRegistries.to_value));
        ("scale", (Option.map x.scale ~f:Scale.to_value));
        ("stabilityStatus",
          (Option.map x.stabilityStatus ~f:StabilityStatus.to_value));
        ("stabilityStatusAt",
          (Option.map x.stabilityStatusAt ~f:Timestamp.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let stabilityStatusAt =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "stabilityStatusAt") in
      let stabilityStatus =
        (Option.map ~f:StabilityStatus.of_xml)
          (Xml.child xml_arg0 "stabilityStatus") in
      let scale = (Option.map ~f:Scale.of_xml) (Xml.child xml_arg0 "scale") in
      let serviceRegistries =
        (Option.map ~f:ServiceRegistries.of_xml)
          (Xml.child xml_arg0 "serviceRegistries") in
      let loadBalancers =
        (Option.map ~f:LoadBalancers.of_xml)
          (Xml.child xml_arg0 "loadBalancers") in
      let networkConfiguration =
        (Option.map ~f:NetworkConfiguration.of_xml)
          (Xml.child xml_arg0 "networkConfiguration") in
      let platformFamily =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platformFamily") in
      let platformVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platformVersion") in
      let capacityProviderStrategy =
        (Option.map ~f:CapacityProviderStrategy.of_xml)
          (Xml.child xml_arg0 "capacityProviderStrategy") in
      let launchType =
        (Option.map ~f:LaunchType.of_xml) (Xml.child xml_arg0 "launchType") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let runningCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "runningCount") in
      let pendingCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "pendingCount") in
      let computedDesiredCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "computedDesiredCount") in
      let taskDefinition =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskDefinition") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let externalId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "externalId") in
      let startedBy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "startedBy") in
      let clusterArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clusterArn") in
      let serviceArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "serviceArn") in
      let taskSetArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskSetArn") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      make ?tags ?stabilityStatusAt ?stabilityStatus ?scale
        ?serviceRegistries ?loadBalancers ?networkConfiguration
        ?platformFamily ?platformVersion ?capacityProviderStrategy
        ?launchType ?updatedAt ?createdAt ?runningCount ?pendingCount
        ?computedDesiredCount ?taskDefinition ?status ?externalId ?startedBy
        ?clusterArn ?serviceArn ?taskSetArn ?id ()
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let stabilityStatusAt =
        field_map json "stabilityStatusAt" Timestamp.of_json in
      let stabilityStatus =
        field_map json "stabilityStatus" StabilityStatus.of_json in
      let scale = field_map json "scale" Scale.of_json in
      let serviceRegistries =
        field_map json "serviceRegistries" ServiceRegistries.of_json in
      let loadBalancers =
        field_map json "loadBalancers" LoadBalancers.of_json in
      let networkConfiguration =
        field_map json "networkConfiguration" NetworkConfiguration.of_json in
      let platformFamily = field_map json "platformFamily" String_.of_json in
      let platformVersion = field_map json "platformVersion" String_.of_json in
      let capacityProviderStrategy =
        field_map json "capacityProviderStrategy"
          CapacityProviderStrategy.of_json in
      let launchType = field_map json "launchType" LaunchType.of_json in
      let updatedAt = field_map json "updatedAt" Timestamp.of_json in
      let createdAt = field_map json "createdAt" Timestamp.of_json in
      let runningCount = field_map json "runningCount" Integer.of_json in
      let pendingCount = field_map json "pendingCount" Integer.of_json in
      let computedDesiredCount =
        field_map json "computedDesiredCount" Integer.of_json in
      let taskDefinition = field_map json "taskDefinition" String_.of_json in
      let status = field_map json "status" String_.of_json in
      let externalId = field_map json "externalId" String_.of_json in
      let startedBy = field_map json "startedBy" String_.of_json in
      let clusterArn = field_map json "clusterArn" String_.of_json in
      let serviceArn = field_map json "serviceArn" String_.of_json in
      let taskSetArn = field_map json "taskSetArn" String_.of_json in
      let id = field_map json "id" String_.of_json in
      make ?tags ?stabilityStatusAt ?stabilityStatus ?scale
        ?serviceRegistries ?loadBalancers ?networkConfiguration
        ?platformFamily ?platformVersion ?capacityProviderStrategy
        ?launchType ?updatedAt ?createdAt ?runningCount ?pendingCount
        ?computedDesiredCount ?taskDefinition ?status ?externalId ?startedBy
        ?clusterArn ?serviceArn ?taskSetArn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a set of Amazon ECS tasks in either an CodeDeploy or an EXTERNAL deployment. An Amazon ECS task set includes details such as the desired number of tasks, how many tasks are running, and whether the task set serves production traffic."]
module ExecuteCommandConfiguration =
  struct
    type nonrec t =
      {
      kmsKeyId: String_.t option
        [@ocaml.doc
          "Specify an Key Management Service key ID to encrypt the data between the local client and the container."];
      logging: ExecuteCommandLogging.t option
        [@ocaml.doc
          "The log setting to use for redirecting logs for your execute command results. The following log settings are available. NONE: The execute command session is not logged. DEFAULT: The awslogs configuration in the task definition is used. If no logging parameter is specified, it defaults to this value. If no awslogs log driver is configured in the task definition, the output won't be logged. OVERRIDE: Specify the logging details as a part of logConfiguration. If the OVERRIDE logging option is specified, the logConfiguration is required."];
      logConfiguration: ExecuteCommandLogConfiguration.t option
        [@ocaml.doc
          "The log configuration for the results of the execute command actions. The logs can be sent to CloudWatch Logs or an Amazon S3 bucket. When logging=OVERRIDE is specified, a logConfiguration must be provided."]}
    let make ?kmsKeyId =
      fun ?logging ->
        fun ?logConfiguration ->
          fun () -> { kmsKeyId; logging; logConfiguration }
    let to_value x =
      structure_to_value
        [("kmsKeyId", (Option.map x.kmsKeyId ~f:String_.to_value));
        ("logging", (Option.map x.logging ~f:ExecuteCommandLogging.to_value));
        ("logConfiguration",
          (Option.map x.logConfiguration
             ~f:ExecuteCommandLogConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logConfiguration =
        (Option.map ~f:ExecuteCommandLogConfiguration.of_xml)
          (Xml.child xml_arg0 "logConfiguration") in
      let logging =
        (Option.map ~f:ExecuteCommandLogging.of_xml)
          (Xml.child xml_arg0 "logging") in
      let kmsKeyId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "kmsKeyId") in
      make ?logConfiguration ?logging ?kmsKeyId ()
    let of_json json =
      let logConfiguration =
        field_map json "logConfiguration"
          ExecuteCommandLogConfiguration.of_json in
      let logging = field_map json "logging" ExecuteCommandLogging.of_json in
      let kmsKeyId = field_map json "kmsKeyId" String_.of_json in
      make ?logConfiguration ?logging ?kmsKeyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The details of the execute command configuration."]
module ClusterSetting =
  struct
    type nonrec t =
      {
      name: ClusterSettingName.t option
        [@ocaml.doc
          "The name of the cluster setting. The only supported value is containerInsights."];
      value: String_.t option
        [@ocaml.doc
          "The value to set for the cluster setting. The supported values are enabled and disabled. If enabled is specified, CloudWatch Container Insights will be enabled for the cluster, otherwise it will be disabled unless the containerInsights account setting is enabled. If a cluster value is specified, it will override the containerInsights value set with PutAccountSetting or PutAccountSettingDefault."]}
    let make ?name = fun ?value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:ClusterSettingName.to_value));
        ("value", (Option.map x.value ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let name =
        (Option.map ~f:ClusterSettingName.of_xml) (Xml.child xml_arg0 "name") in
      make ?value ?name ()
    let of_json json =
      let value = field_map json "value" String_.of_json in
      let name = field_map json "name" ClusterSettingName.of_json in
      make ?value ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The settings to use when creating a cluster. This parameter is used to turn on CloudWatch Container Insights for a cluster."]
module ManagedScaling =
  struct
    type nonrec t =
      {
      status: ManagedScalingStatus.t option
        [@ocaml.doc
          "Determines whether to use managed scaling for the capacity provider."];
      targetCapacity: ManagedScalingTargetCapacity.t option
        [@ocaml.doc
          "The target capacity value for the capacity provider. The specified value must be greater than 0 and less than or equal to 100. A value of 100 results in the Amazon EC2 instances in your Auto Scaling group being completely used."];
      minimumScalingStepSize: ManagedScalingStepSize.t option
        [@ocaml.doc
          "The minimum number of container instances that Amazon ECS scales in or scales out at one time. If this parameter is omitted, the default value of 1 is used."];
      maximumScalingStepSize: ManagedScalingStepSize.t option
        [@ocaml.doc
          "The maximum number of container instances that Amazon ECS scales in or scales out at one time. If this parameter is omitted, the default value of 10000 is used."];
      instanceWarmupPeriod: ManagedScalingInstanceWarmupPeriod.t option
        [@ocaml.doc
          "The period of time, in seconds, after a newly launched Amazon EC2 instance can contribute to CloudWatch metrics for Auto Scaling group. If this parameter is omitted, the default value of 300 seconds is used."]}
    let make ?status =
      fun ?targetCapacity ->
        fun ?minimumScalingStepSize ->
          fun ?maximumScalingStepSize ->
            fun ?instanceWarmupPeriod ->
              fun () ->
                {
                  status;
                  targetCapacity;
                  minimumScalingStepSize;
                  maximumScalingStepSize;
                  instanceWarmupPeriod
                }
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:ManagedScalingStatus.to_value));
        ("targetCapacity",
          (Option.map x.targetCapacity
             ~f:ManagedScalingTargetCapacity.to_value));
        ("minimumScalingStepSize",
          (Option.map x.minimumScalingStepSize
             ~f:ManagedScalingStepSize.to_value));
        ("maximumScalingStepSize",
          (Option.map x.maximumScalingStepSize
             ~f:ManagedScalingStepSize.to_value));
        ("instanceWarmupPeriod",
          (Option.map x.instanceWarmupPeriod
             ~f:ManagedScalingInstanceWarmupPeriod.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceWarmupPeriod =
        (Option.map ~f:ManagedScalingInstanceWarmupPeriod.of_xml)
          (Xml.child xml_arg0 "instanceWarmupPeriod") in
      let maximumScalingStepSize =
        (Option.map ~f:ManagedScalingStepSize.of_xml)
          (Xml.child xml_arg0 "maximumScalingStepSize") in
      let minimumScalingStepSize =
        (Option.map ~f:ManagedScalingStepSize.of_xml)
          (Xml.child xml_arg0 "minimumScalingStepSize") in
      let targetCapacity =
        (Option.map ~f:ManagedScalingTargetCapacity.of_xml)
          (Xml.child xml_arg0 "targetCapacity") in
      let status =
        (Option.map ~f:ManagedScalingStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?instanceWarmupPeriod ?maximumScalingStepSize
        ?minimumScalingStepSize ?targetCapacity ?status ()
    let of_json json =
      let instanceWarmupPeriod =
        field_map json "instanceWarmupPeriod"
          ManagedScalingInstanceWarmupPeriod.of_json in
      let maximumScalingStepSize =
        field_map json "maximumScalingStepSize"
          ManagedScalingStepSize.of_json in
      let minimumScalingStepSize =
        field_map json "minimumScalingStepSize"
          ManagedScalingStepSize.of_json in
      let targetCapacity =
        field_map json "targetCapacity" ManagedScalingTargetCapacity.of_json in
      let status = field_map json "status" ManagedScalingStatus.of_json in
      make ?instanceWarmupPeriod ?maximumScalingStepSize
        ?minimumScalingStepSize ?targetCapacity ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The managed scaling settings for the Auto Scaling group capacity provider. When managed scaling is enabled, Amazon ECS manages the scale-in and scale-out actions of the Auto Scaling group. Amazon ECS manages a target tracking scaling policy using an Amazon ECS managed CloudWatch metric with the specified targetCapacity value as the target value for the metric. For more information, see Using Managed Scaling in the Amazon Elastic Container Service Developer Guide. If managed scaling is disabled, the user must manage the scaling of the Auto Scaling group."]
module ManagedTerminationProtection =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ManagedTerminationProtection"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ManagedTerminationProtection" j)
    let to_json = simple_to_json to_value
  end
module AgentUpdateStatus =
  struct
    type nonrec t =
      | PENDING 
      | STAGING 
      | STAGED 
      | UPDATING 
      | UPDATED 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | STAGING -> "STAGING"
      | STAGED -> "STAGED"
      | UPDATING -> "UPDATING"
      | UPDATED -> "UPDATED"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "STAGING" -> STAGING
      | "STAGED" -> STAGED
      | "UPDATING" -> UPDATING
      | "UPDATED" -> UPDATED
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AgentUpdateStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AgentUpdateStatus" j)
    let to_json = simple_to_json to_value
  end
module Attachments =
  struct
    type nonrec t = Attachment.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Attachment.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Attachment.of_xml)
    let of_json j =
      list_of_json ~kind:"Attachments" ~of_json:Attachment.of_json j
    let to_json v = composed_to_json to_value v
  end
module Attributes =
  struct
    type nonrec t = Attribute.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Attribute.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Attribute.of_xml)
    let of_json j =
      list_of_json ~kind:"Attributes" ~of_json:Attribute.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerInstanceHealthStatus =
  struct
    type nonrec t =
      {
      overallStatus: InstanceHealthCheckState.t option
        [@ocaml.doc
          "The overall health status of the container instance. This is an aggregate status of all container instance health checks."];
      details: InstanceHealthCheckResultList.t option
        [@ocaml.doc
          "An array of objects representing the details of the container instance health status."]}
    let make ?overallStatus =
      fun ?details -> fun () -> { overallStatus; details }
    let to_value x =
      structure_to_value
        [("overallStatus",
           (Option.map x.overallStatus ~f:InstanceHealthCheckState.to_value));
        ("details",
          (Option.map x.details ~f:InstanceHealthCheckResultList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let details =
        (Option.map ~f:InstanceHealthCheckResultList.of_xml)
          (Xml.child xml_arg0 "details") in
      let overallStatus =
        (Option.map ~f:InstanceHealthCheckState.of_xml)
          (Xml.child xml_arg0 "overallStatus") in
      make ?details ?overallStatus ()
    let of_json json =
      let details =
        field_map json "details" InstanceHealthCheckResultList.of_json in
      let overallStatus =
        field_map json "overallStatus" InstanceHealthCheckState.of_json in
      make ?details ?overallStatus ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing the health status of the container instance."]
module Resources =
  struct
    type nonrec t = Resource.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Resource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Resource.of_xml)
    let of_json j =
      list_of_json ~kind:"Resources" ~of_json:Resource.of_json j
    let to_json v = composed_to_json to_value v
  end
module VersionInfo =
  struct
    type nonrec t =
      {
      agentVersion: String_.t option
        [@ocaml.doc "The version number of the Amazon ECS container agent."];
      agentHash: String_.t option
        [@ocaml.doc
          "The Git commit hash for the Amazon ECS container agent build on the amazon-ecs-agent GitHub repository."];
      dockerVersion: String_.t option
        [@ocaml.doc
          "The Docker version that's running on the container instance."]}
    let make ?agentVersion =
      fun ?agentHash ->
        fun ?dockerVersion ->
          fun () -> { agentVersion; agentHash; dockerVersion }
    let to_value x =
      structure_to_value
        [("agentVersion", (Option.map x.agentVersion ~f:String_.to_value));
        ("agentHash", (Option.map x.agentHash ~f:String_.to_value));
        ("dockerVersion", (Option.map x.dockerVersion ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dockerVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "dockerVersion") in
      let agentHash =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "agentHash") in
      let agentVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "agentVersion") in
      make ?dockerVersion ?agentHash ?agentVersion ()
    let of_json json =
      let dockerVersion = field_map json "dockerVersion" String_.of_json in
      let agentHash = field_map json "agentHash" String_.of_json in
      let agentVersion = field_map json "agentVersion" String_.of_json in
      make ?dockerVersion ?agentHash ?agentVersion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Docker and Amazon ECS container agent version information about a container instance."]
module Connectivity =
  struct
    type nonrec t =
      | CONNECTED 
      | DISCONNECTED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CONNECTED -> "CONNECTED"
      | DISCONNECTED -> "DISCONNECTED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CONNECTED" -> CONNECTED
      | "DISCONNECTED" -> DISCONNECTED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Connectivity" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Connectivity" j)
    let to_json = simple_to_json to_value
  end
module Containers =
  struct
    type nonrec t = Container.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Container.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Container.of_xml)
    let of_json j =
      list_of_json ~kind:"Containers" ~of_json:Container.of_json j
    let to_json v = composed_to_json to_value v
  end
module InferenceAccelerators =
  struct
    type nonrec t = InferenceAccelerator.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InferenceAccelerator.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InferenceAccelerator.of_xml)
    let of_json j =
      list_of_json ~kind:"InferenceAccelerators"
        ~of_json:InferenceAccelerator.of_json j
    let to_json v = composed_to_json to_value v
  end
module TaskOverride =
  struct
    type nonrec t =
      {
      containerOverrides: ContainerOverrides.t option
        [@ocaml.doc
          "One or more container overrides that are sent to a task."];
      cpu: String_.t option [@ocaml.doc "The cpu override for the task."];
      inferenceAcceleratorOverrides: InferenceAcceleratorOverrides.t option
        [@ocaml.doc
          "The Elastic Inference accelerator override for the task."];
      executionRoleArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the task execution IAM role override for the task. For more information, see Amazon ECS task execution IAM role in the Amazon Elastic Container Service Developer Guide."];
      memory: String_.t option
        [@ocaml.doc "The memory override for the task."];
      taskRoleArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM role that containers in this task can assume. All containers in this task are granted the permissions that are specified in this role. For more information, see IAM Role for Tasks in the Amazon Elastic Container Service Developer Guide."];
      ephemeralStorage: EphemeralStorage.t option
        [@ocaml.doc
          "The ephemeral storage setting override for the task. This parameter is only supported for tasks hosted on Fargate that use the following platform versions: Linux platform version 1.4.0 or later. Windows platform version 1.0.0 or later."]}
    let make ?containerOverrides =
      fun ?cpu ->
        fun ?inferenceAcceleratorOverrides ->
          fun ?executionRoleArn ->
            fun ?memory ->
              fun ?taskRoleArn ->
                fun ?ephemeralStorage ->
                  fun () ->
                    {
                      containerOverrides;
                      cpu;
                      inferenceAcceleratorOverrides;
                      executionRoleArn;
                      memory;
                      taskRoleArn;
                      ephemeralStorage
                    }
    let to_value x =
      structure_to_value
        [("containerOverrides",
           (Option.map x.containerOverrides ~f:ContainerOverrides.to_value));
        ("cpu", (Option.map x.cpu ~f:String_.to_value));
        ("inferenceAcceleratorOverrides",
          (Option.map x.inferenceAcceleratorOverrides
             ~f:InferenceAcceleratorOverrides.to_value));
        ("executionRoleArn",
          (Option.map x.executionRoleArn ~f:String_.to_value));
        ("memory", (Option.map x.memory ~f:String_.to_value));
        ("taskRoleArn", (Option.map x.taskRoleArn ~f:String_.to_value));
        ("ephemeralStorage",
          (Option.map x.ephemeralStorage ~f:EphemeralStorage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ephemeralStorage =
        (Option.map ~f:EphemeralStorage.of_xml)
          (Xml.child xml_arg0 "ephemeralStorage") in
      let taskRoleArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskRoleArn") in
      let memory =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "memory") in
      let executionRoleArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "executionRoleArn") in
      let inferenceAcceleratorOverrides =
        (Option.map ~f:InferenceAcceleratorOverrides.of_xml)
          (Xml.child xml_arg0 "inferenceAcceleratorOverrides") in
      let cpu = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cpu") in
      let containerOverrides =
        (Option.map ~f:ContainerOverrides.of_xml)
          (Xml.child xml_arg0 "containerOverrides") in
      make ?ephemeralStorage ?taskRoleArn ?memory ?executionRoleArn
        ?inferenceAcceleratorOverrides ?cpu ?containerOverrides ()
    let of_json json =
      let ephemeralStorage =
        field_map json "ephemeralStorage" EphemeralStorage.of_json in
      let taskRoleArn = field_map json "taskRoleArn" String_.of_json in
      let memory = field_map json "memory" String_.of_json in
      let executionRoleArn =
        field_map json "executionRoleArn" String_.of_json in
      let inferenceAcceleratorOverrides =
        field_map json "inferenceAcceleratorOverrides"
          InferenceAcceleratorOverrides.of_json in
      let cpu = field_map json "cpu" String_.of_json in
      let containerOverrides =
        field_map json "containerOverrides" ContainerOverrides.of_json in
      make ?ephemeralStorage ?taskRoleArn ?memory ?executionRoleArn
        ?inferenceAcceleratorOverrides ?cpu ?containerOverrides ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The overrides that are associated with a task."]
module TaskStopCode =
  struct
    type nonrec t =
      | TaskFailedToStart 
      | EssentialContainerExited 
      | UserInitiated 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TaskFailedToStart -> "TaskFailedToStart"
      | EssentialContainerExited -> "EssentialContainerExited"
      | UserInitiated -> "UserInitiated"
      | Non_static_id s -> s
    let of_string =
      function
      | "TaskFailedToStart" -> TaskFailedToStart
      | "EssentialContainerExited" -> EssentialContainerExited
      | "UserInitiated" -> UserInitiated
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TaskStopCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TaskStopCode" j)
    let to_json = simple_to_json to_value
  end
module Compatibility =
  struct
    type nonrec t =
      | EC2 
      | FARGATE 
      | EXTERNAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | EC2 -> "EC2"
      | FARGATE -> "FARGATE"
      | EXTERNAL -> "EXTERNAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "EC2" -> EC2
      | "FARGATE" -> FARGATE
      | "EXTERNAL" -> EXTERNAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Compatibility" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Compatibility" j)
    let to_json = simple_to_json to_value
  end
module ContainerDefinition =
  struct
    type nonrec t =
      {
      name: String_.t option
        [@ocaml.doc
          "The name of a container. If you're linking multiple containers together in a task definition, the name of one container can be entered in the links of another container to connect the containers. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed. This parameter maps to name in the Create a container section of the Docker Remote API and the --name option to docker run."];
      image: String_.t option
        [@ocaml.doc
          "The image used to start a container. This string is passed directly to the Docker daemon. By default, images in the Docker Hub registry are available. Other repositories are specified with either repository-url/image:tag or repository-url/image\\@digest . Up to 255 letters (uppercase and lowercase), numbers, hyphens, underscores, colons, periods, forward slashes, and number signs are allowed. This parameter maps to Image in the Create a container section of the Docker Remote API and the IMAGE parameter of docker run. When a new task starts, the Amazon ECS container agent pulls the latest version of the specified image and tag for the container to use. However, subsequent updates to a repository image aren't propagated to already running tasks. Images in Amazon ECR repositories can be specified by either using the full registry/repository:tag or registry/repository\\@digest. For example, 012345678910.dkr.ecr.<region-name>.amazonaws.com/<repository-name>:latest or 012345678910.dkr.ecr.<region-name>.amazonaws.com/<repository-name>\\@sha256:94afd1f2e64d908bc90dbca0035a5b567EXAMPLE. Images in official repositories on Docker Hub use a single name (for example, ubuntu or mongo). Images in other repositories on Docker Hub are qualified with an organization name (for example, amazon/amazon-ecs-agent). Images in other online repositories are qualified further by a domain name (for example, quay.io/assemblyline/ubuntu)."];
      repositoryCredentials: RepositoryCredentials.t option
        [@ocaml.doc
          "The private repository authentication credentials to use."];
      cpu: Integer.t option
        [@ocaml.doc
          "The number of cpu units reserved for the container. This parameter maps to CpuShares in the Create a container section of the Docker Remote API and the --cpu-shares option to docker run. This field is optional for tasks using the Fargate launch type, and the only requirement is that the total amount of CPU reserved for all containers within a task be lower than the task-level cpu value. You can determine the number of CPU units that are available per EC2 instance type by multiplying the vCPUs listed for that instance type on the Amazon EC2 Instances detail page by 1,024. Linux containers share unallocated CPU units with other containers on the container instance with the same ratio as their allocated amount. For example, if you run a single-container task on a single-core instance type with 512 CPU units specified for that container, and that's the only task running on the container instance, that container could use the full 1,024 CPU unit share at any given time. However, if you launched another copy of the same task on that container instance, each task is guaranteed a minimum of 512 CPU units when needed. Moreover, each container could float to higher CPU usage if the other container was not using it. If both tasks were 100% active all of the time, they would be limited to 512 CPU units. On Linux container instances, the Docker daemon on the container instance uses the CPU value to calculate the relative CPU share ratios for running containers. For more information, see CPU share constraint in the Docker documentation. The minimum valid CPU share value that the Linux kernel allows is 2. However, the CPU parameter isn't required, and you can use CPU values below 2 in your container definitions. For CPU values below 2 (including null), the behavior varies based on your Amazon ECS container agent version: Agent versions less than or equal to 1.1.0: Null and zero CPU values are passed to Docker as 0, which Docker then converts to 1,024 CPU shares. CPU values of 1 are passed to Docker as 1, which the Linux kernel converts to two CPU shares. Agent versions greater than or equal to 1.2.0: Null, zero, and CPU values of 1 are passed to Docker as 2. On Windows container instances, the CPU limit is enforced as an absolute limit, or a quota. Windows containers only have access to the specified amount of CPU that's described in the task definition. A null or zero CPU value is passed to Docker as 0, which Windows interprets as 1% of one CPU."];
      memory: BoxedInteger.t option
        [@ocaml.doc
          "The amount (in MiB) of memory to present to the container. If your container attempts to exceed the memory specified here, the container is killed. The total amount of memory reserved for all containers within a task must be lower than the task memory value, if one is specified. This parameter maps to Memory in the Create a container section of the Docker Remote API and the --memory option to docker run. If using the Fargate launch type, this parameter is optional. If using the EC2 launch type, you must specify either a task-level memory value or a container-level memory value. If you specify both a container-level memory and memoryReservation value, memory must be greater than memoryReservation. If you specify memoryReservation, then that value is subtracted from the available memory resources for the container instance where the container is placed. Otherwise, the value of memory is used. The Docker 20.10.0 or later daemon reserves a minimum of 6 MiB of memory for a container, so you should not specify fewer than 6 MiB of memory for your containers. The Docker 19.03.13-ce or earlier daemon reserves a minimum of 4 MiB of memory for a container, so you should not specify fewer than 4 MiB of memory for your containers."];
      memoryReservation: BoxedInteger.t option
        [@ocaml.doc
          "The soft limit (in MiB) of memory to reserve for the container. When system memory is under heavy contention, Docker attempts to keep the container memory to this soft limit. However, your container can consume more memory when it needs to, up to either the hard limit specified with the memory parameter (if applicable), or all of the available memory on the container instance, whichever comes first. This parameter maps to MemoryReservation in the Create a container section of the Docker Remote API and the --memory-reservation option to docker run. If a task-level memory value is not specified, you must specify a non-zero integer for one or both of memory or memoryReservation in a container definition. If you specify both, memory must be greater than memoryReservation. If you specify memoryReservation, then that value is subtracted from the available memory resources for the container instance where the container is placed. Otherwise, the value of memory is used. For example, if your container normally uses 128 MiB of memory, but occasionally bursts to 256 MiB of memory for short periods of time, you can set a memoryReservation of 128 MiB, and a memory hard limit of 300 MiB. This configuration would allow the container to only reserve 128 MiB of memory from the remaining resources on the container instance, but also allow the container to consume more memory resources when needed. The Docker daemon reserves a minimum of 4 MiB of memory for a container. Therefore, we recommend that you specify fewer than 4 MiB of memory for your containers."];
      links: StringList.t option
        [@ocaml.doc
          "The links parameter allows containers to communicate with each other without the need for port mappings. This parameter is only supported if the network mode of a task definition is bridge. The name:internalName construct is analogous to name:alias in Docker links. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed. For more information about linking Docker containers, go to Legacy container links in the Docker documentation. This parameter maps to Links in the Create a container section of the Docker Remote API and the --link option to docker run. This parameter is not supported for Windows containers. Containers that are collocated on a single container instance may be able to communicate with each other without requiring links or host port mappings. Network isolation is achieved on the container instance using security groups and VPC settings."];
      portMappings: PortMappingList.t option
        [@ocaml.doc
          "The list of port mappings for the container. Port mappings allow containers to access ports on the host container instance to send or receive traffic. For task definitions that use the awsvpc network mode, only specify the containerPort. The hostPort can be left blank or it must be the same value as the containerPort. Port mappings on Windows use the NetNAT gateway address rather than localhost. There's no loopback for port mappings on Windows, so you can't access a container's mapped port from the host itself. This parameter maps to PortBindings in the Create a container section of the Docker Remote API and the --publish option to docker run. If the network mode of a task definition is set to none, then you can't specify port mappings. If the network mode of a task definition is set to host, then host ports must either be undefined or they must match the container port in the port mapping. After a task reaches the RUNNING status, manual and automatic host and container port assignments are visible in the Network Bindings section of a container description for a selected task in the Amazon ECS console. The assignments are also visible in the networkBindings section DescribeTasks responses."];
      essential: BoxedBoolean.t option
        [@ocaml.doc
          "If the essential parameter of a container is marked as true, and that container fails or stops for any reason, all other containers that are part of the task are stopped. If the essential parameter of a container is marked as false, its failure doesn't affect the rest of the containers in a task. If this parameter is omitted, a container is assumed to be essential. All tasks must have at least one essential container. If you have an application that's composed of multiple containers, group containers that are used for a common purpose into components, and separate the different components into multiple task definitions. For more information, see Application Architecture in the Amazon Elastic Container Service Developer Guide."];
      entryPoint: StringList.t option
        [@ocaml.doc
          "Early versions of the Amazon ECS container agent don't properly handle entryPoint parameters. If you have problems using entryPoint, update your container agent or enter your commands and arguments as command array items instead. The entry point that's passed to the container. This parameter maps to Entrypoint in the Create a container section of the Docker Remote API and the --entrypoint option to docker run. For more information, see https://docs.docker.com/engine/reference/builder/#entrypoint."];
      command: StringList.t option
        [@ocaml.doc
          "The command that's passed to the container. This parameter maps to Cmd in the Create a container section of the Docker Remote API and the COMMAND parameter to docker run. For more information, see https://docs.docker.com/engine/reference/builder/#cmd. If there are multiple arguments, each argument is a separated string in the array."];
      environment: EnvironmentVariables.t option
        [@ocaml.doc
          "The environment variables to pass to a container. This parameter maps to Env in the Create a container section of the Docker Remote API and the --env option to docker run. We don't recommend that you use plaintext environment variables for sensitive information, such as credential data."];
      environmentFiles: EnvironmentFiles.t option
        [@ocaml.doc
          "A list of files containing the environment variables to pass to a container. This parameter maps to the --env-file option to docker run. You can specify up to ten environment files. The file must have a .env file extension. Each line in an environment file contains an environment variable in VARIABLE=VALUE format. Lines beginning with # are treated as comments and are ignored. For more information about the environment variable file syntax, see Declare default environment variables in file. If there are environment variables specified using the environment parameter in a container definition, they take precedence over the variables contained within an environment file. If multiple environment files are specified that contain the same variable, they're processed from the top down. We recommend that you use unique variable names. For more information, see Specifying Environment Variables in the Amazon Elastic Container Service Developer Guide."];
      mountPoints: MountPointList.t option
        [@ocaml.doc
          "The mount points for data volumes in your container. This parameter maps to Volumes in the Create a container section of the Docker Remote API and the --volume option to docker run. Windows containers can mount whole directories on the same drive as $env:ProgramData. Windows containers can't mount directories on a different drive, and mount point can't be across drives."];
      volumesFrom: VolumeFromList.t option
        [@ocaml.doc
          "Data volumes to mount from another container. This parameter maps to VolumesFrom in the Create a container section of the Docker Remote API and the --volumes-from option to docker run."];
      linuxParameters: LinuxParameters.t option
        [@ocaml.doc
          "Linux-specific modifications that are applied to the container, such as Linux kernel capabilities. For more information see KernelCapabilities. This parameter is not supported for Windows containers."];
      secrets: SecretList.t option
        [@ocaml.doc
          "The secrets to pass to the container. For more information, see Specifying Sensitive Data in the Amazon Elastic Container Service Developer Guide."];
      dependsOn: ContainerDependencies.t option
        [@ocaml.doc
          "The dependencies defined for container startup and shutdown. A container can contain multiple dependencies. When a dependency is defined for container startup, for container shutdown it is reversed. For tasks using the EC2 launch type, the container instances require at least version 1.26.0 of the container agent to turn on container dependencies. However, we recommend using the latest container agent version. For information about checking your agent version and updating to the latest version, see Updating the Amazon ECS Container Agent in the Amazon Elastic Container Service Developer Guide. If you're using an Amazon ECS-optimized Linux AMI, your instance needs at least version 1.26.0-1 of the ecs-init package. If your container instances are launched from version 20190301 or later, then they contain the required versions of the container agent and ecs-init. For more information, see Amazon ECS-optimized Linux AMI in the Amazon Elastic Container Service Developer Guide. For tasks using the Fargate launch type, the task or service requires the following platforms: Linux platform version 1.3.0 or later. Windows platform version 1.0.0 or later."];
      startTimeout: BoxedInteger.t option
        [@ocaml.doc
          "Time duration (in seconds) to wait before giving up on resolving dependencies for a container. For example, you specify two containers in a task definition with containerA having a dependency on containerB reaching a COMPLETE, SUCCESS, or HEALTHY status. If a startTimeout value is specified for containerB and it doesn't reach the desired status within that time then containerA gives up and not start. This results in the task transitioning to a STOPPED state. When the ECS_CONTAINER_START_TIMEOUT container agent configuration variable is used, it's enforced independently from this start timeout value. For tasks using the Fargate launch type, the task or service requires the following platforms: Linux platform version 1.3.0 or later. Windows platform version 1.0.0 or later. For tasks using the EC2 launch type, your container instances require at least version 1.26.0 of the container agent to use a container start timeout value. However, we recommend using the latest container agent version. For information about checking your agent version and updating to the latest version, see Updating the Amazon ECS Container Agent in the Amazon Elastic Container Service Developer Guide. If you're using an Amazon ECS-optimized Linux AMI, your instance needs at least version 1.26.0-1 of the ecs-init package. If your container instances are launched from version 20190301 or later, then they contain the required versions of the container agent and ecs-init. For more information, see Amazon ECS-optimized Linux AMI in the Amazon Elastic Container Service Developer Guide."];
      stopTimeout: BoxedInteger.t option
        [@ocaml.doc
          "Time duration (in seconds) to wait before the container is forcefully killed if it doesn't exit normally on its own. For tasks using the Fargate launch type, the task or service requires the following platforms: Linux platform version 1.3.0 or later. Windows platform version 1.0.0 or later. The max stop timeout value is 120 seconds and if the parameter is not specified, the default value of 30 seconds is used. For tasks that use the EC2 launch type, if the stopTimeout parameter isn't specified, the value set for the Amazon ECS container agent configuration variable ECS_CONTAINER_STOP_TIMEOUT is used. If neither the stopTimeout parameter or the ECS_CONTAINER_STOP_TIMEOUT agent configuration variable are set, then the default values of 30 seconds for Linux containers and 30 seconds on Windows containers are used. Your container instances require at least version 1.26.0 of the container agent to use a container stop timeout value. However, we recommend using the latest container agent version. For information about checking your agent version and updating to the latest version, see Updating the Amazon ECS Container Agent in the Amazon Elastic Container Service Developer Guide. If you're using an Amazon ECS-optimized Linux AMI, your instance needs at least version 1.26.0-1 of the ecs-init package. If your container instances are launched from version 20190301 or later, then they contain the required versions of the container agent and ecs-init. For more information, see Amazon ECS-optimized Linux AMI in the Amazon Elastic Container Service Developer Guide."];
      hostname: String_.t option
        [@ocaml.doc
          "The hostname to use for your container. This parameter maps to Hostname in the Create a container section of the Docker Remote API and the --hostname option to docker run. The hostname parameter is not supported if you're using the awsvpc network mode."];
      user: String_.t option
        [@ocaml.doc
          "The user to use inside the container. This parameter maps to User in the Create a container section of the Docker Remote API and the --user option to docker run. When running tasks using the host network mode, don't run containers using the root user (UID 0). We recommend using a non-root user for better security. You can specify the user using the following formats. If specifying a UID or GID, you must specify it as a positive integer. user user:group uid uid:gid user:gid uid:group This parameter is not supported for Windows containers."];
      workingDirectory: String_.t option
        [@ocaml.doc
          "The working directory to run commands inside the container in. This parameter maps to WorkingDir in the Create a container section of the Docker Remote API and the --workdir option to docker run."];
      disableNetworking: BoxedBoolean.t option
        [@ocaml.doc
          "When this parameter is true, networking is disabled within the container. This parameter maps to NetworkDisabled in the Create a container section of the Docker Remote API. This parameter is not supported for Windows containers."];
      privileged: BoxedBoolean.t option
        [@ocaml.doc
          "When this parameter is true, the container is given elevated privileges on the host container instance (similar to the root user). This parameter maps to Privileged in the Create a container section of the Docker Remote API and the --privileged option to docker run. This parameter is not supported for Windows containers or tasks run on Fargate."];
      readonlyRootFilesystem: BoxedBoolean.t option
        [@ocaml.doc
          "When this parameter is true, the container is given read-only access to its root file system. This parameter maps to ReadonlyRootfs in the Create a container section of the Docker Remote API and the --read-only option to docker run. This parameter is not supported for Windows containers."];
      dnsServers: StringList.t option
        [@ocaml.doc
          "A list of DNS servers that are presented to the container. This parameter maps to Dns in the Create a container section of the Docker Remote API and the --dns option to docker run. This parameter is not supported for Windows containers."];
      dnsSearchDomains: StringList.t option
        [@ocaml.doc
          "A list of DNS search domains that are presented to the container. This parameter maps to DnsSearch in the Create a container section of the Docker Remote API and the --dns-search option to docker run. This parameter is not supported for Windows containers."];
      extraHosts: HostEntryList.t option
        [@ocaml.doc
          "A list of hostnames and IP address mappings to append to the /etc/hosts file on the container. This parameter maps to ExtraHosts in the Create a container section of the Docker Remote API and the --add-host option to docker run. This parameter isn't supported for Windows containers or tasks that use the awsvpc network mode."];
      dockerSecurityOptions: StringList.t option
        [@ocaml.doc
          "A list of strings to provide custom labels for SELinux and AppArmor multi-level security systems. This field isn't valid for containers in tasks using the Fargate launch type. With Windows containers, this parameter can be used to reference a credential spec file when configuring a container for Active Directory authentication. For more information, see Using gMSAs for Windows Containers in the Amazon Elastic Container Service Developer Guide. This parameter maps to SecurityOpt in the Create a container section of the Docker Remote API and the --security-opt option to docker run. The Amazon ECS container agent running on a container instance must register with the ECS_SELINUX_CAPABLE=true or ECS_APPARMOR_CAPABLE=true environment variables before containers placed on that instance can use these security options. For more information, see Amazon ECS Container Agent Configuration in the Amazon Elastic Container Service Developer Guide. For more information about valid values, see Docker Run Security Configuration. Valid values: \"no-new-privileges\" | \"apparmor:PROFILE\" | \"label:value\" | \"credentialspec:CredentialSpecFilePath\""];
      interactive: BoxedBoolean.t option
        [@ocaml.doc
          "When this parameter is true, you can deploy containerized applications that require stdin or a tty to be allocated. This parameter maps to OpenStdin in the Create a container section of the Docker Remote API and the --interactive option to docker run."];
      pseudoTerminal: BoxedBoolean.t option
        [@ocaml.doc
          "When this parameter is true, a TTY is allocated. This parameter maps to Tty in the Create a container section of the Docker Remote API and the --tty option to docker run."];
      dockerLabels: DockerLabelsMap.t option
        [@ocaml.doc
          "A key/value map of labels to add to the container. This parameter maps to Labels in the Create a container section of the Docker Remote API and the --label option to docker run. This parameter requires version 1.18 of the Docker Remote API or greater on your container instance. To check the Docker Remote API version on your container instance, log in to your container instance and run the following command: sudo docker version --format '\\{\\{.Server.APIVersion\\}\\}'"];
      ulimits: UlimitList.t option
        [@ocaml.doc
          "A list of ulimits to set in the container. If a ulimit value is specified in a task definition, it overrides the default values set by Docker. This parameter maps to Ulimits in the Create a container section of the Docker Remote API and the --ulimit option to docker run. Valid naming values are displayed in the Ulimit data type. Amazon ECS tasks hosted on Fargate use the default resource limit values set by the operating system with the exception of the nofile resource limit parameter which Fargate overrides. The nofile resource limit sets a restriction on the number of open files that a container can use. The default nofile soft limit is 1024 and hard limit is 4096. This parameter requires version 1.18 of the Docker Remote API or greater on your container instance. To check the Docker Remote API version on your container instance, log in to your container instance and run the following command: sudo docker version --format '\\{\\{.Server.APIVersion\\}\\}' This parameter is not supported for Windows containers."];
      logConfiguration: LogConfiguration.t option
        [@ocaml.doc
          "The log configuration specification for the container. This parameter maps to LogConfig in the Create a container section of the Docker Remote API and the --log-driver option to docker run. By default, containers use the same logging driver that the Docker daemon uses. However the container can use a different logging driver than the Docker daemon by specifying a log driver with this parameter in the container definition. To use a different logging driver for a container, the log system must be configured properly on the container instance (or on a different log server for remote logging options). For more information about the options for different supported log drivers, see Configure logging drivers in the Docker documentation. Amazon ECS currently supports a subset of the logging drivers available to the Docker daemon (shown in the LogConfiguration data type). Additional log drivers may be available in future releases of the Amazon ECS container agent. This parameter requires version 1.18 of the Docker Remote API or greater on your container instance. To check the Docker Remote API version on your container instance, log in to your container instance and run the following command: sudo docker version --format '\\{\\{.Server.APIVersion\\}\\}' The Amazon ECS container agent running on a container instance must register the logging drivers available on that instance with the ECS_AVAILABLE_LOGGING_DRIVERS environment variable before containers placed on that instance can use these log configuration options. For more information, see Amazon ECS Container Agent Configuration in the Amazon Elastic Container Service Developer Guide."];
      healthCheck: HealthCheck.t option
        [@ocaml.doc
          "The container health check command and associated configuration parameters for the container. This parameter maps to HealthCheck in the Create a container section of the Docker Remote API and the HEALTHCHECK parameter of docker run."];
      systemControls: SystemControls.t option
        [@ocaml.doc
          "A list of namespaced kernel parameters to set in the container. This parameter maps to Sysctls in the Create a container section of the Docker Remote API and the --sysctl option to docker run. We don't recommended that you specify network-related systemControls parameters for multiple containers in a single task that also uses either the awsvpc or host network modes. For tasks that use the awsvpc network mode, the container that's started last determines which systemControls parameters take effect. For tasks that use the host network mode, it changes the container instance's namespaced kernel parameters as well as the containers."];
      resourceRequirements: ResourceRequirements.t option
        [@ocaml.doc
          "The type and amount of a resource to assign to a container. The only supported resource is a GPU."];
      firelensConfiguration: FirelensConfiguration.t option
        [@ocaml.doc
          "The FireLens configuration for the container. This is used to specify and configure a log router for container logs. For more information, see Custom Log Routing in the Amazon Elastic Container Service Developer Guide."]}
    let make ?name =
      fun ?image ->
        fun ?repositoryCredentials ->
          fun ?cpu ->
            fun ?memory ->
              fun ?memoryReservation ->
                fun ?links ->
                  fun ?portMappings ->
                    fun ?essential ->
                      fun ?entryPoint ->
                        fun ?command ->
                          fun ?environment ->
                            fun ?environmentFiles ->
                              fun ?mountPoints ->
                                fun ?volumesFrom ->
                                  fun ?linuxParameters ->
                                    fun ?secrets ->
                                      fun ?dependsOn ->
                                        fun ?startTimeout ->
                                          fun ?stopTimeout ->
                                            fun ?hostname ->
                                              fun ?user ->
                                                fun ?workingDirectory ->
                                                  fun ?disableNetworking ->
                                                    fun ?privileged ->
                                                      fun
                                                        ?readonlyRootFilesystem
                                                        ->
                                                        fun ?dnsServers ->
                                                          fun
                                                            ?dnsSearchDomains
                                                            ->
                                                            fun ?extraHosts
                                                              ->
                                                              fun
                                                                ?dockerSecurityOptions
                                                                ->
                                                                fun
                                                                  ?interactive
                                                                  ->
                                                                  fun
                                                                    ?pseudoTerminal
                                                                    ->
                                                                    fun
                                                                    ?dockerLabels
                                                                    ->
                                                                    fun
                                                                    ?ulimits
                                                                    ->
                                                                    fun
                                                                    ?logConfiguration
                                                                    ->
                                                                    fun
                                                                    ?healthCheck
                                                                    ->
                                                                    fun
                                                                    ?systemControls
                                                                    ->
                                                                    fun
                                                                    ?resourceRequirements
                                                                    ->
                                                                    fun
                                                                    ?firelensConfiguration
                                                                    ->
                                                                    fun () ->
                                                                    {
                                                                    name;
                                                                    image;
                                                                    repositoryCredentials;
                                                                    cpu;
                                                                    memory;
                                                                    memoryReservation;
                                                                    links;
                                                                    portMappings;
                                                                    essential;
                                                                    entryPoint;
                                                                    command;
                                                                    environment;
                                                                    environmentFiles;
                                                                    mountPoints;
                                                                    volumesFrom;
                                                                    linuxParameters;
                                                                    secrets;
                                                                    dependsOn;
                                                                    startTimeout;
                                                                    stopTimeout;
                                                                    hostname;
                                                                    user;
                                                                    workingDirectory;
                                                                    disableNetworking;
                                                                    privileged;
                                                                    readonlyRootFilesystem;
                                                                    dnsServers;
                                                                    dnsSearchDomains;
                                                                    extraHosts;
                                                                    dockerSecurityOptions;
                                                                    interactive;
                                                                    pseudoTerminal;
                                                                    dockerLabels;
                                                                    ulimits;
                                                                    logConfiguration;
                                                                    healthCheck;
                                                                    systemControls;
                                                                    resourceRequirements;
                                                                    firelensConfiguration
                                                                    }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("image", (Option.map x.image ~f:String_.to_value));
        ("repositoryCredentials",
          (Option.map x.repositoryCredentials
             ~f:RepositoryCredentials.to_value));
        ("cpu", (Option.map x.cpu ~f:Integer.to_value));
        ("memory", (Option.map x.memory ~f:BoxedInteger.to_value));
        ("memoryReservation",
          (Option.map x.memoryReservation ~f:BoxedInteger.to_value));
        ("links", (Option.map x.links ~f:StringList.to_value));
        ("portMappings",
          (Option.map x.portMappings ~f:PortMappingList.to_value));
        ("essential", (Option.map x.essential ~f:BoxedBoolean.to_value));
        ("entryPoint", (Option.map x.entryPoint ~f:StringList.to_value));
        ("command", (Option.map x.command ~f:StringList.to_value));
        ("environment",
          (Option.map x.environment ~f:EnvironmentVariables.to_value));
        ("environmentFiles",
          (Option.map x.environmentFiles ~f:EnvironmentFiles.to_value));
        ("mountPoints",
          (Option.map x.mountPoints ~f:MountPointList.to_value));
        ("volumesFrom",
          (Option.map x.volumesFrom ~f:VolumeFromList.to_value));
        ("linuxParameters",
          (Option.map x.linuxParameters ~f:LinuxParameters.to_value));
        ("secrets", (Option.map x.secrets ~f:SecretList.to_value));
        ("dependsOn",
          (Option.map x.dependsOn ~f:ContainerDependencies.to_value));
        ("startTimeout",
          (Option.map x.startTimeout ~f:BoxedInteger.to_value));
        ("stopTimeout", (Option.map x.stopTimeout ~f:BoxedInteger.to_value));
        ("hostname", (Option.map x.hostname ~f:String_.to_value));
        ("user", (Option.map x.user ~f:String_.to_value));
        ("workingDirectory",
          (Option.map x.workingDirectory ~f:String_.to_value));
        ("disableNetworking",
          (Option.map x.disableNetworking ~f:BoxedBoolean.to_value));
        ("privileged", (Option.map x.privileged ~f:BoxedBoolean.to_value));
        ("readonlyRootFilesystem",
          (Option.map x.readonlyRootFilesystem ~f:BoxedBoolean.to_value));
        ("dnsServers", (Option.map x.dnsServers ~f:StringList.to_value));
        ("dnsSearchDomains",
          (Option.map x.dnsSearchDomains ~f:StringList.to_value));
        ("extraHosts", (Option.map x.extraHosts ~f:HostEntryList.to_value));
        ("dockerSecurityOptions",
          (Option.map x.dockerSecurityOptions ~f:StringList.to_value));
        ("interactive", (Option.map x.interactive ~f:BoxedBoolean.to_value));
        ("pseudoTerminal",
          (Option.map x.pseudoTerminal ~f:BoxedBoolean.to_value));
        ("dockerLabels",
          (Option.map x.dockerLabels ~f:DockerLabelsMap.to_value));
        ("ulimits", (Option.map x.ulimits ~f:UlimitList.to_value));
        ("logConfiguration",
          (Option.map x.logConfiguration ~f:LogConfiguration.to_value));
        ("healthCheck", (Option.map x.healthCheck ~f:HealthCheck.to_value));
        ("systemControls",
          (Option.map x.systemControls ~f:SystemControls.to_value));
        ("resourceRequirements",
          (Option.map x.resourceRequirements ~f:ResourceRequirements.to_value));
        ("firelensConfiguration",
          (Option.map x.firelensConfiguration
             ~f:FirelensConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let firelensConfiguration =
        (Option.map ~f:FirelensConfiguration.of_xml)
          (Xml.child xml_arg0 "firelensConfiguration") in
      let resourceRequirements =
        (Option.map ~f:ResourceRequirements.of_xml)
          (Xml.child xml_arg0 "resourceRequirements") in
      let systemControls =
        (Option.map ~f:SystemControls.of_xml)
          (Xml.child xml_arg0 "systemControls") in
      let healthCheck =
        (Option.map ~f:HealthCheck.of_xml) (Xml.child xml_arg0 "healthCheck") in
      let logConfiguration =
        (Option.map ~f:LogConfiguration.of_xml)
          (Xml.child xml_arg0 "logConfiguration") in
      let ulimits =
        (Option.map ~f:UlimitList.of_xml) (Xml.child xml_arg0 "ulimits") in
      let dockerLabels =
        (Option.map ~f:DockerLabelsMap.of_xml)
          (Xml.child xml_arg0 "dockerLabels") in
      let pseudoTerminal =
        (Option.map ~f:BoxedBoolean.of_xml)
          (Xml.child xml_arg0 "pseudoTerminal") in
      let interactive =
        (Option.map ~f:BoxedBoolean.of_xml)
          (Xml.child xml_arg0 "interactive") in
      let dockerSecurityOptions =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "dockerSecurityOptions") in
      let extraHosts =
        (Option.map ~f:HostEntryList.of_xml)
          (Xml.child xml_arg0 "extraHosts") in
      let dnsSearchDomains =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "dnsSearchDomains") in
      let dnsServers =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "dnsServers") in
      let readonlyRootFilesystem =
        (Option.map ~f:BoxedBoolean.of_xml)
          (Xml.child xml_arg0 "readonlyRootFilesystem") in
      let privileged =
        (Option.map ~f:BoxedBoolean.of_xml) (Xml.child xml_arg0 "privileged") in
      let disableNetworking =
        (Option.map ~f:BoxedBoolean.of_xml)
          (Xml.child xml_arg0 "disableNetworking") in
      let workingDirectory =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "workingDirectory") in
      let user = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "user") in
      let hostname =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "hostname") in
      let stopTimeout =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "stopTimeout") in
      let startTimeout =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "startTimeout") in
      let dependsOn =
        (Option.map ~f:ContainerDependencies.of_xml)
          (Xml.child xml_arg0 "dependsOn") in
      let secrets =
        (Option.map ~f:SecretList.of_xml) (Xml.child xml_arg0 "secrets") in
      let linuxParameters =
        (Option.map ~f:LinuxParameters.of_xml)
          (Xml.child xml_arg0 "linuxParameters") in
      let volumesFrom =
        (Option.map ~f:VolumeFromList.of_xml)
          (Xml.child xml_arg0 "volumesFrom") in
      let mountPoints =
        (Option.map ~f:MountPointList.of_xml)
          (Xml.child xml_arg0 "mountPoints") in
      let environmentFiles =
        (Option.map ~f:EnvironmentFiles.of_xml)
          (Xml.child xml_arg0 "environmentFiles") in
      let environment =
        (Option.map ~f:EnvironmentVariables.of_xml)
          (Xml.child xml_arg0 "environment") in
      let command =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "command") in
      let entryPoint =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "entryPoint") in
      let essential =
        (Option.map ~f:BoxedBoolean.of_xml) (Xml.child xml_arg0 "essential") in
      let portMappings =
        (Option.map ~f:PortMappingList.of_xml)
          (Xml.child xml_arg0 "portMappings") in
      let links =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "links") in
      let memoryReservation =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "memoryReservation") in
      let memory =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "memory") in
      let cpu = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "cpu") in
      let repositoryCredentials =
        (Option.map ~f:RepositoryCredentials.of_xml)
          (Xml.child xml_arg0 "repositoryCredentials") in
      let image = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "image") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?firelensConfiguration ?resourceRequirements ?systemControls
        ?healthCheck ?logConfiguration ?ulimits ?dockerLabels ?pseudoTerminal
        ?interactive ?dockerSecurityOptions ?extraHosts ?dnsSearchDomains
        ?dnsServers ?readonlyRootFilesystem ?privileged ?disableNetworking
        ?workingDirectory ?user ?hostname ?stopTimeout ?startTimeout
        ?dependsOn ?secrets ?linuxParameters ?volumesFrom ?mountPoints
        ?environmentFiles ?environment ?command ?entryPoint ?essential
        ?portMappings ?links ?memoryReservation ?memory ?cpu
        ?repositoryCredentials ?image ?name ()
    let of_json json =
      let firelensConfiguration =
        field_map json "firelensConfiguration" FirelensConfiguration.of_json in
      let resourceRequirements =
        field_map json "resourceRequirements" ResourceRequirements.of_json in
      let systemControls =
        field_map json "systemControls" SystemControls.of_json in
      let healthCheck = field_map json "healthCheck" HealthCheck.of_json in
      let logConfiguration =
        field_map json "logConfiguration" LogConfiguration.of_json in
      let ulimits = field_map json "ulimits" UlimitList.of_json in
      let dockerLabels =
        field_map json "dockerLabels" DockerLabelsMap.of_json in
      let pseudoTerminal =
        field_map json "pseudoTerminal" BoxedBoolean.of_json in
      let interactive = field_map json "interactive" BoxedBoolean.of_json in
      let dockerSecurityOptions =
        field_map json "dockerSecurityOptions" StringList.of_json in
      let extraHosts = field_map json "extraHosts" HostEntryList.of_json in
      let dnsSearchDomains =
        field_map json "dnsSearchDomains" StringList.of_json in
      let dnsServers = field_map json "dnsServers" StringList.of_json in
      let readonlyRootFilesystem =
        field_map json "readonlyRootFilesystem" BoxedBoolean.of_json in
      let privileged = field_map json "privileged" BoxedBoolean.of_json in
      let disableNetworking =
        field_map json "disableNetworking" BoxedBoolean.of_json in
      let workingDirectory =
        field_map json "workingDirectory" String_.of_json in
      let user = field_map json "user" String_.of_json in
      let hostname = field_map json "hostname" String_.of_json in
      let stopTimeout = field_map json "stopTimeout" BoxedInteger.of_json in
      let startTimeout = field_map json "startTimeout" BoxedInteger.of_json in
      let dependsOn =
        field_map json "dependsOn" ContainerDependencies.of_json in
      let secrets = field_map json "secrets" SecretList.of_json in
      let linuxParameters =
        field_map json "linuxParameters" LinuxParameters.of_json in
      let volumesFrom = field_map json "volumesFrom" VolumeFromList.of_json in
      let mountPoints = field_map json "mountPoints" MountPointList.of_json in
      let environmentFiles =
        field_map json "environmentFiles" EnvironmentFiles.of_json in
      let environment =
        field_map json "environment" EnvironmentVariables.of_json in
      let command = field_map json "command" StringList.of_json in
      let entryPoint = field_map json "entryPoint" StringList.of_json in
      let essential = field_map json "essential" BoxedBoolean.of_json in
      let portMappings =
        field_map json "portMappings" PortMappingList.of_json in
      let links = field_map json "links" StringList.of_json in
      let memoryReservation =
        field_map json "memoryReservation" BoxedInteger.of_json in
      let memory = field_map json "memory" BoxedInteger.of_json in
      let cpu = field_map json "cpu" Integer.of_json in
      let repositoryCredentials =
        field_map json "repositoryCredentials" RepositoryCredentials.of_json in
      let image = field_map json "image" String_.of_json in
      let name = field_map json "name" String_.of_json in
      make ?firelensConfiguration ?resourceRequirements ?systemControls
        ?healthCheck ?logConfiguration ?ulimits ?dockerLabels ?pseudoTerminal
        ?interactive ?dockerSecurityOptions ?extraHosts ?dnsSearchDomains
        ?dnsServers ?readonlyRootFilesystem ?privileged ?disableNetworking
        ?workingDirectory ?user ?hostname ?stopTimeout ?startTimeout
        ?dependsOn ?secrets ?linuxParameters ?volumesFrom ?mountPoints
        ?environmentFiles ?environment ?command ?entryPoint ?essential
        ?portMappings ?links ?memoryReservation ?memory ?cpu
        ?repositoryCredentials ?image ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container definitions are used in task definitions to describe the different containers that are launched as part of a task."]
module ProxyConfigurationProperties =
  struct
    type nonrec t = KeyValuePair.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:KeyValuePair.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:KeyValuePair.of_xml)
    let of_json j =
      list_of_json ~kind:"ProxyConfigurationProperties"
        ~of_json:KeyValuePair.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProxyConfigurationType =
  struct
    type nonrec t =
      | APPMESH 
      | Non_static_id of string 
    let make i = i
    let to_string = function | APPMESH -> "APPMESH" | Non_static_id s -> s
    let of_string = function | "APPMESH" -> APPMESH | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProxyConfigurationType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ProxyConfigurationType" j)
    let to_json = simple_to_json to_value
  end
module CPUArchitecture =
  struct
    type nonrec t =
      | X86_64 
      | ARM64 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | X86_64 -> "X86_64" | ARM64 -> "ARM64" | Non_static_id s -> s
    let of_string =
      function | "X86_64" -> X86_64 | "ARM64" -> ARM64 | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CPUArchitecture" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CPUArchitecture" j)
    let to_json = simple_to_json to_value
  end
module OSFamily =
  struct
    type nonrec t =
      | WINDOWS_SERVER_2019_FULL 
      | WINDOWS_SERVER_2019_CORE 
      | WINDOWS_SERVER_2016_FULL 
      | WINDOWS_SERVER_2004_CORE 
      | WINDOWS_SERVER_2022_CORE 
      | WINDOWS_SERVER_2022_FULL 
      | WINDOWS_SERVER_20H2_CORE 
      | LINUX 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | WINDOWS_SERVER_2019_FULL -> "WINDOWS_SERVER_2019_FULL"
      | WINDOWS_SERVER_2019_CORE -> "WINDOWS_SERVER_2019_CORE"
      | WINDOWS_SERVER_2016_FULL -> "WINDOWS_SERVER_2016_FULL"
      | WINDOWS_SERVER_2004_CORE -> "WINDOWS_SERVER_2004_CORE"
      | WINDOWS_SERVER_2022_CORE -> "WINDOWS_SERVER_2022_CORE"
      | WINDOWS_SERVER_2022_FULL -> "WINDOWS_SERVER_2022_FULL"
      | WINDOWS_SERVER_20H2_CORE -> "WINDOWS_SERVER_20H2_CORE"
      | LINUX -> "LINUX"
      | Non_static_id s -> s
    let of_string =
      function
      | "WINDOWS_SERVER_2019_FULL" -> WINDOWS_SERVER_2019_FULL
      | "WINDOWS_SERVER_2019_CORE" -> WINDOWS_SERVER_2019_CORE
      | "WINDOWS_SERVER_2016_FULL" -> WINDOWS_SERVER_2016_FULL
      | "WINDOWS_SERVER_2004_CORE" -> WINDOWS_SERVER_2004_CORE
      | "WINDOWS_SERVER_2022_CORE" -> WINDOWS_SERVER_2022_CORE
      | "WINDOWS_SERVER_2022_FULL" -> WINDOWS_SERVER_2022_FULL
      | "WINDOWS_SERVER_20H2_CORE" -> WINDOWS_SERVER_20H2_CORE
      | "LINUX" -> LINUX
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration OSFamily" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"OSFamily" j)
    let to_json = simple_to_json to_value
  end
module TaskDefinitionPlacementConstraint =
  struct
    type nonrec t =
      {
      type_: TaskDefinitionPlacementConstraintType.t option
        [@ocaml.doc
          "The type of constraint. The MemberOf constraint restricts selection to be from a group of valid candidates."];
      expression: String_.t option
        [@ocaml.doc
          "A cluster query language expression to apply to the constraint. For more information, see Cluster query language in the Amazon Elastic Container Service Developer Guide."]}
    let make ?type_ = fun ?expression -> fun () -> { type_; expression }
    let to_value x =
      structure_to_value
        [("type",
           (Option.map x.type_
              ~f:TaskDefinitionPlacementConstraintType.to_value));
        ("expression", (Option.map x.expression ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expression =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "expression") in
      let type_ =
        (Option.map ~f:TaskDefinitionPlacementConstraintType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?expression ?type_ ()
    let of_json json =
      let expression = field_map json "expression" String_.of_json in
      let type_ =
        field_map json "type" TaskDefinitionPlacementConstraintType.of_json in
      make ?expression ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing a constraint on task placement in the task definition. For more information, see Task placement constraints in the Amazon Elastic Container Service Developer Guide. Task placement constraints aren't supported for tasks run on Fargate."]
module Volume =
  struct
    type nonrec t =
      {
      name: String_.t option
        [@ocaml.doc
          "The name of the volume. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed. This name is referenced in the sourceVolume parameter of container definition mountPoints."];
      host: HostVolumeProperties.t option
        [@ocaml.doc
          "This parameter is specified when you use bind mount host volumes. The contents of the host parameter determine whether your bind mount host volume persists on the host container instance and where it's stored. If the host parameter is empty, then the Docker daemon assigns a host path for your data volume. However, the data isn't guaranteed to persist after the containers that are associated with it stop running. Windows containers can mount whole directories on the same drive as $env:ProgramData. Windows containers can't mount directories on a different drive, and mount point can't be across drives. For example, you can mount C:\\my\\path:C:\\my\\path and D:\\:D:\\, but not D:\\my\\path:C:\\my\\path or D:\\:C:\\my\\path."];
      dockerVolumeConfiguration: DockerVolumeConfiguration.t option
        [@ocaml.doc
          "This parameter is specified when you use Docker volumes. Windows containers only support the use of the local driver. To use bind mounts, specify the host parameter instead. Docker volumes aren't supported by tasks run on Fargate."];
      efsVolumeConfiguration: EFSVolumeConfiguration.t option
        [@ocaml.doc
          "This parameter is specified when you use an Amazon Elastic File System file system for task storage."];
      fsxWindowsFileServerVolumeConfiguration:
        FSxWindowsFileServerVolumeConfiguration.t option
        [@ocaml.doc
          "This parameter is specified when you use Amazon FSx for Windows File Server file system for task storage."]}
    let make ?name =
      fun ?host ->
        fun ?dockerVolumeConfiguration ->
          fun ?efsVolumeConfiguration ->
            fun ?fsxWindowsFileServerVolumeConfiguration ->
              fun () ->
                {
                  name;
                  host;
                  dockerVolumeConfiguration;
                  efsVolumeConfiguration;
                  fsxWindowsFileServerVolumeConfiguration
                }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("host", (Option.map x.host ~f:HostVolumeProperties.to_value));
        ("dockerVolumeConfiguration",
          (Option.map x.dockerVolumeConfiguration
             ~f:DockerVolumeConfiguration.to_value));
        ("efsVolumeConfiguration",
          (Option.map x.efsVolumeConfiguration
             ~f:EFSVolumeConfiguration.to_value));
        ("fsxWindowsFileServerVolumeConfiguration",
          (Option.map x.fsxWindowsFileServerVolumeConfiguration
             ~f:FSxWindowsFileServerVolumeConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fsxWindowsFileServerVolumeConfiguration =
        (Option.map ~f:FSxWindowsFileServerVolumeConfiguration.of_xml)
          (Xml.child xml_arg0 "fsxWindowsFileServerVolumeConfiguration") in
      let efsVolumeConfiguration =
        (Option.map ~f:EFSVolumeConfiguration.of_xml)
          (Xml.child xml_arg0 "efsVolumeConfiguration") in
      let dockerVolumeConfiguration =
        (Option.map ~f:DockerVolumeConfiguration.of_xml)
          (Xml.child xml_arg0 "dockerVolumeConfiguration") in
      let host =
        (Option.map ~f:HostVolumeProperties.of_xml)
          (Xml.child xml_arg0 "host") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?fsxWindowsFileServerVolumeConfiguration ?efsVolumeConfiguration
        ?dockerVolumeConfiguration ?host ?name ()
    let of_json json =
      let fsxWindowsFileServerVolumeConfiguration =
        field_map json "fsxWindowsFileServerVolumeConfiguration"
          FSxWindowsFileServerVolumeConfiguration.of_json in
      let efsVolumeConfiguration =
        field_map json "efsVolumeConfiguration"
          EFSVolumeConfiguration.of_json in
      let dockerVolumeConfiguration =
        field_map json "dockerVolumeConfiguration"
          DockerVolumeConfiguration.of_json in
      let host = field_map json "host" HostVolumeProperties.of_json in
      let name = field_map json "name" String_.of_json in
      make ?fsxWindowsFileServerVolumeConfiguration ?efsVolumeConfiguration
        ?dockerVolumeConfiguration ?host ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A data volume that's used in a task definition. For tasks that use the Amazon Elastic File System (Amazon EFS), specify an efsVolumeConfiguration. For Windows tasks that use Amazon FSx for Windows File Server file system, specify a fsxWindowsFileServerVolumeConfiguration. For tasks that use a Docker volume, specify a DockerVolumeConfiguration. For tasks that use a bind mount host volume, specify a host and optional sourcePath. For more information, see Using Data Volumes in Tasks."]
module PlatformDeviceType =
  struct
    type nonrec t =
      | GPU 
      | Non_static_id of string 
    let make i = i
    let to_string = function | GPU -> "GPU" | Non_static_id s -> s
    let of_string = function | "GPU" -> GPU | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PlatformDeviceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PlatformDeviceType" j)
    let to_json = simple_to_json to_value
  end
module SettingName =
  struct
    type nonrec t =
      | ServiceLongArnFormat 
      | TaskLongArnFormat 
      | ContainerInstanceLongArnFormat 
      | AwsvpcTrunking 
      | ContainerInsights 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ServiceLongArnFormat -> "serviceLongArnFormat"
      | TaskLongArnFormat -> "taskLongArnFormat"
      | ContainerInstanceLongArnFormat -> "containerInstanceLongArnFormat"
      | AwsvpcTrunking -> "awsvpcTrunking"
      | ContainerInsights -> "containerInsights"
      | Non_static_id s -> s
    let of_string =
      function
      | "serviceLongArnFormat" -> ServiceLongArnFormat
      | "taskLongArnFormat" -> TaskLongArnFormat
      | "containerInstanceLongArnFormat" -> ContainerInstanceLongArnFormat
      | "awsvpcTrunking" -> AwsvpcTrunking
      | "containerInsights" -> ContainerInsights
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SettingName" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SettingName" j)
    let to_json = simple_to_json to_value
  end
module DeploymentConfiguration =
  struct
    type nonrec t =
      {
      deploymentCircuitBreaker: DeploymentCircuitBreaker.t option
        [@ocaml.doc
          "The deployment circuit breaker can only be used for services using the rolling update (ECS) deployment type. The deployment circuit breaker determines whether a service deployment will fail if the service can't reach a steady state. If deployment circuit breaker is enabled, a service deployment will transition to a failed state and stop launching new tasks. If rollback is enabled, when a service deployment fails, the service is rolled back to the last deployment that completed successfully."];
      maximumPercent: BoxedInteger.t option
        [@ocaml.doc
          "If a service is using the rolling update (ECS) deployment type, the maximum percent parameter represents an upper limit on the number of tasks in a service that are allowed in the RUNNING or PENDING state during a deployment, as a percentage of the desired number of tasks (rounded down to the nearest integer), and while any container instances are in the DRAINING state if the service contains tasks using the EC2 launch type. This parameter enables you to define the deployment batch size. For example, if your service has a desired number of four tasks and a maximum percent value of 200%, the scheduler may start four new tasks before stopping the four older tasks (provided that the cluster resources required to do this are available). The default value for maximum percent is 200%. If a service is using the blue/green (CODE_DEPLOY) or EXTERNAL deployment types and tasks that use the EC2 launch type, the maximum percent value is set to the default value and is used to define the upper limit on the number of the tasks in the service that remain in the RUNNING state while the container instances are in the DRAINING state. If the tasks in the service use the Fargate launch type, the maximum percent value is not used, although it is returned when describing your service."];
      minimumHealthyPercent: BoxedInteger.t option
        [@ocaml.doc
          "If a service is using the rolling update (ECS) deployment type, the minimum healthy percent represents a lower limit on the number of tasks in a service that must remain in the RUNNING state during a deployment, as a percentage of the desired number of tasks (rounded up to the nearest integer), and while any container instances are in the DRAINING state if the service contains tasks using the EC2 launch type. This parameter enables you to deploy without using additional cluster capacity. For example, if your service has a desired number of four tasks and a minimum healthy percent of 50%, the scheduler may stop two existing tasks to free up cluster capacity before starting two new tasks. Tasks for services that do not use a load balancer are considered healthy if they're in the RUNNING state; tasks for services that do use a load balancer are considered healthy if they're in the RUNNING state and they're reported as healthy by the load balancer. The default value for minimum healthy percent is 100%. If a service is using the blue/green (CODE_DEPLOY) or EXTERNAL deployment types and tasks that use the EC2 launch type, the minimum healthy percent value is set to the default value and is used to define the lower limit on the number of the tasks in the service that remain in the RUNNING state while the container instances are in the DRAINING state. If the tasks in the service use the Fargate launch type, the minimum healthy percent value is not used, although it is returned when describing your service."]}
    let make ?deploymentCircuitBreaker =
      fun ?maximumPercent ->
        fun ?minimumHealthyPercent ->
          fun () ->
            { deploymentCircuitBreaker; maximumPercent; minimumHealthyPercent
            }
    let to_value x =
      structure_to_value
        [("deploymentCircuitBreaker",
           (Option.map x.deploymentCircuitBreaker
              ~f:DeploymentCircuitBreaker.to_value));
        ("maximumPercent",
          (Option.map x.maximumPercent ~f:BoxedInteger.to_value));
        ("minimumHealthyPercent",
          (Option.map x.minimumHealthyPercent ~f:BoxedInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let minimumHealthyPercent =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "minimumHealthyPercent") in
      let maximumPercent =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "maximumPercent") in
      let deploymentCircuitBreaker =
        (Option.map ~f:DeploymentCircuitBreaker.of_xml)
          (Xml.child xml_arg0 "deploymentCircuitBreaker") in
      make ?minimumHealthyPercent ?maximumPercent ?deploymentCircuitBreaker
        ()
    let of_json json =
      let minimumHealthyPercent =
        field_map json "minimumHealthyPercent" BoxedInteger.of_json in
      let maximumPercent =
        field_map json "maximumPercent" BoxedInteger.of_json in
      let deploymentCircuitBreaker =
        field_map json "deploymentCircuitBreaker"
          DeploymentCircuitBreaker.of_json in
      make ?minimumHealthyPercent ?maximumPercent ?deploymentCircuitBreaker
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Optional deployment parameters that control how many tasks run during a deployment and the ordering of stopping and starting tasks."]
module DeploymentController =
  struct
    type nonrec t =
      {
      type_: DeploymentControllerType.t
        [@ocaml.doc
          "The deployment controller type to use. There are three deployment controller types available: ECS The rolling update (ECS) deployment type involves replacing the current running version of the container with the latest version. The number of containers Amazon ECS adds or removes from the service during a rolling update is controlled by adjusting the minimum and maximum number of healthy tasks allowed during a service deployment, as specified in the DeploymentConfiguration. CODE_DEPLOY The blue/green (CODE_DEPLOY) deployment type uses the blue/green deployment model powered by CodeDeploy, which allows you to verify a new deployment of a service before sending production traffic to it. EXTERNAL The external (EXTERNAL) deployment type enables you to use any third-party deployment controller for full control over the deployment process for an Amazon ECS service."]}
    let context_ = "DeploymentController"
    let make ~type_ = fun () -> { type_ }
    let to_value x =
      structure_to_value
        [("type", (Some (DeploymentControllerType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        DeploymentControllerType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ~type_ ()
    let of_json json =
      let type_ = field_map_exn json "type" DeploymentControllerType.of_json in
      make ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The deployment controller to use for the service. For more information, see Amazon ECS Deployment Types in the Amazon Elastic Container Service Developer Guide."]
module Deployments =
  struct
    type nonrec t = Deployment.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Deployment.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Deployment.of_xml)
    let of_json j =
      list_of_json ~kind:"Deployments" ~of_json:Deployment.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlacementConstraints =
  struct
    type nonrec t = PlacementConstraint.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PlacementConstraint.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlacementConstraint.of_xml)
    let of_json j =
      list_of_json ~kind:"PlacementConstraints"
        ~of_json:PlacementConstraint.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlacementStrategies =
  struct
    type nonrec t = PlacementStrategy.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PlacementStrategy.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlacementStrategy.of_xml)
    let of_json j =
      list_of_json ~kind:"PlacementStrategies"
        ~of_json:PlacementStrategy.of_json j
    let to_json v = composed_to_json to_value v
  end
module PropagateTags =
  struct
    type nonrec t =
      | TASK_DEFINITION 
      | SERVICE 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TASK_DEFINITION -> "TASK_DEFINITION"
      | SERVICE -> "SERVICE"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "TASK_DEFINITION" -> TASK_DEFINITION
      | "SERVICE" -> SERVICE
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PropagateTags" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PropagateTags" j)
    let to_json = simple_to_json to_value
  end
module SchedulingStrategy =
  struct
    type nonrec t =
      | REPLICA 
      | DAEMON 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | REPLICA -> "REPLICA"
      | DAEMON -> "DAEMON"
      | Non_static_id s -> s
    let of_string =
      function
      | "REPLICA" -> REPLICA
      | "DAEMON" -> DAEMON
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SchedulingStrategy" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SchedulingStrategy" j)
    let to_json = simple_to_json to_value
  end
module ServiceEvents =
  struct
    type nonrec t = ServiceEvent.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ServiceEvent.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ServiceEvent.of_xml)
    let of_json j =
      list_of_json ~kind:"ServiceEvents" ~of_json:ServiceEvent.of_json j
    let to_json v = composed_to_json to_value v
  end
module TaskSets =
  struct
    type nonrec t = TaskSet.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TaskSet.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TaskSet.of_xml)
    let of_json j = list_of_json ~kind:"TaskSets" ~of_json:TaskSet.of_json j
    let to_json v = composed_to_json to_value v
  end
module ClusterConfiguration =
  struct
    type nonrec t =
      {
      executeCommandConfiguration: ExecuteCommandConfiguration.t option
        [@ocaml.doc "The details of the execute command configuration."]}
    let make ?executeCommandConfiguration =
      fun () -> { executeCommandConfiguration }
    let to_value x =
      structure_to_value
        [("executeCommandConfiguration",
           (Option.map x.executeCommandConfiguration
              ~f:ExecuteCommandConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let executeCommandConfiguration =
        (Option.map ~f:ExecuteCommandConfiguration.of_xml)
          (Xml.child xml_arg0 "executeCommandConfiguration") in
      make ?executeCommandConfiguration ()
    let of_json json =
      let executeCommandConfiguration =
        field_map json "executeCommandConfiguration"
          ExecuteCommandConfiguration.of_json in
      make ?executeCommandConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The execute command configuration for the cluster."]
module ClusterSettings =
  struct
    type nonrec t = ClusterSetting.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ClusterSetting.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ClusterSetting.of_xml)
    let of_json j =
      list_of_json ~kind:"ClusterSettings" ~of_json:ClusterSetting.of_json j
    let to_json v = composed_to_json to_value v
  end
module Statistics =
  struct
    type nonrec t = KeyValuePair.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:KeyValuePair.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:KeyValuePair.of_xml)
    let of_json j =
      list_of_json ~kind:"Statistics" ~of_json:KeyValuePair.of_json j
    let to_json v = composed_to_json to_value v
  end
module AutoScalingGroupProvider =
  struct
    type nonrec t =
      {
      autoScalingGroupArn: String_.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that identifies the Auto Scaling group."];
      managedScaling: ManagedScaling.t option
        [@ocaml.doc
          "The managed scaling settings for the Auto Scaling group capacity provider."];
      managedTerminationProtection: ManagedTerminationProtection.t option
        [@ocaml.doc
          "The managed termination protection setting to use for the Auto Scaling group capacity provider. This determines whether the Auto Scaling group has managed termination protection. The default is disabled. When using managed termination protection, managed scaling must also be used otherwise managed termination protection doesn't work. When managed termination protection is enabled, Amazon ECS prevents the Amazon EC2 instances in an Auto Scaling group that contain tasks from being terminated during a scale-in action. The Auto Scaling group and each instance in the Auto Scaling group must have instance protection from scale-in actions enabled as well. For more information, see Instance Protection in the Auto Scaling User Guide. When managed termination protection is disabled, your Amazon EC2 instances aren't protected from termination when the Auto Scaling group scales in."]}
    let context_ = "AutoScalingGroupProvider"
    let make ?managedScaling =
      fun ?managedTerminationProtection ->
        fun ~autoScalingGroupArn ->
          fun () ->
            {
              managedScaling;
              managedTerminationProtection;
              autoScalingGroupArn
            }
    let to_value x =
      structure_to_value
        [("autoScalingGroupArn",
           (Some (String_.to_value x.autoScalingGroupArn)));
        ("managedScaling",
          (Option.map x.managedScaling ~f:ManagedScaling.to_value));
        ("managedTerminationProtection",
          (Option.map x.managedTerminationProtection
             ~f:ManagedTerminationProtection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let managedTerminationProtection =
        (Option.map ~f:ManagedTerminationProtection.of_xml)
          (Xml.child xml_arg0 "managedTerminationProtection") in
      let managedScaling =
        (Option.map ~f:ManagedScaling.of_xml)
          (Xml.child xml_arg0 "managedScaling") in
      let autoScalingGroupArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "autoScalingGroupArn") in
      make ?managedTerminationProtection ?managedScaling ~autoScalingGroupArn
        ()
    let of_json json =
      let managedTerminationProtection =
        field_map json "managedTerminationProtection"
          ManagedTerminationProtection.of_json in
      let managedScaling =
        field_map json "managedScaling" ManagedScaling.of_json in
      let autoScalingGroupArn =
        field_map_exn json "autoScalingGroupArn" String_.of_json in
      make ?managedTerminationProtection ?managedScaling ~autoScalingGroupArn
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details of the Auto Scaling group for the capacity provider."]
module CapacityProviderStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | INACTIVE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | INACTIVE -> "INACTIVE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "INACTIVE" -> INACTIVE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration CapacityProviderStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"CapacityProviderStatus" j)
    let to_json = simple_to_json to_value
  end
module CapacityProviderUpdateStatus =
  struct
    type nonrec t =
      | DELETE_IN_PROGRESS 
      | DELETE_COMPLETE 
      | DELETE_FAILED 
      | UPDATE_IN_PROGRESS 
      | UPDATE_COMPLETE 
      | UPDATE_FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DELETE_IN_PROGRESS -> "DELETE_IN_PROGRESS"
      | DELETE_COMPLETE -> "DELETE_COMPLETE"
      | DELETE_FAILED -> "DELETE_FAILED"
      | UPDATE_IN_PROGRESS -> "UPDATE_IN_PROGRESS"
      | UPDATE_COMPLETE -> "UPDATE_COMPLETE"
      | UPDATE_FAILED -> "UPDATE_FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "DELETE_IN_PROGRESS" -> DELETE_IN_PROGRESS
      | "DELETE_COMPLETE" -> DELETE_COMPLETE
      | "DELETE_FAILED" -> DELETE_FAILED
      | "UPDATE_IN_PROGRESS" -> UPDATE_IN_PROGRESS
      | "UPDATE_COMPLETE" -> UPDATE_COMPLETE
      | "UPDATE_FAILED" -> UPDATE_FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration CapacityProviderUpdateStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"CapacityProviderUpdateStatus" j)
    let to_json = simple_to_json to_value
  end
module ContainerInstance =
  struct
    type nonrec t =
      {
      containerInstanceArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the container instance. The ARN contains the arn:aws:ecs namespace, followed by the Region of the container instance, the Amazon Web Services account ID of the container instance owner, the container-instance namespace, and then the container instance ID. For example, arn:aws:ecs:region:aws_account_id:container-instance/container_instance_ID."];
      ec2InstanceId: String_.t option
        [@ocaml.doc
          "The ID of the container instance. For Amazon EC2 instances, this value is the Amazon EC2 instance ID. For external instances, this value is the Amazon Web Services Systems Manager managed instance ID."];
      capacityProviderName: String_.t option
        [@ocaml.doc
          "The capacity provider that's associated with the container instance."];
      version: Long.t option
        [@ocaml.doc
          "The version counter for the container instance. Every time a container instance experiences a change that triggers a CloudWatch event, the version counter is incremented. If you're replicating your Amazon ECS container instance state with CloudWatch Events, you can compare the version of a container instance reported by the Amazon ECS APIs with the version reported in CloudWatch Events for the container instance (inside the detail object) to verify that the version in your event stream is current."];
      versionInfo: VersionInfo.t option
        [@ocaml.doc
          "The version information for the Amazon ECS container agent and Docker daemon running on the container instance."];
      remainingResources: Resources.t option
        [@ocaml.doc
          "For CPU and memory resource types, this parameter describes the remaining CPU and memory that wasn't already allocated to tasks and is therefore available for new tasks. For port resource types, this parameter describes the ports that were reserved by the Amazon ECS container agent (at instance registration time) and any task containers that have reserved port mappings on the host (with the host or bridge network mode). Any port that's not specified here is available for new tasks."];
      registeredResources: Resources.t option
        [@ocaml.doc
          "For CPU and memory resource types, this parameter describes the amount of each resource that was available on the container instance when the container agent registered it with Amazon ECS. This value represents the total amount of CPU and memory that can be allocated on this container instance to tasks. For port resource types, this parameter describes the ports that were reserved by the Amazon ECS container agent when it registered the container instance with Amazon ECS."];
      status: String_.t option
        [@ocaml.doc
          "The status of the container instance. The valid values are REGISTERING, REGISTRATION_FAILED, ACTIVE, INACTIVE, DEREGISTERING, or DRAINING. If your account has opted in to the awsvpcTrunking account setting, then any newly registered container instance will transition to a REGISTERING status while the trunk elastic network interface is provisioned for the instance. If the registration fails, the instance will transition to a REGISTRATION_FAILED status. You can describe the container instance and see the reason for failure in the statusReason parameter. Once the container instance is terminated, the instance transitions to a DEREGISTERING status while the trunk elastic network interface is deprovisioned. The instance then transitions to an INACTIVE status. The ACTIVE status indicates that the container instance can accept tasks. The DRAINING indicates that new tasks aren't placed on the container instance and any service tasks running on the container instance are removed if possible. For more information, see Container Instance Draining in the Amazon Elastic Container Service Developer Guide."];
      statusReason: String_.t option
        [@ocaml.doc
          "The reason that the container instance reached its current status."];
      agentConnected: Boolean.t option
        [@ocaml.doc
          "This parameter returns true if the agent is connected to Amazon ECS. Registered instances with an agent that may be unhealthy or stopped return false. Only instances connected to an agent can accept placement requests."];
      runningTasksCount: Integer.t option
        [@ocaml.doc
          "The number of tasks on the container instance that are in the RUNNING status."];
      pendingTasksCount: Integer.t option
        [@ocaml.doc
          "The number of tasks on the container instance that are in the PENDING status."];
      agentUpdateStatus: AgentUpdateStatus.t option
        [@ocaml.doc
          "The status of the most recent agent update. If an update wasn't ever requested, this value is NULL."];
      attributes: Attributes.t option
        [@ocaml.doc
          "The attributes set for the container instance, either by the Amazon ECS container agent at instance registration or manually with the PutAttributes operation."];
      registeredAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the container instance was registered."];
      attachments: Attachments.t option
        [@ocaml.doc
          "The resources attached to a container instance, such as elastic network interfaces."];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the container instance to help you categorize and organize them. Each tag consists of a key and an optional value. You define both. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."];
      healthStatus: ContainerInstanceHealthStatus.t option
        [@ocaml.doc
          "An object representing the health status of the container instance."]}
    let make ?containerInstanceArn =
      fun ?ec2InstanceId ->
        fun ?capacityProviderName ->
          fun ?version ->
            fun ?versionInfo ->
              fun ?remainingResources ->
                fun ?registeredResources ->
                  fun ?status ->
                    fun ?statusReason ->
                      fun ?agentConnected ->
                        fun ?runningTasksCount ->
                          fun ?pendingTasksCount ->
                            fun ?agentUpdateStatus ->
                              fun ?attributes ->
                                fun ?registeredAt ->
                                  fun ?attachments ->
                                    fun ?tags ->
                                      fun ?healthStatus ->
                                        fun () ->
                                          {
                                            containerInstanceArn;
                                            ec2InstanceId;
                                            capacityProviderName;
                                            version;
                                            versionInfo;
                                            remainingResources;
                                            registeredResources;
                                            status;
                                            statusReason;
                                            agentConnected;
                                            runningTasksCount;
                                            pendingTasksCount;
                                            agentUpdateStatus;
                                            attributes;
                                            registeredAt;
                                            attachments;
                                            tags;
                                            healthStatus
                                          }
    let to_value x =
      structure_to_value
        [("containerInstanceArn",
           (Option.map x.containerInstanceArn ~f:String_.to_value));
        ("ec2InstanceId", (Option.map x.ec2InstanceId ~f:String_.to_value));
        ("capacityProviderName",
          (Option.map x.capacityProviderName ~f:String_.to_value));
        ("version", (Option.map x.version ~f:Long.to_value));
        ("versionInfo", (Option.map x.versionInfo ~f:VersionInfo.to_value));
        ("remainingResources",
          (Option.map x.remainingResources ~f:Resources.to_value));
        ("registeredResources",
          (Option.map x.registeredResources ~f:Resources.to_value));
        ("status", (Option.map x.status ~f:String_.to_value));
        ("statusReason", (Option.map x.statusReason ~f:String_.to_value));
        ("agentConnected", (Option.map x.agentConnected ~f:Boolean.to_value));
        ("runningTasksCount",
          (Option.map x.runningTasksCount ~f:Integer.to_value));
        ("pendingTasksCount",
          (Option.map x.pendingTasksCount ~f:Integer.to_value));
        ("agentUpdateStatus",
          (Option.map x.agentUpdateStatus ~f:AgentUpdateStatus.to_value));
        ("attributes", (Option.map x.attributes ~f:Attributes.to_value));
        ("registeredAt", (Option.map x.registeredAt ~f:Timestamp.to_value));
        ("attachments", (Option.map x.attachments ~f:Attachments.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("healthStatus",
          (Option.map x.healthStatus
             ~f:ContainerInstanceHealthStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthStatus =
        (Option.map ~f:ContainerInstanceHealthStatus.of_xml)
          (Xml.child xml_arg0 "healthStatus") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let attachments =
        (Option.map ~f:Attachments.of_xml) (Xml.child xml_arg0 "attachments") in
      let registeredAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "registeredAt") in
      let attributes =
        (Option.map ~f:Attributes.of_xml) (Xml.child xml_arg0 "attributes") in
      let agentUpdateStatus =
        (Option.map ~f:AgentUpdateStatus.of_xml)
          (Xml.child xml_arg0 "agentUpdateStatus") in
      let pendingTasksCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "pendingTasksCount") in
      let runningTasksCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "runningTasksCount") in
      let agentConnected =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "agentConnected") in
      let statusReason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusReason") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let registeredResources =
        (Option.map ~f:Resources.of_xml)
          (Xml.child xml_arg0 "registeredResources") in
      let remainingResources =
        (Option.map ~f:Resources.of_xml)
          (Xml.child xml_arg0 "remainingResources") in
      let versionInfo =
        (Option.map ~f:VersionInfo.of_xml) (Xml.child xml_arg0 "versionInfo") in
      let version =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "version") in
      let capacityProviderName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "capacityProviderName") in
      let ec2InstanceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ec2InstanceId") in
      let containerInstanceArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "containerInstanceArn") in
      make ?healthStatus ?tags ?attachments ?registeredAt ?attributes
        ?agentUpdateStatus ?pendingTasksCount ?runningTasksCount
        ?agentConnected ?statusReason ?status ?registeredResources
        ?remainingResources ?versionInfo ?version ?capacityProviderName
        ?ec2InstanceId ?containerInstanceArn ()
    let of_json json =
      let healthStatus =
        field_map json "healthStatus" ContainerInstanceHealthStatus.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let attachments = field_map json "attachments" Attachments.of_json in
      let registeredAt = field_map json "registeredAt" Timestamp.of_json in
      let attributes = field_map json "attributes" Attributes.of_json in
      let agentUpdateStatus =
        field_map json "agentUpdateStatus" AgentUpdateStatus.of_json in
      let pendingTasksCount =
        field_map json "pendingTasksCount" Integer.of_json in
      let runningTasksCount =
        field_map json "runningTasksCount" Integer.of_json in
      let agentConnected = field_map json "agentConnected" Boolean.of_json in
      let statusReason = field_map json "statusReason" String_.of_json in
      let status = field_map json "status" String_.of_json in
      let registeredResources =
        field_map json "registeredResources" Resources.of_json in
      let remainingResources =
        field_map json "remainingResources" Resources.of_json in
      let versionInfo = field_map json "versionInfo" VersionInfo.of_json in
      let version = field_map json "version" Long.of_json in
      let capacityProviderName =
        field_map json "capacityProviderName" String_.of_json in
      let ec2InstanceId = field_map json "ec2InstanceId" String_.of_json in
      let containerInstanceArn =
        field_map json "containerInstanceArn" String_.of_json in
      make ?healthStatus ?tags ?attachments ?registeredAt ?attributes
        ?agentUpdateStatus ?pendingTasksCount ?runningTasksCount
        ?agentConnected ?statusReason ?status ?registeredResources
        ?remainingResources ?versionInfo ?version ?capacityProviderName
        ?ec2InstanceId ?containerInstanceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An EC2 instance that's running the Amazon ECS agent and has been registered with a cluster."]
module Failure =
  struct
    type nonrec t =
      {
      arn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the failed resource."];
      reason: String_.t option [@ocaml.doc "The reason for the failure."];
      detail: String_.t option [@ocaml.doc "The details of the failure."]}
    let make ?arn =
      fun ?reason -> fun ?detail -> fun () -> { arn; reason; detail }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:String_.to_value));
        ("reason", (Option.map x.reason ~f:String_.to_value));
        ("detail", (Option.map x.detail ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let detail =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "detail") in
      let reason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "reason") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "arn") in
      make ?detail ?reason ?arn ()
    let of_json json =
      let detail = field_map json "detail" String_.of_json in
      let reason = field_map json "reason" String_.of_json in
      let arn = field_map json "arn" String_.of_json in
      make ?detail ?reason ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A failed resource. For a list of common causes, see API failure reasons in the Amazon Elastic Container Service Developer Guide."]
module AttachmentStateChange =
  struct
    type nonrec t =
      {
      attachmentArn: String_.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the attachment."];
      status: String_.t [@ocaml.doc "The status of the attachment."]}
    let context_ = "AttachmentStateChange"
    let make ~attachmentArn =
      fun ~status -> fun () -> { attachmentArn; status }
    let to_value x =
      structure_to_value
        [("attachmentArn", (Some (String_.to_value x.attachmentArn)));
        ("status", (Some (String_.to_value x.status)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "status") in
      let attachmentArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "attachmentArn") in
      make ~status ~attachmentArn ()
    let of_json json =
      let status = field_map_exn json "status" String_.of_json in
      let attachmentArn = field_map_exn json "attachmentArn" String_.of_json in
      make ~status ~attachmentArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing a change in state for a task attachment."]
module ContainerStateChange =
  struct
    type nonrec t =
      {
      containerName: String_.t option
        [@ocaml.doc "The name of the container."];
      imageDigest: String_.t option
        [@ocaml.doc "The container image SHA 256 digest."];
      runtimeId: String_.t option
        [@ocaml.doc "The ID of the Docker container."];
      exitCode: BoxedInteger.t option
        [@ocaml.doc
          "The exit code for the container, if the state change is a result of the container exiting."];
      networkBindings: NetworkBindings.t option
        [@ocaml.doc
          "Any network bindings that are associated with the container."];
      reason: String_.t option
        [@ocaml.doc "The reason for the state change."];
      status: String_.t option [@ocaml.doc "The status of the container."]}
    let make ?containerName =
      fun ?imageDigest ->
        fun ?runtimeId ->
          fun ?exitCode ->
            fun ?networkBindings ->
              fun ?reason ->
                fun ?status ->
                  fun () ->
                    {
                      containerName;
                      imageDigest;
                      runtimeId;
                      exitCode;
                      networkBindings;
                      reason;
                      status
                    }
    let to_value x =
      structure_to_value
        [("containerName", (Option.map x.containerName ~f:String_.to_value));
        ("imageDigest", (Option.map x.imageDigest ~f:String_.to_value));
        ("runtimeId", (Option.map x.runtimeId ~f:String_.to_value));
        ("exitCode", (Option.map x.exitCode ~f:BoxedInteger.to_value));
        ("networkBindings",
          (Option.map x.networkBindings ~f:NetworkBindings.to_value));
        ("reason", (Option.map x.reason ~f:String_.to_value));
        ("status", (Option.map x.status ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let reason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "reason") in
      let networkBindings =
        (Option.map ~f:NetworkBindings.of_xml)
          (Xml.child xml_arg0 "networkBindings") in
      let exitCode =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "exitCode") in
      let runtimeId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "runtimeId") in
      let imageDigest =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageDigest") in
      let containerName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "containerName") in
      make ?status ?reason ?networkBindings ?exitCode ?runtimeId ?imageDigest
        ?containerName ()
    let of_json json =
      let status = field_map json "status" String_.of_json in
      let reason = field_map json "reason" String_.of_json in
      let networkBindings =
        field_map json "networkBindings" NetworkBindings.of_json in
      let exitCode = field_map json "exitCode" BoxedInteger.of_json in
      let runtimeId = field_map json "runtimeId" String_.of_json in
      let imageDigest = field_map json "imageDigest" String_.of_json in
      let containerName = field_map json "containerName" String_.of_json in
      make ?status ?reason ?networkBindings ?exitCode ?runtimeId ?imageDigest
        ?containerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object that represents a change in state for a container."]
module ManagedAgentStateChange =
  struct
    type nonrec t =
      {
      containerName: String_.t
        [@ocaml.doc
          "The name of the container that's associated with the managed agent."];
      managedAgentName: ManagedAgentName.t
        [@ocaml.doc "The name of the managed agent."];
      status: String_.t [@ocaml.doc "The status of the managed agent."];
      reason: String_.t option
        [@ocaml.doc "The reason for the status of the managed agent."]}
    let context_ = "ManagedAgentStateChange"
    let make ?reason =
      fun ~containerName ->
        fun ~managedAgentName ->
          fun ~status ->
            fun () -> { reason; containerName; managedAgentName; status }
    let to_value x =
      structure_to_value
        [("containerName", (Some (String_.to_value x.containerName)));
        ("managedAgentName",
          (Some (ManagedAgentName.to_value x.managedAgentName)));
        ("status", (Some (String_.to_value x.status)));
        ("reason", (Option.map x.reason ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "reason") in
      let status =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "status") in
      let managedAgentName =
        ManagedAgentName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "managedAgentName") in
      let containerName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "containerName") in
      make ?reason ~status ~managedAgentName ~containerName ()
    let of_json json =
      let reason = field_map json "reason" String_.of_json in
      let status = field_map_exn json "status" String_.of_json in
      let managedAgentName =
        field_map_exn json "managedAgentName" ManagedAgentName.of_json in
      let containerName = field_map_exn json "containerName" String_.of_json in
      make ?reason ~status ~managedAgentName ~containerName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing a change in state for a managed agent."]
module Task =
  struct
    type nonrec t =
      {
      attachments: Attachments.t option
        [@ocaml.doc
          "The Elastic Network Adapter that's associated with the task if the task uses the awsvpc network mode."];
      attributes: Attributes.t option
        [@ocaml.doc "The attributes of the task"];
      availabilityZone: String_.t option
        [@ocaml.doc "The Availability Zone for the task."];
      capacityProviderName: String_.t option
        [@ocaml.doc "The capacity provider that's associated with the task."];
      clusterArn: String_.t option
        [@ocaml.doc "The ARN of the cluster that hosts the task."];
      connectivity: Connectivity.t option
        [@ocaml.doc "The connectivity status of a task."];
      connectivityAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the task last went into CONNECTED status."];
      containerInstanceArn: String_.t option
        [@ocaml.doc "The ARN of the container instances that host the task."];
      containers: Containers.t option
        [@ocaml.doc "The containers that's associated with the task."];
      cpu: String_.t option
        [@ocaml.doc
          "The number of CPU units used by the task as expressed in a task definition. It can be expressed as an integer using CPU units (for example, 1024). It can also be expressed as a string using vCPUs (for example, 1 vCPU or 1 vcpu). String values are converted to an integer that indicates the CPU units when the task definition is registered. If you use the EC2 launch type, this field is optional. Supported values are between 128 CPU units (0.125 vCPUs) and 10240 CPU units (10 vCPUs). If you use the Fargate launch type, this field is required. You must use one of the following values. These values determine the range of supported values for the memory parameter: The CPU units cannot be less than 1 vCPU when you use Windows containers on Fargate. 256 (.25 vCPU) - Available memory values: 512 (0.5 GB), 1024 (1 GB), 2048 (2 GB) 512 (.5 vCPU) - Available memory values: 1024 (1 GB), 2048 (2 GB), 3072 (3 GB), 4096 (4 GB) 1024 (1 vCPU) - Available memory values: 2048 (2 GB), 3072 (3 GB), 4096 (4 GB), 5120 (5 GB), 6144 (6 GB), 7168 (7 GB), 8192 (8 GB) 2048 (2 vCPU) - Available memory values: Between 4096 (4 GB) and 16384 (16 GB) in increments of 1024 (1 GB) 4096 (4 vCPU) - Available memory values: Between 8192 (8 GB) and 30720 (30 GB) in increments of 1024 (1 GB)"];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the task was created. More specifically, it's for the time when the task entered the PENDING state."];
      desiredStatus: String_.t option
        [@ocaml.doc
          "The desired status of the task. For more information, see Task Lifecycle."];
      enableExecuteCommand: Boolean.t option
        [@ocaml.doc
          "Determines whether execute command functionality is enabled for this task. If true, execute command functionality is enabled on all the containers in the task."];
      executionStoppedAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the task execution stopped."];
      group: String_.t option
        [@ocaml.doc
          "The name of the task group that's associated with the task."];
      healthStatus: HealthStatus.t option
        [@ocaml.doc
          "The health status for the task. It's determined by the health of the essential containers in the task. If all essential containers in the task are reporting as HEALTHY, the task status also reports as HEALTHY. If any essential containers in the task are reporting as UNHEALTHY or UNKNOWN, the task status also reports as UNHEALTHY or UNKNOWN. The Amazon ECS container agent doesn't monitor or report on Docker health checks that are embedded in a container image and not specified in the container definition. For example, this includes those specified in a parent image or from the image's Dockerfile. Health check parameters that are specified in a container definition override any Docker health checks that are found in the container image."];
      inferenceAccelerators: InferenceAccelerators.t option
        [@ocaml.doc
          "The Elastic Inference accelerator that's associated with the task."];
      lastStatus: String_.t option
        [@ocaml.doc
          "The last known status for the task. For more information, see Task Lifecycle."];
      launchType: LaunchType.t option
        [@ocaml.doc
          "The infrastructure where your task runs on. For more information, see Amazon ECS launch types in the Amazon Elastic Container Service Developer Guide."];
      memory: String_.t option
        [@ocaml.doc
          "The amount of memory (in MiB) that the task uses as expressed in a task definition. It can be expressed as an integer using MiB (for example, 1024). If it's expressed as a string using GB (for example, 1GB or 1 GB), it's converted to an integer indicating the MiB when the task definition is registered. If you use the EC2 launch type, this field is optional. If you use the Fargate launch type, this field is required. You must use one of the following values. The value that you choose determines the range of supported values for the cpu parameter. 512 (0.5 GB), 1024 (1 GB), 2048 (2 GB) - Available cpu values: 256 (.25 vCPU) 1024 (1 GB), 2048 (2 GB), 3072 (3 GB), 4096 (4 GB) - Available cpu values: 512 (.5 vCPU) 2048 (2 GB), 3072 (3 GB), 4096 (4 GB), 5120 (5 GB), 6144 (6 GB), 7168 (7 GB), 8192 (8 GB) - Available cpu values: 1024 (1 vCPU) Between 4096 (4 GB) and 16384 (16 GB) in increments of 1024 (1 GB) - Available cpu values: 2048 (2 vCPU) Between 8192 (8 GB) and 30720 (30 GB) in increments of 1024 (1 GB) - Available cpu values: 4096 (4 vCPU)"];
      overrides: TaskOverride.t option
        [@ocaml.doc "One or more container overrides."];
      platformVersion: String_.t option
        [@ocaml.doc
          "The platform version where your task runs on. A platform version is only specified for tasks that use the Fargate launch type. If you didn't specify one, the LATEST platform version is used. For more information, see Fargate Platform Versions in the Amazon Elastic Container Service Developer Guide."];
      platformFamily: String_.t option
        [@ocaml.doc
          "The operating system that your tasks are running on. A platform family is specified only for tasks that use the Fargate launch type. All tasks that run as part of this service must use the same platformFamily value as the service (for example, LINUX.)."];
      pullStartedAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the container image pull began."];
      pullStoppedAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the container image pull completed."];
      startedAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the task started. More specifically, it's for the time when the task transitioned from the PENDING state to the RUNNING state."];
      startedBy: String_.t option
        [@ocaml.doc
          "The tag specified when a task is started. If an Amazon ECS service started the task, the startedBy parameter contains the deployment ID of that service."];
      stopCode: TaskStopCode.t option
        [@ocaml.doc
          "The stop code indicating why a task was stopped. The stoppedReason might contain additional details. The following are valid values: TaskFailedToStart EssentialContainerExited UserInitiated TerminationNotice ServiceSchedulerInitiated SpotInterruption"];
      stoppedAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the task was stopped. More specifically, it's for the time when the task transitioned from the RUNNING state to the STOPPED state."];
      stoppedReason: String_.t option
        [@ocaml.doc "The reason that the task was stopped."];
      stoppingAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the task stops. More specifically, it's for the time when the task transitions from the RUNNING state to STOPPED."];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the task to help you categorize and organize the task. Each tag consists of a key and an optional value. You define both the key and value. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."];
      taskArn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the task."];
      taskDefinitionArn: String_.t option
        [@ocaml.doc "The ARN of the task definition that creates the task."];
      version: Long.t option
        [@ocaml.doc
          "The version counter for the task. Every time a task experiences a change that starts a CloudWatch event, the version counter is incremented. If you replicate your Amazon ECS task state with CloudWatch Events, you can compare the version of a task reported by the Amazon ECS API actions with the version reported in CloudWatch Events for the task (inside the detail object) to verify that the version in your event stream is current."];
      ephemeralStorage: EphemeralStorage.t option
        [@ocaml.doc "The ephemeral storage settings for the task."]}
    let make ?attachments =
      fun ?attributes ->
        fun ?availabilityZone ->
          fun ?capacityProviderName ->
            fun ?clusterArn ->
              fun ?connectivity ->
                fun ?connectivityAt ->
                  fun ?containerInstanceArn ->
                    fun ?containers ->
                      fun ?cpu ->
                        fun ?createdAt ->
                          fun ?desiredStatus ->
                            fun ?enableExecuteCommand ->
                              fun ?executionStoppedAt ->
                                fun ?group ->
                                  fun ?healthStatus ->
                                    fun ?inferenceAccelerators ->
                                      fun ?lastStatus ->
                                        fun ?launchType ->
                                          fun ?memory ->
                                            fun ?overrides ->
                                              fun ?platformVersion ->
                                                fun ?platformFamily ->
                                                  fun ?pullStartedAt ->
                                                    fun ?pullStoppedAt ->
                                                      fun ?startedAt ->
                                                        fun ?startedBy ->
                                                          fun ?stopCode ->
                                                            fun ?stoppedAt ->
                                                              fun
                                                                ?stoppedReason
                                                                ->
                                                                fun
                                                                  ?stoppingAt
                                                                  ->
                                                                  fun ?tags
                                                                    ->
                                                                    fun
                                                                    ?taskArn
                                                                    ->
                                                                    fun
                                                                    ?taskDefinitionArn
                                                                    ->
                                                                    fun
                                                                    ?version
                                                                    ->
                                                                    fun
                                                                    ?ephemeralStorage
                                                                    ->
                                                                    fun () ->
                                                                    {
                                                                    attachments;
                                                                    attributes;
                                                                    availabilityZone;
                                                                    capacityProviderName;
                                                                    clusterArn;
                                                                    connectivity;
                                                                    connectivityAt;
                                                                    containerInstanceArn;
                                                                    containers;
                                                                    cpu;
                                                                    createdAt;
                                                                    desiredStatus;
                                                                    enableExecuteCommand;
                                                                    executionStoppedAt;
                                                                    group;
                                                                    healthStatus;
                                                                    inferenceAccelerators;
                                                                    lastStatus;
                                                                    launchType;
                                                                    memory;
                                                                    overrides;
                                                                    platformVersion;
                                                                    platformFamily;
                                                                    pullStartedAt;
                                                                    pullStoppedAt;
                                                                    startedAt;
                                                                    startedBy;
                                                                    stopCode;
                                                                    stoppedAt;
                                                                    stoppedReason;
                                                                    stoppingAt;
                                                                    tags;
                                                                    taskArn;
                                                                    taskDefinitionArn;
                                                                    version;
                                                                    ephemeralStorage
                                                                    }
    let to_value x =
      structure_to_value
        [("attachments", (Option.map x.attachments ~f:Attachments.to_value));
        ("attributes", (Option.map x.attributes ~f:Attributes.to_value));
        ("availabilityZone",
          (Option.map x.availabilityZone ~f:String_.to_value));
        ("capacityProviderName",
          (Option.map x.capacityProviderName ~f:String_.to_value));
        ("clusterArn", (Option.map x.clusterArn ~f:String_.to_value));
        ("connectivity",
          (Option.map x.connectivity ~f:Connectivity.to_value));
        ("connectivityAt",
          (Option.map x.connectivityAt ~f:Timestamp.to_value));
        ("containerInstanceArn",
          (Option.map x.containerInstanceArn ~f:String_.to_value));
        ("containers", (Option.map x.containers ~f:Containers.to_value));
        ("cpu", (Option.map x.cpu ~f:String_.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("desiredStatus", (Option.map x.desiredStatus ~f:String_.to_value));
        ("enableExecuteCommand",
          (Option.map x.enableExecuteCommand ~f:Boolean.to_value));
        ("executionStoppedAt",
          (Option.map x.executionStoppedAt ~f:Timestamp.to_value));
        ("group", (Option.map x.group ~f:String_.to_value));
        ("healthStatus",
          (Option.map x.healthStatus ~f:HealthStatus.to_value));
        ("inferenceAccelerators",
          (Option.map x.inferenceAccelerators
             ~f:InferenceAccelerators.to_value));
        ("lastStatus", (Option.map x.lastStatus ~f:String_.to_value));
        ("launchType", (Option.map x.launchType ~f:LaunchType.to_value));
        ("memory", (Option.map x.memory ~f:String_.to_value));
        ("overrides", (Option.map x.overrides ~f:TaskOverride.to_value));
        ("platformVersion",
          (Option.map x.platformVersion ~f:String_.to_value));
        ("platformFamily", (Option.map x.platformFamily ~f:String_.to_value));
        ("pullStartedAt", (Option.map x.pullStartedAt ~f:Timestamp.to_value));
        ("pullStoppedAt", (Option.map x.pullStoppedAt ~f:Timestamp.to_value));
        ("startedAt", (Option.map x.startedAt ~f:Timestamp.to_value));
        ("startedBy", (Option.map x.startedBy ~f:String_.to_value));
        ("stopCode", (Option.map x.stopCode ~f:TaskStopCode.to_value));
        ("stoppedAt", (Option.map x.stoppedAt ~f:Timestamp.to_value));
        ("stoppedReason", (Option.map x.stoppedReason ~f:String_.to_value));
        ("stoppingAt", (Option.map x.stoppingAt ~f:Timestamp.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("taskArn", (Option.map x.taskArn ~f:String_.to_value));
        ("taskDefinitionArn",
          (Option.map x.taskDefinitionArn ~f:String_.to_value));
        ("version", (Option.map x.version ~f:Long.to_value));
        ("ephemeralStorage",
          (Option.map x.ephemeralStorage ~f:EphemeralStorage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ephemeralStorage =
        (Option.map ~f:EphemeralStorage.of_xml)
          (Xml.child xml_arg0 "ephemeralStorage") in
      let version =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "version") in
      let taskDefinitionArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "taskDefinitionArn") in
      let taskArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskArn") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let stoppingAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "stoppingAt") in
      let stoppedReason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "stoppedReason") in
      let stoppedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "stoppedAt") in
      let stopCode =
        (Option.map ~f:TaskStopCode.of_xml) (Xml.child xml_arg0 "stopCode") in
      let startedBy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "startedBy") in
      let startedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startedAt") in
      let pullStoppedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "pullStoppedAt") in
      let pullStartedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "pullStartedAt") in
      let platformFamily =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platformFamily") in
      let platformVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platformVersion") in
      let overrides =
        (Option.map ~f:TaskOverride.of_xml) (Xml.child xml_arg0 "overrides") in
      let memory =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "memory") in
      let launchType =
        (Option.map ~f:LaunchType.of_xml) (Xml.child xml_arg0 "launchType") in
      let lastStatus =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "lastStatus") in
      let inferenceAccelerators =
        (Option.map ~f:InferenceAccelerators.of_xml)
          (Xml.child xml_arg0 "inferenceAccelerators") in
      let healthStatus =
        (Option.map ~f:HealthStatus.of_xml)
          (Xml.child xml_arg0 "healthStatus") in
      let group = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "group") in
      let executionStoppedAt =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "executionStoppedAt") in
      let enableExecuteCommand =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableExecuteCommand") in
      let desiredStatus =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "desiredStatus") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let cpu = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cpu") in
      let containers =
        (Option.map ~f:Containers.of_xml) (Xml.child xml_arg0 "containers") in
      let containerInstanceArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "containerInstanceArn") in
      let connectivityAt =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "connectivityAt") in
      let connectivity =
        (Option.map ~f:Connectivity.of_xml)
          (Xml.child xml_arg0 "connectivity") in
      let clusterArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clusterArn") in
      let capacityProviderName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "capacityProviderName") in
      let availabilityZone =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "availabilityZone") in
      let attributes =
        (Option.map ~f:Attributes.of_xml) (Xml.child xml_arg0 "attributes") in
      let attachments =
        (Option.map ~f:Attachments.of_xml) (Xml.child xml_arg0 "attachments") in
      make ?ephemeralStorage ?version ?taskDefinitionArn ?taskArn ?tags
        ?stoppingAt ?stoppedReason ?stoppedAt ?stopCode ?startedBy ?startedAt
        ?pullStoppedAt ?pullStartedAt ?platformFamily ?platformVersion
        ?overrides ?memory ?launchType ?lastStatus ?inferenceAccelerators
        ?healthStatus ?group ?executionStoppedAt ?enableExecuteCommand
        ?desiredStatus ?createdAt ?cpu ?containers ?containerInstanceArn
        ?connectivityAt ?connectivity ?clusterArn ?capacityProviderName
        ?availabilityZone ?attributes ?attachments ()
    let of_json json =
      let ephemeralStorage =
        field_map json "ephemeralStorage" EphemeralStorage.of_json in
      let version = field_map json "version" Long.of_json in
      let taskDefinitionArn =
        field_map json "taskDefinitionArn" String_.of_json in
      let taskArn = field_map json "taskArn" String_.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let stoppingAt = field_map json "stoppingAt" Timestamp.of_json in
      let stoppedReason = field_map json "stoppedReason" String_.of_json in
      let stoppedAt = field_map json "stoppedAt" Timestamp.of_json in
      let stopCode = field_map json "stopCode" TaskStopCode.of_json in
      let startedBy = field_map json "startedBy" String_.of_json in
      let startedAt = field_map json "startedAt" Timestamp.of_json in
      let pullStoppedAt = field_map json "pullStoppedAt" Timestamp.of_json in
      let pullStartedAt = field_map json "pullStartedAt" Timestamp.of_json in
      let platformFamily = field_map json "platformFamily" String_.of_json in
      let platformVersion = field_map json "platformVersion" String_.of_json in
      let overrides = field_map json "overrides" TaskOverride.of_json in
      let memory = field_map json "memory" String_.of_json in
      let launchType = field_map json "launchType" LaunchType.of_json in
      let lastStatus = field_map json "lastStatus" String_.of_json in
      let inferenceAccelerators =
        field_map json "inferenceAccelerators" InferenceAccelerators.of_json in
      let healthStatus = field_map json "healthStatus" HealthStatus.of_json in
      let group = field_map json "group" String_.of_json in
      let executionStoppedAt =
        field_map json "executionStoppedAt" Timestamp.of_json in
      let enableExecuteCommand =
        field_map json "enableExecuteCommand" Boolean.of_json in
      let desiredStatus = field_map json "desiredStatus" String_.of_json in
      let createdAt = field_map json "createdAt" Timestamp.of_json in
      let cpu = field_map json "cpu" String_.of_json in
      let containers = field_map json "containers" Containers.of_json in
      let containerInstanceArn =
        field_map json "containerInstanceArn" String_.of_json in
      let connectivityAt = field_map json "connectivityAt" Timestamp.of_json in
      let connectivity = field_map json "connectivity" Connectivity.of_json in
      let clusterArn = field_map json "clusterArn" String_.of_json in
      let capacityProviderName =
        field_map json "capacityProviderName" String_.of_json in
      let availabilityZone =
        field_map json "availabilityZone" String_.of_json in
      let attributes = field_map json "attributes" Attributes.of_json in
      let attachments = field_map json "attachments" Attachments.of_json in
      make ?ephemeralStorage ?version ?taskDefinitionArn ?taskArn ?tags
        ?stoppingAt ?stoppedReason ?stoppedAt ?stopCode ?startedBy ?startedAt
        ?pullStoppedAt ?pullStartedAt ?platformFamily ?platformVersion
        ?overrides ?memory ?launchType ?lastStatus ?inferenceAccelerators
        ?healthStatus ?group ?executionStoppedAt ?enableExecuteCommand
        ?desiredStatus ?createdAt ?cpu ?containers ?containerInstanceArn
        ?connectivityAt ?connectivity ?clusterArn ?capacityProviderName
        ?availabilityZone ?attributes ?attachments ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Details on a task in a cluster."]
module CompatibilityList =
  struct
    type nonrec t = Compatibility.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Compatibility.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Compatibility.of_xml)
    let of_json j =
      list_of_json ~kind:"CompatibilityList" ~of_json:Compatibility.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerDefinitions =
  struct
    type nonrec t = ContainerDefinition.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ContainerDefinition.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerDefinition.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerDefinitions"
        ~of_json:ContainerDefinition.of_json j
    let to_json v = composed_to_json to_value v
  end
module IpcMode =
  struct
    type nonrec t =
      | Host 
      | Task 
      | None 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Host -> "host"
      | Task -> "task"
      | None -> "none"
      | Non_static_id s -> s
    let of_string =
      function
      | "host" -> Host
      | "task" -> Task
      | "none" -> None
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration IpcMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"IpcMode" j)
    let to_json = simple_to_json to_value
  end
module NetworkMode =
  struct
    type nonrec t =
      | Bridge 
      | Host 
      | Awsvpc 
      | None 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Bridge -> "bridge"
      | Host -> "host"
      | Awsvpc -> "awsvpc"
      | None -> "none"
      | Non_static_id s -> s
    let of_string =
      function
      | "bridge" -> Bridge
      | "host" -> Host
      | "awsvpc" -> Awsvpc
      | "none" -> None
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration NetworkMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"NetworkMode" j)
    let to_json = simple_to_json to_value
  end
module PidMode =
  struct
    type nonrec t =
      | Host 
      | Task 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Host -> "host" | Task -> "task" | Non_static_id s -> s
    let of_string =
      function | "host" -> Host | "task" -> Task | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PidMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PidMode" j)
    let to_json = simple_to_json to_value
  end
module ProxyConfiguration =
  struct
    type nonrec t =
      {
      type_: ProxyConfigurationType.t option
        [@ocaml.doc "The proxy type. The only supported value is APPMESH."];
      containerName: String_.t
        [@ocaml.doc
          "The name of the container that will serve as the App Mesh proxy."];
      properties: ProxyConfigurationProperties.t option
        [@ocaml.doc
          "The set of network configuration parameters to provide the Container Network Interface (CNI) plugin, specified as key-value pairs. IgnoredUID - (Required) The user ID (UID) of the proxy container as defined by the user parameter in a container definition. This is used to ensure the proxy ignores its own traffic. If IgnoredGID is specified, this field can be empty. IgnoredGID - (Required) The group ID (GID) of the proxy container as defined by the user parameter in a container definition. This is used to ensure the proxy ignores its own traffic. If IgnoredUID is specified, this field can be empty. AppPorts - (Required) The list of ports that the application uses. Network traffic to these ports is forwarded to the ProxyIngressPort and ProxyEgressPort. ProxyIngressPort - (Required) Specifies the port that incoming traffic to the AppPorts is directed to. ProxyEgressPort - (Required) Specifies the port that outgoing traffic from the AppPorts is directed to. EgressIgnoredPorts - (Required) The egress traffic going to the specified ports is ignored and not redirected to the ProxyEgressPort. It can be an empty list. EgressIgnoredIPs - (Required) The egress traffic going to the specified IP addresses is ignored and not redirected to the ProxyEgressPort. It can be an empty list."]}
    let context_ = "ProxyConfiguration"
    let make ?type_ =
      fun ?properties ->
        fun ~containerName -> fun () -> { type_; properties; containerName }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:ProxyConfigurationType.to_value));
        ("containerName", (Some (String_.to_value x.containerName)));
        ("properties",
          (Option.map x.properties ~f:ProxyConfigurationProperties.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let properties =
        (Option.map ~f:ProxyConfigurationProperties.of_xml)
          (Xml.child xml_arg0 "properties") in
      let containerName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "containerName") in
      let type_ =
        (Option.map ~f:ProxyConfigurationType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?properties ~containerName ?type_ ()
    let of_json json =
      let properties =
        field_map json "properties" ProxyConfigurationProperties.of_json in
      let containerName = field_map_exn json "containerName" String_.of_json in
      let type_ = field_map json "type" ProxyConfigurationType.of_json in
      make ?properties ~containerName ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration details for the App Mesh proxy. For tasks that use the EC2 launch type, the container instances require at least version 1.26.0 of the container agent and at least version 1.26.0-1 of the ecs-init package to use a proxy configuration. If your container instances are launched from the Amazon ECS optimized AMI version 20190301 or later, then they contain the required versions of the container agent and ecs-init. For more information, see Amazon ECS-optimized Linux AMI"]
module RequiresAttributes =
  struct
    type nonrec t = Attribute.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Attribute.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Attribute.of_xml)
    let of_json j =
      list_of_json ~kind:"RequiresAttributes" ~of_json:Attribute.of_json j
    let to_json v = composed_to_json to_value v
  end
module RuntimePlatform =
  struct
    type nonrec t =
      {
      cpuArchitecture: CPUArchitecture.t option
        [@ocaml.doc
          "The CPU architecture. You can run your Linux tasks on an ARM-based platform by setting the value to ARM64. This option is avaiable for tasks that run on Linuc Amazon EC2 instance or Linux containers on Fargate."];
      operatingSystemFamily: OSFamily.t option
        [@ocaml.doc "The operating system."]}
    let make ?cpuArchitecture =
      fun ?operatingSystemFamily ->
        fun () -> { cpuArchitecture; operatingSystemFamily }
    let to_value x =
      structure_to_value
        [("cpuArchitecture",
           (Option.map x.cpuArchitecture ~f:CPUArchitecture.to_value));
        ("operatingSystemFamily",
          (Option.map x.operatingSystemFamily ~f:OSFamily.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let operatingSystemFamily =
        (Option.map ~f:OSFamily.of_xml)
          (Xml.child xml_arg0 "operatingSystemFamily") in
      let cpuArchitecture =
        (Option.map ~f:CPUArchitecture.of_xml)
          (Xml.child xml_arg0 "cpuArchitecture") in
      make ?operatingSystemFamily ?cpuArchitecture ()
    let of_json json =
      let operatingSystemFamily =
        field_map json "operatingSystemFamily" OSFamily.of_json in
      let cpuArchitecture =
        field_map json "cpuArchitecture" CPUArchitecture.of_json in
      make ?operatingSystemFamily ?cpuArchitecture ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the platform for the Amazon ECS service or task. For more informataion about RuntimePlatform, see RuntimePlatform in the Amazon Elastic Container Service Developer Guide."]
module TaskDefinitionPlacementConstraints =
  struct
    type nonrec t = TaskDefinitionPlacementConstraint.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TaskDefinitionPlacementConstraint.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:TaskDefinitionPlacementConstraint.of_xml)
    let of_json j =
      list_of_json ~kind:"TaskDefinitionPlacementConstraints"
        ~of_json:TaskDefinitionPlacementConstraint.of_json j
    let to_json v = composed_to_json to_value v
  end
module TaskDefinitionStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | INACTIVE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | INACTIVE -> "INACTIVE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "INACTIVE" -> INACTIVE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration TaskDefinitionStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TaskDefinitionStatus" j)
    let to_json = simple_to_json to_value
  end
module VolumeList =
  struct
    type nonrec t = Volume.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Volume.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Volume.of_xml)
    let of_json j = list_of_json ~kind:"VolumeList" ~of_json:Volume.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlatformDevice =
  struct
    type nonrec t =
      {
      id: String_.t
        [@ocaml.doc
          "The ID for the GPUs on the container instance. The available GPU IDs can also be obtained on the container instance in the /var/lib/ecs/gpu/nvidia_gpu_info.json file."];
      type_: PlatformDeviceType.t
        [@ocaml.doc
          "The type of device that's available on the container instance. The only supported value is GPU."]}
    let context_ = "PlatformDevice"
    let make ~id = fun ~type_ -> fun () -> { id; type_ }
    let to_value x =
      structure_to_value
        [("id", (Some (String_.to_value x.id)));
        ("type", (Some (PlatformDeviceType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        PlatformDeviceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let id = String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~type_ ~id ()
    let of_json json =
      let type_ = field_map_exn json "type" PlatformDeviceType.of_json in
      let id = field_map_exn json "id" String_.of_json in make ~type_ ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The devices that are available on the container instance. The only supported device type is a GPU."]
module Setting =
  struct
    type nonrec t =
      {
      name: SettingName.t option [@ocaml.doc "The Amazon ECS resource name."];
      value: String_.t option
        [@ocaml.doc
          "Determines whether the account setting is enabled or disabled for the specified resource."];
      principalArn: String_.t option
        [@ocaml.doc
          "The ARN of the principal. It can be an IAM user, IAM role, or the root user. If this field is omitted, the authenticated user is assumed."]}
    let make ?name =
      fun ?value ->
        fun ?principalArn -> fun () -> { name; value; principalArn }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:SettingName.to_value));
        ("value", (Option.map x.value ~f:String_.to_value));
        ("principalArn", (Option.map x.principalArn ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let principalArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "principalArn") in
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let name =
        (Option.map ~f:SettingName.of_xml) (Xml.child xml_arg0 "name") in
      make ?principalArn ?value ?name ()
    let of_json json =
      let principalArn = field_map json "principalArn" String_.of_json in
      let value = field_map json "value" String_.of_json in
      let name = field_map json "name" SettingName.of_json in
      make ?principalArn ?value ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The current account setting for a resource."]
module SensitiveString =
  struct
    type nonrec t = string
    let context_ = "SensitiveString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SensitiveString" j
    let to_json = simple_to_json to_value
  end
module TaskField =
  struct
    type nonrec t =
      | TAGS 
      | Non_static_id of string 
    let make i = i
    let to_string = function | TAGS -> "TAGS" | Non_static_id s -> s
    let of_string = function | "TAGS" -> TAGS | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TaskField" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TaskField" j)
    let to_json = simple_to_json to_value
  end
module TaskSetField =
  struct
    type nonrec t =
      | TAGS 
      | Non_static_id of string 
    let make i = i
    let to_string = function | TAGS -> "TAGS" | Non_static_id s -> s
    let of_string = function | "TAGS" -> TAGS | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TaskSetField" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TaskSetField" j)
    let to_json = simple_to_json to_value
  end
module TaskDefinitionField =
  struct
    type nonrec t =
      | TAGS 
      | Non_static_id of string 
    let make i = i
    let to_string = function | TAGS -> "TAGS" | Non_static_id s -> s
    let of_string = function | "TAGS" -> TAGS | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration TaskDefinitionField" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TaskDefinitionField" j)
    let to_json = simple_to_json to_value
  end
module Service =
  struct
    type nonrec t =
      {
      serviceArn: String_.t option
        [@ocaml.doc
          "The ARN that identifies the service. The ARN contains the arn:aws:ecs namespace, followed by the Region of the service, the Amazon Web Services account ID of the service owner, the service namespace, and then the service name. For example, arn:aws:ecs:region:012345678910:service/my-service."];
      serviceName: String_.t option
        [@ocaml.doc
          "The name of your service. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed. Service names must be unique within a cluster. However, you can have similarly named services in multiple clusters within a Region or across multiple Regions."];
      clusterArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the cluster that hosts the service."];
      loadBalancers: LoadBalancers.t option
        [@ocaml.doc
          "A list of Elastic Load Balancing load balancer objects. It contains the load balancer name, the container name, and the container port to access from the load balancer. The container name is as it appears in a container definition."];
      serviceRegistries: ServiceRegistries.t option
        [@ocaml.doc
          "The details for the service discovery registries to assign to this service. For more information, see Service Discovery."];
      status: String_.t option
        [@ocaml.doc
          "The status of the service. The valid values are ACTIVE, DRAINING, or INACTIVE."];
      desiredCount: Integer.t option
        [@ocaml.doc
          "The desired number of instantiations of the task definition to keep running on the service. This value is specified when the service is created with CreateService, and it can be modified with UpdateService."];
      runningCount: Integer.t option
        [@ocaml.doc
          "The number of tasks in the cluster that are in the RUNNING state."];
      pendingCount: Integer.t option
        [@ocaml.doc
          "The number of tasks in the cluster that are in the PENDING state."];
      launchType: LaunchType.t option
        [@ocaml.doc
          "The launch type the service is using. When using the DescribeServices API, this field is omitted if the service was created using a capacity provider strategy."];
      capacityProviderStrategy: CapacityProviderStrategy.t option
        [@ocaml.doc
          "The capacity provider strategy the service uses. When using the DescribeServices API, this field is omitted if the service was created using a launch type."];
      platformVersion: String_.t option
        [@ocaml.doc
          "The platform version to run your service on. A platform version is only specified for tasks that are hosted on Fargate. If one isn't specified, the LATEST platform version is used. For more information, see Fargate Platform Versions in the Amazon Elastic Container Service Developer Guide."];
      platformFamily: String_.t option
        [@ocaml.doc
          "The operating system that your tasks in the service run on. A platform family is specified only for tasks using the Fargate launch type. All tasks that run as part of this service must use the same platformFamily value as the service (for example, LINUX)."];
      taskDefinition: String_.t option
        [@ocaml.doc
          "The task definition to use for tasks in the service. This value is specified when the service is created with CreateService, and it can be modified with UpdateService."];
      deploymentConfiguration: DeploymentConfiguration.t option
        [@ocaml.doc
          "Optional deployment parameters that control how many tasks run during the deployment and the ordering of stopping and starting tasks."];
      taskSets: TaskSets.t option
        [@ocaml.doc
          "Information about a set of Amazon ECS tasks in either an CodeDeploy or an EXTERNAL deployment. An Amazon ECS task set includes details such as the desired number of tasks, how many tasks are running, and whether the task set serves production traffic."];
      deployments: Deployments.t option
        [@ocaml.doc "The current state of deployments for the service."];
      roleArn: String_.t option
        [@ocaml.doc
          "The ARN of the IAM role that's associated with the service. It allows the Amazon ECS container agent to register container instances with an Elastic Load Balancing load balancer."];
      events: ServiceEvents.t option
        [@ocaml.doc
          "The event stream for your service. A maximum of 100 of the latest events are displayed."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the service was created."];
      placementConstraints: PlacementConstraints.t option
        [@ocaml.doc
          "The placement constraints for the tasks in the service."];
      placementStrategy: PlacementStrategies.t option
        [@ocaml.doc
          "The placement strategy that determines how tasks for the service are placed."];
      networkConfiguration: NetworkConfiguration.t option
        [@ocaml.doc
          "The VPC subnet and security group configuration for tasks that receive their own elastic network interface by using the awsvpc networking mode."];
      healthCheckGracePeriodSeconds: BoxedInteger.t option
        [@ocaml.doc
          "The period of time, in seconds, that the Amazon ECS service scheduler ignores unhealthy Elastic Load Balancing target health checks after a task has first started."];
      schedulingStrategy: SchedulingStrategy.t option
        [@ocaml.doc
          "The scheduling strategy to use for the service. For more information, see Services. There are two service scheduler strategies available. REPLICA-The replica scheduling strategy places and maintains the desired number of tasks across your cluster. By default, the service scheduler spreads tasks across Availability Zones. You can use task placement strategies and constraints to customize task placement decisions. DAEMON-The daemon scheduling strategy deploys exactly one task on each active container instance. This task meets all of the task placement constraints that you specify in your cluster. The service scheduler also evaluates the task placement constraints for running tasks. It stop tasks that don't meet the placement constraints. Fargate tasks don't support the DAEMON scheduling strategy."];
      deploymentController: DeploymentController.t option
        [@ocaml.doc
          "The deployment controller type the service is using. When using the DescribeServices API, this field is omitted if the service uses the ECS deployment controller type."];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the service to help you categorize and organize them. Each tag consists of a key and an optional value. You define bot the key and value. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."];
      createdBy: String_.t option
        [@ocaml.doc "The principal that created the service."];
      enableECSManagedTags: Boolean.t option
        [@ocaml.doc
          "Determines whether to use Amazon ECS managed tags for the tasks in the service. For more information, see Tagging Your Amazon ECS Resources in the Amazon Elastic Container Service Developer Guide."];
      propagateTags: PropagateTags.t option
        [@ocaml.doc
          "Determines whether to propagate the tags from the task definition or the service to the task. If no value is specified, the tags aren't propagated."];
      enableExecuteCommand: Boolean.t option
        [@ocaml.doc
          "Determines whether the execute command functionality is enabled for the service. If true, the execute command functionality is enabled for all containers in tasks as part of the service."]}
    let make ?serviceArn =
      fun ?serviceName ->
        fun ?clusterArn ->
          fun ?loadBalancers ->
            fun ?serviceRegistries ->
              fun ?status ->
                fun ?desiredCount ->
                  fun ?runningCount ->
                    fun ?pendingCount ->
                      fun ?launchType ->
                        fun ?capacityProviderStrategy ->
                          fun ?platformVersion ->
                            fun ?platformFamily ->
                              fun ?taskDefinition ->
                                fun ?deploymentConfiguration ->
                                  fun ?taskSets ->
                                    fun ?deployments ->
                                      fun ?roleArn ->
                                        fun ?events ->
                                          fun ?createdAt ->
                                            fun ?placementConstraints ->
                                              fun ?placementStrategy ->
                                                fun ?networkConfiguration ->
                                                  fun
                                                    ?healthCheckGracePeriodSeconds
                                                    ->
                                                    fun ?schedulingStrategy
                                                      ->
                                                      fun
                                                        ?deploymentController
                                                        ->
                                                        fun ?tags ->
                                                          fun ?createdBy ->
                                                            fun
                                                              ?enableECSManagedTags
                                                              ->
                                                              fun
                                                                ?propagateTags
                                                                ->
                                                                fun
                                                                  ?enableExecuteCommand
                                                                  ->
                                                                  fun () ->
                                                                    {
                                                                    serviceArn;
                                                                    serviceName;
                                                                    clusterArn;
                                                                    loadBalancers;
                                                                    serviceRegistries;
                                                                    status;
                                                                    desiredCount;
                                                                    runningCount;
                                                                    pendingCount;
                                                                    launchType;
                                                                    capacityProviderStrategy;
                                                                    platformVersion;
                                                                    platformFamily;
                                                                    taskDefinition;
                                                                    deploymentConfiguration;
                                                                    taskSets;
                                                                    deployments;
                                                                    roleArn;
                                                                    events;
                                                                    createdAt;
                                                                    placementConstraints;
                                                                    placementStrategy;
                                                                    networkConfiguration;
                                                                    healthCheckGracePeriodSeconds;
                                                                    schedulingStrategy;
                                                                    deploymentController;
                                                                    tags;
                                                                    createdBy;
                                                                    enableECSManagedTags;
                                                                    propagateTags;
                                                                    enableExecuteCommand
                                                                    }
    let to_value x =
      structure_to_value
        [("serviceArn", (Option.map x.serviceArn ~f:String_.to_value));
        ("serviceName", (Option.map x.serviceName ~f:String_.to_value));
        ("clusterArn", (Option.map x.clusterArn ~f:String_.to_value));
        ("loadBalancers",
          (Option.map x.loadBalancers ~f:LoadBalancers.to_value));
        ("serviceRegistries",
          (Option.map x.serviceRegistries ~f:ServiceRegistries.to_value));
        ("status", (Option.map x.status ~f:String_.to_value));
        ("desiredCount", (Option.map x.desiredCount ~f:Integer.to_value));
        ("runningCount", (Option.map x.runningCount ~f:Integer.to_value));
        ("pendingCount", (Option.map x.pendingCount ~f:Integer.to_value));
        ("launchType", (Option.map x.launchType ~f:LaunchType.to_value));
        ("capacityProviderStrategy",
          (Option.map x.capacityProviderStrategy
             ~f:CapacityProviderStrategy.to_value));
        ("platformVersion",
          (Option.map x.platformVersion ~f:String_.to_value));
        ("platformFamily", (Option.map x.platformFamily ~f:String_.to_value));
        ("taskDefinition", (Option.map x.taskDefinition ~f:String_.to_value));
        ("deploymentConfiguration",
          (Option.map x.deploymentConfiguration
             ~f:DeploymentConfiguration.to_value));
        ("taskSets", (Option.map x.taskSets ~f:TaskSets.to_value));
        ("deployments", (Option.map x.deployments ~f:Deployments.to_value));
        ("roleArn", (Option.map x.roleArn ~f:String_.to_value));
        ("events", (Option.map x.events ~f:ServiceEvents.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("placementConstraints",
          (Option.map x.placementConstraints ~f:PlacementConstraints.to_value));
        ("placementStrategy",
          (Option.map x.placementStrategy ~f:PlacementStrategies.to_value));
        ("networkConfiguration",
          (Option.map x.networkConfiguration ~f:NetworkConfiguration.to_value));
        ("healthCheckGracePeriodSeconds",
          (Option.map x.healthCheckGracePeriodSeconds
             ~f:BoxedInteger.to_value));
        ("schedulingStrategy",
          (Option.map x.schedulingStrategy ~f:SchedulingStrategy.to_value));
        ("deploymentController",
          (Option.map x.deploymentController ~f:DeploymentController.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("createdBy", (Option.map x.createdBy ~f:String_.to_value));
        ("enableECSManagedTags",
          (Option.map x.enableECSManagedTags ~f:Boolean.to_value));
        ("propagateTags",
          (Option.map x.propagateTags ~f:PropagateTags.to_value));
        ("enableExecuteCommand",
          (Option.map x.enableExecuteCommand ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enableExecuteCommand =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableExecuteCommand") in
      let propagateTags =
        (Option.map ~f:PropagateTags.of_xml)
          (Xml.child xml_arg0 "propagateTags") in
      let enableECSManagedTags =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableECSManagedTags") in
      let createdBy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "createdBy") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let deploymentController =
        (Option.map ~f:DeploymentController.of_xml)
          (Xml.child xml_arg0 "deploymentController") in
      let schedulingStrategy =
        (Option.map ~f:SchedulingStrategy.of_xml)
          (Xml.child xml_arg0 "schedulingStrategy") in
      let healthCheckGracePeriodSeconds =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "healthCheckGracePeriodSeconds") in
      let networkConfiguration =
        (Option.map ~f:NetworkConfiguration.of_xml)
          (Xml.child xml_arg0 "networkConfiguration") in
      let placementStrategy =
        (Option.map ~f:PlacementStrategies.of_xml)
          (Xml.child xml_arg0 "placementStrategy") in
      let placementConstraints =
        (Option.map ~f:PlacementConstraints.of_xml)
          (Xml.child xml_arg0 "placementConstraints") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let events =
        (Option.map ~f:ServiceEvents.of_xml) (Xml.child xml_arg0 "events") in
      let roleArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "roleArn") in
      let deployments =
        (Option.map ~f:Deployments.of_xml) (Xml.child xml_arg0 "deployments") in
      let taskSets =
        (Option.map ~f:TaskSets.of_xml) (Xml.child xml_arg0 "taskSets") in
      let deploymentConfiguration =
        (Option.map ~f:DeploymentConfiguration.of_xml)
          (Xml.child xml_arg0 "deploymentConfiguration") in
      let taskDefinition =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskDefinition") in
      let platformFamily =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platformFamily") in
      let platformVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platformVersion") in
      let capacityProviderStrategy =
        (Option.map ~f:CapacityProviderStrategy.of_xml)
          (Xml.child xml_arg0 "capacityProviderStrategy") in
      let launchType =
        (Option.map ~f:LaunchType.of_xml) (Xml.child xml_arg0 "launchType") in
      let pendingCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "pendingCount") in
      let runningCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "runningCount") in
      let desiredCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "desiredCount") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let serviceRegistries =
        (Option.map ~f:ServiceRegistries.of_xml)
          (Xml.child xml_arg0 "serviceRegistries") in
      let loadBalancers =
        (Option.map ~f:LoadBalancers.of_xml)
          (Xml.child xml_arg0 "loadBalancers") in
      let clusterArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clusterArn") in
      let serviceName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "serviceName") in
      let serviceArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "serviceArn") in
      make ?enableExecuteCommand ?propagateTags ?enableECSManagedTags
        ?createdBy ?tags ?deploymentController ?schedulingStrategy
        ?healthCheckGracePeriodSeconds ?networkConfiguration
        ?placementStrategy ?placementConstraints ?createdAt ?events ?roleArn
        ?deployments ?taskSets ?deploymentConfiguration ?taskDefinition
        ?platformFamily ?platformVersion ?capacityProviderStrategy
        ?launchType ?pendingCount ?runningCount ?desiredCount ?status
        ?serviceRegistries ?loadBalancers ?clusterArn ?serviceName
        ?serviceArn ()
    let of_json json =
      let enableExecuteCommand =
        field_map json "enableExecuteCommand" Boolean.of_json in
      let propagateTags =
        field_map json "propagateTags" PropagateTags.of_json in
      let enableECSManagedTags =
        field_map json "enableECSManagedTags" Boolean.of_json in
      let createdBy = field_map json "createdBy" String_.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let deploymentController =
        field_map json "deploymentController" DeploymentController.of_json in
      let schedulingStrategy =
        field_map json "schedulingStrategy" SchedulingStrategy.of_json in
      let healthCheckGracePeriodSeconds =
        field_map json "healthCheckGracePeriodSeconds" BoxedInteger.of_json in
      let networkConfiguration =
        field_map json "networkConfiguration" NetworkConfiguration.of_json in
      let placementStrategy =
        field_map json "placementStrategy" PlacementStrategies.of_json in
      let placementConstraints =
        field_map json "placementConstraints" PlacementConstraints.of_json in
      let createdAt = field_map json "createdAt" Timestamp.of_json in
      let events = field_map json "events" ServiceEvents.of_json in
      let roleArn = field_map json "roleArn" String_.of_json in
      let deployments = field_map json "deployments" Deployments.of_json in
      let taskSets = field_map json "taskSets" TaskSets.of_json in
      let deploymentConfiguration =
        field_map json "deploymentConfiguration"
          DeploymentConfiguration.of_json in
      let taskDefinition = field_map json "taskDefinition" String_.of_json in
      let platformFamily = field_map json "platformFamily" String_.of_json in
      let platformVersion = field_map json "platformVersion" String_.of_json in
      let capacityProviderStrategy =
        field_map json "capacityProviderStrategy"
          CapacityProviderStrategy.of_json in
      let launchType = field_map json "launchType" LaunchType.of_json in
      let pendingCount = field_map json "pendingCount" Integer.of_json in
      let runningCount = field_map json "runningCount" Integer.of_json in
      let desiredCount = field_map json "desiredCount" Integer.of_json in
      let status = field_map json "status" String_.of_json in
      let serviceRegistries =
        field_map json "serviceRegistries" ServiceRegistries.of_json in
      let loadBalancers =
        field_map json "loadBalancers" LoadBalancers.of_json in
      let clusterArn = field_map json "clusterArn" String_.of_json in
      let serviceName = field_map json "serviceName" String_.of_json in
      let serviceArn = field_map json "serviceArn" String_.of_json in
      make ?enableExecuteCommand ?propagateTags ?enableECSManagedTags
        ?createdBy ?tags ?deploymentController ?schedulingStrategy
        ?healthCheckGracePeriodSeconds ?networkConfiguration
        ?placementStrategy ?placementConstraints ?createdAt ?events ?roleArn
        ?deployments ?taskSets ?deploymentConfiguration ?taskDefinition
        ?platformFamily ?platformVersion ?capacityProviderStrategy
        ?launchType ?pendingCount ?runningCount ?desiredCount ?status
        ?serviceRegistries ?loadBalancers ?clusterArn ?serviceName
        ?serviceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Details on a service within a cluster"]
module ServiceField =
  struct
    type nonrec t =
      | TAGS 
      | Non_static_id of string 
    let make i = i
    let to_string = function | TAGS -> "TAGS" | Non_static_id s -> s
    let of_string = function | "TAGS" -> TAGS | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ServiceField" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ServiceField" j)
    let to_json = simple_to_json to_value
  end
module ContainerInstanceField =
  struct
    type nonrec t =
      | TAGS 
      | CONTAINER_INSTANCE_HEALTH 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TAGS -> "TAGS"
      | CONTAINER_INSTANCE_HEALTH -> "CONTAINER_INSTANCE_HEALTH"
      | Non_static_id s -> s
    let of_string =
      function
      | "TAGS" -> TAGS
      | "CONTAINER_INSTANCE_HEALTH" -> CONTAINER_INSTANCE_HEALTH
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ContainerInstanceField" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ContainerInstanceField" j)
    let to_json = simple_to_json to_value
  end
module Cluster =
  struct
    type nonrec t =
      {
      clusterArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that identifies the cluster. The ARN contains the arn:aws:ecs namespace, followed by the Region of the cluster, the Amazon Web Services account ID of the cluster owner, the cluster namespace, and then the cluster name. For example, arn:aws:ecs:region:012345678910:cluster/test."];
      clusterName: String_.t option
        [@ocaml.doc
          "A user-generated string that you use to identify your cluster."];
      configuration: ClusterConfiguration.t option
        [@ocaml.doc "The execute command configuration for the cluster."];
      status: String_.t option
        [@ocaml.doc
          "The status of the cluster. The following are the possible states that are returned. ACTIVE The cluster is ready to accept tasks and if applicable you can register container instances with the cluster. PROVISIONING The cluster has capacity providers that are associated with it and the resources needed for the capacity provider are being created. DEPROVISIONING The cluster has capacity providers that are associated with it and the resources needed for the capacity provider are being deleted. FAILED The cluster has capacity providers that are associated with it and the resources needed for the capacity provider have failed to create. INACTIVE The cluster has been deleted. Clusters with an INACTIVE status may remain discoverable in your account for a period of time. However, this behavior is subject to change in the future. We don't recommend that you rely on INACTIVE clusters persisting."];
      registeredContainerInstancesCount: Integer.t option
        [@ocaml.doc
          "The number of container instances registered into the cluster. This includes container instances in both ACTIVE and DRAINING status."];
      runningTasksCount: Integer.t option
        [@ocaml.doc
          "The number of tasks in the cluster that are in the RUNNING state."];
      pendingTasksCount: Integer.t option
        [@ocaml.doc
          "The number of tasks in the cluster that are in the PENDING state."];
      activeServicesCount: Integer.t option
        [@ocaml.doc
          "The number of services that are running on the cluster in an ACTIVE state. You can view these services with ListServices."];
      statistics: Statistics.t option
        [@ocaml.doc
          "Additional information about your clusters that are separated by launch type. They include the following: runningEC2TasksCount RunningFargateTasksCount pendingEC2TasksCount pendingFargateTasksCount activeEC2ServiceCount activeFargateServiceCount drainingEC2ServiceCount drainingFargateServiceCount"];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the cluster to help you categorize and organize them. Each tag consists of a key and an optional value. You define both. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."];
      settings: ClusterSettings.t option
        [@ocaml.doc
          "The settings for the cluster. This parameter indicates whether CloudWatch Container Insights is enabled or disabled for a cluster."];
      capacityProviders: StringList.t option
        [@ocaml.doc "The capacity providers associated with the cluster."];
      defaultCapacityProviderStrategy: CapacityProviderStrategy.t option
        [@ocaml.doc
          "The default capacity provider strategy for the cluster. When services or tasks are run in the cluster with no launch type or capacity provider strategy specified, the default capacity provider strategy is used."];
      attachments: Attachments.t option
        [@ocaml.doc
          "The resources attached to a cluster. When using a capacity provider with a cluster, the Auto Scaling plan that's created is returned as a cluster attachment."];
      attachmentsStatus: String_.t option
        [@ocaml.doc
          "The status of the capacity providers associated with the cluster. The following are the states that are returned. UPDATE_IN_PROGRESS The available capacity providers for the cluster are updating. This occurs when the Auto Scaling plan is provisioning or deprovisioning. UPDATE_COMPLETE The capacity providers have successfully updated. UPDATE_FAILED The capacity provider updates failed."]}
    let make ?clusterArn =
      fun ?clusterName ->
        fun ?configuration ->
          fun ?status ->
            fun ?registeredContainerInstancesCount ->
              fun ?runningTasksCount ->
                fun ?pendingTasksCount ->
                  fun ?activeServicesCount ->
                    fun ?statistics ->
                      fun ?tags ->
                        fun ?settings ->
                          fun ?capacityProviders ->
                            fun ?defaultCapacityProviderStrategy ->
                              fun ?attachments ->
                                fun ?attachmentsStatus ->
                                  fun () ->
                                    {
                                      clusterArn;
                                      clusterName;
                                      configuration;
                                      status;
                                      registeredContainerInstancesCount;
                                      runningTasksCount;
                                      pendingTasksCount;
                                      activeServicesCount;
                                      statistics;
                                      tags;
                                      settings;
                                      capacityProviders;
                                      defaultCapacityProviderStrategy;
                                      attachments;
                                      attachmentsStatus
                                    }
    let to_value x =
      structure_to_value
        [("clusterArn", (Option.map x.clusterArn ~f:String_.to_value));
        ("clusterName", (Option.map x.clusterName ~f:String_.to_value));
        ("configuration",
          (Option.map x.configuration ~f:ClusterConfiguration.to_value));
        ("status", (Option.map x.status ~f:String_.to_value));
        ("registeredContainerInstancesCount",
          (Option.map x.registeredContainerInstancesCount ~f:Integer.to_value));
        ("runningTasksCount",
          (Option.map x.runningTasksCount ~f:Integer.to_value));
        ("pendingTasksCount",
          (Option.map x.pendingTasksCount ~f:Integer.to_value));
        ("activeServicesCount",
          (Option.map x.activeServicesCount ~f:Integer.to_value));
        ("statistics", (Option.map x.statistics ~f:Statistics.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("settings", (Option.map x.settings ~f:ClusterSettings.to_value));
        ("capacityProviders",
          (Option.map x.capacityProviders ~f:StringList.to_value));
        ("defaultCapacityProviderStrategy",
          (Option.map x.defaultCapacityProviderStrategy
             ~f:CapacityProviderStrategy.to_value));
        ("attachments", (Option.map x.attachments ~f:Attachments.to_value));
        ("attachmentsStatus",
          (Option.map x.attachmentsStatus ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attachmentsStatus =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "attachmentsStatus") in
      let attachments =
        (Option.map ~f:Attachments.of_xml) (Xml.child xml_arg0 "attachments") in
      let defaultCapacityProviderStrategy =
        (Option.map ~f:CapacityProviderStrategy.of_xml)
          (Xml.child xml_arg0 "defaultCapacityProviderStrategy") in
      let capacityProviders =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "capacityProviders") in
      let settings =
        (Option.map ~f:ClusterSettings.of_xml)
          (Xml.child xml_arg0 "settings") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let statistics =
        (Option.map ~f:Statistics.of_xml) (Xml.child xml_arg0 "statistics") in
      let activeServicesCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "activeServicesCount") in
      let pendingTasksCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "pendingTasksCount") in
      let runningTasksCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "runningTasksCount") in
      let registeredContainerInstancesCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "registeredContainerInstancesCount") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let configuration =
        (Option.map ~f:ClusterConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      let clusterName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clusterName") in
      let clusterArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clusterArn") in
      make ?attachmentsStatus ?attachments ?defaultCapacityProviderStrategy
        ?capacityProviders ?settings ?tags ?statistics ?activeServicesCount
        ?pendingTasksCount ?runningTasksCount
        ?registeredContainerInstancesCount ?status ?configuration
        ?clusterName ?clusterArn ()
    let of_json json =
      let attachmentsStatus =
        field_map json "attachmentsStatus" String_.of_json in
      let attachments = field_map json "attachments" Attachments.of_json in
      let defaultCapacityProviderStrategy =
        field_map json "defaultCapacityProviderStrategy"
          CapacityProviderStrategy.of_json in
      let capacityProviders =
        field_map json "capacityProviders" StringList.of_json in
      let settings = field_map json "settings" ClusterSettings.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let statistics = field_map json "statistics" Statistics.of_json in
      let activeServicesCount =
        field_map json "activeServicesCount" Integer.of_json in
      let pendingTasksCount =
        field_map json "pendingTasksCount" Integer.of_json in
      let runningTasksCount =
        field_map json "runningTasksCount" Integer.of_json in
      let registeredContainerInstancesCount =
        field_map json "registeredContainerInstancesCount" Integer.of_json in
      let status = field_map json "status" String_.of_json in
      let configuration =
        field_map json "configuration" ClusterConfiguration.of_json in
      let clusterName = field_map json "clusterName" String_.of_json in
      let clusterArn = field_map json "clusterArn" String_.of_json in
      make ?attachmentsStatus ?attachments ?defaultCapacityProviderStrategy
        ?capacityProviders ?settings ?tags ?statistics ?activeServicesCount
        ?pendingTasksCount ?runningTasksCount
        ?registeredContainerInstancesCount ?status ?configuration
        ?clusterName ?clusterArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A regional grouping of one or more container instances where you can run task requests. Each account receives a default cluster the first time you use the Amazon ECS service, but you may also create other clusters. Clusters may contain more than one instance type simultaneously."]
module ClusterField =
  struct
    type nonrec t =
      | ATTACHMENTS 
      | CONFIGURATIONS 
      | SETTINGS 
      | STATISTICS 
      | TAGS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ATTACHMENTS -> "ATTACHMENTS"
      | CONFIGURATIONS -> "CONFIGURATIONS"
      | SETTINGS -> "SETTINGS"
      | STATISTICS -> "STATISTICS"
      | TAGS -> "TAGS"
      | Non_static_id s -> s
    let of_string =
      function
      | "ATTACHMENTS" -> ATTACHMENTS
      | "CONFIGURATIONS" -> CONFIGURATIONS
      | "SETTINGS" -> SETTINGS
      | "STATISTICS" -> STATISTICS
      | "TAGS" -> TAGS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ClusterField" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ClusterField" j)
    let to_json = simple_to_json to_value
  end
module CapacityProvider =
  struct
    type nonrec t =
      {
      capacityProviderArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that identifies the capacity provider."];
      name: String_.t option
        [@ocaml.doc "The name of the capacity provider."];
      status: CapacityProviderStatus.t option
        [@ocaml.doc
          "The current status of the capacity provider. Only capacity providers in an ACTIVE state can be used in a cluster. When a capacity provider is successfully deleted, it has an INACTIVE status."];
      autoScalingGroupProvider: AutoScalingGroupProvider.t option
        [@ocaml.doc
          "The Auto Scaling group settings for the capacity provider."];
      updateStatus: CapacityProviderUpdateStatus.t option
        [@ocaml.doc
          "The update status of the capacity provider. The following are the possible states that is returned. DELETE_IN_PROGRESS The capacity provider is in the process of being deleted. DELETE_COMPLETE The capacity provider was successfully deleted and has an INACTIVE status. DELETE_FAILED The capacity provider can't be deleted. The update status reason provides further details about why the delete failed."];
      updateStatusReason: String_.t option
        [@ocaml.doc
          "The update status reason. This provides further details about the update status for the capacity provider."];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the capacity provider to help you categorize and organize it. Each tag consists of a key and an optional value. You define both. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."]}
    let make ?capacityProviderArn =
      fun ?name ->
        fun ?status ->
          fun ?autoScalingGroupProvider ->
            fun ?updateStatus ->
              fun ?updateStatusReason ->
                fun ?tags ->
                  fun () ->
                    {
                      capacityProviderArn;
                      name;
                      status;
                      autoScalingGroupProvider;
                      updateStatus;
                      updateStatusReason;
                      tags
                    }
    let to_value x =
      structure_to_value
        [("capacityProviderArn",
           (Option.map x.capacityProviderArn ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("status", (Option.map x.status ~f:CapacityProviderStatus.to_value));
        ("autoScalingGroupProvider",
          (Option.map x.autoScalingGroupProvider
             ~f:AutoScalingGroupProvider.to_value));
        ("updateStatus",
          (Option.map x.updateStatus ~f:CapacityProviderUpdateStatus.to_value));
        ("updateStatusReason",
          (Option.map x.updateStatusReason ~f:String_.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let updateStatusReason =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "updateStatusReason") in
      let updateStatus =
        (Option.map ~f:CapacityProviderUpdateStatus.of_xml)
          (Xml.child xml_arg0 "updateStatus") in
      let autoScalingGroupProvider =
        (Option.map ~f:AutoScalingGroupProvider.of_xml)
          (Xml.child xml_arg0 "autoScalingGroupProvider") in
      let status =
        (Option.map ~f:CapacityProviderStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let capacityProviderArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "capacityProviderArn") in
      make ?tags ?updateStatusReason ?updateStatus ?autoScalingGroupProvider
        ?status ?name ?capacityProviderArn ()
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let updateStatusReason =
        field_map json "updateStatusReason" String_.of_json in
      let updateStatus =
        field_map json "updateStatus" CapacityProviderUpdateStatus.of_json in
      let autoScalingGroupProvider =
        field_map json "autoScalingGroupProvider"
          AutoScalingGroupProvider.of_json in
      let status = field_map json "status" CapacityProviderStatus.of_json in
      let name = field_map json "name" String_.of_json in
      let capacityProviderArn =
        field_map json "capacityProviderArn" String_.of_json in
      make ?tags ?updateStatusReason ?updateStatus ?autoScalingGroupProvider
        ?status ?name ?capacityProviderArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The details for a capacity provider."]
module CapacityProviderField =
  struct
    type nonrec t =
      | TAGS 
      | Non_static_id of string 
    let make i = i
    let to_string = function | TAGS -> "TAGS" | Non_static_id s -> s
    let of_string = function | "TAGS" -> TAGS | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration CapacityProviderField" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"CapacityProviderField" j)
    let to_json = simple_to_json to_value
  end
module AccessDeniedException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You don't have authorization to perform the requested action."]
module ClientException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "These errors are usually caused by a client action. This client action might be using an action or resource on behalf of a user that doesn't have permissions to use the action or resource,. Or, it might be specifying an identifier that isn't valid."]
module ClusterNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified cluster wasn't found. You can view your available clusters with ListClusters. Amazon ECS clusters are Region specific."]
module InvalidParameterException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified parameter isn't valid. Review the available parameters for the API request."]
module ServerException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "These errors are usually caused by a server issue."]
module ServiceNotActiveException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified service isn't active. You can't update a service that's inactive. If you have previously deleted a service, you can re-create it with CreateService."]
module ServiceNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified service wasn't found. You can view your available services with ListServices. Amazon ECS services are cluster specific and Region specific."]
module TaskSetNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified task set wasn't found. You can view your available task sets with DescribeTaskSets. Task sets are specific to each cluster, service and Region."]
module UnsupportedFeatureException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified task isn't supported in this Region."]
module PlatformTaskDefinitionIncompatibilityException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified platform version doesn't satisfy the required capabilities of the task definition."]
module PlatformUnknownException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified platform version doesn't exist."]
module ContainerInstances =
  struct
    type nonrec t = ContainerInstance.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ContainerInstance.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerInstance.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerInstances"
        ~of_json:ContainerInstance.of_json j
    let to_json v = composed_to_json to_value v
  end
module Failures =
  struct
    type nonrec t = Failure.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Failure.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Failure.of_xml)
    let of_json j = list_of_json ~kind:"Failures" ~of_json:Failure.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerInstanceStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | DRAINING 
      | REGISTERING 
      | DEREGISTERING 
      | REGISTRATION_FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | DRAINING -> "DRAINING"
      | REGISTERING -> "REGISTERING"
      | DEREGISTERING -> "DEREGISTERING"
      | REGISTRATION_FAILED -> "REGISTRATION_FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "DRAINING" -> DRAINING
      | "REGISTERING" -> REGISTERING
      | "DEREGISTERING" -> DEREGISTERING
      | "REGISTRATION_FAILED" -> REGISTRATION_FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ContainerInstanceStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ContainerInstanceStatus" j)
    let to_json = simple_to_json to_value
  end
module MissingVersionException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Amazon ECS can't determine the current version of the Amazon ECS container agent on the container instance and doesn't have enough information to proceed with an update. This could be because the agent running on the container instance is a previous or custom version that doesn't use our version information."]
module NoUpdateAvailableException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "There's no update available for this Amazon ECS container agent. This might be because the agent is already running the latest version or because it's so old that there's no update path to the current version."]
module UpdateInProgressException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "There's already a current Amazon ECS container agent update in progress on the container instance that's specified. If the container agent becomes disconnected while it's in a transitional stage, such as PENDING or STAGING, the update process can get stuck in that state. However, when the agent reconnects, it resumes where it stopped previously."]
module AutoScalingGroupProviderUpdate =
  struct
    type nonrec t =
      {
      managedScaling: ManagedScaling.t option
        [@ocaml.doc
          "The managed scaling settings for the Auto Scaling group capacity provider."];
      managedTerminationProtection: ManagedTerminationProtection.t option
        [@ocaml.doc
          "The managed termination protection setting to use for the Auto Scaling group capacity provider. This determines whether the Auto Scaling group has managed termination protection. When using managed termination protection, managed scaling must also be used otherwise managed termination protection doesn't work. When managed termination protection is enabled, Amazon ECS prevents the Amazon EC2 instances in an Auto Scaling group that contain tasks from being terminated during a scale-in action. The Auto Scaling group and each instance in the Auto Scaling group must have instance protection from scale-in actions enabled. For more information, see Instance Protection in the Auto Scaling User Guide. When managed termination protection is disabled, your Amazon EC2 instances aren't protected from termination when the Auto Scaling group scales in."]}
    let make ?managedScaling =
      fun ?managedTerminationProtection ->
        fun () -> { managedScaling; managedTerminationProtection }
    let to_value x =
      structure_to_value
        [("managedScaling",
           (Option.map x.managedScaling ~f:ManagedScaling.to_value));
        ("managedTerminationProtection",
          (Option.map x.managedTerminationProtection
             ~f:ManagedTerminationProtection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let managedTerminationProtection =
        (Option.map ~f:ManagedTerminationProtection.of_xml)
          (Xml.child xml_arg0 "managedTerminationProtection") in
      let managedScaling =
        (Option.map ~f:ManagedScaling.of_xml)
          (Xml.child xml_arg0 "managedScaling") in
      make ?managedTerminationProtection ?managedScaling ()
    let of_json json =
      let managedTerminationProtection =
        field_map json "managedTerminationProtection"
          ManagedTerminationProtection.of_json in
      let managedScaling =
        field_map json "managedScaling" ManagedScaling.of_json in
      make ?managedTerminationProtection ?managedScaling ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details of the Auto Scaling group capacity provider to update."]
module ResourceNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified resource wasn't found."]
module TagKeys =
  struct
    type nonrec t = TagKey.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeys" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module AttachmentStateChanges =
  struct
    type nonrec t = AttachmentStateChange.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AttachmentStateChange.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AttachmentStateChange.of_xml)
    let of_json j =
      list_of_json ~kind:"AttachmentStateChanges"
        ~of_json:AttachmentStateChange.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerStateChanges =
  struct
    type nonrec t = ContainerStateChange.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ContainerStateChange.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerStateChange.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerStateChanges"
        ~of_json:ContainerStateChange.of_json j
    let to_json v = composed_to_json to_value v
  end
module ManagedAgentStateChanges =
  struct
    type nonrec t = ManagedAgentStateChange.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ManagedAgentStateChange.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ManagedAgentStateChange.of_xml)
    let of_json j =
      list_of_json ~kind:"ManagedAgentStateChanges"
        ~of_json:ManagedAgentStateChange.of_json j
    let to_json v = composed_to_json to_value v
  end
module Tasks =
  struct
    type nonrec t = Task.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Task.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Task.of_xml)
    let of_json j = list_of_json ~kind:"Tasks" ~of_json:Task.of_json j
    let to_json v = composed_to_json to_value v
  end
module BlockedException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Your Amazon Web Services account was blocked. For more information, contact Amazon Web Services Support."]
module TaskDefinition =
  struct
    type nonrec t =
      {
      taskDefinitionArn: String_.t option
        [@ocaml.doc
          "The full Amazon Resource Name (ARN) of the task definition."];
      containerDefinitions: ContainerDefinitions.t option
        [@ocaml.doc
          "A list of container definitions in JSON format that describe the different containers that make up your task. For more information about container definition parameters and defaults, see Amazon ECS Task Definitions in the Amazon Elastic Container Service Developer Guide."];
      family: String_.t option
        [@ocaml.doc
          "The name of a family that this task definition is registered to. Up to 255 characters are allowed. Letters (both uppercase and lowercase letters), numbers, hyphens (-), and underscores (_) are allowed. A family groups multiple versions of a task definition. Amazon ECS gives the first task definition that you registered to a family a revision number of 1. Amazon ECS gives sequential revision numbers to each task definition that you add."];
      taskRoleArn: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the Identity and Access Management role that grants containers in the task permission to call Amazon Web Services APIs on your behalf. For more information, see Amazon ECS Task Role in the Amazon Elastic Container Service Developer Guide. IAM roles for tasks on Windows require that the -EnableTaskIAMRole option is set when you launch the Amazon ECS-optimized Windows AMI. Your containers must also run some configuration code to use the feature. For more information, see Windows IAM roles for tasks in the Amazon Elastic Container Service Developer Guide."];
      executionRoleArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the task execution role that grants the Amazon ECS container agent permission to make Amazon Web Services API calls on your behalf. The task execution IAM role is required depending on the requirements of your task. For more information, see Amazon ECS task execution IAM role in the Amazon Elastic Container Service Developer Guide."];
      networkMode: NetworkMode.t option
        [@ocaml.doc
          "The Docker networking mode to use for the containers in the task. The valid values are none, bridge, awsvpc, and host. If no network mode is specified, the default is bridge. For Amazon ECS tasks on Fargate, the awsvpc network mode is required. For Amazon ECS tasks on Amazon EC2 Linux instances, any network mode can be used. For Amazon ECS tasks on Amazon EC2 Windows instances, <default> or awsvpc can be used. If the network mode is set to none, you cannot specify port mappings in your container definitions, and the tasks containers do not have external connectivity. The host and awsvpc network modes offer the highest networking performance for containers because they use the EC2 network stack instead of the virtualized network stack provided by the bridge mode. With the host and awsvpc network modes, exposed container ports are mapped directly to the corresponding host port (for the host network mode) or the attached elastic network interface port (for the awsvpc network mode), so you cannot take advantage of dynamic host port mappings. When using the host network mode, you should not run containers using the root user (UID 0). It is considered best practice to use a non-root user. If the network mode is awsvpc, the task is allocated an elastic network interface, and you must specify a NetworkConfiguration value when you create a service or run a task with the task definition. For more information, see Task Networking in the Amazon Elastic Container Service Developer Guide. If the network mode is host, you cannot run multiple instantiations of the same task on a single container instance when port mappings are used. For more information, see Network settings in the Docker run reference."];
      revision: Integer.t option
        [@ocaml.doc
          "The revision of the task in a particular family. The revision is a version number of a task definition in a family. When you register a task definition for the first time, the revision is 1. Each time that you register a new revision of a task definition in the same family, the revision value always increases by one. This is even if you deregistered previous revisions in this family."];
      volumes: VolumeList.t option
        [@ocaml.doc
          "The list of data volume definitions for the task. For more information, see Using data volumes in tasks in the Amazon Elastic Container Service Developer Guide. The host and sourcePath parameters aren't supported for tasks run on Fargate."];
      status: TaskDefinitionStatus.t option
        [@ocaml.doc "The status of the task definition."];
      requiresAttributes: RequiresAttributes.t option
        [@ocaml.doc
          "The container instance attributes required by your task. When an Amazon EC2 instance is registered to your cluster, the Amazon ECS container agent assigns some standard attributes to the instance. You can apply custom attributes. These are specified as key-value pairs using the Amazon ECS console or the PutAttributes API. These attributes are used when determining task placement for tasks hosted on Amazon EC2 instances. For more information, see Attributes in the Amazon Elastic Container Service Developer Guide. This parameter isn't supported for tasks run on Fargate."];
      placementConstraints: TaskDefinitionPlacementConstraints.t option
        [@ocaml.doc
          "An array of placement constraint objects to use for tasks. This parameter isn't supported for tasks run on Fargate."];
      compatibilities: CompatibilityList.t option
        [@ocaml.doc
          "The task launch types the task definition validated against during task definition registration. For more information, see Amazon ECS launch types in the Amazon Elastic Container Service Developer Guide."];
      runtimePlatform: RuntimePlatform.t option
        [@ocaml.doc
          "The operating system that your task definitions are running on. A platform family is specified only for tasks using the Fargate launch type. When you specify a task in a service, this value must match the runtimePlatform value of the service."];
      requiresCompatibilities: CompatibilityList.t option
        [@ocaml.doc
          "The task launch types the task definition was validated against. To determine which task launch types the task definition is validated for, see the TaskDefinition$compatibilities parameter."];
      cpu: String_.t option
        [@ocaml.doc
          "The number of cpu units used by the task. If you use the EC2 launch type, this field is optional. Any value can be used. If you use the Fargate launch type, this field is required. You must use one of the following values. The value that you choose determines your range of valid values for the memory parameter. The CPU units cannot be less than 1 vCPU when you use Windows containers on Fargate. 256 (.25 vCPU) - Available memory values: 512 (0.5 GB), 1024 (1 GB), 2048 (2 GB) 512 (.5 vCPU) - Available memory values: 1024 (1 GB), 2048 (2 GB), 3072 (3 GB), 4096 (4 GB) 1024 (1 vCPU) - Available memory values: 2048 (2 GB), 3072 (3 GB), 4096 (4 GB), 5120 (5 GB), 6144 (6 GB), 7168 (7 GB), 8192 (8 GB) 2048 (2 vCPU) - Available memory values: Between 4096 (4 GB) and 16384 (16 GB) in increments of 1024 (1 GB) 4096 (4 vCPU) - Available memory values: Between 8192 (8 GB) and 30720 (30 GB) in increments of 1024 (1 GB)"];
      memory: String_.t option
        [@ocaml.doc
          "The amount (in MiB) of memory used by the task. If your tasks runs on Amazon EC2 instances, you must specify either a task-level memory value or a container-level memory value. This field is optional and any value can be used. If a task-level memory value is specified, the container-level memory value is optional. For more information regarding container-level memory and memory reservation, see ContainerDefinition. If your tasks runs on Fargate, this field is required. You must use one of the following values. The value you choose determines your range of valid values for the cpu parameter. 512 (0.5 GB), 1024 (1 GB), 2048 (2 GB) - Available cpu values: 256 (.25 vCPU) 1024 (1 GB), 2048 (2 GB), 3072 (3 GB), 4096 (4 GB) - Available cpu values: 512 (.5 vCPU) 2048 (2 GB), 3072 (3 GB), 4096 (4 GB), 5120 (5 GB), 6144 (6 GB), 7168 (7 GB), 8192 (8 GB) - Available cpu values: 1024 (1 vCPU) Between 4096 (4 GB) and 16384 (16 GB) in increments of 1024 (1 GB) - Available cpu values: 2048 (2 vCPU) Between 8192 (8 GB) and 30720 (30 GB) in increments of 1024 (1 GB) - Available cpu values: 4096 (4 vCPU)"];
      inferenceAccelerators: InferenceAccelerators.t option
        [@ocaml.doc
          "The Elastic Inference accelerator that's associated with the task."];
      pidMode: PidMode.t option
        [@ocaml.doc
          "The process namespace to use for the containers in the task. The valid values are host or task. If host is specified, then all containers within the tasks that specified the host PID mode on the same container instance share the same process namespace with the host Amazon EC2 instance. If task is specified, all containers within the specified task share the same process namespace. If no value is specified, the default is a private namespace. For more information, see PID settings in the Docker run reference. If the host PID mode is used, be aware that there is a heightened risk of undesired process namespace expose. For more information, see Docker security. This parameter is not supported for Windows containers or tasks run on Fargate."];
      ipcMode: IpcMode.t option
        [@ocaml.doc
          "The IPC resource namespace to use for the containers in the task. The valid values are host, task, or none. If host is specified, then all containers within the tasks that specified the host IPC mode on the same container instance share the same IPC resources with the host Amazon EC2 instance. If task is specified, all containers within the specified task share the same IPC resources. If none is specified, then IPC resources within the containers of a task are private and not shared with other containers in a task or on the container instance. If no value is specified, then the IPC resource namespace sharing depends on the Docker daemon setting on the container instance. For more information, see IPC settings in the Docker run reference. If the host IPC mode is used, be aware that there is a heightened risk of undesired IPC namespace expose. For more information, see Docker security. If you are setting namespaced kernel parameters using systemControls for the containers in the task, the following will apply to your IPC resource namespace. For more information, see System Controls in the Amazon Elastic Container Service Developer Guide. For tasks that use the host IPC mode, IPC namespace related systemControls are not supported. For tasks that use the task IPC mode, IPC namespace related systemControls will apply to all containers within a task. This parameter is not supported for Windows containers or tasks run on Fargate."];
      proxyConfiguration: ProxyConfiguration.t option
        [@ocaml.doc
          "The configuration details for the App Mesh proxy. Your Amazon ECS container instances require at least version 1.26.0 of the container agent and at least version 1.26.0-1 of the ecs-init package to use a proxy configuration. If your container instances are launched from the Amazon ECS optimized AMI version 20190301 or later, they contain the required versions of the container agent and ecs-init. For more information, see Amazon ECS-optimized Linux AMI in the Amazon Elastic Container Service Developer Guide."];
      registeredAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the task definition was registered."];
      deregisteredAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the task definition was deregistered."];
      registeredBy: String_.t option
        [@ocaml.doc "The principal that registered the task definition."];
      ephemeralStorage: EphemeralStorage.t option
        [@ocaml.doc
          "The ephemeral storage settings to use for tasks run with the task definition."]}
    let make ?taskDefinitionArn =
      fun ?containerDefinitions ->
        fun ?family ->
          fun ?taskRoleArn ->
            fun ?executionRoleArn ->
              fun ?networkMode ->
                fun ?revision ->
                  fun ?volumes ->
                    fun ?status ->
                      fun ?requiresAttributes ->
                        fun ?placementConstraints ->
                          fun ?compatibilities ->
                            fun ?runtimePlatform ->
                              fun ?requiresCompatibilities ->
                                fun ?cpu ->
                                  fun ?memory ->
                                    fun ?inferenceAccelerators ->
                                      fun ?pidMode ->
                                        fun ?ipcMode ->
                                          fun ?proxyConfiguration ->
                                            fun ?registeredAt ->
                                              fun ?deregisteredAt ->
                                                fun ?registeredBy ->
                                                  fun ?ephemeralStorage ->
                                                    fun () ->
                                                      {
                                                        taskDefinitionArn;
                                                        containerDefinitions;
                                                        family;
                                                        taskRoleArn;
                                                        executionRoleArn;
                                                        networkMode;
                                                        revision;
                                                        volumes;
                                                        status;
                                                        requiresAttributes;
                                                        placementConstraints;
                                                        compatibilities;
                                                        runtimePlatform;
                                                        requiresCompatibilities;
                                                        cpu;
                                                        memory;
                                                        inferenceAccelerators;
                                                        pidMode;
                                                        ipcMode;
                                                        proxyConfiguration;
                                                        registeredAt;
                                                        deregisteredAt;
                                                        registeredBy;
                                                        ephemeralStorage
                                                      }
    let to_value x =
      structure_to_value
        [("taskDefinitionArn",
           (Option.map x.taskDefinitionArn ~f:String_.to_value));
        ("containerDefinitions",
          (Option.map x.containerDefinitions ~f:ContainerDefinitions.to_value));
        ("family", (Option.map x.family ~f:String_.to_value));
        ("taskRoleArn", (Option.map x.taskRoleArn ~f:String_.to_value));
        ("executionRoleArn",
          (Option.map x.executionRoleArn ~f:String_.to_value));
        ("networkMode", (Option.map x.networkMode ~f:NetworkMode.to_value));
        ("revision", (Option.map x.revision ~f:Integer.to_value));
        ("volumes", (Option.map x.volumes ~f:VolumeList.to_value));
        ("status", (Option.map x.status ~f:TaskDefinitionStatus.to_value));
        ("requiresAttributes",
          (Option.map x.requiresAttributes ~f:RequiresAttributes.to_value));
        ("placementConstraints",
          (Option.map x.placementConstraints
             ~f:TaskDefinitionPlacementConstraints.to_value));
        ("compatibilities",
          (Option.map x.compatibilities ~f:CompatibilityList.to_value));
        ("runtimePlatform",
          (Option.map x.runtimePlatform ~f:RuntimePlatform.to_value));
        ("requiresCompatibilities",
          (Option.map x.requiresCompatibilities ~f:CompatibilityList.to_value));
        ("cpu", (Option.map x.cpu ~f:String_.to_value));
        ("memory", (Option.map x.memory ~f:String_.to_value));
        ("inferenceAccelerators",
          (Option.map x.inferenceAccelerators
             ~f:InferenceAccelerators.to_value));
        ("pidMode", (Option.map x.pidMode ~f:PidMode.to_value));
        ("ipcMode", (Option.map x.ipcMode ~f:IpcMode.to_value));
        ("proxyConfiguration",
          (Option.map x.proxyConfiguration ~f:ProxyConfiguration.to_value));
        ("registeredAt", (Option.map x.registeredAt ~f:Timestamp.to_value));
        ("deregisteredAt",
          (Option.map x.deregisteredAt ~f:Timestamp.to_value));
        ("registeredBy", (Option.map x.registeredBy ~f:String_.to_value));
        ("ephemeralStorage",
          (Option.map x.ephemeralStorage ~f:EphemeralStorage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ephemeralStorage =
        (Option.map ~f:EphemeralStorage.of_xml)
          (Xml.child xml_arg0 "ephemeralStorage") in
      let registeredBy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "registeredBy") in
      let deregisteredAt =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "deregisteredAt") in
      let registeredAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "registeredAt") in
      let proxyConfiguration =
        (Option.map ~f:ProxyConfiguration.of_xml)
          (Xml.child xml_arg0 "proxyConfiguration") in
      let ipcMode =
        (Option.map ~f:IpcMode.of_xml) (Xml.child xml_arg0 "ipcMode") in
      let pidMode =
        (Option.map ~f:PidMode.of_xml) (Xml.child xml_arg0 "pidMode") in
      let inferenceAccelerators =
        (Option.map ~f:InferenceAccelerators.of_xml)
          (Xml.child xml_arg0 "inferenceAccelerators") in
      let memory =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "memory") in
      let cpu = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cpu") in
      let requiresCompatibilities =
        (Option.map ~f:CompatibilityList.of_xml)
          (Xml.child xml_arg0 "requiresCompatibilities") in
      let runtimePlatform =
        (Option.map ~f:RuntimePlatform.of_xml)
          (Xml.child xml_arg0 "runtimePlatform") in
      let compatibilities =
        (Option.map ~f:CompatibilityList.of_xml)
          (Xml.child xml_arg0 "compatibilities") in
      let placementConstraints =
        (Option.map ~f:TaskDefinitionPlacementConstraints.of_xml)
          (Xml.child xml_arg0 "placementConstraints") in
      let requiresAttributes =
        (Option.map ~f:RequiresAttributes.of_xml)
          (Xml.child xml_arg0 "requiresAttributes") in
      let status =
        (Option.map ~f:TaskDefinitionStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let volumes =
        (Option.map ~f:VolumeList.of_xml) (Xml.child xml_arg0 "volumes") in
      let revision =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "revision") in
      let networkMode =
        (Option.map ~f:NetworkMode.of_xml) (Xml.child xml_arg0 "networkMode") in
      let executionRoleArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "executionRoleArn") in
      let taskRoleArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskRoleArn") in
      let family =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "family") in
      let containerDefinitions =
        (Option.map ~f:ContainerDefinitions.of_xml)
          (Xml.child xml_arg0 "containerDefinitions") in
      let taskDefinitionArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "taskDefinitionArn") in
      make ?ephemeralStorage ?registeredBy ?deregisteredAt ?registeredAt
        ?proxyConfiguration ?ipcMode ?pidMode ?inferenceAccelerators ?memory
        ?cpu ?requiresCompatibilities ?runtimePlatform ?compatibilities
        ?placementConstraints ?requiresAttributes ?status ?volumes ?revision
        ?networkMode ?executionRoleArn ?taskRoleArn ?family
        ?containerDefinitions ?taskDefinitionArn ()
    let of_json json =
      let ephemeralStorage =
        field_map json "ephemeralStorage" EphemeralStorage.of_json in
      let registeredBy = field_map json "registeredBy" String_.of_json in
      let deregisteredAt = field_map json "deregisteredAt" Timestamp.of_json in
      let registeredAt = field_map json "registeredAt" Timestamp.of_json in
      let proxyConfiguration =
        field_map json "proxyConfiguration" ProxyConfiguration.of_json in
      let ipcMode = field_map json "ipcMode" IpcMode.of_json in
      let pidMode = field_map json "pidMode" PidMode.of_json in
      let inferenceAccelerators =
        field_map json "inferenceAccelerators" InferenceAccelerators.of_json in
      let memory = field_map json "memory" String_.of_json in
      let cpu = field_map json "cpu" String_.of_json in
      let requiresCompatibilities =
        field_map json "requiresCompatibilities" CompatibilityList.of_json in
      let runtimePlatform =
        field_map json "runtimePlatform" RuntimePlatform.of_json in
      let compatibilities =
        field_map json "compatibilities" CompatibilityList.of_json in
      let placementConstraints =
        field_map json "placementConstraints"
          TaskDefinitionPlacementConstraints.of_json in
      let requiresAttributes =
        field_map json "requiresAttributes" RequiresAttributes.of_json in
      let status = field_map json "status" TaskDefinitionStatus.of_json in
      let volumes = field_map json "volumes" VolumeList.of_json in
      let revision = field_map json "revision" Integer.of_json in
      let networkMode = field_map json "networkMode" NetworkMode.of_json in
      let executionRoleArn =
        field_map json "executionRoleArn" String_.of_json in
      let taskRoleArn = field_map json "taskRoleArn" String_.of_json in
      let family = field_map json "family" String_.of_json in
      let containerDefinitions =
        field_map json "containerDefinitions" ContainerDefinitions.of_json in
      let taskDefinitionArn =
        field_map json "taskDefinitionArn" String_.of_json in
      make ?ephemeralStorage ?registeredBy ?deregisteredAt ?registeredAt
        ?proxyConfiguration ?ipcMode ?pidMode ?inferenceAccelerators ?memory
        ?cpu ?requiresCompatibilities ?runtimePlatform ?compatibilities
        ?placementConstraints ?requiresAttributes ?status ?volumes ?revision
        ?networkMode ?executionRoleArn ?taskRoleArn ?family
        ?containerDefinitions ?taskDefinitionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details of a task definition which describes the container and volume definitions of an Amazon Elastic Container Service task. You can specify which Docker images to use, the required resources, and other configurations related to launching the task definition through an Amazon ECS service or task."]
module PlatformDevices =
  struct
    type nonrec t = PlatformDevice.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PlatformDevice.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlatformDevice.of_xml)
    let of_json j =
      list_of_json ~kind:"PlatformDevices" ~of_json:PlatformDevice.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceInUseException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified resource is in-use and can't be removed."]
module AttributeLimitExceededException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You can apply up to 10 custom attributes for each resource. You can view the attributes of a resource with ListAttributes. You can remove existing attributes on a resource with DeleteAttributes."]
module TargetNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified target wasn't found. You can view your available container instances with ListContainerInstances. Amazon ECS container instances are cluster-specific and Region-specific."]
module DesiredStatus =
  struct
    type nonrec t =
      | RUNNING 
      | PENDING 
      | STOPPED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | RUNNING -> "RUNNING"
      | PENDING -> "PENDING"
      | STOPPED -> "STOPPED"
      | Non_static_id s -> s
    let of_string =
      function
      | "RUNNING" -> RUNNING
      | "PENDING" -> PENDING
      | "STOPPED" -> STOPPED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DesiredStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DesiredStatus" j)
    let to_json = simple_to_json to_value
  end
module SortOrder =
  struct
    type nonrec t =
      | ASC 
      | DESC 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | ASC -> "ASC" | DESC -> "DESC" | Non_static_id s -> s
    let of_string =
      function | "ASC" -> ASC | "DESC" -> DESC | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SortOrder" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SortOrder" j)
    let to_json = simple_to_json to_value
  end
module TaskDefinitionFamilyStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | INACTIVE 
      | ALL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | INACTIVE -> "INACTIVE"
      | ALL -> "ALL"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "INACTIVE" -> INACTIVE
      | "ALL" -> ALL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration TaskDefinitionFamilyStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"TaskDefinitionFamilyStatus" j)
    let to_json = simple_to_json to_value
  end
module Settings =
  struct
    type nonrec t = Setting.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Setting.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Setting.of_xml)
    let of_json j = list_of_json ~kind:"Settings" ~of_json:Setting.of_json j
    let to_json v = composed_to_json to_value v
  end
module Session =
  struct
    type nonrec t =
      {
      sessionId: String_.t option
        [@ocaml.doc "The ID of the execute command session."];
      streamUrl: String_.t option
        [@ocaml.doc
          "A URL to the managed agent on the container that the SSM Session Manager client uses to send commands and receive output from the container."];
      tokenValue: SensitiveString.t option
        [@ocaml.doc
          "An encrypted token value containing session and caller information. It's used to authenticate the connection to the container."]}
    let make ?sessionId =
      fun ?streamUrl ->
        fun ?tokenValue -> fun () -> { sessionId; streamUrl; tokenValue }
    let to_value x =
      structure_to_value
        [("sessionId", (Option.map x.sessionId ~f:String_.to_value));
        ("streamUrl", (Option.map x.streamUrl ~f:String_.to_value));
        ("tokenValue", (Option.map x.tokenValue ~f:SensitiveString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tokenValue =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "tokenValue") in
      let streamUrl =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "streamUrl") in
      let sessionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sessionId") in
      make ?tokenValue ?streamUrl ?sessionId ()
    let of_json json =
      let tokenValue = field_map json "tokenValue" SensitiveString.of_json in
      let streamUrl = field_map json "streamUrl" String_.of_json in
      let sessionId = field_map json "sessionId" String_.of_json in
      make ?tokenValue ?streamUrl ?sessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The details for the execute command session."]
module TargetNotConnectedException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The execute command cannot run. This error can be caused by any of the following configuration issues: Incorrect IAM permissions The SSM agent is not installed or is not running There is an interface Amazon VPC endpoint for Amazon ECS, but there is not one for for Systems Manager Session Manager For information about how to troubleshoot the issues, see Troubleshooting issues with ECS Exec in the Amazon Elastic Container Service Developer Guide."]
module TaskFieldList =
  struct
    type nonrec t = TaskField.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TaskField.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TaskField.of_xml)
    let of_json j =
      list_of_json ~kind:"TaskFieldList" ~of_json:TaskField.of_json j
    let to_json v = composed_to_json to_value v
  end
module TaskSetFieldList =
  struct
    type nonrec t = TaskSetField.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TaskSetField.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TaskSetField.of_xml)
    let of_json j =
      list_of_json ~kind:"TaskSetFieldList" ~of_json:TaskSetField.of_json j
    let to_json v = composed_to_json to_value v
  end
module TaskDefinitionFieldList =
  struct
    type nonrec t = TaskDefinitionField.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TaskDefinitionField.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TaskDefinitionField.of_xml)
    let of_json j =
      list_of_json ~kind:"TaskDefinitionFieldList"
        ~of_json:TaskDefinitionField.of_json j
    let to_json v = composed_to_json to_value v
  end
module Services =
  struct
    type nonrec t = Service.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Service.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Service.of_xml)
    let of_json j = list_of_json ~kind:"Services" ~of_json:Service.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServiceFieldList =
  struct
    type nonrec t = ServiceField.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ServiceField.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ServiceField.of_xml)
    let of_json j =
      list_of_json ~kind:"ServiceFieldList" ~of_json:ServiceField.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerInstanceFieldList =
  struct
    type nonrec t = ContainerInstanceField.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ContainerInstanceField.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContainerInstanceField.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerInstanceFieldList"
        ~of_json:ContainerInstanceField.of_json j
    let to_json v = composed_to_json to_value v
  end
module Clusters =
  struct
    type nonrec t = Cluster.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Cluster.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Cluster.of_xml)
    let of_json j = list_of_json ~kind:"Clusters" ~of_json:Cluster.of_json j
    let to_json v = composed_to_json to_value v
  end
module ClusterFieldList =
  struct
    type nonrec t = ClusterField.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ClusterField.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ClusterField.of_xml)
    let of_json j =
      list_of_json ~kind:"ClusterFieldList" ~of_json:ClusterField.of_json j
    let to_json v = composed_to_json to_value v
  end
module CapacityProviders =
  struct
    type nonrec t = CapacityProvider.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CapacityProvider.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CapacityProvider.of_xml)
    let of_json j =
      list_of_json ~kind:"CapacityProviders"
        ~of_json:CapacityProvider.of_json j
    let to_json v = composed_to_json to_value v
  end
module CapacityProviderFieldList =
  struct
    type nonrec t = CapacityProviderField.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CapacityProviderField.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CapacityProviderField.of_xml)
    let of_json j =
      list_of_json ~kind:"CapacityProviderFieldList"
        ~of_json:CapacityProviderField.of_json j
    let to_json v = composed_to_json to_value v
  end
module ClusterContainsContainerInstancesException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You can't delete a cluster that has registered container instances. First, deregister the container instances before you can delete the cluster. For more information, see DeregisterContainerInstance."]
module ClusterContainsServicesException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You can't delete a cluster that contains services. First, update the service to reduce its desired task count to 0, and then delete the service. For more information, see UpdateService and DeleteService."]
module ClusterContainsTasksException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "You can't delete a cluster that has active tasks."]
module LimitExceededException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The limit for the resource was exceeded."]
module UpdateTaskSetResponse =
  struct
    type nonrec t =
      {
      taskSet: TaskSet.t option [@ocaml.doc "Details about the task set."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `ServiceNotActiveException of ServiceNotActiveException.t 
      | `ServiceNotFoundException of ServiceNotFoundException.t 
      | `TaskSetNotFoundException of TaskSetNotFoundException.t 
      | `UnsupportedFeatureException of UnsupportedFeatureException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?taskSet = fun () -> { taskSet }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "ServiceNotActiveException" ->
          `ServiceNotActiveException (ServiceNotActiveException.of_json json)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_json json)
      | "TaskSetNotFoundException" ->
          `TaskSetNotFoundException (TaskSetNotFoundException.of_json json)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "ServiceNotActiveException" ->
          `ServiceNotActiveException (ServiceNotActiveException.of_xml xml)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_xml xml)
      | "TaskSetNotFoundException" ->
          `TaskSetNotFoundException (TaskSetNotFoundException.of_xml xml)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `ServiceNotActiveException e ->
          `Assoc
            [("error", (`String "ServiceNotActiveException"));
            ("details", (ServiceNotActiveException.to_json e))]
      | `ServiceNotFoundException e ->
          `Assoc
            [("error", (`String "ServiceNotFoundException"));
            ("details", (ServiceNotFoundException.to_json e))]
      | `TaskSetNotFoundException e ->
          `Assoc
            [("error", (`String "TaskSetNotFoundException"));
            ("details", (TaskSetNotFoundException.to_json e))]
      | `UnsupportedFeatureException e ->
          `Assoc
            [("error", (`String "UnsupportedFeatureException"));
            ("details", (UnsupportedFeatureException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("taskSet", (Option.map x.taskSet ~f:TaskSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskSet =
        (Option.map ~f:TaskSet.of_xml) (Xml.child xml_arg0 "taskSet") in
      make ?taskSet ()
    let of_json json =
      let taskSet = field_map json "taskSet" TaskSet.of_json in
      make ?taskSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a task set. This is used when a service uses the EXTERNAL deployment controller type. For more information, see Amazon ECS Deployment Types in the Amazon Elastic Container Service Developer Guide."]
module UpdateTaskSetRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service that the task set is found in."];
      service: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the service that the task set is found in."];
      taskSet: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the task set to update."];
      scale: Scale.t
        [@ocaml.doc
          "A floating-point percentage of the desired number of tasks to place and keep running in the task set."]}
    let context_ = "UpdateTaskSetRequest"
    let make ~cluster =
      fun ~service ->
        fun ~taskSet ->
          fun ~scale -> fun () -> { cluster; service; taskSet; scale }
    let to_value x =
      structure_to_value
        [("cluster", (Some (String_.to_value x.cluster)));
        ("service", (Some (String_.to_value x.service)));
        ("taskSet", (Some (String_.to_value x.taskSet)));
        ("scale", (Some (Scale.to_value x.scale)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scale =
        Scale.of_xml (Xml.child_exn ~context:context_ xml_arg0 "scale") in
      let taskSet =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "taskSet") in
      let service =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "service") in
      let cluster =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "cluster") in
      make ~scale ~taskSet ~service ~cluster ()
    let of_json json =
      let scale = field_map_exn json "scale" Scale.of_json in
      let taskSet = field_map_exn json "taskSet" String_.of_json in
      let service = field_map_exn json "service" String_.of_json in
      let cluster = field_map_exn json "cluster" String_.of_json in
      make ~scale ~taskSet ~service ~cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies a task set. This is used when a service uses the EXTERNAL deployment controller type. For more information, see Amazon ECS Deployment Types in the Amazon Elastic Container Service Developer Guide."]
module UpdateServiceResponse =
  struct
    type nonrec t =
      {
      service: Service.t option
        [@ocaml.doc
          "The full description of your service following the update call."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `PlatformTaskDefinitionIncompatibilityException of
          PlatformTaskDefinitionIncompatibilityException.t 
      | `PlatformUnknownException of PlatformUnknownException.t 
      | `ServerException of ServerException.t 
      | `ServiceNotActiveException of ServiceNotActiveException.t 
      | `ServiceNotFoundException of ServiceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?service = fun () -> { service }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "PlatformTaskDefinitionIncompatibilityException" ->
          `PlatformTaskDefinitionIncompatibilityException
            (PlatformTaskDefinitionIncompatibilityException.of_json json)
      | "PlatformUnknownException" ->
          `PlatformUnknownException (PlatformUnknownException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "ServiceNotActiveException" ->
          `ServiceNotActiveException (ServiceNotActiveException.of_json json)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "PlatformTaskDefinitionIncompatibilityException" ->
          `PlatformTaskDefinitionIncompatibilityException
            (PlatformTaskDefinitionIncompatibilityException.of_xml xml)
      | "PlatformUnknownException" ->
          `PlatformUnknownException (PlatformUnknownException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "ServiceNotActiveException" ->
          `ServiceNotActiveException (ServiceNotActiveException.of_xml xml)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `PlatformTaskDefinitionIncompatibilityException e ->
          `Assoc
            [("error",
               (`String "PlatformTaskDefinitionIncompatibilityException"));
            ("details",
              (PlatformTaskDefinitionIncompatibilityException.to_json e))]
      | `PlatformUnknownException e ->
          `Assoc
            [("error", (`String "PlatformUnknownException"));
            ("details", (PlatformUnknownException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `ServiceNotActiveException e ->
          `Assoc
            [("error", (`String "ServiceNotActiveException"));
            ("details", (ServiceNotActiveException.to_json e))]
      | `ServiceNotFoundException e ->
          `Assoc
            [("error", (`String "ServiceNotFoundException"));
            ("details", (ServiceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("service", (Option.map x.service ~f:Service.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let service =
        (Option.map ~f:Service.of_xml) (Xml.child xml_arg0 "service") in
      make ?service ()
    let of_json json =
      let service = field_map json "service" Service.of_json in
      make ?service ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updating the task placement strategies and constraints on an Amazon ECS service remains in preview and is a Beta Service as defined by and subject to the Beta Service Participation Service Terms located at https://aws.amazon.com/service-terms (\"Beta Terms\"). These Beta Terms apply to your participation in this preview. Modifies the parameters of a service. For services using the rolling update (ECS) you can update the desired count, deployment configuration, network configuration, load balancers, service registries, enable ECS managed tags option, propagate tags option, task placement constraints and strategies, and task definition. When you update any of these parameters, Amazon ECS starts new tasks with the new configuration. For services using the blue/green (CODE_DEPLOY) deployment controller, only the desired count, deployment configuration, health check grace period, task placement constraints and strategies, enable ECS managed tags option, and propagate tags can be updated using this API. If the network configuration, platform version, task definition, or load balancer need to be updated, create a new CodeDeploy deployment. For more information, see CreateDeployment in the CodeDeploy API Reference. For services using an external deployment controller, you can update only the desired count, task placement constraints and strategies, health check grace period, enable ECS managed tags option, and propagate tags option, using this API. If the launch type, load balancer, network configuration, platform version, or task definition need to be updated, create a new task set For more information, see CreateTaskSet. You can add to or subtract from the number of instantiations of a task definition in a service by specifying the cluster that the service is running in and a new desiredCount parameter. If you have updated the Docker image of your application, you can create a new task definition with that image and deploy it to your service. The service scheduler uses the minimum healthy percent and maximum percent parameters (in the service's deployment configuration) to determine the deployment strategy. If your updated Docker image uses the same tag as what is in the existing task definition for your service (for example, my_image:latest), you don't need to create a new revision of your task definition. You can update the service using the forceNewDeployment option. The new tasks launched by the deployment pull the current image/tag combination from your repository when they start. You can also update the deployment configuration of a service. When a deployment is triggered by updating the task definition of a service, the service scheduler uses the deployment configuration parameters, minimumHealthyPercent and maximumPercent, to determine the deployment strategy. If minimumHealthyPercent is below 100%, the scheduler can ignore desiredCount temporarily during a deployment. For example, if desiredCount is four tasks, a minimum of 50% allows the scheduler to stop two existing tasks before starting two new tasks. Tasks for services that don't use a load balancer are considered healthy if they're in the RUNNING state. Tasks for services that use a load balancer are considered healthy if they're in the RUNNING state and are reported as healthy by the load balancer. The maximumPercent parameter represents an upper limit on the number of running tasks during a deployment. You can use it to define the deployment batch size. For example, if desiredCount is four tasks, a maximum of 200% starts four new tasks before stopping the four older tasks (provided that the cluster resources required to do this are available). When UpdateService stops a task during a deployment, the equivalent of docker stop is issued to the containers running in the task. This results in a SIGTERM and a 30-second timeout. After this, SIGKILL is sent and the containers are forcibly stopped. If the container handles the SIGTERM gracefully and exits within 30 seconds from receiving it, no SIGKILL is sent. When the service scheduler launches new tasks, it determines task placement in your cluster with the following logic. Determine which of the container instances in your cluster can support your service's task definition. For example, they have the required CPU, memory, ports, and container instance attributes. By default, the service scheduler attempts to balance tasks across Availability Zones in this manner even though you can choose a different placement strategy. Sort the valid container instances by the fewest number of running tasks for this service in the same Availability Zone as the instance. For example, if zone A has one running service task and zones B and C each have zero, valid container instances in either zone B or C are considered optimal for placement. Place the new service task on a valid container instance in an optimal Availability Zone (based on the previous steps), favoring container instances with the fewest number of running tasks for this service. When the service scheduler stops running tasks, it attempts to maintain balance across the Availability Zones in your cluster using the following logic: Sort the container instances by the largest number of running tasks for this service in the same Availability Zone as the instance. For example, if zone A has one running service task and zones B and C each have two, container instances in either zone B or C are considered optimal for termination. Stop the task on a container instance in an optimal Availability Zone (based on the previous steps), favoring container instances with the largest number of running tasks for this service. You must have a service-linked role when you update any of the following service properties. If you specified a custom IAM role when you created the service, Amazon ECS automatically replaces the roleARN associated with the service with the ARN of your service-linked role. For more information, see Service-linked roles in the Amazon Elastic Container Service Developer Guide. loadBalancers, serviceRegistries"]
module UpdateServiceRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that your service runs on. If you do not specify a cluster, the default cluster is assumed."];
      service: String_.t [@ocaml.doc "The name of the service to update."];
      desiredCount: BoxedInteger.t option
        [@ocaml.doc
          "The number of instantiations of the task to place and keep running in your service."];
      taskDefinition: String_.t option
        [@ocaml.doc
          "The family and revision (family:revision) or full ARN of the task definition to run in your service. If a revision is not specified, the latest ACTIVE revision is used. If you modify the task definition with UpdateService, Amazon ECS spawns a task with the new version of the task definition and then stops an old task after the new version is running."];
      capacityProviderStrategy: CapacityProviderStrategy.t option
        [@ocaml.doc
          "The capacity provider strategy to update the service to use. if the service uses the default capacity provider strategy for the cluster, the service can be updated to use one or more capacity providers as opposed to the default capacity provider strategy. However, when a service is using a capacity provider strategy that's not the default capacity provider strategy, the service can't be updated to use the cluster's default capacity provider strategy. A capacity provider strategy consists of one or more capacity providers along with the base and weight to assign to them. A capacity provider must be associated with the cluster to be used in a capacity provider strategy. The PutClusterCapacityProviders API is used to associate a capacity provider with a cluster. Only capacity providers with an ACTIVE or UPDATING status can be used. If specifying a capacity provider that uses an Auto Scaling group, the capacity provider must already be created. New capacity providers can be created with the CreateCapacityProvider API operation. To use a Fargate capacity provider, specify either the FARGATE or FARGATE_SPOT capacity providers. The Fargate capacity providers are available to all accounts and only need to be associated with a cluster to be used. The PutClusterCapacityProviders API operation is used to update the list of available capacity providers for a cluster after the cluster is created."];
      deploymentConfiguration: DeploymentConfiguration.t option
        [@ocaml.doc
          "Optional deployment parameters that control how many tasks run during the deployment and the ordering of stopping and starting tasks."];
      networkConfiguration: NetworkConfiguration.t option
        [@ocaml.doc
          "An object representing the network configuration for the service."];
      placementConstraints: PlacementConstraints.t option
        [@ocaml.doc
          "An array of task placement constraint objects to update the service to use. If no value is specified, the existing placement constraints for the service will remain unchanged. If this value is specified, it will override any existing placement constraints defined for the service. To remove all existing placement constraints, specify an empty array. You can specify a maximum of 10 constraints for each task. This limit includes constraints in the task definition and those specified at runtime."];
      placementStrategy: PlacementStrategies.t option
        [@ocaml.doc
          "The task placement strategy objects to update the service to use. If no value is specified, the existing placement strategy for the service will remain unchanged. If this value is specified, it will override the existing placement strategy defined for the service. To remove an existing placement strategy, specify an empty object. You can specify a maximum of five strategy rules for each service."];
      platformVersion: String_.t option
        [@ocaml.doc
          "The platform version that your tasks in the service run on. A platform version is only specified for tasks using the Fargate launch type. If a platform version is not specified, the LATEST platform version is used. For more information, see Fargate Platform Versions in the Amazon Elastic Container Service Developer Guide."];
      forceNewDeployment: Boolean.t option
        [@ocaml.doc
          "Determines whether to force a new deployment of the service. By default, deployments aren't forced. You can use this option to start a new deployment with no service definition changes. For example, you can update a service's tasks to use a newer Docker image with the same image/tag combination (my_image:latest) or to roll Fargate tasks onto a newer platform version."];
      healthCheckGracePeriodSeconds: BoxedInteger.t option
        [@ocaml.doc
          "The period of time, in seconds, that the Amazon ECS service scheduler ignores unhealthy Elastic Load Balancing target health checks after a task has first started. This is only valid if your service is configured to use a load balancer. If your service's tasks take a while to start and respond to Elastic Load Balancing health checks, you can specify a health check grace period of up to 2,147,483,647 seconds. During that time, the Amazon ECS service scheduler ignores the Elastic Load Balancing health check status. This grace period can prevent the ECS service scheduler from marking tasks as unhealthy and stopping them before they have time to come up."];
      enableExecuteCommand: BoxedBoolean.t option
        [@ocaml.doc
          "If true, this enables execute command functionality on all task containers. If you do not want to override the value that was set when the service was created, you can set this to null when performing this action."];
      enableECSManagedTags: BoxedBoolean.t option
        [@ocaml.doc
          "Determines whether to turn on Amazon ECS managed tags for the tasks in the service. For more information, see Tagging Your Amazon ECS Resources in the Amazon Elastic Container Service Developer Guide. Only tasks launched after the update will reflect the update. To update the tags on all tasks, set forceNewDeployment to true, so that Amazon ECS starts new tasks with the updated tags."];
      loadBalancers: LoadBalancers.t option
        [@ocaml.doc
          "A list of Elastic Load Balancing load balancer objects. It contains the load balancer name, the container name, and the container port to access from the load balancer. The container name is as it appears in a container definition. When you add, update, or remove a load balancer configuration, Amazon ECS starts new tasks with the updated Elastic Load Balancing configuration, and then stops the old tasks when the new tasks are running. For services that use rolling updates, you can add, update, or remove Elastic Load Balancing target groups. You can update from a single target group to multiple target groups and from multiple target groups to a single target group. For services that use blue/green deployments, you can update Elastic Load Balancing target groups by using CreateDeployment through CodeDeploy. Note that multiple target groups are not supported for blue/green deployments. For more information see Register multiple target groups with a service in the Amazon Elastic Container Service Developer Guide. For services that use the external deployment controller, you can add, update, or remove load balancers by using CreateTaskSet. Note that multiple target groups are not supported for external deployments. For more information see Register multiple target groups with a service in the Amazon Elastic Container Service Developer Guide. You can remove existing loadBalancers by passing an empty list."];
      propagateTags: PropagateTags.t option
        [@ocaml.doc
          "Determines whether to propagate the tags from the task definition or the service to the task. If no value is specified, the tags aren't propagated. Only tasks launched after the update will reflect the update. To update the tags on all tasks, set forceNewDeployment to true, so that Amazon ECS starts new tasks with the updated tags."];
      serviceRegistries: ServiceRegistries.t option
        [@ocaml.doc
          "The details for the service discovery registries to assign to this service. For more information, see Service Discovery. When you add, update, or remove the service registries configuration, Amazon ECS starts new tasks with the updated service registries configuration, and then stops the old tasks when the new tasks are running. You can remove existing serviceRegistries by passing an empty list."]}
    let context_ = "UpdateServiceRequest"
    let make ?cluster =
      fun ?desiredCount ->
        fun ?taskDefinition ->
          fun ?capacityProviderStrategy ->
            fun ?deploymentConfiguration ->
              fun ?networkConfiguration ->
                fun ?placementConstraints ->
                  fun ?placementStrategy ->
                    fun ?platformVersion ->
                      fun ?forceNewDeployment ->
                        fun ?healthCheckGracePeriodSeconds ->
                          fun ?enableExecuteCommand ->
                            fun ?enableECSManagedTags ->
                              fun ?loadBalancers ->
                                fun ?propagateTags ->
                                  fun ?serviceRegistries ->
                                    fun ~service ->
                                      fun () ->
                                        {
                                          cluster;
                                          desiredCount;
                                          taskDefinition;
                                          capacityProviderStrategy;
                                          deploymentConfiguration;
                                          networkConfiguration;
                                          placementConstraints;
                                          placementStrategy;
                                          platformVersion;
                                          forceNewDeployment;
                                          healthCheckGracePeriodSeconds;
                                          enableExecuteCommand;
                                          enableECSManagedTags;
                                          loadBalancers;
                                          propagateTags;
                                          serviceRegistries;
                                          service
                                        }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("service", (Some (String_.to_value x.service)));
        ("desiredCount",
          (Option.map x.desiredCount ~f:BoxedInteger.to_value));
        ("taskDefinition", (Option.map x.taskDefinition ~f:String_.to_value));
        ("capacityProviderStrategy",
          (Option.map x.capacityProviderStrategy
             ~f:CapacityProviderStrategy.to_value));
        ("deploymentConfiguration",
          (Option.map x.deploymentConfiguration
             ~f:DeploymentConfiguration.to_value));
        ("networkConfiguration",
          (Option.map x.networkConfiguration ~f:NetworkConfiguration.to_value));
        ("placementConstraints",
          (Option.map x.placementConstraints ~f:PlacementConstraints.to_value));
        ("placementStrategy",
          (Option.map x.placementStrategy ~f:PlacementStrategies.to_value));
        ("platformVersion",
          (Option.map x.platformVersion ~f:String_.to_value));
        ("forceNewDeployment",
          (Option.map x.forceNewDeployment ~f:Boolean.to_value));
        ("healthCheckGracePeriodSeconds",
          (Option.map x.healthCheckGracePeriodSeconds
             ~f:BoxedInteger.to_value));
        ("enableExecuteCommand",
          (Option.map x.enableExecuteCommand ~f:BoxedBoolean.to_value));
        ("enableECSManagedTags",
          (Option.map x.enableECSManagedTags ~f:BoxedBoolean.to_value));
        ("loadBalancers",
          (Option.map x.loadBalancers ~f:LoadBalancers.to_value));
        ("propagateTags",
          (Option.map x.propagateTags ~f:PropagateTags.to_value));
        ("serviceRegistries",
          (Option.map x.serviceRegistries ~f:ServiceRegistries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceRegistries =
        (Option.map ~f:ServiceRegistries.of_xml)
          (Xml.child xml_arg0 "serviceRegistries") in
      let propagateTags =
        (Option.map ~f:PropagateTags.of_xml)
          (Xml.child xml_arg0 "propagateTags") in
      let loadBalancers =
        (Option.map ~f:LoadBalancers.of_xml)
          (Xml.child xml_arg0 "loadBalancers") in
      let enableECSManagedTags =
        (Option.map ~f:BoxedBoolean.of_xml)
          (Xml.child xml_arg0 "enableECSManagedTags") in
      let enableExecuteCommand =
        (Option.map ~f:BoxedBoolean.of_xml)
          (Xml.child xml_arg0 "enableExecuteCommand") in
      let healthCheckGracePeriodSeconds =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "healthCheckGracePeriodSeconds") in
      let forceNewDeployment =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "forceNewDeployment") in
      let platformVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platformVersion") in
      let placementStrategy =
        (Option.map ~f:PlacementStrategies.of_xml)
          (Xml.child xml_arg0 "placementStrategy") in
      let placementConstraints =
        (Option.map ~f:PlacementConstraints.of_xml)
          (Xml.child xml_arg0 "placementConstraints") in
      let networkConfiguration =
        (Option.map ~f:NetworkConfiguration.of_xml)
          (Xml.child xml_arg0 "networkConfiguration") in
      let deploymentConfiguration =
        (Option.map ~f:DeploymentConfiguration.of_xml)
          (Xml.child xml_arg0 "deploymentConfiguration") in
      let capacityProviderStrategy =
        (Option.map ~f:CapacityProviderStrategy.of_xml)
          (Xml.child xml_arg0 "capacityProviderStrategy") in
      let taskDefinition =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskDefinition") in
      let desiredCount =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "desiredCount") in
      let service =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "service") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?serviceRegistries ?propagateTags ?loadBalancers
        ?enableECSManagedTags ?enableExecuteCommand
        ?healthCheckGracePeriodSeconds ?forceNewDeployment ?platformVersion
        ?placementStrategy ?placementConstraints ?networkConfiguration
        ?deploymentConfiguration ?capacityProviderStrategy ?taskDefinition
        ?desiredCount ~service ?cluster ()
    let of_json json =
      let serviceRegistries =
        field_map json "serviceRegistries" ServiceRegistries.of_json in
      let propagateTags =
        field_map json "propagateTags" PropagateTags.of_json in
      let loadBalancers =
        field_map json "loadBalancers" LoadBalancers.of_json in
      let enableECSManagedTags =
        field_map json "enableECSManagedTags" BoxedBoolean.of_json in
      let enableExecuteCommand =
        field_map json "enableExecuteCommand" BoxedBoolean.of_json in
      let healthCheckGracePeriodSeconds =
        field_map json "healthCheckGracePeriodSeconds" BoxedInteger.of_json in
      let forceNewDeployment =
        field_map json "forceNewDeployment" Boolean.of_json in
      let platformVersion = field_map json "platformVersion" String_.of_json in
      let placementStrategy =
        field_map json "placementStrategy" PlacementStrategies.of_json in
      let placementConstraints =
        field_map json "placementConstraints" PlacementConstraints.of_json in
      let networkConfiguration =
        field_map json "networkConfiguration" NetworkConfiguration.of_json in
      let deploymentConfiguration =
        field_map json "deploymentConfiguration"
          DeploymentConfiguration.of_json in
      let capacityProviderStrategy =
        field_map json "capacityProviderStrategy"
          CapacityProviderStrategy.of_json in
      let taskDefinition = field_map json "taskDefinition" String_.of_json in
      let desiredCount = field_map json "desiredCount" BoxedInteger.of_json in
      let service = field_map_exn json "service" String_.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?serviceRegistries ?propagateTags ?loadBalancers
        ?enableECSManagedTags ?enableExecuteCommand
        ?healthCheckGracePeriodSeconds ?forceNewDeployment ?platformVersion
        ?placementStrategy ?placementConstraints ?networkConfiguration
        ?deploymentConfiguration ?capacityProviderStrategy ?taskDefinition
        ?desiredCount ~service ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updating the task placement strategies and constraints on an Amazon ECS service remains in preview and is a Beta Service as defined by and subject to the Beta Service Participation Service Terms located at https://aws.amazon.com/service-terms (\"Beta Terms\"). These Beta Terms apply to your participation in this preview. Modifies the parameters of a service. For services using the rolling update (ECS) you can update the desired count, deployment configuration, network configuration, load balancers, service registries, enable ECS managed tags option, propagate tags option, task placement constraints and strategies, and task definition. When you update any of these parameters, Amazon ECS starts new tasks with the new configuration. For services using the blue/green (CODE_DEPLOY) deployment controller, only the desired count, deployment configuration, health check grace period, task placement constraints and strategies, enable ECS managed tags option, and propagate tags can be updated using this API. If the network configuration, platform version, task definition, or load balancer need to be updated, create a new CodeDeploy deployment. For more information, see CreateDeployment in the CodeDeploy API Reference. For services using an external deployment controller, you can update only the desired count, task placement constraints and strategies, health check grace period, enable ECS managed tags option, and propagate tags option, using this API. If the launch type, load balancer, network configuration, platform version, or task definition need to be updated, create a new task set For more information, see CreateTaskSet. You can add to or subtract from the number of instantiations of a task definition in a service by specifying the cluster that the service is running in and a new desiredCount parameter. If you have updated the Docker image of your application, you can create a new task definition with that image and deploy it to your service. The service scheduler uses the minimum healthy percent and maximum percent parameters (in the service's deployment configuration) to determine the deployment strategy. If your updated Docker image uses the same tag as what is in the existing task definition for your service (for example, my_image:latest), you don't need to create a new revision of your task definition. You can update the service using the forceNewDeployment option. The new tasks launched by the deployment pull the current image/tag combination from your repository when they start. You can also update the deployment configuration of a service. When a deployment is triggered by updating the task definition of a service, the service scheduler uses the deployment configuration parameters, minimumHealthyPercent and maximumPercent, to determine the deployment strategy. If minimumHealthyPercent is below 100%, the scheduler can ignore desiredCount temporarily during a deployment. For example, if desiredCount is four tasks, a minimum of 50% allows the scheduler to stop two existing tasks before starting two new tasks. Tasks for services that don't use a load balancer are considered healthy if they're in the RUNNING state. Tasks for services that use a load balancer are considered healthy if they're in the RUNNING state and are reported as healthy by the load balancer. The maximumPercent parameter represents an upper limit on the number of running tasks during a deployment. You can use it to define the deployment batch size. For example, if desiredCount is four tasks, a maximum of 200% starts four new tasks before stopping the four older tasks (provided that the cluster resources required to do this are available). When UpdateService stops a task during a deployment, the equivalent of docker stop is issued to the containers running in the task. This results in a SIGTERM and a 30-second timeout. After this, SIGKILL is sent and the containers are forcibly stopped. If the container handles the SIGTERM gracefully and exits within 30 seconds from receiving it, no SIGKILL is sent. When the service scheduler launches new tasks, it determines task placement in your cluster with the following logic. Determine which of the container instances in your cluster can support your service's task definition. For example, they have the required CPU, memory, ports, and container instance attributes. By default, the service scheduler attempts to balance tasks across Availability Zones in this manner even though you can choose a different placement strategy. Sort the valid container instances by the fewest number of running tasks for this service in the same Availability Zone as the instance. For example, if zone A has one running service task and zones B and C each have zero, valid container instances in either zone B or C are considered optimal for placement. Place the new service task on a valid container instance in an optimal Availability Zone (based on the previous steps), favoring container instances with the fewest number of running tasks for this service. When the service scheduler stops running tasks, it attempts to maintain balance across the Availability Zones in your cluster using the following logic: Sort the container instances by the largest number of running tasks for this service in the same Availability Zone as the instance. For example, if zone A has one running service task and zones B and C each have two, container instances in either zone B or C are considered optimal for termination. Stop the task on a container instance in an optimal Availability Zone (based on the previous steps), favoring container instances with the largest number of running tasks for this service. You must have a service-linked role when you update any of the following service properties. If you specified a custom IAM role when you created the service, Amazon ECS automatically replaces the roleARN associated with the service with the ARN of your service-linked role. For more information, see Service-linked roles in the Amazon Elastic Container Service Developer Guide. loadBalancers, serviceRegistries"]
module UpdateServicePrimaryTaskSetResponse =
  struct
    type nonrec t =
      {
      taskSet: TaskSet.t option [@ocaml.doc "etails about the task set."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `ServiceNotActiveException of ServiceNotActiveException.t 
      | `ServiceNotFoundException of ServiceNotFoundException.t 
      | `TaskSetNotFoundException of TaskSetNotFoundException.t 
      | `UnsupportedFeatureException of UnsupportedFeatureException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?taskSet = fun () -> { taskSet }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "ServiceNotActiveException" ->
          `ServiceNotActiveException (ServiceNotActiveException.of_json json)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_json json)
      | "TaskSetNotFoundException" ->
          `TaskSetNotFoundException (TaskSetNotFoundException.of_json json)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "ServiceNotActiveException" ->
          `ServiceNotActiveException (ServiceNotActiveException.of_xml xml)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_xml xml)
      | "TaskSetNotFoundException" ->
          `TaskSetNotFoundException (TaskSetNotFoundException.of_xml xml)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `ServiceNotActiveException e ->
          `Assoc
            [("error", (`String "ServiceNotActiveException"));
            ("details", (ServiceNotActiveException.to_json e))]
      | `ServiceNotFoundException e ->
          `Assoc
            [("error", (`String "ServiceNotFoundException"));
            ("details", (ServiceNotFoundException.to_json e))]
      | `TaskSetNotFoundException e ->
          `Assoc
            [("error", (`String "TaskSetNotFoundException"));
            ("details", (TaskSetNotFoundException.to_json e))]
      | `UnsupportedFeatureException e ->
          `Assoc
            [("error", (`String "UnsupportedFeatureException"));
            ("details", (UnsupportedFeatureException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("taskSet", (Option.map x.taskSet ~f:TaskSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskSet =
        (Option.map ~f:TaskSet.of_xml) (Xml.child xml_arg0 "taskSet") in
      make ?taskSet ()
    let of_json json =
      let taskSet = field_map json "taskSet" TaskSet.of_json in
      make ?taskSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies which task set in a service is the primary task set. Any parameters that are updated on the primary task set in a service will transition to the service. This is used when a service uses the EXTERNAL deployment controller type. For more information, see Amazon ECS Deployment Types in the Amazon Elastic Container Service Developer Guide."]
module UpdateServicePrimaryTaskSetRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service that the task set exists in."];
      service: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the service that the task set exists in."];
      primaryTaskSet: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the task set to set as the primary task set in the deployment."]}
    let context_ = "UpdateServicePrimaryTaskSetRequest"
    let make ~cluster =
      fun ~service ->
        fun ~primaryTaskSet -> fun () -> { cluster; service; primaryTaskSet }
    let to_value x =
      structure_to_value
        [("cluster", (Some (String_.to_value x.cluster)));
        ("service", (Some (String_.to_value x.service)));
        ("primaryTaskSet", (Some (String_.to_value x.primaryTaskSet)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let primaryTaskSet =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "primaryTaskSet") in
      let service =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "service") in
      let cluster =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "cluster") in
      make ~primaryTaskSet ~service ~cluster ()
    let of_json json =
      let primaryTaskSet =
        field_map_exn json "primaryTaskSet" String_.of_json in
      let service = field_map_exn json "service" String_.of_json in
      let cluster = field_map_exn json "cluster" String_.of_json in
      make ~primaryTaskSet ~service ~cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies which task set in a service is the primary task set. Any parameters that are updated on the primary task set in a service will transition to the service. This is used when a service uses the EXTERNAL deployment controller type. For more information, see Amazon ECS Deployment Types in the Amazon Elastic Container Service Developer Guide."]
module UpdateContainerInstancesStateResponse =
  struct
    type nonrec t =
      {
      containerInstances: ContainerInstances.t option
        [@ocaml.doc "The list of container instances."];
      failures: Failures.t option
        [@ocaml.doc "Any failures associated with the call."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerInstances =
      fun ?failures -> fun () -> { containerInstances; failures }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("containerInstances",
           (Option.map x.containerInstances ~f:ContainerInstances.to_value));
        ("failures", (Option.map x.failures ~f:Failures.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failures =
        (Option.map ~f:Failures.of_xml) (Xml.child xml_arg0 "failures") in
      let containerInstances =
        (Option.map ~f:ContainerInstances.of_xml)
          (Xml.child xml_arg0 "containerInstances") in
      make ?failures ?containerInstances ()
    let of_json json =
      let failures = field_map json "failures" Failures.of_json in
      let containerInstances =
        field_map json "containerInstances" ContainerInstances.of_json in
      make ?failures ?containerInstances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the status of an Amazon ECS container instance. Once a container instance has reached an ACTIVE state, you can change the status of a container instance to DRAINING to manually remove an instance from a cluster, for example to perform system updates, update the Docker daemon, or scale down the cluster size. A container instance can't be changed to DRAINING until it has reached an ACTIVE status. If the instance is in any other status, an error will be received. When you set a container instance to DRAINING, Amazon ECS prevents new tasks from being scheduled for placement on the container instance and replacement service tasks are started on other container instances in the cluster if the resources are available. Service tasks on the container instance that are in the PENDING state are stopped immediately. Service tasks on the container instance that are in the RUNNING state are stopped and replaced according to the service's deployment configuration parameters, minimumHealthyPercent and maximumPercent. You can change the deployment configuration of your service using UpdateService. If minimumHealthyPercent is below 100%, the scheduler can ignore desiredCount temporarily during task replacement. For example, desiredCount is four tasks, a minimum of 50% allows the scheduler to stop two existing tasks before starting two new tasks. If the minimum is 100%, the service scheduler can't remove existing tasks until the replacement tasks are considered healthy. Tasks for services that do not use a load balancer are considered healthy if they're in the RUNNING state. Tasks for services that use a load balancer are considered healthy if they're in the RUNNING state and are reported as healthy by the load balancer. The maximumPercent parameter represents an upper limit on the number of running tasks during task replacement. You can use this to define the replacement batch size. For example, if desiredCount is four tasks, a maximum of 200% starts four new tasks before stopping the four tasks to be drained, provided that the cluster resources required to do this are available. If the maximum is 100%, then replacement tasks can't start until the draining tasks have stopped. Any PENDING or RUNNING tasks that do not belong to a service aren't affected. You must wait for them to finish or stop them manually. A container instance has completed draining when it has no more RUNNING tasks. You can verify this using ListTasks. When a container instance has been drained, you can set a container instance to ACTIVE status and once it has reached that status the Amazon ECS scheduler can begin scheduling tasks on the instance again."]
module UpdateContainerInstancesStateRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that hosts the container instance to update. If you do not specify a cluster, the default cluster is assumed."];
      containerInstances: StringList.t
        [@ocaml.doc
          "A list of up to 10 container instance IDs or full ARN entries."];
      status: ContainerInstanceStatus.t
        [@ocaml.doc
          "The container instance state to update the container instance with. The only valid values for this action are ACTIVE and DRAINING. A container instance can only be updated to DRAINING status once it has reached an ACTIVE state. If a container instance is in REGISTERING, DEREGISTERING, or REGISTRATION_FAILED state you can describe the container instance but can't update the container instance state."]}
    let context_ = "UpdateContainerInstancesStateRequest"
    let make ?cluster =
      fun ~containerInstances ->
        fun ~status -> fun () -> { cluster; containerInstances; status }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("containerInstances",
          (Some (StringList.to_value x.containerInstances)));
        ("status", (Some (ContainerInstanceStatus.to_value x.status)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        ContainerInstanceStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "status") in
      let containerInstances =
        StringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "containerInstances") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ~status ~containerInstances ?cluster ()
    let of_json json =
      let status =
        field_map_exn json "status" ContainerInstanceStatus.of_json in
      let containerInstances =
        field_map_exn json "containerInstances" StringList.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ~status ~containerInstances ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the status of an Amazon ECS container instance. Once a container instance has reached an ACTIVE state, you can change the status of a container instance to DRAINING to manually remove an instance from a cluster, for example to perform system updates, update the Docker daemon, or scale down the cluster size. A container instance can't be changed to DRAINING until it has reached an ACTIVE status. If the instance is in any other status, an error will be received. When you set a container instance to DRAINING, Amazon ECS prevents new tasks from being scheduled for placement on the container instance and replacement service tasks are started on other container instances in the cluster if the resources are available. Service tasks on the container instance that are in the PENDING state are stopped immediately. Service tasks on the container instance that are in the RUNNING state are stopped and replaced according to the service's deployment configuration parameters, minimumHealthyPercent and maximumPercent. You can change the deployment configuration of your service using UpdateService. If minimumHealthyPercent is below 100%, the scheduler can ignore desiredCount temporarily during task replacement. For example, desiredCount is four tasks, a minimum of 50% allows the scheduler to stop two existing tasks before starting two new tasks. If the minimum is 100%, the service scheduler can't remove existing tasks until the replacement tasks are considered healthy. Tasks for services that do not use a load balancer are considered healthy if they're in the RUNNING state. Tasks for services that use a load balancer are considered healthy if they're in the RUNNING state and are reported as healthy by the load balancer. The maximumPercent parameter represents an upper limit on the number of running tasks during task replacement. You can use this to define the replacement batch size. For example, if desiredCount is four tasks, a maximum of 200% starts four new tasks before stopping the four tasks to be drained, provided that the cluster resources required to do this are available. If the maximum is 100%, then replacement tasks can't start until the draining tasks have stopped. Any PENDING or RUNNING tasks that do not belong to a service aren't affected. You must wait for them to finish or stop them manually. A container instance has completed draining when it has no more RUNNING tasks. You can verify this using ListTasks. When a container instance has been drained, you can set a container instance to ACTIVE status and once it has reached that status the Amazon ECS scheduler can begin scheduling tasks on the instance again."]
module UpdateContainerAgentResponse =
  struct
    type nonrec t =
      {
      containerInstance: ContainerInstance.t option
        [@ocaml.doc
          "The container instance that the container agent was updated for."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `MissingVersionException of MissingVersionException.t 
      | `NoUpdateAvailableException of NoUpdateAvailableException.t 
      | `ServerException of ServerException.t 
      | `UpdateInProgressException of UpdateInProgressException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerInstance = fun () -> { containerInstance }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "MissingVersionException" ->
          `MissingVersionException (MissingVersionException.of_json json)
      | "NoUpdateAvailableException" ->
          `NoUpdateAvailableException
            (NoUpdateAvailableException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "UpdateInProgressException" ->
          `UpdateInProgressException (UpdateInProgressException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "MissingVersionException" ->
          `MissingVersionException (MissingVersionException.of_xml xml)
      | "NoUpdateAvailableException" ->
          `NoUpdateAvailableException (NoUpdateAvailableException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "UpdateInProgressException" ->
          `UpdateInProgressException (UpdateInProgressException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `MissingVersionException e ->
          `Assoc
            [("error", (`String "MissingVersionException"));
            ("details", (MissingVersionException.to_json e))]
      | `NoUpdateAvailableException e ->
          `Assoc
            [("error", (`String "NoUpdateAvailableException"));
            ("details", (NoUpdateAvailableException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `UpdateInProgressException e ->
          `Assoc
            [("error", (`String "UpdateInProgressException"));
            ("details", (UpdateInProgressException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("containerInstance",
           (Option.map x.containerInstance ~f:ContainerInstance.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerInstance =
        (Option.map ~f:ContainerInstance.of_xml)
          (Xml.child xml_arg0 "containerInstance") in
      make ?containerInstance ()
    let of_json json =
      let containerInstance =
        field_map json "containerInstance" ContainerInstance.of_json in
      make ?containerInstance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the Amazon ECS container agent on a specified container instance. Updating the Amazon ECS container agent doesn't interrupt running tasks or services on the container instance. The process for updating the agent differs depending on whether your container instance was launched with the Amazon ECS-optimized AMI or another operating system. The UpdateContainerAgent API isn't supported for container instances using the Amazon ECS-optimized Amazon Linux 2 (arm64) AMI. To update the container agent, you can update the ecs-init package. This updates the agent. For more information, see Updating the Amazon ECS container agent in the Amazon Elastic Container Service Developer Guide. The UpdateContainerAgent API requires an Amazon ECS-optimized AMI or Amazon Linux AMI with the ecs-init service installed and running. For help updating the Amazon ECS container agent on other operating systems, see Manually updating the Amazon ECS container agent in the Amazon Elastic Container Service Developer Guide."]
module UpdateContainerAgentRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that your container instance is running on. If you do not specify a cluster, the default cluster is assumed."];
      containerInstance: String_.t
        [@ocaml.doc
          "The container instance ID or full ARN entries for the container instance where you would like to update the Amazon ECS container agent."]}
    let context_ = "UpdateContainerAgentRequest"
    let make ?cluster =
      fun ~containerInstance -> fun () -> { cluster; containerInstance }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("containerInstance", (Some (String_.to_value x.containerInstance)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerInstance =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "containerInstance") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ~containerInstance ?cluster ()
    let of_json json =
      let containerInstance =
        field_map_exn json "containerInstance" String_.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ~containerInstance ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the Amazon ECS container agent on a specified container instance. Updating the Amazon ECS container agent doesn't interrupt running tasks or services on the container instance. The process for updating the agent differs depending on whether your container instance was launched with the Amazon ECS-optimized AMI or another operating system. The UpdateContainerAgent API isn't supported for container instances using the Amazon ECS-optimized Amazon Linux 2 (arm64) AMI. To update the container agent, you can update the ecs-init package. This updates the agent. For more information, see Updating the Amazon ECS container agent in the Amazon Elastic Container Service Developer Guide. The UpdateContainerAgent API requires an Amazon ECS-optimized AMI or Amazon Linux AMI with the ecs-init service installed and running. For help updating the Amazon ECS container agent on other operating systems, see Manually updating the Amazon ECS container agent in the Amazon Elastic Container Service Developer Guide."]
module UpdateClusterSettingsResponse =
  struct
    type nonrec t =
      {
      cluster: Cluster.t option [@ocaml.doc "Details about the cluster"]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?cluster = fun () -> { cluster }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:Cluster.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cluster =
        (Option.map ~f:Cluster.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?cluster ()
    let of_json json =
      let cluster = field_map json "cluster" Cluster.of_json in
      make ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies the settings to use for a cluster."]
module UpdateClusterSettingsRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t
        [@ocaml.doc "The name of the cluster to modify the settings for."];
      settings: ClusterSettings.t
        [@ocaml.doc
          "The setting to use by default for a cluster. This parameter is used to turn on CloudWatch Container Insights for a cluster. If this value is specified, it overrides the containerInsights value set with PutAccountSetting or PutAccountSettingDefault."]}
    let context_ = "UpdateClusterSettingsRequest"
    let make ~cluster = fun ~settings -> fun () -> { cluster; settings }
    let to_value x =
      structure_to_value
        [("cluster", (Some (String_.to_value x.cluster)));
        ("settings", (Some (ClusterSettings.to_value x.settings)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let settings =
        ClusterSettings.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "settings") in
      let cluster =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "cluster") in
      make ~settings ~cluster ()
    let of_json json =
      let settings = field_map_exn json "settings" ClusterSettings.of_json in
      let cluster = field_map_exn json "cluster" String_.of_json in
      make ~settings ~cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies the settings to use for a cluster."]
module UpdateClusterResponse =
  struct
    type nonrec t =
      {
      cluster: Cluster.t option [@ocaml.doc "Details about the cluster."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?cluster = fun () -> { cluster }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:Cluster.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cluster =
        (Option.map ~f:Cluster.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?cluster ()
    let of_json json =
      let cluster = field_map json "cluster" Cluster.of_json in
      make ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the cluster."]
module UpdateClusterRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t
        [@ocaml.doc "The name of the cluster to modify the settings for."];
      settings: ClusterSettings.t option
        [@ocaml.doc "The cluster settings for your cluster."];
      configuration: ClusterConfiguration.t option
        [@ocaml.doc "The execute command configuration for the cluster."]}
    let context_ = "UpdateClusterRequest"
    let make ?settings =
      fun ?configuration ->
        fun ~cluster -> fun () -> { settings; configuration; cluster }
    let to_value x =
      structure_to_value
        [("cluster", (Some (String_.to_value x.cluster)));
        ("settings", (Option.map x.settings ~f:ClusterSettings.to_value));
        ("configuration",
          (Option.map x.configuration ~f:ClusterConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuration =
        (Option.map ~f:ClusterConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      let settings =
        (Option.map ~f:ClusterSettings.of_xml)
          (Xml.child xml_arg0 "settings") in
      let cluster =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "cluster") in
      make ?configuration ?settings ~cluster ()
    let of_json json =
      let configuration =
        field_map json "configuration" ClusterConfiguration.of_json in
      let settings = field_map json "settings" ClusterSettings.of_json in
      let cluster = field_map_exn json "cluster" String_.of_json in
      make ?configuration ?settings ~cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the cluster."]
module UpdateCapacityProviderResponse =
  struct
    type nonrec t =
      {
      capacityProvider: CapacityProvider.t option
        [@ocaml.doc "Details about the capacity provider."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?capacityProvider = fun () -> { capacityProvider }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityProvider",
           (Option.map x.capacityProvider ~f:CapacityProvider.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityProvider =
        (Option.map ~f:CapacityProvider.of_xml)
          (Xml.child xml_arg0 "capacityProvider") in
      make ?capacityProvider ()
    let of_json json =
      let capacityProvider =
        field_map json "capacityProvider" CapacityProvider.of_json in
      make ?capacityProvider ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies the parameters for a capacity provider."]
module UpdateCapacityProviderRequest =
  struct
    type nonrec t =
      {
      name: String_.t
        [@ocaml.doc "The name of the capacity provider to update."];
      autoScalingGroupProvider: AutoScalingGroupProviderUpdate.t
        [@ocaml.doc
          "An object that represent the parameters to update for the Auto Scaling group capacity provider."]}
    let context_ = "UpdateCapacityProviderRequest"
    let make ~name =
      fun ~autoScalingGroupProvider ->
        fun () -> { name; autoScalingGroupProvider }
    let to_value x =
      structure_to_value
        [("name", (Some (String_.to_value x.name)));
        ("autoScalingGroupProvider",
          (Some
             (AutoScalingGroupProviderUpdate.to_value
                x.autoScalingGroupProvider)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoScalingGroupProvider =
        AutoScalingGroupProviderUpdate.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "autoScalingGroupProvider") in
      let name =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~autoScalingGroupProvider ~name ()
    let of_json json =
      let autoScalingGroupProvider =
        field_map_exn json "autoScalingGroupProvider"
          AutoScalingGroupProviderUpdate.of_json in
      let name = field_map_exn json "name" String_.of_json in
      make ~autoScalingGroupProvider ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies the parameters for a capacity provider."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes specified tags from a resource."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: String_.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the resource to delete tags from. Currently, the supported resources are Amazon ECS capacity providers, tasks, services, task definitions, clusters, and container instances."];
      tagKeys: TagKeys.t [@ocaml.doc "The keys of the tags to be removed."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (String_.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeys.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeys.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_json json =
      let tagKeys = field_map_exn json "tagKeys" TagKeys.of_json in
      let resourceArn = field_map_exn json "resourceArn" String_.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes specified tags from a resource."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates the specified tags to a resource with the specified resourceArn. If existing tags on a resource aren't specified in the request parameters, they aren't changed. When a resource is deleted, the tags that are associated with that resource are deleted as well."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: String_.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the resource to add tags to. Currently, the supported resources are Amazon ECS capacity providers, tasks, services, task definitions, clusters, and container instances."];
      tags: Tags.t
        [@ocaml.doc
          "The tags to add to the resource. A tag is an array of key-value pairs. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (String_.to_value x.resourceArn)));
        ("tags", (Some (Tags.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        Tags.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_json json =
      let tags = field_map_exn json "tags" Tags.of_json in
      let resourceArn = field_map_exn json "resourceArn" String_.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates the specified tags to a resource with the specified resourceArn. If existing tags on a resource aren't specified in the request parameters, they aren't changed. When a resource is deleted, the tags that are associated with that resource are deleted as well."]
module SubmitTaskStateChangeResponse =
  struct
    type nonrec t =
      {
      acknowledgment: String_.t option
        [@ocaml.doc "Acknowledgement of the state change."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?acknowledgment = fun () -> { acknowledgment }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("acknowledgment",
           (Option.map x.acknowledgment ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acknowledgment =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "acknowledgment") in
      make ?acknowledgment ()
    let of_json json =
      let acknowledgment = field_map json "acknowledgment" String_.of_json in
      make ?acknowledgment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is only used by the Amazon ECS agent, and it is not intended for use outside of the agent. Sent to acknowledge that a task changed states."]
module SubmitTaskStateChangeRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that hosts the task."];
      task: String_.t option
        [@ocaml.doc
          "The task ID or full ARN of the task in the state change request."];
      status: String_.t option
        [@ocaml.doc "The status of the state change request."];
      reason: String_.t option
        [@ocaml.doc "The reason for the state change request."];
      containers: ContainerStateChanges.t option
        [@ocaml.doc
          "Any containers that's associated with the state change request."];
      attachments: AttachmentStateChanges.t option
        [@ocaml.doc
          "Any attachments associated with the state change request."];
      managedAgents: ManagedAgentStateChanges.t option
        [@ocaml.doc
          "The details for the managed agent that's associated with the task."];
      pullStartedAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the container image pull started."];
      pullStoppedAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the container image pull completed."];
      executionStoppedAt: Timestamp.t option
        [@ocaml.doc
          "The Unix timestamp for the time when the task execution stopped."]}
    let make ?cluster =
      fun ?task ->
        fun ?status ->
          fun ?reason ->
            fun ?containers ->
              fun ?attachments ->
                fun ?managedAgents ->
                  fun ?pullStartedAt ->
                    fun ?pullStoppedAt ->
                      fun ?executionStoppedAt ->
                        fun () ->
                          {
                            cluster;
                            task;
                            status;
                            reason;
                            containers;
                            attachments;
                            managedAgents;
                            pullStartedAt;
                            pullStoppedAt;
                            executionStoppedAt
                          }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("task", (Option.map x.task ~f:String_.to_value));
        ("status", (Option.map x.status ~f:String_.to_value));
        ("reason", (Option.map x.reason ~f:String_.to_value));
        ("containers",
          (Option.map x.containers ~f:ContainerStateChanges.to_value));
        ("attachments",
          (Option.map x.attachments ~f:AttachmentStateChanges.to_value));
        ("managedAgents",
          (Option.map x.managedAgents ~f:ManagedAgentStateChanges.to_value));
        ("pullStartedAt", (Option.map x.pullStartedAt ~f:Timestamp.to_value));
        ("pullStoppedAt", (Option.map x.pullStoppedAt ~f:Timestamp.to_value));
        ("executionStoppedAt",
          (Option.map x.executionStoppedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let executionStoppedAt =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "executionStoppedAt") in
      let pullStoppedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "pullStoppedAt") in
      let pullStartedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "pullStartedAt") in
      let managedAgents =
        (Option.map ~f:ManagedAgentStateChanges.of_xml)
          (Xml.child xml_arg0 "managedAgents") in
      let attachments =
        (Option.map ~f:AttachmentStateChanges.of_xml)
          (Xml.child xml_arg0 "attachments") in
      let containers =
        (Option.map ~f:ContainerStateChanges.of_xml)
          (Xml.child xml_arg0 "containers") in
      let reason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "reason") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let task = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "task") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?executionStoppedAt ?pullStoppedAt ?pullStartedAt ?managedAgents
        ?attachments ?containers ?reason ?status ?task ?cluster ()
    let of_json json =
      let executionStoppedAt =
        field_map json "executionStoppedAt" Timestamp.of_json in
      let pullStoppedAt = field_map json "pullStoppedAt" Timestamp.of_json in
      let pullStartedAt = field_map json "pullStartedAt" Timestamp.of_json in
      let managedAgents =
        field_map json "managedAgents" ManagedAgentStateChanges.of_json in
      let attachments =
        field_map json "attachments" AttachmentStateChanges.of_json in
      let containers =
        field_map json "containers" ContainerStateChanges.of_json in
      let reason = field_map json "reason" String_.of_json in
      let status = field_map json "status" String_.of_json in
      let task = field_map json "task" String_.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?executionStoppedAt ?pullStoppedAt ?pullStartedAt ?managedAgents
        ?attachments ?containers ?reason ?status ?task ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is only used by the Amazon ECS agent, and it is not intended for use outside of the agent. Sent to acknowledge that a task changed states."]
module SubmitContainerStateChangeResponse =
  struct
    type nonrec t =
      {
      acknowledgment: String_.t option
        [@ocaml.doc "Acknowledgement of the state change."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ClientException of ClientException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?acknowledgment = fun () -> { acknowledgment }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("acknowledgment",
           (Option.map x.acknowledgment ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acknowledgment =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "acknowledgment") in
      make ?acknowledgment ()
    let of_json json =
      let acknowledgment = field_map json "acknowledgment" String_.of_json in
      make ?acknowledgment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is only used by the Amazon ECS agent, and it is not intended for use outside of the agent. Sent to acknowledge that a container changed states."]
module SubmitContainerStateChangeRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full ARN of the cluster that hosts the container."];
      task: String_.t option
        [@ocaml.doc
          "The task ID or full Amazon Resource Name (ARN) of the task that hosts the container."];
      containerName: String_.t option
        [@ocaml.doc "The name of the container."];
      runtimeId: String_.t option
        [@ocaml.doc "The ID of the Docker container."];
      status: String_.t option
        [@ocaml.doc "The status of the state change request."];
      exitCode: BoxedInteger.t option
        [@ocaml.doc
          "The exit code that's returned for the state change request."];
      reason: String_.t option
        [@ocaml.doc "The reason for the state change request."];
      networkBindings: NetworkBindings.t option
        [@ocaml.doc "The network bindings of the container."]}
    let make ?cluster =
      fun ?task ->
        fun ?containerName ->
          fun ?runtimeId ->
            fun ?status ->
              fun ?exitCode ->
                fun ?reason ->
                  fun ?networkBindings ->
                    fun () ->
                      {
                        cluster;
                        task;
                        containerName;
                        runtimeId;
                        status;
                        exitCode;
                        reason;
                        networkBindings
                      }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("task", (Option.map x.task ~f:String_.to_value));
        ("containerName", (Option.map x.containerName ~f:String_.to_value));
        ("runtimeId", (Option.map x.runtimeId ~f:String_.to_value));
        ("status", (Option.map x.status ~f:String_.to_value));
        ("exitCode", (Option.map x.exitCode ~f:BoxedInteger.to_value));
        ("reason", (Option.map x.reason ~f:String_.to_value));
        ("networkBindings",
          (Option.map x.networkBindings ~f:NetworkBindings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkBindings =
        (Option.map ~f:NetworkBindings.of_xml)
          (Xml.child xml_arg0 "networkBindings") in
      let reason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "reason") in
      let exitCode =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "exitCode") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      let runtimeId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "runtimeId") in
      let containerName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "containerName") in
      let task = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "task") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?networkBindings ?reason ?exitCode ?status ?runtimeId
        ?containerName ?task ?cluster ()
    let of_json json =
      let networkBindings =
        field_map json "networkBindings" NetworkBindings.of_json in
      let reason = field_map json "reason" String_.of_json in
      let exitCode = field_map json "exitCode" BoxedInteger.of_json in
      let status = field_map json "status" String_.of_json in
      let runtimeId = field_map json "runtimeId" String_.of_json in
      let containerName = field_map json "containerName" String_.of_json in
      let task = field_map json "task" String_.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?networkBindings ?reason ?exitCode ?status ?runtimeId
        ?containerName ?task ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is only used by the Amazon ECS agent, and it is not intended for use outside of the agent. Sent to acknowledge that a container changed states."]
module SubmitAttachmentStateChangesResponse =
  struct
    type nonrec t =
      {
      acknowledgment: String_.t option
        [@ocaml.doc "Acknowledgement of the state change."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?acknowledgment = fun () -> { acknowledgment }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("acknowledgment",
           (Option.map x.acknowledgment ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acknowledgment =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "acknowledgment") in
      make ?acknowledgment ()
    let of_json json =
      let acknowledgment = field_map json "acknowledgment" String_.of_json in
      make ?acknowledgment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is only used by the Amazon ECS agent, and it is not intended for use outside of the agent. Sent to acknowledge that an attachment changed states."]
module SubmitAttachmentStateChangesRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full ARN of the cluster that hosts the container instance the attachment belongs to."];
      attachments: AttachmentStateChanges.t
        [@ocaml.doc
          "Any attachments associated with the state change request."]}
    let context_ = "SubmitAttachmentStateChangesRequest"
    let make ?cluster =
      fun ~attachments -> fun () -> { cluster; attachments }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("attachments",
          (Some (AttachmentStateChanges.to_value x.attachments)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attachments =
        AttachmentStateChanges.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "attachments") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ~attachments ?cluster ()
    let of_json json =
      let attachments =
        field_map_exn json "attachments" AttachmentStateChanges.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ~attachments ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is only used by the Amazon ECS agent, and it is not intended for use outside of the agent. Sent to acknowledge that an attachment changed states."]
module StopTaskResponse =
  struct
    type nonrec t =
      {
      task: Task.t option [@ocaml.doc "The task that was stopped."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?task = fun () -> { task }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("task", (Option.map x.task ~f:Task.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let task = (Option.map ~f:Task.of_xml) (Xml.child xml_arg0 "task") in
      make ?task ()
    let of_json json =
      let task = field_map json "task" Task.of_json in make ?task ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stops a running task. Any tags associated with the task will be deleted. When StopTask is called on a task, the equivalent of docker stop is issued to the containers running in the task. This results in a SIGTERM value and a default 30-second timeout, after which the SIGKILL value is sent and the containers are forcibly stopped. If the container handles the SIGTERM value gracefully and exits within 30 seconds from receiving it, no SIGKILL value is sent. The default 30-second timeout can be configured on the Amazon ECS container agent with the ECS_CONTAINER_STOP_TIMEOUT variable. For more information, see Amazon ECS Container Agent Configuration in the Amazon Elastic Container Service Developer Guide."]
module StopTaskRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that hosts the task to stop. If you do not specify a cluster, the default cluster is assumed."];
      task: String_.t
        [@ocaml.doc
          "The task ID or full Amazon Resource Name (ARN) of the task to stop."];
      reason: String_.t option
        [@ocaml.doc
          "An optional message specified when a task is stopped. For example, if you're using a custom scheduler, you can use this parameter to specify the reason for stopping the task here, and the message appears in subsequent DescribeTasks API operations on this task. Up to 255 characters are allowed in this message."]}
    let context_ = "StopTaskRequest"
    let make ?cluster =
      fun ?reason -> fun ~task -> fun () -> { cluster; reason; task }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("task", (Some (String_.to_value x.task)));
        ("reason", (Option.map x.reason ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "reason") in
      let task =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "task") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?reason ~task ?cluster ()
    let of_json json =
      let reason = field_map json "reason" String_.of_json in
      let task = field_map_exn json "task" String_.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?reason ~task ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stops a running task. Any tags associated with the task will be deleted. When StopTask is called on a task, the equivalent of docker stop is issued to the containers running in the task. This results in a SIGTERM value and a default 30-second timeout, after which the SIGKILL value is sent and the containers are forcibly stopped. If the container handles the SIGTERM value gracefully and exits within 30 seconds from receiving it, no SIGKILL value is sent. The default 30-second timeout can be configured on the Amazon ECS container agent with the ECS_CONTAINER_STOP_TIMEOUT variable. For more information, see Amazon ECS Container Agent Configuration in the Amazon Elastic Container Service Developer Guide."]
module StartTaskResponse =
  struct
    type nonrec t =
      {
      tasks: Tasks.t option
        [@ocaml.doc
          "A full description of the tasks that were started. Each task that was successfully placed on your container instances is described."];
      failures: Failures.t option
        [@ocaml.doc "Any failures associated with the call."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tasks = fun ?failures -> fun () -> { tasks; failures }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("tasks", (Option.map x.tasks ~f:Tasks.to_value));
        ("failures", (Option.map x.failures ~f:Failures.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failures =
        (Option.map ~f:Failures.of_xml) (Xml.child xml_arg0 "failures") in
      let tasks = (Option.map ~f:Tasks.of_xml) (Xml.child xml_arg0 "tasks") in
      make ?failures ?tasks ()
    let of_json json =
      let failures = field_map json "failures" Failures.of_json in
      let tasks = field_map json "tasks" Tasks.of_json in
      make ?failures ?tasks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts a new task from the specified task definition on the specified container instance or instances. Alternatively, you can use RunTask to place tasks for you. For more information, see Scheduling Tasks in the Amazon Elastic Container Service Developer Guide."]
module StartTaskRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster where to start your task. If you do not specify a cluster, the default cluster is assumed."];
      containerInstances: StringList.t
        [@ocaml.doc
          "The container instance IDs or full ARN entries for the container instances where you would like to place your task. You can specify up to 10 container instances."];
      enableECSManagedTags: Boolean.t option
        [@ocaml.doc
          "Specifies whether to use Amazon ECS managed tags for the task. For more information, see Tagging Your Amazon ECS Resources in the Amazon Elastic Container Service Developer Guide."];
      enableExecuteCommand: Boolean.t option
        [@ocaml.doc
          "Whether or not the execute command functionality is enabled for the task. If true, this enables execute command functionality on all containers in the task."];
      group: String_.t option
        [@ocaml.doc
          "The name of the task group to associate with the task. The default value is the family name of the task definition (for example, family:my-family-name)."];
      networkConfiguration: NetworkConfiguration.t option
        [@ocaml.doc
          "The VPC subnet and security group configuration for tasks that receive their own elastic network interface by using the awsvpc networking mode."];
      overrides: TaskOverride.t option
        [@ocaml.doc
          "A list of container overrides in JSON format that specify the name of a container in the specified task definition and the overrides it receives. You can override the default command for a container (that's specified in the task definition or Docker image) with a command override. You can also override existing environment variables (that are specified in the task definition or Docker image) on a container or add new environment variables to it with an environment override. A total of 8192 characters are allowed for overrides. This limit includes the JSON formatting characters of the override structure."];
      propagateTags: PropagateTags.t option
        [@ocaml.doc
          "Specifies whether to propagate the tags from the task definition or the service to the task. If no value is specified, the tags aren't propagated."];
      referenceId: String_.t option
        [@ocaml.doc "The reference ID to use for the task."];
      startedBy: String_.t option
        [@ocaml.doc
          "An optional tag specified when a task is started. For example, if you automatically trigger a task to run a batch process job, you could apply a unique identifier for that job to your task with the startedBy parameter. You can then identify which tasks belong to that job by filtering the results of a ListTasks call with the startedBy value. Up to 36 letters (uppercase and lowercase), numbers, hyphens (-), and underscores (_) are allowed. If a task is started by an Amazon ECS service, the startedBy parameter contains the deployment ID of the service that starts it."];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the task to help you categorize and organize them. Each tag consists of a key and an optional value, both of which you define. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."];
      taskDefinition: String_.t
        [@ocaml.doc
          "The family and revision (family:revision) or full ARN of the task definition to start. If a revision isn't specified, the latest ACTIVE revision is used."]}
    let context_ = "StartTaskRequest"
    let make ?cluster =
      fun ?enableECSManagedTags ->
        fun ?enableExecuteCommand ->
          fun ?group ->
            fun ?networkConfiguration ->
              fun ?overrides ->
                fun ?propagateTags ->
                  fun ?referenceId ->
                    fun ?startedBy ->
                      fun ?tags ->
                        fun ~containerInstances ->
                          fun ~taskDefinition ->
                            fun () ->
                              {
                                cluster;
                                enableECSManagedTags;
                                enableExecuteCommand;
                                group;
                                networkConfiguration;
                                overrides;
                                propagateTags;
                                referenceId;
                                startedBy;
                                tags;
                                containerInstances;
                                taskDefinition
                              }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("containerInstances",
          (Some (StringList.to_value x.containerInstances)));
        ("enableECSManagedTags",
          (Option.map x.enableECSManagedTags ~f:Boolean.to_value));
        ("enableExecuteCommand",
          (Option.map x.enableExecuteCommand ~f:Boolean.to_value));
        ("group", (Option.map x.group ~f:String_.to_value));
        ("networkConfiguration",
          (Option.map x.networkConfiguration ~f:NetworkConfiguration.to_value));
        ("overrides", (Option.map x.overrides ~f:TaskOverride.to_value));
        ("propagateTags",
          (Option.map x.propagateTags ~f:PropagateTags.to_value));
        ("referenceId", (Option.map x.referenceId ~f:String_.to_value));
        ("startedBy", (Option.map x.startedBy ~f:String_.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("taskDefinition", (Some (String_.to_value x.taskDefinition)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskDefinition =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "taskDefinition") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let startedBy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "startedBy") in
      let referenceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "referenceId") in
      let propagateTags =
        (Option.map ~f:PropagateTags.of_xml)
          (Xml.child xml_arg0 "propagateTags") in
      let overrides =
        (Option.map ~f:TaskOverride.of_xml) (Xml.child xml_arg0 "overrides") in
      let networkConfiguration =
        (Option.map ~f:NetworkConfiguration.of_xml)
          (Xml.child xml_arg0 "networkConfiguration") in
      let group = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "group") in
      let enableExecuteCommand =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableExecuteCommand") in
      let enableECSManagedTags =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableECSManagedTags") in
      let containerInstances =
        StringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "containerInstances") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ~taskDefinition ?tags ?startedBy ?referenceId ?propagateTags
        ?overrides ?networkConfiguration ?group ?enableExecuteCommand
        ?enableECSManagedTags ~containerInstances ?cluster ()
    let of_json json =
      let taskDefinition =
        field_map_exn json "taskDefinition" String_.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let startedBy = field_map json "startedBy" String_.of_json in
      let referenceId = field_map json "referenceId" String_.of_json in
      let propagateTags =
        field_map json "propagateTags" PropagateTags.of_json in
      let overrides = field_map json "overrides" TaskOverride.of_json in
      let networkConfiguration =
        field_map json "networkConfiguration" NetworkConfiguration.of_json in
      let group = field_map json "group" String_.of_json in
      let enableExecuteCommand =
        field_map json "enableExecuteCommand" Boolean.of_json in
      let enableECSManagedTags =
        field_map json "enableECSManagedTags" Boolean.of_json in
      let containerInstances =
        field_map_exn json "containerInstances" StringList.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ~taskDefinition ?tags ?startedBy ?referenceId ?propagateTags
        ?overrides ?networkConfiguration ?group ?enableExecuteCommand
        ?enableECSManagedTags ~containerInstances ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts a new task from the specified task definition on the specified container instance or instances. Alternatively, you can use RunTask to place tasks for you. For more information, see Scheduling Tasks in the Amazon Elastic Container Service Developer Guide."]
module RunTaskResponse =
  struct
    type nonrec t =
      {
      tasks: Tasks.t option
        [@ocaml.doc
          "A full description of the tasks that were run. The tasks that were successfully placed on your cluster are described here."];
      failures: Failures.t option
        [@ocaml.doc "Any failures associated with the call."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BlockedException of BlockedException.t 
      | `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `PlatformTaskDefinitionIncompatibilityException of
          PlatformTaskDefinitionIncompatibilityException.t 
      | `PlatformUnknownException of PlatformUnknownException.t 
      | `ServerException of ServerException.t 
      | `UnsupportedFeatureException of UnsupportedFeatureException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tasks = fun ?failures -> fun () -> { tasks; failures }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BlockedException" ->
          `BlockedException (BlockedException.of_json json)
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "PlatformTaskDefinitionIncompatibilityException" ->
          `PlatformTaskDefinitionIncompatibilityException
            (PlatformTaskDefinitionIncompatibilityException.of_json json)
      | "PlatformUnknownException" ->
          `PlatformUnknownException (PlatformUnknownException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BlockedException" -> `BlockedException (BlockedException.of_xml xml)
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "PlatformTaskDefinitionIncompatibilityException" ->
          `PlatformTaskDefinitionIncompatibilityException
            (PlatformTaskDefinitionIncompatibilityException.of_xml xml)
      | "PlatformUnknownException" ->
          `PlatformUnknownException (PlatformUnknownException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BlockedException e ->
          `Assoc
            [("error", (`String "BlockedException"));
            ("details", (BlockedException.to_json e))]
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `PlatformTaskDefinitionIncompatibilityException e ->
          `Assoc
            [("error",
               (`String "PlatformTaskDefinitionIncompatibilityException"));
            ("details",
              (PlatformTaskDefinitionIncompatibilityException.to_json e))]
      | `PlatformUnknownException e ->
          `Assoc
            [("error", (`String "PlatformUnknownException"));
            ("details", (PlatformUnknownException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `UnsupportedFeatureException e ->
          `Assoc
            [("error", (`String "UnsupportedFeatureException"));
            ("details", (UnsupportedFeatureException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("tasks", (Option.map x.tasks ~f:Tasks.to_value));
        ("failures", (Option.map x.failures ~f:Failures.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failures =
        (Option.map ~f:Failures.of_xml) (Xml.child xml_arg0 "failures") in
      let tasks = (Option.map ~f:Tasks.of_xml) (Xml.child xml_arg0 "tasks") in
      make ?failures ?tasks ()
    let of_json json =
      let failures = field_map json "failures" Failures.of_json in
      let tasks = field_map json "tasks" Tasks.of_json in
      make ?failures ?tasks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts a new task using the specified task definition. You can allow Amazon ECS to place tasks for you, or you can customize how Amazon ECS places tasks using placement constraints and placement strategies. For more information, see Scheduling Tasks in the Amazon Elastic Container Service Developer Guide. Alternatively, you can use StartTask to use your own scheduler or place tasks manually on specific container instances. The Amazon ECS API follows an eventual consistency model. This is because of the distributed nature of the system supporting the API. This means that the result of an API command you run that affects your Amazon ECS resources might not be immediately visible to all subsequent commands you run. Keep this in mind when you carry out an API command that immediately follows a previous API command. To manage eventual consistency, you can do the following: Confirm the state of the resource before you run a command to modify it. Run the DescribeTasks command using an exponential backoff algorithm to ensure that you allow enough time for the previous command to propagate through the system. To do this, run the DescribeTasks command repeatedly, starting with a couple of seconds of wait time and increasing gradually up to five minutes of wait time. Add wait time between subsequent commands, even if the DescribeTasks command returns an accurate response. Apply an exponential backoff algorithm starting with a couple of seconds of wait time, and increase gradually up to about five minutes of wait time."]
module RunTaskRequest =
  struct
    type nonrec t =
      {
      capacityProviderStrategy: CapacityProviderStrategy.t option
        [@ocaml.doc
          "The capacity provider strategy to use for the task. If a capacityProviderStrategy is specified, the launchType parameter must be omitted. If no capacityProviderStrategy or launchType is specified, the defaultCapacityProviderStrategy for the cluster is used. When you use cluster auto scaling, you must specify capacityProviderStrategy and not launchType. A capacity provider strategy may contain a maximum of 6 capacity providers."];
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster to run your task on. If you do not specify a cluster, the default cluster is assumed."];
      count: BoxedInteger.t option
        [@ocaml.doc
          "The number of instantiations of the specified task to place on your cluster. You can specify up to 10 tasks for each call."];
      enableECSManagedTags: Boolean.t option
        [@ocaml.doc
          "Specifies whether to use Amazon ECS managed tags for the task. For more information, see Tagging Your Amazon ECS Resources in the Amazon Elastic Container Service Developer Guide."];
      enableExecuteCommand: Boolean.t option
        [@ocaml.doc
          "Determines whether to use the execute command functionality for the containers in this task. If true, this enables execute command functionality on all containers in the task. If true, then the task definition must have a task role, or you must provide one as an override."];
      group: String_.t option
        [@ocaml.doc
          "The name of the task group to associate with the task. The default value is the family name of the task definition (for example, family:my-family-name)."];
      launchType: LaunchType.t option
        [@ocaml.doc
          "The infrastructure to run your standalone task on. For more information, see Amazon ECS launch types in the Amazon Elastic Container Service Developer Guide. The FARGATE launch type runs your tasks on Fargate On-Demand infrastructure. Fargate Spot infrastructure is available for use but a capacity provider strategy must be used. For more information, see Fargate capacity providers in the Amazon ECS User Guide for Fargate. The EC2 launch type runs your tasks on Amazon EC2 instances registered to your cluster. The EXTERNAL launch type runs your tasks on your on-premises server or virtual machine (VM) capacity registered to your cluster. A task can use either a launch type or a capacity provider strategy. If a launchType is specified, the capacityProviderStrategy parameter must be omitted. When you use cluster auto scaling, you must specify capacityProviderStrategy and not launchType."];
      networkConfiguration: NetworkConfiguration.t option
        [@ocaml.doc
          "The network configuration for the task. This parameter is required for task definitions that use the awsvpc network mode to receive their own elastic network interface, and it isn't supported for other network modes. For more information, see Task networking in the Amazon Elastic Container Service Developer Guide."];
      overrides: TaskOverride.t option
        [@ocaml.doc
          "A list of container overrides in JSON format that specify the name of a container in the specified task definition and the overrides it should receive. You can override the default command for a container (that's specified in the task definition or Docker image) with a command override. You can also override existing environment variables (that are specified in the task definition or Docker image) on a container or add new environment variables to it with an environment override. A total of 8192 characters are allowed for overrides. This limit includes the JSON formatting characters of the override structure."];
      placementConstraints: PlacementConstraints.t option
        [@ocaml.doc
          "An array of placement constraint objects to use for the task. You can specify up to 10 constraints for each task (including constraints in the task definition and those specified at runtime)."];
      placementStrategy: PlacementStrategies.t option
        [@ocaml.doc
          "The placement strategy objects to use for the task. You can specify a maximum of 5 strategy rules for each task."];
      platformVersion: String_.t option
        [@ocaml.doc
          "The platform version the task uses. A platform version is only specified for tasks hosted on Fargate. If one isn't specified, the LATEST platform version is used. For more information, see Fargate platform versions in the Amazon Elastic Container Service Developer Guide."];
      propagateTags: PropagateTags.t option
        [@ocaml.doc
          "Specifies whether to propagate the tags from the task definition to the task. If no value is specified, the tags aren't propagated. Tags can only be propagated to the task during task creation. To add tags to a task after task creation, use the TagResource API action. An error will be received if you specify the SERVICE option when running a task."];
      referenceId: String_.t option
        [@ocaml.doc
          "The reference ID to use for the task. The reference ID can have a maximum length of 1024 characters."];
      startedBy: String_.t option
        [@ocaml.doc
          "An optional tag specified when a task is started. For example, if you automatically trigger a task to run a batch process job, you could apply a unique identifier for that job to your task with the startedBy parameter. You can then identify which tasks belong to that job by filtering the results of a ListTasks call with the startedBy value. Up to 36 letters (uppercase and lowercase), numbers, hyphens (-), and underscores (_) are allowed. If a task is started by an Amazon ECS service, then the startedBy parameter contains the deployment ID of the service that starts it."];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the task to help you categorize and organize them. Each tag consists of a key and an optional value, both of which you define. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."];
      taskDefinition: String_.t
        [@ocaml.doc
          "The family and revision (family:revision) or full ARN of the task definition to run. If a revision isn't specified, the latest ACTIVE revision is used. When you create an IAM policy for run-task, you can set the resource to be the latest task definition revision, or a specific revision. The full ARN value must match the value that you specified as the Resource of the IAM principal's permissions policy. When you specify the policy resource as the latest task definition version (by setting the Resource in the policy to arn:aws:ecs:us-east-1:111122223333:task-definition/TaskFamilyName), then set this value to arn:aws:ecs:us-east-1:111122223333:task-definition/TaskFamilyName. When you specify the policy resource as a specific task definition version (by setting the Resource in the policy to arn:aws:ecs:us-east-1:111122223333:task-definition/TaskFamilyName:1 or arn:aws:ecs:us-east-1:111122223333:task-definition/TaskFamilyName:*), then set this value to arn:aws:ecs:us-east-1:111122223333:task-definition/TaskFamilyName:1. For more information, see Policy Resources for Amazon ECS in the Amazon Elastic Container Service developer Guide."]}
    let context_ = "RunTaskRequest"
    let make ?capacityProviderStrategy =
      fun ?cluster ->
        fun ?count ->
          fun ?enableECSManagedTags ->
            fun ?enableExecuteCommand ->
              fun ?group ->
                fun ?launchType ->
                  fun ?networkConfiguration ->
                    fun ?overrides ->
                      fun ?placementConstraints ->
                        fun ?placementStrategy ->
                          fun ?platformVersion ->
                            fun ?propagateTags ->
                              fun ?referenceId ->
                                fun ?startedBy ->
                                  fun ?tags ->
                                    fun ~taskDefinition ->
                                      fun () ->
                                        {
                                          capacityProviderStrategy;
                                          cluster;
                                          count;
                                          enableECSManagedTags;
                                          enableExecuteCommand;
                                          group;
                                          launchType;
                                          networkConfiguration;
                                          overrides;
                                          placementConstraints;
                                          placementStrategy;
                                          platformVersion;
                                          propagateTags;
                                          referenceId;
                                          startedBy;
                                          tags;
                                          taskDefinition
                                        }
    let to_value x =
      structure_to_value
        [("capacityProviderStrategy",
           (Option.map x.capacityProviderStrategy
              ~f:CapacityProviderStrategy.to_value));
        ("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("count", (Option.map x.count ~f:BoxedInteger.to_value));
        ("enableECSManagedTags",
          (Option.map x.enableECSManagedTags ~f:Boolean.to_value));
        ("enableExecuteCommand",
          (Option.map x.enableExecuteCommand ~f:Boolean.to_value));
        ("group", (Option.map x.group ~f:String_.to_value));
        ("launchType", (Option.map x.launchType ~f:LaunchType.to_value));
        ("networkConfiguration",
          (Option.map x.networkConfiguration ~f:NetworkConfiguration.to_value));
        ("overrides", (Option.map x.overrides ~f:TaskOverride.to_value));
        ("placementConstraints",
          (Option.map x.placementConstraints ~f:PlacementConstraints.to_value));
        ("placementStrategy",
          (Option.map x.placementStrategy ~f:PlacementStrategies.to_value));
        ("platformVersion",
          (Option.map x.platformVersion ~f:String_.to_value));
        ("propagateTags",
          (Option.map x.propagateTags ~f:PropagateTags.to_value));
        ("referenceId", (Option.map x.referenceId ~f:String_.to_value));
        ("startedBy", (Option.map x.startedBy ~f:String_.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("taskDefinition", (Some (String_.to_value x.taskDefinition)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskDefinition =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "taskDefinition") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let startedBy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "startedBy") in
      let referenceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "referenceId") in
      let propagateTags =
        (Option.map ~f:PropagateTags.of_xml)
          (Xml.child xml_arg0 "propagateTags") in
      let platformVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platformVersion") in
      let placementStrategy =
        (Option.map ~f:PlacementStrategies.of_xml)
          (Xml.child xml_arg0 "placementStrategy") in
      let placementConstraints =
        (Option.map ~f:PlacementConstraints.of_xml)
          (Xml.child xml_arg0 "placementConstraints") in
      let overrides =
        (Option.map ~f:TaskOverride.of_xml) (Xml.child xml_arg0 "overrides") in
      let networkConfiguration =
        (Option.map ~f:NetworkConfiguration.of_xml)
          (Xml.child xml_arg0 "networkConfiguration") in
      let launchType =
        (Option.map ~f:LaunchType.of_xml) (Xml.child xml_arg0 "launchType") in
      let group = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "group") in
      let enableExecuteCommand =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableExecuteCommand") in
      let enableECSManagedTags =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableECSManagedTags") in
      let count =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "count") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      let capacityProviderStrategy =
        (Option.map ~f:CapacityProviderStrategy.of_xml)
          (Xml.child xml_arg0 "capacityProviderStrategy") in
      make ~taskDefinition ?tags ?startedBy ?referenceId ?propagateTags
        ?platformVersion ?placementStrategy ?placementConstraints ?overrides
        ?networkConfiguration ?launchType ?group ?enableExecuteCommand
        ?enableECSManagedTags ?count ?cluster ?capacityProviderStrategy ()
    let of_json json =
      let taskDefinition =
        field_map_exn json "taskDefinition" String_.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let startedBy = field_map json "startedBy" String_.of_json in
      let referenceId = field_map json "referenceId" String_.of_json in
      let propagateTags =
        field_map json "propagateTags" PropagateTags.of_json in
      let platformVersion = field_map json "platformVersion" String_.of_json in
      let placementStrategy =
        field_map json "placementStrategy" PlacementStrategies.of_json in
      let placementConstraints =
        field_map json "placementConstraints" PlacementConstraints.of_json in
      let overrides = field_map json "overrides" TaskOverride.of_json in
      let networkConfiguration =
        field_map json "networkConfiguration" NetworkConfiguration.of_json in
      let launchType = field_map json "launchType" LaunchType.of_json in
      let group = field_map json "group" String_.of_json in
      let enableExecuteCommand =
        field_map json "enableExecuteCommand" Boolean.of_json in
      let enableECSManagedTags =
        field_map json "enableECSManagedTags" Boolean.of_json in
      let count = field_map json "count" BoxedInteger.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      let capacityProviderStrategy =
        field_map json "capacityProviderStrategy"
          CapacityProviderStrategy.of_json in
      make ~taskDefinition ?tags ?startedBy ?referenceId ?propagateTags
        ?platformVersion ?placementStrategy ?placementConstraints ?overrides
        ?networkConfiguration ?launchType ?group ?enableExecuteCommand
        ?enableECSManagedTags ?count ?cluster ?capacityProviderStrategy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts a new task using the specified task definition. You can allow Amazon ECS to place tasks for you, or you can customize how Amazon ECS places tasks using placement constraints and placement strategies. For more information, see Scheduling Tasks in the Amazon Elastic Container Service Developer Guide. Alternatively, you can use StartTask to use your own scheduler or place tasks manually on specific container instances. The Amazon ECS API follows an eventual consistency model. This is because of the distributed nature of the system supporting the API. This means that the result of an API command you run that affects your Amazon ECS resources might not be immediately visible to all subsequent commands you run. Keep this in mind when you carry out an API command that immediately follows a previous API command. To manage eventual consistency, you can do the following: Confirm the state of the resource before you run a command to modify it. Run the DescribeTasks command using an exponential backoff algorithm to ensure that you allow enough time for the previous command to propagate through the system. To do this, run the DescribeTasks command repeatedly, starting with a couple of seconds of wait time and increasing gradually up to five minutes of wait time. Add wait time between subsequent commands, even if the DescribeTasks command returns an accurate response. Apply an exponential backoff algorithm starting with a couple of seconds of wait time, and increase gradually up to about five minutes of wait time."]
module RegisterTaskDefinitionResponse =
  struct
    type nonrec t =
      {
      taskDefinition: TaskDefinition.t option
        [@ocaml.doc
          "The full description of the registered task definition."];
      tags: Tags.t option
        [@ocaml.doc "The list of tags associated with the task definition."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?taskDefinition =
      fun ?tags -> fun () -> { taskDefinition; tags }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("taskDefinition",
           (Option.map x.taskDefinition ~f:TaskDefinition.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let taskDefinition =
        (Option.map ~f:TaskDefinition.of_xml)
          (Xml.child xml_arg0 "taskDefinition") in
      make ?tags ?taskDefinition ()
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let taskDefinition =
        field_map json "taskDefinition" TaskDefinition.of_json in
      make ?tags ?taskDefinition ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Registers a new task definition from the supplied family and containerDefinitions. Optionally, you can add data volumes to your containers with the volumes parameter. For more information about task definition parameters and defaults, see Amazon ECS Task Definitions in the Amazon Elastic Container Service Developer Guide. You can specify an IAM role for your task with the taskRoleArn parameter. When you specify an IAM role for a task, its containers can then use the latest versions of the CLI or SDKs to make API requests to the Amazon Web Services services that are specified in the IAM policy that's associated with the role. For more information, see IAM Roles for Tasks in the Amazon Elastic Container Service Developer Guide. You can specify a Docker networking mode for the containers in your task definition with the networkMode parameter. The available network modes correspond to those described in Network settings in the Docker run reference. If you specify the awsvpc network mode, the task is allocated an elastic network interface, and you must specify a NetworkConfiguration when you create a service or run a task with the task definition. For more information, see Task Networking in the Amazon Elastic Container Service Developer Guide."]
module RegisterTaskDefinitionRequest =
  struct
    type nonrec t =
      {
      family: String_.t
        [@ocaml.doc
          "You must specify a family for a task definition. You can use it track multiple versions of the same task definition. The family is used as a name for your task definition. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed."];
      taskRoleArn: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the IAM role that containers in this task can assume. All containers in this task are granted the permissions that are specified in this role. For more information, see IAM Roles for Tasks in the Amazon Elastic Container Service Developer Guide."];
      executionRoleArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the task execution role that grants the Amazon ECS container agent permission to make Amazon Web Services API calls on your behalf. The task execution IAM role is required depending on the requirements of your task. For more information, see Amazon ECS task execution IAM role in the Amazon Elastic Container Service Developer Guide."];
      networkMode: NetworkMode.t option
        [@ocaml.doc
          "The Docker networking mode to use for the containers in the task. The valid values are none, bridge, awsvpc, and host. If no network mode is specified, the default is bridge. For Amazon ECS tasks on Fargate, the awsvpc network mode is required. For Amazon ECS tasks on Amazon EC2 Linux instances, any network mode can be used. For Amazon ECS tasks on Amazon EC2 Windows instances, <default> or awsvpc can be used. If the network mode is set to none, you cannot specify port mappings in your container definitions, and the tasks containers do not have external connectivity. The host and awsvpc network modes offer the highest networking performance for containers because they use the EC2 network stack instead of the virtualized network stack provided by the bridge mode. With the host and awsvpc network modes, exposed container ports are mapped directly to the corresponding host port (for the host network mode) or the attached elastic network interface port (for the awsvpc network mode), so you cannot take advantage of dynamic host port mappings. When using the host network mode, you should not run containers using the root user (UID 0). It is considered best practice to use a non-root user. If the network mode is awsvpc, the task is allocated an elastic network interface, and you must specify a NetworkConfiguration value when you create a service or run a task with the task definition. For more information, see Task Networking in the Amazon Elastic Container Service Developer Guide. If the network mode is host, you cannot run multiple instantiations of the same task on a single container instance when port mappings are used. For more information, see Network settings in the Docker run reference."];
      containerDefinitions: ContainerDefinitions.t
        [@ocaml.doc
          "A list of container definitions in JSON format that describe the different containers that make up your task."];
      volumes: VolumeList.t option
        [@ocaml.doc
          "A list of volume definitions in JSON format that containers in your task might use."];
      placementConstraints: TaskDefinitionPlacementConstraints.t option
        [@ocaml.doc
          "An array of placement constraint objects to use for the task. You can specify a maximum of 10 constraints for each task. This limit includes constraints in the task definition and those specified at runtime."];
      requiresCompatibilities: CompatibilityList.t option
        [@ocaml.doc
          "The task launch type that Amazon ECS validates the task definition against. A client exception is returned if the task definition doesn't validate against the compatibilities specified. If no value is specified, the parameter is omitted from the response."];
      cpu: String_.t option
        [@ocaml.doc
          "The number of CPU units used by the task. It can be expressed as an integer using CPU units (for example, 1024) or as a string using vCPUs (for example, 1 vCPU or 1 vcpu) in a task definition. String values are converted to an integer indicating the CPU units when the task definition is registered. Task-level CPU and memory parameters are ignored for Windows containers. We recommend specifying container-level resources for Windows containers. If you're using the EC2 launch type, this field is optional. Supported values are between 128 CPU units (0.125 vCPUs) and 10240 CPU units (10 vCPUs). If you're using the Fargate launch type, this field is required and you must use one of the following values, which determines your range of supported values for the memory parameter: The CPU units cannot be less than 1 vCPU when you use Windows containers on Fargate. 256 (.25 vCPU) - Available memory values: 512 (0.5 GB), 1024 (1 GB), 2048 (2 GB) 512 (.5 vCPU) - Available memory values: 1024 (1 GB), 2048 (2 GB), 3072 (3 GB), 4096 (4 GB) 1024 (1 vCPU) - Available memory values: 2048 (2 GB), 3072 (3 GB), 4096 (4 GB), 5120 (5 GB), 6144 (6 GB), 7168 (7 GB), 8192 (8 GB) 2048 (2 vCPU) - Available memory values: Between 4096 (4 GB) and 16384 (16 GB) in increments of 1024 (1 GB) 4096 (4 vCPU) - Available memory values: Between 8192 (8 GB) and 30720 (30 GB) in increments of 1024 (1 GB)"];
      memory: String_.t option
        [@ocaml.doc
          "The amount of memory (in MiB) used by the task. It can be expressed as an integer using MiB (for example ,1024) or as a string using GB (for example, 1GB or 1 GB) in a task definition. String values are converted to an integer indicating the MiB when the task definition is registered. Task-level CPU and memory parameters are ignored for Windows containers. We recommend specifying container-level resources for Windows containers. If using the EC2 launch type, this field is optional. If using the Fargate launch type, this field is required and you must use one of the following values. This determines your range of supported values for the cpu parameter. The CPU units cannot be less than 1 vCPU when you use Windows containers on Fargate. 512 (0.5 GB), 1024 (1 GB), 2048 (2 GB) - Available cpu values: 256 (.25 vCPU) 1024 (1 GB), 2048 (2 GB), 3072 (3 GB), 4096 (4 GB) - Available cpu values: 512 (.5 vCPU) 2048 (2 GB), 3072 (3 GB), 4096 (4 GB), 5120 (5 GB), 6144 (6 GB), 7168 (7 GB), 8192 (8 GB) - Available cpu values: 1024 (1 vCPU) Between 4096 (4 GB) and 16384 (16 GB) in increments of 1024 (1 GB) - Available cpu values: 2048 (2 vCPU) Between 8192 (8 GB) and 30720 (30 GB) in increments of 1024 (1 GB) - Available cpu values: 4096 (4 vCPU)"];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the task definition to help you categorize and organize them. Each tag consists of a key and an optional value. You define both of them. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."];
      pidMode: PidMode.t option
        [@ocaml.doc
          "The process namespace to use for the containers in the task. The valid values are host or task. If host is specified, then all containers within the tasks that specified the host PID mode on the same container instance share the same process namespace with the host Amazon EC2 instance. If task is specified, all containers within the specified task share the same process namespace. If no value is specified, the default is a private namespace. For more information, see PID settings in the Docker run reference. If the host PID mode is used, be aware that there is a heightened risk of undesired process namespace expose. For more information, see Docker security. This parameter is not supported for Windows containers or tasks run on Fargate."];
      ipcMode: IpcMode.t option
        [@ocaml.doc
          "The IPC resource namespace to use for the containers in the task. The valid values are host, task, or none. If host is specified, then all containers within the tasks that specified the host IPC mode on the same container instance share the same IPC resources with the host Amazon EC2 instance. If task is specified, all containers within the specified task share the same IPC resources. If none is specified, then IPC resources within the containers of a task are private and not shared with other containers in a task or on the container instance. If no value is specified, then the IPC resource namespace sharing depends on the Docker daemon setting on the container instance. For more information, see IPC settings in the Docker run reference. If the host IPC mode is used, be aware that there is a heightened risk of undesired IPC namespace expose. For more information, see Docker security. If you are setting namespaced kernel parameters using systemControls for the containers in the task, the following will apply to your IPC resource namespace. For more information, see System Controls in the Amazon Elastic Container Service Developer Guide. For tasks that use the host IPC mode, IPC namespace related systemControls are not supported. For tasks that use the task IPC mode, IPC namespace related systemControls will apply to all containers within a task. This parameter is not supported for Windows containers or tasks run on Fargate."];
      proxyConfiguration: ProxyConfiguration.t option
        [@ocaml.doc
          "The configuration details for the App Mesh proxy. For tasks hosted on Amazon EC2 instances, the container instances require at least version 1.26.0 of the container agent and at least version 1.26.0-1 of the ecs-init package to use a proxy configuration. If your container instances are launched from the Amazon ECS-optimized AMI version 20190301 or later, then they contain the required versions of the container agent and ecs-init. For more information, see Amazon ECS-optimized AMI versions in the Amazon Elastic Container Service Developer Guide."];
      inferenceAccelerators: InferenceAccelerators.t option
        [@ocaml.doc
          "The Elastic Inference accelerators to use for the containers in the task."];
      ephemeralStorage: EphemeralStorage.t option
        [@ocaml.doc
          "The amount of ephemeral storage to allocate for the task. This parameter is used to expand the total amount of ephemeral storage available, beyond the default amount, for tasks hosted on Fargate. For more information, see Fargate task storage in the Amazon ECS User Guide for Fargate. This parameter is only supported for tasks hosted on Fargate using the following platform versions: Linux platform version 1.4.0 or later. Windows platform version 1.0.0 or later."];
      runtimePlatform: RuntimePlatform.t option
        [@ocaml.doc
          "The operating system that your tasks definitions run on. A platform family is specified only for tasks using the Fargate launch type. When you specify a task definition in a service, this value must match the runtimePlatform value of the service."]}
    let context_ = "RegisterTaskDefinitionRequest"
    let make ?taskRoleArn =
      fun ?executionRoleArn ->
        fun ?networkMode ->
          fun ?volumes ->
            fun ?placementConstraints ->
              fun ?requiresCompatibilities ->
                fun ?cpu ->
                  fun ?memory ->
                    fun ?tags ->
                      fun ?pidMode ->
                        fun ?ipcMode ->
                          fun ?proxyConfiguration ->
                            fun ?inferenceAccelerators ->
                              fun ?ephemeralStorage ->
                                fun ?runtimePlatform ->
                                  fun ~family ->
                                    fun ~containerDefinitions ->
                                      fun () ->
                                        {
                                          taskRoleArn;
                                          executionRoleArn;
                                          networkMode;
                                          volumes;
                                          placementConstraints;
                                          requiresCompatibilities;
                                          cpu;
                                          memory;
                                          tags;
                                          pidMode;
                                          ipcMode;
                                          proxyConfiguration;
                                          inferenceAccelerators;
                                          ephemeralStorage;
                                          runtimePlatform;
                                          family;
                                          containerDefinitions
                                        }
    let to_value x =
      structure_to_value
        [("family", (Some (String_.to_value x.family)));
        ("taskRoleArn", (Option.map x.taskRoleArn ~f:String_.to_value));
        ("executionRoleArn",
          (Option.map x.executionRoleArn ~f:String_.to_value));
        ("networkMode", (Option.map x.networkMode ~f:NetworkMode.to_value));
        ("containerDefinitions",
          (Some (ContainerDefinitions.to_value x.containerDefinitions)));
        ("volumes", (Option.map x.volumes ~f:VolumeList.to_value));
        ("placementConstraints",
          (Option.map x.placementConstraints
             ~f:TaskDefinitionPlacementConstraints.to_value));
        ("requiresCompatibilities",
          (Option.map x.requiresCompatibilities ~f:CompatibilityList.to_value));
        ("cpu", (Option.map x.cpu ~f:String_.to_value));
        ("memory", (Option.map x.memory ~f:String_.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("pidMode", (Option.map x.pidMode ~f:PidMode.to_value));
        ("ipcMode", (Option.map x.ipcMode ~f:IpcMode.to_value));
        ("proxyConfiguration",
          (Option.map x.proxyConfiguration ~f:ProxyConfiguration.to_value));
        ("inferenceAccelerators",
          (Option.map x.inferenceAccelerators
             ~f:InferenceAccelerators.to_value));
        ("ephemeralStorage",
          (Option.map x.ephemeralStorage ~f:EphemeralStorage.to_value));
        ("runtimePlatform",
          (Option.map x.runtimePlatform ~f:RuntimePlatform.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let runtimePlatform =
        (Option.map ~f:RuntimePlatform.of_xml)
          (Xml.child xml_arg0 "runtimePlatform") in
      let ephemeralStorage =
        (Option.map ~f:EphemeralStorage.of_xml)
          (Xml.child xml_arg0 "ephemeralStorage") in
      let inferenceAccelerators =
        (Option.map ~f:InferenceAccelerators.of_xml)
          (Xml.child xml_arg0 "inferenceAccelerators") in
      let proxyConfiguration =
        (Option.map ~f:ProxyConfiguration.of_xml)
          (Xml.child xml_arg0 "proxyConfiguration") in
      let ipcMode =
        (Option.map ~f:IpcMode.of_xml) (Xml.child xml_arg0 "ipcMode") in
      let pidMode =
        (Option.map ~f:PidMode.of_xml) (Xml.child xml_arg0 "pidMode") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let memory =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "memory") in
      let cpu = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cpu") in
      let requiresCompatibilities =
        (Option.map ~f:CompatibilityList.of_xml)
          (Xml.child xml_arg0 "requiresCompatibilities") in
      let placementConstraints =
        (Option.map ~f:TaskDefinitionPlacementConstraints.of_xml)
          (Xml.child xml_arg0 "placementConstraints") in
      let volumes =
        (Option.map ~f:VolumeList.of_xml) (Xml.child xml_arg0 "volumes") in
      let containerDefinitions =
        ContainerDefinitions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "containerDefinitions") in
      let networkMode =
        (Option.map ~f:NetworkMode.of_xml) (Xml.child xml_arg0 "networkMode") in
      let executionRoleArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "executionRoleArn") in
      let taskRoleArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskRoleArn") in
      let family =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "family") in
      make ?runtimePlatform ?ephemeralStorage ?inferenceAccelerators
        ?proxyConfiguration ?ipcMode ?pidMode ?tags ?memory ?cpu
        ?requiresCompatibilities ?placementConstraints ?volumes
        ~containerDefinitions ?networkMode ?executionRoleArn ?taskRoleArn
        ~family ()
    let of_json json =
      let runtimePlatform =
        field_map json "runtimePlatform" RuntimePlatform.of_json in
      let ephemeralStorage =
        field_map json "ephemeralStorage" EphemeralStorage.of_json in
      let inferenceAccelerators =
        field_map json "inferenceAccelerators" InferenceAccelerators.of_json in
      let proxyConfiguration =
        field_map json "proxyConfiguration" ProxyConfiguration.of_json in
      let ipcMode = field_map json "ipcMode" IpcMode.of_json in
      let pidMode = field_map json "pidMode" PidMode.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let memory = field_map json "memory" String_.of_json in
      let cpu = field_map json "cpu" String_.of_json in
      let requiresCompatibilities =
        field_map json "requiresCompatibilities" CompatibilityList.of_json in
      let placementConstraints =
        field_map json "placementConstraints"
          TaskDefinitionPlacementConstraints.of_json in
      let volumes = field_map json "volumes" VolumeList.of_json in
      let containerDefinitions =
        field_map_exn json "containerDefinitions"
          ContainerDefinitions.of_json in
      let networkMode = field_map json "networkMode" NetworkMode.of_json in
      let executionRoleArn =
        field_map json "executionRoleArn" String_.of_json in
      let taskRoleArn = field_map json "taskRoleArn" String_.of_json in
      let family = field_map_exn json "family" String_.of_json in
      make ?runtimePlatform ?ephemeralStorage ?inferenceAccelerators
        ?proxyConfiguration ?ipcMode ?pidMode ?tags ?memory ?cpu
        ?requiresCompatibilities ?placementConstraints ?volumes
        ~containerDefinitions ?networkMode ?executionRoleArn ?taskRoleArn
        ~family ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Registers a new task definition from the supplied family and containerDefinitions. Optionally, you can add data volumes to your containers with the volumes parameter. For more information about task definition parameters and defaults, see Amazon ECS Task Definitions in the Amazon Elastic Container Service Developer Guide. You can specify an IAM role for your task with the taskRoleArn parameter. When you specify an IAM role for a task, its containers can then use the latest versions of the CLI or SDKs to make API requests to the Amazon Web Services services that are specified in the IAM policy that's associated with the role. For more information, see IAM Roles for Tasks in the Amazon Elastic Container Service Developer Guide. You can specify a Docker networking mode for the containers in your task definition with the networkMode parameter. The available network modes correspond to those described in Network settings in the Docker run reference. If you specify the awsvpc network mode, the task is allocated an elastic network interface, and you must specify a NetworkConfiguration when you create a service or run a task with the task definition. For more information, see Task Networking in the Amazon Elastic Container Service Developer Guide."]
module RegisterContainerInstanceResponse =
  struct
    type nonrec t =
      {
      containerInstance: ContainerInstance.t option
        [@ocaml.doc "The container instance that was registered."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerInstance = fun () -> { containerInstance }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("containerInstance",
           (Option.map x.containerInstance ~f:ContainerInstance.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerInstance =
        (Option.map ~f:ContainerInstance.of_xml)
          (Xml.child xml_arg0 "containerInstance") in
      make ?containerInstance ()
    let of_json json =
      let containerInstance =
        field_map json "containerInstance" ContainerInstance.of_json in
      make ?containerInstance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is only used by the Amazon ECS agent, and it is not intended for use outside of the agent. Registers an EC2 instance into the specified cluster. This instance becomes available to place containers on."]
module RegisterContainerInstanceRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster to register your container instance with. If you do not specify a cluster, the default cluster is assumed."];
      instanceIdentityDocument: String_.t option
        [@ocaml.doc
          "The instance identity document for the EC2 instance to register. This document can be found by running the following command from the instance: curl http://169.254.169.254/latest/dynamic/instance-identity/document/"];
      instanceIdentityDocumentSignature: String_.t option
        [@ocaml.doc
          "The instance identity document signature for the EC2 instance to register. This signature can be found by running the following command from the instance: curl http://169.254.169.254/latest/dynamic/instance-identity/signature/"];
      totalResources: Resources.t option
        [@ocaml.doc "The resources available on the instance."];
      versionInfo: VersionInfo.t option
        [@ocaml.doc
          "The version information for the Amazon ECS container agent and Docker daemon that runs on the container instance."];
      containerInstanceArn: String_.t option
        [@ocaml.doc
          "The ARN of the container instance (if it was previously registered)."];
      attributes: Attributes.t option
        [@ocaml.doc
          "The container instance attributes that this container instance supports."];
      platformDevices: PlatformDevices.t option
        [@ocaml.doc
          "The devices that are available on the container instance. The only supported device type is a GPU."];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the container instance to help you categorize and organize them. Each tag consists of a key and an optional value. You define both. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."]}
    let make ?cluster =
      fun ?instanceIdentityDocument ->
        fun ?instanceIdentityDocumentSignature ->
          fun ?totalResources ->
            fun ?versionInfo ->
              fun ?containerInstanceArn ->
                fun ?attributes ->
                  fun ?platformDevices ->
                    fun ?tags ->
                      fun () ->
                        {
                          cluster;
                          instanceIdentityDocument;
                          instanceIdentityDocumentSignature;
                          totalResources;
                          versionInfo;
                          containerInstanceArn;
                          attributes;
                          platformDevices;
                          tags
                        }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("instanceIdentityDocument",
          (Option.map x.instanceIdentityDocument ~f:String_.to_value));
        ("instanceIdentityDocumentSignature",
          (Option.map x.instanceIdentityDocumentSignature ~f:String_.to_value));
        ("totalResources",
          (Option.map x.totalResources ~f:Resources.to_value));
        ("versionInfo", (Option.map x.versionInfo ~f:VersionInfo.to_value));
        ("containerInstanceArn",
          (Option.map x.containerInstanceArn ~f:String_.to_value));
        ("attributes", (Option.map x.attributes ~f:Attributes.to_value));
        ("platformDevices",
          (Option.map x.platformDevices ~f:PlatformDevices.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let platformDevices =
        (Option.map ~f:PlatformDevices.of_xml)
          (Xml.child xml_arg0 "platformDevices") in
      let attributes =
        (Option.map ~f:Attributes.of_xml) (Xml.child xml_arg0 "attributes") in
      let containerInstanceArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "containerInstanceArn") in
      let versionInfo =
        (Option.map ~f:VersionInfo.of_xml) (Xml.child xml_arg0 "versionInfo") in
      let totalResources =
        (Option.map ~f:Resources.of_xml)
          (Xml.child xml_arg0 "totalResources") in
      let instanceIdentityDocumentSignature =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "instanceIdentityDocumentSignature") in
      let instanceIdentityDocument =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "instanceIdentityDocument") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?tags ?platformDevices ?attributes ?containerInstanceArn
        ?versionInfo ?totalResources ?instanceIdentityDocumentSignature
        ?instanceIdentityDocument ?cluster ()
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let platformDevices =
        field_map json "platformDevices" PlatformDevices.of_json in
      let attributes = field_map json "attributes" Attributes.of_json in
      let containerInstanceArn =
        field_map json "containerInstanceArn" String_.of_json in
      let versionInfo = field_map json "versionInfo" VersionInfo.of_json in
      let totalResources = field_map json "totalResources" Resources.of_json in
      let instanceIdentityDocumentSignature =
        field_map json "instanceIdentityDocumentSignature" String_.of_json in
      let instanceIdentityDocument =
        field_map json "instanceIdentityDocument" String_.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?tags ?platformDevices ?attributes ?containerInstanceArn
        ?versionInfo ?totalResources ?instanceIdentityDocumentSignature
        ?instanceIdentityDocument ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is only used by the Amazon ECS agent, and it is not intended for use outside of the agent. Registers an EC2 instance into the specified cluster. This instance becomes available to place containers on."]
module PutClusterCapacityProvidersResponse =
  struct
    type nonrec t =
      {
      cluster: Cluster.t option [@ocaml.doc "Details about the cluster."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ServerException of ServerException.t 
      | `UpdateInProgressException of UpdateInProgressException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?cluster = fun () -> { cluster }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "UpdateInProgressException" ->
          `UpdateInProgressException (UpdateInProgressException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "UpdateInProgressException" ->
          `UpdateInProgressException (UpdateInProgressException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `UpdateInProgressException e ->
          `Assoc
            [("error", (`String "UpdateInProgressException"));
            ("details", (UpdateInProgressException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:Cluster.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cluster =
        (Option.map ~f:Cluster.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?cluster ()
    let of_json json =
      let cluster = field_map json "cluster" Cluster.of_json in
      make ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the available capacity providers and the default capacity provider strategy for a cluster. You must specify both the available capacity providers and a default capacity provider strategy for the cluster. If the specified cluster has existing capacity providers associated with it, you must specify all existing capacity providers in addition to any new ones you want to add. Any existing capacity providers that are associated with a cluster that are omitted from a PutClusterCapacityProviders API call will be disassociated with the cluster. You can only disassociate an existing capacity provider from a cluster if it's not being used by any existing tasks. When creating a service or running a task on a cluster, if no capacity provider or launch type is specified, then the cluster's default capacity provider strategy is used. We recommend that you define a default capacity provider strategy for your cluster. However, you must specify an empty array (\\[\\]) to bypass defining a default strategy."]
module PutClusterCapacityProvidersRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster to modify the capacity provider settings for. If you don't specify a cluster, the default cluster is assumed."];
      capacityProviders: StringList.t
        [@ocaml.doc
          "The name of one or more capacity providers to associate with the cluster. If specifying a capacity provider that uses an Auto Scaling group, the capacity provider must already be created. New capacity providers can be created with the CreateCapacityProvider API operation. To use a Fargate capacity provider, specify either the FARGATE or FARGATE_SPOT capacity providers. The Fargate capacity providers are available to all accounts and only need to be associated with a cluster to be used."];
      defaultCapacityProviderStrategy: CapacityProviderStrategy.t
        [@ocaml.doc
          "The capacity provider strategy to use by default for the cluster. When creating a service or running a task on a cluster, if no capacity provider or launch type is specified then the default capacity provider strategy for the cluster is used. A capacity provider strategy consists of one or more capacity providers along with the base and weight to assign to them. A capacity provider must be associated with the cluster to be used in a capacity provider strategy. The PutClusterCapacityProviders API is used to associate a capacity provider with a cluster. Only capacity providers with an ACTIVE or UPDATING status can be used. If specifying a capacity provider that uses an Auto Scaling group, the capacity provider must already be created. New capacity providers can be created with the CreateCapacityProvider API operation. To use a Fargate capacity provider, specify either the FARGATE or FARGATE_SPOT capacity providers. The Fargate capacity providers are available to all accounts and only need to be associated with a cluster to be used."]}
    let context_ = "PutClusterCapacityProvidersRequest"
    let make ~cluster =
      fun ~capacityProviders ->
        fun ~defaultCapacityProviderStrategy ->
          fun () ->
            { cluster; capacityProviders; defaultCapacityProviderStrategy }
    let to_value x =
      structure_to_value
        [("cluster", (Some (String_.to_value x.cluster)));
        ("capacityProviders",
          (Some (StringList.to_value x.capacityProviders)));
        ("defaultCapacityProviderStrategy",
          (Some
             (CapacityProviderStrategy.to_value
                x.defaultCapacityProviderStrategy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let defaultCapacityProviderStrategy =
        CapacityProviderStrategy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "defaultCapacityProviderStrategy") in
      let capacityProviders =
        StringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "capacityProviders") in
      let cluster =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "cluster") in
      make ~defaultCapacityProviderStrategy ~capacityProviders ~cluster ()
    let of_json json =
      let defaultCapacityProviderStrategy =
        field_map_exn json "defaultCapacityProviderStrategy"
          CapacityProviderStrategy.of_json in
      let capacityProviders =
        field_map_exn json "capacityProviders" StringList.of_json in
      let cluster = field_map_exn json "cluster" String_.of_json in
      make ~defaultCapacityProviderStrategy ~capacityProviders ~cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies the available capacity providers and the default capacity provider strategy for a cluster. You must specify both the available capacity providers and a default capacity provider strategy for the cluster. If the specified cluster has existing capacity providers associated with it, you must specify all existing capacity providers in addition to any new ones you want to add. Any existing capacity providers that are associated with a cluster that are omitted from a PutClusterCapacityProviders API call will be disassociated with the cluster. You can only disassociate an existing capacity provider from a cluster if it's not being used by any existing tasks. When creating a service or running a task on a cluster, if no capacity provider or launch type is specified, then the cluster's default capacity provider strategy is used. We recommend that you define a default capacity provider strategy for your cluster. However, you must specify an empty array (\\[\\]) to bypass defining a default strategy."]
module PutAttributesResponse =
  struct
    type nonrec t =
      {
      attributes: Attributes.t option
        [@ocaml.doc "The attributes applied to your resource."]}
    type nonrec error =
      [
        `AttributeLimitExceededException of AttributeLimitExceededException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `TargetNotFoundException of TargetNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?attributes = fun () -> { attributes }
    let error_of_json name json =
      match name with
      | "AttributeLimitExceededException" ->
          `AttributeLimitExceededException
            (AttributeLimitExceededException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "TargetNotFoundException" ->
          `TargetNotFoundException (TargetNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AttributeLimitExceededException" ->
          `AttributeLimitExceededException
            (AttributeLimitExceededException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "TargetNotFoundException" ->
          `TargetNotFoundException (TargetNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AttributeLimitExceededException e ->
          `Assoc
            [("error", (`String "AttributeLimitExceededException"));
            ("details", (AttributeLimitExceededException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `TargetNotFoundException e ->
          `Assoc
            [("error", (`String "TargetNotFoundException"));
            ("details", (TargetNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("attributes", (Option.map x.attributes ~f:Attributes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributes =
        (Option.map ~f:Attributes.of_xml) (Xml.child xml_arg0 "attributes") in
      make ?attributes ()
    let of_json json =
      let attributes = field_map json "attributes" Attributes.of_json in
      make ?attributes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create or update an attribute on an Amazon ECS resource. If the attribute doesn't exist, it's created. If the attribute exists, its value is replaced with the specified value. To delete an attribute, use DeleteAttributes. For more information, see Attributes in the Amazon Elastic Container Service Developer Guide."]
module PutAttributesRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that contains the resource to apply attributes. If you do not specify a cluster, the default cluster is assumed."];
      attributes: Attributes.t
        [@ocaml.doc
          "The attributes to apply to your resource. You can specify up to 10 custom attributes for each resource. You can specify up to 10 attributes in a single call."]}
    let context_ = "PutAttributesRequest"
    let make ?cluster = fun ~attributes -> fun () -> { cluster; attributes }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("attributes", (Some (Attributes.to_value x.attributes)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributes =
        Attributes.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "attributes") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ~attributes ?cluster ()
    let of_json json =
      let attributes = field_map_exn json "attributes" Attributes.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ~attributes ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create or update an attribute on an Amazon ECS resource. If the attribute doesn't exist, it's created. If the attribute exists, its value is replaced with the specified value. To delete an attribute, use DeleteAttributes. For more information, see Attributes in the Amazon Elastic Container Service Developer Guide."]
module PutAccountSettingResponse =
  struct
    type nonrec t =
      {
      setting: Setting.t option
        [@ocaml.doc "The current account setting for a resource."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?setting = fun () -> { setting }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("setting", (Option.map x.setting ~f:Setting.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let setting =
        (Option.map ~f:Setting.of_xml) (Xml.child xml_arg0 "setting") in
      make ?setting ()
    let of_json json =
      let setting = field_map json "setting" Setting.of_json in
      make ?setting ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies an account setting. Account settings are set on a per-Region basis. If you change the account setting for the root user, the default settings for all of the IAM users and roles that no individual account setting was specified are reset for. For more information, see Account Settings in the Amazon Elastic Container Service Developer Guide. When serviceLongArnFormat, taskLongArnFormat, or containerInstanceLongArnFormat are specified, the Amazon Resource Name (ARN) and resource ID format of the resource type for a specified IAM user, IAM role, or the root user for an account is affected. The opt-in and opt-out account setting must be set for each Amazon ECS resource separately. The ARN and resource ID format of a resource is defined by the opt-in status of the IAM user or role that created the resource. You must turn on this setting to use Amazon ECS features such as resource tagging. When awsvpcTrunking is specified, the elastic network interface (ENI) limit for any new container instances that support the feature is changed. If awsvpcTrunking is enabled, any new container instances that support the feature are launched have the increased ENI limits available to them. For more information, see Elastic Network Interface Trunking in the Amazon Elastic Container Service Developer Guide. When containerInsights is specified, the default setting indicating whether CloudWatch Container Insights is enabled for your clusters is changed. If containerInsights is enabled, any new clusters that are created will have Container Insights enabled unless you disable it during cluster creation. For more information, see CloudWatch Container Insights in the Amazon Elastic Container Service Developer Guide."]
module PutAccountSettingRequest =
  struct
    type nonrec t =
      {
      name: SettingName.t
        [@ocaml.doc
          "The Amazon ECS resource name for which to modify the account setting. If serviceLongArnFormat is specified, the ARN for your Amazon ECS services is affected. If taskLongArnFormat is specified, the ARN and resource ID for your Amazon ECS tasks is affected. If containerInstanceLongArnFormat is specified, the ARN and resource ID for your Amazon ECS container instances is affected. If awsvpcTrunking is specified, the elastic network interface (ENI) limit for your Amazon ECS container instances is affected. If containerInsights is specified, the default setting for CloudWatch Container Insights for your clusters is affected."];
      value: String_.t
        [@ocaml.doc
          "The account setting value for the specified principal ARN. Accepted values are enabled and disabled."];
      principalArn: String_.t option
        [@ocaml.doc
          "The ARN of the principal, which can be an IAM user, IAM role, or the root user. If you specify the root user, it modifies the account setting for all IAM users, IAM roles, and the root user of the account unless an IAM user or role explicitly overrides these settings. If this field is omitted, the setting is changed only for the authenticated user. Federated users assume the account setting of the root user and can't have explicit account settings set for them."]}
    let context_ = "PutAccountSettingRequest"
    let make ?principalArn =
      fun ~name -> fun ~value -> fun () -> { principalArn; name; value }
    let to_value x =
      structure_to_value
        [("name", (Some (SettingName.to_value x.name)));
        ("value", (Some (String_.to_value x.value)));
        ("principalArn", (Option.map x.principalArn ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let principalArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "principalArn") in
      let value =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "value") in
      let name =
        SettingName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?principalArn ~value ~name ()
    let of_json json =
      let principalArn = field_map json "principalArn" String_.of_json in
      let value = field_map_exn json "value" String_.of_json in
      let name = field_map_exn json "name" SettingName.of_json in
      make ?principalArn ~value ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies an account setting. Account settings are set on a per-Region basis. If you change the account setting for the root user, the default settings for all of the IAM users and roles that no individual account setting was specified are reset for. For more information, see Account Settings in the Amazon Elastic Container Service Developer Guide. When serviceLongArnFormat, taskLongArnFormat, or containerInstanceLongArnFormat are specified, the Amazon Resource Name (ARN) and resource ID format of the resource type for a specified IAM user, IAM role, or the root user for an account is affected. The opt-in and opt-out account setting must be set for each Amazon ECS resource separately. The ARN and resource ID format of a resource is defined by the opt-in status of the IAM user or role that created the resource. You must turn on this setting to use Amazon ECS features such as resource tagging. When awsvpcTrunking is specified, the elastic network interface (ENI) limit for any new container instances that support the feature is changed. If awsvpcTrunking is enabled, any new container instances that support the feature are launched have the increased ENI limits available to them. For more information, see Elastic Network Interface Trunking in the Amazon Elastic Container Service Developer Guide. When containerInsights is specified, the default setting indicating whether CloudWatch Container Insights is enabled for your clusters is changed. If containerInsights is enabled, any new clusters that are created will have Container Insights enabled unless you disable it during cluster creation. For more information, see CloudWatch Container Insights in the Amazon Elastic Container Service Developer Guide."]
module PutAccountSettingDefaultResponse =
  struct
    type nonrec t =
      {
      setting: Setting.t option
        [@ocaml.doc "The current setting for a resource."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?setting = fun () -> { setting }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("setting", (Option.map x.setting ~f:Setting.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let setting =
        (Option.map ~f:Setting.of_xml) (Xml.child xml_arg0 "setting") in
      make ?setting ()
    let of_json json =
      let setting = field_map json "setting" Setting.of_json in
      make ?setting ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies an account setting for all IAM users on an account for whom no individual account setting has been specified. Account settings are set on a per-Region basis."]
module PutAccountSettingDefaultRequest =
  struct
    type nonrec t =
      {
      name: SettingName.t
        [@ocaml.doc
          "The resource name for which to modify the account setting. If serviceLongArnFormat is specified, the ARN for your Amazon ECS services is affected. If taskLongArnFormat is specified, the ARN and resource ID for your Amazon ECS tasks is affected. If containerInstanceLongArnFormat is specified, the ARN and resource ID for your Amazon ECS container instances is affected. If awsvpcTrunking is specified, the ENI limit for your Amazon ECS container instances is affected. If containerInsights is specified, the default setting for CloudWatch Container Insights for your clusters is affected."];
      value: String_.t
        [@ocaml.doc
          "The account setting value for the specified principal ARN. Accepted values are enabled and disabled."]}
    let context_ = "PutAccountSettingDefaultRequest"
    let make ~name = fun ~value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("name", (Some (SettingName.to_value x.name)));
        ("value", (Some (String_.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "value") in
      let name =
        SettingName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~value ~name ()
    let of_json json =
      let value = field_map_exn json "value" String_.of_json in
      let name = field_map_exn json "name" SettingName.of_json in
      make ~value ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Modifies an account setting for all IAM users on an account for whom no individual account setting has been specified. Account settings are set on a per-Region basis."]
module ListTasksResponse =
  struct
    type nonrec t =
      {
      taskArns: StringList.t option
        [@ocaml.doc
          "The list of task ARN entries for the ListTasks request."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value to include in a future ListTasks request. When the results of a ListTasks request exceed maxResults, this value can be used to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `ServiceNotFoundException of ServiceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?taskArns = fun ?nextToken -> fun () -> { taskArns; nextToken }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `ServiceNotFoundException e ->
          `Assoc
            [("error", (`String "ServiceNotFoundException"));
            ("details", (ServiceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("taskArns", (Option.map x.taskArns ~f:StringList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let taskArns =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "taskArns") in
      make ?nextToken ?taskArns ()
    let of_json json =
      let nextToken = field_map json "nextToken" String_.of_json in
      let taskArns = field_map json "taskArns" StringList.of_json in
      make ?nextToken ?taskArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of tasks. You can filter the results by cluster, task definition family, container instance, launch type, what IAM principal started the task, or by the desired status of the task. Recently stopped tasks might appear in the returned results. Currently, stopped tasks appear in the returned results for at least one hour."]
module ListTasksRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster to use when filtering the ListTasks results. If you do not specify a cluster, the default cluster is assumed."];
      containerInstance: String_.t option
        [@ocaml.doc
          "The container instance ID or full ARN of the container instance to use when filtering the ListTasks results. Specifying a containerInstance limits the results to tasks that belong to that container instance."];
      family: String_.t option
        [@ocaml.doc
          "The name of the task definition family to use when filtering the ListTasks results. Specifying a family limits the results to tasks that belong to that family."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value returned from a ListTasks request indicating that more results are available to fulfill the request and further calls will be needed. If maxResults was provided, it's possible the number of results to be fewer than maxResults. This token should be treated as an opaque identifier that is only used to retrieve the next items in a list and not for other programmatic purposes."];
      maxResults: BoxedInteger.t option
        [@ocaml.doc
          "The maximum number of task results that ListTasks returned in paginated output. When this parameter is used, ListTasks only returns maxResults results in a single page along with a nextToken response element. The remaining results of the initial request can be seen by sending another ListTasks request with the returned nextToken value. This value can be between 1 and 100. If this parameter isn't used, then ListTasks returns up to 100 results and a nextToken value if applicable."];
      startedBy: String_.t option
        [@ocaml.doc
          "The startedBy value to filter the task results with. Specifying a startedBy value limits the results to tasks that were started with that value."];
      serviceName: String_.t option
        [@ocaml.doc
          "The name of the service to use when filtering the ListTasks results. Specifying a serviceName limits the results to tasks that belong to that service."];
      desiredStatus: DesiredStatus.t option
        [@ocaml.doc
          "The task desired status to use when filtering the ListTasks results. Specifying a desiredStatus of STOPPED limits the results to tasks that Amazon ECS has set the desired status to STOPPED. This can be useful for debugging tasks that aren't starting properly or have died or finished. The default status filter is RUNNING, which shows tasks that Amazon ECS has set the desired status to RUNNING. Although you can filter results based on a desired status of PENDING, this doesn't return any results. Amazon ECS never sets the desired status of a task to that value (only a task's lastStatus may have a value of PENDING)."];
      launchType: LaunchType.t option
        [@ocaml.doc
          "The launch type to use when filtering the ListTasks results."]}
    let make ?cluster =
      fun ?containerInstance ->
        fun ?family ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun ?startedBy ->
                fun ?serviceName ->
                  fun ?desiredStatus ->
                    fun ?launchType ->
                      fun () ->
                        {
                          cluster;
                          containerInstance;
                          family;
                          nextToken;
                          maxResults;
                          startedBy;
                          serviceName;
                          desiredStatus;
                          launchType
                        }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("containerInstance",
          (Option.map x.containerInstance ~f:String_.to_value));
        ("family", (Option.map x.family ~f:String_.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:BoxedInteger.to_value));
        ("startedBy", (Option.map x.startedBy ~f:String_.to_value));
        ("serviceName", (Option.map x.serviceName ~f:String_.to_value));
        ("desiredStatus",
          (Option.map x.desiredStatus ~f:DesiredStatus.to_value));
        ("launchType", (Option.map x.launchType ~f:LaunchType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchType =
        (Option.map ~f:LaunchType.of_xml) (Xml.child xml_arg0 "launchType") in
      let desiredStatus =
        (Option.map ~f:DesiredStatus.of_xml)
          (Xml.child xml_arg0 "desiredStatus") in
      let serviceName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "serviceName") in
      let startedBy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "startedBy") in
      let maxResults =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let family =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "family") in
      let containerInstance =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "containerInstance") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?launchType ?desiredStatus ?serviceName ?startedBy ?maxResults
        ?nextToken ?family ?containerInstance ?cluster ()
    let of_json json =
      let launchType = field_map json "launchType" LaunchType.of_json in
      let desiredStatus =
        field_map json "desiredStatus" DesiredStatus.of_json in
      let serviceName = field_map json "serviceName" String_.of_json in
      let startedBy = field_map json "startedBy" String_.of_json in
      let maxResults = field_map json "maxResults" BoxedInteger.of_json in
      let nextToken = field_map json "nextToken" String_.of_json in
      let family = field_map json "family" String_.of_json in
      let containerInstance =
        field_map json "containerInstance" String_.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?launchType ?desiredStatus ?serviceName ?startedBy ?maxResults
        ?nextToken ?family ?containerInstance ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of tasks. You can filter the results by cluster, task definition family, container instance, launch type, what IAM principal started the task, or by the desired status of the task. Recently stopped tasks might appear in the returned results. Currently, stopped tasks appear in the returned results for at least one hour."]
module ListTaskDefinitionsResponse =
  struct
    type nonrec t =
      {
      taskDefinitionArns: StringList.t option
        [@ocaml.doc
          "The list of task definition Amazon Resource Name (ARN) entries for the ListTaskDefinitions request."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value to include in a future ListTaskDefinitions request. When the results of a ListTaskDefinitions request exceed maxResults, this value can be used to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?taskDefinitionArns =
      fun ?nextToken -> fun () -> { taskDefinitionArns; nextToken }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("taskDefinitionArns",
           (Option.map x.taskDefinitionArns ~f:StringList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let taskDefinitionArns =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "taskDefinitionArns") in
      make ?nextToken ?taskDefinitionArns ()
    let of_json json =
      let nextToken = field_map json "nextToken" String_.of_json in
      let taskDefinitionArns =
        field_map json "taskDefinitionArns" StringList.of_json in
      make ?nextToken ?taskDefinitionArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of task definitions that are registered to your account. You can filter the results by family name with the familyPrefix parameter or by status with the status parameter."]
module ListTaskDefinitionsRequest =
  struct
    type nonrec t =
      {
      familyPrefix: String_.t option
        [@ocaml.doc
          "The full family name to filter the ListTaskDefinitions results with. Specifying a familyPrefix limits the listed task definitions to task definition revisions that belong to that family."];
      status: TaskDefinitionStatus.t option
        [@ocaml.doc
          "The task definition status to filter the ListTaskDefinitions results with. By default, only ACTIVE task definitions are listed. By setting this parameter to INACTIVE, you can view task definitions that are INACTIVE as long as an active task or service still references them. If you paginate the resulting output, be sure to keep the status value constant in each subsequent request."];
      sort: SortOrder.t option
        [@ocaml.doc
          "The order to sort the results in. Valid values are ASC and DESC. By default, (ASC) task definitions are listed lexicographically by family name and in ascending numerical order by revision so that the newest task definitions in a family are listed last. Setting this parameter to DESC reverses the sort order on family name and revision. This is so that the newest task definitions in a family are listed first."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value returned from a ListTaskDefinitions request indicating that more results are available to fulfill the request and further calls will be needed. If maxResults was provided, it is possible the number of results to be fewer than maxResults. This token should be treated as an opaque identifier that is only used to retrieve the next items in a list and not for other programmatic purposes."];
      maxResults: BoxedInteger.t option
        [@ocaml.doc
          "The maximum number of task definition results that ListTaskDefinitions returned in paginated output. When this parameter is used, ListTaskDefinitions only returns maxResults results in a single page along with a nextToken response element. The remaining results of the initial request can be seen by sending another ListTaskDefinitions request with the returned nextToken value. This value can be between 1 and 100. If this parameter isn't used, then ListTaskDefinitions returns up to 100 results and a nextToken value if applicable."]}
    let make ?familyPrefix =
      fun ?status ->
        fun ?sort ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () -> { familyPrefix; status; sort; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("familyPrefix", (Option.map x.familyPrefix ~f:String_.to_value));
        ("status", (Option.map x.status ~f:TaskDefinitionStatus.to_value));
        ("sort", (Option.map x.sort ~f:SortOrder.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:BoxedInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let sort = (Option.map ~f:SortOrder.of_xml) (Xml.child xml_arg0 "sort") in
      let status =
        (Option.map ~f:TaskDefinitionStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let familyPrefix =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "familyPrefix") in
      make ?maxResults ?nextToken ?sort ?status ?familyPrefix ()
    let of_json json =
      let maxResults = field_map json "maxResults" BoxedInteger.of_json in
      let nextToken = field_map json "nextToken" String_.of_json in
      let sort = field_map json "sort" SortOrder.of_json in
      let status = field_map json "status" TaskDefinitionStatus.of_json in
      let familyPrefix = field_map json "familyPrefix" String_.of_json in
      make ?maxResults ?nextToken ?sort ?status ?familyPrefix ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of task definitions that are registered to your account. You can filter the results by family name with the familyPrefix parameter or by status with the status parameter."]
module ListTaskDefinitionFamiliesResponse =
  struct
    type nonrec t =
      {
      families: StringList.t option
        [@ocaml.doc
          "The list of task definition family names that match the ListTaskDefinitionFamilies request."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value to include in a future ListTaskDefinitionFamilies request. When the results of a ListTaskDefinitionFamilies request exceed maxResults, this value can be used to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?families = fun ?nextToken -> fun () -> { families; nextToken }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("families", (Option.map x.families ~f:StringList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let families =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "families") in
      make ?nextToken ?families ()
    let of_json json =
      let nextToken = field_map json "nextToken" String_.of_json in
      let families = field_map json "families" StringList.of_json in
      make ?nextToken ?families ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of task definition families that are registered to your account. This list includes task definition families that no longer have any ACTIVE task definition revisions. You can filter out task definition families that don't contain any ACTIVE task definition revisions by setting the status parameter to ACTIVE. You can also filter the results with the familyPrefix parameter."]
module ListTaskDefinitionFamiliesRequest =
  struct
    type nonrec t =
      {
      familyPrefix: String_.t option
        [@ocaml.doc
          "The familyPrefix is a string that's used to filter the results of ListTaskDefinitionFamilies. If you specify a familyPrefix, only task definition family names that begin with the familyPrefix string are returned."];
      status: TaskDefinitionFamilyStatus.t option
        [@ocaml.doc
          "The task definition family status to filter the ListTaskDefinitionFamilies results with. By default, both ACTIVE and INACTIVE task definition families are listed. If this parameter is set to ACTIVE, only task definition families that have an ACTIVE task definition revision are returned. If this parameter is set to INACTIVE, only task definition families that do not have any ACTIVE task definition revisions are returned. If you paginate the resulting output, be sure to keep the status value constant in each subsequent request."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value returned from a ListTaskDefinitionFamilies request indicating that more results are available to fulfill the request and further calls will be needed. If maxResults was provided, it is possible the number of results to be fewer than maxResults. This token should be treated as an opaque identifier that is only used to retrieve the next items in a list and not for other programmatic purposes."];
      maxResults: BoxedInteger.t option
        [@ocaml.doc
          "The maximum number of task definition family results that ListTaskDefinitionFamilies returned in paginated output. When this parameter is used, ListTaskDefinitions only returns maxResults results in a single page along with a nextToken response element. The remaining results of the initial request can be seen by sending another ListTaskDefinitionFamilies request with the returned nextToken value. This value can be between 1 and 100. If this parameter isn't used, then ListTaskDefinitionFamilies returns up to 100 results and a nextToken value if applicable."]}
    let make ?familyPrefix =
      fun ?status ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun () -> { familyPrefix; status; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("familyPrefix", (Option.map x.familyPrefix ~f:String_.to_value));
        ("status",
          (Option.map x.status ~f:TaskDefinitionFamilyStatus.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:BoxedInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let status =
        (Option.map ~f:TaskDefinitionFamilyStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let familyPrefix =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "familyPrefix") in
      make ?maxResults ?nextToken ?status ?familyPrefix ()
    let of_json json =
      let maxResults = field_map json "maxResults" BoxedInteger.of_json in
      let nextToken = field_map json "nextToken" String_.of_json in
      let status = field_map json "status" TaskDefinitionFamilyStatus.of_json in
      let familyPrefix = field_map json "familyPrefix" String_.of_json in
      make ?maxResults ?nextToken ?status ?familyPrefix ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of task definition families that are registered to your account. This list includes task definition families that no longer have any ACTIVE task definition revisions. You can filter out task definition families that don't contain any ACTIVE task definition revisions by setting the status parameter to ACTIVE. You can also filter the results with the familyPrefix parameter."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: Tags.t option [@ocaml.doc "The tags for the resource."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the tags for an Amazon ECS resource."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: String_.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that identifies the resource to list the tags for. Currently, the supported resources are Amazon ECS tasks, services, task definitions, clusters, and container instances."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (String_.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_json json =
      let resourceArn = field_map_exn json "resourceArn" String_.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the tags for an Amazon ECS resource."]
module ListServicesResponse =
  struct
    type nonrec t =
      {
      serviceArns: StringList.t option
        [@ocaml.doc
          "The list of full ARN entries for each service that's associated with the specified cluster."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value to include in a future ListServices request. When the results of a ListServices request exceed maxResults, this value can be used to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?serviceArns =
      fun ?nextToken -> fun () -> { serviceArns; nextToken }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("serviceArns", (Option.map x.serviceArns ~f:StringList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let serviceArns =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "serviceArns") in
      make ?nextToken ?serviceArns ()
    let of_json json =
      let nextToken = field_map json "nextToken" String_.of_json in
      let serviceArns = field_map json "serviceArns" StringList.of_json in
      make ?nextToken ?serviceArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of services. You can filter the results by cluster, launch type, and scheduling strategy."]
module ListServicesRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster to use when filtering the ListServices results. If you do not specify a cluster, the default cluster is assumed."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value returned from a ListServices request indicating that more results are available to fulfill the request and further calls will be needed. If maxResults was provided, it is possible the number of results to be fewer than maxResults. This token should be treated as an opaque identifier that is only used to retrieve the next items in a list and not for other programmatic purposes."];
      maxResults: BoxedInteger.t option
        [@ocaml.doc
          "The maximum number of service results that ListServices returned in paginated output. When this parameter is used, ListServices only returns maxResults results in a single page along with a nextToken response element. The remaining results of the initial request can be seen by sending another ListServices request with the returned nextToken value. This value can be between 1 and 100. If this parameter isn't used, then ListServices returns up to 10 results and a nextToken value if applicable."];
      launchType: LaunchType.t option
        [@ocaml.doc
          "The launch type to use when filtering the ListServices results."];
      schedulingStrategy: SchedulingStrategy.t option
        [@ocaml.doc
          "The scheduling strategy to use when filtering the ListServices results."]}
    let make ?cluster =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?launchType ->
            fun ?schedulingStrategy ->
              fun () ->
                {
                  cluster;
                  nextToken;
                  maxResults;
                  launchType;
                  schedulingStrategy
                }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:BoxedInteger.to_value));
        ("launchType", (Option.map x.launchType ~f:LaunchType.to_value));
        ("schedulingStrategy",
          (Option.map x.schedulingStrategy ~f:SchedulingStrategy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let schedulingStrategy =
        (Option.map ~f:SchedulingStrategy.of_xml)
          (Xml.child xml_arg0 "schedulingStrategy") in
      let launchType =
        (Option.map ~f:LaunchType.of_xml) (Xml.child xml_arg0 "launchType") in
      let maxResults =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?schedulingStrategy ?launchType ?maxResults ?nextToken ?cluster ()
    let of_json json =
      let schedulingStrategy =
        field_map json "schedulingStrategy" SchedulingStrategy.of_json in
      let launchType = field_map json "launchType" LaunchType.of_json in
      let maxResults = field_map json "maxResults" BoxedInteger.of_json in
      let nextToken = field_map json "nextToken" String_.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?schedulingStrategy ?launchType ?maxResults ?nextToken ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of services. You can filter the results by cluster, launch type, and scheduling strategy."]
module ListContainerInstancesResponse =
  struct
    type nonrec t =
      {
      containerInstanceArns: StringList.t option
        [@ocaml.doc
          "The list of container instances with full ARN entries for each container instance associated with the specified cluster."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value to include in a future ListContainerInstances request. When the results of a ListContainerInstances request exceed maxResults, this value can be used to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerInstanceArns =
      fun ?nextToken -> fun () -> { containerInstanceArns; nextToken }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("containerInstanceArns",
           (Option.map x.containerInstanceArns ~f:StringList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let containerInstanceArns =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "containerInstanceArns") in
      make ?nextToken ?containerInstanceArns ()
    let of_json json =
      let nextToken = field_map json "nextToken" String_.of_json in
      let containerInstanceArns =
        field_map json "containerInstanceArns" StringList.of_json in
      make ?nextToken ?containerInstanceArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of container instances in a specified cluster. You can filter the results of a ListContainerInstances operation with cluster query language statements inside the filter parameter. For more information, see Cluster Query Language in the Amazon Elastic Container Service Developer Guide."]
module ListContainerInstancesRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that hosts the container instances to list. If you do not specify a cluster, the default cluster is assumed."];
      filter: String_.t option
        [@ocaml.doc
          "You can filter the results of a ListContainerInstances operation with cluster query language statements. For more information, see Cluster Query Language in the Amazon Elastic Container Service Developer Guide."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value returned from a ListContainerInstances request indicating that more results are available to fulfill the request and further calls are needed. If maxResults was provided, it's possible the number of results to be fewer than maxResults. This token should be treated as an opaque identifier that is only used to retrieve the next items in a list and not for other programmatic purposes."];
      maxResults: BoxedInteger.t option
        [@ocaml.doc
          "The maximum number of container instance results that ListContainerInstances returned in paginated output. When this parameter is used, ListContainerInstances only returns maxResults results in a single page along with a nextToken response element. The remaining results of the initial request can be seen by sending another ListContainerInstances request with the returned nextToken value. This value can be between 1 and 100. If this parameter isn't used, then ListContainerInstances returns up to 100 results and a nextToken value if applicable."];
      status: ContainerInstanceStatus.t option
        [@ocaml.doc
          "Filters the container instances by status. For example, if you specify the DRAINING status, the results include only container instances that have been set to DRAINING using UpdateContainerInstancesState. If you don't specify this parameter, the default is to include container instances set to all states other than INACTIVE."]}
    let make ?cluster =
      fun ?filter ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ?status ->
              fun () -> { cluster; filter; nextToken; maxResults; status }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("filter", (Option.map x.filter ~f:String_.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:BoxedInteger.to_value));
        ("status", (Option.map x.status ~f:ContainerInstanceStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ContainerInstanceStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let maxResults =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let filter =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "filter") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?status ?maxResults ?nextToken ?filter ?cluster ()
    let of_json json =
      let status = field_map json "status" ContainerInstanceStatus.of_json in
      let maxResults = field_map json "maxResults" BoxedInteger.of_json in
      let nextToken = field_map json "nextToken" String_.of_json in
      let filter = field_map json "filter" String_.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?status ?maxResults ?nextToken ?filter ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of container instances in a specified cluster. You can filter the results of a ListContainerInstances operation with cluster query language statements inside the filter parameter. For more information, see Cluster Query Language in the Amazon Elastic Container Service Developer Guide."]
module ListClustersResponse =
  struct
    type nonrec t =
      {
      clusterArns: StringList.t option
        [@ocaml.doc
          "The list of full Amazon Resource Name (ARN) entries for each cluster that's associated with your account."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value to include in a future ListClusters request. When the results of a ListClusters request exceed maxResults, this value can be used to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?clusterArns =
      fun ?nextToken -> fun () -> { clusterArns; nextToken }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clusterArns", (Option.map x.clusterArns ~f:StringList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let clusterArns =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "clusterArns") in
      make ?nextToken ?clusterArns ()
    let of_json json =
      let nextToken = field_map json "nextToken" String_.of_json in
      let clusterArns = field_map json "clusterArns" StringList.of_json in
      make ?nextToken ?clusterArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of existing clusters."]
module ListClustersRequest =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value returned from a ListClusters request indicating that more results are available to fulfill the request and further calls are needed. If maxResults was provided, it's possible the number of results to be fewer than maxResults. This token should be treated as an opaque identifier that is only used to retrieve the next items in a list and not for other programmatic purposes."];
      maxResults: BoxedInteger.t option
        [@ocaml.doc
          "The maximum number of cluster results that ListClusters returned in paginated output. When this parameter is used, ListClusters only returns maxResults results in a single page along with a nextToken response element. The remaining results of the initial request can be seen by sending another ListClusters request with the returned nextToken value. This value can be between 1 and 100. If this parameter isn't used, then ListClusters returns up to 100 results and a nextToken value if applicable."]}
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:BoxedInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_json json =
      let maxResults = field_map json "maxResults" BoxedInteger.of_json in
      let nextToken = field_map json "nextToken" String_.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of existing clusters."]
module ListAttributesResponse =
  struct
    type nonrec t =
      {
      attributes: Attributes.t option
        [@ocaml.doc
          "A list of attribute objects that meet the criteria of the request."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value to include in a future ListAttributes request. When the results of a ListAttributes request exceed maxResults, this value can be used to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?attributes =
      fun ?nextToken -> fun () -> { attributes; nextToken }
    let error_of_json name json =
      match name with
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("attributes", (Option.map x.attributes ~f:Attributes.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let attributes =
        (Option.map ~f:Attributes.of_xml) (Xml.child xml_arg0 "attributes") in
      make ?nextToken ?attributes ()
    let of_json json =
      let nextToken = field_map json "nextToken" String_.of_json in
      let attributes = field_map json "attributes" Attributes.of_json in
      make ?nextToken ?attributes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the attributes for Amazon ECS resources within a specified target type and cluster. When you specify a target type and cluster, ListAttributes returns a list of attribute objects, one for each attribute on each resource. You can filter the list of results to a single attribute name to only return results that have that name. You can also filter the results by attribute name and value. You can do this, for example, to see which container instances in a cluster are running a Linux AMI (ecs.os-type=linux)."]
module ListAttributesRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster to list attributes. If you do not specify a cluster, the default cluster is assumed."];
      targetType: TargetType.t
        [@ocaml.doc "The type of the target to list attributes with."];
      attributeName: String_.t option
        [@ocaml.doc "The name of the attribute to filter the results with."];
      attributeValue: String_.t option
        [@ocaml.doc
          "The value of the attribute to filter results with. You must also specify an attribute name to use this parameter."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value returned from a ListAttributes request indicating that more results are available to fulfill the request and further calls are needed. If maxResults was provided, it's possible the number of results to be fewer than maxResults. This token should be treated as an opaque identifier that is only used to retrieve the next items in a list and not for other programmatic purposes."];
      maxResults: BoxedInteger.t option
        [@ocaml.doc
          "The maximum number of cluster results that ListAttributes returned in paginated output. When this parameter is used, ListAttributes only returns maxResults results in a single page along with a nextToken response element. The remaining results of the initial request can be seen by sending another ListAttributes request with the returned nextToken value. This value can be between 1 and 100. If this parameter isn't used, then ListAttributes returns up to 100 results and a nextToken value if applicable."]}
    let context_ = "ListAttributesRequest"
    let make ?cluster =
      fun ?attributeName ->
        fun ?attributeValue ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun ~targetType ->
                fun () ->
                  {
                    cluster;
                    attributeName;
                    attributeValue;
                    nextToken;
                    maxResults;
                    targetType
                  }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("targetType", (Some (TargetType.to_value x.targetType)));
        ("attributeName", (Option.map x.attributeName ~f:String_.to_value));
        ("attributeValue", (Option.map x.attributeValue ~f:String_.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:BoxedInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let attributeValue =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "attributeValue") in
      let attributeName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "attributeName") in
      let targetType =
        TargetType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "targetType") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?maxResults ?nextToken ?attributeValue ?attributeName ~targetType
        ?cluster ()
    let of_json json =
      let maxResults = field_map json "maxResults" BoxedInteger.of_json in
      let nextToken = field_map json "nextToken" String_.of_json in
      let attributeValue = field_map json "attributeValue" String_.of_json in
      let attributeName = field_map json "attributeName" String_.of_json in
      let targetType = field_map_exn json "targetType" TargetType.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?maxResults ?nextToken ?attributeValue ?attributeName ~targetType
        ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the attributes for Amazon ECS resources within a specified target type and cluster. When you specify a target type and cluster, ListAttributes returns a list of attribute objects, one for each attribute on each resource. You can filter the list of results to a single attribute name to only return results that have that name. You can also filter the results by attribute name and value. You can do this, for example, to see which container instances in a cluster are running a Linux AMI (ecs.os-type=linux)."]
module ListAccountSettingsResponse =
  struct
    type nonrec t =
      {
      settings: Settings.t option
        [@ocaml.doc "The account settings for the resource."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value to include in a future ListAccountSettings request. When the results of a ListAccountSettings request exceed maxResults, this value can be used to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?settings = fun ?nextToken -> fun () -> { settings; nextToken }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("settings", (Option.map x.settings ~f:Settings.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let settings =
        (Option.map ~f:Settings.of_xml) (Xml.child xml_arg0 "settings") in
      make ?nextToken ?settings ()
    let of_json json =
      let nextToken = field_map json "nextToken" String_.of_json in
      let settings = field_map json "settings" Settings.of_json in
      make ?nextToken ?settings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the account settings for a specified principal."]
module ListAccountSettingsRequest =
  struct
    type nonrec t =
      {
      name: SettingName.t option
        [@ocaml.doc
          "The name of the account setting you want to list the settings for."];
      value: String_.t option
        [@ocaml.doc
          "The value of the account settings to filter results with. You must also specify an account setting name to use this parameter."];
      principalArn: String_.t option
        [@ocaml.doc
          "The ARN of the principal, which can be an IAM user, IAM role, or the root user. If this field is omitted, the account settings are listed only for the authenticated user. Federated users assume the account setting of the root user and can't have explicit account settings set for them."];
      effectiveSettings: Boolean.t option
        [@ocaml.doc
          "Determines whether to return the effective settings. If true, the account settings for the root user or the default setting for the principalArn are returned. If false, the account settings for the principalArn are returned if they're set. Otherwise, no account settings are returned."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value returned from a ListAccountSettings request indicating that more results are available to fulfill the request and further calls will be needed. If maxResults was provided, it's possible the number of results to be fewer than maxResults. This token should be treated as an opaque identifier that is only used to retrieve the next items in a list and not for other programmatic purposes."];
      maxResults: Integer.t option
        [@ocaml.doc
          "The maximum number of account setting results returned by ListAccountSettings in paginated output. When this parameter is used, ListAccountSettings only returns maxResults results in a single page along with a nextToken response element. The remaining results of the initial request can be seen by sending another ListAccountSettings request with the returned nextToken value. This value can be between 1 and 10. If this parameter isn't used, then ListAccountSettings returns up to 10 results and a nextToken value if applicable."]}
    let make ?name =
      fun ?value ->
        fun ?principalArn ->
          fun ?effectiveSettings ->
            fun ?nextToken ->
              fun ?maxResults ->
                fun () ->
                  {
                    name;
                    value;
                    principalArn;
                    effectiveSettings;
                    nextToken;
                    maxResults
                  }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:SettingName.to_value));
        ("value", (Option.map x.value ~f:String_.to_value));
        ("principalArn", (Option.map x.principalArn ~f:String_.to_value));
        ("effectiveSettings",
          (Option.map x.effectiveSettings ~f:Boolean.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let effectiveSettings =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "effectiveSettings") in
      let principalArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "principalArn") in
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let name =
        (Option.map ~f:SettingName.of_xml) (Xml.child xml_arg0 "name") in
      make ?maxResults ?nextToken ?effectiveSettings ?principalArn ?value
        ?name ()
    let of_json json =
      let maxResults = field_map json "maxResults" Integer.of_json in
      let nextToken = field_map json "nextToken" String_.of_json in
      let effectiveSettings =
        field_map json "effectiveSettings" Boolean.of_json in
      let principalArn = field_map json "principalArn" String_.of_json in
      let value = field_map json "value" String_.of_json in
      let name = field_map json "name" SettingName.of_json in
      make ?maxResults ?nextToken ?effectiveSettings ?principalArn ?value
        ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the account settings for a specified principal."]
module ExecuteCommandResponse =
  struct
    type nonrec t =
      {
      clusterArn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the cluster."];
      containerArn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the container."];
      containerName: String_.t option
        [@ocaml.doc "The name of the container."];
      interactive: Boolean.t option
        [@ocaml.doc
          "Determines whether the execute command session is running in interactive mode. Amazon ECS only supports initiating interactive sessions, so you must specify true for this value."];
      session: Session.t option
        [@ocaml.doc
          "The details of the SSM session that was created for this instance of execute-command."];
      taskArn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the task."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `TargetNotConnectedException of TargetNotConnectedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?clusterArn =
      fun ?containerArn ->
        fun ?containerName ->
          fun ?interactive ->
            fun ?session ->
              fun ?taskArn ->
                fun () ->
                  {
                    clusterArn;
                    containerArn;
                    containerName;
                    interactive;
                    session;
                    taskArn
                  }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "TargetNotConnectedException" ->
          `TargetNotConnectedException
            (TargetNotConnectedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "TargetNotConnectedException" ->
          `TargetNotConnectedException
            (TargetNotConnectedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `TargetNotConnectedException e ->
          `Assoc
            [("error", (`String "TargetNotConnectedException"));
            ("details", (TargetNotConnectedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clusterArn", (Option.map x.clusterArn ~f:String_.to_value));
        ("containerArn", (Option.map x.containerArn ~f:String_.to_value));
        ("containerName", (Option.map x.containerName ~f:String_.to_value));
        ("interactive", (Option.map x.interactive ~f:Boolean.to_value));
        ("session", (Option.map x.session ~f:Session.to_value));
        ("taskArn", (Option.map x.taskArn ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskArn") in
      let session =
        (Option.map ~f:Session.of_xml) (Xml.child xml_arg0 "session") in
      let interactive =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "interactive") in
      let containerName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "containerName") in
      let containerArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "containerArn") in
      let clusterArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clusterArn") in
      make ?taskArn ?session ?interactive ?containerName ?containerArn
        ?clusterArn ()
    let of_json json =
      let taskArn = field_map json "taskArn" String_.of_json in
      let session = field_map json "session" Session.of_json in
      let interactive = field_map json "interactive" Boolean.of_json in
      let containerName = field_map json "containerName" String_.of_json in
      let containerArn = field_map json "containerArn" String_.of_json in
      let clusterArn = field_map json "clusterArn" String_.of_json in
      make ?taskArn ?session ?interactive ?containerName ?containerArn
        ?clusterArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Runs a command remotely on a container within a task."]
module ExecuteCommandRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) or short name of the cluster the task is running in. If you do not specify a cluster, the default cluster is assumed."];
      container: String_.t option
        [@ocaml.doc
          "The name of the container to execute the command on. A container name only needs to be specified for tasks containing multiple containers."];
      command: String_.t [@ocaml.doc "The command to run on the container."];
      interactive: Boolean.t
        [@ocaml.doc "Use this flag to run your command in interactive mode."];
      task: String_.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) or ID of the task the container is part of."]}
    let context_ = "ExecuteCommandRequest"
    let make ?cluster =
      fun ?container ->
        fun ~command ->
          fun ~interactive ->
            fun ~task ->
              fun () -> { cluster; container; command; interactive; task }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("container", (Option.map x.container ~f:String_.to_value));
        ("command", (Some (String_.to_value x.command)));
        ("interactive", (Some (Boolean.to_value x.interactive)));
        ("task", (Some (String_.to_value x.task)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let task =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "task") in
      let interactive =
        Boolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "interactive") in
      let command =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "command") in
      let container =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "container") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ~task ~interactive ~command ?container ?cluster ()
    let of_json json =
      let task = field_map_exn json "task" String_.of_json in
      let interactive = field_map_exn json "interactive" Boolean.of_json in
      let command = field_map_exn json "command" String_.of_json in
      let container = field_map json "container" String_.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ~task ~interactive ~command ?container ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Runs a command remotely on a container within a task."]
module DiscoverPollEndpointResponse =
  struct
    type nonrec t =
      {
      endpoint: String_.t option
        [@ocaml.doc "The endpoint for the Amazon ECS agent to poll."];
      telemetryEndpoint: String_.t option
        [@ocaml.doc "The telemetry endpoint for the Amazon ECS agent."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?endpoint =
      fun ?telemetryEndpoint -> fun () -> { endpoint; telemetryEndpoint }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("endpoint", (Option.map x.endpoint ~f:String_.to_value));
        ("telemetryEndpoint",
          (Option.map x.telemetryEndpoint ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let telemetryEndpoint =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "telemetryEndpoint") in
      let endpoint =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "endpoint") in
      make ?telemetryEndpoint ?endpoint ()
    let of_json json =
      let telemetryEndpoint =
        field_map json "telemetryEndpoint" String_.of_json in
      let endpoint = field_map json "endpoint" String_.of_json in
      make ?telemetryEndpoint ?endpoint ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is only used by the Amazon ECS agent, and it is not intended for use outside of the agent. Returns an endpoint for the Amazon ECS agent to poll for updates."]
module DiscoverPollEndpointRequest =
  struct
    type nonrec t =
      {
      containerInstance: String_.t option
        [@ocaml.doc
          "The container instance ID or full ARN of the container instance. The ARN contains the arn:aws:ecs namespace, followed by the Region of the container instance, the Amazon Web Services account ID of the container instance owner, the container-instance namespace, and then the container instance ID. For example, arn:aws:ecs:region:aws_account_id:container-instance/container_instance_ID."];
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that the container instance belongs to."]}
    let make ?containerInstance =
      fun ?cluster -> fun () -> { containerInstance; cluster }
    let to_value x =
      structure_to_value
        [("containerInstance",
           (Option.map x.containerInstance ~f:String_.to_value));
        ("cluster", (Option.map x.cluster ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      let containerInstance =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "containerInstance") in
      make ?cluster ?containerInstance ()
    let of_json json =
      let cluster = field_map json "cluster" String_.of_json in
      let containerInstance =
        field_map json "containerInstance" String_.of_json in
      make ?cluster ?containerInstance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action is only used by the Amazon ECS agent, and it is not intended for use outside of the agent. Returns an endpoint for the Amazon ECS agent to poll for updates."]
module DescribeTasksResponse =
  struct
    type nonrec t =
      {
      tasks: Tasks.t option [@ocaml.doc "The list of tasks."];
      failures: Failures.t option
        [@ocaml.doc "Any failures associated with the call."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tasks = fun ?failures -> fun () -> { tasks; failures }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("tasks", (Option.map x.tasks ~f:Tasks.to_value));
        ("failures", (Option.map x.failures ~f:Failures.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failures =
        (Option.map ~f:Failures.of_xml) (Xml.child xml_arg0 "failures") in
      let tasks = (Option.map ~f:Tasks.of_xml) (Xml.child xml_arg0 "tasks") in
      make ?failures ?tasks ()
    let of_json json =
      let failures = field_map json "failures" Failures.of_json in
      let tasks = field_map json "tasks" Tasks.of_json in
      make ?failures ?tasks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a specified task or tasks."]
module DescribeTasksRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that hosts the task or tasks to describe. If you do not specify a cluster, the default cluster is assumed. This parameter is required if the task or tasks you are describing were launched in any cluster other than the default cluster."];
      tasks: StringList.t
        [@ocaml.doc "A list of up to 100 task IDs or full ARN entries."];
      include_: TaskFieldList.t option
        [@ocaml.doc
          "Specifies whether you want to see the resource tags for the task. If TAGS is specified, the tags are included in the response. If this field is omitted, tags aren't included in the response."]}
    let context_ = "DescribeTasksRequest"
    let make ?cluster =
      fun ?include_ -> fun ~tasks -> fun () -> { cluster; include_; tasks }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("tasks", (Some (StringList.to_value x.tasks)));
        ("include", (Option.map x.include_ ~f:TaskFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let include_ =
        (Option.map ~f:TaskFieldList.of_xml) (Xml.child xml_arg0 "include") in
      let tasks =
        StringList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tasks") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?include_ ~tasks ?cluster ()
    let of_json json =
      let include_ = field_map json "include" TaskFieldList.of_json in
      let tasks = field_map_exn json "tasks" StringList.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?include_ ~tasks ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a specified task or tasks."]
module DescribeTaskSetsResponse =
  struct
    type nonrec t =
      {
      taskSets: TaskSets.t option
        [@ocaml.doc "The list of task sets described."];
      failures: Failures.t option
        [@ocaml.doc "Any failures associated with the call."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `ServiceNotActiveException of ServiceNotActiveException.t 
      | `ServiceNotFoundException of ServiceNotFoundException.t 
      | `UnsupportedFeatureException of UnsupportedFeatureException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?taskSets = fun ?failures -> fun () -> { taskSets; failures }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "ServiceNotActiveException" ->
          `ServiceNotActiveException (ServiceNotActiveException.of_json json)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_json json)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "ServiceNotActiveException" ->
          `ServiceNotActiveException (ServiceNotActiveException.of_xml xml)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_xml xml)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `ServiceNotActiveException e ->
          `Assoc
            [("error", (`String "ServiceNotActiveException"));
            ("details", (ServiceNotActiveException.to_json e))]
      | `ServiceNotFoundException e ->
          `Assoc
            [("error", (`String "ServiceNotFoundException"));
            ("details", (ServiceNotFoundException.to_json e))]
      | `UnsupportedFeatureException e ->
          `Assoc
            [("error", (`String "UnsupportedFeatureException"));
            ("details", (UnsupportedFeatureException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("taskSets", (Option.map x.taskSets ~f:TaskSets.to_value));
        ("failures", (Option.map x.failures ~f:Failures.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failures =
        (Option.map ~f:Failures.of_xml) (Xml.child xml_arg0 "failures") in
      let taskSets =
        (Option.map ~f:TaskSets.of_xml) (Xml.child xml_arg0 "taskSets") in
      make ?failures ?taskSets ()
    let of_json json =
      let failures = field_map json "failures" Failures.of_json in
      let taskSets = field_map json "taskSets" TaskSets.of_json in
      make ?failures ?taskSets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the task sets in the specified cluster and service. This is used when a service uses the EXTERNAL deployment controller type. For more information, see Amazon ECS Deployment Types in the Amazon Elastic Container Service Developer Guide."]
module DescribeTaskSetsRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service that the task sets exist in."];
      service: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the service that the task sets exist in."];
      taskSets: StringList.t option
        [@ocaml.doc
          "The ID or full Amazon Resource Name (ARN) of task sets to describe."];
      include_: TaskSetFieldList.t option
        [@ocaml.doc
          "Specifies whether to see the resource tags for the task set. If TAGS is specified, the tags are included in the response. If this field is omitted, tags aren't included in the response."]}
    let context_ = "DescribeTaskSetsRequest"
    let make ?taskSets =
      fun ?include_ ->
        fun ~cluster ->
          fun ~service -> fun () -> { taskSets; include_; cluster; service }
    let to_value x =
      structure_to_value
        [("cluster", (Some (String_.to_value x.cluster)));
        ("service", (Some (String_.to_value x.service)));
        ("taskSets", (Option.map x.taskSets ~f:StringList.to_value));
        ("include", (Option.map x.include_ ~f:TaskSetFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let include_ =
        (Option.map ~f:TaskSetFieldList.of_xml)
          (Xml.child xml_arg0 "include") in
      let taskSets =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "taskSets") in
      let service =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "service") in
      let cluster =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "cluster") in
      make ?include_ ?taskSets ~service ~cluster ()
    let of_json json =
      let include_ = field_map json "include" TaskSetFieldList.of_json in
      let taskSets = field_map json "taskSets" StringList.of_json in
      let service = field_map_exn json "service" String_.of_json in
      let cluster = field_map_exn json "cluster" String_.of_json in
      make ?include_ ?taskSets ~service ~cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the task sets in the specified cluster and service. This is used when a service uses the EXTERNAL deployment controller type. For more information, see Amazon ECS Deployment Types in the Amazon Elastic Container Service Developer Guide."]
module DescribeTaskDefinitionResponse =
  struct
    type nonrec t =
      {
      taskDefinition: TaskDefinition.t option
        [@ocaml.doc "The full task definition description."];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that's applied to the task definition to help you categorize and organize them. Each tag consists of a key and an optional value. You define both. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?taskDefinition =
      fun ?tags -> fun () -> { taskDefinition; tags }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("taskDefinition",
           (Option.map x.taskDefinition ~f:TaskDefinition.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let taskDefinition =
        (Option.map ~f:TaskDefinition.of_xml)
          (Xml.child xml_arg0 "taskDefinition") in
      make ?tags ?taskDefinition ()
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let taskDefinition =
        field_map json "taskDefinition" TaskDefinition.of_json in
      make ?tags ?taskDefinition ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a task definition. You can specify a family and revision to find information about a specific task definition, or you can simply specify the family to find the latest ACTIVE revision in that family. You can only describe INACTIVE task definitions while an active task or service references them."]
module DescribeTaskDefinitionRequest =
  struct
    type nonrec t =
      {
      taskDefinition: String_.t
        [@ocaml.doc
          "The family for the latest ACTIVE revision, family and revision (family:revision) for a specific revision in the family, or full Amazon Resource Name (ARN) of the task definition to describe."];
      include_: TaskDefinitionFieldList.t option
        [@ocaml.doc
          "Determines whether to see the resource tags for the task definition. If TAGS is specified, the tags are included in the response. If this field is omitted, tags aren't included in the response."]}
    let context_ = "DescribeTaskDefinitionRequest"
    let make ?include_ =
      fun ~taskDefinition -> fun () -> { include_; taskDefinition }
    let to_value x =
      structure_to_value
        [("taskDefinition", (Some (String_.to_value x.taskDefinition)));
        ("include",
          (Option.map x.include_ ~f:TaskDefinitionFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let include_ =
        (Option.map ~f:TaskDefinitionFieldList.of_xml)
          (Xml.child xml_arg0 "include") in
      let taskDefinition =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "taskDefinition") in
      make ?include_ ~taskDefinition ()
    let of_json json =
      let include_ = field_map json "include" TaskDefinitionFieldList.of_json in
      let taskDefinition =
        field_map_exn json "taskDefinition" String_.of_json in
      make ?include_ ~taskDefinition ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a task definition. You can specify a family and revision to find information about a specific task definition, or you can simply specify the family to find the latest ACTIVE revision in that family. You can only describe INACTIVE task definitions while an active task or service references them."]
module DescribeServicesResponse =
  struct
    type nonrec t =
      {
      services: Services.t option
        [@ocaml.doc "The list of services described."];
      failures: Failures.t option
        [@ocaml.doc "Any failures associated with the call."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?services = fun ?failures -> fun () -> { services; failures }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("services", (Option.map x.services ~f:Services.to_value));
        ("failures", (Option.map x.failures ~f:Failures.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failures =
        (Option.map ~f:Failures.of_xml) (Xml.child xml_arg0 "failures") in
      let services =
        (Option.map ~f:Services.of_xml) (Xml.child xml_arg0 "services") in
      make ?failures ?services ()
    let of_json json =
      let failures = field_map json "failures" Failures.of_json in
      let services = field_map json "services" Services.of_json in
      make ?failures ?services ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified services running in your cluster."]
module DescribeServicesRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN)the cluster that hosts the service to describe. If you do not specify a cluster, the default cluster is assumed. This parameter is required if the service or services you are describing were launched in any cluster other than the default cluster."];
      services: StringList.t
        [@ocaml.doc
          "A list of services to describe. You may specify up to 10 services to describe in a single operation."];
      include_: ServiceFieldList.t option
        [@ocaml.doc
          "Determines whether you want to see the resource tags for the service. If TAGS is specified, the tags are included in the response. If this field is omitted, tags aren't included in the response."]}
    let context_ = "DescribeServicesRequest"
    let make ?cluster =
      fun ?include_ ->
        fun ~services -> fun () -> { cluster; include_; services }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("services", (Some (StringList.to_value x.services)));
        ("include", (Option.map x.include_ ~f:ServiceFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let include_ =
        (Option.map ~f:ServiceFieldList.of_xml)
          (Xml.child xml_arg0 "include") in
      let services =
        StringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "services") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?include_ ~services ?cluster ()
    let of_json json =
      let include_ = field_map json "include" ServiceFieldList.of_json in
      let services = field_map_exn json "services" StringList.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?include_ ~services ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the specified services running in your cluster."]
module DescribeContainerInstancesResponse =
  struct
    type nonrec t =
      {
      containerInstances: ContainerInstances.t option
        [@ocaml.doc "The list of container instances."];
      failures: Failures.t option
        [@ocaml.doc "Any failures associated with the call."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerInstances =
      fun ?failures -> fun () -> { containerInstances; failures }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("containerInstances",
           (Option.map x.containerInstances ~f:ContainerInstances.to_value));
        ("failures", (Option.map x.failures ~f:Failures.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failures =
        (Option.map ~f:Failures.of_xml) (Xml.child xml_arg0 "failures") in
      let containerInstances =
        (Option.map ~f:ContainerInstances.of_xml)
          (Xml.child xml_arg0 "containerInstances") in
      make ?failures ?containerInstances ()
    let of_json json =
      let failures = field_map json "failures" Failures.of_json in
      let containerInstances =
        field_map json "containerInstances" ContainerInstances.of_json in
      make ?failures ?containerInstances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more container instances. Returns metadata about each container instance requested."]
module DescribeContainerInstancesRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that hosts the container instances to describe. If you do not specify a cluster, the default cluster is assumed. This parameter is required if the container instance or container instances you are describing were launched in any cluster other than the default cluster."];
      containerInstances: StringList.t
        [@ocaml.doc
          "A list of up to 100 container instance IDs or full Amazon Resource Name (ARN) entries."];
      include_: ContainerInstanceFieldList.t option
        [@ocaml.doc
          "Specifies whether you want to see the resource tags for the container instance. If TAGS is specified, the tags are included in the response. If CONTAINER_INSTANCE_HEALTH is specified, the container instance health is included in the response. If this field is omitted, tags and container instance health status aren't included in the response."]}
    let context_ = "DescribeContainerInstancesRequest"
    let make ?cluster =
      fun ?include_ ->
        fun ~containerInstances ->
          fun () -> { cluster; include_; containerInstances }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("containerInstances",
          (Some (StringList.to_value x.containerInstances)));
        ("include",
          (Option.map x.include_ ~f:ContainerInstanceFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let include_ =
        (Option.map ~f:ContainerInstanceFieldList.of_xml)
          (Xml.child xml_arg0 "include") in
      let containerInstances =
        StringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "containerInstances") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?include_ ~containerInstances ?cluster ()
    let of_json json =
      let include_ =
        field_map json "include" ContainerInstanceFieldList.of_json in
      let containerInstances =
        field_map_exn json "containerInstances" StringList.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?include_ ~containerInstances ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes one or more container instances. Returns metadata about each container instance requested."]
module DescribeClustersResponse =
  struct
    type nonrec t =
      {
      clusters: Clusters.t option [@ocaml.doc "The list of clusters."];
      failures: Failures.t option
        [@ocaml.doc "Any failures associated with the call."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?clusters = fun ?failures -> fun () -> { clusters; failures }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("clusters", (Option.map x.clusters ~f:Clusters.to_value));
        ("failures", (Option.map x.failures ~f:Failures.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failures =
        (Option.map ~f:Failures.of_xml) (Xml.child xml_arg0 "failures") in
      let clusters =
        (Option.map ~f:Clusters.of_xml) (Xml.child xml_arg0 "clusters") in
      make ?failures ?clusters ()
    let of_json json =
      let failures = field_map json "failures" Failures.of_json in
      let clusters = field_map json "clusters" Clusters.of_json in
      make ?failures ?clusters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your clusters."]
module DescribeClustersRequest =
  struct
    type nonrec t =
      {
      clusters: StringList.t option
        [@ocaml.doc
          "A list of up to 100 cluster names or full cluster Amazon Resource Name (ARN) entries. If you do not specify a cluster, the default cluster is assumed."];
      include_: ClusterFieldList.t option
        [@ocaml.doc
          "Determines whether to include additional information about the clusters in the response. If this field is omitted, this information isn't included. If ATTACHMENTS is specified, the attachments for the container instances or tasks within the cluster are included. If SETTINGS is specified, the settings for the cluster are included. If CONFIGURATIONS is specified, the configuration for the cluster is included. If STATISTICS is specified, the task and service count is included, separated by launch type. If TAGS is specified, the metadata tags associated with the cluster are included."]}
    let make ?clusters = fun ?include_ -> fun () -> { clusters; include_ }
    let to_value x =
      structure_to_value
        [("clusters", (Option.map x.clusters ~f:StringList.to_value));
        ("include", (Option.map x.include_ ~f:ClusterFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let include_ =
        (Option.map ~f:ClusterFieldList.of_xml)
          (Xml.child xml_arg0 "include") in
      let clusters =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "clusters") in
      make ?include_ ?clusters ()
    let of_json json =
      let include_ = field_map json "include" ClusterFieldList.of_json in
      let clusters = field_map json "clusters" StringList.of_json in
      make ?include_ ?clusters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your clusters."]
module DescribeCapacityProvidersResponse =
  struct
    type nonrec t =
      {
      capacityProviders: CapacityProviders.t option
        [@ocaml.doc "The list of capacity providers."];
      failures: Failures.t option
        [@ocaml.doc "Any failures associated with the call."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value to include in a future DescribeCapacityProviders request. When the results of a DescribeCapacityProviders request exceed maxResults, this value can be used to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?capacityProviders =
      fun ?failures ->
        fun ?nextToken ->
          fun () -> { capacityProviders; failures; nextToken }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityProviders",
           (Option.map x.capacityProviders ~f:CapacityProviders.to_value));
        ("failures", (Option.map x.failures ~f:Failures.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let failures =
        (Option.map ~f:Failures.of_xml) (Xml.child xml_arg0 "failures") in
      let capacityProviders =
        (Option.map ~f:CapacityProviders.of_xml)
          (Xml.child xml_arg0 "capacityProviders") in
      make ?nextToken ?failures ?capacityProviders ()
    let of_json json =
      let nextToken = field_map json "nextToken" String_.of_json in
      let failures = field_map json "failures" Failures.of_json in
      let capacityProviders =
        field_map json "capacityProviders" CapacityProviders.of_json in
      make ?nextToken ?failures ?capacityProviders ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your capacity providers."]
module DescribeCapacityProvidersRequest =
  struct
    type nonrec t =
      {
      capacityProviders: StringList.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of one or more capacity providers. Up to 100 capacity providers can be described in an action."];
      include_: CapacityProviderFieldList.t option
        [@ocaml.doc
          "Specifies whether or not you want to see the resource tags for the capacity provider. If TAGS is specified, the tags are included in the response. If this field is omitted, tags aren't included in the response."];
      maxResults: BoxedInteger.t option
        [@ocaml.doc
          "The maximum number of account setting results returned by DescribeCapacityProviders in paginated output. When this parameter is used, DescribeCapacityProviders only returns maxResults results in a single page along with a nextToken response element. The remaining results of the initial request can be seen by sending another DescribeCapacityProviders request with the returned nextToken value. This value can be between 1 and 10. If this parameter is not used, then DescribeCapacityProviders returns up to 10 results and a nextToken value if applicable."];
      nextToken: String_.t option
        [@ocaml.doc
          "The nextToken value returned from a previous paginated DescribeCapacityProviders request where maxResults was used and the results exceeded the value of that parameter. Pagination continues from the end of the previous results that returned the nextToken value. This token should be treated as an opaque identifier that is only used to retrieve the next items in a list and not for other programmatic purposes."]}
    let make ?capacityProviders =
      fun ?include_ ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () -> { capacityProviders; include_; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("capacityProviders",
           (Option.map x.capacityProviders ~f:StringList.to_value));
        ("include",
          (Option.map x.include_ ~f:CapacityProviderFieldList.to_value));
        ("maxResults", (Option.map x.maxResults ~f:BoxedInteger.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:BoxedInteger.of_xml) (Xml.child xml_arg0 "maxResults") in
      let include_ =
        (Option.map ~f:CapacityProviderFieldList.of_xml)
          (Xml.child xml_arg0 "include") in
      let capacityProviders =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "capacityProviders") in
      make ?nextToken ?maxResults ?include_ ?capacityProviders ()
    let of_json json =
      let nextToken = field_map json "nextToken" String_.of_json in
      let maxResults = field_map json "maxResults" BoxedInteger.of_json in
      let include_ =
        field_map json "include" CapacityProviderFieldList.of_json in
      let capacityProviders =
        field_map json "capacityProviders" StringList.of_json in
      make ?nextToken ?maxResults ?include_ ?capacityProviders ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes one or more of your capacity providers."]
module DeregisterTaskDefinitionResponse =
  struct
    type nonrec t =
      {
      taskDefinition: TaskDefinition.t option
        [@ocaml.doc "The full description of the deregistered task."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?taskDefinition = fun () -> { taskDefinition }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("taskDefinition",
           (Option.map x.taskDefinition ~f:TaskDefinition.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskDefinition =
        (Option.map ~f:TaskDefinition.of_xml)
          (Xml.child xml_arg0 "taskDefinition") in
      make ?taskDefinition ()
    let of_json json =
      let taskDefinition =
        field_map json "taskDefinition" TaskDefinition.of_json in
      make ?taskDefinition ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deregisters the specified task definition by family and revision. Upon deregistration, the task definition is marked as INACTIVE. Existing tasks and services that reference an INACTIVE task definition continue to run without disruption. Existing services that reference an INACTIVE task definition can still scale up or down by modifying the service's desired count. You can't use an INACTIVE task definition to run new tasks or create new services, and you can't update an existing service to reference an INACTIVE task definition. However, there may be up to a 10-minute window following deregistration where these restrictions have not yet taken effect. At this time, INACTIVE task definitions remain discoverable in your account indefinitely. However, this behavior is subject to change in the future. We don't recommend that you rely on INACTIVE task definitions persisting beyond the lifecycle of any associated tasks and services."]
module DeregisterTaskDefinitionRequest =
  struct
    type nonrec t =
      {
      taskDefinition: String_.t
        [@ocaml.doc
          "The family and revision (family:revision) or full Amazon Resource Name (ARN) of the task definition to deregister. You must specify a revision."]}
    let context_ = "DeregisterTaskDefinitionRequest"
    let make ~taskDefinition = fun () -> { taskDefinition }
    let to_value x =
      structure_to_value
        [("taskDefinition", (Some (String_.to_value x.taskDefinition)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskDefinition =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "taskDefinition") in
      make ~taskDefinition ()
    let of_json json =
      let taskDefinition =
        field_map_exn json "taskDefinition" String_.of_json in
      make ~taskDefinition ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deregisters the specified task definition by family and revision. Upon deregistration, the task definition is marked as INACTIVE. Existing tasks and services that reference an INACTIVE task definition continue to run without disruption. Existing services that reference an INACTIVE task definition can still scale up or down by modifying the service's desired count. You can't use an INACTIVE task definition to run new tasks or create new services, and you can't update an existing service to reference an INACTIVE task definition. However, there may be up to a 10-minute window following deregistration where these restrictions have not yet taken effect. At this time, INACTIVE task definitions remain discoverable in your account indefinitely. However, this behavior is subject to change in the future. We don't recommend that you rely on INACTIVE task definitions persisting beyond the lifecycle of any associated tasks and services."]
module DeregisterContainerInstanceResponse =
  struct
    type nonrec t =
      {
      containerInstance: ContainerInstance.t option
        [@ocaml.doc "The container instance that was deregistered."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?containerInstance = fun () -> { containerInstance }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("containerInstance",
           (Option.map x.containerInstance ~f:ContainerInstance.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerInstance =
        (Option.map ~f:ContainerInstance.of_xml)
          (Xml.child xml_arg0 "containerInstance") in
      make ?containerInstance ()
    let of_json json =
      let containerInstance =
        field_map json "containerInstance" ContainerInstance.of_json in
      make ?containerInstance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deregisters an Amazon ECS container instance from the specified cluster. This instance is no longer available to run tasks. If you intend to use the container instance for some other purpose after deregistration, we recommend that you stop all of the tasks running on the container instance before deregistration. That prevents any orphaned tasks from consuming resources. Deregistering a container instance removes the instance from a cluster, but it doesn't terminate the EC2 instance. If you are finished using the instance, be sure to terminate it in the Amazon EC2 console to stop billing. If you terminate a running container instance, Amazon ECS automatically deregisters the instance from your cluster (stopped container instances or instances with disconnected agents aren't automatically deregistered when terminated)."]
module DeregisterContainerInstanceRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that hosts the container instance to deregister. If you do not specify a cluster, the default cluster is assumed."];
      containerInstance: String_.t
        [@ocaml.doc
          "The container instance ID or full ARN of the container instance to deregister. The ARN contains the arn:aws:ecs namespace, followed by the Region of the container instance, the Amazon Web Services account ID of the container instance owner, the container-instance namespace, and then the container instance ID. For example, arn:aws:ecs:region:aws_account_id:container-instance/container_instance_ID."];
      force: BoxedBoolean.t option
        [@ocaml.doc
          "Forces the container instance to be deregistered. If you have tasks running on the container instance when you deregister it with the force option, these tasks remain running until you terminate the instance or the tasks stop through some other means, but they're orphaned (no longer monitored or accounted for by Amazon ECS). If an orphaned task on your container instance is part of an Amazon ECS service, then the service scheduler starts another copy of that task, on a different container instance if possible. Any containers in orphaned service tasks that are registered with a Classic Load Balancer or an Application Load Balancer target group are deregistered. They begin connection draining according to the settings on the load balancer or target group."]}
    let context_ = "DeregisterContainerInstanceRequest"
    let make ?cluster =
      fun ?force ->
        fun ~containerInstance ->
          fun () -> { cluster; force; containerInstance }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("containerInstance", (Some (String_.to_value x.containerInstance)));
        ("force", (Option.map x.force ~f:BoxedBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let force =
        (Option.map ~f:BoxedBoolean.of_xml) (Xml.child xml_arg0 "force") in
      let containerInstance =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "containerInstance") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?force ~containerInstance ?cluster ()
    let of_json json =
      let force = field_map json "force" BoxedBoolean.of_json in
      let containerInstance =
        field_map_exn json "containerInstance" String_.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?force ~containerInstance ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deregisters an Amazon ECS container instance from the specified cluster. This instance is no longer available to run tasks. If you intend to use the container instance for some other purpose after deregistration, we recommend that you stop all of the tasks running on the container instance before deregistration. That prevents any orphaned tasks from consuming resources. Deregistering a container instance removes the instance from a cluster, but it doesn't terminate the EC2 instance. If you are finished using the instance, be sure to terminate it in the Amazon EC2 console to stop billing. If you terminate a running container instance, Amazon ECS automatically deregisters the instance from your cluster (stopped container instances or instances with disconnected agents aren't automatically deregistered when terminated)."]
module DeleteTaskSetResponse =
  struct
    type nonrec t =
      {
      taskSet: TaskSet.t option [@ocaml.doc "Details about the task set."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `ServiceNotActiveException of ServiceNotActiveException.t 
      | `ServiceNotFoundException of ServiceNotFoundException.t 
      | `TaskSetNotFoundException of TaskSetNotFoundException.t 
      | `UnsupportedFeatureException of UnsupportedFeatureException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?taskSet = fun () -> { taskSet }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "ServiceNotActiveException" ->
          `ServiceNotActiveException (ServiceNotActiveException.of_json json)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_json json)
      | "TaskSetNotFoundException" ->
          `TaskSetNotFoundException (TaskSetNotFoundException.of_json json)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "ServiceNotActiveException" ->
          `ServiceNotActiveException (ServiceNotActiveException.of_xml xml)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_xml xml)
      | "TaskSetNotFoundException" ->
          `TaskSetNotFoundException (TaskSetNotFoundException.of_xml xml)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `ServiceNotActiveException e ->
          `Assoc
            [("error", (`String "ServiceNotActiveException"));
            ("details", (ServiceNotActiveException.to_json e))]
      | `ServiceNotFoundException e ->
          `Assoc
            [("error", (`String "ServiceNotFoundException"));
            ("details", (ServiceNotFoundException.to_json e))]
      | `TaskSetNotFoundException e ->
          `Assoc
            [("error", (`String "TaskSetNotFoundException"));
            ("details", (TaskSetNotFoundException.to_json e))]
      | `UnsupportedFeatureException e ->
          `Assoc
            [("error", (`String "UnsupportedFeatureException"));
            ("details", (UnsupportedFeatureException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("taskSet", (Option.map x.taskSet ~f:TaskSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskSet =
        (Option.map ~f:TaskSet.of_xml) (Xml.child xml_arg0 "taskSet") in
      make ?taskSet ()
    let of_json json =
      let taskSet = field_map json "taskSet" TaskSet.of_json in
      make ?taskSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a specified task set within a service. This is used when a service uses the EXTERNAL deployment controller type. For more information, see Amazon ECS Deployment Types in the Amazon Elastic Container Service Developer Guide."]
module DeleteTaskSetRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service that the task set found in to delete."];
      service: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the service that hosts the task set to delete."];
      taskSet: String_.t
        [@ocaml.doc
          "The task set ID or full Amazon Resource Name (ARN) of the task set to delete."];
      force: BoxedBoolean.t option
        [@ocaml.doc
          "If true, you can delete a task set even if it hasn't been scaled down to zero."]}
    let context_ = "DeleteTaskSetRequest"
    let make ?force =
      fun ~cluster ->
        fun ~service ->
          fun ~taskSet -> fun () -> { force; cluster; service; taskSet }
    let to_value x =
      structure_to_value
        [("cluster", (Some (String_.to_value x.cluster)));
        ("service", (Some (String_.to_value x.service)));
        ("taskSet", (Some (String_.to_value x.taskSet)));
        ("force", (Option.map x.force ~f:BoxedBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let force =
        (Option.map ~f:BoxedBoolean.of_xml) (Xml.child xml_arg0 "force") in
      let taskSet =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "taskSet") in
      let service =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "service") in
      let cluster =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "cluster") in
      make ?force ~taskSet ~service ~cluster ()
    let of_json json =
      let force = field_map json "force" BoxedBoolean.of_json in
      let taskSet = field_map_exn json "taskSet" String_.of_json in
      let service = field_map_exn json "service" String_.of_json in
      let cluster = field_map_exn json "cluster" String_.of_json in
      make ?force ~taskSet ~service ~cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a specified task set within a service. This is used when a service uses the EXTERNAL deployment controller type. For more information, see Amazon ECS Deployment Types in the Amazon Elastic Container Service Developer Guide."]
module DeleteServiceResponse =
  struct
    type nonrec t =
      {
      service: Service.t option
        [@ocaml.doc "The full description of the deleted service."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `ServiceNotFoundException of ServiceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?service = fun () -> { service }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `ServiceNotFoundException e ->
          `Assoc
            [("error", (`String "ServiceNotFoundException"));
            ("details", (ServiceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("service", (Option.map x.service ~f:Service.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let service =
        (Option.map ~f:Service.of_xml) (Xml.child xml_arg0 "service") in
      make ?service ()
    let of_json json =
      let service = field_map json "service" Service.of_json in
      make ?service ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a specified service within a cluster. You can delete a service if you have no running tasks in it and the desired task count is zero. If the service is actively maintaining tasks, you can't delete it, and you must update the service to a desired task count of zero. For more information, see UpdateService. When you delete a service, if there are still running tasks that require cleanup, the service status moves from ACTIVE to DRAINING, and the service is no longer visible in the console or in the ListServices API operation. After all tasks have transitioned to either STOPPING or STOPPED status, the service status moves from DRAINING to INACTIVE. Services in the DRAINING or INACTIVE status can still be viewed with the DescribeServices API operation. However, in the future, INACTIVE services may be cleaned up and purged from Amazon ECS record keeping, and DescribeServices calls on those services return a ServiceNotFoundException error. If you attempt to create a new service with the same name as an existing service in either ACTIVE or DRAINING status, you receive an error."]
module DeleteServiceRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service to delete. If you do not specify a cluster, the default cluster is assumed."];
      service: String_.t [@ocaml.doc "The name of the service to delete."];
      force: BoxedBoolean.t option
        [@ocaml.doc
          "If true, allows you to delete a service even if it wasn't scaled down to zero tasks. It's only necessary to use this if the service uses the REPLICA scheduling strategy."]}
    let context_ = "DeleteServiceRequest"
    let make ?cluster =
      fun ?force -> fun ~service -> fun () -> { cluster; force; service }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("service", (Some (String_.to_value x.service)));
        ("force", (Option.map x.force ~f:BoxedBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let force =
        (Option.map ~f:BoxedBoolean.of_xml) (Xml.child xml_arg0 "force") in
      let service =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "service") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?force ~service ?cluster ()
    let of_json json =
      let force = field_map json "force" BoxedBoolean.of_json in
      let service = field_map_exn json "service" String_.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?force ~service ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a specified service within a cluster. You can delete a service if you have no running tasks in it and the desired task count is zero. If the service is actively maintaining tasks, you can't delete it, and you must update the service to a desired task count of zero. For more information, see UpdateService. When you delete a service, if there are still running tasks that require cleanup, the service status moves from ACTIVE to DRAINING, and the service is no longer visible in the console or in the ListServices API operation. After all tasks have transitioned to either STOPPING or STOPPED status, the service status moves from DRAINING to INACTIVE. Services in the DRAINING or INACTIVE status can still be viewed with the DescribeServices API operation. However, in the future, INACTIVE services may be cleaned up and purged from Amazon ECS record keeping, and DescribeServices calls on those services return a ServiceNotFoundException error. If you attempt to create a new service with the same name as an existing service in either ACTIVE or DRAINING status, you receive an error."]
module DeleteClusterResponse =
  struct
    type nonrec t =
      {
      cluster: Cluster.t option
        [@ocaml.doc "The full description of the deleted cluster."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `ClusterContainsContainerInstancesException of
          ClusterContainsContainerInstancesException.t 
      | `ClusterContainsServicesException of
          ClusterContainsServicesException.t 
      | `ClusterContainsTasksException of ClusterContainsTasksException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `UpdateInProgressException of UpdateInProgressException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?cluster = fun () -> { cluster }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterContainsContainerInstancesException" ->
          `ClusterContainsContainerInstancesException
            (ClusterContainsContainerInstancesException.of_json json)
      | "ClusterContainsServicesException" ->
          `ClusterContainsServicesException
            (ClusterContainsServicesException.of_json json)
      | "ClusterContainsTasksException" ->
          `ClusterContainsTasksException
            (ClusterContainsTasksException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "UpdateInProgressException" ->
          `UpdateInProgressException (UpdateInProgressException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterContainsContainerInstancesException" ->
          `ClusterContainsContainerInstancesException
            (ClusterContainsContainerInstancesException.of_xml xml)
      | "ClusterContainsServicesException" ->
          `ClusterContainsServicesException
            (ClusterContainsServicesException.of_xml xml)
      | "ClusterContainsTasksException" ->
          `ClusterContainsTasksException
            (ClusterContainsTasksException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "UpdateInProgressException" ->
          `UpdateInProgressException (UpdateInProgressException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterContainsContainerInstancesException e ->
          `Assoc
            [("error",
               (`String "ClusterContainsContainerInstancesException"));
            ("details",
              (ClusterContainsContainerInstancesException.to_json e))]
      | `ClusterContainsServicesException e ->
          `Assoc
            [("error", (`String "ClusterContainsServicesException"));
            ("details", (ClusterContainsServicesException.to_json e))]
      | `ClusterContainsTasksException e ->
          `Assoc
            [("error", (`String "ClusterContainsTasksException"));
            ("details", (ClusterContainsTasksException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `UpdateInProgressException e ->
          `Assoc
            [("error", (`String "UpdateInProgressException"));
            ("details", (UpdateInProgressException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:Cluster.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cluster =
        (Option.map ~f:Cluster.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?cluster ()
    let of_json json =
      let cluster = field_map json "cluster" Cluster.of_json in
      make ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified cluster. The cluster transitions to the INACTIVE state. Clusters with an INACTIVE status might remain discoverable in your account for a period of time. However, this behavior is subject to change in the future. We don't recommend that you rely on INACTIVE clusters persisting. You must deregister all container instances from this cluster before you may delete it. You can list the container instances in a cluster with ListContainerInstances and deregister them with DeregisterContainerInstance."]
module DeleteClusterRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster to delete."]}
    let context_ = "DeleteClusterRequest"
    let make ~cluster = fun () -> { cluster }
    let to_value x =
      structure_to_value [("cluster", (Some (String_.to_value x.cluster)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cluster =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "cluster") in
      make ~cluster ()
    let of_json json =
      let cluster = field_map_exn json "cluster" String_.of_json in
      make ~cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified cluster. The cluster transitions to the INACTIVE state. Clusters with an INACTIVE status might remain discoverable in your account for a period of time. However, this behavior is subject to change in the future. We don't recommend that you rely on INACTIVE clusters persisting. You must deregister all container instances from this cluster before you may delete it. You can list the container instances in a cluster with ListContainerInstances and deregister them with DeregisterContainerInstance."]
module DeleteCapacityProviderResponse =
  struct
    type nonrec t =
      {
      capacityProvider: CapacityProvider.t option
        [@ocaml.doc "The details of the capacity provider."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?capacityProvider = fun () -> { capacityProvider }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityProvider",
           (Option.map x.capacityProvider ~f:CapacityProvider.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityProvider =
        (Option.map ~f:CapacityProvider.of_xml)
          (Xml.child xml_arg0 "capacityProvider") in
      make ?capacityProvider ()
    let of_json json =
      let capacityProvider =
        field_map json "capacityProvider" CapacityProvider.of_json in
      make ?capacityProvider ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified capacity provider. The FARGATE and FARGATE_SPOT capacity providers are reserved and can't be deleted. You can disassociate them from a cluster using either the PutClusterCapacityProviders API or by deleting the cluster. Prior to a capacity provider being deleted, the capacity provider must be removed from the capacity provider strategy from all services. The UpdateService API can be used to remove a capacity provider from a service's capacity provider strategy. When updating a service, the forceNewDeployment option can be used to ensure that any tasks using the Amazon EC2 instance capacity provided by the capacity provider are transitioned to use the capacity from the remaining capacity providers. Only capacity providers that aren't associated with a cluster can be deleted. To remove a capacity provider from a cluster, you can either use PutClusterCapacityProviders or delete the cluster."]
module DeleteCapacityProviderRequest =
  struct
    type nonrec t =
      {
      capacityProvider: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the capacity provider to delete."]}
    let context_ = "DeleteCapacityProviderRequest"
    let make ~capacityProvider = fun () -> { capacityProvider }
    let to_value x =
      structure_to_value
        [("capacityProvider", (Some (String_.to_value x.capacityProvider)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityProvider =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "capacityProvider") in
      make ~capacityProvider ()
    let of_json json =
      let capacityProvider =
        field_map_exn json "capacityProvider" String_.of_json in
      make ~capacityProvider ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified capacity provider. The FARGATE and FARGATE_SPOT capacity providers are reserved and can't be deleted. You can disassociate them from a cluster using either the PutClusterCapacityProviders API or by deleting the cluster. Prior to a capacity provider being deleted, the capacity provider must be removed from the capacity provider strategy from all services. The UpdateService API can be used to remove a capacity provider from a service's capacity provider strategy. When updating a service, the forceNewDeployment option can be used to ensure that any tasks using the Amazon EC2 instance capacity provided by the capacity provider are transitioned to use the capacity from the remaining capacity providers. Only capacity providers that aren't associated with a cluster can be deleted. To remove a capacity provider from a cluster, you can either use PutClusterCapacityProviders or delete the cluster."]
module DeleteAttributesResponse =
  struct
    type nonrec t =
      {
      attributes: Attributes.t option
        [@ocaml.doc
          "A list of attribute objects that were successfully deleted from your resource."]}
    type nonrec error =
      [ `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `TargetNotFoundException of TargetNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?attributes = fun () -> { attributes }
    let error_of_json name json =
      match name with
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "TargetNotFoundException" ->
          `TargetNotFoundException (TargetNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "TargetNotFoundException" ->
          `TargetNotFoundException (TargetNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `TargetNotFoundException e ->
          `Assoc
            [("error", (`String "TargetNotFoundException"));
            ("details", (TargetNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("attributes", (Option.map x.attributes ~f:Attributes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributes =
        (Option.map ~f:Attributes.of_xml) (Xml.child xml_arg0 "attributes") in
      make ?attributes ()
    let of_json json =
      let attributes = field_map json "attributes" Attributes.of_json in
      make ?attributes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes one or more custom attributes from an Amazon ECS resource."]
module DeleteAttributesRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that contains the resource to delete attributes. If you do not specify a cluster, the default cluster is assumed."];
      attributes: Attributes.t
        [@ocaml.doc
          "The attributes to delete from your resource. You can specify up to 10 attributes for each request. For custom attributes, specify the attribute name and target ID, but don't specify the value. If you specify the target ID using the short form, you must also specify the target type."]}
    let context_ = "DeleteAttributesRequest"
    let make ?cluster = fun ~attributes -> fun () -> { cluster; attributes }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("attributes", (Some (Attributes.to_value x.attributes)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributes =
        Attributes.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "attributes") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ~attributes ?cluster ()
    let of_json json =
      let attributes = field_map_exn json "attributes" Attributes.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ~attributes ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes one or more custom attributes from an Amazon ECS resource."]
module DeleteAccountSettingResponse =
  struct
    type nonrec t =
      {
      setting: Setting.t option
        [@ocaml.doc "The account setting for the specified principal ARN."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?setting = fun () -> { setting }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("setting", (Option.map x.setting ~f:Setting.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let setting =
        (Option.map ~f:Setting.of_xml) (Xml.child xml_arg0 "setting") in
      make ?setting ()
    let of_json json =
      let setting = field_map json "setting" Setting.of_json in
      make ?setting ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Disables an account setting for a specified IAM user, IAM role, or the root user for an account."]
module DeleteAccountSettingRequest =
  struct
    type nonrec t =
      {
      name: SettingName.t
        [@ocaml.doc
          "The resource name to disable the account setting for. If serviceLongArnFormat is specified, the ARN for your Amazon ECS services is affected. If taskLongArnFormat is specified, the ARN and resource ID for your Amazon ECS tasks is affected. If containerInstanceLongArnFormat is specified, the ARN and resource ID for your Amazon ECS container instances is affected. If awsvpcTrunking is specified, the ENI limit for your Amazon ECS container instances is affected."];
      principalArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the principal. It can be an IAM user, IAM role, or the root user. If you specify the root user, it disables the account setting for all IAM users, IAM roles, and the root user of the account unless an IAM user or role explicitly overrides these settings. If this field is omitted, the setting is changed only for the authenticated user."]}
    let context_ = "DeleteAccountSettingRequest"
    let make ?principalArn = fun ~name -> fun () -> { principalArn; name }
    let to_value x =
      structure_to_value
        [("name", (Some (SettingName.to_value x.name)));
        ("principalArn", (Option.map x.principalArn ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let principalArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "principalArn") in
      let name =
        SettingName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?principalArn ~name ()
    let of_json json =
      let principalArn = field_map json "principalArn" String_.of_json in
      let name = field_map_exn json "name" SettingName.of_json in
      make ?principalArn ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Disables an account setting for a specified IAM user, IAM role, or the root user for an account."]
module CreateTaskSetResponse =
  struct
    type nonrec t =
      {
      taskSet: TaskSet.t option
        [@ocaml.doc
          "Information about a set of Amazon ECS tasks in either an CodeDeploy or an EXTERNAL deployment. A task set includes details such as the desired number of tasks, how many tasks are running, and whether the task set serves production traffic."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `PlatformTaskDefinitionIncompatibilityException of
          PlatformTaskDefinitionIncompatibilityException.t 
      | `PlatformUnknownException of PlatformUnknownException.t 
      | `ServerException of ServerException.t 
      | `ServiceNotActiveException of ServiceNotActiveException.t 
      | `ServiceNotFoundException of ServiceNotFoundException.t 
      | `UnsupportedFeatureException of UnsupportedFeatureException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?taskSet = fun () -> { taskSet }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "PlatformTaskDefinitionIncompatibilityException" ->
          `PlatformTaskDefinitionIncompatibilityException
            (PlatformTaskDefinitionIncompatibilityException.of_json json)
      | "PlatformUnknownException" ->
          `PlatformUnknownException (PlatformUnknownException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "ServiceNotActiveException" ->
          `ServiceNotActiveException (ServiceNotActiveException.of_json json)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_json json)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "PlatformTaskDefinitionIncompatibilityException" ->
          `PlatformTaskDefinitionIncompatibilityException
            (PlatformTaskDefinitionIncompatibilityException.of_xml xml)
      | "PlatformUnknownException" ->
          `PlatformUnknownException (PlatformUnknownException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "ServiceNotActiveException" ->
          `ServiceNotActiveException (ServiceNotActiveException.of_xml xml)
      | "ServiceNotFoundException" ->
          `ServiceNotFoundException (ServiceNotFoundException.of_xml xml)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `PlatformTaskDefinitionIncompatibilityException e ->
          `Assoc
            [("error",
               (`String "PlatformTaskDefinitionIncompatibilityException"));
            ("details",
              (PlatformTaskDefinitionIncompatibilityException.to_json e))]
      | `PlatformUnknownException e ->
          `Assoc
            [("error", (`String "PlatformUnknownException"));
            ("details", (PlatformUnknownException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `ServiceNotActiveException e ->
          `Assoc
            [("error", (`String "ServiceNotActiveException"));
            ("details", (ServiceNotActiveException.to_json e))]
      | `ServiceNotFoundException e ->
          `Assoc
            [("error", (`String "ServiceNotFoundException"));
            ("details", (ServiceNotFoundException.to_json e))]
      | `UnsupportedFeatureException e ->
          `Assoc
            [("error", (`String "UnsupportedFeatureException"));
            ("details", (UnsupportedFeatureException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("taskSet", (Option.map x.taskSet ~f:TaskSet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskSet =
        (Option.map ~f:TaskSet.of_xml) (Xml.child xml_arg0 "taskSet") in
      make ?taskSet ()
    let of_json json =
      let taskSet = field_map json "taskSet" TaskSet.of_json in
      make ?taskSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a task set in the specified cluster and service. This is used when a service uses the EXTERNAL deployment controller type. For more information, see Amazon ECS Deployment Types in the Amazon Elastic Container Service Developer Guide."]
module CreateTaskSetRequest =
  struct
    type nonrec t =
      {
      service: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the service to create the task set in."];
      cluster: String_.t
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service to create the task set in."];
      externalId: String_.t option
        [@ocaml.doc
          "An optional non-unique tag that identifies this task set in external systems. If the task set is associated with a service discovery registry, the tasks in this task set will have the ECS_TASK_SET_EXTERNAL_ID Cloud Map attribute set to the provided value."];
      taskDefinition: String_.t
        [@ocaml.doc
          "The task definition for the tasks in the task set to use."];
      networkConfiguration: NetworkConfiguration.t option
        [@ocaml.doc
          "An object representing the network configuration for a task set."];
      loadBalancers: LoadBalancers.t option
        [@ocaml.doc
          "A load balancer object representing the load balancer to use with the task set. The supported load balancer types are either an Application Load Balancer or a Network Load Balancer."];
      serviceRegistries: ServiceRegistries.t option
        [@ocaml.doc
          "The details of the service discovery registries to assign to this task set. For more information, see Service Discovery."];
      launchType: LaunchType.t option
        [@ocaml.doc
          "The launch type that new tasks in the task set uses. For more information, see Amazon ECS Launch Types in the Amazon Elastic Container Service Developer Guide. If a launchType is specified, the capacityProviderStrategy parameter must be omitted."];
      capacityProviderStrategy: CapacityProviderStrategy.t option
        [@ocaml.doc
          "The capacity provider strategy to use for the task set. A capacity provider strategy consists of one or more capacity providers along with the base and weight to assign to them. A capacity provider must be associated with the cluster to be used in a capacity provider strategy. The PutClusterCapacityProviders API is used to associate a capacity provider with a cluster. Only capacity providers with an ACTIVE or UPDATING status can be used. If a capacityProviderStrategy is specified, the launchType parameter must be omitted. If no capacityProviderStrategy or launchType is specified, the defaultCapacityProviderStrategy for the cluster is used. If specifying a capacity provider that uses an Auto Scaling group, the capacity provider must already be created. New capacity providers can be created with the CreateCapacityProvider API operation. To use a Fargate capacity provider, specify either the FARGATE or FARGATE_SPOT capacity providers. The Fargate capacity providers are available to all accounts and only need to be associated with a cluster to be used. The PutClusterCapacityProviders API operation is used to update the list of available capacity providers for a cluster after the cluster is created."];
      platformVersion: String_.t option
        [@ocaml.doc
          "The platform version that the tasks in the task set uses. A platform version is specified only for tasks using the Fargate launch type. If one isn't specified, the LATEST platform version is used."];
      scale: Scale.t option
        [@ocaml.doc
          "A floating-point percentage of the desired number of tasks to place and keep running in the task set."];
      clientToken: String_.t option
        [@ocaml.doc
          "The identifier that you provide to ensure the idempotency of the request. It's case sensitive and must be unique. It can be up to 32 ASCII characters are allowed."];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the task set to help you categorize and organize them. Each tag consists of a key and an optional value. You define both. When a service is deleted, the tags are deleted. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."]}
    let context_ = "CreateTaskSetRequest"
    let make ?externalId =
      fun ?networkConfiguration ->
        fun ?loadBalancers ->
          fun ?serviceRegistries ->
            fun ?launchType ->
              fun ?capacityProviderStrategy ->
                fun ?platformVersion ->
                  fun ?scale ->
                    fun ?clientToken ->
                      fun ?tags ->
                        fun ~service ->
                          fun ~cluster ->
                            fun ~taskDefinition ->
                              fun () ->
                                {
                                  externalId;
                                  networkConfiguration;
                                  loadBalancers;
                                  serviceRegistries;
                                  launchType;
                                  capacityProviderStrategy;
                                  platformVersion;
                                  scale;
                                  clientToken;
                                  tags;
                                  service;
                                  cluster;
                                  taskDefinition
                                }
    let to_value x =
      structure_to_value
        [("service", (Some (String_.to_value x.service)));
        ("cluster", (Some (String_.to_value x.cluster)));
        ("externalId", (Option.map x.externalId ~f:String_.to_value));
        ("taskDefinition", (Some (String_.to_value x.taskDefinition)));
        ("networkConfiguration",
          (Option.map x.networkConfiguration ~f:NetworkConfiguration.to_value));
        ("loadBalancers",
          (Option.map x.loadBalancers ~f:LoadBalancers.to_value));
        ("serviceRegistries",
          (Option.map x.serviceRegistries ~f:ServiceRegistries.to_value));
        ("launchType", (Option.map x.launchType ~f:LaunchType.to_value));
        ("capacityProviderStrategy",
          (Option.map x.capacityProviderStrategy
             ~f:CapacityProviderStrategy.to_value));
        ("platformVersion",
          (Option.map x.platformVersion ~f:String_.to_value));
        ("scale", (Option.map x.scale ~f:Scale.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let scale = (Option.map ~f:Scale.of_xml) (Xml.child xml_arg0 "scale") in
      let platformVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platformVersion") in
      let capacityProviderStrategy =
        (Option.map ~f:CapacityProviderStrategy.of_xml)
          (Xml.child xml_arg0 "capacityProviderStrategy") in
      let launchType =
        (Option.map ~f:LaunchType.of_xml) (Xml.child xml_arg0 "launchType") in
      let serviceRegistries =
        (Option.map ~f:ServiceRegistries.of_xml)
          (Xml.child xml_arg0 "serviceRegistries") in
      let loadBalancers =
        (Option.map ~f:LoadBalancers.of_xml)
          (Xml.child xml_arg0 "loadBalancers") in
      let networkConfiguration =
        (Option.map ~f:NetworkConfiguration.of_xml)
          (Xml.child xml_arg0 "networkConfiguration") in
      let taskDefinition =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "taskDefinition") in
      let externalId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "externalId") in
      let cluster =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "cluster") in
      let service =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "service") in
      make ?tags ?clientToken ?scale ?platformVersion
        ?capacityProviderStrategy ?launchType ?serviceRegistries
        ?loadBalancers ?networkConfiguration ~taskDefinition ?externalId
        ~cluster ~service ()
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let clientToken = field_map json "clientToken" String_.of_json in
      let scale = field_map json "scale" Scale.of_json in
      let platformVersion = field_map json "platformVersion" String_.of_json in
      let capacityProviderStrategy =
        field_map json "capacityProviderStrategy"
          CapacityProviderStrategy.of_json in
      let launchType = field_map json "launchType" LaunchType.of_json in
      let serviceRegistries =
        field_map json "serviceRegistries" ServiceRegistries.of_json in
      let loadBalancers =
        field_map json "loadBalancers" LoadBalancers.of_json in
      let networkConfiguration =
        field_map json "networkConfiguration" NetworkConfiguration.of_json in
      let taskDefinition =
        field_map_exn json "taskDefinition" String_.of_json in
      let externalId = field_map json "externalId" String_.of_json in
      let cluster = field_map_exn json "cluster" String_.of_json in
      let service = field_map_exn json "service" String_.of_json in
      make ?tags ?clientToken ?scale ?platformVersion
        ?capacityProviderStrategy ?launchType ?serviceRegistries
        ?loadBalancers ?networkConfiguration ~taskDefinition ?externalId
        ~cluster ~service ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a task set in the specified cluster and service. This is used when a service uses the EXTERNAL deployment controller type. For more information, see Amazon ECS Deployment Types in the Amazon Elastic Container Service Developer Guide."]
module CreateServiceResponse =
  struct
    type nonrec t =
      {
      service: Service.t option
        [@ocaml.doc
          "The full description of your service following the create call. A service will return either a capacityProviderStrategy or launchType parameter, but not both, depending where one was specified when it was created. If a service is using the ECS deployment controller, the deploymentController and taskSets parameters will not be returned. if the service uses the CODE_DEPLOY deployment controller, the deploymentController, taskSets and deployments parameters will be returned, however the deployments parameter will be an empty list."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ClientException of ClientException.t 
      | `ClusterNotFoundException of ClusterNotFoundException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `PlatformTaskDefinitionIncompatibilityException of
          PlatformTaskDefinitionIncompatibilityException.t 
      | `PlatformUnknownException of PlatformUnknownException.t 
      | `ServerException of ServerException.t 
      | `UnsupportedFeatureException of UnsupportedFeatureException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?service = fun () -> { service }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "PlatformTaskDefinitionIncompatibilityException" ->
          `PlatformTaskDefinitionIncompatibilityException
            (PlatformTaskDefinitionIncompatibilityException.of_json json)
      | "PlatformUnknownException" ->
          `PlatformUnknownException (PlatformUnknownException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "ClusterNotFoundException" ->
          `ClusterNotFoundException (ClusterNotFoundException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "PlatformTaskDefinitionIncompatibilityException" ->
          `PlatformTaskDefinitionIncompatibilityException
            (PlatformTaskDefinitionIncompatibilityException.of_xml xml)
      | "PlatformUnknownException" ->
          `PlatformUnknownException (PlatformUnknownException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "UnsupportedFeatureException" ->
          `UnsupportedFeatureException
            (UnsupportedFeatureException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `ClusterNotFoundException e ->
          `Assoc
            [("error", (`String "ClusterNotFoundException"));
            ("details", (ClusterNotFoundException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `PlatformTaskDefinitionIncompatibilityException e ->
          `Assoc
            [("error",
               (`String "PlatformTaskDefinitionIncompatibilityException"));
            ("details",
              (PlatformTaskDefinitionIncompatibilityException.to_json e))]
      | `PlatformUnknownException e ->
          `Assoc
            [("error", (`String "PlatformUnknownException"));
            ("details", (PlatformUnknownException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `UnsupportedFeatureException e ->
          `Assoc
            [("error", (`String "UnsupportedFeatureException"));
            ("details", (UnsupportedFeatureException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("service", (Option.map x.service ~f:Service.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let service =
        (Option.map ~f:Service.of_xml) (Xml.child xml_arg0 "service") in
      make ?service ()
    let of_json json =
      let service = field_map json "service" Service.of_json in
      make ?service ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Runs and maintains your desired number of tasks from a specified task definition. If the number of tasks running in a service drops below the desiredCount, Amazon ECS runs another copy of the task in the specified cluster. To update an existing service, see the UpdateService action. In addition to maintaining the desired count of tasks in your service, you can optionally run your service behind one or more load balancers. The load balancers distribute traffic across the tasks that are associated with the service. For more information, see Service Load Balancing in the Amazon Elastic Container Service Developer Guide. Tasks for services that don't use a load balancer are considered healthy if they're in the RUNNING state. Tasks for services that use a load balancer are considered healthy if they're in the RUNNING state and are reported as healthy by the load balancer. There are two service scheduler strategies available: REPLICA - The replica scheduling strategy places and maintains your desired number of tasks across your cluster. By default, the service scheduler spreads tasks across Availability Zones. You can use task placement strategies and constraints to customize task placement decisions. For more information, see Service Scheduler Concepts in the Amazon Elastic Container Service Developer Guide. DAEMON - The daemon scheduling strategy deploys exactly one task on each active container instance that meets all of the task placement constraints that you specify in your cluster. The service scheduler also evaluates the task placement constraints for running tasks. It also stops tasks that don't meet the placement constraints. When using this strategy, you don't need to specify a desired number of tasks, a task placement strategy, or use Service Auto Scaling policies. For more information, see Service Scheduler Concepts in the Amazon Elastic Container Service Developer Guide. You can optionally specify a deployment configuration for your service. The deployment is initiated by changing properties. For example, the deployment might be initiated by the task definition or by your desired count of a service. This is done with an UpdateService operation. The default value for a replica service for minimumHealthyPercent is 100%. The default value for a daemon service for minimumHealthyPercent is 0%. If a service uses the ECS deployment controller, the minimum healthy percent represents a lower limit on the number of tasks in a service that must remain in the RUNNING state during a deployment. Specifically, it represents it as a percentage of your desired number of tasks (rounded up to the nearest integer). This happens when any of your container instances are in the DRAINING state if the service contains tasks using the EC2 launch type. Using this parameter, you can deploy without using additional cluster capacity. For example, if you set your service to have desired number of four tasks and a minimum healthy percent of 50%, the scheduler might stop two existing tasks to free up cluster capacity before starting two new tasks. If they're in the RUNNING state, tasks for services that don't use a load balancer are considered healthy . If they're in the RUNNING state and reported as healthy by the load balancer, tasks for services that do use a load balancer are considered healthy . The default value for minimum healthy percent is 100%. If a service uses the ECS deployment controller, the maximum percent parameter represents an upper limit on the number of tasks in a service that are allowed in the RUNNING or PENDING state during a deployment. Specifically, it represents it as a percentage of the desired number of tasks (rounded down to the nearest integer). This happens when any of your container instances are in the DRAINING state if the service contains tasks using the EC2 launch type. Using this parameter, you can define the deployment batch size. For example, if your service has a desired number of four tasks and a maximum percent value of 200%, the scheduler may start four new tasks before stopping the four older tasks (provided that the cluster resources required to do this are available). The default value for maximum percent is 200%. If a service uses either the CODE_DEPLOY or EXTERNAL deployment controller types and tasks that use the EC2 launch type, the minimum healthy percent and maximum percent values are used only to define the lower and upper limit on the number of the tasks in the service that remain in the RUNNING state. This is while the container instances are in the DRAINING state. If the tasks in the service use the Fargate launch type, the minimum healthy percent and maximum percent values aren't used. This is the case even if they're currently visible when describing your service. When creating a service that uses the EXTERNAL deployment controller, you can specify only parameters that aren't controlled at the task set level. The only required parameter is the service name. You control your services using the CreateTaskSet operation. For more information, see Amazon ECS Deployment Types in the Amazon Elastic Container Service Developer Guide. When the service scheduler launches new tasks, it determines task placement in your cluster using the following logic: Determine which of the container instances in your cluster can support the task definition of your service. For example, they have the required CPU, memory, ports, and container instance attributes. By default, the service scheduler attempts to balance tasks across Availability Zones in this manner. This is the case even if you can choose a different placement strategy with the placementStrategy parameter. Sort the valid container instances, giving priority to instances that have the fewest number of running tasks for this service in their respective Availability Zone. For example, if zone A has one running service task and zones B and C each have zero, valid container instances in either zone B or C are considered optimal for placement. Place the new service task on a valid container instance in an optimal Availability Zone based on the previous steps, favoring container instances with the fewest number of running tasks for this service."]
module CreateServiceRequest =
  struct
    type nonrec t =
      {
      cluster: String_.t option
        [@ocaml.doc
          "The short name or full Amazon Resource Name (ARN) of the cluster that you run your service on. If you do not specify a cluster, the default cluster is assumed."];
      serviceName: String_.t
        [@ocaml.doc
          "The name of your service. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed. Service names must be unique within a cluster, but you can have similarly named services in multiple clusters within a Region or across multiple Regions."];
      taskDefinition: String_.t option
        [@ocaml.doc
          "The family and revision (family:revision) or full ARN of the task definition to run in your service. If a revision isn't specified, the latest ACTIVE revision is used. A task definition must be specified if the service uses either the ECS or CODE_DEPLOY deployment controllers."];
      loadBalancers: LoadBalancers.t option
        [@ocaml.doc
          "A load balancer object representing the load balancers to use with your service. For more information, see Service Load Balancing in the Amazon Elastic Container Service Developer Guide. If the service uses the rolling update (ECS) deployment controller and using either an Application Load Balancer or Network Load Balancer, you must specify one or more target group ARNs to attach to the service. The service-linked role is required for services that use multiple target groups. For more information, see Using service-linked roles for Amazon ECS in the Amazon Elastic Container Service Developer Guide. If the service uses the CODE_DEPLOY deployment controller, the service is required to use either an Application Load Balancer or Network Load Balancer. When creating an CodeDeploy deployment group, you specify two target groups (referred to as a targetGroupPair). During a deployment, CodeDeploy determines which task set in your service has the status PRIMARY, and it associates one target group with it. Then, it also associates the other target group with the replacement task set. The load balancer can also have up to two listeners: a required listener for production traffic and an optional listener that you can use to perform validation tests with Lambda functions before routing production traffic to it. If you use the CODE_DEPLOY deployment controller, these values can be changed when updating the service. For Application Load Balancers and Network Load Balancers, this object must contain the load balancer target group ARN, the container name, and the container port to access from the load balancer. The container name must be as it appears in a container definition. The load balancer name parameter must be omitted. When a task from this service is placed on a container instance, the container instance and port combination is registered as a target in the target group that's specified here. For Classic Load Balancers, this object must contain the load balancer name, the container name , and the container port to access from the load balancer. The container name must be as it appears in a container definition. The target group ARN parameter must be omitted. When a task from this service is placed on a container instance, the container instance is registered with the load balancer that's specified here. Services with tasks that use the awsvpc network mode (for example, those with the Fargate launch type) only support Application Load Balancers and Network Load Balancers. Classic Load Balancers aren't supported. Also, when you create any target groups for these services, you must choose ip as the target type, not instance. This is because tasks that use the awsvpc network mode are associated with an elastic network interface, not an Amazon EC2 instance."];
      serviceRegistries: ServiceRegistries.t option
        [@ocaml.doc
          "The details of the service discovery registry to associate with this service. For more information, see Service discovery. Each service may be associated with one service registry. Multiple service registries for each service isn't supported."];
      desiredCount: BoxedInteger.t option
        [@ocaml.doc
          "The number of instantiations of the specified task definition to place and keep running on your cluster. This is required if schedulingStrategy is REPLICA or isn't specified. If schedulingStrategy is DAEMON then this isn't required."];
      clientToken: String_.t option
        [@ocaml.doc
          "An identifier that you provide to ensure the idempotency of the request. It must be unique and is case sensitive. Up to 32 ASCII characters are allowed."];
      launchType: LaunchType.t option
        [@ocaml.doc
          "The infrastructure that you run your service on. For more information, see Amazon ECS launch types in the Amazon Elastic Container Service Developer Guide. The FARGATE launch type runs your tasks on Fargate On-Demand infrastructure. Fargate Spot infrastructure is available for use but a capacity provider strategy must be used. For more information, see Fargate capacity providers in the Amazon ECS User Guide for Fargate. The EC2 launch type runs your tasks on Amazon EC2 instances registered to your cluster. The EXTERNAL launch type runs your tasks on your on-premises server or virtual machine (VM) capacity registered to your cluster. A service can use either a launch type or a capacity provider strategy. If a launchType is specified, the capacityProviderStrategy parameter must be omitted."];
      capacityProviderStrategy: CapacityProviderStrategy.t option
        [@ocaml.doc
          "The capacity provider strategy to use for the service. If a capacityProviderStrategy is specified, the launchType parameter must be omitted. If no capacityProviderStrategy or launchType is specified, the defaultCapacityProviderStrategy for the cluster is used. A capacity provider strategy may contain a maximum of 6 capacity providers."];
      platformVersion: String_.t option
        [@ocaml.doc
          "The platform version that your tasks in the service are running on. A platform version is specified only for tasks using the Fargate launch type. If one isn't specified, the LATEST platform version is used. For more information, see Fargate platform versions in the Amazon Elastic Container Service Developer Guide."];
      role: String_.t option
        [@ocaml.doc
          "The name or full Amazon Resource Name (ARN) of the IAM role that allows Amazon ECS to make calls to your load balancer on your behalf. This parameter is only permitted if you are using a load balancer with your service and your task definition doesn't use the awsvpc network mode. If you specify the role parameter, you must also specify a load balancer object with the loadBalancers parameter. If your account has already created the Amazon ECS service-linked role, that role is used for your service unless you specify a role here. The service-linked role is required if your task definition uses the awsvpc network mode or if the service is configured to use service discovery, an external deployment controller, multiple target groups, or Elastic Inference accelerators in which case you don't specify a role here. For more information, see Using service-linked roles for Amazon ECS in the Amazon Elastic Container Service Developer Guide. If your specified role has a path other than /, then you must either specify the full role ARN (this is recommended) or prefix the role name with the path. For example, if a role with the name bar has a path of /foo/ then you would specify /foo/bar as the role name. For more information, see Friendly names and paths in the IAM User Guide."];
      deploymentConfiguration: DeploymentConfiguration.t option
        [@ocaml.doc
          "Optional deployment parameters that control how many tasks run during the deployment and the ordering of stopping and starting tasks."];
      placementConstraints: PlacementConstraints.t option
        [@ocaml.doc
          "An array of placement constraint objects to use for tasks in your service. You can specify a maximum of 10 constraints for each task. This limit includes constraints in the task definition and those specified at runtime."];
      placementStrategy: PlacementStrategies.t option
        [@ocaml.doc
          "The placement strategy objects to use for tasks in your service. You can specify a maximum of 5 strategy rules for each service."];
      networkConfiguration: NetworkConfiguration.t option
        [@ocaml.doc
          "The network configuration for the service. This parameter is required for task definitions that use the awsvpc network mode to receive their own elastic network interface, and it isn't supported for other network modes. For more information, see Task networking in the Amazon Elastic Container Service Developer Guide."];
      healthCheckGracePeriodSeconds: BoxedInteger.t option
        [@ocaml.doc
          "The period of time, in seconds, that the Amazon ECS service scheduler ignores unhealthy Elastic Load Balancing target health checks after a task has first started. This is only used when your service is configured to use a load balancer. If your service has a load balancer defined and you don't specify a health check grace period value, the default value of 0 is used. If you do not use an Elastic Load Balancing, we recomend that you use the startPeriod in the task definition healtch check parameters. For more information, see Health check. If your service's tasks take a while to start and respond to Elastic Load Balancing health checks, you can specify a health check grace period of up to 2,147,483,647 seconds (about 69 years). During that time, the Amazon ECS service scheduler ignores health check status. This grace period can prevent the service scheduler from marking tasks as unhealthy and stopping them before they have time to come up."];
      schedulingStrategy: SchedulingStrategy.t option
        [@ocaml.doc
          "The scheduling strategy to use for the service. For more information, see Services. There are two service scheduler strategies available: REPLICA-The replica scheduling strategy places and maintains the desired number of tasks across your cluster. By default, the service scheduler spreads tasks across Availability Zones. You can use task placement strategies and constraints to customize task placement decisions. This scheduler strategy is required if the service uses the CODE_DEPLOY or EXTERNAL deployment controller types. DAEMON-The daemon scheduling strategy deploys exactly one task on each active container instance that meets all of the task placement constraints that you specify in your cluster. The service scheduler also evaluates the task placement constraints for running tasks and will stop tasks that don't meet the placement constraints. When you're using this strategy, you don't need to specify a desired number of tasks, a task placement strategy, or use Service Auto Scaling policies. Tasks using the Fargate launch type or the CODE_DEPLOY or EXTERNAL deployment controller types don't support the DAEMON scheduling strategy."];
      deploymentController: DeploymentController.t option
        [@ocaml.doc
          "The deployment controller to use for the service. If no deployment controller is specified, the default value of ECS is used."];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the service to help you categorize and organize them. Each tag consists of a key and an optional value, both of which you define. When a service is deleted, the tags are deleted as well. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."];
      enableECSManagedTags: Boolean.t option
        [@ocaml.doc
          "Specifies whether to turn on Amazon ECS managed tags for the tasks within the service. For more information, see Tagging Your Amazon ECS Resources in the Amazon Elastic Container Service Developer Guide."];
      propagateTags: PropagateTags.t option
        [@ocaml.doc
          "Specifies whether to propagate the tags from the task definition to the task. If no value is specified, the tags aren't propagated. Tags can only be propagated to the task during task creation. To add tags to a task after task creation, use the TagResource API action."];
      enableExecuteCommand: Boolean.t option
        [@ocaml.doc
          "Determines whether the execute command functionality is enabled for the service. If true, this enables execute command functionality on all containers in the service tasks."]}
    let context_ = "CreateServiceRequest"
    let make ?cluster =
      fun ?taskDefinition ->
        fun ?loadBalancers ->
          fun ?serviceRegistries ->
            fun ?desiredCount ->
              fun ?clientToken ->
                fun ?launchType ->
                  fun ?capacityProviderStrategy ->
                    fun ?platformVersion ->
                      fun ?role ->
                        fun ?deploymentConfiguration ->
                          fun ?placementConstraints ->
                            fun ?placementStrategy ->
                              fun ?networkConfiguration ->
                                fun ?healthCheckGracePeriodSeconds ->
                                  fun ?schedulingStrategy ->
                                    fun ?deploymentController ->
                                      fun ?tags ->
                                        fun ?enableECSManagedTags ->
                                          fun ?propagateTags ->
                                            fun ?enableExecuteCommand ->
                                              fun ~serviceName ->
                                                fun () ->
                                                  {
                                                    cluster;
                                                    taskDefinition;
                                                    loadBalancers;
                                                    serviceRegistries;
                                                    desiredCount;
                                                    clientToken;
                                                    launchType;
                                                    capacityProviderStrategy;
                                                    platformVersion;
                                                    role;
                                                    deploymentConfiguration;
                                                    placementConstraints;
                                                    placementStrategy;
                                                    networkConfiguration;
                                                    healthCheckGracePeriodSeconds;
                                                    schedulingStrategy;
                                                    deploymentController;
                                                    tags;
                                                    enableECSManagedTags;
                                                    propagateTags;
                                                    enableExecuteCommand;
                                                    serviceName
                                                  }
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:String_.to_value));
        ("serviceName", (Some (String_.to_value x.serviceName)));
        ("taskDefinition", (Option.map x.taskDefinition ~f:String_.to_value));
        ("loadBalancers",
          (Option.map x.loadBalancers ~f:LoadBalancers.to_value));
        ("serviceRegistries",
          (Option.map x.serviceRegistries ~f:ServiceRegistries.to_value));
        ("desiredCount",
          (Option.map x.desiredCount ~f:BoxedInteger.to_value));
        ("clientToken", (Option.map x.clientToken ~f:String_.to_value));
        ("launchType", (Option.map x.launchType ~f:LaunchType.to_value));
        ("capacityProviderStrategy",
          (Option.map x.capacityProviderStrategy
             ~f:CapacityProviderStrategy.to_value));
        ("platformVersion",
          (Option.map x.platformVersion ~f:String_.to_value));
        ("role", (Option.map x.role ~f:String_.to_value));
        ("deploymentConfiguration",
          (Option.map x.deploymentConfiguration
             ~f:DeploymentConfiguration.to_value));
        ("placementConstraints",
          (Option.map x.placementConstraints ~f:PlacementConstraints.to_value));
        ("placementStrategy",
          (Option.map x.placementStrategy ~f:PlacementStrategies.to_value));
        ("networkConfiguration",
          (Option.map x.networkConfiguration ~f:NetworkConfiguration.to_value));
        ("healthCheckGracePeriodSeconds",
          (Option.map x.healthCheckGracePeriodSeconds
             ~f:BoxedInteger.to_value));
        ("schedulingStrategy",
          (Option.map x.schedulingStrategy ~f:SchedulingStrategy.to_value));
        ("deploymentController",
          (Option.map x.deploymentController ~f:DeploymentController.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("enableECSManagedTags",
          (Option.map x.enableECSManagedTags ~f:Boolean.to_value));
        ("propagateTags",
          (Option.map x.propagateTags ~f:PropagateTags.to_value));
        ("enableExecuteCommand",
          (Option.map x.enableExecuteCommand ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enableExecuteCommand =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableExecuteCommand") in
      let propagateTags =
        (Option.map ~f:PropagateTags.of_xml)
          (Xml.child xml_arg0 "propagateTags") in
      let enableECSManagedTags =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "enableECSManagedTags") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let deploymentController =
        (Option.map ~f:DeploymentController.of_xml)
          (Xml.child xml_arg0 "deploymentController") in
      let schedulingStrategy =
        (Option.map ~f:SchedulingStrategy.of_xml)
          (Xml.child xml_arg0 "schedulingStrategy") in
      let healthCheckGracePeriodSeconds =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "healthCheckGracePeriodSeconds") in
      let networkConfiguration =
        (Option.map ~f:NetworkConfiguration.of_xml)
          (Xml.child xml_arg0 "networkConfiguration") in
      let placementStrategy =
        (Option.map ~f:PlacementStrategies.of_xml)
          (Xml.child xml_arg0 "placementStrategy") in
      let placementConstraints =
        (Option.map ~f:PlacementConstraints.of_xml)
          (Xml.child xml_arg0 "placementConstraints") in
      let deploymentConfiguration =
        (Option.map ~f:DeploymentConfiguration.of_xml)
          (Xml.child xml_arg0 "deploymentConfiguration") in
      let role = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "role") in
      let platformVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platformVersion") in
      let capacityProviderStrategy =
        (Option.map ~f:CapacityProviderStrategy.of_xml)
          (Xml.child xml_arg0 "capacityProviderStrategy") in
      let launchType =
        (Option.map ~f:LaunchType.of_xml) (Xml.child xml_arg0 "launchType") in
      let clientToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientToken") in
      let desiredCount =
        (Option.map ~f:BoxedInteger.of_xml)
          (Xml.child xml_arg0 "desiredCount") in
      let serviceRegistries =
        (Option.map ~f:ServiceRegistries.of_xml)
          (Xml.child xml_arg0 "serviceRegistries") in
      let loadBalancers =
        (Option.map ~f:LoadBalancers.of_xml)
          (Xml.child xml_arg0 "loadBalancers") in
      let taskDefinition =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskDefinition") in
      let serviceName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "serviceName") in
      let cluster =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?enableExecuteCommand ?propagateTags ?enableECSManagedTags ?tags
        ?deploymentController ?schedulingStrategy
        ?healthCheckGracePeriodSeconds ?networkConfiguration
        ?placementStrategy ?placementConstraints ?deploymentConfiguration
        ?role ?platformVersion ?capacityProviderStrategy ?launchType
        ?clientToken ?desiredCount ?serviceRegistries ?loadBalancers
        ?taskDefinition ~serviceName ?cluster ()
    let of_json json =
      let enableExecuteCommand =
        field_map json "enableExecuteCommand" Boolean.of_json in
      let propagateTags =
        field_map json "propagateTags" PropagateTags.of_json in
      let enableECSManagedTags =
        field_map json "enableECSManagedTags" Boolean.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let deploymentController =
        field_map json "deploymentController" DeploymentController.of_json in
      let schedulingStrategy =
        field_map json "schedulingStrategy" SchedulingStrategy.of_json in
      let healthCheckGracePeriodSeconds =
        field_map json "healthCheckGracePeriodSeconds" BoxedInteger.of_json in
      let networkConfiguration =
        field_map json "networkConfiguration" NetworkConfiguration.of_json in
      let placementStrategy =
        field_map json "placementStrategy" PlacementStrategies.of_json in
      let placementConstraints =
        field_map json "placementConstraints" PlacementConstraints.of_json in
      let deploymentConfiguration =
        field_map json "deploymentConfiguration"
          DeploymentConfiguration.of_json in
      let role = field_map json "role" String_.of_json in
      let platformVersion = field_map json "platformVersion" String_.of_json in
      let capacityProviderStrategy =
        field_map json "capacityProviderStrategy"
          CapacityProviderStrategy.of_json in
      let launchType = field_map json "launchType" LaunchType.of_json in
      let clientToken = field_map json "clientToken" String_.of_json in
      let desiredCount = field_map json "desiredCount" BoxedInteger.of_json in
      let serviceRegistries =
        field_map json "serviceRegistries" ServiceRegistries.of_json in
      let loadBalancers =
        field_map json "loadBalancers" LoadBalancers.of_json in
      let taskDefinition = field_map json "taskDefinition" String_.of_json in
      let serviceName = field_map_exn json "serviceName" String_.of_json in
      let cluster = field_map json "cluster" String_.of_json in
      make ?enableExecuteCommand ?propagateTags ?enableECSManagedTags ?tags
        ?deploymentController ?schedulingStrategy
        ?healthCheckGracePeriodSeconds ?networkConfiguration
        ?placementStrategy ?placementConstraints ?deploymentConfiguration
        ?role ?platformVersion ?capacityProviderStrategy ?launchType
        ?clientToken ?desiredCount ?serviceRegistries ?loadBalancers
        ?taskDefinition ~serviceName ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Runs and maintains your desired number of tasks from a specified task definition. If the number of tasks running in a service drops below the desiredCount, Amazon ECS runs another copy of the task in the specified cluster. To update an existing service, see the UpdateService action. In addition to maintaining the desired count of tasks in your service, you can optionally run your service behind one or more load balancers. The load balancers distribute traffic across the tasks that are associated with the service. For more information, see Service Load Balancing in the Amazon Elastic Container Service Developer Guide. Tasks for services that don't use a load balancer are considered healthy if they're in the RUNNING state. Tasks for services that use a load balancer are considered healthy if they're in the RUNNING state and are reported as healthy by the load balancer. There are two service scheduler strategies available: REPLICA - The replica scheduling strategy places and maintains your desired number of tasks across your cluster. By default, the service scheduler spreads tasks across Availability Zones. You can use task placement strategies and constraints to customize task placement decisions. For more information, see Service Scheduler Concepts in the Amazon Elastic Container Service Developer Guide. DAEMON - The daemon scheduling strategy deploys exactly one task on each active container instance that meets all of the task placement constraints that you specify in your cluster. The service scheduler also evaluates the task placement constraints for running tasks. It also stops tasks that don't meet the placement constraints. When using this strategy, you don't need to specify a desired number of tasks, a task placement strategy, or use Service Auto Scaling policies. For more information, see Service Scheduler Concepts in the Amazon Elastic Container Service Developer Guide. You can optionally specify a deployment configuration for your service. The deployment is initiated by changing properties. For example, the deployment might be initiated by the task definition or by your desired count of a service. This is done with an UpdateService operation. The default value for a replica service for minimumHealthyPercent is 100%. The default value for a daemon service for minimumHealthyPercent is 0%. If a service uses the ECS deployment controller, the minimum healthy percent represents a lower limit on the number of tasks in a service that must remain in the RUNNING state during a deployment. Specifically, it represents it as a percentage of your desired number of tasks (rounded up to the nearest integer). This happens when any of your container instances are in the DRAINING state if the service contains tasks using the EC2 launch type. Using this parameter, you can deploy without using additional cluster capacity. For example, if you set your service to have desired number of four tasks and a minimum healthy percent of 50%, the scheduler might stop two existing tasks to free up cluster capacity before starting two new tasks. If they're in the RUNNING state, tasks for services that don't use a load balancer are considered healthy . If they're in the RUNNING state and reported as healthy by the load balancer, tasks for services that do use a load balancer are considered healthy . The default value for minimum healthy percent is 100%. If a service uses the ECS deployment controller, the maximum percent parameter represents an upper limit on the number of tasks in a service that are allowed in the RUNNING or PENDING state during a deployment. Specifically, it represents it as a percentage of the desired number of tasks (rounded down to the nearest integer). This happens when any of your container instances are in the DRAINING state if the service contains tasks using the EC2 launch type. Using this parameter, you can define the deployment batch size. For example, if your service has a desired number of four tasks and a maximum percent value of 200%, the scheduler may start four new tasks before stopping the four older tasks (provided that the cluster resources required to do this are available). The default value for maximum percent is 200%. If a service uses either the CODE_DEPLOY or EXTERNAL deployment controller types and tasks that use the EC2 launch type, the minimum healthy percent and maximum percent values are used only to define the lower and upper limit on the number of the tasks in the service that remain in the RUNNING state. This is while the container instances are in the DRAINING state. If the tasks in the service use the Fargate launch type, the minimum healthy percent and maximum percent values aren't used. This is the case even if they're currently visible when describing your service. When creating a service that uses the EXTERNAL deployment controller, you can specify only parameters that aren't controlled at the task set level. The only required parameter is the service name. You control your services using the CreateTaskSet operation. For more information, see Amazon ECS Deployment Types in the Amazon Elastic Container Service Developer Guide. When the service scheduler launches new tasks, it determines task placement in your cluster using the following logic: Determine which of the container instances in your cluster can support the task definition of your service. For example, they have the required CPU, memory, ports, and container instance attributes. By default, the service scheduler attempts to balance tasks across Availability Zones in this manner. This is the case even if you can choose a different placement strategy with the placementStrategy parameter. Sort the valid container instances, giving priority to instances that have the fewest number of running tasks for this service in their respective Availability Zone. For example, if zone A has one running service task and zones B and C each have zero, valid container instances in either zone B or C are considered optimal for placement. Place the new service task on a valid container instance in an optimal Availability Zone based on the previous steps, favoring container instances with the fewest number of running tasks for this service."]
module CreateClusterResponse =
  struct
    type nonrec t =
      {
      cluster: Cluster.t option
        [@ocaml.doc "The full description of your new cluster."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ServerException of ServerException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?cluster = fun () -> { cluster }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("cluster", (Option.map x.cluster ~f:Cluster.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cluster =
        (Option.map ~f:Cluster.of_xml) (Xml.child xml_arg0 "cluster") in
      make ?cluster ()
    let of_json json =
      let cluster = field_map json "cluster" Cluster.of_json in
      make ?cluster ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new Amazon ECS cluster. By default, your account receives a default cluster when you launch your first container instance. However, you can create your own cluster with a unique name with the CreateCluster action. When you call the CreateCluster API operation, Amazon ECS attempts to create the Amazon ECS service-linked role for your account. This is so that it can manage required resources in other Amazon Web Services services on your behalf. However, if the IAM user that makes the call doesn't have permissions to create the service-linked role, it isn't created. For more information, see Using Service-Linked Roles for Amazon ECS in the Amazon Elastic Container Service Developer Guide."]
module CreateClusterRequest =
  struct
    type nonrec t =
      {
      clusterName: String_.t option
        [@ocaml.doc
          "The name of your cluster. If you don't specify a name for your cluster, you create a cluster that's named default. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed."];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the cluster to help you categorize and organize them. Each tag consists of a key and an optional value. You define both. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."];
      settings: ClusterSettings.t option
        [@ocaml.doc
          "The setting to use when creating a cluster. This parameter is used to turn on CloudWatch Container Insights for a cluster. If this value is specified, it overrides the containerInsights value set with PutAccountSetting or PutAccountSettingDefault."];
      configuration: ClusterConfiguration.t option
        [@ocaml.doc "The execute command configuration for the cluster."];
      capacityProviders: StringList.t option
        [@ocaml.doc
          "The short name of one or more capacity providers to associate with the cluster. A capacity provider must be associated with a cluster before it can be included as part of the default capacity provider strategy of the cluster or used in a capacity provider strategy when calling the CreateService or RunTask actions. If specifying a capacity provider that uses an Auto Scaling group, the capacity provider must be created but not associated with another cluster. New Auto Scaling group capacity providers can be created with the CreateCapacityProvider API operation. To use a Fargate capacity provider, specify either the FARGATE or FARGATE_SPOT capacity providers. The Fargate capacity providers are available to all accounts and only need to be associated with a cluster to be used. The PutClusterCapacityProviders API operation is used to update the list of available capacity providers for a cluster after the cluster is created."];
      defaultCapacityProviderStrategy: CapacityProviderStrategy.t option
        [@ocaml.doc
          "The capacity provider strategy to set as the default for the cluster. After a default capacity provider strategy is set for a cluster, when you call the RunTask or CreateService APIs with no capacity provider strategy or launch type specified, the default capacity provider strategy for the cluster is used. If a default capacity provider strategy isn't defined for a cluster when it was created, it can be defined later with the PutClusterCapacityProviders API operation."]}
    let make ?clusterName =
      fun ?tags ->
        fun ?settings ->
          fun ?configuration ->
            fun ?capacityProviders ->
              fun ?defaultCapacityProviderStrategy ->
                fun () ->
                  {
                    clusterName;
                    tags;
                    settings;
                    configuration;
                    capacityProviders;
                    defaultCapacityProviderStrategy
                  }
    let to_value x =
      structure_to_value
        [("clusterName", (Option.map x.clusterName ~f:String_.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("settings", (Option.map x.settings ~f:ClusterSettings.to_value));
        ("configuration",
          (Option.map x.configuration ~f:ClusterConfiguration.to_value));
        ("capacityProviders",
          (Option.map x.capacityProviders ~f:StringList.to_value));
        ("defaultCapacityProviderStrategy",
          (Option.map x.defaultCapacityProviderStrategy
             ~f:CapacityProviderStrategy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let defaultCapacityProviderStrategy =
        (Option.map ~f:CapacityProviderStrategy.of_xml)
          (Xml.child xml_arg0 "defaultCapacityProviderStrategy") in
      let capacityProviders =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "capacityProviders") in
      let configuration =
        (Option.map ~f:ClusterConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      let settings =
        (Option.map ~f:ClusterSettings.of_xml)
          (Xml.child xml_arg0 "settings") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let clusterName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clusterName") in
      make ?defaultCapacityProviderStrategy ?capacityProviders ?configuration
        ?settings ?tags ?clusterName ()
    let of_json json =
      let defaultCapacityProviderStrategy =
        field_map json "defaultCapacityProviderStrategy"
          CapacityProviderStrategy.of_json in
      let capacityProviders =
        field_map json "capacityProviders" StringList.of_json in
      let configuration =
        field_map json "configuration" ClusterConfiguration.of_json in
      let settings = field_map json "settings" ClusterSettings.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let clusterName = field_map json "clusterName" String_.of_json in
      make ?defaultCapacityProviderStrategy ?capacityProviders ?configuration
        ?settings ?tags ?clusterName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new Amazon ECS cluster. By default, your account receives a default cluster when you launch your first container instance. However, you can create your own cluster with a unique name with the CreateCluster action. When you call the CreateCluster API operation, Amazon ECS attempts to create the Amazon ECS service-linked role for your account. This is so that it can manage required resources in other Amazon Web Services services on your behalf. However, if the IAM user that makes the call doesn't have permissions to create the service-linked role, it isn't created. For more information, see Using Service-Linked Roles for Amazon ECS in the Amazon Elastic Container Service Developer Guide."]
module CreateCapacityProviderResponse =
  struct
    type nonrec t =
      {
      capacityProvider: CapacityProvider.t option
        [@ocaml.doc "The full description of the new capacity provider."]}
    type nonrec error =
      [ `ClientException of ClientException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ServerException of ServerException.t 
      | `UpdateInProgressException of UpdateInProgressException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?capacityProvider = fun () -> { capacityProvider }
    let error_of_json name json =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ServerException" -> `ServerException (ServerException.of_json json)
      | "UpdateInProgressException" ->
          `UpdateInProgressException (UpdateInProgressException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ClientException" -> `ClientException (ClientException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ServerException" -> `ServerException (ServerException.of_xml xml)
      | "UpdateInProgressException" ->
          `UpdateInProgressException (UpdateInProgressException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ClientException e ->
          `Assoc
            [("error", (`String "ClientException"));
            ("details", (ClientException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ServerException e ->
          `Assoc
            [("error", (`String "ServerException"));
            ("details", (ServerException.to_json e))]
      | `UpdateInProgressException e ->
          `Assoc
            [("error", (`String "UpdateInProgressException"));
            ("details", (UpdateInProgressException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("capacityProvider",
           (Option.map x.capacityProvider ~f:CapacityProvider.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityProvider =
        (Option.map ~f:CapacityProvider.of_xml)
          (Xml.child xml_arg0 "capacityProvider") in
      make ?capacityProvider ()
    let of_json json =
      let capacityProvider =
        field_map json "capacityProvider" CapacityProvider.of_json in
      make ?capacityProvider ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new capacity provider. Capacity providers are associated with an Amazon ECS cluster and are used in capacity provider strategies to facilitate cluster auto scaling. Only capacity providers that use an Auto Scaling group can be created. Amazon ECS tasks on Fargate use the FARGATE and FARGATE_SPOT capacity providers. These providers are available to all accounts in the Amazon Web Services Regions that Fargate supports."]
module CreateCapacityProviderRequest =
  struct
    type nonrec t =
      {
      name: String_.t
        [@ocaml.doc
          "The name of the capacity provider. Up to 255 characters are allowed. They include letters (both upper and lowercase letters), numbers, underscores (_), and hyphens (-). The name can't be prefixed with \"aws\", \"ecs\", or \"fargate\"."];
      autoScalingGroupProvider: AutoScalingGroupProvider.t
        [@ocaml.doc
          "The details of the Auto Scaling group for the capacity provider."];
      tags: Tags.t option
        [@ocaml.doc
          "The metadata that you apply to the capacity provider to categorize and organize them more conveniently. Each tag consists of a key and an optional value. You define both of them. The following basic restrictions apply to tags: Maximum number of tags per resource - 50 For each resource, each tag key must be unique, and each tag key can have only one value. Maximum key length - 128 Unicode characters in UTF-8 Maximum value length - 256 Unicode characters in UTF-8 If your tagging schema is used across multiple services and resources, remember that other services may have restrictions on allowed characters. Generally allowed characters are: letters, numbers, and spaces representable in UTF-8, and the following characters: + - = . _ : / \\@. Tag keys and values are case-sensitive. Do not use aws:, AWS:, or any upper or lowercase combination of such as a prefix for either keys or values as it is reserved for Amazon Web Services use. You cannot edit or delete tag keys or values with this prefix. Tags with this prefix do not count against your tags per resource limit."]}
    let context_ = "CreateCapacityProviderRequest"
    let make ?tags =
      fun ~name ->
        fun ~autoScalingGroupProvider ->
          fun () -> { tags; name; autoScalingGroupProvider }
    let to_value x =
      structure_to_value
        [("name", (Some (String_.to_value x.name)));
        ("autoScalingGroupProvider",
          (Some
             (AutoScalingGroupProvider.to_value x.autoScalingGroupProvider)));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let autoScalingGroupProvider =
        AutoScalingGroupProvider.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "autoScalingGroupProvider") in
      let name =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ~autoScalingGroupProvider ~name ()
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let autoScalingGroupProvider =
        field_map_exn json "autoScalingGroupProvider"
          AutoScalingGroupProvider.of_json in
      let name = field_map_exn json "name" String_.of_json in
      make ?tags ~autoScalingGroupProvider ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new capacity provider. Capacity providers are associated with an Amazon ECS cluster and are used in capacity provider strategies to facilitate cluster auto scaling. Only capacity providers that use an Auto Scaling group can be created. Amazon ECS tasks on Fargate use the FARGATE and FARGATE_SPOT capacity providers. These providers are available to all accounts in the Amazon Web Services Regions that Fargate supports."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_attribute_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and application =
         flag "application" (required string)
           ~doc:"STRING ApplicationSpecifier"
       and attributeGroup =
         flag "attribute-group" (required string)
           ~doc:"STRING AttributeGroupSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_attribute_group
           (Values.AssociateAttributeGroupRequest.make ~application
              ~attributeGroup ())
           (Some Values.AssociateAttributeGroupResponse.to_json)
           (Some Values.AssociateAttributeGroupResponse.error_to_json)])
let associate_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and application =
         flag "application" (required string)
           ~doc:"STRING ApplicationSpecifier"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON ResourceType"
       and resource =
         flag "resource" (required string) ~doc:"STRING ResourceSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_resource
           (Values.AssociateResourceRequest.make ~application
              ~resourceType:(Values.ResourceType.of_json resourceType)
              ~resource ()) (Some Values.AssociateResourceResponse.to_json)
           (Some Values.AssociateResourceResponse.error_to_json)])
let create_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application
           (Values.CreateApplicationRequest.make ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~clientToken ())
           (Some Values.CreateApplicationResponse.to_json)
           (Some Values.CreateApplicationResponse.error_to_json)])
let create_attribute_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and attributes =
         flag "attributes" (required string) ~doc:"STRING Attributes"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_attribute_group
           (Values.CreateAttributeGroupRequest.make ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~attributes ~clientToken ())
           (Some Values.CreateAttributeGroupResponse.to_json)
           (Some Values.CreateAttributeGroupResponse.error_to_json)])
let delete_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and application =
         flag "application" (required string)
           ~doc:"STRING ApplicationSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_application
           (Values.DeleteApplicationRequest.make ~application ())
           (Some Values.DeleteApplicationResponse.to_json)
           (Some Values.DeleteApplicationResponse.error_to_json)])
let delete_attribute_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeGroup =
         flag "attribute-group" (required string)
           ~doc:"STRING AttributeGroupSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_attribute_group
           (Values.DeleteAttributeGroupRequest.make ~attributeGroup ())
           (Some Values.DeleteAttributeGroupResponse.to_json)
           (Some Values.DeleteAttributeGroupResponse.error_to_json)])
let disassociate_attribute_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and application =
         flag "application" (required string)
           ~doc:"STRING ApplicationSpecifier"
       and attributeGroup =
         flag "attribute-group" (required string)
           ~doc:"STRING AttributeGroupSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_attribute_group
           (Values.DisassociateAttributeGroupRequest.make ~application
              ~attributeGroup ())
           (Some Values.DisassociateAttributeGroupResponse.to_json)
           (Some Values.DisassociateAttributeGroupResponse.error_to_json)])
let disassociate_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and application =
         flag "application" (required string)
           ~doc:"STRING ApplicationSpecifier"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON ResourceType"
       and resource =
         flag "resource" (required string) ~doc:"STRING ResourceSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_resource
           (Values.DisassociateResourceRequest.make ~application
              ~resourceType:(Values.ResourceType.of_json resourceType)
              ~resource ())
           (Some Values.DisassociateResourceResponse.to_json)
           (Some Values.DisassociateResourceResponse.error_to_json)])
let get_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and application =
         flag "application" (required string)
           ~doc:"STRING ApplicationSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_application
           (Values.GetApplicationRequest.make ~application ())
           (Some Values.GetApplicationResponse.to_json)
           (Some Values.GetApplicationResponse.error_to_json)])
let get_associated_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and application =
         flag "application" (required string)
           ~doc:"STRING ApplicationSpecifier"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON ResourceType"
       and resource =
         flag "resource" (required string) ~doc:"STRING ResourceSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_associated_resource
           (Values.GetAssociatedResourceRequest.make ~application
              ~resourceType:(Values.ResourceType.of_json resourceType)
              ~resource ())
           (Some Values.GetAssociatedResourceResponse.to_json)
           (Some Values.GetAssociatedResourceResponse.error_to_json)])
let get_attribute_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeGroup =
         flag "attribute-group" (required string)
           ~doc:"STRING AttributeGroupSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_attribute_group
           (Values.GetAttributeGroupRequest.make ~attributeGroup ())
           (Some Values.GetAttributeGroupResponse.to_json)
           (Some Values.GetAttributeGroupResponse.error_to_json)])
let list_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_applications
           (Values.ListApplicationsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListApplicationsResponse.to_json)
           (Some Values.ListApplicationsResponse.error_to_json)])
let list_associated_attribute_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and application =
         flag "application" (required string)
           ~doc:"STRING ApplicationSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_associated_attribute_groups
           (Values.ListAssociatedAttributeGroupsRequest.make ?nextToken
              ?maxResults ~application ())
           (Some Values.ListAssociatedAttributeGroupsResponse.to_json)
           (Some Values.ListAssociatedAttributeGroupsResponse.error_to_json)])
let list_associated_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and application =
         flag "application" (required string)
           ~doc:"STRING ApplicationSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_associated_resources
           (Values.ListAssociatedResourcesRequest.make ?nextToken ?maxResults
              ~application ())
           (Some Values.ListAssociatedResourcesResponse.to_json)
           (Some Values.ListAssociatedResourcesResponse.error_to_json)])
let list_attribute_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attribute_groups
           (Values.ListAttributeGroupsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListAttributeGroupsResponse.to_json)
           (Some Values.ListAttributeGroupsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let sync_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON ResourceType"
       and resource =
         flag "resource" (required string) ~doc:"STRING ResourceSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.sync_resource
           (Values.SyncResourceRequest.make
              ~resourceType:(Values.ResourceType.of_json resourceType)
              ~resource ()) (Some Values.SyncResourceResponse.to_json)
           (Some Values.SyncResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and application =
         flag "application" (required string)
           ~doc:"STRING ApplicationSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application
           (Values.UpdateApplicationRequest.make ?name ?description
              ~application ())
           (Some Values.UpdateApplicationResponse.to_json)
           (Some Values.UpdateApplicationResponse.error_to_json)])
let update_attribute_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and attributes =
         flag "attributes" (optional string) ~doc:"STRING Attributes"
       and attributeGroup =
         flag "attribute-group" (required string)
           ~doc:"STRING AttributeGroupSpecifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_attribute_group
           (Values.UpdateAttributeGroupRequest.make ?name ?description
              ?attributes ~attributeGroup ())
           (Some Values.UpdateAttributeGroupResponse.to_json)
           (Some Values.UpdateAttributeGroupResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-attribute-group", associate_attribute_group);
    ("associate-resource", associate_resource);
    ("create-application", create_application);
    ("create-attribute-group", create_attribute_group);
    ("delete-application", delete_application);
    ("delete-attribute-group", delete_attribute_group);
    ("disassociate-attribute-group", disassociate_attribute_group);
    ("disassociate-resource", disassociate_resource);
    ("get-application", get_application);
    ("get-associated-resource", get_associated_resource);
    ("get-attribute-group", get_attribute_group);
    ("list-applications", list_applications);
    ("list-associated-attribute-groups", list_associated_attribute_groups);
    ("list-associated-resources", list_associated_resources);
    ("list-attribute-groups", list_attribute_groups);
    ("list-tags-for-resource", list_tags_for_resource);
    ("sync-resource", sync_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-application", update_application);
    ("update-attribute-group", update_attribute_group)]
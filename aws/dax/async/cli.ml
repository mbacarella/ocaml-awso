(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and availabilityZones =
         flag "availability-zones" (optional json_arg)
           ~doc:"JSON AvailabilityZoneList"
       and subnetGroupName =
         flag "subnet-group-name" (optional string) ~doc:"STRING String"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIdentifierList"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and notificationTopicArn =
         flag "notification-topic-arn" (optional string) ~doc:"STRING String"
       and parameterGroupName =
         flag "parameter-group-name" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and sSESpecification =
         flag "s-s-e-specification" (optional json_arg)
           ~doc:"JSON SSESpecification"
       and clusterEndpointEncryptionType =
         flag "cluster-endpoint-encryption-type" (optional json_arg)
           ~doc:"JSON ClusterEndpointEncryptionType"
       and networkType =
         flag "network-type" (optional json_arg) ~doc:"JSON NetworkType"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and nodeType = flag "node-type" (required string) ~doc:"STRING String"
       and replicationFactor =
         flag "replication-factor" (required int) ~doc:"INT Integer"
       and iamRoleArn =
         flag "iam-role-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster
           (Values.CreateClusterRequest.make ?description
              ?availabilityZones:(Option.map
                                    ~f:Values.AvailabilityZoneList.of_json
                                    availabilityZones) ?subnetGroupName
              ?securityGroupIds:(Option.map
                                   ~f:Values.SecurityGroupIdentifierList.of_json
                                   securityGroupIds)
              ?preferredMaintenanceWindow ?notificationTopicArn
              ?parameterGroupName
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?sSESpecification:(Option.map
                                   ~f:Values.SSESpecification.of_json
                                   sSESpecification)
              ?clusterEndpointEncryptionType:(Option.map
                                                ~f:Values.ClusterEndpointEncryptionType.of_json
                                                clusterEndpointEncryptionType)
              ?networkType:(Option.map ~f:Values.NetworkType.of_json
                              networkType) ~clusterName ~nodeType
              ~replicationFactor ~iamRoleArn ())
           (Some Values.CreateClusterResponse.to_json)
           (Some Values.CreateClusterResponse.error_to_json)])
let create_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and parameterGroupName =
         flag "parameter-group-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_parameter_group
           (Values.CreateParameterGroupRequest.make ?description
              ~parameterGroupName ())
           (Some Values.CreateParameterGroupResponse.to_json)
           (Some Values.CreateParameterGroupResponse.error_to_json)])
let create_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and subnetGroupName =
         flag "subnet-group-name" (required string) ~doc:"STRING String"
       and subnetIds =
         flag "subnet-ids" (required json_arg)
           ~doc:"JSON SubnetIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subnet_group
           (Values.CreateSubnetGroupRequest.make ?description
              ~subnetGroupName
              ~subnetIds:(Values.SubnetIdentifierList.of_json subnetIds) ())
           (Some Values.CreateSubnetGroupResponse.to_json)
           (Some Values.CreateSubnetGroupResponse.error_to_json)])
let decrease_replication_factor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and availabilityZones =
         flag "availability-zones" (optional json_arg)
           ~doc:"JSON AvailabilityZoneList"
       and nodeIdsToRemove =
         flag "node-ids-to-remove" (optional json_arg)
           ~doc:"JSON NodeIdentifierList"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and newReplicationFactor =
         flag "new-replication-factor" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.decrease_replication_factor
           (Values.DecreaseReplicationFactorRequest.make
              ?availabilityZones:(Option.map
                                    ~f:Values.AvailabilityZoneList.of_json
                                    availabilityZones)
              ?nodeIdsToRemove:(Option.map
                                  ~f:Values.NodeIdentifierList.of_json
                                  nodeIdsToRemove) ~clusterName
              ~newReplicationFactor ())
           (Some Values.DecreaseReplicationFactorResponse.to_json)
           (Some Values.DecreaseReplicationFactorResponse.error_to_json)])
let delete_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster
           (Values.DeleteClusterRequest.make ~clusterName ())
           (Some Values.DeleteClusterResponse.to_json)
           (Some Values.DeleteClusterResponse.error_to_json)])
let delete_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameterGroupName =
         flag "parameter-group-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_parameter_group
           (Values.DeleteParameterGroupRequest.make ~parameterGroupName ())
           (Some Values.DeleteParameterGroupResponse.to_json)
           (Some Values.DeleteParameterGroupResponse.error_to_json)])
let delete_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subnetGroupName =
         flag "subnet-group-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_subnet_group
           (Values.DeleteSubnetGroupRequest.make ~subnetGroupName ())
           (Some Values.DeleteSubnetGroupResponse.to_json)
           (Some Values.DeleteSubnetGroupResponse.error_to_json)])
let describe_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterNames =
         flag "cluster-names" (optional json_arg) ~doc:"JSON ClusterNameList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IntegerOptional"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_clusters
           (Values.DescribeClustersRequest.make
              ?clusterNames:(Option.map ~f:Values.ClusterNameList.of_json
                               clusterNames) ?maxResults ?nextToken ())
           (Some Values.DescribeClustersResponse.to_json)
           (Some Values.DescribeClustersResponse.error_to_json)])
let describe_default_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IntegerOptional"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_default_parameters
           (Values.DescribeDefaultParametersRequest.make ?maxResults
              ?nextToken ())
           (Some Values.DescribeDefaultParametersResponse.to_json)
           (Some Values.DescribeDefaultParametersResponse.error_to_json)])
let describe_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceName =
         flag "source-name" (optional string) ~doc:"STRING String"
       and sourceType =
         flag "source-type" (optional json_arg) ~doc:"JSON SourceType"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TStamp"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON TStamp"
       and duration =
         flag "duration" (optional int) ~doc:"INT IntegerOptional"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IntegerOptional"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_events
           (Values.DescribeEventsRequest.make ?sourceName
              ?sourceType:(Option.map ~f:Values.SourceType.of_json sourceType)
              ?startTime:(Option.map ~f:Values.TStamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.TStamp.of_json endTime)
              ?duration ?maxResults ?nextToken ())
           (Some Values.DescribeEventsResponse.to_json)
           (Some Values.DescribeEventsResponse.error_to_json)])
let describe_parameter_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameterGroupNames =
         flag "parameter-group-names" (optional json_arg)
           ~doc:"JSON ParameterGroupNameList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IntegerOptional"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_parameter_groups
           (Values.DescribeParameterGroupsRequest.make
              ?parameterGroupNames:(Option.map
                                      ~f:Values.ParameterGroupNameList.of_json
                                      parameterGroupNames) ?maxResults
              ?nextToken ())
           (Some Values.DescribeParameterGroupsResponse.to_json)
           (Some Values.DescribeParameterGroupsResponse.error_to_json)])
let describe_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and source = flag "source" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IntegerOptional"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and parameterGroupName =
         flag "parameter-group-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_parameters
           (Values.DescribeParametersRequest.make ?source ?maxResults
              ?nextToken ~parameterGroupName ())
           (Some Values.DescribeParametersResponse.to_json)
           (Some Values.DescribeParametersResponse.error_to_json)])
let describe_subnet_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subnetGroupNames =
         flag "subnet-group-names" (optional json_arg)
           ~doc:"JSON SubnetGroupNameList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT IntegerOptional"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_subnet_groups
           (Values.DescribeSubnetGroupsRequest.make
              ?subnetGroupNames:(Option.map
                                   ~f:Values.SubnetGroupNameList.of_json
                                   subnetGroupNames) ?maxResults ?nextToken
              ()) (Some Values.DescribeSubnetGroupsResponse.to_json)
           (Some Values.DescribeSubnetGroupsResponse.error_to_json)])
let increase_replication_factor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and availabilityZones =
         flag "availability-zones" (optional json_arg)
           ~doc:"JSON AvailabilityZoneList"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and newReplicationFactor =
         flag "new-replication-factor" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.increase_replication_factor
           (Values.IncreaseReplicationFactorRequest.make
              ?availabilityZones:(Option.map
                                    ~f:Values.AvailabilityZoneList.of_json
                                    availabilityZones) ~clusterName
              ~newReplicationFactor ())
           (Some Values.IncreaseReplicationFactorResponse.to_json)
           (Some Values.IncreaseReplicationFactorResponse.error_to_json)])
let list_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags
           (Values.ListTagsRequest.make ?nextToken ~resourceName ())
           (Some Values.ListTagsResponse.to_json)
           (Some Values.ListTagsResponse.error_to_json)])
let reboot_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and nodeId = flag "node-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_node
           (Values.RebootNodeRequest.make ~clusterName ~nodeId ())
           (Some Values.RebootNodeResponse.to_json)
           (Some Values.RebootNodeResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceName
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING String"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON KeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceName
              ~tagKeys:(Values.KeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and notificationTopicArn =
         flag "notification-topic-arn" (optional string) ~doc:"STRING String"
       and notificationTopicStatus =
         flag "notification-topic-status" (optional string)
           ~doc:"STRING String"
       and parameterGroupName =
         flag "parameter-group-name" (optional string) ~doc:"STRING String"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIdentifierList"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cluster
           (Values.UpdateClusterRequest.make ?description
              ?preferredMaintenanceWindow ?notificationTopicArn
              ?notificationTopicStatus ?parameterGroupName
              ?securityGroupIds:(Option.map
                                   ~f:Values.SecurityGroupIdentifierList.of_json
                                   securityGroupIds) ~clusterName ())
           (Some Values.UpdateClusterResponse.to_json)
           (Some Values.UpdateClusterResponse.error_to_json)])
let update_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameterGroupName =
         flag "parameter-group-name" (required string) ~doc:"STRING String"
       and parameterNameValues =
         flag "parameter-name-values" (required json_arg)
           ~doc:"JSON ParameterNameValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_parameter_group
           (Values.UpdateParameterGroupRequest.make ~parameterGroupName
              ~parameterNameValues:(Values.ParameterNameValueList.of_json
                                      parameterNameValues) ())
           (Some Values.UpdateParameterGroupResponse.to_json)
           (Some Values.UpdateParameterGroupResponse.error_to_json)])
let update_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and subnetIds =
         flag "subnet-ids" (optional json_arg)
           ~doc:"JSON SubnetIdentifierList"
       and subnetGroupName =
         flag "subnet-group-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_subnet_group
           (Values.UpdateSubnetGroupRequest.make ?description
              ?subnetIds:(Option.map ~f:Values.SubnetIdentifierList.of_json
                            subnetIds) ~subnetGroupName ())
           (Some Values.UpdateSubnetGroupResponse.to_json)
           (Some Values.UpdateSubnetGroupResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-cluster", create_cluster);
    ("create-parameter-group", create_parameter_group);
    ("create-subnet-group", create_subnet_group);
    ("decrease-replication-factor", decrease_replication_factor);
    ("delete-cluster", delete_cluster);
    ("delete-parameter-group", delete_parameter_group);
    ("delete-subnet-group", delete_subnet_group);
    ("describe-clusters", describe_clusters);
    ("describe-default-parameters", describe_default_parameters);
    ("describe-events", describe_events);
    ("describe-parameter-groups", describe_parameter_groups);
    ("describe-parameters", describe_parameters);
    ("describe-subnet-groups", describe_subnet_groups);
    ("increase-replication-factor", increase_replication_factor);
    ("list-tags", list_tags);
    ("reboot-node", reboot_node);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-cluster", update_cluster);
    ("update-parameter-group", update_parameter_group);
    ("update-subnet-group", update_subnet_group)]
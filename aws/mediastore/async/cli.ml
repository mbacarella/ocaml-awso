(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_container =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_container
           (Values.CreateContainerInput.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~containerName ()) (Some Values.CreateContainerOutput.to_json)
           (Some Values.CreateContainerOutput.error_to_json)])
let delete_container =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_container
           (Values.DeleteContainerInput.make ~containerName ())
           (Some Values.DeleteContainerOutput.to_json)
           (Some Values.DeleteContainerOutput.error_to_json)])
let delete_container_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_container_policy
           (Values.DeleteContainerPolicyInput.make ~containerName ())
           (Some Values.DeleteContainerPolicyOutput.to_json)
           (Some Values.DeleteContainerPolicyOutput.error_to_json)])
let delete_cors_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cors_policy
           (Values.DeleteCorsPolicyInput.make ~containerName ())
           (Some Values.DeleteCorsPolicyOutput.to_json)
           (Some Values.DeleteCorsPolicyOutput.error_to_json)])
let delete_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_lifecycle_policy
           (Values.DeleteLifecyclePolicyInput.make ~containerName ())
           (Some Values.DeleteLifecyclePolicyOutput.to_json)
           (Some Values.DeleteLifecyclePolicyOutput.error_to_json)])
let delete_metric_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_metric_policy
           (Values.DeleteMetricPolicyInput.make ~containerName ())
           (Some Values.DeleteMetricPolicyOutput.to_json)
           (Some Values.DeleteMetricPolicyOutput.error_to_json)])
let describe_container =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (optional string) ~doc:"STRING ContainerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_container
           (Values.DescribeContainerInput.make ?containerName ())
           (Some Values.DescribeContainerOutput.to_json)
           (Some Values.DescribeContainerOutput.error_to_json)])
let get_container_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_container_policy
           (Values.GetContainerPolicyInput.make ~containerName ())
           (Some Values.GetContainerPolicyOutput.to_json)
           (Some Values.GetContainerPolicyOutput.error_to_json)])
let get_cors_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cors_policy
           (Values.GetCorsPolicyInput.make ~containerName ())
           (Some Values.GetCorsPolicyOutput.to_json)
           (Some Values.GetCorsPolicyOutput.error_to_json)])
let get_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_lifecycle_policy
           (Values.GetLifecyclePolicyInput.make ~containerName ())
           (Some Values.GetLifecyclePolicyOutput.to_json)
           (Some Values.GetLifecyclePolicyOutput.error_to_json)])
let get_metric_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_metric_policy
           (Values.GetMetricPolicyInput.make ~containerName ())
           (Some Values.GetMetricPolicyOutput.to_json)
           (Some Values.GetMetricPolicyOutput.error_to_json)])
let list_containers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ContainerListLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_containers
           (Values.ListContainersInput.make ?nextToken ?maxResults ())
           (Some Values.ListContainersOutput.to_json)
           (Some Values.ListContainersOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resource =
         flag "resource" (required string) ~doc:"STRING ContainerARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resource ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let put_container_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName"
       and policy =
         flag "policy" (required string) ~doc:"STRING ContainerPolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_container_policy
           (Values.PutContainerPolicyInput.make ~containerName ~policy ())
           (Some Values.PutContainerPolicyOutput.to_json)
           (Some Values.PutContainerPolicyOutput.error_to_json)])
let put_cors_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName"
       and corsPolicy =
         flag "cors-policy" (required json_arg) ~doc:"JSON CorsPolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_cors_policy
           (Values.PutCorsPolicyInput.make ~containerName
              ~corsPolicy:(Values.CorsPolicy.of_json corsPolicy) ())
           (Some Values.PutCorsPolicyOutput.to_json)
           (Some Values.PutCorsPolicyOutput.error_to_json)])
let put_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName"
       and lifecyclePolicy =
         flag "lifecycle-policy" (required string)
           ~doc:"STRING LifecyclePolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_lifecycle_policy
           (Values.PutLifecyclePolicyInput.make ~containerName
              ~lifecyclePolicy ())
           (Some Values.PutLifecyclePolicyOutput.to_json)
           (Some Values.PutLifecyclePolicyOutput.error_to_json)])
let put_metric_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName"
       and metricPolicy =
         flag "metric-policy" (required json_arg) ~doc:"JSON MetricPolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_metric_policy
           (Values.PutMetricPolicyInput.make ~containerName
              ~metricPolicy:(Values.MetricPolicy.of_json metricPolicy) ())
           (Some Values.PutMetricPolicyOutput.to_json)
           (Some Values.PutMetricPolicyOutput.error_to_json)])
let start_access_logging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_access_logging
           (Values.StartAccessLoggingInput.make ~containerName ())
           (Some Values.StartAccessLoggingOutput.to_json)
           (Some Values.StartAccessLoggingOutput.error_to_json)])
let stop_access_logging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerName =
         flag "container-name" (required string) ~doc:"STRING ContainerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_access_logging
           (Values.StopAccessLoggingInput.make ~containerName ())
           (Some Values.StopAccessLoggingOutput.to_json)
           (Some Values.StopAccessLoggingOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resource =
         flag "resource" (required string) ~doc:"STRING ContainerARN"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resource
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resource =
         flag "resource" (required string) ~doc:"STRING ContainerARN"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resource
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-container", create_container);
    ("delete-container", delete_container);
    ("delete-container-policy", delete_container_policy);
    ("delete-cors-policy", delete_cors_policy);
    ("delete-lifecycle-policy", delete_lifecycle_policy);
    ("delete-metric-policy", delete_metric_policy);
    ("describe-container", describe_container);
    ("get-container-policy", get_container_policy);
    ("get-cors-policy", get_cors_policy);
    ("get-lifecycle-policy", get_lifecycle_policy);
    ("get-metric-policy", get_metric_policy);
    ("list-containers", list_containers);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-container-policy", put_container_policy);
    ("put-cors-policy", put_cors_policy);
    ("put-lifecycle-policy", put_lifecycle_policy);
    ("put-metric-policy", put_metric_policy);
    ("start-access-logging", start_access_logging);
    ("stop-access-logging", stop_access_logging);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.mediastore
let apiVersion = "2017-09-01"
let endpointPrefix = "mediastore"
let serviceFullName = "AWS Elemental MediaStore"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "MediaStore"
let targetPrefix = "MediaStore_20170901"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module ObjectGroup =
  struct
    type nonrec t = string
    let context_ = "ObjectGroup"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:900) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"/?(?:[A-Za-z0-9_=:\\.\\-\\~\\*]+/){0,10}(?:[A-Za-z0-9_=:\\.\\-\\~\\*]+)?/?")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ObjectGroup" j
    let to_json = simple_to_json to_value
  end
module ObjectGroupName =
  struct
    type nonrec t = string
    let context_ = "ObjectGroupName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:30) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9_]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ObjectGroupName" j
    let to_json = simple_to_json to_value
  end
module Header =
  struct
    type nonrec t = string
    let context_ = "Header"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:8192) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0009\\u000A\\u000D\\u0020-\\u00FF]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Header" j
    let to_json = simple_to_json to_value
  end
module MethodName =
  struct
    type nonrec t =
      | PUT 
      | GET 
      | DELETE 
      | HEAD 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PUT -> "PUT"
      | GET -> "GET"
      | DELETE -> "DELETE"
      | HEAD -> "HEAD"
      | Non_static_id s -> s
    let of_string =
      function
      | "PUT" -> PUT
      | "GET" -> GET
      | "DELETE" -> DELETE
      | "HEAD" -> HEAD
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration MethodName" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"MethodName" j)
    let to_json = simple_to_json to_value
  end
module Origin =
  struct
    type nonrec t = string
    let context_ = "Origin"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0009\\u000A\\u000D\\u0020-\\u00FF]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Origin" j
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module MetricPolicyRule =
  struct
    type nonrec t =
      {
      objectGroup: ObjectGroup.t ;
      objectGroupName: ObjectGroupName.t }
    let context_ = "MetricPolicyRule"
    let make ~objectGroup =
      fun ~objectGroupName -> fun () -> { objectGroup; objectGroupName }
    let to_value x =
      structure_to_value
        [("ObjectGroup", (Some (ObjectGroup.to_value x.objectGroup)));
        ("ObjectGroupName",
          (Some (ObjectGroupName.to_value x.objectGroupName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let objectGroupName =
        ObjectGroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ObjectGroupName") in
      let objectGroup =
        ObjectGroup.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ObjectGroup") in
      make ~objectGroupName ~objectGroup ()
    let of_json json =
      let objectGroupName =
        field_map_exn json "ObjectGroupName" ObjectGroupName.of_json in
      let objectGroup = field_map_exn json "ObjectGroup" ObjectGroup.of_json in
      make ~objectGroupName ~objectGroup ()
    let to_json v = composed_to_json to_value v
  end
module AllowedHeaders =
  struct
    type nonrec t = Header.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:Header.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Header.of_xml)
    let of_json j =
      list_of_json ~kind:"AllowedHeaders" ~of_json:Header.of_json j
    let to_json v = composed_to_json to_value v
  end
module AllowedMethods =
  struct
    type nonrec t = MethodName.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:4) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:MethodName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MethodName.of_xml)
    let of_json j =
      list_of_json ~kind:"AllowedMethods" ~of_json:MethodName.of_json j
    let to_json v = composed_to_json to_value v
  end
module AllowedOrigins =
  struct
    type nonrec t = Origin.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:Origin.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Origin.of_xml)
    let of_json j =
      list_of_json ~kind:"AllowedOrigins" ~of_json:Origin.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExposeHeaders =
  struct
    type nonrec t = Header.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:Header.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Header.of_xml)
    let of_json j =
      list_of_json ~kind:"ExposeHeaders" ~of_json:Header.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxAgeSeconds =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:2147483647) >>=
             (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxAgeSeconds" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ContainerARN =
  struct
    type nonrec t = string
    let context_ = "ContainerARN"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws:mediastore:[a-z]+-[a-z]+-\\d:\\d{12}:container/[\\w-]{1,255}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ContainerARN" j
    let to_json = simple_to_json to_value
  end
module ContainerAccessLoggingEnabled =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ContainerName =
  struct
    type nonrec t = string
    let context_ = "ContainerName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () -> check_pattern i ~pattern:"[\\w-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ContainerName" j
    let to_json = simple_to_json to_value
  end
module ContainerStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | CREATING 
      | DELETING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | CREATING -> "CREATING"
      | DELETING -> "DELETING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "CREATING" -> CREATING
      | "DELETING" -> DELETING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ContainerStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ContainerStatus" j)
    let to_json = simple_to_json to_value
  end
module Endpoint =
  struct
    type nonrec t = string
    let context_ = "Endpoint"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0009\\u000A\\u000D\\u0020-\\u00FF]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Endpoint" j
    let to_json = simple_to_json to_value
  end
module TimeStamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () -> check_pattern i ~pattern:"[ \\w:\\.\\?-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t = {
      key: TagKey.t ;
      value: TagValue.t option }
    let context_ = "Tag"
    let make ?value = fun ~key -> fun () -> { value; key }
    let to_value x =
      structure_to_value
        [("Key", (Some (TagKey.to_value x.key)));
        ("Value", (Option.map x.value ~f:TagValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:TagValue.of_xml) (Xml.child xml_arg0 "Value") in
      let key =
        TagKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Key") in
      make ?value ~key ()
    let of_json json =
      let value = field_map json "Value" TagValue.of_json in
      let key = field_map_exn json "Key" TagKey.of_json in
      make ?value ~key ()
    let to_json v = composed_to_json to_value v
  end
module ContainerLevelMetrics =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ContainerLevelMetrics" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ContainerLevelMetrics" j)
    let to_json = simple_to_json to_value
  end
module MetricPolicyRules =
  struct
    type nonrec t = MetricPolicyRule.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:300) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:MetricPolicyRule.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MetricPolicyRule.of_xml)
    let of_json j =
      list_of_json ~kind:"MetricPolicyRules"
        ~of_json:MetricPolicyRule.of_json j
    let to_json v = composed_to_json to_value v
  end
module CorsRule =
  struct
    type nonrec t =
      {
      allowedOrigins: AllowedOrigins.t ;
      allowedMethods: AllowedMethods.t option ;
      allowedHeaders: AllowedHeaders.t ;
      maxAgeSeconds: MaxAgeSeconds.t option ;
      exposeHeaders: ExposeHeaders.t option }
    let context_ = "CorsRule"
    let make ?allowedMethods =
      fun ?maxAgeSeconds ->
        fun ?exposeHeaders ->
          fun ~allowedOrigins ->
            fun ~allowedHeaders ->
              fun () ->
                {
                  allowedMethods;
                  maxAgeSeconds;
                  exposeHeaders;
                  allowedOrigins;
                  allowedHeaders
                }
    let to_value x =
      structure_to_value
        [("AllowedOrigins",
           (Some (AllowedOrigins.to_value x.allowedOrigins)));
        ("AllowedMethods",
          (Option.map x.allowedMethods ~f:AllowedMethods.to_value));
        ("AllowedHeaders", (Some (AllowedHeaders.to_value x.allowedHeaders)));
        ("MaxAgeSeconds",
          (Option.map x.maxAgeSeconds ~f:MaxAgeSeconds.to_value));
        ("ExposeHeaders",
          (Option.map x.exposeHeaders ~f:ExposeHeaders.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exposeHeaders =
        (Option.map ~f:ExposeHeaders.of_xml)
          (Xml.child xml_arg0 "ExposeHeaders") in
      let maxAgeSeconds =
        (Option.map ~f:MaxAgeSeconds.of_xml)
          (Xml.child xml_arg0 "MaxAgeSeconds") in
      let allowedHeaders =
        AllowedHeaders.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AllowedHeaders") in
      let allowedMethods =
        (Option.map ~f:AllowedMethods.of_xml)
          (Xml.child xml_arg0 "AllowedMethods") in
      let allowedOrigins =
        AllowedOrigins.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AllowedOrigins") in
      make ?exposeHeaders ?maxAgeSeconds ~allowedHeaders ?allowedMethods
        ~allowedOrigins ()
    let of_json json =
      let exposeHeaders =
        field_map json "ExposeHeaders" ExposeHeaders.of_json in
      let maxAgeSeconds =
        field_map json "MaxAgeSeconds" MaxAgeSeconds.of_json in
      let allowedHeaders =
        field_map_exn json "AllowedHeaders" AllowedHeaders.of_json in
      let allowedMethods =
        field_map json "AllowedMethods" AllowedMethods.of_json in
      let allowedOrigins =
        field_map_exn json "AllowedOrigins" AllowedOrigins.of_json in
      make ?exposeHeaders ?maxAgeSeconds ~allowedHeaders ?allowedMethods
        ~allowedOrigins ()
    let to_json v = composed_to_json to_value v
  end
module Container =
  struct
    type nonrec t =
      {
      endpoint: Endpoint.t option ;
      creationTime: TimeStamp.t option ;
      aRN: ContainerARN.t option ;
      name: ContainerName.t option ;
      status: ContainerStatus.t option ;
      accessLoggingEnabled: ContainerAccessLoggingEnabled.t option }
    let make ?endpoint =
      fun ?creationTime ->
        fun ?aRN ->
          fun ?name ->
            fun ?status ->
              fun ?accessLoggingEnabled ->
                fun () ->
                  {
                    endpoint;
                    creationTime;
                    aRN;
                    name;
                    status;
                    accessLoggingEnabled
                  }
    let to_value x =
      structure_to_value
        [("Endpoint", (Option.map x.endpoint ~f:Endpoint.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:TimeStamp.to_value));
        ("ARN", (Option.map x.aRN ~f:ContainerARN.to_value));
        ("Name", (Option.map x.name ~f:ContainerName.to_value));
        ("Status", (Option.map x.status ~f:ContainerStatus.to_value));
        ("AccessLoggingEnabled",
          (Option.map x.accessLoggingEnabled
             ~f:ContainerAccessLoggingEnabled.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accessLoggingEnabled =
        (Option.map ~f:ContainerAccessLoggingEnabled.of_xml)
          (Xml.child xml_arg0 "AccessLoggingEnabled") in
      let status =
        (Option.map ~f:ContainerStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let name =
        (Option.map ~f:ContainerName.of_xml) (Xml.child xml_arg0 "Name") in
      let aRN =
        (Option.map ~f:ContainerARN.of_xml) (Xml.child xml_arg0 "ARN") in
      let creationTime =
        (Option.map ~f:TimeStamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let endpoint =
        (Option.map ~f:Endpoint.of_xml) (Xml.child xml_arg0 "Endpoint") in
      make ?accessLoggingEnabled ?status ?name ?aRN ?creationTime ?endpoint
        ()
    let of_json json =
      let accessLoggingEnabled =
        field_map json "AccessLoggingEnabled"
          ContainerAccessLoggingEnabled.of_json in
      let status = field_map json "Status" ContainerStatus.of_json in
      let name = field_map json "Name" ContainerName.of_json in
      let aRN = field_map json "ARN" ContainerARN.of_json in
      let creationTime = field_map json "CreationTime" TimeStamp.of_json in
      let endpoint = field_map json "Endpoint" Endpoint.of_json in
      make ?accessLoggingEnabled ?status ?name ?aRN ?creationTime ?endpoint
        ()
    let to_json v = composed_to_json to_value v
  end
module ContainerInUseException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module ContainerNotFoundException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module InternalServerError =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module MetricPolicy =
  struct
    type nonrec t =
      {
      containerLevelMetrics: ContainerLevelMetrics.t ;
      metricPolicyRules: MetricPolicyRules.t option }
    let context_ = "MetricPolicy"
    let make ?metricPolicyRules =
      fun ~containerLevelMetrics ->
        fun () -> { metricPolicyRules; containerLevelMetrics }
    let to_value x =
      structure_to_value
        [("ContainerLevelMetrics",
           (Some (ContainerLevelMetrics.to_value x.containerLevelMetrics)));
        ("MetricPolicyRules",
          (Option.map x.metricPolicyRules ~f:MetricPolicyRules.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metricPolicyRules =
        (Option.map ~f:MetricPolicyRules.of_xml)
          (Xml.child xml_arg0 "MetricPolicyRules") in
      let containerLevelMetrics =
        ContainerLevelMetrics.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerLevelMetrics") in
      make ?metricPolicyRules ~containerLevelMetrics ()
    let of_json json =
      let metricPolicyRules =
        field_map json "MetricPolicyRules" MetricPolicyRules.of_json in
      let containerLevelMetrics =
        field_map_exn json "ContainerLevelMetrics"
          ContainerLevelMetrics.of_json in
      make ?metricPolicyRules ~containerLevelMetrics ()
    let to_json v = composed_to_json to_value v
  end
module LifecyclePolicy =
  struct
    type nonrec t = string
    let context_ = "LifecyclePolicy"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:8192) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0009\\u000A\\u000D\\u0020-\\u00FF]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LifecyclePolicy" j
    let to_json = simple_to_json to_value
  end
module CorsPolicy =
  struct
    type nonrec t = CorsRule.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:CorsRule.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CorsRule.of_xml)
    let of_json j =
      list_of_json ~kind:"CorsPolicy" ~of_json:CorsRule.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContainerPolicy =
  struct
    type nonrec t = string
    let context_ = "ContainerPolicy"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:8192) >>=
                  (fun () -> check_pattern i ~pattern:"[\\x00-\\x7F]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ContainerPolicy" j
    let to_json = simple_to_json to_value
  end
module ContainerList =
  struct
    type nonrec t = Container.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Container.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Container.of_xml)
    let of_json j =
      list_of_json ~kind:"ContainerList" ~of_json:Container.of_json j
    let to_json v = composed_to_json to_value v
  end
module PaginationToken =
  struct
    type nonrec t = string
    let context_ = "PaginationToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9A-Za-z=/+]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PaginationToken" j
    let to_json = simple_to_json to_value
  end
module ContainerListLimit =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ContainerListLimit" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module PolicyNotFoundException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module CorsPolicyNotFoundException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module LimitExceededException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module UntagResourceOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module UntagResourceInput =
  struct
    type nonrec t = {
      resource: ContainerARN.t ;
      tagKeys: TagKeyList.t }
    let context_ = "UntagResourceInput"
    let make ~resource = fun ~tagKeys -> fun () -> { resource; tagKeys }
    let to_value x =
      structure_to_value
        [("Resource", (Some (ContainerARN.to_value x.resource)));
        ("TagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TagKeys") in
      let resource =
        ContainerARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Resource") in
      make ~tagKeys ~resource ()
    let of_json json =
      let tagKeys = field_map_exn json "TagKeys" TagKeyList.of_json in
      let resource = field_map_exn json "Resource" ContainerARN.of_json in
      make ~tagKeys ~resource ()
    let to_json v = composed_to_json to_value v
  end
module TagResourceOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module TagResourceInput =
  struct
    type nonrec t = {
      resource: ContainerARN.t ;
      tags: TagList.t }
    let context_ = "TagResourceInput"
    let make ~resource = fun ~tags -> fun () -> { resource; tags }
    let to_value x =
      structure_to_value
        [("Resource", (Some (ContainerARN.to_value x.resource)));
        ("Tags", (Some (TagList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resource =
        ContainerARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Resource") in
      make ~tags ~resource ()
    let of_json json =
      let tags = field_map_exn json "Tags" TagList.of_json in
      let resource = field_map_exn json "Resource" ContainerARN.of_json in
      make ~tags ~resource ()
    let to_json v = composed_to_json to_value v
  end
module StopAccessLoggingOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module StopAccessLoggingInput =
  struct
    type nonrec t = {
      containerName: ContainerName.t }
    let context_ = "StopAccessLoggingInput"
    let make ~containerName = fun () -> { containerName }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~containerName ()
    let of_json json =
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module StartAccessLoggingOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module StartAccessLoggingInput =
  struct
    type nonrec t = {
      containerName: ContainerName.t }
    let context_ = "StartAccessLoggingInput"
    let make ~containerName = fun () -> { containerName }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~containerName ()
    let of_json json =
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module PutMetricPolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module PutMetricPolicyInput =
  struct
    type nonrec t =
      {
      containerName: ContainerName.t ;
      metricPolicy: MetricPolicy.t }
    let context_ = "PutMetricPolicyInput"
    let make ~containerName =
      fun ~metricPolicy -> fun () -> { containerName; metricPolicy }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)));
        ("MetricPolicy", (Some (MetricPolicy.to_value x.metricPolicy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metricPolicy =
        MetricPolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MetricPolicy") in
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~metricPolicy ~containerName ()
    let of_json json =
      let metricPolicy =
        field_map_exn json "MetricPolicy" MetricPolicy.of_json in
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~metricPolicy ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module PutLifecyclePolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module PutLifecyclePolicyInput =
  struct
    type nonrec t =
      {
      containerName: ContainerName.t ;
      lifecyclePolicy: LifecyclePolicy.t }
    let context_ = "PutLifecyclePolicyInput"
    let make ~containerName =
      fun ~lifecyclePolicy -> fun () -> { containerName; lifecyclePolicy }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)));
        ("LifecyclePolicy",
          (Some (LifecyclePolicy.to_value x.lifecyclePolicy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lifecyclePolicy =
        LifecyclePolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LifecyclePolicy") in
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~lifecyclePolicy ~containerName ()
    let of_json json =
      let lifecyclePolicy =
        field_map_exn json "LifecyclePolicy" LifecyclePolicy.of_json in
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~lifecyclePolicy ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module PutCorsPolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module PutCorsPolicyInput =
  struct
    type nonrec t =
      {
      containerName: ContainerName.t ;
      corsPolicy: CorsPolicy.t }
    let context_ = "PutCorsPolicyInput"
    let make ~containerName =
      fun ~corsPolicy -> fun () -> { containerName; corsPolicy }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)));
        ("CorsPolicy", (Some (CorsPolicy.to_value x.corsPolicy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let corsPolicy =
        CorsPolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CorsPolicy") in
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~corsPolicy ~containerName ()
    let of_json json =
      let corsPolicy = field_map_exn json "CorsPolicy" CorsPolicy.of_json in
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~corsPolicy ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module PutContainerPolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module PutContainerPolicyInput =
  struct
    type nonrec t =
      {
      containerName: ContainerName.t ;
      policy: ContainerPolicy.t }
    let context_ = "PutContainerPolicyInput"
    let make ~containerName =
      fun ~policy -> fun () -> { containerName; policy }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)));
        ("Policy", (Some (ContainerPolicy.to_value x.policy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        ContainerPolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Policy") in
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~policy ~containerName ()
    let of_json json =
      let policy = field_map_exn json "Policy" ContainerPolicy.of_json in
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~policy ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module ListTagsForResourceOutput =
  struct
    type nonrec t = {
      tags: TagList.t option }
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?tags ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end
module ListTagsForResourceInput =
  struct
    type nonrec t = {
      resource: ContainerARN.t }
    let context_ = "ListTagsForResourceInput"
    let make ~resource = fun () -> { resource }
    let to_value x =
      structure_to_value
        [("Resource", (Some (ContainerARN.to_value x.resource)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resource =
        ContainerARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Resource") in
      make ~resource ()
    let of_json json =
      let resource = field_map_exn json "Resource" ContainerARN.of_json in
      make ~resource ()
    let to_json v = composed_to_json to_value v
  end
module ListContainersOutput =
  struct
    type nonrec t =
      {
      containers: ContainerList.t ;
      nextToken: PaginationToken.t option }
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListContainersOutput"
    let make ?nextToken =
      fun ~containers -> fun () -> { nextToken; containers }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Containers", (Some (ContainerList.to_value x.containers)));
        ("NextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let containers =
        ContainerList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Containers") in
      make ?nextToken ~containers ()
    let of_json json =
      let nextToken = field_map json "NextToken" PaginationToken.of_json in
      let containers = field_map_exn json "Containers" ContainerList.of_json in
      make ?nextToken ~containers ()
    let to_json v = composed_to_json to_value v
  end
module ListContainersInput =
  struct
    type nonrec t =
      {
      nextToken: PaginationToken.t option ;
      maxResults: ContainerListLimit.t option }
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:ContainerListLimit.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:ContainerListLimit.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      make ?maxResults ?nextToken ()
    let of_json json =
      let maxResults = field_map json "MaxResults" ContainerListLimit.of_json in
      let nextToken = field_map json "NextToken" PaginationToken.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module GetMetricPolicyOutput =
  struct
    type nonrec t = {
      metricPolicy: MetricPolicy.t }
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `PolicyNotFoundException of PolicyNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetMetricPolicyOutput"
    let make ~metricPolicy = fun () -> { metricPolicy }
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `PolicyNotFoundException e ->
          `Assoc
            [("error", (`String "PolicyNotFoundException"));
            ("details", (PolicyNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("MetricPolicy", (Some (MetricPolicy.to_value x.metricPolicy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metricPolicy =
        MetricPolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MetricPolicy") in
      make ~metricPolicy ()
    let of_json json =
      let metricPolicy =
        field_map_exn json "MetricPolicy" MetricPolicy.of_json in
      make ~metricPolicy ()
    let to_json v = composed_to_json to_value v
  end
module GetMetricPolicyInput =
  struct
    type nonrec t = {
      containerName: ContainerName.t }
    let context_ = "GetMetricPolicyInput"
    let make ~containerName = fun () -> { containerName }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~containerName ()
    let of_json json =
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module GetLifecyclePolicyOutput =
  struct
    type nonrec t = {
      lifecyclePolicy: LifecyclePolicy.t }
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `PolicyNotFoundException of PolicyNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetLifecyclePolicyOutput"
    let make ~lifecyclePolicy = fun () -> { lifecyclePolicy }
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `PolicyNotFoundException e ->
          `Assoc
            [("error", (`String "PolicyNotFoundException"));
            ("details", (PolicyNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("LifecyclePolicy",
           (Some (LifecyclePolicy.to_value x.lifecyclePolicy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lifecyclePolicy =
        LifecyclePolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LifecyclePolicy") in
      make ~lifecyclePolicy ()
    let of_json json =
      let lifecyclePolicy =
        field_map_exn json "LifecyclePolicy" LifecyclePolicy.of_json in
      make ~lifecyclePolicy ()
    let to_json v = composed_to_json to_value v
  end
module GetLifecyclePolicyInput =
  struct
    type nonrec t = {
      containerName: ContainerName.t }
    let context_ = "GetLifecyclePolicyInput"
    let make ~containerName = fun () -> { containerName }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~containerName ()
    let of_json json =
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module GetCorsPolicyOutput =
  struct
    type nonrec t = {
      corsPolicy: CorsPolicy.t }
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `CorsPolicyNotFoundException of CorsPolicyNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetCorsPolicyOutput"
    let make ~corsPolicy = fun () -> { corsPolicy }
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "CorsPolicyNotFoundException" ->
          `CorsPolicyNotFoundException
            (CorsPolicyNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "CorsPolicyNotFoundException" ->
          `CorsPolicyNotFoundException
            (CorsPolicyNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `CorsPolicyNotFoundException e ->
          `Assoc
            [("error", (`String "CorsPolicyNotFoundException"));
            ("details", (CorsPolicyNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("CorsPolicy", (Some (CorsPolicy.to_value x.corsPolicy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let corsPolicy =
        CorsPolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CorsPolicy") in
      make ~corsPolicy ()
    let of_json json =
      let corsPolicy = field_map_exn json "CorsPolicy" CorsPolicy.of_json in
      make ~corsPolicy ()
    let to_json v = composed_to_json to_value v
  end
module GetCorsPolicyInput =
  struct
    type nonrec t = {
      containerName: ContainerName.t }
    let context_ = "GetCorsPolicyInput"
    let make ~containerName = fun () -> { containerName }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~containerName ()
    let of_json json =
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module GetContainerPolicyOutput =
  struct
    type nonrec t = {
      policy: ContainerPolicy.t }
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `PolicyNotFoundException of PolicyNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetContainerPolicyOutput"
    let make ~policy = fun () -> { policy }
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `PolicyNotFoundException e ->
          `Assoc
            [("error", (`String "PolicyNotFoundException"));
            ("details", (PolicyNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Policy", (Some (ContainerPolicy.to_value x.policy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        ContainerPolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Policy") in
      make ~policy ()
    let of_json json =
      let policy = field_map_exn json "Policy" ContainerPolicy.of_json in
      make ~policy ()
    let to_json v = composed_to_json to_value v
  end
module GetContainerPolicyInput =
  struct
    type nonrec t = {
      containerName: ContainerName.t }
    let context_ = "GetContainerPolicyInput"
    let make ~containerName = fun () -> { containerName }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~containerName ()
    let of_json json =
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module DescribeContainerOutput =
  struct
    type nonrec t = {
      container: Container.t option }
    type nonrec error =
      [ `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?container = fun () -> { container }
    let error_of_json name json =
      match name with
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Container", (Option.map x.container ~f:Container.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let container =
        (Option.map ~f:Container.of_xml) (Xml.child xml_arg0 "Container") in
      make ?container ()
    let of_json json =
      let container = field_map json "Container" Container.of_json in
      make ?container ()
    let to_json v = composed_to_json to_value v
  end
module DescribeContainerInput =
  struct
    type nonrec t = {
      containerName: ContainerName.t option }
    let make ?containerName = fun () -> { containerName }
    let to_value x =
      structure_to_value
        [("ContainerName",
           (Option.map x.containerName ~f:ContainerName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerName =
        (Option.map ~f:ContainerName.of_xml)
          (Xml.child xml_arg0 "ContainerName") in
      make ?containerName ()
    let of_json json =
      let containerName =
        field_map json "ContainerName" ContainerName.of_json in
      make ?containerName ()
    let to_json v = composed_to_json to_value v
  end
module DeleteMetricPolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `PolicyNotFoundException of PolicyNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `PolicyNotFoundException e ->
          `Assoc
            [("error", (`String "PolicyNotFoundException"));
            ("details", (PolicyNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteMetricPolicyInput =
  struct
    type nonrec t = {
      containerName: ContainerName.t }
    let context_ = "DeleteMetricPolicyInput"
    let make ~containerName = fun () -> { containerName }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~containerName ()
    let of_json json =
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLifecyclePolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `PolicyNotFoundException of PolicyNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `PolicyNotFoundException e ->
          `Assoc
            [("error", (`String "PolicyNotFoundException"));
            ("details", (PolicyNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLifecyclePolicyInput =
  struct
    type nonrec t = {
      containerName: ContainerName.t }
    let context_ = "DeleteLifecyclePolicyInput"
    let make ~containerName = fun () -> { containerName }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~containerName ()
    let of_json json =
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module DeleteCorsPolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `CorsPolicyNotFoundException of CorsPolicyNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "CorsPolicyNotFoundException" ->
          `CorsPolicyNotFoundException
            (CorsPolicyNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "CorsPolicyNotFoundException" ->
          `CorsPolicyNotFoundException
            (CorsPolicyNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `CorsPolicyNotFoundException e ->
          `Assoc
            [("error", (`String "CorsPolicyNotFoundException"));
            ("details", (CorsPolicyNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteCorsPolicyInput =
  struct
    type nonrec t = {
      containerName: ContainerName.t }
    let context_ = "DeleteCorsPolicyInput"
    let make ~containerName = fun () -> { containerName }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~containerName ()
    let of_json json =
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module DeleteContainerPolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `PolicyNotFoundException of PolicyNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "PolicyNotFoundException" ->
          `PolicyNotFoundException (PolicyNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `PolicyNotFoundException e ->
          `Assoc
            [("error", (`String "PolicyNotFoundException"));
            ("details", (PolicyNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteContainerPolicyInput =
  struct
    type nonrec t = {
      containerName: ContainerName.t }
    let context_ = "DeleteContainerPolicyInput"
    let make ~containerName = fun () -> { containerName }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~containerName ()
    let of_json json =
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module DeleteContainerOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `ContainerNotFoundException of ContainerNotFoundException.t 
      | `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException
            (ContainerNotFoundException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "ContainerNotFoundException" ->
          `ContainerNotFoundException (ContainerNotFoundException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `ContainerNotFoundException e ->
          `Assoc
            [("error", (`String "ContainerNotFoundException"));
            ("details", (ContainerNotFoundException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteContainerInput =
  struct
    type nonrec t = {
      containerName: ContainerName.t }
    let context_ = "DeleteContainerInput"
    let make ~containerName = fun () -> { containerName }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ~containerName ()
    let of_json json =
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ~containerName ()
    let to_json v = composed_to_json to_value v
  end
module CreateContainerOutput =
  struct
    type nonrec t = {
      container: Container.t }
    type nonrec error =
      [ `ContainerInUseException of ContainerInUseException.t 
      | `InternalServerError of InternalServerError.t 
      | `LimitExceededException of LimitExceededException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CreateContainerOutput"
    let make ~container = fun () -> { container }
    let error_of_json name json =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_json json)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ContainerInUseException" ->
          `ContainerInUseException (ContainerInUseException.of_xml xml)
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ContainerInUseException e ->
          `Assoc
            [("error", (`String "ContainerInUseException"));
            ("details", (ContainerInUseException.to_json e))]
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Container", (Some (Container.to_value x.container)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let container =
        Container.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Container") in
      make ~container ()
    let of_json json =
      let container = field_map_exn json "Container" Container.of_json in
      make ~container ()
    let to_json v = composed_to_json to_value v
  end
module CreateContainerInput =
  struct
    type nonrec t = {
      containerName: ContainerName.t ;
      tags: TagList.t option }
    let context_ = "CreateContainerInput"
    let make ?tags = fun ~containerName -> fun () -> { tags; containerName }
    let to_value x =
      structure_to_value
        [("ContainerName", (Some (ContainerName.to_value x.containerName)));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let containerName =
        ContainerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerName") in
      make ?tags ~containerName ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let containerName =
        field_map_exn json "ContainerName" ContainerName.of_json in
      make ?tags ~containerName ()
    let to_json v = composed_to_json to_value v
  end
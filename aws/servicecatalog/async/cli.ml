(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_portfolio_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and portfolioShareType =
         flag "portfolio-share-type" (optional json_arg)
           ~doc:"JSON PortfolioShareType"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_portfolio_share
           (Values.AcceptPortfolioShareInput.make ?acceptLanguage
              ?portfolioShareType:(Option.map
                                     ~f:Values.PortfolioShareType.of_json
                                     portfolioShareType) ~portfolioId ())
           (Some Values.AcceptPortfolioShareOutput.to_json)
           (Some Values.AcceptPortfolioShareOutput.error_to_json)])
let associate_budget_with_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and resourceId = flag "resource-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_budget_with_resource
           (Values.AssociateBudgetWithResourceInput.make ~budgetName
              ~resourceId ())
           (Some Values.AssociateBudgetWithResourceOutput.to_json)
           (Some Values.AssociateBudgetWithResourceOutput.error_to_json)])
let associate_principal_with_portfolio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id"
       and principalARN =
         flag "principal-a-r-n" (required string) ~doc:"STRING PrincipalARN"
       and principalType =
         flag "principal-type" (required json_arg) ~doc:"JSON PrincipalType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_principal_with_portfolio
           (Values.AssociatePrincipalWithPortfolioInput.make ?acceptLanguage
              ~portfolioId ~principalARN
              ~principalType:(Values.PrincipalType.of_json principalType) ())
           (Some Values.AssociatePrincipalWithPortfolioOutput.to_json)
           (Some Values.AssociatePrincipalWithPortfolioOutput.error_to_json)])
let associate_product_with_portfolio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and sourcePortfolioId =
         flag "source-portfolio-id" (optional string) ~doc:"STRING Id"
       and productId = flag "product-id" (required string) ~doc:"STRING Id"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_product_with_portfolio
           (Values.AssociateProductWithPortfolioInput.make ?acceptLanguage
              ?sourcePortfolioId ~productId ~portfolioId ())
           (Some Values.AssociateProductWithPortfolioOutput.to_json)
           (Some Values.AssociateProductWithPortfolioOutput.error_to_json)])
let associate_service_action_with_provisioning_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and productId = flag "product-id" (required string) ~doc:"STRING Id"
       and provisioningArtifactId =
         flag "provisioning-artifact-id" (required string) ~doc:"STRING Id"
       and serviceActionId =
         flag "service-action-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_service_action_with_provisioning_artifact
           (Values.AssociateServiceActionWithProvisioningArtifactInput.make
              ?acceptLanguage ~productId ~provisioningArtifactId
              ~serviceActionId ())
           (Some
              Values.AssociateServiceActionWithProvisioningArtifactOutput.to_json)
           (Some
              Values.AssociateServiceActionWithProvisioningArtifactOutput.error_to_json)])
let associate_tag_option_with_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and tagOptionId =
         flag "tag-option-id" (required string) ~doc:"STRING TagOptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_tag_option_with_resource
           (Values.AssociateTagOptionWithResourceInput.make ~resourceId
              ~tagOptionId ())
           (Some Values.AssociateTagOptionWithResourceOutput.to_json)
           (Some Values.AssociateTagOptionWithResourceOutput.error_to_json)])
let batch_associate_service_action_with_provisioning_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and serviceActionAssociations =
         flag "service-action-associations" (required json_arg)
           ~doc:"JSON ServiceActionAssociations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_associate_service_action_with_provisioning_artifact
           (Values.BatchAssociateServiceActionWithProvisioningArtifactInput.make
              ?acceptLanguage
              ~serviceActionAssociations:(Values.ServiceActionAssociations.of_json
                                            serviceActionAssociations) ())
           (Some
              Values.BatchAssociateServiceActionWithProvisioningArtifactOutput.to_json)
           (Some
              Values.BatchAssociateServiceActionWithProvisioningArtifactOutput.error_to_json)])
let batch_disassociate_service_action_from_provisioning_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and serviceActionAssociations =
         flag "service-action-associations" (required json_arg)
           ~doc:"JSON ServiceActionAssociations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_disassociate_service_action_from_provisioning_artifact
           (Values.BatchDisassociateServiceActionFromProvisioningArtifactInput.make
              ?acceptLanguage
              ~serviceActionAssociations:(Values.ServiceActionAssociations.of_json
                                            serviceActionAssociations) ())
           (Some
              Values.BatchDisassociateServiceActionFromProvisioningArtifactOutput.to_json)
           (Some
              Values.BatchDisassociateServiceActionFromProvisioningArtifactOutput.error_to_json)])
let copy_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and targetProductId =
         flag "target-product-id" (optional string) ~doc:"STRING Id"
       and targetProductName =
         flag "target-product-name" (optional string)
           ~doc:"STRING ProductViewName"
       and sourceProvisioningArtifactIdentifiers =
         flag "source-provisioning-artifact-identifiers" (optional json_arg)
           ~doc:"JSON SourceProvisioningArtifactProperties"
       and copyOptions =
         flag "copy-options" (optional json_arg) ~doc:"JSON CopyOptions"
       and sourceProductArn =
         flag "source-product-arn" (required string) ~doc:"STRING ProductArn"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_product
           (Values.CopyProductInput.make ?acceptLanguage ?targetProductId
              ?targetProductName
              ?sourceProvisioningArtifactIdentifiers:(Option.map
                                                        ~f:Values.SourceProvisioningArtifactProperties.of_json
                                                        sourceProvisioningArtifactIdentifiers)
              ?copyOptions:(Option.map ~f:Values.CopyOptions.of_json
                              copyOptions) ~sourceProductArn
              ~idempotencyToken ()) (Some Values.CopyProductOutput.to_json)
           (Some Values.CopyProductOutput.error_to_json)])
let create_constraint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ConstraintDescription"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id"
       and productId = flag "product-id" (required string) ~doc:"STRING Id"
       and parameters =
         flag "parameters" (required string)
           ~doc:"STRING ConstraintParameters"
       and type_ =
         flag "type-" (required string) ~doc:"STRING ConstraintType"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_constraint
           (Values.CreateConstraintInput.make ?acceptLanguage ?description
              ~portfolioId ~productId ~parameters ~type_ ~idempotencyToken ())
           (Some Values.CreateConstraintOutput.to_json)
           (Some Values.CreateConstraintOutput.error_to_json)])
let create_portfolio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PortfolioDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON AddTags"
       and displayName =
         flag "display-name" (required string)
           ~doc:"STRING PortfolioDisplayName"
       and providerName =
         flag "provider-name" (required string) ~doc:"STRING ProviderName"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_portfolio
           (Values.CreatePortfolioInput.make ?acceptLanguage ?description
              ?tags:(Option.map ~f:Values.AddTags.of_json tags) ~displayName
              ~providerName ~idempotencyToken ())
           (Some Values.CreatePortfolioOutput.to_json)
           (Some Values.CreatePortfolioOutput.error_to_json)])
let create_portfolio_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AccountId"
       and organizationNode =
         flag "organization-node" (optional json_arg)
           ~doc:"JSON OrganizationNode"
       and shareTagOptions =
         flag "share-tag-options" (optional bool) ~doc:"BOOL Boolean"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_portfolio_share
           (Values.CreatePortfolioShareInput.make ?acceptLanguage ?accountId
              ?organizationNode:(Option.map
                                   ~f:Values.OrganizationNode.of_json
                                   organizationNode) ?shareTagOptions
              ~portfolioId ())
           (Some Values.CreatePortfolioShareOutput.to_json)
           (Some Values.CreatePortfolioShareOutput.error_to_json)])
let create_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ProductViewShortDescription"
       and distributor =
         flag "distributor" (optional string) ~doc:"STRING ProductViewOwner"
       and supportDescription =
         flag "support-description" (optional string)
           ~doc:"STRING SupportDescription"
       and supportEmail =
         flag "support-email" (optional string) ~doc:"STRING SupportEmail"
       and supportUrl =
         flag "support-url" (optional string) ~doc:"STRING SupportUrl"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON AddTags"
       and name = flag "name" (required string) ~doc:"STRING ProductViewName"
       and owner =
         flag "owner" (required string) ~doc:"STRING ProductViewOwner"
       and productType =
         flag "product-type" (required json_arg) ~doc:"JSON ProductType"
       and provisioningArtifactParameters =
         flag "provisioning-artifact-parameters" (required json_arg)
           ~doc:"JSON ProvisioningArtifactProperties"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_product
           (Values.CreateProductInput.make ?acceptLanguage ?description
              ?distributor ?supportDescription ?supportEmail ?supportUrl
              ?tags:(Option.map ~f:Values.AddTags.of_json tags) ~name ~owner
              ~productType:(Values.ProductType.of_json productType)
              ~provisioningArtifactParameters:(Values.ProvisioningArtifactProperties.of_json
                                                 provisioningArtifactParameters)
              ~idempotencyToken ()) (Some Values.CreateProductOutput.to_json)
           (Some Values.CreateProductOutput.error_to_json)])
let create_provisioned_product_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and notificationArns =
         flag "notification-arns" (optional json_arg)
           ~doc:"JSON NotificationArns"
       and pathId = flag "path-id" (optional string) ~doc:"STRING Id"
       and provisioningParameters =
         flag "provisioning-parameters" (optional json_arg)
           ~doc:"JSON UpdateProvisioningParameters"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and planName =
         flag "plan-name" (required string)
           ~doc:"STRING ProvisionedProductPlanName"
       and planType =
         flag "plan-type" (required json_arg)
           ~doc:"JSON ProvisionedProductPlanType"
       and productId = flag "product-id" (required string) ~doc:"STRING Id"
       and provisionedProductName =
         flag "provisioned-product-name" (required string)
           ~doc:"STRING ProvisionedProductName"
       and provisioningArtifactId =
         flag "provisioning-artifact-id" (required string) ~doc:"STRING Id"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_provisioned_product_plan
           (Values.CreateProvisionedProductPlanInput.make ?acceptLanguage
              ?notificationArns:(Option.map
                                   ~f:Values.NotificationArns.of_json
                                   notificationArns) ?pathId
              ?provisioningParameters:(Option.map
                                         ~f:Values.UpdateProvisioningParameters.of_json
                                         provisioningParameters)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~planName
              ~planType:(Values.ProvisionedProductPlanType.of_json planType)
              ~productId ~provisionedProductName ~provisioningArtifactId
              ~idempotencyToken ())
           (Some Values.CreateProvisionedProductPlanOutput.to_json)
           (Some Values.CreateProvisionedProductPlanOutput.error_to_json)])
let create_provisioning_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and productId = flag "product-id" (required string) ~doc:"STRING Id"
       and parameters =
         flag "parameters" (required json_arg)
           ~doc:"JSON ProvisioningArtifactProperties"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_provisioning_artifact
           (Values.CreateProvisioningArtifactInput.make ?acceptLanguage
              ~productId
              ~parameters:(Values.ProvisioningArtifactProperties.of_json
                             parameters) ~idempotencyToken ())
           (Some Values.CreateProvisioningArtifactOutput.to_json)
           (Some Values.CreateProvisioningArtifactOutput.error_to_json)])
let create_service_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ServiceActionDescription"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and name =
         flag "name" (required string) ~doc:"STRING ServiceActionName"
       and definitionType =
         flag "definition-type" (required json_arg)
           ~doc:"JSON ServiceActionDefinitionType"
       and definition =
         flag "definition" (required json_arg)
           ~doc:"JSON ServiceActionDefinitionMap"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service_action
           (Values.CreateServiceActionInput.make ?description ?acceptLanguage
              ~name
              ~definitionType:(Values.ServiceActionDefinitionType.of_json
                                 definitionType)
              ~definition:(Values.ServiceActionDefinitionMap.of_json
                             definition) ~idempotencyToken ())
           (Some Values.CreateServiceActionOutput.to_json)
           (Some Values.CreateServiceActionOutput.error_to_json)])
let create_tag_option =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and key = flag "key" (required string) ~doc:"STRING TagOptionKey"
       and value =
         flag "value" (required string) ~doc:"STRING TagOptionValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_tag_option
           (Values.CreateTagOptionInput.make ~key ~value ())
           (Some Values.CreateTagOptionOutput.to_json)
           (Some Values.CreateTagOptionOutput.error_to_json)])
let delete_constraint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_constraint
           (Values.DeleteConstraintInput.make ?acceptLanguage ~id ())
           (Some Values.DeleteConstraintOutput.to_json)
           (Some Values.DeleteConstraintOutput.error_to_json)])
let delete_portfolio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_portfolio
           (Values.DeletePortfolioInput.make ?acceptLanguage ~id ())
           (Some Values.DeletePortfolioOutput.to_json)
           (Some Values.DeletePortfolioOutput.error_to_json)])
let delete_portfolio_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AccountId"
       and organizationNode =
         flag "organization-node" (optional json_arg)
           ~doc:"JSON OrganizationNode"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_portfolio_share
           (Values.DeletePortfolioShareInput.make ?acceptLanguage ?accountId
              ?organizationNode:(Option.map
                                   ~f:Values.OrganizationNode.of_json
                                   organizationNode) ~portfolioId ())
           (Some Values.DeletePortfolioShareOutput.to_json)
           (Some Values.DeletePortfolioShareOutput.error_to_json)])
let delete_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_product
           (Values.DeleteProductInput.make ?acceptLanguage ~id ())
           (Some Values.DeleteProductOutput.to_json)
           (Some Values.DeleteProductOutput.error_to_json)])
let delete_provisioned_product_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and ignoreErrors =
         flag "ignore-errors" (optional bool) ~doc:"BOOL IgnoreErrors"
       and planId = flag "plan-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_provisioned_product_plan
           (Values.DeleteProvisionedProductPlanInput.make ?acceptLanguage
              ?ignoreErrors ~planId ())
           (Some Values.DeleteProvisionedProductPlanOutput.to_json)
           (Some Values.DeleteProvisionedProductPlanOutput.error_to_json)])
let delete_provisioning_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and productId = flag "product-id" (required string) ~doc:"STRING Id"
       and provisioningArtifactId =
         flag "provisioning-artifact-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_provisioning_artifact
           (Values.DeleteProvisioningArtifactInput.make ?acceptLanguage
              ~productId ~provisioningArtifactId ())
           (Some Values.DeleteProvisioningArtifactOutput.to_json)
           (Some Values.DeleteProvisioningArtifactOutput.error_to_json)])
let delete_service_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service_action
           (Values.DeleteServiceActionInput.make ?acceptLanguage ~id ())
           (Some Values.DeleteServiceActionOutput.to_json)
           (Some Values.DeleteServiceActionOutput.error_to_json)])
let delete_tag_option =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING TagOptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tag_option (Values.DeleteTagOptionInput.make ~id ())
           (Some Values.DeleteTagOptionOutput.to_json)
           (Some Values.DeleteTagOptionOutput.error_to_json)])
let describe_constraint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_constraint
           (Values.DescribeConstraintInput.make ?acceptLanguage ~id ())
           (Some Values.DescribeConstraintOutput.to_json)
           (Some Values.DescribeConstraintOutput.error_to_json)])
let describe_copy_product_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and copyProductToken =
         flag "copy-product-token" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_copy_product_status
           (Values.DescribeCopyProductStatusInput.make ?acceptLanguage
              ~copyProductToken ())
           (Some Values.DescribeCopyProductStatusOutput.to_json)
           (Some Values.DescribeCopyProductStatusOutput.error_to_json)])
let describe_portfolio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_portfolio
           (Values.DescribePortfolioInput.make ?acceptLanguage ~id ())
           (Some Values.DescribePortfolioOutput.to_json)
           (Some Values.DescribePortfolioOutput.error_to_json)])
let describe_portfolio_share_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portfolioShareToken =
         flag "portfolio-share-token" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_portfolio_share_status
           (Values.DescribePortfolioShareStatusInput.make
              ~portfolioShareToken ())
           (Some Values.DescribePortfolioShareStatusOutput.to_json)
           (Some Values.DescribePortfolioShareStatusOutput.error_to_json)])
let describe_portfolio_shares =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and pageSize =
         flag "page-size" (optional int) ~doc:"INT PageSizeMax100"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id"
       and type_ =
         flag "type-" (required json_arg)
           ~doc:"JSON DescribePortfolioShareType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_portfolio_shares
           (Values.DescribePortfolioSharesInput.make ?pageToken ?pageSize
              ~portfolioId
              ~type_:(Values.DescribePortfolioShareType.of_json type_) ())
           (Some Values.DescribePortfolioSharesOutput.to_json)
           (Some Values.DescribePortfolioSharesOutput.error_to_json)])
let describe_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and id = flag "id" (optional string) ~doc:"STRING Id"
       and name = flag "name" (optional string) ~doc:"STRING ProductViewName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_product
           (Values.DescribeProductInput.make ?acceptLanguage ?id ?name ())
           (Some Values.DescribeProductOutput.to_json)
           (Some Values.DescribeProductOutput.error_to_json)])
let describe_product_as_admin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and id = flag "id" (optional string) ~doc:"STRING Id"
       and name = flag "name" (optional string) ~doc:"STRING ProductViewName"
       and sourcePortfolioId =
         flag "source-portfolio-id" (optional string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_product_as_admin
           (Values.DescribeProductAsAdminInput.make ?acceptLanguage ?id ?name
              ?sourcePortfolioId ())
           (Some Values.DescribeProductAsAdminOutput.to_json)
           (Some Values.DescribeProductAsAdminOutput.error_to_json)])
let describe_product_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_product_view
           (Values.DescribeProductViewInput.make ?acceptLanguage ~id ())
           (Some Values.DescribeProductViewOutput.to_json)
           (Some Values.DescribeProductViewOutput.error_to_json)])
let describe_provisioned_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and id = flag "id" (optional string) ~doc:"STRING Id"
       and name =
         flag "name" (optional string) ~doc:"STRING ProvisionedProductName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_provisioned_product
           (Values.DescribeProvisionedProductInput.make ?acceptLanguage ?id
              ?name ())
           (Some Values.DescribeProvisionedProductOutput.to_json)
           (Some Values.DescribeProvisionedProductOutput.error_to_json)])
let describe_provisioned_product_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and planId = flag "plan-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_provisioned_product_plan
           (Values.DescribeProvisionedProductPlanInput.make ?acceptLanguage
              ?pageSize ?pageToken ~planId ())
           (Some Values.DescribeProvisionedProductPlanOutput.to_json)
           (Some Values.DescribeProvisionedProductPlanOutput.error_to_json)])
let describe_provisioning_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and provisioningArtifactId =
         flag "provisioning-artifact-id" (optional string) ~doc:"STRING Id"
       and productId = flag "product-id" (optional string) ~doc:"STRING Id"
       and provisioningArtifactName =
         flag "provisioning-artifact-name" (optional string)
           ~doc:"STRING ProvisioningArtifactName"
       and productName =
         flag "product-name" (optional string) ~doc:"STRING ProductViewName"
       and verbose = flag "verbose" (optional bool) ~doc:"BOOL Verbose" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_provisioning_artifact
           (Values.DescribeProvisioningArtifactInput.make ?acceptLanguage
              ?provisioningArtifactId ?productId ?provisioningArtifactName
              ?productName ?verbose ())
           (Some Values.DescribeProvisioningArtifactOutput.to_json)
           (Some Values.DescribeProvisioningArtifactOutput.error_to_json)])
let describe_provisioning_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and productId = flag "product-id" (optional string) ~doc:"STRING Id"
       and productName =
         flag "product-name" (optional string) ~doc:"STRING ProductViewName"
       and provisioningArtifactId =
         flag "provisioning-artifact-id" (optional string) ~doc:"STRING Id"
       and provisioningArtifactName =
         flag "provisioning-artifact-name" (optional string)
           ~doc:"STRING ProvisioningArtifactName"
       and pathId = flag "path-id" (optional string) ~doc:"STRING Id"
       and pathName =
         flag "path-name" (optional string)
           ~doc:"STRING PortfolioDisplayName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_provisioning_parameters
           (Values.DescribeProvisioningParametersInput.make ?acceptLanguage
              ?productId ?productName ?provisioningArtifactId
              ?provisioningArtifactName ?pathId ?pathName ())
           (Some Values.DescribeProvisioningParametersOutput.to_json)
           (Some Values.DescribeProvisioningParametersOutput.error_to_json)])
let describe_record =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_record
           (Values.DescribeRecordInput.make ?acceptLanguage ?pageToken
              ?pageSize ~id ()) (Some Values.DescribeRecordOutput.to_json)
           (Some Values.DescribeRecordOutput.error_to_json)])
let describe_service_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_service_action
           (Values.DescribeServiceActionInput.make ?acceptLanguage ~id ())
           (Some Values.DescribeServiceActionOutput.to_json)
           (Some Values.DescribeServiceActionOutput.error_to_json)])
let describe_service_action_execution_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and provisionedProductId =
         flag "provisioned-product-id" (required string) ~doc:"STRING Id"
       and serviceActionId =
         flag "service-action-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_service_action_execution_parameters
           (Values.DescribeServiceActionExecutionParametersInput.make
              ?acceptLanguage ~provisionedProductId ~serviceActionId ())
           (Some
              Values.DescribeServiceActionExecutionParametersOutput.to_json)
           (Some
              Values.DescribeServiceActionExecutionParametersOutput.error_to_json)])
let describe_tag_option =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING TagOptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_tag_option (Values.DescribeTagOptionInput.make ~id ())
           (Some Values.DescribeTagOptionOutput.to_json)
           (Some Values.DescribeTagOptionOutput.error_to_json)])
let disable_a_w_s_organizations_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_a_w_s_organizations_access
           (Values.DisableAWSOrganizationsAccessInput.make ())
           (Some Values.DisableAWSOrganizationsAccessOutput.to_json)
           (Some Values.DisableAWSOrganizationsAccessOutput.error_to_json)])
let disassociate_budget_from_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and budgetName =
         flag "budget-name" (required string) ~doc:"STRING BudgetName"
       and resourceId = flag "resource-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_budget_from_resource
           (Values.DisassociateBudgetFromResourceInput.make ~budgetName
              ~resourceId ())
           (Some Values.DisassociateBudgetFromResourceOutput.to_json)
           (Some Values.DisassociateBudgetFromResourceOutput.error_to_json)])
let disassociate_principal_from_portfolio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id"
       and principalARN =
         flag "principal-a-r-n" (required string) ~doc:"STRING PrincipalARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_principal_from_portfolio
           (Values.DisassociatePrincipalFromPortfolioInput.make
              ?acceptLanguage ~portfolioId ~principalARN ())
           (Some Values.DisassociatePrincipalFromPortfolioOutput.to_json)
           (Some
              Values.DisassociatePrincipalFromPortfolioOutput.error_to_json)])
let disassociate_product_from_portfolio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and productId = flag "product-id" (required string) ~doc:"STRING Id"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_product_from_portfolio
           (Values.DisassociateProductFromPortfolioInput.make ?acceptLanguage
              ~productId ~portfolioId ())
           (Some Values.DisassociateProductFromPortfolioOutput.to_json)
           (Some Values.DisassociateProductFromPortfolioOutput.error_to_json)])
let disassociate_service_action_from_provisioning_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and productId = flag "product-id" (required string) ~doc:"STRING Id"
       and provisioningArtifactId =
         flag "provisioning-artifact-id" (required string) ~doc:"STRING Id"
       and serviceActionId =
         flag "service-action-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_service_action_from_provisioning_artifact
           (Values.DisassociateServiceActionFromProvisioningArtifactInput.make
              ?acceptLanguage ~productId ~provisioningArtifactId
              ~serviceActionId ())
           (Some
              Values.DisassociateServiceActionFromProvisioningArtifactOutput.to_json)
           (Some
              Values.DisassociateServiceActionFromProvisioningArtifactOutput.error_to_json)])
let disassociate_tag_option_from_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and tagOptionId =
         flag "tag-option-id" (required string) ~doc:"STRING TagOptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_tag_option_from_resource
           (Values.DisassociateTagOptionFromResourceInput.make ~resourceId
              ~tagOptionId ())
           (Some Values.DisassociateTagOptionFromResourceOutput.to_json)
           (Some Values.DisassociateTagOptionFromResourceOutput.error_to_json)])
let enable_a_w_s_organizations_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_a_w_s_organizations_access
           (Values.EnableAWSOrganizationsAccessInput.make ())
           (Some Values.EnableAWSOrganizationsAccessOutput.to_json)
           (Some Values.EnableAWSOrganizationsAccessOutput.error_to_json)])
let execute_provisioned_product_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and planId = flag "plan-id" (required string) ~doc:"STRING Id"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_provisioned_product_plan
           (Values.ExecuteProvisionedProductPlanInput.make ?acceptLanguage
              ~planId ~idempotencyToken ())
           (Some Values.ExecuteProvisionedProductPlanOutput.to_json)
           (Some Values.ExecuteProvisionedProductPlanOutput.error_to_json)])
let execute_provisioned_product_service_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON ExecutionParameterMap"
       and provisionedProductId =
         flag "provisioned-product-id" (required string) ~doc:"STRING Id"
       and serviceActionId =
         flag "service-action-id" (required string) ~doc:"STRING Id"
       and executeToken =
         flag "execute-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_provisioned_product_service_action
           (Values.ExecuteProvisionedProductServiceActionInput.make
              ?acceptLanguage
              ?parameters:(Option.map ~f:Values.ExecutionParameterMap.of_json
                             parameters) ~provisionedProductId
              ~serviceActionId ~executeToken ())
           (Some Values.ExecuteProvisionedProductServiceActionOutput.to_json)
           (Some
              Values.ExecuteProvisionedProductServiceActionOutput.error_to_json)])
let get_a_w_s_organizations_access_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_a_w_s_organizations_access_status
           (Values.GetAWSOrganizationsAccessStatusInput.make ())
           (Some Values.GetAWSOrganizationsAccessStatusOutput.to_json)
           (Some Values.GetAWSOrganizationsAccessStatusOutput.error_to_json)])
let get_provisioned_product_outputs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and provisionedProductId =
         flag "provisioned-product-id" (optional string) ~doc:"STRING Id"
       and provisionedProductName =
         flag "provisioned-product-name" (optional string)
           ~doc:"STRING ProvisionedProductName"
       and outputKeys =
         flag "output-keys" (optional json_arg) ~doc:"JSON OutputKeys"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_provisioned_product_outputs
           (Values.GetProvisionedProductOutputsInput.make ?acceptLanguage
              ?provisionedProductId ?provisionedProductName
              ?outputKeys:(Option.map ~f:Values.OutputKeys.of_json outputKeys)
              ?pageSize ?pageToken ())
           (Some Values.GetProvisionedProductOutputsOutput.to_json)
           (Some Values.GetProvisionedProductOutputsOutput.error_to_json)])
let import_as_provisioned_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and productId = flag "product-id" (required string) ~doc:"STRING Id"
       and provisioningArtifactId =
         flag "provisioning-artifact-id" (required string) ~doc:"STRING Id"
       and provisionedProductName =
         flag "provisioned-product-name" (required string)
           ~doc:"STRING ProvisionedProductName"
       and physicalId =
         flag "physical-id" (required string) ~doc:"STRING PhysicalId"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_as_provisioned_product
           (Values.ImportAsProvisionedProductInput.make ?acceptLanguage
              ~productId ~provisioningArtifactId ~provisionedProductName
              ~physicalId ~idempotencyToken ())
           (Some Values.ImportAsProvisionedProductOutput.to_json)
           (Some Values.ImportAsProvisionedProductOutput.error_to_json)])
let list_accepted_portfolio_shares =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and pageSize =
         flag "page-size" (optional int) ~doc:"INT PageSizeMax100"
       and portfolioShareType =
         flag "portfolio-share-type" (optional json_arg)
           ~doc:"JSON PortfolioShareType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_accepted_portfolio_shares
           (Values.ListAcceptedPortfolioSharesInput.make ?acceptLanguage
              ?pageToken ?pageSize
              ?portfolioShareType:(Option.map
                                     ~f:Values.PortfolioShareType.of_json
                                     portfolioShareType) ())
           (Some Values.ListAcceptedPortfolioSharesOutput.to_json)
           (Some Values.ListAcceptedPortfolioSharesOutput.error_to_json)])
let list_budgets_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and resourceId = flag "resource-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_budgets_for_resource
           (Values.ListBudgetsForResourceInput.make ?acceptLanguage ?pageSize
              ?pageToken ~resourceId ())
           (Some Values.ListBudgetsForResourceOutput.to_json)
           (Some Values.ListBudgetsForResourceOutput.error_to_json)])
let list_constraints_for_portfolio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and productId = flag "product-id" (optional string) ~doc:"STRING Id"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_constraints_for_portfolio
           (Values.ListConstraintsForPortfolioInput.make ?acceptLanguage
              ?productId ?pageSize ?pageToken ~portfolioId ())
           (Some Values.ListConstraintsForPortfolioOutput.to_json)
           (Some Values.ListConstraintsForPortfolioOutput.error_to_json)])
let list_launch_paths =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and productId = flag "product-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_launch_paths
           (Values.ListLaunchPathsInput.make ?acceptLanguage ?pageSize
              ?pageToken ~productId ())
           (Some Values.ListLaunchPathsOutput.to_json)
           (Some Values.ListLaunchPathsOutput.error_to_json)])
let list_organization_portfolio_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id"
       and organizationNodeType =
         flag "organization-node-type" (required json_arg)
           ~doc:"JSON OrganizationNodeType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_organization_portfolio_access
           (Values.ListOrganizationPortfolioAccessInput.make ?acceptLanguage
              ?pageToken ?pageSize ~portfolioId
              ~organizationNodeType:(Values.OrganizationNodeType.of_json
                                       organizationNodeType) ())
           (Some Values.ListOrganizationPortfolioAccessOutput.to_json)
           (Some Values.ListOrganizationPortfolioAccessOutput.error_to_json)])
let list_portfolio_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and organizationParentId =
         flag "organization-parent-id" (optional string) ~doc:"STRING Id"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and pageSize =
         flag "page-size" (optional int) ~doc:"INT PageSizeMax100"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_portfolio_access
           (Values.ListPortfolioAccessInput.make ?acceptLanguage
              ?organizationParentId ?pageToken ?pageSize ~portfolioId ())
           (Some Values.ListPortfolioAccessOutput.to_json)
           (Some Values.ListPortfolioAccessOutput.error_to_json)])
let list_portfolios =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and pageSize =
         flag "page-size" (optional int) ~doc:"INT PageSizeMax100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_portfolios
           (Values.ListPortfoliosInput.make ?acceptLanguage ?pageToken
              ?pageSize ()) (Some Values.ListPortfoliosOutput.to_json)
           (Some Values.ListPortfoliosOutput.error_to_json)])
let list_portfolios_for_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and pageSize =
         flag "page-size" (optional int) ~doc:"INT PageSizeMax100"
       and productId = flag "product-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_portfolios_for_product
           (Values.ListPortfoliosForProductInput.make ?acceptLanguage
              ?pageToken ?pageSize ~productId ())
           (Some Values.ListPortfoliosForProductOutput.to_json)
           (Some Values.ListPortfoliosForProductOutput.error_to_json)])
let list_principals_for_portfolio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_principals_for_portfolio
           (Values.ListPrincipalsForPortfolioInput.make ?acceptLanguage
              ?pageSize ?pageToken ~portfolioId ())
           (Some Values.ListPrincipalsForPortfolioOutput.to_json)
           (Some Values.ListPrincipalsForPortfolioOutput.error_to_json)])
let list_provisioned_product_plans =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and provisionProductId =
         flag "provision-product-id" (optional string) ~doc:"STRING Id"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and accessLevelFilter =
         flag "access-level-filter" (optional json_arg)
           ~doc:"JSON AccessLevelFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_provisioned_product_plans
           (Values.ListProvisionedProductPlansInput.make ?acceptLanguage
              ?provisionProductId ?pageSize ?pageToken
              ?accessLevelFilter:(Option.map
                                    ~f:Values.AccessLevelFilter.of_json
                                    accessLevelFilter) ())
           (Some Values.ListProvisionedProductPlansOutput.to_json)
           (Some Values.ListProvisionedProductPlansOutput.error_to_json)])
let list_provisioning_artifacts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and productId = flag "product-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_provisioning_artifacts
           (Values.ListProvisioningArtifactsInput.make ?acceptLanguage
              ~productId ())
           (Some Values.ListProvisioningArtifactsOutput.to_json)
           (Some Values.ListProvisioningArtifactsOutput.error_to_json)])
let list_provisioning_artifacts_for_service_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and serviceActionId =
         flag "service-action-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_provisioning_artifacts_for_service_action
           (Values.ListProvisioningArtifactsForServiceActionInput.make
              ?pageSize ?pageToken ?acceptLanguage ~serviceActionId ())
           (Some
              Values.ListProvisioningArtifactsForServiceActionOutput.to_json)
           (Some
              Values.ListProvisioningArtifactsForServiceActionOutput.error_to_json)])
let list_record_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and accessLevelFilter =
         flag "access-level-filter" (optional json_arg)
           ~doc:"JSON AccessLevelFilter"
       and searchFilter =
         flag "search-filter" (optional json_arg)
           ~doc:"JSON ListRecordHistorySearchFilter"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_record_history
           (Values.ListRecordHistoryInput.make ?acceptLanguage
              ?accessLevelFilter:(Option.map
                                    ~f:Values.AccessLevelFilter.of_json
                                    accessLevelFilter)
              ?searchFilter:(Option.map
                               ~f:Values.ListRecordHistorySearchFilter.of_json
                               searchFilter) ?pageSize ?pageToken ())
           (Some Values.ListRecordHistoryOutput.to_json)
           (Some Values.ListRecordHistoryOutput.error_to_json)])
let list_resources_for_tag_option =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (optional string) ~doc:"STRING ResourceType"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and tagOptionId =
         flag "tag-option-id" (required string) ~doc:"STRING TagOptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resources_for_tag_option
           (Values.ListResourcesForTagOptionInput.make ?resourceType
              ?pageSize ?pageToken ~tagOptionId ())
           (Some Values.ListResourcesForTagOptionOutput.to_json)
           (Some Values.ListResourcesForTagOptionOutput.error_to_json)])
let list_service_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_actions
           (Values.ListServiceActionsInput.make ?acceptLanguage ?pageSize
              ?pageToken ()) (Some Values.ListServiceActionsOutput.to_json)
           (Some Values.ListServiceActionsOutput.error_to_json)])
let list_service_actions_for_provisioning_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and productId = flag "product-id" (required string) ~doc:"STRING Id"
       and provisioningArtifactId =
         flag "provisioning-artifact-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_actions_for_provisioning_artifact
           (Values.ListServiceActionsForProvisioningArtifactInput.make
              ?pageSize ?pageToken ?acceptLanguage ~productId
              ~provisioningArtifactId ())
           (Some
              Values.ListServiceActionsForProvisioningArtifactOutput.to_json)
           (Some
              Values.ListServiceActionsForProvisioningArtifactOutput.error_to_json)])
let list_stack_instances_for_provisioned_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and provisionedProductId =
         flag "provisioned-product-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stack_instances_for_provisioned_product
           (Values.ListStackInstancesForProvisionedProductInput.make
              ?acceptLanguage ?pageToken ?pageSize ~provisionedProductId ())
           (Some Values.ListStackInstancesForProvisionedProductOutput.to_json)
           (Some
              Values.ListStackInstancesForProvisionedProductOutput.error_to_json)])
let list_tag_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ListTagOptionsFilters"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tag_options
           (Values.ListTagOptionsInput.make
              ?filters:(Option.map ~f:Values.ListTagOptionsFilters.of_json
                          filters) ?pageSize ?pageToken ())
           (Some Values.ListTagOptionsOutput.to_json)
           (Some Values.ListTagOptionsOutput.error_to_json)])
let provision_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and productId = flag "product-id" (optional string) ~doc:"STRING Id"
       and productName =
         flag "product-name" (optional string) ~doc:"STRING ProductViewName"
       and provisioningArtifactId =
         flag "provisioning-artifact-id" (optional string) ~doc:"STRING Id"
       and provisioningArtifactName =
         flag "provisioning-artifact-name" (optional string)
           ~doc:"STRING ProvisioningArtifactName"
       and pathId = flag "path-id" (optional string) ~doc:"STRING Id"
       and pathName =
         flag "path-name" (optional string)
           ~doc:"STRING PortfolioDisplayName"
       and provisioningParameters =
         flag "provisioning-parameters" (optional json_arg)
           ~doc:"JSON ProvisioningParameters"
       and provisioningPreferences =
         flag "provisioning-preferences" (optional json_arg)
           ~doc:"JSON ProvisioningPreferences"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and notificationArns =
         flag "notification-arns" (optional json_arg)
           ~doc:"JSON NotificationArns"
       and provisionedProductName =
         flag "provisioned-product-name" (required string)
           ~doc:"STRING ProvisionedProductName"
       and provisionToken =
         flag "provision-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.provision_product
           (Values.ProvisionProductInput.make ?acceptLanguage ?productId
              ?productName ?provisioningArtifactId ?provisioningArtifactName
              ?pathId ?pathName
              ?provisioningParameters:(Option.map
                                         ~f:Values.ProvisioningParameters.of_json
                                         provisioningParameters)
              ?provisioningPreferences:(Option.map
                                          ~f:Values.ProvisioningPreferences.of_json
                                          provisioningPreferences)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?notificationArns:(Option.map
                                   ~f:Values.NotificationArns.of_json
                                   notificationArns) ~provisionedProductName
              ~provisionToken ())
           (Some Values.ProvisionProductOutput.to_json)
           (Some Values.ProvisionProductOutput.error_to_json)])
let reject_portfolio_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and portfolioShareType =
         flag "portfolio-share-type" (optional json_arg)
           ~doc:"JSON PortfolioShareType"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_portfolio_share
           (Values.RejectPortfolioShareInput.make ?acceptLanguage
              ?portfolioShareType:(Option.map
                                     ~f:Values.PortfolioShareType.of_json
                                     portfolioShareType) ~portfolioId ())
           (Some Values.RejectPortfolioShareOutput.to_json)
           (Some Values.RejectPortfolioShareOutput.error_to_json)])
let scan_provisioned_products =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and accessLevelFilter =
         flag "access-level-filter" (optional json_arg)
           ~doc:"JSON AccessLevelFilter"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.scan_provisioned_products
           (Values.ScanProvisionedProductsInput.make ?acceptLanguage
              ?accessLevelFilter:(Option.map
                                    ~f:Values.AccessLevelFilter.of_json
                                    accessLevelFilter) ?pageSize ?pageToken
              ()) (Some Values.ScanProvisionedProductsOutput.to_json)
           (Some Values.ScanProvisionedProductsOutput.error_to_json)])
let search_products =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ProductViewFilters"
       and pageSize =
         flag "page-size" (optional int) ~doc:"INT PageSizeMax100"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON ProductViewSortBy"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_products
           (Values.SearchProductsInput.make ?acceptLanguage
              ?filters:(Option.map ~f:Values.ProductViewFilters.of_json
                          filters) ?pageSize
              ?sortBy:(Option.map ~f:Values.ProductViewSortBy.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?pageToken ()) (Some Values.SearchProductsOutput.to_json)
           (Some Values.SearchProductsOutput.error_to_json)])
let search_products_as_admin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and portfolioId =
         flag "portfolio-id" (optional string) ~doc:"STRING Id"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ProductViewFilters"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON ProductViewSortBy"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and productSource =
         flag "product-source" (optional json_arg) ~doc:"JSON ProductSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_products_as_admin
           (Values.SearchProductsAsAdminInput.make ?acceptLanguage
              ?portfolioId
              ?filters:(Option.map ~f:Values.ProductViewFilters.of_json
                          filters)
              ?sortBy:(Option.map ~f:Values.ProductViewSortBy.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?pageToken ?pageSize
              ?productSource:(Option.map ~f:Values.ProductSource.of_json
                                productSource) ())
           (Some Values.SearchProductsAsAdminOutput.to_json)
           (Some Values.SearchProductsAsAdminOutput.error_to_json)])
let search_provisioned_products =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and accessLevelFilter =
         flag "access-level-filter" (optional json_arg)
           ~doc:"JSON AccessLevelFilter"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ProvisionedProductFilters"
       and sortBy = flag "sort-by" (optional string) ~doc:"STRING SortField"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and pageSize =
         flag "page-size" (optional int)
           ~doc:"INT SearchProvisionedProductsPageSize"
       and pageToken =
         flag "page-token" (optional string) ~doc:"STRING PageToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_provisioned_products
           (Values.SearchProvisionedProductsInput.make ?acceptLanguage
              ?accessLevelFilter:(Option.map
                                    ~f:Values.AccessLevelFilter.of_json
                                    accessLevelFilter)
              ?filters:(Option.map
                          ~f:Values.ProvisionedProductFilters.of_json filters)
              ?sortBy
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?pageSize ?pageToken ())
           (Some Values.SearchProvisionedProductsOutput.to_json)
           (Some Values.SearchProvisionedProductsOutput.error_to_json)])
let terminate_provisioned_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and provisionedProductName =
         flag "provisioned-product-name" (optional string)
           ~doc:"STRING ProvisionedProductNameOrArn"
       and provisionedProductId =
         flag "provisioned-product-id" (optional string) ~doc:"STRING Id"
       and ignoreErrors =
         flag "ignore-errors" (optional bool) ~doc:"BOOL IgnoreErrors"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and retainPhysicalResources =
         flag "retain-physical-resources" (optional bool)
           ~doc:"BOOL RetainPhysicalResources"
       and terminateToken =
         flag "terminate-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.terminate_provisioned_product
           (Values.TerminateProvisionedProductInput.make
              ?provisionedProductName ?provisionedProductId ?ignoreErrors
              ?acceptLanguage ?retainPhysicalResources ~terminateToken ())
           (Some Values.TerminateProvisionedProductOutput.to_json)
           (Some Values.TerminateProvisionedProductOutput.error_to_json)])
let update_constraint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ConstraintDescription"
       and parameters =
         flag "parameters" (optional string)
           ~doc:"STRING ConstraintParameters"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_constraint
           (Values.UpdateConstraintInput.make ?acceptLanguage ?description
              ?parameters ~id ())
           (Some Values.UpdateConstraintOutput.to_json)
           (Some Values.UpdateConstraintOutput.error_to_json)])
let update_portfolio =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING PortfolioDisplayName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PortfolioDescription"
       and providerName =
         flag "provider-name" (optional string) ~doc:"STRING ProviderName"
       and addTags = flag "add-tags" (optional json_arg) ~doc:"JSON AddTags"
       and removeTags =
         flag "remove-tags" (optional json_arg) ~doc:"JSON TagKeys"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_portfolio
           (Values.UpdatePortfolioInput.make ?acceptLanguage ?displayName
              ?description ?providerName
              ?addTags:(Option.map ~f:Values.AddTags.of_json addTags)
              ?removeTags:(Option.map ~f:Values.TagKeys.of_json removeTags)
              ~id ()) (Some Values.UpdatePortfolioOutput.to_json)
           (Some Values.UpdatePortfolioOutput.error_to_json)])
let update_portfolio_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AccountId"
       and organizationNode =
         flag "organization-node" (optional json_arg)
           ~doc:"JSON OrganizationNode"
       and shareTagOptions =
         flag "share-tag-options" (optional bool) ~doc:"BOOL NullableBoolean"
       and portfolioId =
         flag "portfolio-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_portfolio_share
           (Values.UpdatePortfolioShareInput.make ?acceptLanguage ?accountId
              ?organizationNode:(Option.map
                                   ~f:Values.OrganizationNode.of_json
                                   organizationNode) ?shareTagOptions
              ~portfolioId ())
           (Some Values.UpdatePortfolioShareOutput.to_json)
           (Some Values.UpdatePortfolioShareOutput.error_to_json)])
let update_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and name = flag "name" (optional string) ~doc:"STRING ProductViewName"
       and owner =
         flag "owner" (optional string) ~doc:"STRING ProductViewOwner"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ProductViewShortDescription"
       and distributor =
         flag "distributor" (optional string) ~doc:"STRING ProductViewOwner"
       and supportDescription =
         flag "support-description" (optional string)
           ~doc:"STRING SupportDescription"
       and supportEmail =
         flag "support-email" (optional string) ~doc:"STRING SupportEmail"
       and supportUrl =
         flag "support-url" (optional string) ~doc:"STRING SupportUrl"
       and addTags = flag "add-tags" (optional json_arg) ~doc:"JSON AddTags"
       and removeTags =
         flag "remove-tags" (optional json_arg) ~doc:"JSON TagKeys"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_product
           (Values.UpdateProductInput.make ?acceptLanguage ?name ?owner
              ?description ?distributor ?supportDescription ?supportEmail
              ?supportUrl
              ?addTags:(Option.map ~f:Values.AddTags.of_json addTags)
              ?removeTags:(Option.map ~f:Values.TagKeys.of_json removeTags)
              ~id ()) (Some Values.UpdateProductOutput.to_json)
           (Some Values.UpdateProductOutput.error_to_json)])
let update_provisioned_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and provisionedProductName =
         flag "provisioned-product-name" (optional string)
           ~doc:"STRING ProvisionedProductNameOrArn"
       and provisionedProductId =
         flag "provisioned-product-id" (optional string) ~doc:"STRING Id"
       and productId = flag "product-id" (optional string) ~doc:"STRING Id"
       and productName =
         flag "product-name" (optional string) ~doc:"STRING ProductViewName"
       and provisioningArtifactId =
         flag "provisioning-artifact-id" (optional string) ~doc:"STRING Id"
       and provisioningArtifactName =
         flag "provisioning-artifact-name" (optional string)
           ~doc:"STRING ProvisioningArtifactName"
       and pathId = flag "path-id" (optional string) ~doc:"STRING Id"
       and pathName =
         flag "path-name" (optional string)
           ~doc:"STRING PortfolioDisplayName"
       and provisioningParameters =
         flag "provisioning-parameters" (optional json_arg)
           ~doc:"JSON UpdateProvisioningParameters"
       and provisioningPreferences =
         flag "provisioning-preferences" (optional json_arg)
           ~doc:"JSON UpdateProvisioningPreferences"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and updateToken =
         flag "update-token" (required string) ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_provisioned_product
           (Values.UpdateProvisionedProductInput.make ?acceptLanguage
              ?provisionedProductName ?provisionedProductId ?productId
              ?productName ?provisioningArtifactId ?provisioningArtifactName
              ?pathId ?pathName
              ?provisioningParameters:(Option.map
                                         ~f:Values.UpdateProvisioningParameters.of_json
                                         provisioningParameters)
              ?provisioningPreferences:(Option.map
                                          ~f:Values.UpdateProvisioningPreferences.of_json
                                          provisioningPreferences)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~updateToken ())
           (Some Values.UpdateProvisionedProductOutput.to_json)
           (Some Values.UpdateProvisionedProductOutput.error_to_json)])
let update_provisioned_product_properties =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and provisionedProductId =
         flag "provisioned-product-id" (required string) ~doc:"STRING Id"
       and provisionedProductProperties =
         flag "provisioned-product-properties" (required json_arg)
           ~doc:"JSON ProvisionedProductProperties"
       and idempotencyToken =
         flag "idempotency-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_provisioned_product_properties
           (Values.UpdateProvisionedProductPropertiesInput.make
              ?acceptLanguage ~provisionedProductId
              ~provisionedProductProperties:(Values.ProvisionedProductProperties.of_json
                                               provisionedProductProperties)
              ~idempotencyToken ())
           (Some Values.UpdateProvisionedProductPropertiesOutput.to_json)
           (Some
              Values.UpdateProvisionedProductPropertiesOutput.error_to_json)])
let update_provisioning_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and name =
         flag "name" (optional string) ~doc:"STRING ProvisioningArtifactName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ProvisioningArtifactDescription"
       and active =
         flag "active" (optional bool) ~doc:"BOOL ProvisioningArtifactActive"
       and guidance =
         flag "guidance" (optional json_arg)
           ~doc:"JSON ProvisioningArtifactGuidance"
       and productId = flag "product-id" (required string) ~doc:"STRING Id"
       and provisioningArtifactId =
         flag "provisioning-artifact-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_provisioning_artifact
           (Values.UpdateProvisioningArtifactInput.make ?acceptLanguage ?name
              ?description ?active
              ?guidance:(Option.map
                           ~f:Values.ProvisioningArtifactGuidance.of_json
                           guidance) ~productId ~provisioningArtifactId ())
           (Some Values.UpdateProvisioningArtifactOutput.to_json)
           (Some Values.UpdateProvisioningArtifactOutput.error_to_json)])
let update_service_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING ServiceActionName"
       and definition =
         flag "definition" (optional json_arg)
           ~doc:"JSON ServiceActionDefinitionMap"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ServiceActionDescription"
       and acceptLanguage =
         flag "accept-language" (optional string)
           ~doc:"STRING AcceptLanguage"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_action
           (Values.UpdateServiceActionInput.make ?name
              ?definition:(Option.map
                             ~f:Values.ServiceActionDefinitionMap.of_json
                             definition) ?description ?acceptLanguage ~id ())
           (Some Values.UpdateServiceActionOutput.to_json)
           (Some Values.UpdateServiceActionOutput.error_to_json)])
let update_tag_option =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and value =
         flag "value" (optional string) ~doc:"STRING TagOptionValue"
       and active = flag "active" (optional bool) ~doc:"BOOL TagOptionActive"
       and id = flag "id" (required string) ~doc:"STRING TagOptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_tag_option
           (Values.UpdateTagOptionInput.make ?value ?active ~id ())
           (Some Values.UpdateTagOptionOutput.to_json)
           (Some Values.UpdateTagOptionOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("accept-portfolio-share", accept_portfolio_share);
    ("associate-budget-with-resource", associate_budget_with_resource);
    ("associate-principal-with-portfolio",
      associate_principal_with_portfolio);
    ("associate-product-with-portfolio", associate_product_with_portfolio);
    ("associate-service-action-with-provisioning-artifact",
      associate_service_action_with_provisioning_artifact);
    ("associate-tag-option-with-resource",
      associate_tag_option_with_resource);
    ("batch-associate-service-action-with-provisioning-artifact",
      batch_associate_service_action_with_provisioning_artifact);
    ("batch-disassociate-service-action-from-provisioning-artifact",
      batch_disassociate_service_action_from_provisioning_artifact);
    ("copy-product", copy_product);
    ("create-constraint", create_constraint);
    ("create-portfolio", create_portfolio);
    ("create-portfolio-share", create_portfolio_share);
    ("create-product", create_product);
    ("create-provisioned-product-plan", create_provisioned_product_plan);
    ("create-provisioning-artifact", create_provisioning_artifact);
    ("create-service-action", create_service_action);
    ("create-tag-option", create_tag_option);
    ("delete-constraint", delete_constraint);
    ("delete-portfolio", delete_portfolio);
    ("delete-portfolio-share", delete_portfolio_share);
    ("delete-product", delete_product);
    ("delete-provisioned-product-plan", delete_provisioned_product_plan);
    ("delete-provisioning-artifact", delete_provisioning_artifact);
    ("delete-service-action", delete_service_action);
    ("delete-tag-option", delete_tag_option);
    ("describe-constraint", describe_constraint);
    ("describe-copy-product-status", describe_copy_product_status);
    ("describe-portfolio", describe_portfolio);
    ("describe-portfolio-share-status", describe_portfolio_share_status);
    ("describe-portfolio-shares", describe_portfolio_shares);
    ("describe-product", describe_product);
    ("describe-product-as-admin", describe_product_as_admin);
    ("describe-product-view", describe_product_view);
    ("describe-provisioned-product", describe_provisioned_product);
    ("describe-provisioned-product-plan", describe_provisioned_product_plan);
    ("describe-provisioning-artifact", describe_provisioning_artifact);
    ("describe-provisioning-parameters", describe_provisioning_parameters);
    ("describe-record", describe_record);
    ("describe-service-action", describe_service_action);
    ("describe-service-action-execution-parameters",
      describe_service_action_execution_parameters);
    ("describe-tag-option", describe_tag_option);
    ("disable-a-w-s-organizations-access",
      disable_a_w_s_organizations_access);
    ("disassociate-budget-from-resource", disassociate_budget_from_resource);
    ("disassociate-principal-from-portfolio",
      disassociate_principal_from_portfolio);
    ("disassociate-product-from-portfolio",
      disassociate_product_from_portfolio);
    ("disassociate-service-action-from-provisioning-artifact",
      disassociate_service_action_from_provisioning_artifact);
    ("disassociate-tag-option-from-resource",
      disassociate_tag_option_from_resource);
    ("enable-a-w-s-organizations-access", enable_a_w_s_organizations_access);
    ("execute-provisioned-product-plan", execute_provisioned_product_plan);
    ("execute-provisioned-product-service-action",
      execute_provisioned_product_service_action);
    ("get-a-w-s-organizations-access-status",
      get_a_w_s_organizations_access_status);
    ("get-provisioned-product-outputs", get_provisioned_product_outputs);
    ("import-as-provisioned-product", import_as_provisioned_product);
    ("list-accepted-portfolio-shares", list_accepted_portfolio_shares);
    ("list-budgets-for-resource", list_budgets_for_resource);
    ("list-constraints-for-portfolio", list_constraints_for_portfolio);
    ("list-launch-paths", list_launch_paths);
    ("list-organization-portfolio-access",
      list_organization_portfolio_access);
    ("list-portfolio-access", list_portfolio_access);
    ("list-portfolios", list_portfolios);
    ("list-portfolios-for-product", list_portfolios_for_product);
    ("list-principals-for-portfolio", list_principals_for_portfolio);
    ("list-provisioned-product-plans", list_provisioned_product_plans);
    ("list-provisioning-artifacts", list_provisioning_artifacts);
    ("list-provisioning-artifacts-for-service-action",
      list_provisioning_artifacts_for_service_action);
    ("list-record-history", list_record_history);
    ("list-resources-for-tag-option", list_resources_for_tag_option);
    ("list-service-actions", list_service_actions);
    ("list-service-actions-for-provisioning-artifact",
      list_service_actions_for_provisioning_artifact);
    ("list-stack-instances-for-provisioned-product",
      list_stack_instances_for_provisioned_product);
    ("list-tag-options", list_tag_options);
    ("provision-product", provision_product);
    ("reject-portfolio-share", reject_portfolio_share);
    ("scan-provisioned-products", scan_provisioned_products);
    ("search-products", search_products);
    ("search-products-as-admin", search_products_as_admin);
    ("search-provisioned-products", search_provisioned_products);
    ("terminate-provisioned-product", terminate_provisioned_product);
    ("update-constraint", update_constraint);
    ("update-portfolio", update_portfolio);
    ("update-portfolio-share", update_portfolio_share);
    ("update-product", update_product);
    ("update-provisioned-product", update_provisioned_product);
    ("update-provisioned-product-properties",
      update_provisioned_product_properties);
    ("update-provisioning-artifact", update_provisioning_artifact);
    ("update-service-action", update_service_action);
    ("update-tag-option", update_tag_option)]
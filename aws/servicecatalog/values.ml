(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.servicecatalog
let apiVersion = "2015-12-10"
let endpointPrefix = "servicecatalog"
let serviceFullName = "AWS Service Catalog"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let targetPrefix = "AWS242ServiceCatalogService"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module PropertyName =
  struct
    type nonrec t = string
    let context_ = "PropertyName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PropertyName" j
    let to_json = simple_to_json to_value
  end
module RequiresRecreation =
  struct
    type nonrec t =
      | NEVER 
      | CONDITIONALLY 
      | ALWAYS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NEVER -> "NEVER"
      | CONDITIONALLY -> "CONDITIONALLY"
      | ALWAYS -> "ALWAYS"
      | Non_static_id s -> s
    let of_string =
      function
      | "NEVER" -> NEVER
      | "CONDITIONALLY" -> CONDITIONALLY
      | "ALWAYS" -> ALWAYS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RequiresRecreation" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RequiresRecreation" j)
    let to_json = simple_to_json to_value
  end
module ResourceAttribute =
  struct
    type nonrec t =
      | PROPERTIES 
      | METADATA 
      | CREATIONPOLICY 
      | UPDATEPOLICY 
      | DELETIONPOLICY 
      | TAGS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PROPERTIES -> "PROPERTIES"
      | METADATA -> "METADATA"
      | CREATIONPOLICY -> "CREATIONPOLICY"
      | UPDATEPOLICY -> "UPDATEPOLICY"
      | DELETIONPOLICY -> "DELETIONPOLICY"
      | TAGS -> "TAGS"
      | Non_static_id s -> s
    let of_string =
      function
      | "PROPERTIES" -> PROPERTIES
      | "METADATA" -> METADATA
      | "CREATIONPOLICY" -> CREATIONPOLICY
      | "UPDATEPOLICY" -> UPDATEPOLICY
      | "DELETIONPOLICY" -> DELETIONPOLICY
      | "TAGS" -> TAGS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ResourceAttribute" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ResourceAttribute" j)
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module ErrorCode =
  struct
    type nonrec t = string
    let context_ = "ErrorCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorCode" j
    let to_json = simple_to_json to_value
  end
module ErrorDescription =
  struct
    type nonrec t = string
    let context_ = "ErrorDescription"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorDescription" j
    let to_json = simple_to_json to_value
  end
module RecordTagKey =
  struct
    type nonrec t = string
    let context_ = "RecordTagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-%@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RecordTagKey" j
    let to_json = simple_to_json to_value
  end
module RecordTagValue =
  struct
    type nonrec t = string
    let context_ = "RecordTagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-%@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RecordTagValue" j
    let to_json = simple_to_json to_value
  end
module ConstraintDescription =
  struct
    type nonrec t = string
    let context_ = "ConstraintDescription"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:2000); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ConstraintDescription" j
    let to_json = simple_to_json to_value
  end
module ConstraintType =
  struct
    type nonrec t = string
    let context_ = "ConstraintType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ConstraintType" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module CausingEntity =
  struct
    type nonrec t = string
    let context_ = "CausingEntity"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CausingEntity" j
    let to_json = simple_to_json to_value
  end
module EvaluationType =
  struct
    type nonrec t =
      | STATIC 
      | DYNAMIC 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | STATIC -> "STATIC"
      | DYNAMIC -> "DYNAMIC"
      | Non_static_id s -> s
    let of_string =
      function
      | "STATIC" -> STATIC
      | "DYNAMIC" -> DYNAMIC
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EvaluationType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EvaluationType" j)
    let to_json = simple_to_json to_value
  end
module ResourceTargetDefinition =
  struct
    type nonrec t =
      {
      attribute: ResourceAttribute.t option ;
      name: PropertyName.t option ;
      requiresRecreation: RequiresRecreation.t option }
    let make ?attribute =
      fun ?name ->
        fun ?requiresRecreation ->
          fun () -> { attribute; name; requiresRecreation }
    let to_value x =
      structure_to_value
        [("Attribute",
           (Option.map x.attribute ~f:ResourceAttribute.to_value));
        ("Name", (Option.map x.name ~f:PropertyName.to_value));
        ("RequiresRecreation",
          (Option.map x.requiresRecreation ~f:RequiresRecreation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let requiresRecreation =
        (Option.map ~f:RequiresRecreation.of_xml)
          (Xml.child xml_arg0 "RequiresRecreation") in
      let name =
        (Option.map ~f:PropertyName.of_xml) (Xml.child xml_arg0 "Name") in
      let attribute =
        (Option.map ~f:ResourceAttribute.of_xml)
          (Xml.child xml_arg0 "Attribute") in
      make ?requiresRecreation ?name ?attribute ()
    let of_json json =
      let requiresRecreation =
        field_map json "RequiresRecreation" RequiresRecreation.of_json in
      let name = field_map json "Name" PropertyName.of_json in
      let attribute = field_map json "Attribute" ResourceAttribute.of_json in
      make ?requiresRecreation ?name ?attribute ()
    let to_json v = composed_to_json to_value v
  end
module AccountId =
  struct
    type nonrec t = string
    let context_ = "AccountId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"^[0-9]{12}$"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AccountId" j
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t = {
      key: TagKey.t ;
      value: TagValue.t }
    let context_ = "Tag"
    let make ~key = fun ~value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Some (TagKey.to_value x.key)));
        ("Value", (Some (TagValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        TagValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let key =
        TagKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Key") in
      make ~value ~key ()
    let of_json json =
      let value = field_map_exn json "Value" TagValue.of_json in
      let key = field_map_exn json "Key" TagKey.of_json in
      make ~value ~key ()
    let to_json v = composed_to_json to_value v
  end
module ApproximateCount =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ApproximateCount" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AttributeValue =
  struct
    type nonrec t = string
    let context_ = "AttributeValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttributeValue" j
    let to_json = simple_to_json to_value
  end
module HasDefaultPath =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module Id =
  struct
    type nonrec t = string
    let context_ = "Id"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9_\\-]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Id" j
    let to_json = simple_to_json to_value
  end
module ProductType =
  struct
    type nonrec t =
      | CLOUD_FORMATION_TEMPLATE 
      | MARKETPLACE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CLOUD_FORMATION_TEMPLATE -> "CLOUD_FORMATION_TEMPLATE"
      | MARKETPLACE -> "MARKETPLACE"
      | Non_static_id s -> s
    let of_string =
      function
      | "CLOUD_FORMATION_TEMPLATE" -> CLOUD_FORMATION_TEMPLATE
      | "MARKETPLACE" -> MARKETPLACE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ProductType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ProductType" j)
    let to_json = simple_to_json to_value
  end
module ProductViewDistributor =
  struct
    type nonrec t = string
    let context_ = "ProductViewDistributor"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProductViewDistributor" j
    let to_json = simple_to_json to_value
  end
module ProductViewName =
  struct
    type nonrec t = string
    let context_ = "ProductViewName"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:8191); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProductViewName" j
    let to_json = simple_to_json to_value
  end
module ProductViewOwner =
  struct
    type nonrec t = string
    let context_ = "ProductViewOwner"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:8191); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProductViewOwner" j
    let to_json = simple_to_json to_value
  end
module ProductViewShortDescription =
  struct
    type nonrec t = string
    let context_ = "ProductViewShortDescription"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:8191); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProductViewShortDescription" j
    let to_json = simple_to_json to_value
  end
module SupportDescription =
  struct
    type nonrec t = string
    let context_ = "SupportDescription"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:8191); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SupportDescription" j
    let to_json = simple_to_json to_value
  end
module SupportEmail =
  struct
    type nonrec t = string
    let context_ = "SupportEmail"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:254); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SupportEmail" j
    let to_json = simple_to_json to_value
  end
module SupportUrl =
  struct
    type nonrec t = string
    let context_ = "SupportUrl"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:2083); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SupportUrl" j
    let to_json = simple_to_json to_value
  end
module RecordError =
  struct
    type nonrec t =
      {
      code: ErrorCode.t option ;
      description: ErrorDescription.t option }
    let make ?code = fun ?description -> fun () -> { code; description }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:ErrorCode.to_value));
        ("Description",
          (Option.map x.description ~f:ErrorDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:ErrorDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let code = (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "Code") in
      make ?description ?code ()
    let of_json json =
      let description = field_map json "Description" ErrorDescription.of_json in
      let code = field_map json "Code" ErrorCode.of_json in
      make ?description ?code ()
    let to_json v = composed_to_json to_value v
  end
module RecordTag =
  struct
    type nonrec t =
      {
      key: RecordTagKey.t option ;
      value: RecordTagValue.t option }
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:RecordTagKey.to_value));
        ("Value", (Option.map x.value ~f:RecordTagValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:RecordTagValue.of_xml) (Xml.child xml_arg0 "Value") in
      let key =
        (Option.map ~f:RecordTagKey.of_xml) (Xml.child xml_arg0 "Key") in
      make ?value ?key ()
    let of_json json =
      let value = field_map json "Value" RecordTagValue.of_json in
      let key = field_map json "Key" RecordTagKey.of_json in
      make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifactCreatedTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ProvisioningArtifactDescription =
  struct
    type nonrec t = string
    let context_ = "ProvisioningArtifactDescription"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:8192); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProvisioningArtifactDescription" j
    let to_json = simple_to_json to_value
  end
module ProvisioningArtifactGuidance =
  struct
    type nonrec t =
      | DEFAULT 
      | DEPRECATED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DEFAULT -> "DEFAULT"
      | DEPRECATED -> "DEPRECATED"
      | Non_static_id s -> s
    let of_string =
      function
      | "DEFAULT" -> DEFAULT
      | "DEPRECATED" -> DEPRECATED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProvisioningArtifactGuidance"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ProvisioningArtifactGuidance" j)
    let to_json = simple_to_json to_value
  end
module ProvisioningArtifactName =
  struct
    type nonrec t = string
    let context_ = "ProvisioningArtifactName"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:8192); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProvisioningArtifactName" j
    let to_json = simple_to_json to_value
  end
module ConstraintSummary =
  struct
    type nonrec t =
      {
      type_: ConstraintType.t option ;
      description: ConstraintDescription.t option }
    let make ?type_ = fun ?description -> fun () -> { type_; description }
    let to_value x =
      structure_to_value
        [("Type", (Option.map x.type_ ~f:ConstraintType.to_value));
        ("Description",
          (Option.map x.description ~f:ConstraintDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:ConstraintDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let type_ =
        (Option.map ~f:ConstraintType.of_xml) (Xml.child xml_arg0 "Type") in
      make ?description ?type_ ()
    let of_json json =
      let description =
        field_map json "Description" ConstraintDescription.of_json in
      let type_ = field_map json "Type" ConstraintType.of_json in
      make ?description ?type_ ()
    let to_json v = composed_to_json to_value v
  end
module ExecutionParameterValue =
  struct
    type nonrec t = string
    let context_ = "ExecutionParameterValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExecutionParameterValue" j
    let to_json = simple_to_json to_value
  end
module AllowedValues =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"AllowedValues" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagOptionValue =
  struct
    type nonrec t = string
    let context_ = "TagOptionValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagOptionValue" j
    let to_json = simple_to_json to_value
  end
module ParameterKey =
  struct
    type nonrec t = string
    let context_ = "ParameterKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ParameterKey" j
    let to_json = simple_to_json to_value
  end
module ParameterValue =
  struct
    type nonrec t = string
    let context_ = "ParameterValue"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:4096); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ParameterValue" j
    let to_json = simple_to_json to_value
  end
module UsePreviousValue =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ResourceChangeDetail =
  struct
    type nonrec t =
      {
      target: ResourceTargetDefinition.t option ;
      evaluation: EvaluationType.t option ;
      causingEntity: CausingEntity.t option }
    let make ?target =
      fun ?evaluation ->
        fun ?causingEntity -> fun () -> { target; evaluation; causingEntity }
    let to_value x =
      structure_to_value
        [("Target",
           (Option.map x.target ~f:ResourceTargetDefinition.to_value));
        ("Evaluation", (Option.map x.evaluation ~f:EvaluationType.to_value));
        ("CausingEntity",
          (Option.map x.causingEntity ~f:CausingEntity.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let causingEntity =
        (Option.map ~f:CausingEntity.of_xml)
          (Xml.child xml_arg0 "CausingEntity") in
      let evaluation =
        (Option.map ~f:EvaluationType.of_xml)
          (Xml.child xml_arg0 "Evaluation") in
      let target =
        (Option.map ~f:ResourceTargetDefinition.of_xml)
          (Xml.child xml_arg0 "Target") in
      make ?causingEntity ?evaluation ?target ()
    let of_json json =
      let causingEntity =
        field_map json "CausingEntity" CausingEntity.of_json in
      let evaluation = field_map json "Evaluation" EvaluationType.of_json in
      let target = field_map json "Target" ResourceTargetDefinition.of_json in
      make ?causingEntity ?evaluation ?target ()
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifactInfoKey =
  struct
    type nonrec t = string
    let context_ = "ProvisioningArtifactInfoKey"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProvisioningArtifactInfoKey" j
    let to_json = simple_to_json to_value
  end
module ProvisioningArtifactInfoValue =
  struct
    type nonrec t = string
    let context_ = "ProvisioningArtifactInfoValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProvisioningArtifactInfoValue" j
    let to_json = simple_to_json to_value
  end
module Error =
  struct
    type nonrec t = string
    let context_ = "Error"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Error" j
    let to_json = simple_to_json to_value
  end
module Message =
  struct
    type nonrec t = string
    let context_ = "Message"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Message" j
    let to_json = simple_to_json to_value
  end
module Namespaces =
  struct
    type nonrec t = AccountId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AccountId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccountId.of_xml)
    let of_json j =
      list_of_json ~kind:"Namespaces" ~of_json:AccountId.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServiceActionDefinitionKey =
  struct
    type nonrec t =
      | Name 
      | Version 
      | AssumeRole 
      | Parameters 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Name -> "Name"
      | Version -> "Version"
      | AssumeRole -> "AssumeRole"
      | Parameters -> "Parameters"
      | Non_static_id s -> s
    let of_string =
      function
      | "Name" -> Name
      | "Version" -> Version
      | "AssumeRole" -> AssumeRole
      | "Parameters" -> Parameters
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ServiceActionDefinitionKey"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ServiceActionDefinitionKey" j)
    let to_json = simple_to_json to_value
  end
module ServiceActionDefinitionValue =
  struct
    type nonrec t = string
    let context_ = "ServiceActionDefinitionValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServiceActionDefinitionValue" j
    let to_json = simple_to_json to_value
  end
module ServiceActionDefinitionType =
  struct
    type nonrec t =
      | SSM_AUTOMATION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | SSM_AUTOMATION -> "SSM_AUTOMATION" | Non_static_id s -> s
    let of_string =
      function | "SSM_AUTOMATION" -> SSM_AUTOMATION | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ServiceActionDefinitionType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ServiceActionDefinitionType" j)
    let to_json = simple_to_json to_value
  end
module ServiceActionDescription =
  struct
    type nonrec t = string
    let context_ = "ServiceActionDescription"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1024); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServiceActionDescription" j
    let to_json = simple_to_json to_value
  end
module ServiceActionName =
  struct
    type nonrec t = string
    let context_ = "ServiceActionName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9_\\-.]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServiceActionName" j
    let to_json = simple_to_json to_value
  end
module Region =
  struct
    type nonrec t = string
    let context_ = "Region"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Region" j
    let to_json = simple_to_json to_value
  end
module CreatedTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module IdempotencyToken =
  struct
    type nonrec t = string
    let context_ = "IdempotencyToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9][a-zA-Z0-9_-]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IdempotencyToken" j
    let to_json = simple_to_json to_value
  end
module PhysicalId =
  struct
    type nonrec t = string
    let context_ = "PhysicalId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PhysicalId" j
    let to_json = simple_to_json to_value
  end
module ProvisionedProductNameOrArn =
  struct
    type nonrec t = string
    let context_ = "ProvisionedProductNameOrArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1224) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[a-zA-Z0-9][a-zA-Z0-9._-]{0,127}|arn:[a-z0-9-\\.]{1,63}:[a-z0-9-\\.]{0,63}:[a-z0-9-\\.]{0,63}:[a-z0-9-\\.]{0,63}:[^/].{0,1023}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProvisionedProductNameOrArn" j
    let to_json = simple_to_json to_value
  end
module ProvisionedProductStatus =
  struct
    type nonrec t =
      | AVAILABLE 
      | UNDER_CHANGE 
      | TAINTED 
      | ERROR 
      | PLAN_IN_PROGRESS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AVAILABLE -> "AVAILABLE"
      | UNDER_CHANGE -> "UNDER_CHANGE"
      | TAINTED -> "TAINTED"
      | ERROR -> "ERROR"
      | PLAN_IN_PROGRESS -> "PLAN_IN_PROGRESS"
      | Non_static_id s -> s
    let of_string =
      function
      | "AVAILABLE" -> AVAILABLE
      | "UNDER_CHANGE" -> UNDER_CHANGE
      | "TAINTED" -> TAINTED
      | "ERROR" -> ERROR
      | "PLAN_IN_PROGRESS" -> PLAN_IN_PROGRESS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProvisionedProductStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ProvisionedProductStatus" j)
    let to_json = simple_to_json to_value
  end
module ProvisionedProductStatusMessage =
  struct
    type nonrec t = string
    let context_ = "ProvisionedProductStatusMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProvisionedProductStatusMessage" j
    let to_json = simple_to_json to_value
  end
module ProvisionedProductType =
  struct
    type nonrec t = string
    let context_ = "ProvisionedProductType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProvisionedProductType" j
    let to_json = simple_to_json to_value
  end
module Tags =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:50); i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"Tags" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module UserArn =
  struct
    type nonrec t = string
    let context_ = "UserArn"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"UserArn" j
    let to_json = simple_to_json to_value
  end
module UserArnSession =
  struct
    type nonrec t = string
    let context_ = "UserArnSession"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"UserArnSession" j
    let to_json = simple_to_json to_value
  end
module ProvisionedProductViewFilterValue =
  struct
    type nonrec t = string
    let context_ = "ProvisionedProductViewFilterValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ProvisionedProductViewFilterValue" j
    let to_json = simple_to_json to_value
  end
module ProductViewAggregationValue =
  struct
    type nonrec t =
      {
      value: AttributeValue.t option ;
      approximateCount: ApproximateCount.t option }
    let make ?value =
      fun ?approximateCount -> fun () -> { value; approximateCount }
    let to_value x =
      structure_to_value
        [("Value", (Option.map x.value ~f:AttributeValue.to_value));
        ("ApproximateCount",
          (Option.map x.approximateCount ~f:ApproximateCount.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let approximateCount =
        (Option.map ~f:ApproximateCount.of_xml)
          (Xml.child xml_arg0 "ApproximateCount") in
      let value =
        (Option.map ~f:AttributeValue.of_xml) (Xml.child xml_arg0 "Value") in
      make ?approximateCount ?value ()
    let of_json json =
      let approximateCount =
        field_map json "ApproximateCount" ApproximateCount.of_json in
      let value = field_map json "Value" AttributeValue.of_json in
      make ?approximateCount ?value ()
    let to_json v = composed_to_json to_value v
  end
module ProductViewFilterValue =
  struct
    type nonrec t = string
    let context_ = "ProductViewFilterValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProductViewFilterValue" j
    let to_json = simple_to_json to_value
  end
module ProductViewSummary =
  struct
    type nonrec t =
      {
      id: Id.t option ;
      productId: Id.t option ;
      name: ProductViewName.t option ;
      owner: ProductViewOwner.t option ;
      shortDescription: ProductViewShortDescription.t option ;
      type_: ProductType.t option ;
      distributor: ProductViewDistributor.t option ;
      hasDefaultPath: HasDefaultPath.t option ;
      supportEmail: SupportEmail.t option ;
      supportDescription: SupportDescription.t option ;
      supportUrl: SupportUrl.t option }
    let make ?id =
      fun ?productId ->
        fun ?name ->
          fun ?owner ->
            fun ?shortDescription ->
              fun ?type_ ->
                fun ?distributor ->
                  fun ?hasDefaultPath ->
                    fun ?supportEmail ->
                      fun ?supportDescription ->
                        fun ?supportUrl ->
                          fun () ->
                            {
                              id;
                              productId;
                              name;
                              owner;
                              shortDescription;
                              type_;
                              distributor;
                              hasDefaultPath;
                              supportEmail;
                              supportDescription;
                              supportUrl
                            }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:Id.to_value));
        ("ProductId", (Option.map x.productId ~f:Id.to_value));
        ("Name", (Option.map x.name ~f:ProductViewName.to_value));
        ("Owner", (Option.map x.owner ~f:ProductViewOwner.to_value));
        ("ShortDescription",
          (Option.map x.shortDescription
             ~f:ProductViewShortDescription.to_value));
        ("Type", (Option.map x.type_ ~f:ProductType.to_value));
        ("Distributor",
          (Option.map x.distributor ~f:ProductViewDistributor.to_value));
        ("HasDefaultPath",
          (Option.map x.hasDefaultPath ~f:HasDefaultPath.to_value));
        ("SupportEmail",
          (Option.map x.supportEmail ~f:SupportEmail.to_value));
        ("SupportDescription",
          (Option.map x.supportDescription ~f:SupportDescription.to_value));
        ("SupportUrl", (Option.map x.supportUrl ~f:SupportUrl.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let supportUrl =
        (Option.map ~f:SupportUrl.of_xml) (Xml.child xml_arg0 "SupportUrl") in
      let supportDescription =
        (Option.map ~f:SupportDescription.of_xml)
          (Xml.child xml_arg0 "SupportDescription") in
      let supportEmail =
        (Option.map ~f:SupportEmail.of_xml)
          (Xml.child xml_arg0 "SupportEmail") in
      let hasDefaultPath =
        (Option.map ~f:HasDefaultPath.of_xml)
          (Xml.child xml_arg0 "HasDefaultPath") in
      let distributor =
        (Option.map ~f:ProductViewDistributor.of_xml)
          (Xml.child xml_arg0 "Distributor") in
      let type_ =
        (Option.map ~f:ProductType.of_xml) (Xml.child xml_arg0 "Type") in
      let shortDescription =
        (Option.map ~f:ProductViewShortDescription.of_xml)
          (Xml.child xml_arg0 "ShortDescription") in
      let owner =
        (Option.map ~f:ProductViewOwner.of_xml) (Xml.child xml_arg0 "Owner") in
      let name =
        (Option.map ~f:ProductViewName.of_xml) (Xml.child xml_arg0 "Name") in
      let productId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProductId") in
      let id = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "Id") in
      make ?supportUrl ?supportDescription ?supportEmail ?hasDefaultPath
        ?distributor ?type_ ?shortDescription ?owner ?name ?productId ?id ()
    let of_json json =
      let supportUrl = field_map json "SupportUrl" SupportUrl.of_json in
      let supportDescription =
        field_map json "SupportDescription" SupportDescription.of_json in
      let supportEmail = field_map json "SupportEmail" SupportEmail.of_json in
      let hasDefaultPath =
        field_map json "HasDefaultPath" HasDefaultPath.of_json in
      let distributor =
        field_map json "Distributor" ProductViewDistributor.of_json in
      let type_ = field_map json "Type" ProductType.of_json in
      let shortDescription =
        field_map json "ShortDescription" ProductViewShortDescription.of_json in
      let owner = field_map json "Owner" ProductViewOwner.of_json in
      let name = field_map json "Name" ProductViewName.of_json in
      let productId = field_map json "ProductId" Id.of_json in
      let id = field_map json "Id" Id.of_json in
      make ?supportUrl ?supportDescription ?supportEmail ?hasDefaultPath
        ?distributor ?type_ ?shortDescription ?owner ?name ?productId ?id ()
    let to_json v = composed_to_json to_value v
  end
module ResourceARN =
  struct
    type nonrec t = string
    let context_ = "ResourceARN"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:150) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceARN" j
    let to_json = simple_to_json to_value
  end
module Status =
  struct
    type nonrec t =
      | AVAILABLE 
      | CREATING 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AVAILABLE -> "AVAILABLE"
      | CREATING -> "CREATING"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "AVAILABLE" -> AVAILABLE
      | "CREATING" -> CREATING
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Status" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Status" j)
    let to_json = simple_to_json to_value
  end
module LastRequestId =
  struct
    type nonrec t = string
    let context_ = "LastRequestId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LastRequestId" j
    let to_json = simple_to_json to_value
  end
module ProvisionedProductId =
  struct
    type nonrec t = string
    let context_ = "ProvisionedProductId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProvisionedProductId" j
    let to_json = simple_to_json to_value
  end
module RoleArn =
  struct
    type nonrec t = string
    let context_ = "RoleArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1224) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:[a-z0-9-\\.]{1,63}:iam::[a-z0-9-\\.]{0,63}:role\\/.{0,1023}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RoleArn" j
    let to_json = simple_to_json to_value
  end
module Owner =
  struct
    type nonrec t = string
    let context_ = "Owner"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Owner" j
    let to_json = simple_to_json to_value
  end
module TagOptionActive =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module TagOptionId =
  struct
    type nonrec t = string
    let context_ = "TagOptionId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagOptionId" j
    let to_json = simple_to_json to_value
  end
module TagOptionKey =
  struct
    type nonrec t = string
    let context_ = "TagOptionKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagOptionKey" j
    let to_json = simple_to_json to_value
  end
module StackInstanceStatus =
  struct
    type nonrec t =
      | CURRENT 
      | OUTDATED 
      | INOPERABLE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CURRENT -> "CURRENT"
      | OUTDATED -> "OUTDATED"
      | INOPERABLE -> "INOPERABLE"
      | Non_static_id s -> s
    let of_string =
      function
      | "CURRENT" -> CURRENT
      | "OUTDATED" -> OUTDATED
      | "INOPERABLE" -> INOPERABLE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration StackInstanceStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StackInstanceStatus" j)
    let to_json = simple_to_json to_value
  end
module ResourceDetailARN =
  struct
    type nonrec t = string
    let context_ = "ResourceDetailARN"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceDetailARN" j
    let to_json = simple_to_json to_value
  end
module ResourceDetailCreatedTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ResourceDetailDescription =
  struct
    type nonrec t = string
    let context_ = "ResourceDetailDescription"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceDetailDescription" j
    let to_json = simple_to_json to_value
  end
module ResourceDetailId =
  struct
    type nonrec t = string
    let context_ = "ResourceDetailId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceDetailId" j
    let to_json = simple_to_json to_value
  end
module ResourceDetailName =
  struct
    type nonrec t = string
    let context_ = "ResourceDetailName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceDetailName" j
    let to_json = simple_to_json to_value
  end
module ProvisionedProductName =
  struct
    type nonrec t = string
    let context_ = "ProvisionedProductName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z0-9][a-zA-Z0-9._-]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProvisionedProductName" j
    let to_json = simple_to_json to_value
  end
module RecordErrors =
  struct
    type nonrec t = RecordError.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:RecordError.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RecordError.of_xml)
    let of_json j =
      list_of_json ~kind:"RecordErrors" ~of_json:RecordError.of_json j
    let to_json v = composed_to_json to_value v
  end
module RecordStatus =
  struct
    type nonrec t =
      | CREATED 
      | IN_PROGRESS 
      | IN_PROGRESS_IN_ERROR 
      | SUCCEEDED 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATED -> "CREATED"
      | IN_PROGRESS -> "IN_PROGRESS"
      | IN_PROGRESS_IN_ERROR -> "IN_PROGRESS_IN_ERROR"
      | SUCCEEDED -> "SUCCEEDED"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATED" -> CREATED
      | "IN_PROGRESS" -> IN_PROGRESS
      | "IN_PROGRESS_IN_ERROR" -> IN_PROGRESS_IN_ERROR
      | "SUCCEEDED" -> SUCCEEDED
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration RecordStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RecordStatus" j)
    let to_json = simple_to_json to_value
  end
module RecordTags =
  struct
    type nonrec t = RecordTag.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:50); i
    let to_value xs =
      (xs |> (List.map ~f:RecordTag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RecordTag.of_xml)
    let of_json j =
      list_of_json ~kind:"RecordTags" ~of_json:RecordTag.of_json j
    let to_json v = composed_to_json to_value v
  end
module RecordType =
  struct
    type nonrec t = string
    let context_ = "RecordType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RecordType" j
    let to_json = simple_to_json to_value
  end
module UpdatedTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module CreationTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ProvisioningArtifactActive =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ProvisioningArtifactType =
  struct
    type nonrec t =
      | CLOUD_FORMATION_TEMPLATE 
      | MARKETPLACE_AMI 
      | MARKETPLACE_CAR 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CLOUD_FORMATION_TEMPLATE -> "CLOUD_FORMATION_TEMPLATE"
      | MARKETPLACE_AMI -> "MARKETPLACE_AMI"
      | MARKETPLACE_CAR -> "MARKETPLACE_CAR"
      | Non_static_id s -> s
    let of_string =
      function
      | "CLOUD_FORMATION_TEMPLATE" -> CLOUD_FORMATION_TEMPLATE
      | "MARKETPLACE_AMI" -> MARKETPLACE_AMI
      | "MARKETPLACE_CAR" -> MARKETPLACE_CAR
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProvisioningArtifactType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ProvisioningArtifactType" j)
    let to_json = simple_to_json to_value
  end
module ProvisioningArtifact =
  struct
    type nonrec t =
      {
      id: Id.t option ;
      name: ProvisioningArtifactName.t option ;
      description: ProvisioningArtifactDescription.t option ;
      createdTime: ProvisioningArtifactCreatedTime.t option ;
      guidance: ProvisioningArtifactGuidance.t option }
    let make ?id =
      fun ?name ->
        fun ?description ->
          fun ?createdTime ->
            fun ?guidance ->
              fun () -> { id; name; description; createdTime; guidance }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:Id.to_value));
        ("Name", (Option.map x.name ~f:ProvisioningArtifactName.to_value));
        ("Description",
          (Option.map x.description
             ~f:ProvisioningArtifactDescription.to_value));
        ("CreatedTime",
          (Option.map x.createdTime
             ~f:ProvisioningArtifactCreatedTime.to_value));
        ("Guidance",
          (Option.map x.guidance ~f:ProvisioningArtifactGuidance.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let guidance =
        (Option.map ~f:ProvisioningArtifactGuidance.of_xml)
          (Xml.child xml_arg0 "Guidance") in
      let createdTime =
        (Option.map ~f:ProvisioningArtifactCreatedTime.of_xml)
          (Xml.child xml_arg0 "CreatedTime") in
      let description =
        (Option.map ~f:ProvisioningArtifactDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        (Option.map ~f:ProvisioningArtifactName.of_xml)
          (Xml.child xml_arg0 "Name") in
      let id = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "Id") in
      make ?guidance ?createdTime ?description ?name ?id ()
    let of_json json =
      let guidance =
        field_map json "Guidance" ProvisioningArtifactGuidance.of_json in
      let createdTime =
        field_map json "CreatedTime" ProvisioningArtifactCreatedTime.of_json in
      let description =
        field_map json "Description" ProvisioningArtifactDescription.of_json in
      let name = field_map json "Name" ProvisioningArtifactName.of_json in
      let id = field_map json "Id" Id.of_json in
      make ?guidance ?createdTime ?description ?name ?id ()
    let to_json v = composed_to_json to_value v
  end
module ProvisionedProductPlanName =
  struct
    type nonrec t = string
    let context_ = "ProvisionedProductPlanName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProvisionedProductPlanName" j
    let to_json = simple_to_json to_value
  end
module ProvisionedProductPlanType =
  struct
    type nonrec t =
      | CLOUDFORMATION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | CLOUDFORMATION -> "CLOUDFORMATION" | Non_static_id s -> s
    let of_string =
      function | "CLOUDFORMATION" -> CLOUDFORMATION | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProvisionedProductPlanType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ProvisionedProductPlanType" j)
    let to_json = simple_to_json to_value
  end
module PrincipalARN =
  struct
    type nonrec t = string
    let context_ = "PrincipalARN"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PrincipalARN" j
    let to_json = simple_to_json to_value
  end
module PrincipalType =
  struct
    type nonrec t =
      | IAM 
      | Non_static_id of string 
    let make i = i
    let to_string = function | IAM -> "IAM" | Non_static_id s -> s
    let of_string = function | "IAM" -> IAM | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PrincipalType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PrincipalType" j)
    let to_json = simple_to_json to_value
  end
module PortfolioDescription =
  struct
    type nonrec t = string
    let context_ = "PortfolioDescription"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:2000); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PortfolioDescription" j
    let to_json = simple_to_json to_value
  end
module PortfolioDisplayName =
  struct
    type nonrec t = string
    let context_ = "PortfolioDisplayName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PortfolioDisplayName" j
    let to_json = simple_to_json to_value
  end
module ProviderName =
  struct
    type nonrec t = string
    let context_ = "ProviderName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:50) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProviderName" j
    let to_json = simple_to_json to_value
  end
module OrganizationNodeType =
  struct
    type nonrec t =
      | ORGANIZATION 
      | ORGANIZATIONAL_UNIT 
      | ACCOUNT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ORGANIZATION -> "ORGANIZATION"
      | ORGANIZATIONAL_UNIT -> "ORGANIZATIONAL_UNIT"
      | ACCOUNT -> "ACCOUNT"
      | Non_static_id s -> s
    let of_string =
      function
      | "ORGANIZATION" -> ORGANIZATION
      | "ORGANIZATIONAL_UNIT" -> ORGANIZATIONAL_UNIT
      | "ACCOUNT" -> ACCOUNT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration OrganizationNodeType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"OrganizationNodeType" j)
    let to_json = simple_to_json to_value
  end
module OrganizationNodeValue =
  struct
    type nonrec t = string
    let context_ = "OrganizationNodeValue"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"(^[0-9]{12}$)|(^arn:aws:organizations::\\d{12}:organization\\/o-[a-z0-9]{10,32})|(^o-[a-z0-9]{10,32}$)|(^arn:aws:organizations::\\d{12}:ou\\/o-[a-z0-9]{10,32}\\/ou-[0-9a-z]{4,32}-[0-9a-z]{8,32}$)|(^ou-[0-9a-z]{4,32}-[a-z0-9]{8,32}$)");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"OrganizationNodeValue" j
    let to_json = simple_to_json to_value
  end
module ConstraintSummaries =
  struct
    type nonrec t = ConstraintSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ConstraintSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ConstraintSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ConstraintSummaries"
        ~of_json:ConstraintSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module PortfolioName =
  struct
    type nonrec t = string
    let context_ = "PortfolioName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PortfolioName" j
    let to_json = simple_to_json to_value
  end
module BudgetName =
  struct
    type nonrec t = string
    let context_ = "BudgetName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BudgetName" j
    let to_json = simple_to_json to_value
  end
module Description =
  struct
    type nonrec t = string
    let context_ = "Description"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Description" j
    let to_json = simple_to_json to_value
  end
module OutputKey =
  struct
    type nonrec t = string
    let context_ = "OutputKey"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"OutputKey" j
    let to_json = simple_to_json to_value
  end
module OutputValue =
  struct
    type nonrec t = string
    let context_ = "OutputValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"OutputValue" j
    let to_json = simple_to_json to_value
  end
module ExecutionParameterKey =
  struct
    type nonrec t = string
    let context_ = "ExecutionParameterKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:50) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExecutionParameterKey" j
    let to_json = simple_to_json to_value
  end
module ExecutionParameterType =
  struct
    type nonrec t = string
    let context_ = "ExecutionParameterType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExecutionParameterType" j
    let to_json = simple_to_json to_value
  end
module ExecutionParameterValueList =
  struct
    type nonrec t = ExecutionParameterValue.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:25) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:ExecutionParameterValue.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExecutionParameterValue.of_xml)
    let of_json j =
      list_of_json ~kind:"ExecutionParameterValueList"
        ~of_json:ExecutionParameterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module OutputDescription =
  struct
    type nonrec t = string
    let context_ = "OutputDescription"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1024); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"OutputDescription" j
    let to_json = simple_to_json to_value
  end
module ProvisioningArtifactOutputKey =
  struct
    type nonrec t = string
    let context_ = "ProvisioningArtifactOutputKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProvisioningArtifactOutputKey" j
    let to_json = simple_to_json to_value
  end
module DefaultValue =
  struct
    type nonrec t = string
    let context_ = "DefaultValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DefaultValue" j
    let to_json = simple_to_json to_value
  end
module NoEcho =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ParameterConstraints =
  struct
    type nonrec t =
      {
      allowedValues: AllowedValues.t option ;
      allowedPattern: String_.t option ;
      constraintDescription: String_.t option ;
      maxLength: String_.t option ;
      minLength: String_.t option ;
      maxValue: String_.t option ;
      minValue: String_.t option }
    let make ?allowedValues =
      fun ?allowedPattern ->
        fun ?constraintDescription ->
          fun ?maxLength ->
            fun ?minLength ->
              fun ?maxValue ->
                fun ?minValue ->
                  fun () ->
                    {
                      allowedValues;
                      allowedPattern;
                      constraintDescription;
                      maxLength;
                      minLength;
                      maxValue;
                      minValue
                    }
    let to_value x =
      structure_to_value
        [("AllowedValues",
           (Option.map x.allowedValues ~f:AllowedValues.to_value));
        ("AllowedPattern", (Option.map x.allowedPattern ~f:String_.to_value));
        ("ConstraintDescription",
          (Option.map x.constraintDescription ~f:String_.to_value));
        ("MaxLength", (Option.map x.maxLength ~f:String_.to_value));
        ("MinLength", (Option.map x.minLength ~f:String_.to_value));
        ("MaxValue", (Option.map x.maxValue ~f:String_.to_value));
        ("MinValue", (Option.map x.minValue ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let minValue =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "MinValue") in
      let maxValue =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "MaxValue") in
      let minLength =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "MinLength") in
      let maxLength =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "MaxLength") in
      let constraintDescription =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ConstraintDescription") in
      let allowedPattern =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AllowedPattern") in
      let allowedValues =
        (Option.map ~f:AllowedValues.of_xml)
          (Xml.child xml_arg0 "AllowedValues") in
      make ?minValue ?maxValue ?minLength ?maxLength ?constraintDescription
        ?allowedPattern ?allowedValues ()
    let of_json json =
      let minValue = field_map json "MinValue" String_.of_json in
      let maxValue = field_map json "MaxValue" String_.of_json in
      let minLength = field_map json "MinLength" String_.of_json in
      let maxLength = field_map json "MaxLength" String_.of_json in
      let constraintDescription =
        field_map json "ConstraintDescription" String_.of_json in
      let allowedPattern = field_map json "AllowedPattern" String_.of_json in
      let allowedValues =
        field_map json "AllowedValues" AllowedValues.of_json in
      make ?minValue ?maxValue ?minLength ?maxLength ?constraintDescription
        ?allowedPattern ?allowedValues ()
    let to_json v = composed_to_json to_value v
  end
module ParameterType =
  struct
    type nonrec t = string
    let context_ = "ParameterType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ParameterType" j
    let to_json = simple_to_json to_value
  end
module TagOptionValues =
  struct
    type nonrec t = TagOptionValue.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TagOptionValue.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagOptionValue.of_xml)
    let of_json j =
      list_of_json ~kind:"TagOptionValues" ~of_json:TagOptionValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstructionType =
  struct
    type nonrec t = string
    let context_ = "InstructionType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InstructionType" j
    let to_json = simple_to_json to_value
  end
module InstructionValue =
  struct
    type nonrec t = string
    let context_ = "InstructionValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InstructionValue" j
    let to_json = simple_to_json to_value
  end
module NotificationArn =
  struct
    type nonrec t = string
    let context_ = "NotificationArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1224) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:[a-z0-9-\\.]{1,63}:[a-z0-9-\\.]{0,63}:[a-z0-9-\\.]{0,63}:[a-z0-9-\\.]{0,63}:[^/].{0,1023}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NotificationArn" j
    let to_json = simple_to_json to_value
  end
module UpdateProvisioningParameter =
  struct
    type nonrec t =
      {
      key: ParameterKey.t option ;
      value: ParameterValue.t option ;
      usePreviousValue: UsePreviousValue.t option }
    let make ?key =
      fun ?value ->
        fun ?usePreviousValue -> fun () -> { key; value; usePreviousValue }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:ParameterKey.to_value));
        ("Value", (Option.map x.value ~f:ParameterValue.to_value));
        ("UsePreviousValue",
          (Option.map x.usePreviousValue ~f:UsePreviousValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let usePreviousValue =
        (Option.map ~f:UsePreviousValue.of_xml)
          (Xml.child xml_arg0 "UsePreviousValue") in
      let value =
        (Option.map ~f:ParameterValue.of_xml) (Xml.child xml_arg0 "Value") in
      let key =
        (Option.map ~f:ParameterKey.of_xml) (Xml.child xml_arg0 "Key") in
      make ?usePreviousValue ?value ?key ()
    let of_json json =
      let usePreviousValue =
        field_map json "UsePreviousValue" UsePreviousValue.of_json in
      let value = field_map json "Value" ParameterValue.of_json in
      let key = field_map json "Key" ParameterKey.of_json in
      make ?usePreviousValue ?value ?key ()
    let to_json v = composed_to_json to_value v
  end
module ChangeAction =
  struct
    type nonrec t =
      | ADD 
      | MODIFY 
      | REMOVE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ADD -> "ADD"
      | MODIFY -> "MODIFY"
      | REMOVE -> "REMOVE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ADD" -> ADD
      | "MODIFY" -> MODIFY
      | "REMOVE" -> REMOVE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ChangeAction" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ChangeAction" j)
    let to_json = simple_to_json to_value
  end
module LogicalResourceId =
  struct
    type nonrec t = string
    let context_ = "LogicalResourceId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LogicalResourceId" j
    let to_json = simple_to_json to_value
  end
module PhysicalResourceId =
  struct
    type nonrec t = string
    let context_ = "PhysicalResourceId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PhysicalResourceId" j
    let to_json = simple_to_json to_value
  end
module PlanResourceType =
  struct
    type nonrec t = string
    let context_ = "PlanResourceType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PlanResourceType" j
    let to_json = simple_to_json to_value
  end
module Replacement =
  struct
    type nonrec t =
      | TRUE 
      | FALSE 
      | CONDITIONAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TRUE -> "TRUE"
      | FALSE -> "FALSE"
      | CONDITIONAL -> "CONDITIONAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "TRUE" -> TRUE
      | "FALSE" -> FALSE
      | "CONDITIONAL" -> CONDITIONAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Replacement" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Replacement" j)
    let to_json = simple_to_json to_value
  end
module ResourceChangeDetails =
  struct
    type nonrec t = ResourceChangeDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceChangeDetail.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceChangeDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceChangeDetails"
        ~of_json:ResourceChangeDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module Scope =
  struct
    type nonrec t = ResourceAttribute.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceAttribute.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceAttribute.of_xml)
    let of_json j =
      list_of_json ~kind:"Scope" ~of_json:ResourceAttribute.of_json j
    let to_json v = composed_to_json to_value v
  end
module CloudWatchDashboardName =
  struct
    type nonrec t = string
    let context_ = "CloudWatchDashboardName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CloudWatchDashboardName" j
    let to_json = simple_to_json to_value
  end
module ProvisioningArtifactInfo =
  struct
    type nonrec t =
      (ProvisioningArtifactInfoKey.t * ProvisioningArtifactInfoValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ProvisioningArtifactInfoKey.of_string chopped),
                              (ProvisioningArtifactInfoValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ProvisioningArtifactInfoKey.to_value x) |>
                    (fun x ->
                       (ProvisioningArtifactInfoValue.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ProvisioningArtifactInfoKey.of_string
        ~of_json:ProvisioningArtifactInfoValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module DescribePortfolioShareType =
  struct
    type nonrec t =
      | ACCOUNT 
      | ORGANIZATION 
      | ORGANIZATIONAL_UNIT 
      | ORGANIZATION_MEMBER_ACCOUNT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACCOUNT -> "ACCOUNT"
      | ORGANIZATION -> "ORGANIZATION"
      | ORGANIZATIONAL_UNIT -> "ORGANIZATIONAL_UNIT"
      | ORGANIZATION_MEMBER_ACCOUNT -> "ORGANIZATION_MEMBER_ACCOUNT"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACCOUNT" -> ACCOUNT
      | "ORGANIZATION" -> ORGANIZATION
      | "ORGANIZATIONAL_UNIT" -> ORGANIZATIONAL_UNIT
      | "ORGANIZATION_MEMBER_ACCOUNT" -> ORGANIZATION_MEMBER_ACCOUNT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DescribePortfolioShareType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"DescribePortfolioShareType" j)
    let to_json = simple_to_json to_value
  end
module ShareError =
  struct
    type nonrec t =
      {
      accounts: Namespaces.t option ;
      message: Message.t option ;
      error: Error.t option }
    let make ?accounts =
      fun ?message -> fun ?error -> fun () -> { accounts; message; error }
    let to_value x =
      structure_to_value
        [("Accounts", (Option.map x.accounts ~f:Namespaces.to_value));
        ("Message", (Option.map x.message ~f:Message.to_value));
        ("Error", (Option.map x.error ~f:Error.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let error = (Option.map ~f:Error.of_xml) (Xml.child xml_arg0 "Error") in
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "Message") in
      let accounts =
        (Option.map ~f:Namespaces.of_xml) (Xml.child xml_arg0 "Accounts") in
      make ?error ?message ?accounts ()
    let of_json json =
      let error = field_map json "Error" Error.of_json in
      let message = field_map json "Message" Message.of_json in
      let accounts = field_map json "Accounts" Namespaces.of_json in
      make ?error ?message ?accounts ()
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifactPropertyName =
  struct
    type nonrec t =
      | Id 
      | Non_static_id of string 
    let make i = i
    let to_string = function | Id -> "Id" | Non_static_id s -> s
    let of_string = function | "Id" -> Id | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProvisioningArtifactPropertyName"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ProvisioningArtifactPropertyName" j)
    let to_json = simple_to_json to_value
  end
module ProvisioningArtifactPropertyValue =
  struct
    type nonrec t = string
    let context_ = "ProvisioningArtifactPropertyValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ProvisioningArtifactPropertyValue" j
    let to_json = simple_to_json to_value
  end
module ServiceActionAssociationErrorCode =
  struct
    type nonrec t =
      | DUPLICATE_RESOURCE 
      | INTERNAL_FAILURE 
      | LIMIT_EXCEEDED 
      | RESOURCE_NOT_FOUND 
      | THROTTLING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DUPLICATE_RESOURCE -> "DUPLICATE_RESOURCE"
      | INTERNAL_FAILURE -> "INTERNAL_FAILURE"
      | LIMIT_EXCEEDED -> "LIMIT_EXCEEDED"
      | RESOURCE_NOT_FOUND -> "RESOURCE_NOT_FOUND"
      | THROTTLING -> "THROTTLING"
      | Non_static_id s -> s
    let of_string =
      function
      | "DUPLICATE_RESOURCE" -> DUPLICATE_RESOURCE
      | "INTERNAL_FAILURE" -> INTERNAL_FAILURE
      | "LIMIT_EXCEEDED" -> LIMIT_EXCEEDED
      | "RESOURCE_NOT_FOUND" -> RESOURCE_NOT_FOUND
      | "THROTTLING" -> THROTTLING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ServiceActionAssociationErrorCode"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ServiceActionAssociationErrorCode" j)
    let to_json = simple_to_json to_value
  end
module ServiceActionAssociationErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ServiceActionAssociationErrorMessage"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ServiceActionAssociationErrorMessage" j
    let to_json = simple_to_json to_value
  end
module ServiceActionDefinitionMap =
  struct
    type nonrec t =
      (ServiceActionDefinitionKey.t * ServiceActionDefinitionValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ServiceActionDefinitionKey.of_string chopped),
                              (ServiceActionDefinitionValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ServiceActionDefinitionKey.to_value x) |>
                    (fun x ->
                       (ServiceActionDefinitionValue.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ServiceActionDefinitionKey.of_string
        ~of_json:ServiceActionDefinitionValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServiceActionSummary =
  struct
    type nonrec t =
      {
      id: Id.t option ;
      name: ServiceActionName.t option ;
      description: ServiceActionDescription.t option ;
      definitionType: ServiceActionDefinitionType.t option }
    let make ?id =
      fun ?name ->
        fun ?description ->
          fun ?definitionType ->
            fun () -> { id; name; description; definitionType }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:Id.to_value));
        ("Name", (Option.map x.name ~f:ServiceActionName.to_value));
        ("Description",
          (Option.map x.description ~f:ServiceActionDescription.to_value));
        ("DefinitionType",
          (Option.map x.definitionType
             ~f:ServiceActionDefinitionType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let definitionType =
        (Option.map ~f:ServiceActionDefinitionType.of_xml)
          (Xml.child xml_arg0 "DefinitionType") in
      let description =
        (Option.map ~f:ServiceActionDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        (Option.map ~f:ServiceActionName.of_xml) (Xml.child xml_arg0 "Name") in
      let id = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "Id") in
      make ?definitionType ?description ?name ?id ()
    let of_json json =
      let definitionType =
        field_map json "DefinitionType" ServiceActionDefinitionType.of_json in
      let description =
        field_map json "Description" ServiceActionDescription.of_json in
      let name = field_map json "Name" ServiceActionName.of_json in
      let id = field_map json "Id" Id.of_json in
      make ?definitionType ?description ?name ?id ()
    let to_json v = composed_to_json to_value v
  end
module PropertyKey =
  struct
    type nonrec t =
      | OWNER 
      | LAUNCH_ROLE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | OWNER -> "OWNER"
      | LAUNCH_ROLE -> "LAUNCH_ROLE"
      | Non_static_id s -> s
    let of_string =
      function
      | "OWNER" -> OWNER
      | "LAUNCH_ROLE" -> LAUNCH_ROLE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PropertyKey" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PropertyKey" j)
    let to_json = simple_to_json to_value
  end
module PropertyValue =
  struct
    type nonrec t = string
    let context_ = "PropertyValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PropertyValue" j
    let to_json = simple_to_json to_value
  end
module StackSetAccounts =
  struct
    type nonrec t = AccountId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AccountId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccountId.of_xml)
    let of_json j =
      list_of_json ~kind:"StackSetAccounts" ~of_json:AccountId.of_json j
    let to_json v = composed_to_json to_value v
  end
module StackSetFailureToleranceCount =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for StackSetFailureToleranceCount"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module StackSetFailureTolerancePercentage =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for StackSetFailureTolerancePercentage" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module StackSetMaxConcurrencyCount =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for StackSetMaxConcurrencyCount"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module StackSetMaxConcurrencyPercentage =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for StackSetMaxConcurrencyPercentage" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module StackSetOperationType =
  struct
    type nonrec t =
      | CREATE 
      | UPDATE 
      | DELETE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATE -> "CREATE"
      | UPDATE -> "UPDATE"
      | DELETE -> "DELETE"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATE" -> CREATE
      | "UPDATE" -> UPDATE
      | "DELETE" -> DELETE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration StackSetOperationType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"StackSetOperationType" j)
    let to_json = simple_to_json to_value
  end
module StackSetRegions =
  struct
    type nonrec t = Region.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Region.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Region.of_xml)
    let of_json j =
      list_of_json ~kind:"StackSetRegions" ~of_json:Region.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProvisionedProductAttribute =
  struct
    type nonrec t =
      {
      name: ProvisionedProductNameOrArn.t option ;
      arn: ProvisionedProductNameOrArn.t option ;
      type_: ProvisionedProductType.t option ;
      id: Id.t option ;
      status: ProvisionedProductStatus.t option ;
      statusMessage: ProvisionedProductStatusMessage.t option ;
      createdTime: CreatedTime.t option ;
      idempotencyToken: IdempotencyToken.t option ;
      lastRecordId: Id.t option ;
      lastProvisioningRecordId: Id.t option ;
      lastSuccessfulProvisioningRecordId: Id.t option ;
      tags: Tags.t option ;
      physicalId: PhysicalId.t option ;
      productId: Id.t option ;
      productName: ProductViewName.t option ;
      provisioningArtifactId: Id.t option ;
      provisioningArtifactName: ProvisioningArtifactName.t option ;
      userArn: UserArn.t option ;
      userArnSession: UserArnSession.t option }
    let make ?name =
      fun ?arn ->
        fun ?type_ ->
          fun ?id ->
            fun ?status ->
              fun ?statusMessage ->
                fun ?createdTime ->
                  fun ?idempotencyToken ->
                    fun ?lastRecordId ->
                      fun ?lastProvisioningRecordId ->
                        fun ?lastSuccessfulProvisioningRecordId ->
                          fun ?tags ->
                            fun ?physicalId ->
                              fun ?productId ->
                                fun ?productName ->
                                  fun ?provisioningArtifactId ->
                                    fun ?provisioningArtifactName ->
                                      fun ?userArn ->
                                        fun ?userArnSession ->
                                          fun () ->
                                            {
                                              name;
                                              arn;
                                              type_;
                                              id;
                                              status;
                                              statusMessage;
                                              createdTime;
                                              idempotencyToken;
                                              lastRecordId;
                                              lastProvisioningRecordId;
                                              lastSuccessfulProvisioningRecordId;
                                              tags;
                                              physicalId;
                                              productId;
                                              productName;
                                              provisioningArtifactId;
                                              provisioningArtifactName;
                                              userArn;
                                              userArnSession
                                            }
    let to_value x =
      structure_to_value
        [("Name",
           (Option.map x.name ~f:ProvisionedProductNameOrArn.to_value));
        ("Arn", (Option.map x.arn ~f:ProvisionedProductNameOrArn.to_value));
        ("Type", (Option.map x.type_ ~f:ProvisionedProductType.to_value));
        ("Id", (Option.map x.id ~f:Id.to_value));
        ("Status",
          (Option.map x.status ~f:ProvisionedProductStatus.to_value));
        ("StatusMessage",
          (Option.map x.statusMessage
             ~f:ProvisionedProductStatusMessage.to_value));
        ("CreatedTime", (Option.map x.createdTime ~f:CreatedTime.to_value));
        ("IdempotencyToken",
          (Option.map x.idempotencyToken ~f:IdempotencyToken.to_value));
        ("LastRecordId", (Option.map x.lastRecordId ~f:Id.to_value));
        ("LastProvisioningRecordId",
          (Option.map x.lastProvisioningRecordId ~f:Id.to_value));
        ("LastSuccessfulProvisioningRecordId",
          (Option.map x.lastSuccessfulProvisioningRecordId ~f:Id.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("PhysicalId", (Option.map x.physicalId ~f:PhysicalId.to_value));
        ("ProductId", (Option.map x.productId ~f:Id.to_value));
        ("ProductName",
          (Option.map x.productName ~f:ProductViewName.to_value));
        ("ProvisioningArtifactId",
          (Option.map x.provisioningArtifactId ~f:Id.to_value));
        ("ProvisioningArtifactName",
          (Option.map x.provisioningArtifactName
             ~f:ProvisioningArtifactName.to_value));
        ("UserArn", (Option.map x.userArn ~f:UserArn.to_value));
        ("UserArnSession",
          (Option.map x.userArnSession ~f:UserArnSession.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userArnSession =
        (Option.map ~f:UserArnSession.of_xml)
          (Xml.child xml_arg0 "UserArnSession") in
      let userArn =
        (Option.map ~f:UserArn.of_xml) (Xml.child xml_arg0 "UserArn") in
      let provisioningArtifactName =
        (Option.map ~f:ProvisioningArtifactName.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactName") in
      let provisioningArtifactId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactId") in
      let productName =
        (Option.map ~f:ProductViewName.of_xml)
          (Xml.child xml_arg0 "ProductName") in
      let productId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProductId") in
      let physicalId =
        (Option.map ~f:PhysicalId.of_xml) (Xml.child xml_arg0 "PhysicalId") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let lastSuccessfulProvisioningRecordId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "LastSuccessfulProvisioningRecordId") in
      let lastProvisioningRecordId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "LastProvisioningRecordId") in
      let lastRecordId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "LastRecordId") in
      let idempotencyToken =
        (Option.map ~f:IdempotencyToken.of_xml)
          (Xml.child xml_arg0 "IdempotencyToken") in
      let createdTime =
        (Option.map ~f:CreatedTime.of_xml) (Xml.child xml_arg0 "CreatedTime") in
      let statusMessage =
        (Option.map ~f:ProvisionedProductStatusMessage.of_xml)
          (Xml.child xml_arg0 "StatusMessage") in
      let status =
        (Option.map ~f:ProvisionedProductStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let id = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "Id") in
      let type_ =
        (Option.map ~f:ProvisionedProductType.of_xml)
          (Xml.child xml_arg0 "Type") in
      let arn =
        (Option.map ~f:ProvisionedProductNameOrArn.of_xml)
          (Xml.child xml_arg0 "Arn") in
      let name =
        (Option.map ~f:ProvisionedProductNameOrArn.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?userArnSession ?userArn ?provisioningArtifactName
        ?provisioningArtifactId ?productName ?productId ?physicalId ?tags
        ?lastSuccessfulProvisioningRecordId ?lastProvisioningRecordId
        ?lastRecordId ?idempotencyToken ?createdTime ?statusMessage ?status
        ?id ?type_ ?arn ?name ()
    let of_json json =
      let userArnSession =
        field_map json "UserArnSession" UserArnSession.of_json in
      let userArn = field_map json "UserArn" UserArn.of_json in
      let provisioningArtifactName =
        field_map json "ProvisioningArtifactName"
          ProvisioningArtifactName.of_json in
      let provisioningArtifactId =
        field_map json "ProvisioningArtifactId" Id.of_json in
      let productName = field_map json "ProductName" ProductViewName.of_json in
      let productId = field_map json "ProductId" Id.of_json in
      let physicalId = field_map json "PhysicalId" PhysicalId.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let lastSuccessfulProvisioningRecordId =
        field_map json "LastSuccessfulProvisioningRecordId" Id.of_json in
      let lastProvisioningRecordId =
        field_map json "LastProvisioningRecordId" Id.of_json in
      let lastRecordId = field_map json "LastRecordId" Id.of_json in
      let idempotencyToken =
        field_map json "IdempotencyToken" IdempotencyToken.of_json in
      let createdTime = field_map json "CreatedTime" CreatedTime.of_json in
      let statusMessage =
        field_map json "StatusMessage"
          ProvisionedProductStatusMessage.of_json in
      let status = field_map json "Status" ProvisionedProductStatus.of_json in
      let id = field_map json "Id" Id.of_json in
      let type_ = field_map json "Type" ProvisionedProductType.of_json in
      let arn = field_map json "Arn" ProvisionedProductNameOrArn.of_json in
      let name = field_map json "Name" ProvisionedProductNameOrArn.of_json in
      make ?userArnSession ?userArn ?provisioningArtifactName
        ?provisioningArtifactId ?productName ?productId ?physicalId ?tags
        ?lastSuccessfulProvisioningRecordId ?lastProvisioningRecordId
        ?lastRecordId ?idempotencyToken ?createdTime ?statusMessage ?status
        ?id ?type_ ?arn ?name ()
    let to_json v = composed_to_json to_value v
  end
module AccessLevelFilterKey =
  struct
    type nonrec t =
      | Account 
      | Role 
      | User 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Account -> "Account"
      | Role -> "Role"
      | User -> "User"
      | Non_static_id s -> s
    let of_string =
      function
      | "Account" -> Account
      | "Role" -> Role
      | "User" -> User
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AccessLevelFilterKey" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AccessLevelFilterKey" j)
    let to_json = simple_to_json to_value
  end
module AccessLevelFilterValue =
  struct
    type nonrec t = string
    let context_ = "AccessLevelFilterValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AccessLevelFilterValue" j
    let to_json = simple_to_json to_value
  end
module ProvisionedProductViewFilterBy =
  struct
    type nonrec t =
      | SearchQuery 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | SearchQuery -> "SearchQuery" | Non_static_id s -> s
    let of_string =
      function | "SearchQuery" -> SearchQuery | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProvisionedProductViewFilterBy"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ProvisionedProductViewFilterBy" j)
    let to_json = simple_to_json to_value
  end
module ProvisionedProductViewFilterValues =
  struct
    type nonrec t = ProvisionedProductViewFilterValue.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProvisionedProductViewFilterValue.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ProvisionedProductViewFilterValue.of_xml)
    let of_json j =
      list_of_json ~kind:"ProvisionedProductViewFilterValues"
        ~of_json:ProvisionedProductViewFilterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProductViewAggregationType =
  struct
    type nonrec t = string
    let context_ = "ProductViewAggregationType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProductViewAggregationType" j
    let to_json = simple_to_json to_value
  end
module ProductViewAggregationValues =
  struct
    type nonrec t = ProductViewAggregationValue.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProductViewAggregationValue.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProductViewAggregationValue.of_xml)
    let of_json j =
      list_of_json ~kind:"ProductViewAggregationValues"
        ~of_json:ProductViewAggregationValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProductViewFilterBy =
  struct
    type nonrec t =
      | FullTextSearch 
      | Owner 
      | ProductType 
      | SourceProductId 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | FullTextSearch -> "FullTextSearch"
      | Owner -> "Owner"
      | ProductType -> "ProductType"
      | SourceProductId -> "SourceProductId"
      | Non_static_id s -> s
    let of_string =
      function
      | "FullTextSearch" -> FullTextSearch
      | "Owner" -> Owner
      | "ProductType" -> ProductType
      | "SourceProductId" -> SourceProductId
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProductViewFilterBy" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ProductViewFilterBy" j)
    let to_json = simple_to_json to_value
  end
module ProductViewFilterValues =
  struct
    type nonrec t = ProductViewFilterValue.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProductViewFilterValue.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProductViewFilterValue.of_xml)
    let of_json j =
      list_of_json ~kind:"ProductViewFilterValues"
        ~of_json:ProductViewFilterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProductViewDetail =
  struct
    type nonrec t =
      {
      productViewSummary: ProductViewSummary.t option ;
      status: Status.t option ;
      productARN: ResourceARN.t option ;
      createdTime: CreatedTime.t option }
    let make ?productViewSummary =
      fun ?status ->
        fun ?productARN ->
          fun ?createdTime ->
            fun () -> { productViewSummary; status; productARN; createdTime }
    let to_value x =
      structure_to_value
        [("ProductViewSummary",
           (Option.map x.productViewSummary ~f:ProductViewSummary.to_value));
        ("Status", (Option.map x.status ~f:Status.to_value));
        ("ProductARN", (Option.map x.productARN ~f:ResourceARN.to_value));
        ("CreatedTime", (Option.map x.createdTime ~f:CreatedTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let createdTime =
        (Option.map ~f:CreatedTime.of_xml) (Xml.child xml_arg0 "CreatedTime") in
      let productARN =
        (Option.map ~f:ResourceARN.of_xml) (Xml.child xml_arg0 "ProductARN") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "Status") in
      let productViewSummary =
        (Option.map ~f:ProductViewSummary.of_xml)
          (Xml.child xml_arg0 "ProductViewSummary") in
      make ?createdTime ?productARN ?status ?productViewSummary ()
    let of_json json =
      let createdTime = field_map json "CreatedTime" CreatedTime.of_json in
      let productARN = field_map json "ProductARN" ResourceARN.of_json in
      let status = field_map json "Status" Status.of_json in
      let productViewSummary =
        field_map json "ProductViewSummary" ProductViewSummary.of_json in
      make ?createdTime ?productARN ?status ?productViewSummary ()
    let to_json v = composed_to_json to_value v
  end
module ProvisionedProductDetail =
  struct
    type nonrec t =
      {
      name: ProvisionedProductNameOrArn.t option ;
      arn: ProvisionedProductNameOrArn.t option ;
      type_: ProvisionedProductType.t option ;
      id: ProvisionedProductId.t option ;
      status: ProvisionedProductStatus.t option ;
      statusMessage: ProvisionedProductStatusMessage.t option ;
      createdTime: CreatedTime.t option ;
      idempotencyToken: IdempotencyToken.t option ;
      lastRecordId: LastRequestId.t option ;
      lastProvisioningRecordId: Id.t option ;
      lastSuccessfulProvisioningRecordId: Id.t option ;
      productId: Id.t option ;
      provisioningArtifactId: Id.t option ;
      launchRoleArn: RoleArn.t option }
    let make ?name =
      fun ?arn ->
        fun ?type_ ->
          fun ?id ->
            fun ?status ->
              fun ?statusMessage ->
                fun ?createdTime ->
                  fun ?idempotencyToken ->
                    fun ?lastRecordId ->
                      fun ?lastProvisioningRecordId ->
                        fun ?lastSuccessfulProvisioningRecordId ->
                          fun ?productId ->
                            fun ?provisioningArtifactId ->
                              fun ?launchRoleArn ->
                                fun () ->
                                  {
                                    name;
                                    arn;
                                    type_;
                                    id;
                                    status;
                                    statusMessage;
                                    createdTime;
                                    idempotencyToken;
                                    lastRecordId;
                                    lastProvisioningRecordId;
                                    lastSuccessfulProvisioningRecordId;
                                    productId;
                                    provisioningArtifactId;
                                    launchRoleArn
                                  }
    let to_value x =
      structure_to_value
        [("Name",
           (Option.map x.name ~f:ProvisionedProductNameOrArn.to_value));
        ("Arn", (Option.map x.arn ~f:ProvisionedProductNameOrArn.to_value));
        ("Type", (Option.map x.type_ ~f:ProvisionedProductType.to_value));
        ("Id", (Option.map x.id ~f:ProvisionedProductId.to_value));
        ("Status",
          (Option.map x.status ~f:ProvisionedProductStatus.to_value));
        ("StatusMessage",
          (Option.map x.statusMessage
             ~f:ProvisionedProductStatusMessage.to_value));
        ("CreatedTime", (Option.map x.createdTime ~f:CreatedTime.to_value));
        ("IdempotencyToken",
          (Option.map x.idempotencyToken ~f:IdempotencyToken.to_value));
        ("LastRecordId",
          (Option.map x.lastRecordId ~f:LastRequestId.to_value));
        ("LastProvisioningRecordId",
          (Option.map x.lastProvisioningRecordId ~f:Id.to_value));
        ("LastSuccessfulProvisioningRecordId",
          (Option.map x.lastSuccessfulProvisioningRecordId ~f:Id.to_value));
        ("ProductId", (Option.map x.productId ~f:Id.to_value));
        ("ProvisioningArtifactId",
          (Option.map x.provisioningArtifactId ~f:Id.to_value));
        ("LaunchRoleArn", (Option.map x.launchRoleArn ~f:RoleArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchRoleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "LaunchRoleArn") in
      let provisioningArtifactId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactId") in
      let productId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProductId") in
      let lastSuccessfulProvisioningRecordId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "LastSuccessfulProvisioningRecordId") in
      let lastProvisioningRecordId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "LastProvisioningRecordId") in
      let lastRecordId =
        (Option.map ~f:LastRequestId.of_xml)
          (Xml.child xml_arg0 "LastRecordId") in
      let idempotencyToken =
        (Option.map ~f:IdempotencyToken.of_xml)
          (Xml.child xml_arg0 "IdempotencyToken") in
      let createdTime =
        (Option.map ~f:CreatedTime.of_xml) (Xml.child xml_arg0 "CreatedTime") in
      let statusMessage =
        (Option.map ~f:ProvisionedProductStatusMessage.of_xml)
          (Xml.child xml_arg0 "StatusMessage") in
      let status =
        (Option.map ~f:ProvisionedProductStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let id =
        (Option.map ~f:ProvisionedProductId.of_xml) (Xml.child xml_arg0 "Id") in
      let type_ =
        (Option.map ~f:ProvisionedProductType.of_xml)
          (Xml.child xml_arg0 "Type") in
      let arn =
        (Option.map ~f:ProvisionedProductNameOrArn.of_xml)
          (Xml.child xml_arg0 "Arn") in
      let name =
        (Option.map ~f:ProvisionedProductNameOrArn.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?launchRoleArn ?provisioningArtifactId ?productId
        ?lastSuccessfulProvisioningRecordId ?lastProvisioningRecordId
        ?lastRecordId ?idempotencyToken ?createdTime ?statusMessage ?status
        ?id ?type_ ?arn ?name ()
    let of_json json =
      let launchRoleArn = field_map json "LaunchRoleArn" RoleArn.of_json in
      let provisioningArtifactId =
        field_map json "ProvisioningArtifactId" Id.of_json in
      let productId = field_map json "ProductId" Id.of_json in
      let lastSuccessfulProvisioningRecordId =
        field_map json "LastSuccessfulProvisioningRecordId" Id.of_json in
      let lastProvisioningRecordId =
        field_map json "LastProvisioningRecordId" Id.of_json in
      let lastRecordId = field_map json "LastRecordId" LastRequestId.of_json in
      let idempotencyToken =
        field_map json "IdempotencyToken" IdempotencyToken.of_json in
      let createdTime = field_map json "CreatedTime" CreatedTime.of_json in
      let statusMessage =
        field_map json "StatusMessage"
          ProvisionedProductStatusMessage.of_json in
      let status = field_map json "Status" ProvisionedProductStatus.of_json in
      let id = field_map json "Id" ProvisionedProductId.of_json in
      let type_ = field_map json "Type" ProvisionedProductType.of_json in
      let arn = field_map json "Arn" ProvisionedProductNameOrArn.of_json in
      let name = field_map json "Name" ProvisionedProductNameOrArn.of_json in
      make ?launchRoleArn ?provisioningArtifactId ?productId
        ?lastSuccessfulProvisioningRecordId ?lastProvisioningRecordId
        ?lastRecordId ?idempotencyToken ?createdTime ?statusMessage ?status
        ?id ?type_ ?arn ?name ()
    let to_json v = composed_to_json to_value v
  end
module ProvisioningParameter =
  struct
    type nonrec t =
      {
      key: ParameterKey.t option ;
      value: ParameterValue.t option }
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:ParameterKey.to_value));
        ("Value", (Option.map x.value ~f:ParameterValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:ParameterValue.of_xml) (Xml.child xml_arg0 "Value") in
      let key =
        (Option.map ~f:ParameterKey.of_xml) (Xml.child xml_arg0 "Key") in
      make ?value ?key ()
    let of_json json =
      let value = field_map json "Value" ParameterValue.of_json in
      let key = field_map json "Key" ParameterKey.of_json in
      make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end
module TagOptionDetail =
  struct
    type nonrec t =
      {
      key: TagOptionKey.t option ;
      value: TagOptionValue.t option ;
      active: TagOptionActive.t option ;
      id: TagOptionId.t option ;
      owner: Owner.t option }
    let make ?key =
      fun ?value ->
        fun ?active ->
          fun ?id ->
            fun ?owner -> fun () -> { key; value; active; id; owner }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:TagOptionKey.to_value));
        ("Value", (Option.map x.value ~f:TagOptionValue.to_value));
        ("Active", (Option.map x.active ~f:TagOptionActive.to_value));
        ("Id", (Option.map x.id ~f:TagOptionId.to_value));
        ("Owner", (Option.map x.owner ~f:Owner.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let owner = (Option.map ~f:Owner.of_xml) (Xml.child xml_arg0 "Owner") in
      let id = (Option.map ~f:TagOptionId.of_xml) (Xml.child xml_arg0 "Id") in
      let active =
        (Option.map ~f:TagOptionActive.of_xml) (Xml.child xml_arg0 "Active") in
      let value =
        (Option.map ~f:TagOptionValue.of_xml) (Xml.child xml_arg0 "Value") in
      let key =
        (Option.map ~f:TagOptionKey.of_xml) (Xml.child xml_arg0 "Key") in
      make ?owner ?id ?active ?value ?key ()
    let of_json json =
      let owner = field_map json "Owner" Owner.of_json in
      let id = field_map json "Id" TagOptionId.of_json in
      let active = field_map json "Active" TagOptionActive.of_json in
      let value = field_map json "Value" TagOptionValue.of_json in
      let key = field_map json "Key" TagOptionKey.of_json in
      make ?owner ?id ?active ?value ?key ()
    let to_json v = composed_to_json to_value v
  end
module StackInstance =
  struct
    type nonrec t =
      {
      account: AccountId.t option ;
      region: Region.t option ;
      stackInstanceStatus: StackInstanceStatus.t option }
    let make ?account =
      fun ?region ->
        fun ?stackInstanceStatus ->
          fun () -> { account; region; stackInstanceStatus }
    let to_value x =
      structure_to_value
        [("Account", (Option.map x.account ~f:AccountId.to_value));
        ("Region", (Option.map x.region ~f:Region.to_value));
        ("StackInstanceStatus",
          (Option.map x.stackInstanceStatus ~f:StackInstanceStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stackInstanceStatus =
        (Option.map ~f:StackInstanceStatus.of_xml)
          (Xml.child xml_arg0 "StackInstanceStatus") in
      let region =
        (Option.map ~f:Region.of_xml) (Xml.child xml_arg0 "Region") in
      let account =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "Account") in
      make ?stackInstanceStatus ?region ?account ()
    let of_json json =
      let stackInstanceStatus =
        field_map json "StackInstanceStatus" StackInstanceStatus.of_json in
      let region = field_map json "Region" Region.of_json in
      let account = field_map json "Account" AccountId.of_json in
      make ?stackInstanceStatus ?region ?account ()
    let to_json v = composed_to_json to_value v
  end
module ResourceDetail =
  struct
    type nonrec t =
      {
      id: ResourceDetailId.t option ;
      aRN: ResourceDetailARN.t option ;
      name: ResourceDetailName.t option ;
      description: ResourceDetailDescription.t option ;
      createdTime: ResourceDetailCreatedTime.t option }
    let make ?id =
      fun ?aRN ->
        fun ?name ->
          fun ?description ->
            fun ?createdTime ->
              fun () -> { id; aRN; name; description; createdTime }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:ResourceDetailId.to_value));
        ("ARN", (Option.map x.aRN ~f:ResourceDetailARN.to_value));
        ("Name", (Option.map x.name ~f:ResourceDetailName.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDetailDescription.to_value));
        ("CreatedTime",
          (Option.map x.createdTime ~f:ResourceDetailCreatedTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let createdTime =
        (Option.map ~f:ResourceDetailCreatedTime.of_xml)
          (Xml.child xml_arg0 "CreatedTime") in
      let description =
        (Option.map ~f:ResourceDetailDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        (Option.map ~f:ResourceDetailName.of_xml) (Xml.child xml_arg0 "Name") in
      let aRN =
        (Option.map ~f:ResourceDetailARN.of_xml) (Xml.child xml_arg0 "ARN") in
      let id =
        (Option.map ~f:ResourceDetailId.of_xml) (Xml.child xml_arg0 "Id") in
      make ?createdTime ?description ?name ?aRN ?id ()
    let of_json json =
      let createdTime =
        field_map json "CreatedTime" ResourceDetailCreatedTime.of_json in
      let description =
        field_map json "Description" ResourceDetailDescription.of_json in
      let name = field_map json "Name" ResourceDetailName.of_json in
      let aRN = field_map json "ARN" ResourceDetailARN.of_json in
      let id = field_map json "Id" ResourceDetailId.of_json in
      make ?createdTime ?description ?name ?aRN ?id ()
    let to_json v = composed_to_json to_value v
  end
module RecordDetail =
  struct
    type nonrec t =
      {
      recordId: Id.t option ;
      provisionedProductName: ProvisionedProductName.t option ;
      status: RecordStatus.t option ;
      createdTime: CreatedTime.t option ;
      updatedTime: UpdatedTime.t option ;
      provisionedProductType: ProvisionedProductType.t option ;
      recordType: RecordType.t option ;
      provisionedProductId: Id.t option ;
      productId: Id.t option ;
      provisioningArtifactId: Id.t option ;
      pathId: Id.t option ;
      recordErrors: RecordErrors.t option ;
      recordTags: RecordTags.t option ;
      launchRoleArn: RoleArn.t option }
    let make ?recordId =
      fun ?provisionedProductName ->
        fun ?status ->
          fun ?createdTime ->
            fun ?updatedTime ->
              fun ?provisionedProductType ->
                fun ?recordType ->
                  fun ?provisionedProductId ->
                    fun ?productId ->
                      fun ?provisioningArtifactId ->
                        fun ?pathId ->
                          fun ?recordErrors ->
                            fun ?recordTags ->
                              fun ?launchRoleArn ->
                                fun () ->
                                  {
                                    recordId;
                                    provisionedProductName;
                                    status;
                                    createdTime;
                                    updatedTime;
                                    provisionedProductType;
                                    recordType;
                                    provisionedProductId;
                                    productId;
                                    provisioningArtifactId;
                                    pathId;
                                    recordErrors;
                                    recordTags;
                                    launchRoleArn
                                  }
    let to_value x =
      structure_to_value
        [("RecordId", (Option.map x.recordId ~f:Id.to_value));
        ("ProvisionedProductName",
          (Option.map x.provisionedProductName
             ~f:ProvisionedProductName.to_value));
        ("Status", (Option.map x.status ~f:RecordStatus.to_value));
        ("CreatedTime", (Option.map x.createdTime ~f:CreatedTime.to_value));
        ("UpdatedTime", (Option.map x.updatedTime ~f:UpdatedTime.to_value));
        ("ProvisionedProductType",
          (Option.map x.provisionedProductType
             ~f:ProvisionedProductType.to_value));
        ("RecordType", (Option.map x.recordType ~f:RecordType.to_value));
        ("ProvisionedProductId",
          (Option.map x.provisionedProductId ~f:Id.to_value));
        ("ProductId", (Option.map x.productId ~f:Id.to_value));
        ("ProvisioningArtifactId",
          (Option.map x.provisioningArtifactId ~f:Id.to_value));
        ("PathId", (Option.map x.pathId ~f:Id.to_value));
        ("RecordErrors",
          (Option.map x.recordErrors ~f:RecordErrors.to_value));
        ("RecordTags", (Option.map x.recordTags ~f:RecordTags.to_value));
        ("LaunchRoleArn", (Option.map x.launchRoleArn ~f:RoleArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchRoleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "LaunchRoleArn") in
      let recordTags =
        (Option.map ~f:RecordTags.of_xml) (Xml.child xml_arg0 "RecordTags") in
      let recordErrors =
        (Option.map ~f:RecordErrors.of_xml)
          (Xml.child xml_arg0 "RecordErrors") in
      let pathId = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PathId") in
      let provisioningArtifactId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactId") in
      let productId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProductId") in
      let provisionedProductId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProvisionedProductId") in
      let recordType =
        (Option.map ~f:RecordType.of_xml) (Xml.child xml_arg0 "RecordType") in
      let provisionedProductType =
        (Option.map ~f:ProvisionedProductType.of_xml)
          (Xml.child xml_arg0 "ProvisionedProductType") in
      let updatedTime =
        (Option.map ~f:UpdatedTime.of_xml) (Xml.child xml_arg0 "UpdatedTime") in
      let createdTime =
        (Option.map ~f:CreatedTime.of_xml) (Xml.child xml_arg0 "CreatedTime") in
      let status =
        (Option.map ~f:RecordStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let provisionedProductName =
        (Option.map ~f:ProvisionedProductName.of_xml)
          (Xml.child xml_arg0 "ProvisionedProductName") in
      let recordId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "RecordId") in
      make ?launchRoleArn ?recordTags ?recordErrors ?pathId
        ?provisioningArtifactId ?productId ?provisionedProductId ?recordType
        ?provisionedProductType ?updatedTime ?createdTime ?status
        ?provisionedProductName ?recordId ()
    let of_json json =
      let launchRoleArn = field_map json "LaunchRoleArn" RoleArn.of_json in
      let recordTags = field_map json "RecordTags" RecordTags.of_json in
      let recordErrors = field_map json "RecordErrors" RecordErrors.of_json in
      let pathId = field_map json "PathId" Id.of_json in
      let provisioningArtifactId =
        field_map json "ProvisioningArtifactId" Id.of_json in
      let productId = field_map json "ProductId" Id.of_json in
      let provisionedProductId =
        field_map json "ProvisionedProductId" Id.of_json in
      let recordType = field_map json "RecordType" RecordType.of_json in
      let provisionedProductType =
        field_map json "ProvisionedProductType"
          ProvisionedProductType.of_json in
      let updatedTime = field_map json "UpdatedTime" UpdatedTime.of_json in
      let createdTime = field_map json "CreatedTime" CreatedTime.of_json in
      let status = field_map json "Status" RecordStatus.of_json in
      let provisionedProductName =
        field_map json "ProvisionedProductName"
          ProvisionedProductName.of_json in
      let recordId = field_map json "RecordId" Id.of_json in
      make ?launchRoleArn ?recordTags ?recordErrors ?pathId
        ?provisioningArtifactId ?productId ?provisionedProductId ?recordType
        ?provisionedProductType ?updatedTime ?createdTime ?status
        ?provisionedProductName ?recordId ()
    let to_json v = composed_to_json to_value v
  end
module SearchFilterKey =
  struct
    type nonrec t = string
    let context_ = "SearchFilterKey"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SearchFilterKey" j
    let to_json = simple_to_json to_value
  end
module SearchFilterValue =
  struct
    type nonrec t = string
    let context_ = "SearchFilterValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SearchFilterValue" j
    let to_json = simple_to_json to_value
  end
module ProvisioningArtifactDetail =
  struct
    type nonrec t =
      {
      id: Id.t option ;
      name: ProvisioningArtifactName.t option ;
      description: ProvisioningArtifactName.t option ;
      type_: ProvisioningArtifactType.t option ;
      createdTime: CreationTime.t option ;
      active: ProvisioningArtifactActive.t option ;
      guidance: ProvisioningArtifactGuidance.t option }
    let make ?id =
      fun ?name ->
        fun ?description ->
          fun ?type_ ->
            fun ?createdTime ->
              fun ?active ->
                fun ?guidance ->
                  fun () ->
                    {
                      id;
                      name;
                      description;
                      type_;
                      createdTime;
                      active;
                      guidance
                    }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:Id.to_value));
        ("Name", (Option.map x.name ~f:ProvisioningArtifactName.to_value));
        ("Description",
          (Option.map x.description ~f:ProvisioningArtifactName.to_value));
        ("Type", (Option.map x.type_ ~f:ProvisioningArtifactType.to_value));
        ("CreatedTime", (Option.map x.createdTime ~f:CreationTime.to_value));
        ("Active",
          (Option.map x.active ~f:ProvisioningArtifactActive.to_value));
        ("Guidance",
          (Option.map x.guidance ~f:ProvisioningArtifactGuidance.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let guidance =
        (Option.map ~f:ProvisioningArtifactGuidance.of_xml)
          (Xml.child xml_arg0 "Guidance") in
      let active =
        (Option.map ~f:ProvisioningArtifactActive.of_xml)
          (Xml.child xml_arg0 "Active") in
      let createdTime =
        (Option.map ~f:CreationTime.of_xml)
          (Xml.child xml_arg0 "CreatedTime") in
      let type_ =
        (Option.map ~f:ProvisioningArtifactType.of_xml)
          (Xml.child xml_arg0 "Type") in
      let description =
        (Option.map ~f:ProvisioningArtifactName.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        (Option.map ~f:ProvisioningArtifactName.of_xml)
          (Xml.child xml_arg0 "Name") in
      let id = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "Id") in
      make ?guidance ?active ?createdTime ?type_ ?description ?name ?id ()
    let of_json json =
      let guidance =
        field_map json "Guidance" ProvisioningArtifactGuidance.of_json in
      let active = field_map json "Active" ProvisioningArtifactActive.of_json in
      let createdTime = field_map json "CreatedTime" CreationTime.of_json in
      let type_ = field_map json "Type" ProvisioningArtifactType.of_json in
      let description =
        field_map json "Description" ProvisioningArtifactName.of_json in
      let name = field_map json "Name" ProvisioningArtifactName.of_json in
      let id = field_map json "Id" Id.of_json in
      make ?guidance ?active ?createdTime ?type_ ?description ?name ?id ()
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifactView =
  struct
    type nonrec t =
      {
      productViewSummary: ProductViewSummary.t option ;
      provisioningArtifact: ProvisioningArtifact.t option }
    let make ?productViewSummary =
      fun ?provisioningArtifact ->
        fun () -> { productViewSummary; provisioningArtifact }
    let to_value x =
      structure_to_value
        [("ProductViewSummary",
           (Option.map x.productViewSummary ~f:ProductViewSummary.to_value));
        ("ProvisioningArtifact",
          (Option.map x.provisioningArtifact ~f:ProvisioningArtifact.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let provisioningArtifact =
        (Option.map ~f:ProvisioningArtifact.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifact") in
      let productViewSummary =
        (Option.map ~f:ProductViewSummary.of_xml)
          (Xml.child xml_arg0 "ProductViewSummary") in
      make ?provisioningArtifact ?productViewSummary ()
    let of_json json =
      let provisioningArtifact =
        field_map json "ProvisioningArtifact" ProvisioningArtifact.of_json in
      let productViewSummary =
        field_map json "ProductViewSummary" ProductViewSummary.of_json in
      make ?provisioningArtifact ?productViewSummary ()
    let to_json v = composed_to_json to_value v
  end
module ProvisionedProductPlanSummary =
  struct
    type nonrec t =
      {
      planName: ProvisionedProductPlanName.t option ;
      planId: Id.t option ;
      provisionProductId: Id.t option ;
      provisionProductName: ProvisionedProductName.t option ;
      planType: ProvisionedProductPlanType.t option ;
      provisioningArtifactId: Id.t option }
    let make ?planName =
      fun ?planId ->
        fun ?provisionProductId ->
          fun ?provisionProductName ->
            fun ?planType ->
              fun ?provisioningArtifactId ->
                fun () ->
                  {
                    planName;
                    planId;
                    provisionProductId;
                    provisionProductName;
                    planType;
                    provisioningArtifactId
                  }
    let to_value x =
      structure_to_value
        [("PlanName",
           (Option.map x.planName ~f:ProvisionedProductPlanName.to_value));
        ("PlanId", (Option.map x.planId ~f:Id.to_value));
        ("ProvisionProductId",
          (Option.map x.provisionProductId ~f:Id.to_value));
        ("ProvisionProductName",
          (Option.map x.provisionProductName
             ~f:ProvisionedProductName.to_value));
        ("PlanType",
          (Option.map x.planType ~f:ProvisionedProductPlanType.to_value));
        ("ProvisioningArtifactId",
          (Option.map x.provisioningArtifactId ~f:Id.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let provisioningArtifactId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactId") in
      let planType =
        (Option.map ~f:ProvisionedProductPlanType.of_xml)
          (Xml.child xml_arg0 "PlanType") in
      let provisionProductName =
        (Option.map ~f:ProvisionedProductName.of_xml)
          (Xml.child xml_arg0 "ProvisionProductName") in
      let provisionProductId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProvisionProductId") in
      let planId = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PlanId") in
      let planName =
        (Option.map ~f:ProvisionedProductPlanName.of_xml)
          (Xml.child xml_arg0 "PlanName") in
      make ?provisioningArtifactId ?planType ?provisionProductName
        ?provisionProductId ?planId ?planName ()
    let of_json json =
      let provisioningArtifactId =
        field_map json "ProvisioningArtifactId" Id.of_json in
      let planType =
        field_map json "PlanType" ProvisionedProductPlanType.of_json in
      let provisionProductName =
        field_map json "ProvisionProductName" ProvisionedProductName.of_json in
      let provisionProductId = field_map json "ProvisionProductId" Id.of_json in
      let planId = field_map json "PlanId" Id.of_json in
      let planName =
        field_map json "PlanName" ProvisionedProductPlanName.of_json in
      make ?provisioningArtifactId ?planType ?provisionProductName
        ?provisionProductId ?planId ?planName ()
    let to_json v = composed_to_json to_value v
  end
module Principal =
  struct
    type nonrec t =
      {
      principalARN: PrincipalARN.t option ;
      principalType: PrincipalType.t option }
    let make ?principalARN =
      fun ?principalType -> fun () -> { principalARN; principalType }
    let to_value x =
      structure_to_value
        [("PrincipalARN",
           (Option.map x.principalARN ~f:PrincipalARN.to_value));
        ("PrincipalType",
          (Option.map x.principalType ~f:PrincipalType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let principalType =
        (Option.map ~f:PrincipalType.of_xml)
          (Xml.child xml_arg0 "PrincipalType") in
      let principalARN =
        (Option.map ~f:PrincipalARN.of_xml)
          (Xml.child xml_arg0 "PrincipalARN") in
      make ?principalType ?principalARN ()
    let of_json json =
      let principalType =
        field_map json "PrincipalType" PrincipalType.of_json in
      let principalARN = field_map json "PrincipalARN" PrincipalARN.of_json in
      make ?principalType ?principalARN ()
    let to_json v = composed_to_json to_value v
  end
module PortfolioDetail =
  struct
    type nonrec t =
      {
      id: Id.t option ;
      aRN: ResourceARN.t option ;
      displayName: PortfolioDisplayName.t option ;
      description: PortfolioDescription.t option ;
      createdTime: CreationTime.t option ;
      providerName: ProviderName.t option }
    let make ?id =
      fun ?aRN ->
        fun ?displayName ->
          fun ?description ->
            fun ?createdTime ->
              fun ?providerName ->
                fun () ->
                  {
                    id;
                    aRN;
                    displayName;
                    description;
                    createdTime;
                    providerName
                  }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:Id.to_value));
        ("ARN", (Option.map x.aRN ~f:ResourceARN.to_value));
        ("DisplayName",
          (Option.map x.displayName ~f:PortfolioDisplayName.to_value));
        ("Description",
          (Option.map x.description ~f:PortfolioDescription.to_value));
        ("CreatedTime", (Option.map x.createdTime ~f:CreationTime.to_value));
        ("ProviderName",
          (Option.map x.providerName ~f:ProviderName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let providerName =
        (Option.map ~f:ProviderName.of_xml)
          (Xml.child xml_arg0 "ProviderName") in
      let createdTime =
        (Option.map ~f:CreationTime.of_xml)
          (Xml.child xml_arg0 "CreatedTime") in
      let description =
        (Option.map ~f:PortfolioDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let displayName =
        (Option.map ~f:PortfolioDisplayName.of_xml)
          (Xml.child xml_arg0 "DisplayName") in
      let aRN = (Option.map ~f:ResourceARN.of_xml) (Xml.child xml_arg0 "ARN") in
      let id = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "Id") in
      make ?providerName ?createdTime ?description ?displayName ?aRN ?id ()
    let of_json json =
      let providerName = field_map json "ProviderName" ProviderName.of_json in
      let createdTime = field_map json "CreatedTime" CreationTime.of_json in
      let description =
        field_map json "Description" PortfolioDescription.of_json in
      let displayName =
        field_map json "DisplayName" PortfolioDisplayName.of_json in
      let aRN = field_map json "ARN" ResourceARN.of_json in
      let id = field_map json "Id" Id.of_json in
      make ?providerName ?createdTime ?description ?displayName ?aRN ?id ()
    let to_json v = composed_to_json to_value v
  end
module OrganizationNode =
  struct
    type nonrec t =
      {
      type_: OrganizationNodeType.t option ;
      value: OrganizationNodeValue.t option }
    let make ?type_ = fun ?value -> fun () -> { type_; value }
    let to_value x =
      structure_to_value
        [("Type", (Option.map x.type_ ~f:OrganizationNodeType.to_value));
        ("Value", (Option.map x.value ~f:OrganizationNodeValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:OrganizationNodeValue.of_xml)
          (Xml.child xml_arg0 "Value") in
      let type_ =
        (Option.map ~f:OrganizationNodeType.of_xml)
          (Xml.child xml_arg0 "Type") in
      make ?value ?type_ ()
    let of_json json =
      let value = field_map json "Value" OrganizationNodeValue.of_json in
      let type_ = field_map json "Type" OrganizationNodeType.of_json in
      make ?value ?type_ ()
    let to_json v = composed_to_json to_value v
  end
module LaunchPathSummary =
  struct
    type nonrec t =
      {
      id: Id.t option ;
      constraintSummaries: ConstraintSummaries.t option ;
      tags: Tags.t option ;
      name: PortfolioName.t option }
    let make ?id =
      fun ?constraintSummaries ->
        fun ?tags ->
          fun ?name -> fun () -> { id; constraintSummaries; tags; name }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:Id.to_value));
        ("ConstraintSummaries",
          (Option.map x.constraintSummaries ~f:ConstraintSummaries.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("Name", (Option.map x.name ~f:PortfolioName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:PortfolioName.of_xml) (Xml.child xml_arg0 "Name") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let constraintSummaries =
        (Option.map ~f:ConstraintSummaries.of_xml)
          (Xml.child xml_arg0 "ConstraintSummaries") in
      let id = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "Id") in
      make ?name ?tags ?constraintSummaries ?id ()
    let of_json json =
      let name = field_map json "Name" PortfolioName.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let constraintSummaries =
        field_map json "ConstraintSummaries" ConstraintSummaries.of_json in
      let id = field_map json "Id" Id.of_json in
      make ?name ?tags ?constraintSummaries ?id ()
    let to_json v = composed_to_json to_value v
  end
module ConstraintDetail =
  struct
    type nonrec t =
      {
      constraintId: Id.t option ;
      type_: ConstraintType.t option ;
      description: ConstraintDescription.t option ;
      owner: AccountId.t option ;
      productId: Id.t option ;
      portfolioId: Id.t option }
    let make ?constraintId =
      fun ?type_ ->
        fun ?description ->
          fun ?owner ->
            fun ?productId ->
              fun ?portfolioId ->
                fun () ->
                  {
                    constraintId;
                    type_;
                    description;
                    owner;
                    productId;
                    portfolioId
                  }
    let to_value x =
      structure_to_value
        [("ConstraintId", (Option.map x.constraintId ~f:Id.to_value));
        ("Type", (Option.map x.type_ ~f:ConstraintType.to_value));
        ("Description",
          (Option.map x.description ~f:ConstraintDescription.to_value));
        ("Owner", (Option.map x.owner ~f:AccountId.to_value));
        ("ProductId", (Option.map x.productId ~f:Id.to_value));
        ("PortfolioId", (Option.map x.portfolioId ~f:Id.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let portfolioId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PortfolioId") in
      let productId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProductId") in
      let owner =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "Owner") in
      let description =
        (Option.map ~f:ConstraintDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let type_ =
        (Option.map ~f:ConstraintType.of_xml) (Xml.child xml_arg0 "Type") in
      let constraintId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ConstraintId") in
      make ?portfolioId ?productId ?owner ?description ?type_ ?constraintId
        ()
    let of_json json =
      let portfolioId = field_map json "PortfolioId" Id.of_json in
      let productId = field_map json "ProductId" Id.of_json in
      let owner = field_map json "Owner" AccountId.of_json in
      let description =
        field_map json "Description" ConstraintDescription.of_json in
      let type_ = field_map json "Type" ConstraintType.of_json in
      let constraintId = field_map json "ConstraintId" Id.of_json in
      make ?portfolioId ?productId ?owner ?description ?type_ ?constraintId
        ()
    let to_json v = composed_to_json to_value v
  end
module BudgetDetail =
  struct
    type nonrec t = {
      budgetName: BudgetName.t option }
    let make ?budgetName = fun () -> { budgetName }
    let to_value x =
      structure_to_value
        [("BudgetName", (Option.map x.budgetName ~f:BudgetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let budgetName =
        (Option.map ~f:BudgetName.of_xml) (Xml.child xml_arg0 "BudgetName") in
      make ?budgetName ()
    let of_json json =
      let budgetName = field_map json "BudgetName" BudgetName.of_json in
      make ?budgetName ()
    let to_json v = composed_to_json to_value v
  end
module RecordOutput =
  struct
    type nonrec t =
      {
      outputKey: OutputKey.t option ;
      outputValue: OutputValue.t option ;
      description: Description.t option }
    let make ?outputKey =
      fun ?outputValue ->
        fun ?description -> fun () -> { outputKey; outputValue; description }
    let to_value x =
      structure_to_value
        [("OutputKey", (Option.map x.outputKey ~f:OutputKey.to_value));
        ("OutputValue", (Option.map x.outputValue ~f:OutputValue.to_value));
        ("Description", (Option.map x.description ~f:Description.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      let outputValue =
        (Option.map ~f:OutputValue.of_xml) (Xml.child xml_arg0 "OutputValue") in
      let outputKey =
        (Option.map ~f:OutputKey.of_xml) (Xml.child xml_arg0 "OutputKey") in
      make ?description ?outputValue ?outputKey ()
    let of_json json =
      let description = field_map json "Description" Description.of_json in
      let outputValue = field_map json "OutputValue" OutputValue.of_json in
      let outputKey = field_map json "OutputKey" OutputKey.of_json in
      make ?description ?outputValue ?outputKey ()
    let to_json v = composed_to_json to_value v
  end
module ExecutionParameter =
  struct
    type nonrec t =
      {
      name: ExecutionParameterKey.t option ;
      type_: ExecutionParameterType.t option ;
      defaultValues: ExecutionParameterValueList.t option }
    let make ?name =
      fun ?type_ ->
        fun ?defaultValues -> fun () -> { name; type_; defaultValues }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:ExecutionParameterKey.to_value));
        ("Type", (Option.map x.type_ ~f:ExecutionParameterType.to_value));
        ("DefaultValues",
          (Option.map x.defaultValues ~f:ExecutionParameterValueList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let defaultValues =
        (Option.map ~f:ExecutionParameterValueList.of_xml)
          (Xml.child xml_arg0 "DefaultValues") in
      let type_ =
        (Option.map ~f:ExecutionParameterType.of_xml)
          (Xml.child xml_arg0 "Type") in
      let name =
        (Option.map ~f:ExecutionParameterKey.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?defaultValues ?type_ ?name ()
    let of_json json =
      let defaultValues =
        field_map json "DefaultValues" ExecutionParameterValueList.of_json in
      let type_ = field_map json "Type" ExecutionParameterType.of_json in
      let name = field_map json "Name" ExecutionParameterKey.of_json in
      make ?defaultValues ?type_ ?name ()
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifactOutput =
  struct
    type nonrec t =
      {
      key: ProvisioningArtifactOutputKey.t option ;
      description: OutputDescription.t option }
    let make ?key = fun ?description -> fun () -> { key; description }
    let to_value x =
      structure_to_value
        [("Key",
           (Option.map x.key ~f:ProvisioningArtifactOutputKey.to_value));
        ("Description",
          (Option.map x.description ~f:OutputDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:OutputDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let key =
        (Option.map ~f:ProvisioningArtifactOutputKey.of_xml)
          (Xml.child xml_arg0 "Key") in
      make ?description ?key ()
    let of_json json =
      let description =
        field_map json "Description" OutputDescription.of_json in
      let key = field_map json "Key" ProvisioningArtifactOutputKey.of_json in
      make ?description ?key ()
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifactParameter =
  struct
    type nonrec t =
      {
      parameterKey: ParameterKey.t option ;
      defaultValue: DefaultValue.t option ;
      parameterType: ParameterType.t option ;
      isNoEcho: NoEcho.t option ;
      description: Description.t option ;
      parameterConstraints: ParameterConstraints.t option }
    let make ?parameterKey =
      fun ?defaultValue ->
        fun ?parameterType ->
          fun ?isNoEcho ->
            fun ?description ->
              fun ?parameterConstraints ->
                fun () ->
                  {
                    parameterKey;
                    defaultValue;
                    parameterType;
                    isNoEcho;
                    description;
                    parameterConstraints
                  }
    let to_value x =
      structure_to_value
        [("ParameterKey",
           (Option.map x.parameterKey ~f:ParameterKey.to_value));
        ("DefaultValue",
          (Option.map x.defaultValue ~f:DefaultValue.to_value));
        ("ParameterType",
          (Option.map x.parameterType ~f:ParameterType.to_value));
        ("IsNoEcho", (Option.map x.isNoEcho ~f:NoEcho.to_value));
        ("Description", (Option.map x.description ~f:Description.to_value));
        ("ParameterConstraints",
          (Option.map x.parameterConstraints ~f:ParameterConstraints.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameterConstraints =
        (Option.map ~f:ParameterConstraints.of_xml)
          (Xml.child xml_arg0 "ParameterConstraints") in
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      let isNoEcho =
        (Option.map ~f:NoEcho.of_xml) (Xml.child xml_arg0 "IsNoEcho") in
      let parameterType =
        (Option.map ~f:ParameterType.of_xml)
          (Xml.child xml_arg0 "ParameterType") in
      let defaultValue =
        (Option.map ~f:DefaultValue.of_xml)
          (Xml.child xml_arg0 "DefaultValue") in
      let parameterKey =
        (Option.map ~f:ParameterKey.of_xml)
          (Xml.child xml_arg0 "ParameterKey") in
      make ?parameterConstraints ?description ?isNoEcho ?parameterType
        ?defaultValue ?parameterKey ()
    let of_json json =
      let parameterConstraints =
        field_map json "ParameterConstraints" ParameterConstraints.of_json in
      let description = field_map json "Description" Description.of_json in
      let isNoEcho = field_map json "IsNoEcho" NoEcho.of_json in
      let parameterType =
        field_map json "ParameterType" ParameterType.of_json in
      let defaultValue = field_map json "DefaultValue" DefaultValue.of_json in
      let parameterKey = field_map json "ParameterKey" ParameterKey.of_json in
      make ?parameterConstraints ?description ?isNoEcho ?parameterType
        ?defaultValue ?parameterKey ()
    let to_json v = composed_to_json to_value v
  end
module TagOptionSummary =
  struct
    type nonrec t =
      {
      key: TagOptionKey.t option ;
      values: TagOptionValues.t option }
    let make ?key = fun ?values -> fun () -> { key; values }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:TagOptionKey.to_value));
        ("Values", (Option.map x.values ~f:TagOptionValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        (Option.map ~f:TagOptionValues.of_xml) (Xml.child xml_arg0 "Values") in
      let key =
        (Option.map ~f:TagOptionKey.of_xml) (Xml.child xml_arg0 "Key") in
      make ?values ?key ()
    let of_json json =
      let values = field_map json "Values" TagOptionValues.of_json in
      let key = field_map json "Key" TagOptionKey.of_json in
      make ?values ?key ()
    let to_json v = composed_to_json to_value v
  end
module UsageInstruction =
  struct
    type nonrec t =
      {
      type_: InstructionType.t option ;
      value: InstructionValue.t option }
    let make ?type_ = fun ?value -> fun () -> { type_; value }
    let to_value x =
      structure_to_value
        [("Type", (Option.map x.type_ ~f:InstructionType.to_value));
        ("Value", (Option.map x.value ~f:InstructionValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:InstructionValue.of_xml) (Xml.child xml_arg0 "Value") in
      let type_ =
        (Option.map ~f:InstructionType.of_xml) (Xml.child xml_arg0 "Type") in
      make ?value ?type_ ()
    let of_json json =
      let value = field_map json "Value" InstructionValue.of_json in
      let type_ = field_map json "Type" InstructionType.of_json in
      make ?value ?type_ ()
    let to_json v = composed_to_json to_value v
  end
module NotificationArns =
  struct
    type nonrec t = NotificationArn.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:5); i
    let to_value xs =
      (xs |> (List.map ~f:NotificationArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NotificationArn.of_xml)
    let of_json j =
      list_of_json ~kind:"NotificationArns" ~of_json:NotificationArn.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ProvisionedProductPlanStatus =
  struct
    type nonrec t =
      | CREATE_IN_PROGRESS 
      | CREATE_SUCCESS 
      | CREATE_FAILED 
      | EXECUTE_IN_PROGRESS 
      | EXECUTE_SUCCESS 
      | EXECUTE_FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATE_IN_PROGRESS -> "CREATE_IN_PROGRESS"
      | CREATE_SUCCESS -> "CREATE_SUCCESS"
      | CREATE_FAILED -> "CREATE_FAILED"
      | EXECUTE_IN_PROGRESS -> "EXECUTE_IN_PROGRESS"
      | EXECUTE_SUCCESS -> "EXECUTE_SUCCESS"
      | EXECUTE_FAILED -> "EXECUTE_FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATE_IN_PROGRESS" -> CREATE_IN_PROGRESS
      | "CREATE_SUCCESS" -> CREATE_SUCCESS
      | "CREATE_FAILED" -> CREATE_FAILED
      | "EXECUTE_IN_PROGRESS" -> EXECUTE_IN_PROGRESS
      | "EXECUTE_SUCCESS" -> EXECUTE_SUCCESS
      | "EXECUTE_FAILED" -> EXECUTE_FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProvisionedProductPlanStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ProvisionedProductPlanStatus" j)
    let to_json = simple_to_json to_value
  end
module StatusMessage =
  struct
    type nonrec t = string
    let context_ = "StatusMessage"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"[\\u0009\\u000a\\u000d\\u0020-\\uD7FF\\uE000-\\uFFFD]*");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StatusMessage" j
    let to_json = simple_to_json to_value
  end
module UpdateProvisioningParameters =
  struct
    type nonrec t = UpdateProvisioningParameter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:UpdateProvisioningParameter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UpdateProvisioningParameter.of_xml)
    let of_json j =
      list_of_json ~kind:"UpdateProvisioningParameters"
        ~of_json:UpdateProvisioningParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceChange =
  struct
    type nonrec t =
      {
      action: ChangeAction.t option ;
      logicalResourceId: LogicalResourceId.t option ;
      physicalResourceId: PhysicalResourceId.t option ;
      resourceType: PlanResourceType.t option ;
      replacement: Replacement.t option ;
      scope: Scope.t option ;
      details: ResourceChangeDetails.t option }
    let make ?action =
      fun ?logicalResourceId ->
        fun ?physicalResourceId ->
          fun ?resourceType ->
            fun ?replacement ->
              fun ?scope ->
                fun ?details ->
                  fun () ->
                    {
                      action;
                      logicalResourceId;
                      physicalResourceId;
                      resourceType;
                      replacement;
                      scope;
                      details
                    }
    let to_value x =
      structure_to_value
        [("Action", (Option.map x.action ~f:ChangeAction.to_value));
        ("LogicalResourceId",
          (Option.map x.logicalResourceId ~f:LogicalResourceId.to_value));
        ("PhysicalResourceId",
          (Option.map x.physicalResourceId ~f:PhysicalResourceId.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:PlanResourceType.to_value));
        ("Replacement", (Option.map x.replacement ~f:Replacement.to_value));
        ("Scope", (Option.map x.scope ~f:Scope.to_value));
        ("Details", (Option.map x.details ~f:ResourceChangeDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let details =
        (Option.map ~f:ResourceChangeDetails.of_xml)
          (Xml.child xml_arg0 "Details") in
      let scope = (Option.map ~f:Scope.of_xml) (Xml.child xml_arg0 "Scope") in
      let replacement =
        (Option.map ~f:Replacement.of_xml) (Xml.child xml_arg0 "Replacement") in
      let resourceType =
        (Option.map ~f:PlanResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let physicalResourceId =
        (Option.map ~f:PhysicalResourceId.of_xml)
          (Xml.child xml_arg0 "PhysicalResourceId") in
      let logicalResourceId =
        (Option.map ~f:LogicalResourceId.of_xml)
          (Xml.child xml_arg0 "LogicalResourceId") in
      let action =
        (Option.map ~f:ChangeAction.of_xml) (Xml.child xml_arg0 "Action") in
      make ?details ?scope ?replacement ?resourceType ?physicalResourceId
        ?logicalResourceId ?action ()
    let of_json json =
      let details = field_map json "Details" ResourceChangeDetails.of_json in
      let scope = field_map json "Scope" Scope.of_json in
      let replacement = field_map json "Replacement" Replacement.of_json in
      let resourceType =
        field_map json "ResourceType" PlanResourceType.of_json in
      let physicalResourceId =
        field_map json "PhysicalResourceId" PhysicalResourceId.of_json in
      let logicalResourceId =
        field_map json "LogicalResourceId" LogicalResourceId.of_json in
      let action = field_map json "Action" ChangeAction.of_json in
      make ?details ?scope ?replacement ?resourceType ?physicalResourceId
        ?logicalResourceId ?action ()
    let to_json v = composed_to_json to_value v
  end
module CloudWatchDashboard =
  struct
    type nonrec t = {
      name: CloudWatchDashboardName.t option }
    let make ?name = fun () -> { name }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:CloudWatchDashboardName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:CloudWatchDashboardName.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?name ()
    let of_json json =
      let name = field_map json "Name" CloudWatchDashboardName.of_json in
      make ?name ()
    let to_json v = composed_to_json to_value v
  end
module LaunchPath =
  struct
    type nonrec t = {
      id: Id.t option ;
      name: PortfolioName.t option }
    let make ?id = fun ?name -> fun () -> { id; name }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:Id.to_value));
        ("Name", (Option.map x.name ~f:PortfolioName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:PortfolioName.of_xml) (Xml.child xml_arg0 "Name") in
      let id = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "Id") in
      make ?name ?id ()
    let of_json json =
      let name = field_map json "Name" PortfolioName.of_json in
      let id = field_map json "Id" Id.of_json in make ?name ?id ()
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifactSummary =
  struct
    type nonrec t =
      {
      id: Id.t option ;
      name: ProvisioningArtifactName.t option ;
      description: ProvisioningArtifactDescription.t option ;
      createdTime: ProvisioningArtifactCreatedTime.t option ;
      provisioningArtifactMetadata: ProvisioningArtifactInfo.t option }
    let make ?id =
      fun ?name ->
        fun ?description ->
          fun ?createdTime ->
            fun ?provisioningArtifactMetadata ->
              fun () ->
                {
                  id;
                  name;
                  description;
                  createdTime;
                  provisioningArtifactMetadata
                }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:Id.to_value));
        ("Name", (Option.map x.name ~f:ProvisioningArtifactName.to_value));
        ("Description",
          (Option.map x.description
             ~f:ProvisioningArtifactDescription.to_value));
        ("CreatedTime",
          (Option.map x.createdTime
             ~f:ProvisioningArtifactCreatedTime.to_value));
        ("ProvisioningArtifactMetadata",
          (Option.map x.provisioningArtifactMetadata
             ~f:ProvisioningArtifactInfo.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let provisioningArtifactMetadata =
        (Option.map ~f:ProvisioningArtifactInfo.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactMetadata") in
      let createdTime =
        (Option.map ~f:ProvisioningArtifactCreatedTime.of_xml)
          (Xml.child xml_arg0 "CreatedTime") in
      let description =
        (Option.map ~f:ProvisioningArtifactDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        (Option.map ~f:ProvisioningArtifactName.of_xml)
          (Xml.child xml_arg0 "Name") in
      let id = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "Id") in
      make ?provisioningArtifactMetadata ?createdTime ?description ?name ?id
        ()
    let of_json json =
      let provisioningArtifactMetadata =
        field_map json "ProvisioningArtifactMetadata"
          ProvisioningArtifactInfo.of_json in
      let createdTime =
        field_map json "CreatedTime" ProvisioningArtifactCreatedTime.of_json in
      let description =
        field_map json "Description" ProvisioningArtifactDescription.of_json in
      let name = field_map json "Name" ProvisioningArtifactName.of_json in
      let id = field_map json "Id" Id.of_json in
      make ?provisioningArtifactMetadata ?createdTime ?description ?name ?id
        ()
    let to_json v = composed_to_json to_value v
  end
module PortfolioShareDetail =
  struct
    type nonrec t =
      {
      principalId: Id.t option ;
      type_: DescribePortfolioShareType.t option ;
      accepted: Boolean.t option ;
      shareTagOptions: Boolean.t option }
    let make ?principalId =
      fun ?type_ ->
        fun ?accepted ->
          fun ?shareTagOptions ->
            fun () -> { principalId; type_; accepted; shareTagOptions }
    let to_value x =
      structure_to_value
        [("PrincipalId", (Option.map x.principalId ~f:Id.to_value));
        ("Type", (Option.map x.type_ ~f:DescribePortfolioShareType.to_value));
        ("Accepted", (Option.map x.accepted ~f:Boolean.to_value));
        ("ShareTagOptions",
          (Option.map x.shareTagOptions ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shareTagOptions =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ShareTagOptions") in
      let accepted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Accepted") in
      let type_ =
        (Option.map ~f:DescribePortfolioShareType.of_xml)
          (Xml.child xml_arg0 "Type") in
      let principalId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PrincipalId") in
      make ?shareTagOptions ?accepted ?type_ ?principalId ()
    let of_json json =
      let shareTagOptions = field_map json "ShareTagOptions" Boolean.of_json in
      let accepted = field_map json "Accepted" Boolean.of_json in
      let type_ = field_map json "Type" DescribePortfolioShareType.of_json in
      let principalId = field_map json "PrincipalId" Id.of_json in
      make ?shareTagOptions ?accepted ?type_ ?principalId ()
    let to_json v = composed_to_json to_value v
  end
module ShareErrors =
  struct
    type nonrec t = ShareError.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ShareError.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ShareError.of_xml)
    let of_json j =
      list_of_json ~kind:"ShareErrors" ~of_json:ShareError.of_json j
    let to_json v = composed_to_json to_value v
  end
module SuccessfulShares =
  struct
    type nonrec t = AccountId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AccountId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccountId.of_xml)
    let of_json j =
      list_of_json ~kind:"SuccessfulShares" ~of_json:AccountId.of_json j
    let to_json v = composed_to_json to_value v
  end
module DisableTemplateValidation =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module CopyOption =
  struct
    type nonrec t =
      | CopyTags 
      | Non_static_id of string 
    let make i = i
    let to_string = function | CopyTags -> "CopyTags" | Non_static_id s -> s
    let of_string = function | "CopyTags" -> CopyTags | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CopyOption" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CopyOption" j)
    let to_json = simple_to_json to_value
  end
module SourceProvisioningArtifactPropertiesMap =
  struct
    type nonrec t =
      (ProvisioningArtifactPropertyName.t *
        ProvisioningArtifactPropertyValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ProvisioningArtifactPropertyName.of_string
                                chopped),
                              (ProvisioningArtifactPropertyValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ProvisioningArtifactPropertyName.to_value x) |>
                    (fun x ->
                       (ProvisioningArtifactPropertyValue.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json
        ~key_of_string:ProvisioningArtifactPropertyName.of_string
        ~of_json:ProvisioningArtifactPropertyValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module FailedServiceActionAssociation =
  struct
    type nonrec t =
      {
      serviceActionId: Id.t option ;
      productId: Id.t option ;
      provisioningArtifactId: Id.t option ;
      errorCode: ServiceActionAssociationErrorCode.t option ;
      errorMessage: ServiceActionAssociationErrorMessage.t option }
    let make ?serviceActionId =
      fun ?productId ->
        fun ?provisioningArtifactId ->
          fun ?errorCode ->
            fun ?errorMessage ->
              fun () ->
                {
                  serviceActionId;
                  productId;
                  provisioningArtifactId;
                  errorCode;
                  errorMessage
                }
    let to_value x =
      structure_to_value
        [("ServiceActionId", (Option.map x.serviceActionId ~f:Id.to_value));
        ("ProductId", (Option.map x.productId ~f:Id.to_value));
        ("ProvisioningArtifactId",
          (Option.map x.provisioningArtifactId ~f:Id.to_value));
        ("ErrorCode",
          (Option.map x.errorCode
             ~f:ServiceActionAssociationErrorCode.to_value));
        ("ErrorMessage",
          (Option.map x.errorMessage
             ~f:ServiceActionAssociationErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage =
        (Option.map ~f:ServiceActionAssociationErrorMessage.of_xml)
          (Xml.child xml_arg0 "ErrorMessage") in
      let errorCode =
        (Option.map ~f:ServiceActionAssociationErrorCode.of_xml)
          (Xml.child xml_arg0 "ErrorCode") in
      let provisioningArtifactId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactId") in
      let productId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProductId") in
      let serviceActionId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ServiceActionId") in
      make ?errorMessage ?errorCode ?provisioningArtifactId ?productId
        ?serviceActionId ()
    let of_json json =
      let errorMessage =
        field_map json "ErrorMessage"
          ServiceActionAssociationErrorMessage.of_json in
      let errorCode =
        field_map json "ErrorCode" ServiceActionAssociationErrorCode.of_json in
      let provisioningArtifactId =
        field_map json "ProvisioningArtifactId" Id.of_json in
      let productId = field_map json "ProductId" Id.of_json in
      let serviceActionId = field_map json "ServiceActionId" Id.of_json in
      make ?errorMessage ?errorCode ?provisioningArtifactId ?productId
        ?serviceActionId ()
    let to_json v = composed_to_json to_value v
  end
module ServiceActionAssociation =
  struct
    type nonrec t =
      {
      serviceActionId: Id.t ;
      productId: Id.t ;
      provisioningArtifactId: Id.t }
    let context_ = "ServiceActionAssociation"
    let make ~serviceActionId =
      fun ~productId ->
        fun ~provisioningArtifactId ->
          fun () -> { serviceActionId; productId; provisioningArtifactId }
    let to_value x =
      structure_to_value
        [("ServiceActionId", (Some (Id.to_value x.serviceActionId)));
        ("ProductId", (Some (Id.to_value x.productId)));
        ("ProvisioningArtifactId",
          (Some (Id.to_value x.provisioningArtifactId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let provisioningArtifactId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisioningArtifactId") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      let serviceActionId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceActionId") in
      make ~provisioningArtifactId ~productId ~serviceActionId ()
    let of_json json =
      let provisioningArtifactId =
        field_map_exn json "ProvisioningArtifactId" Id.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      let serviceActionId = field_map_exn json "ServiceActionId" Id.of_json in
      make ~provisioningArtifactId ~productId ~serviceActionId ()
    let to_json v = composed_to_json to_value v
  end
module DuplicateResourceException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module InvalidParametersException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module ResourceNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module TagOptionNotMigratedException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module ServiceActionDetail =
  struct
    type nonrec t =
      {
      serviceActionSummary: ServiceActionSummary.t option ;
      definition: ServiceActionDefinitionMap.t option }
    let make ?serviceActionSummary =
      fun ?definition -> fun () -> { serviceActionSummary; definition }
    let to_value x =
      structure_to_value
        [("ServiceActionSummary",
           (Option.map x.serviceActionSummary
              ~f:ServiceActionSummary.to_value));
        ("Definition",
          (Option.map x.definition ~f:ServiceActionDefinitionMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let definition =
        (Option.map ~f:ServiceActionDefinitionMap.of_xml)
          (Xml.child xml_arg0 "Definition") in
      let serviceActionSummary =
        (Option.map ~f:ServiceActionSummary.of_xml)
          (Xml.child xml_arg0 "ServiceActionSummary") in
      make ?definition ?serviceActionSummary ()
    let of_json json =
      let definition =
        field_map json "Definition" ServiceActionDefinitionMap.of_json in
      let serviceActionSummary =
        field_map json "ServiceActionSummary" ServiceActionSummary.of_json in
      make ?definition ?serviceActionSummary ()
    let to_json v = composed_to_json to_value v
  end
module AcceptLanguage =
  struct
    type nonrec t = string
    let context_ = "AcceptLanguage"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:100); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AcceptLanguage" j
    let to_json = simple_to_json to_value
  end
module InvalidStateException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module ProvisionedProductProperties =
  struct
    type nonrec t = (PropertyKey.t * PropertyValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((PropertyKey.of_string chopped),
                              (PropertyValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (PropertyKey.to_value x) |>
                    (fun x -> (PropertyValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:PropertyKey.of_string
        ~of_json:PropertyValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateProvisioningPreferences =
  struct
    type nonrec t =
      {
      stackSetAccounts: StackSetAccounts.t option ;
      stackSetRegions: StackSetRegions.t option ;
      stackSetFailureToleranceCount: StackSetFailureToleranceCount.t option ;
      stackSetFailureTolerancePercentage:
        StackSetFailureTolerancePercentage.t option ;
      stackSetMaxConcurrencyCount: StackSetMaxConcurrencyCount.t option ;
      stackSetMaxConcurrencyPercentage:
        StackSetMaxConcurrencyPercentage.t option ;
      stackSetOperationType: StackSetOperationType.t option }
    let make ?stackSetAccounts =
      fun ?stackSetRegions ->
        fun ?stackSetFailureToleranceCount ->
          fun ?stackSetFailureTolerancePercentage ->
            fun ?stackSetMaxConcurrencyCount ->
              fun ?stackSetMaxConcurrencyPercentage ->
                fun ?stackSetOperationType ->
                  fun () ->
                    {
                      stackSetAccounts;
                      stackSetRegions;
                      stackSetFailureToleranceCount;
                      stackSetFailureTolerancePercentage;
                      stackSetMaxConcurrencyCount;
                      stackSetMaxConcurrencyPercentage;
                      stackSetOperationType
                    }
    let to_value x =
      structure_to_value
        [("StackSetAccounts",
           (Option.map x.stackSetAccounts ~f:StackSetAccounts.to_value));
        ("StackSetRegions",
          (Option.map x.stackSetRegions ~f:StackSetRegions.to_value));
        ("StackSetFailureToleranceCount",
          (Option.map x.stackSetFailureToleranceCount
             ~f:StackSetFailureToleranceCount.to_value));
        ("StackSetFailureTolerancePercentage",
          (Option.map x.stackSetFailureTolerancePercentage
             ~f:StackSetFailureTolerancePercentage.to_value));
        ("StackSetMaxConcurrencyCount",
          (Option.map x.stackSetMaxConcurrencyCount
             ~f:StackSetMaxConcurrencyCount.to_value));
        ("StackSetMaxConcurrencyPercentage",
          (Option.map x.stackSetMaxConcurrencyPercentage
             ~f:StackSetMaxConcurrencyPercentage.to_value));
        ("StackSetOperationType",
          (Option.map x.stackSetOperationType
             ~f:StackSetOperationType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stackSetOperationType =
        (Option.map ~f:StackSetOperationType.of_xml)
          (Xml.child xml_arg0 "StackSetOperationType") in
      let stackSetMaxConcurrencyPercentage =
        (Option.map ~f:StackSetMaxConcurrencyPercentage.of_xml)
          (Xml.child xml_arg0 "StackSetMaxConcurrencyPercentage") in
      let stackSetMaxConcurrencyCount =
        (Option.map ~f:StackSetMaxConcurrencyCount.of_xml)
          (Xml.child xml_arg0 "StackSetMaxConcurrencyCount") in
      let stackSetFailureTolerancePercentage =
        (Option.map ~f:StackSetFailureTolerancePercentage.of_xml)
          (Xml.child xml_arg0 "StackSetFailureTolerancePercentage") in
      let stackSetFailureToleranceCount =
        (Option.map ~f:StackSetFailureToleranceCount.of_xml)
          (Xml.child xml_arg0 "StackSetFailureToleranceCount") in
      let stackSetRegions =
        (Option.map ~f:StackSetRegions.of_xml)
          (Xml.child xml_arg0 "StackSetRegions") in
      let stackSetAccounts =
        (Option.map ~f:StackSetAccounts.of_xml)
          (Xml.child xml_arg0 "StackSetAccounts") in
      make ?stackSetOperationType ?stackSetMaxConcurrencyPercentage
        ?stackSetMaxConcurrencyCount ?stackSetFailureTolerancePercentage
        ?stackSetFailureToleranceCount ?stackSetRegions ?stackSetAccounts ()
    let of_json json =
      let stackSetOperationType =
        field_map json "StackSetOperationType" StackSetOperationType.of_json in
      let stackSetMaxConcurrencyPercentage =
        field_map json "StackSetMaxConcurrencyPercentage"
          StackSetMaxConcurrencyPercentage.of_json in
      let stackSetMaxConcurrencyCount =
        field_map json "StackSetMaxConcurrencyCount"
          StackSetMaxConcurrencyCount.of_json in
      let stackSetFailureTolerancePercentage =
        field_map json "StackSetFailureTolerancePercentage"
          StackSetFailureTolerancePercentage.of_json in
      let stackSetFailureToleranceCount =
        field_map json "StackSetFailureToleranceCount"
          StackSetFailureToleranceCount.of_json in
      let stackSetRegions =
        field_map json "StackSetRegions" StackSetRegions.of_json in
      let stackSetAccounts =
        field_map json "StackSetAccounts" StackSetAccounts.of_json in
      make ?stackSetOperationType ?stackSetMaxConcurrencyPercentage
        ?stackSetMaxConcurrencyCount ?stackSetFailureTolerancePercentage
        ?stackSetFailureToleranceCount ?stackSetRegions ?stackSetAccounts ()
    let to_json v = composed_to_json to_value v
  end
module AddTags =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:20); i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"AddTags" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagKeys =
  struct
    type nonrec t = TagKey.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeys" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module OperationNotSupportedException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module ShareStatus =
  struct
    type nonrec t =
      | NOT_STARTED 
      | IN_PROGRESS 
      | COMPLETED 
      | COMPLETED_WITH_ERRORS 
      | ERROR 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NOT_STARTED -> "NOT_STARTED"
      | IN_PROGRESS -> "IN_PROGRESS"
      | COMPLETED -> "COMPLETED"
      | COMPLETED_WITH_ERRORS -> "COMPLETED_WITH_ERRORS"
      | ERROR -> "ERROR"
      | Non_static_id s -> s
    let of_string =
      function
      | "NOT_STARTED" -> NOT_STARTED
      | "IN_PROGRESS" -> IN_PROGRESS
      | "COMPLETED" -> COMPLETED
      | "COMPLETED_WITH_ERRORS" -> COMPLETED_WITH_ERRORS
      | "ERROR" -> ERROR
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ShareStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ShareStatus" j)
    let to_json = simple_to_json to_value
  end
module NullableBoolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module LimitExceededException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module ConstraintParameters =
  struct
    type nonrec t = string
    let context_ = "ConstraintParameters"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ConstraintParameters" j
    let to_json = simple_to_json to_value
  end
module IgnoreErrors =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module RetainPhysicalResources =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module PageToken =
  struct
    type nonrec t = string
    let context_ = "PageToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2024) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"[\\u0009\\u000a\\u000d\\u0020-\\uD7FF\\uE000-\\uFFFD]*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PageToken" j
    let to_json = simple_to_json to_value
  end
module ProvisionedProductAttributes =
  struct
    type nonrec t = ProvisionedProductAttribute.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProvisionedProductAttribute.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProvisionedProductAttribute.of_xml)
    let of_json j =
      list_of_json ~kind:"ProvisionedProductAttributes"
        ~of_json:ProvisionedProductAttribute.of_json j
    let to_json v = composed_to_json to_value v
  end
module TotalResultsCount =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for TotalResultsCount" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AccessLevelFilter =
  struct
    type nonrec t =
      {
      key: AccessLevelFilterKey.t option ;
      value: AccessLevelFilterValue.t option }
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:AccessLevelFilterKey.to_value));
        ("Value", (Option.map x.value ~f:AccessLevelFilterValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:AccessLevelFilterValue.of_xml)
          (Xml.child xml_arg0 "Value") in
      let key =
        (Option.map ~f:AccessLevelFilterKey.of_xml)
          (Xml.child xml_arg0 "Key") in
      make ?value ?key ()
    let of_json json =
      let value = field_map json "Value" AccessLevelFilterValue.of_json in
      let key = field_map json "Key" AccessLevelFilterKey.of_json in
      make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end
module ProvisionedProductFilters =
  struct
    type nonrec t =
      (ProvisionedProductViewFilterBy.t *
        ProvisionedProductViewFilterValues.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ProvisionedProductViewFilterBy ProvisionedProductViewFilterValues"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ProvisionedProductViewFilterBy.to_value x) |>
                    (fun x ->
                       (ProvisionedProductViewFilterValues.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ProvisionedProductViewFilterBy.of_string
        ~of_json:ProvisionedProductViewFilterValues.of_json j
    let to_json v = composed_to_json to_value v
  end
module SearchProvisionedProductsPageSize =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for SearchProvisionedProductsPageSize" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SortField =
  struct
    type nonrec t = string
    let context_ = "SortField"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SortField" j
    let to_json = simple_to_json to_value
  end
module SortOrder =
  struct
    type nonrec t =
      | ASCENDING 
      | DESCENDING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ASCENDING -> "ASCENDING"
      | DESCENDING -> "DESCENDING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ASCENDING" -> ASCENDING
      | "DESCENDING" -> DESCENDING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SortOrder" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SortOrder" j)
    let to_json = simple_to_json to_value
  end
module ProductViewAggregations =
  struct
    type nonrec t =
      (ProductViewAggregationType.t * ProductViewAggregationValues.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ProductViewAggregationType ProductViewAggregationValues"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ProductViewAggregationType.to_value x) |>
                    (fun x ->
                       (ProductViewAggregationValues.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ProductViewAggregationType.of_string
        ~of_json:ProductViewAggregationValues.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProductViewSummaries =
  struct
    type nonrec t = ProductViewSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProductViewSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProductViewSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ProductViewSummaries"
        ~of_json:ProductViewSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module PageSizeMax100 =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for PageSizeMax100" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ProductViewFilters =
  struct
    type nonrec t = (ProductViewFilterBy.t * ProductViewFilterValues.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ProductViewFilterBy ProductViewFilterValues"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ProductViewFilterBy.to_value x) |>
                    (fun x ->
                       (ProductViewFilterValues.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ProductViewFilterBy.of_string
        ~of_json:ProductViewFilterValues.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProductViewSortBy =
  struct
    type nonrec t =
      | Title 
      | VersionCount 
      | CreationDate 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Title -> "Title"
      | VersionCount -> "VersionCount"
      | CreationDate -> "CreationDate"
      | Non_static_id s -> s
    let of_string =
      function
      | "Title" -> Title
      | "VersionCount" -> VersionCount
      | "CreationDate" -> CreationDate
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProductViewSortBy" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ProductViewSortBy" j)
    let to_json = simple_to_json to_value
  end
module ProductViewDetails =
  struct
    type nonrec t = ProductViewDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProductViewDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProductViewDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"ProductViewDetails"
        ~of_json:ProductViewDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module PageSize =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:20) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for PageSize" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ProductSource =
  struct
    type nonrec t =
      | ACCOUNT 
      | Non_static_id of string 
    let make i = i
    let to_string = function | ACCOUNT -> "ACCOUNT" | Non_static_id s -> s
    let of_string = function | "ACCOUNT" -> ACCOUNT | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ProductSource" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ProductSource" j)
    let to_json = simple_to_json to_value
  end
module ProvisionedProductDetails =
  struct
    type nonrec t = ProvisionedProductDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProvisionedProductDetail.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProvisionedProductDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"ProvisionedProductDetails"
        ~of_json:ProvisionedProductDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module PortfolioShareType =
  struct
    type nonrec t =
      | IMPORTED 
      | AWS_SERVICECATALOG 
      | AWS_ORGANIZATIONS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | IMPORTED -> "IMPORTED"
      | AWS_SERVICECATALOG -> "AWS_SERVICECATALOG"
      | AWS_ORGANIZATIONS -> "AWS_ORGANIZATIONS"
      | Non_static_id s -> s
    let of_string =
      function
      | "IMPORTED" -> IMPORTED
      | "AWS_SERVICECATALOG" -> AWS_SERVICECATALOG
      | "AWS_ORGANIZATIONS" -> AWS_ORGANIZATIONS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PortfolioShareType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PortfolioShareType" j)
    let to_json = simple_to_json to_value
  end
module ProvisioningParameters =
  struct
    type nonrec t = ProvisioningParameter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProvisioningParameter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProvisioningParameter.of_xml)
    let of_json j =
      list_of_json ~kind:"ProvisioningParameters"
        ~of_json:ProvisioningParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProvisioningPreferences =
  struct
    type nonrec t =
      {
      stackSetAccounts: StackSetAccounts.t option ;
      stackSetRegions: StackSetRegions.t option ;
      stackSetFailureToleranceCount: StackSetFailureToleranceCount.t option ;
      stackSetFailureTolerancePercentage:
        StackSetFailureTolerancePercentage.t option ;
      stackSetMaxConcurrencyCount: StackSetMaxConcurrencyCount.t option ;
      stackSetMaxConcurrencyPercentage:
        StackSetMaxConcurrencyPercentage.t option }
    let make ?stackSetAccounts =
      fun ?stackSetRegions ->
        fun ?stackSetFailureToleranceCount ->
          fun ?stackSetFailureTolerancePercentage ->
            fun ?stackSetMaxConcurrencyCount ->
              fun ?stackSetMaxConcurrencyPercentage ->
                fun () ->
                  {
                    stackSetAccounts;
                    stackSetRegions;
                    stackSetFailureToleranceCount;
                    stackSetFailureTolerancePercentage;
                    stackSetMaxConcurrencyCount;
                    stackSetMaxConcurrencyPercentage
                  }
    let to_value x =
      structure_to_value
        [("StackSetAccounts",
           (Option.map x.stackSetAccounts ~f:StackSetAccounts.to_value));
        ("StackSetRegions",
          (Option.map x.stackSetRegions ~f:StackSetRegions.to_value));
        ("StackSetFailureToleranceCount",
          (Option.map x.stackSetFailureToleranceCount
             ~f:StackSetFailureToleranceCount.to_value));
        ("StackSetFailureTolerancePercentage",
          (Option.map x.stackSetFailureTolerancePercentage
             ~f:StackSetFailureTolerancePercentage.to_value));
        ("StackSetMaxConcurrencyCount",
          (Option.map x.stackSetMaxConcurrencyCount
             ~f:StackSetMaxConcurrencyCount.to_value));
        ("StackSetMaxConcurrencyPercentage",
          (Option.map x.stackSetMaxConcurrencyPercentage
             ~f:StackSetMaxConcurrencyPercentage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stackSetMaxConcurrencyPercentage =
        (Option.map ~f:StackSetMaxConcurrencyPercentage.of_xml)
          (Xml.child xml_arg0 "StackSetMaxConcurrencyPercentage") in
      let stackSetMaxConcurrencyCount =
        (Option.map ~f:StackSetMaxConcurrencyCount.of_xml)
          (Xml.child xml_arg0 "StackSetMaxConcurrencyCount") in
      let stackSetFailureTolerancePercentage =
        (Option.map ~f:StackSetFailureTolerancePercentage.of_xml)
          (Xml.child xml_arg0 "StackSetFailureTolerancePercentage") in
      let stackSetFailureToleranceCount =
        (Option.map ~f:StackSetFailureToleranceCount.of_xml)
          (Xml.child xml_arg0 "StackSetFailureToleranceCount") in
      let stackSetRegions =
        (Option.map ~f:StackSetRegions.of_xml)
          (Xml.child xml_arg0 "StackSetRegions") in
      let stackSetAccounts =
        (Option.map ~f:StackSetAccounts.of_xml)
          (Xml.child xml_arg0 "StackSetAccounts") in
      make ?stackSetMaxConcurrencyPercentage ?stackSetMaxConcurrencyCount
        ?stackSetFailureTolerancePercentage ?stackSetFailureToleranceCount
        ?stackSetRegions ?stackSetAccounts ()
    let of_json json =
      let stackSetMaxConcurrencyPercentage =
        field_map json "StackSetMaxConcurrencyPercentage"
          StackSetMaxConcurrencyPercentage.of_json in
      let stackSetMaxConcurrencyCount =
        field_map json "StackSetMaxConcurrencyCount"
          StackSetMaxConcurrencyCount.of_json in
      let stackSetFailureTolerancePercentage =
        field_map json "StackSetFailureTolerancePercentage"
          StackSetFailureTolerancePercentage.of_json in
      let stackSetFailureToleranceCount =
        field_map json "StackSetFailureToleranceCount"
          StackSetFailureToleranceCount.of_json in
      let stackSetRegions =
        field_map json "StackSetRegions" StackSetRegions.of_json in
      let stackSetAccounts =
        field_map json "StackSetAccounts" StackSetAccounts.of_json in
      make ?stackSetMaxConcurrencyPercentage ?stackSetMaxConcurrencyCount
        ?stackSetFailureTolerancePercentage ?stackSetFailureToleranceCount
        ?stackSetRegions ?stackSetAccounts ()
    let to_json v = composed_to_json to_value v
  end
module TagOptionDetails =
  struct
    type nonrec t = TagOptionDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TagOptionDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagOptionDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"TagOptionDetails" ~of_json:TagOptionDetail.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ListTagOptionsFilters =
  struct
    type nonrec t =
      {
      key: TagOptionKey.t option ;
      value: TagOptionValue.t option ;
      active: TagOptionActive.t option }
    let make ?key =
      fun ?value -> fun ?active -> fun () -> { key; value; active }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:TagOptionKey.to_value));
        ("Value", (Option.map x.value ~f:TagOptionValue.to_value));
        ("Active", (Option.map x.active ~f:TagOptionActive.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let active =
        (Option.map ~f:TagOptionActive.of_xml) (Xml.child xml_arg0 "Active") in
      let value =
        (Option.map ~f:TagOptionValue.of_xml) (Xml.child xml_arg0 "Value") in
      let key =
        (Option.map ~f:TagOptionKey.of_xml) (Xml.child xml_arg0 "Key") in
      make ?active ?value ?key ()
    let of_json json =
      let active = field_map json "Active" TagOptionActive.of_json in
      let value = field_map json "Value" TagOptionValue.of_json in
      let key = field_map json "Key" TagOptionKey.of_json in
      make ?active ?value ?key ()
    let to_json v = composed_to_json to_value v
  end
module StackInstances =
  struct
    type nonrec t = StackInstance.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:StackInstance.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StackInstance.of_xml)
    let of_json j =
      list_of_json ~kind:"StackInstances" ~of_json:StackInstance.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServiceActionSummaries =
  struct
    type nonrec t = ServiceActionSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ServiceActionSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ServiceActionSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ServiceActionSummaries"
        ~of_json:ServiceActionSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceDetails =
  struct
    type nonrec t = ResourceDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceDetails" ~of_json:ResourceDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceType =
  struct
    type nonrec t = string
    let context_ = "ResourceType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceType" j
    let to_json = simple_to_json to_value
  end
module RecordDetails =
  struct
    type nonrec t = RecordDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:RecordDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RecordDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"RecordDetails" ~of_json:RecordDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListRecordHistorySearchFilter =
  struct
    type nonrec t =
      {
      key: SearchFilterKey.t option ;
      value: SearchFilterValue.t option }
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:SearchFilterKey.to_value));
        ("Value", (Option.map x.value ~f:SearchFilterValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:SearchFilterValue.of_xml) (Xml.child xml_arg0 "Value") in
      let key =
        (Option.map ~f:SearchFilterKey.of_xml) (Xml.child xml_arg0 "Key") in
      make ?value ?key ()
    let of_json json =
      let value = field_map json "Value" SearchFilterValue.of_json in
      let key = field_map json "Key" SearchFilterKey.of_json in
      make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifactDetails =
  struct
    type nonrec t = ProvisioningArtifactDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProvisioningArtifactDetail.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProvisioningArtifactDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"ProvisioningArtifactDetails"
        ~of_json:ProvisioningArtifactDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifactViews =
  struct
    type nonrec t = ProvisioningArtifactView.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProvisioningArtifactView.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProvisioningArtifactView.of_xml)
    let of_json j =
      list_of_json ~kind:"ProvisioningArtifactViews"
        ~of_json:ProvisioningArtifactView.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProvisionedProductPlans =
  struct
    type nonrec t = ProvisionedProductPlanSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProvisionedProductPlanSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProvisionedProductPlanSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ProvisionedProductPlans"
        ~of_json:ProvisionedProductPlanSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module Principals =
  struct
    type nonrec t = Principal.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Principal.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Principal.of_xml)
    let of_json j =
      list_of_json ~kind:"Principals" ~of_json:Principal.of_json j
    let to_json v = composed_to_json to_value v
  end
module PortfolioDetails =
  struct
    type nonrec t = PortfolioDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PortfolioDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PortfolioDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"PortfolioDetails" ~of_json:PortfolioDetail.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module AccountIds =
  struct
    type nonrec t = AccountId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AccountId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccountId.of_xml)
    let of_json j =
      list_of_json ~kind:"AccountIds" ~of_json:AccountId.of_json j
    let to_json v = composed_to_json to_value v
  end
module OrganizationNodes =
  struct
    type nonrec t = OrganizationNode.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:OrganizationNode.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:OrganizationNode.of_xml)
    let of_json j =
      list_of_json ~kind:"OrganizationNodes"
        ~of_json:OrganizationNode.of_json j
    let to_json v = composed_to_json to_value v
  end
module LaunchPathSummaries =
  struct
    type nonrec t = LaunchPathSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LaunchPathSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LaunchPathSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"LaunchPathSummaries"
        ~of_json:LaunchPathSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConstraintDetails =
  struct
    type nonrec t = ConstraintDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ConstraintDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ConstraintDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"ConstraintDetails"
        ~of_json:ConstraintDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module Budgets =
  struct
    type nonrec t = BudgetDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BudgetDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BudgetDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"Budgets" ~of_json:BudgetDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module RecordOutputs =
  struct
    type nonrec t = RecordOutput.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:RecordOutput.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RecordOutput.of_xml)
    let of_json j =
      list_of_json ~kind:"RecordOutputs" ~of_json:RecordOutput.of_json j
    let to_json v = composed_to_json to_value v
  end
module OutputKeys =
  struct
    type nonrec t = OutputKey.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:60); i
    let to_value xs =
      (xs |> (List.map ~f:OutputKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:OutputKey.of_xml)
    let of_json j =
      list_of_json ~kind:"OutputKeys" ~of_json:OutputKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module AccessStatus =
  struct
    type nonrec t =
      | ENABLED 
      | UNDER_CHANGE 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | UNDER_CHANGE -> "UNDER_CHANGE"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "UNDER_CHANGE" -> UNDER_CHANGE
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AccessStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AccessStatus" j)
    let to_json = simple_to_json to_value
  end
module ExecutionParameterMap =
  struct
    type nonrec t =
      (ExecutionParameterKey.t * ExecutionParameterValueList.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ExecutionParameterKey ExecutionParameterValueList"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExecutionParameterKey.to_value x) |>
                    (fun x ->
                       (ExecutionParameterValueList.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ExecutionParameterKey.of_string
        ~of_json:ExecutionParameterValueList.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceId =
  struct
    type nonrec t = string
    let context_ = "ResourceId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceId" j
    let to_json = simple_to_json to_value
  end
module ResourceInUseException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module ExecutionParameters =
  struct
    type nonrec t = ExecutionParameter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ExecutionParameter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExecutionParameter.of_xml)
    let of_json j =
      list_of_json ~kind:"ExecutionParameters"
        ~of_json:ExecutionParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifactOutputs =
  struct
    type nonrec t = ProvisioningArtifactOutput.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:60); i
    let to_value xs =
      (xs |> (List.map ~f:ProvisioningArtifactOutput.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProvisioningArtifactOutput.of_xml)
    let of_json j =
      list_of_json ~kind:"ProvisioningArtifactOutputs"
        ~of_json:ProvisioningArtifactOutput.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifactParameters =
  struct
    type nonrec t = ProvisioningArtifactParameter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProvisioningArtifactParameter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProvisioningArtifactParameter.of_xml)
    let of_json j =
      list_of_json ~kind:"ProvisioningArtifactParameters"
        ~of_json:ProvisioningArtifactParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifactPreferences =
  struct
    type nonrec t =
      {
      stackSetAccounts: StackSetAccounts.t option ;
      stackSetRegions: StackSetRegions.t option }
    let make ?stackSetAccounts =
      fun ?stackSetRegions -> fun () -> { stackSetAccounts; stackSetRegions }
    let to_value x =
      structure_to_value
        [("StackSetAccounts",
           (Option.map x.stackSetAccounts ~f:StackSetAccounts.to_value));
        ("StackSetRegions",
          (Option.map x.stackSetRegions ~f:StackSetRegions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stackSetRegions =
        (Option.map ~f:StackSetRegions.of_xml)
          (Xml.child xml_arg0 "StackSetRegions") in
      let stackSetAccounts =
        (Option.map ~f:StackSetAccounts.of_xml)
          (Xml.child xml_arg0 "StackSetAccounts") in
      make ?stackSetRegions ?stackSetAccounts ()
    let of_json json =
      let stackSetRegions =
        field_map json "StackSetRegions" StackSetRegions.of_json in
      let stackSetAccounts =
        field_map json "StackSetAccounts" StackSetAccounts.of_json in
      make ?stackSetRegions ?stackSetAccounts ()
    let to_json v = composed_to_json to_value v
  end
module TagOptionSummaries =
  struct
    type nonrec t = TagOptionSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TagOptionSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagOptionSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"TagOptionSummaries"
        ~of_json:TagOptionSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module UsageInstructions =
  struct
    type nonrec t = UsageInstruction.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:UsageInstruction.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UsageInstruction.of_xml)
    let of_json j =
      list_of_json ~kind:"UsageInstructions"
        ~of_json:UsageInstruction.of_json j
    let to_json v = composed_to_json to_value v
  end
module Verbose =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ProvisionedProductPlanDetails =
  struct
    type nonrec t =
      {
      createdTime: CreatedTime.t option ;
      pathId: Id.t option ;
      productId: Id.t option ;
      planName: ProvisionedProductPlanName.t option ;
      planId: Id.t option ;
      provisionProductId: Id.t option ;
      provisionProductName: ProvisionedProductName.t option ;
      planType: ProvisionedProductPlanType.t option ;
      provisioningArtifactId: Id.t option ;
      status: ProvisionedProductPlanStatus.t option ;
      updatedTime: UpdatedTime.t option ;
      notificationArns: NotificationArns.t option ;
      provisioningParameters: UpdateProvisioningParameters.t option ;
      tags: Tags.t option ;
      statusMessage: StatusMessage.t option }
    let make ?createdTime =
      fun ?pathId ->
        fun ?productId ->
          fun ?planName ->
            fun ?planId ->
              fun ?provisionProductId ->
                fun ?provisionProductName ->
                  fun ?planType ->
                    fun ?provisioningArtifactId ->
                      fun ?status ->
                        fun ?updatedTime ->
                          fun ?notificationArns ->
                            fun ?provisioningParameters ->
                              fun ?tags ->
                                fun ?statusMessage ->
                                  fun () ->
                                    {
                                      createdTime;
                                      pathId;
                                      productId;
                                      planName;
                                      planId;
                                      provisionProductId;
                                      provisionProductName;
                                      planType;
                                      provisioningArtifactId;
                                      status;
                                      updatedTime;
                                      notificationArns;
                                      provisioningParameters;
                                      tags;
                                      statusMessage
                                    }
    let to_value x =
      structure_to_value
        [("CreatedTime", (Option.map x.createdTime ~f:CreatedTime.to_value));
        ("PathId", (Option.map x.pathId ~f:Id.to_value));
        ("ProductId", (Option.map x.productId ~f:Id.to_value));
        ("PlanName",
          (Option.map x.planName ~f:ProvisionedProductPlanName.to_value));
        ("PlanId", (Option.map x.planId ~f:Id.to_value));
        ("ProvisionProductId",
          (Option.map x.provisionProductId ~f:Id.to_value));
        ("ProvisionProductName",
          (Option.map x.provisionProductName
             ~f:ProvisionedProductName.to_value));
        ("PlanType",
          (Option.map x.planType ~f:ProvisionedProductPlanType.to_value));
        ("ProvisioningArtifactId",
          (Option.map x.provisioningArtifactId ~f:Id.to_value));
        ("Status",
          (Option.map x.status ~f:ProvisionedProductPlanStatus.to_value));
        ("UpdatedTime", (Option.map x.updatedTime ~f:UpdatedTime.to_value));
        ("NotificationArns",
          (Option.map x.notificationArns ~f:NotificationArns.to_value));
        ("ProvisioningParameters",
          (Option.map x.provisioningParameters
             ~f:UpdateProvisioningParameters.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("StatusMessage",
          (Option.map x.statusMessage ~f:StatusMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statusMessage =
        (Option.map ~f:StatusMessage.of_xml)
          (Xml.child xml_arg0 "StatusMessage") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let provisioningParameters =
        (Option.map ~f:UpdateProvisioningParameters.of_xml)
          (Xml.child xml_arg0 "ProvisioningParameters") in
      let notificationArns =
        (Option.map ~f:NotificationArns.of_xml)
          (Xml.child xml_arg0 "NotificationArns") in
      let updatedTime =
        (Option.map ~f:UpdatedTime.of_xml) (Xml.child xml_arg0 "UpdatedTime") in
      let status =
        (Option.map ~f:ProvisionedProductPlanStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let provisioningArtifactId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactId") in
      let planType =
        (Option.map ~f:ProvisionedProductPlanType.of_xml)
          (Xml.child xml_arg0 "PlanType") in
      let provisionProductName =
        (Option.map ~f:ProvisionedProductName.of_xml)
          (Xml.child xml_arg0 "ProvisionProductName") in
      let provisionProductId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProvisionProductId") in
      let planId = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PlanId") in
      let planName =
        (Option.map ~f:ProvisionedProductPlanName.of_xml)
          (Xml.child xml_arg0 "PlanName") in
      let productId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProductId") in
      let pathId = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PathId") in
      let createdTime =
        (Option.map ~f:CreatedTime.of_xml) (Xml.child xml_arg0 "CreatedTime") in
      make ?statusMessage ?tags ?provisioningParameters ?notificationArns
        ?updatedTime ?status ?provisioningArtifactId ?planType
        ?provisionProductName ?provisionProductId ?planId ?planName
        ?productId ?pathId ?createdTime ()
    let of_json json =
      let statusMessage =
        field_map json "StatusMessage" StatusMessage.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let provisioningParameters =
        field_map json "ProvisioningParameters"
          UpdateProvisioningParameters.of_json in
      let notificationArns =
        field_map json "NotificationArns" NotificationArns.of_json in
      let updatedTime = field_map json "UpdatedTime" UpdatedTime.of_json in
      let status =
        field_map json "Status" ProvisionedProductPlanStatus.of_json in
      let provisioningArtifactId =
        field_map json "ProvisioningArtifactId" Id.of_json in
      let planType =
        field_map json "PlanType" ProvisionedProductPlanType.of_json in
      let provisionProductName =
        field_map json "ProvisionProductName" ProvisionedProductName.of_json in
      let provisionProductId = field_map json "ProvisionProductId" Id.of_json in
      let planId = field_map json "PlanId" Id.of_json in
      let planName =
        field_map json "PlanName" ProvisionedProductPlanName.of_json in
      let productId = field_map json "ProductId" Id.of_json in
      let pathId = field_map json "PathId" Id.of_json in
      let createdTime = field_map json "CreatedTime" CreatedTime.of_json in
      make ?statusMessage ?tags ?provisioningParameters ?notificationArns
        ?updatedTime ?status ?provisioningArtifactId ?planType
        ?provisionProductName ?provisionProductId ?planId ?planName
        ?productId ?pathId ?createdTime ()
    let to_json v = composed_to_json to_value v
  end
module ResourceChanges =
  struct
    type nonrec t = ResourceChange.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceChange.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceChange.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceChanges" ~of_json:ResourceChange.of_json j
    let to_json v = composed_to_json to_value v
  end
module CloudWatchDashboards =
  struct
    type nonrec t = CloudWatchDashboard.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CloudWatchDashboard.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CloudWatchDashboard.of_xml)
    let of_json j =
      list_of_json ~kind:"CloudWatchDashboards"
        ~of_json:CloudWatchDashboard.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifacts =
  struct
    type nonrec t = ProvisioningArtifact.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProvisioningArtifact.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProvisioningArtifact.of_xml)
    let of_json j =
      list_of_json ~kind:"ProvisioningArtifacts"
        ~of_json:ProvisioningArtifact.of_json j
    let to_json v = composed_to_json to_value v
  end
module LaunchPaths =
  struct
    type nonrec t = LaunchPath.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LaunchPath.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LaunchPath.of_xml)
    let of_json j =
      list_of_json ~kind:"LaunchPaths" ~of_json:LaunchPath.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProvisioningArtifactSummaries =
  struct
    type nonrec t = ProvisioningArtifactSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProvisioningArtifactSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProvisioningArtifactSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ProvisioningArtifactSummaries"
        ~of_json:ProvisioningArtifactSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module PortfolioShareDetails =
  struct
    type nonrec t = PortfolioShareDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PortfolioShareDetail.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PortfolioShareDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"PortfolioShareDetails"
        ~of_json:PortfolioShareDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module ShareDetails =
  struct
    type nonrec t =
      {
      successfulShares: SuccessfulShares.t option ;
      shareErrors: ShareErrors.t option }
    let make ?successfulShares =
      fun ?shareErrors -> fun () -> { successfulShares; shareErrors }
    let to_value x =
      structure_to_value
        [("SuccessfulShares",
           (Option.map x.successfulShares ~f:SuccessfulShares.to_value));
        ("ShareErrors", (Option.map x.shareErrors ~f:ShareErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shareErrors =
        (Option.map ~f:ShareErrors.of_xml) (Xml.child xml_arg0 "ShareErrors") in
      let successfulShares =
        (Option.map ~f:SuccessfulShares.of_xml)
          (Xml.child xml_arg0 "SuccessfulShares") in
      make ?shareErrors ?successfulShares ()
    let of_json json =
      let shareErrors = field_map json "ShareErrors" ShareErrors.of_json in
      let successfulShares =
        field_map json "SuccessfulShares" SuccessfulShares.of_json in
      make ?shareErrors ?successfulShares ()
    let to_json v = composed_to_json to_value v
  end
module CopyProductStatus =
  struct
    type nonrec t =
      | SUCCEEDED 
      | IN_PROGRESS 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SUCCEEDED -> "SUCCEEDED"
      | IN_PROGRESS -> "IN_PROGRESS"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "SUCCEEDED" -> SUCCEEDED
      | "IN_PROGRESS" -> IN_PROGRESS
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration CopyProductStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CopyProductStatus" j)
    let to_json = simple_to_json to_value
  end
module StatusDetail =
  struct
    type nonrec t = string
    let context_ = "StatusDetail"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StatusDetail" j
    let to_json = simple_to_json to_value
  end
module ProvisioningArtifactProperties =
  struct
    type nonrec t =
      {
      name: ProvisioningArtifactName.t option ;
      description: ProvisioningArtifactDescription.t option ;
      info: ProvisioningArtifactInfo.t ;
      type_: ProvisioningArtifactType.t option ;
      disableTemplateValidation: DisableTemplateValidation.t option }
    let context_ = "ProvisioningArtifactProperties"
    let make ?name =
      fun ?description ->
        fun ?type_ ->
          fun ?disableTemplateValidation ->
            fun ~info ->
              fun () ->
                { name; description; type_; disableTemplateValidation; info }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:ProvisioningArtifactName.to_value));
        ("Description",
          (Option.map x.description
             ~f:ProvisioningArtifactDescription.to_value));
        ("Info", (Some (ProvisioningArtifactInfo.to_value x.info)));
        ("Type", (Option.map x.type_ ~f:ProvisioningArtifactType.to_value));
        ("DisableTemplateValidation",
          (Option.map x.disableTemplateValidation
             ~f:DisableTemplateValidation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let disableTemplateValidation =
        (Option.map ~f:DisableTemplateValidation.of_xml)
          (Xml.child xml_arg0 "DisableTemplateValidation") in
      let type_ =
        (Option.map ~f:ProvisioningArtifactType.of_xml)
          (Xml.child xml_arg0 "Type") in
      let info =
        ProvisioningArtifactInfo.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Info") in
      let description =
        (Option.map ~f:ProvisioningArtifactDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        (Option.map ~f:ProvisioningArtifactName.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?disableTemplateValidation ?type_ ~info ?description ?name ()
    let of_json json =
      let disableTemplateValidation =
        field_map json "DisableTemplateValidation"
          DisableTemplateValidation.of_json in
      let type_ = field_map json "Type" ProvisioningArtifactType.of_json in
      let info = field_map_exn json "Info" ProvisioningArtifactInfo.of_json in
      let description =
        field_map json "Description" ProvisioningArtifactDescription.of_json in
      let name = field_map json "Name" ProvisioningArtifactName.of_json in
      make ?disableTemplateValidation ?type_ ~info ?description ?name ()
    let to_json v = composed_to_json to_value v
  end
module CopyOptions =
  struct
    type nonrec t = CopyOption.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CopyOption.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CopyOption.of_xml)
    let of_json j =
      list_of_json ~kind:"CopyOptions" ~of_json:CopyOption.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProductArn =
  struct
    type nonrec t = string
    let context_ = "ProductArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1224) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:[a-z0-9-\\.]{1,63}:[a-z0-9-\\.]{0,63}:[a-z0-9-\\.]{0,63}:[a-z0-9-\\.]{0,63}:[^/].{0,1023}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProductArn" j
    let to_json = simple_to_json to_value
  end
module SourceProvisioningArtifactProperties =
  struct
    type nonrec t = SourceProvisioningArtifactPropertiesMap.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SourceProvisioningArtifactPropertiesMap.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:SourceProvisioningArtifactPropertiesMap.of_xml)
    let of_json j =
      list_of_json ~kind:"SourceProvisioningArtifactProperties"
        ~of_json:SourceProvisioningArtifactPropertiesMap.of_json j
    let to_json v = composed_to_json to_value v
  end
module FailedServiceActionAssociations =
  struct
    type nonrec t = FailedServiceActionAssociation.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:50); i
    let to_value xs =
      (xs |> (List.map ~f:FailedServiceActionAssociation.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FailedServiceActionAssociation.of_xml)
    let of_json j =
      list_of_json ~kind:"FailedServiceActionAssociations"
        ~of_json:FailedServiceActionAssociation.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServiceActionAssociations =
  struct
    type nonrec t = ServiceActionAssociation.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:ServiceActionAssociation.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ServiceActionAssociation.of_xml)
    let of_json j =
      list_of_json ~kind:"ServiceActionAssociations"
        ~of_json:ServiceActionAssociation.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateTagOptionOutput =
  struct
    type nonrec t = {
      tagOptionDetail: TagOptionDetail.t option }
    type nonrec error =
      [ `DuplicateResourceException of DuplicateResourceException.t 
      | `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tagOptionDetail = fun () -> { tagOptionDetail }
    let error_of_json name json =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException
            (DuplicateResourceException.of_json json)
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException (DuplicateResourceException.of_xml xml)
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DuplicateResourceException e ->
          `Assoc
            [("error", (`String "DuplicateResourceException"));
            ("details", (DuplicateResourceException.to_json e))]
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TagOptionDetail",
           (Option.map x.tagOptionDetail ~f:TagOptionDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagOptionDetail =
        (Option.map ~f:TagOptionDetail.of_xml)
          (Xml.child xml_arg0 "TagOptionDetail") in
      make ?tagOptionDetail ()
    let of_json json =
      let tagOptionDetail =
        field_map json "TagOptionDetail" TagOptionDetail.of_json in
      make ?tagOptionDetail ()
    let to_json v = composed_to_json to_value v
  end
module UpdateTagOptionInput =
  struct
    type nonrec t =
      {
      id: TagOptionId.t ;
      value: TagOptionValue.t option ;
      active: TagOptionActive.t option }
    let context_ = "UpdateTagOptionInput"
    let make ?value =
      fun ?active -> fun ~id -> fun () -> { value; active; id }
    let to_value x =
      structure_to_value
        [("Id", (Some (TagOptionId.to_value x.id)));
        ("Value", (Option.map x.value ~f:TagOptionValue.to_value));
        ("Active", (Option.map x.active ~f:TagOptionActive.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let active =
        (Option.map ~f:TagOptionActive.of_xml) (Xml.child xml_arg0 "Active") in
      let value =
        (Option.map ~f:TagOptionValue.of_xml) (Xml.child xml_arg0 "Value") in
      let id =
        TagOptionId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?active ?value ~id ()
    let of_json json =
      let active = field_map json "Active" TagOptionActive.of_json in
      let value = field_map json "Value" TagOptionValue.of_json in
      let id = field_map_exn json "Id" TagOptionId.of_json in
      make ?active ?value ~id ()
    let to_json v = composed_to_json to_value v
  end
module UpdateServiceActionOutput =
  struct
    type nonrec t = {
      serviceActionDetail: ServiceActionDetail.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?serviceActionDetail = fun () -> { serviceActionDetail }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ServiceActionDetail",
           (Option.map x.serviceActionDetail ~f:ServiceActionDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceActionDetail =
        (Option.map ~f:ServiceActionDetail.of_xml)
          (Xml.child xml_arg0 "ServiceActionDetail") in
      make ?serviceActionDetail ()
    let of_json json =
      let serviceActionDetail =
        field_map json "ServiceActionDetail" ServiceActionDetail.of_json in
      make ?serviceActionDetail ()
    let to_json v = composed_to_json to_value v
  end
module UpdateServiceActionInput =
  struct
    type nonrec t =
      {
      id: Id.t ;
      name: ServiceActionName.t option ;
      definition: ServiceActionDefinitionMap.t option ;
      description: ServiceActionDescription.t option ;
      acceptLanguage: AcceptLanguage.t option }
    let context_ = "UpdateServiceActionInput"
    let make ?name =
      fun ?definition ->
        fun ?description ->
          fun ?acceptLanguage ->
            fun ~id ->
              fun () -> { name; definition; description; acceptLanguage; id }
    let to_value x =
      structure_to_value
        [("Id", (Some (Id.to_value x.id)));
        ("Name", (Option.map x.name ~f:ServiceActionName.to_value));
        ("Definition",
          (Option.map x.definition ~f:ServiceActionDefinitionMap.to_value));
        ("Description",
          (Option.map x.description ~f:ServiceActionDescription.to_value));
        ("AcceptLanguage",
          (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      let description =
        (Option.map ~f:ServiceActionDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let definition =
        (Option.map ~f:ServiceActionDefinitionMap.of_xml)
          (Xml.child xml_arg0 "Definition") in
      let name =
        (Option.map ~f:ServiceActionName.of_xml) (Xml.child xml_arg0 "Name") in
      let id = Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?acceptLanguage ?description ?definition ?name ~id ()
    let of_json json =
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      let description =
        field_map json "Description" ServiceActionDescription.of_json in
      let definition =
        field_map json "Definition" ServiceActionDefinitionMap.of_json in
      let name = field_map json "Name" ServiceActionName.of_json in
      let id = field_map_exn json "Id" Id.of_json in
      make ?acceptLanguage ?description ?definition ?name ~id ()
    let to_json v = composed_to_json to_value v
  end
module UpdateProvisioningArtifactOutput =
  struct
    type nonrec t =
      {
      provisioningArtifactDetail: ProvisioningArtifactDetail.t option ;
      info: ProvisioningArtifactInfo.t option ;
      status: Status.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?provisioningArtifactDetail =
      fun ?info ->
        fun ?status -> fun () -> { provisioningArtifactDetail; info; status }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProvisioningArtifactDetail",
           (Option.map x.provisioningArtifactDetail
              ~f:ProvisioningArtifactDetail.to_value));
        ("Info", (Option.map x.info ~f:ProvisioningArtifactInfo.to_value));
        ("Status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "Status") in
      let info =
        (Option.map ~f:ProvisioningArtifactInfo.of_xml)
          (Xml.child xml_arg0 "Info") in
      let provisioningArtifactDetail =
        (Option.map ~f:ProvisioningArtifactDetail.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactDetail") in
      make ?status ?info ?provisioningArtifactDetail ()
    let of_json json =
      let status = field_map json "Status" Status.of_json in
      let info = field_map json "Info" ProvisioningArtifactInfo.of_json in
      let provisioningArtifactDetail =
        field_map json "ProvisioningArtifactDetail"
          ProvisioningArtifactDetail.of_json in
      make ?status ?info ?provisioningArtifactDetail ()
    let to_json v = composed_to_json to_value v
  end
module UpdateProvisioningArtifactInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      productId: Id.t ;
      provisioningArtifactId: Id.t ;
      name: ProvisioningArtifactName.t option ;
      description: ProvisioningArtifactDescription.t option ;
      active: ProvisioningArtifactActive.t option ;
      guidance: ProvisioningArtifactGuidance.t option }
    let context_ = "UpdateProvisioningArtifactInput"
    let make ?acceptLanguage =
      fun ?name ->
        fun ?description ->
          fun ?active ->
            fun ?guidance ->
              fun ~productId ->
                fun ~provisioningArtifactId ->
                  fun () ->
                    {
                      acceptLanguage;
                      name;
                      description;
                      active;
                      guidance;
                      productId;
                      provisioningArtifactId
                    }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProductId", (Some (Id.to_value x.productId)));
        ("ProvisioningArtifactId",
          (Some (Id.to_value x.provisioningArtifactId)));
        ("Name", (Option.map x.name ~f:ProvisioningArtifactName.to_value));
        ("Description",
          (Option.map x.description
             ~f:ProvisioningArtifactDescription.to_value));
        ("Active",
          (Option.map x.active ~f:ProvisioningArtifactActive.to_value));
        ("Guidance",
          (Option.map x.guidance ~f:ProvisioningArtifactGuidance.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let guidance =
        (Option.map ~f:ProvisioningArtifactGuidance.of_xml)
          (Xml.child xml_arg0 "Guidance") in
      let active =
        (Option.map ~f:ProvisioningArtifactActive.of_xml)
          (Xml.child xml_arg0 "Active") in
      let description =
        (Option.map ~f:ProvisioningArtifactDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        (Option.map ~f:ProvisioningArtifactName.of_xml)
          (Xml.child xml_arg0 "Name") in
      let provisioningArtifactId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisioningArtifactId") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?guidance ?active ?description ?name ~provisioningArtifactId
        ~productId ?acceptLanguage ()
    let of_json json =
      let guidance =
        field_map json "Guidance" ProvisioningArtifactGuidance.of_json in
      let active = field_map json "Active" ProvisioningArtifactActive.of_json in
      let description =
        field_map json "Description" ProvisioningArtifactDescription.of_json in
      let name = field_map json "Name" ProvisioningArtifactName.of_json in
      let provisioningArtifactId =
        field_map_exn json "ProvisioningArtifactId" Id.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?guidance ?active ?description ?name ~provisioningArtifactId
        ~productId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module UpdateProvisionedProductPropertiesOutput =
  struct
    type nonrec t =
      {
      provisionedProductId: Id.t option ;
      provisionedProductProperties: ProvisionedProductProperties.t option ;
      recordId: Id.t option ;
      status: RecordStatus.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?provisionedProductId =
      fun ?provisionedProductProperties ->
        fun ?recordId ->
          fun ?status ->
            fun () ->
              {
                provisionedProductId;
                provisionedProductProperties;
                recordId;
                status
              }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProvisionedProductId",
           (Option.map x.provisionedProductId ~f:Id.to_value));
        ("ProvisionedProductProperties",
          (Option.map x.provisionedProductProperties
             ~f:ProvisionedProductProperties.to_value));
        ("RecordId", (Option.map x.recordId ~f:Id.to_value));
        ("Status", (Option.map x.status ~f:RecordStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:RecordStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let recordId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "RecordId") in
      let provisionedProductProperties =
        (Option.map ~f:ProvisionedProductProperties.of_xml)
          (Xml.child xml_arg0 "ProvisionedProductProperties") in
      let provisionedProductId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProvisionedProductId") in
      make ?status ?recordId ?provisionedProductProperties
        ?provisionedProductId ()
    let of_json json =
      let status = field_map json "Status" RecordStatus.of_json in
      let recordId = field_map json "RecordId" Id.of_json in
      let provisionedProductProperties =
        field_map json "ProvisionedProductProperties"
          ProvisionedProductProperties.of_json in
      let provisionedProductId =
        field_map json "ProvisionedProductId" Id.of_json in
      make ?status ?recordId ?provisionedProductProperties
        ?provisionedProductId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateProvisionedProductPropertiesInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      provisionedProductId: Id.t ;
      provisionedProductProperties: ProvisionedProductProperties.t ;
      idempotencyToken: IdempotencyToken.t }
    let context_ = "UpdateProvisionedProductPropertiesInput"
    let make ?acceptLanguage =
      fun ~provisionedProductId ->
        fun ~provisionedProductProperties ->
          fun ~idempotencyToken ->
            fun () ->
              {
                acceptLanguage;
                provisionedProductId;
                provisionedProductProperties;
                idempotencyToken
              }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProvisionedProductId", (Some (Id.to_value x.provisionedProductId)));
        ("ProvisionedProductProperties",
          (Some
             (ProvisionedProductProperties.to_value
                x.provisionedProductProperties)));
        ("IdempotencyToken",
          (Some (IdempotencyToken.to_value x.idempotencyToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdempotencyToken") in
      let provisionedProductProperties =
        ProvisionedProductProperties.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ProvisionedProductProperties") in
      let provisionedProductId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisionedProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~idempotencyToken ~provisionedProductProperties
        ~provisionedProductId ?acceptLanguage ()
    let of_json json =
      let idempotencyToken =
        field_map_exn json "IdempotencyToken" IdempotencyToken.of_json in
      let provisionedProductProperties =
        field_map_exn json "ProvisionedProductProperties"
          ProvisionedProductProperties.of_json in
      let provisionedProductId =
        field_map_exn json "ProvisionedProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~idempotencyToken ~provisionedProductProperties
        ~provisionedProductId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module UpdateProvisionedProductOutput =
  struct
    type nonrec t = {
      recordDetail: RecordDetail.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recordDetail = fun () -> { recordDetail }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RecordDetail",
           (Option.map x.recordDetail ~f:RecordDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recordDetail =
        (Option.map ~f:RecordDetail.of_xml)
          (Xml.child xml_arg0 "RecordDetail") in
      make ?recordDetail ()
    let of_json json =
      let recordDetail = field_map json "RecordDetail" RecordDetail.of_json in
      make ?recordDetail ()
    let to_json v = composed_to_json to_value v
  end
module UpdateProvisionedProductInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      provisionedProductName: ProvisionedProductNameOrArn.t option ;
      provisionedProductId: Id.t option ;
      productId: Id.t option ;
      productName: ProductViewName.t option ;
      provisioningArtifactId: Id.t option ;
      provisioningArtifactName: ProvisioningArtifactName.t option ;
      pathId: Id.t option ;
      pathName: PortfolioDisplayName.t option ;
      provisioningParameters: UpdateProvisioningParameters.t option ;
      provisioningPreferences: UpdateProvisioningPreferences.t option ;
      tags: Tags.t option ;
      updateToken: IdempotencyToken.t }
    let context_ = "UpdateProvisionedProductInput"
    let make ?acceptLanguage =
      fun ?provisionedProductName ->
        fun ?provisionedProductId ->
          fun ?productId ->
            fun ?productName ->
              fun ?provisioningArtifactId ->
                fun ?provisioningArtifactName ->
                  fun ?pathId ->
                    fun ?pathName ->
                      fun ?provisioningParameters ->
                        fun ?provisioningPreferences ->
                          fun ?tags ->
                            fun ~updateToken ->
                              fun () ->
                                {
                                  acceptLanguage;
                                  provisionedProductName;
                                  provisionedProductId;
                                  productId;
                                  productName;
                                  provisioningArtifactId;
                                  provisioningArtifactName;
                                  pathId;
                                  pathName;
                                  provisioningParameters;
                                  provisioningPreferences;
                                  tags;
                                  updateToken
                                }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProvisionedProductName",
          (Option.map x.provisionedProductName
             ~f:ProvisionedProductNameOrArn.to_value));
        ("ProvisionedProductId",
          (Option.map x.provisionedProductId ~f:Id.to_value));
        ("ProductId", (Option.map x.productId ~f:Id.to_value));
        ("ProductName",
          (Option.map x.productName ~f:ProductViewName.to_value));
        ("ProvisioningArtifactId",
          (Option.map x.provisioningArtifactId ~f:Id.to_value));
        ("ProvisioningArtifactName",
          (Option.map x.provisioningArtifactName
             ~f:ProvisioningArtifactName.to_value));
        ("PathId", (Option.map x.pathId ~f:Id.to_value));
        ("PathName",
          (Option.map x.pathName ~f:PortfolioDisplayName.to_value));
        ("ProvisioningParameters",
          (Option.map x.provisioningParameters
             ~f:UpdateProvisioningParameters.to_value));
        ("ProvisioningPreferences",
          (Option.map x.provisioningPreferences
             ~f:UpdateProvisioningPreferences.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("UpdateToken", (Some (IdempotencyToken.to_value x.updateToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updateToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "UpdateToken") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let provisioningPreferences =
        (Option.map ~f:UpdateProvisioningPreferences.of_xml)
          (Xml.child xml_arg0 "ProvisioningPreferences") in
      let provisioningParameters =
        (Option.map ~f:UpdateProvisioningParameters.of_xml)
          (Xml.child xml_arg0 "ProvisioningParameters") in
      let pathName =
        (Option.map ~f:PortfolioDisplayName.of_xml)
          (Xml.child xml_arg0 "PathName") in
      let pathId = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PathId") in
      let provisioningArtifactName =
        (Option.map ~f:ProvisioningArtifactName.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactName") in
      let provisioningArtifactId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactId") in
      let productName =
        (Option.map ~f:ProductViewName.of_xml)
          (Xml.child xml_arg0 "ProductName") in
      let productId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProductId") in
      let provisionedProductId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProvisionedProductId") in
      let provisionedProductName =
        (Option.map ~f:ProvisionedProductNameOrArn.of_xml)
          (Xml.child xml_arg0 "ProvisionedProductName") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~updateToken ?tags ?provisioningPreferences
        ?provisioningParameters ?pathName ?pathId ?provisioningArtifactName
        ?provisioningArtifactId ?productName ?productId ?provisionedProductId
        ?provisionedProductName ?acceptLanguage ()
    let of_json json =
      let updateToken =
        field_map_exn json "UpdateToken" IdempotencyToken.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let provisioningPreferences =
        field_map json "ProvisioningPreferences"
          UpdateProvisioningPreferences.of_json in
      let provisioningParameters =
        field_map json "ProvisioningParameters"
          UpdateProvisioningParameters.of_json in
      let pathName = field_map json "PathName" PortfolioDisplayName.of_json in
      let pathId = field_map json "PathId" Id.of_json in
      let provisioningArtifactName =
        field_map json "ProvisioningArtifactName"
          ProvisioningArtifactName.of_json in
      let provisioningArtifactId =
        field_map json "ProvisioningArtifactId" Id.of_json in
      let productName = field_map json "ProductName" ProductViewName.of_json in
      let productId = field_map json "ProductId" Id.of_json in
      let provisionedProductId =
        field_map json "ProvisionedProductId" Id.of_json in
      let provisionedProductName =
        field_map json "ProvisionedProductName"
          ProvisionedProductNameOrArn.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~updateToken ?tags ?provisioningPreferences
        ?provisioningParameters ?pathName ?pathId ?provisioningArtifactName
        ?provisioningArtifactId ?productName ?productId ?provisionedProductId
        ?provisionedProductName ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module UpdateProductOutput =
  struct
    type nonrec t =
      {
      productViewDetail: ProductViewDetail.t option ;
      tags: Tags.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?productViewDetail =
      fun ?tags -> fun () -> { productViewDetail; tags }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProductViewDetail",
           (Option.map x.productViewDetail ~f:ProductViewDetail.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let productViewDetail =
        (Option.map ~f:ProductViewDetail.of_xml)
          (Xml.child xml_arg0 "ProductViewDetail") in
      make ?tags ?productViewDetail ()
    let of_json json =
      let tags = field_map json "Tags" Tags.of_json in
      let productViewDetail =
        field_map json "ProductViewDetail" ProductViewDetail.of_json in
      make ?tags ?productViewDetail ()
    let to_json v = composed_to_json to_value v
  end
module UpdateProductInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      id: Id.t ;
      name: ProductViewName.t option ;
      owner: ProductViewOwner.t option ;
      description: ProductViewShortDescription.t option ;
      distributor: ProductViewOwner.t option ;
      supportDescription: SupportDescription.t option ;
      supportEmail: SupportEmail.t option ;
      supportUrl: SupportUrl.t option ;
      addTags: AddTags.t option ;
      removeTags: TagKeys.t option }
    let context_ = "UpdateProductInput"
    let make ?acceptLanguage =
      fun ?name ->
        fun ?owner ->
          fun ?description ->
            fun ?distributor ->
              fun ?supportDescription ->
                fun ?supportEmail ->
                  fun ?supportUrl ->
                    fun ?addTags ->
                      fun ?removeTags ->
                        fun ~id ->
                          fun () ->
                            {
                              acceptLanguage;
                              name;
                              owner;
                              description;
                              distributor;
                              supportDescription;
                              supportEmail;
                              supportUrl;
                              addTags;
                              removeTags;
                              id
                            }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Id", (Some (Id.to_value x.id)));
        ("Name", (Option.map x.name ~f:ProductViewName.to_value));
        ("Owner", (Option.map x.owner ~f:ProductViewOwner.to_value));
        ("Description",
          (Option.map x.description ~f:ProductViewShortDescription.to_value));
        ("Distributor",
          (Option.map x.distributor ~f:ProductViewOwner.to_value));
        ("SupportDescription",
          (Option.map x.supportDescription ~f:SupportDescription.to_value));
        ("SupportEmail",
          (Option.map x.supportEmail ~f:SupportEmail.to_value));
        ("SupportUrl", (Option.map x.supportUrl ~f:SupportUrl.to_value));
        ("AddTags", (Option.map x.addTags ~f:AddTags.to_value));
        ("RemoveTags", (Option.map x.removeTags ~f:TagKeys.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let removeTags =
        (Option.map ~f:TagKeys.of_xml) (Xml.child xml_arg0 "RemoveTags") in
      let addTags =
        (Option.map ~f:AddTags.of_xml) (Xml.child xml_arg0 "AddTags") in
      let supportUrl =
        (Option.map ~f:SupportUrl.of_xml) (Xml.child xml_arg0 "SupportUrl") in
      let supportEmail =
        (Option.map ~f:SupportEmail.of_xml)
          (Xml.child xml_arg0 "SupportEmail") in
      let supportDescription =
        (Option.map ~f:SupportDescription.of_xml)
          (Xml.child xml_arg0 "SupportDescription") in
      let distributor =
        (Option.map ~f:ProductViewOwner.of_xml)
          (Xml.child xml_arg0 "Distributor") in
      let description =
        (Option.map ~f:ProductViewShortDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let owner =
        (Option.map ~f:ProductViewOwner.of_xml) (Xml.child xml_arg0 "Owner") in
      let name =
        (Option.map ~f:ProductViewName.of_xml) (Xml.child xml_arg0 "Name") in
      let id = Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?removeTags ?addTags ?supportUrl ?supportEmail ?supportDescription
        ?distributor ?description ?owner ?name ~id ?acceptLanguage ()
    let of_json json =
      let removeTags = field_map json "RemoveTags" TagKeys.of_json in
      let addTags = field_map json "AddTags" AddTags.of_json in
      let supportUrl = field_map json "SupportUrl" SupportUrl.of_json in
      let supportEmail = field_map json "SupportEmail" SupportEmail.of_json in
      let supportDescription =
        field_map json "SupportDescription" SupportDescription.of_json in
      let distributor = field_map json "Distributor" ProductViewOwner.of_json in
      let description =
        field_map json "Description" ProductViewShortDescription.of_json in
      let owner = field_map json "Owner" ProductViewOwner.of_json in
      let name = field_map json "Name" ProductViewName.of_json in
      let id = field_map_exn json "Id" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?removeTags ?addTags ?supportUrl ?supportEmail ?supportDescription
        ?distributor ?description ?owner ?name ~id ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module UpdatePortfolioShareOutput =
  struct
    type nonrec t =
      {
      portfolioShareToken: Id.t option ;
      status: ShareStatus.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `InvalidStateException of InvalidStateException.t 
      | `OperationNotSupportedException of OperationNotSupportedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?portfolioShareToken =
      fun ?status -> fun () -> { portfolioShareToken; status }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `OperationNotSupportedException e ->
          `Assoc
            [("error", (`String "OperationNotSupportedException"));
            ("details", (OperationNotSupportedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PortfolioShareToken",
           (Option.map x.portfolioShareToken ~f:Id.to_value));
        ("Status", (Option.map x.status ~f:ShareStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ShareStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let portfolioShareToken =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PortfolioShareToken") in
      make ?status ?portfolioShareToken ()
    let of_json json =
      let status = field_map json "Status" ShareStatus.of_json in
      let portfolioShareToken =
        field_map json "PortfolioShareToken" Id.of_json in
      make ?status ?portfolioShareToken ()
    let to_json v = composed_to_json to_value v
  end
module UpdatePortfolioShareInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      portfolioId: Id.t ;
      accountId: AccountId.t option ;
      organizationNode: OrganizationNode.t option ;
      shareTagOptions: NullableBoolean.t option }
    let context_ = "UpdatePortfolioShareInput"
    let make ?acceptLanguage =
      fun ?accountId ->
        fun ?organizationNode ->
          fun ?shareTagOptions ->
            fun ~portfolioId ->
              fun () ->
                {
                  acceptLanguage;
                  accountId;
                  organizationNode;
                  shareTagOptions;
                  portfolioId
                }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("AccountId", (Option.map x.accountId ~f:AccountId.to_value));
        ("OrganizationNode",
          (Option.map x.organizationNode ~f:OrganizationNode.to_value));
        ("ShareTagOptions",
          (Option.map x.shareTagOptions ~f:NullableBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shareTagOptions =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "ShareTagOptions") in
      let organizationNode =
        (Option.map ~f:OrganizationNode.of_xml)
          (Xml.child xml_arg0 "OrganizationNode") in
      let accountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "AccountId") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?shareTagOptions ?organizationNode ?accountId ~portfolioId
        ?acceptLanguage ()
    let of_json json =
      let shareTagOptions =
        field_map json "ShareTagOptions" NullableBoolean.of_json in
      let organizationNode =
        field_map json "OrganizationNode" OrganizationNode.of_json in
      let accountId = field_map json "AccountId" AccountId.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?shareTagOptions ?organizationNode ?accountId ~portfolioId
        ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module UpdatePortfolioOutput =
  struct
    type nonrec t =
      {
      portfolioDetail: PortfolioDetail.t option ;
      tags: Tags.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?portfolioDetail =
      fun ?tags -> fun () -> { portfolioDetail; tags }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PortfolioDetail",
           (Option.map x.portfolioDetail ~f:PortfolioDetail.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let portfolioDetail =
        (Option.map ~f:PortfolioDetail.of_xml)
          (Xml.child xml_arg0 "PortfolioDetail") in
      make ?tags ?portfolioDetail ()
    let of_json json =
      let tags = field_map json "Tags" Tags.of_json in
      let portfolioDetail =
        field_map json "PortfolioDetail" PortfolioDetail.of_json in
      make ?tags ?portfolioDetail ()
    let to_json v = composed_to_json to_value v
  end
module UpdatePortfolioInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      id: Id.t ;
      displayName: PortfolioDisplayName.t option ;
      description: PortfolioDescription.t option ;
      providerName: ProviderName.t option ;
      addTags: AddTags.t option ;
      removeTags: TagKeys.t option }
    let context_ = "UpdatePortfolioInput"
    let make ?acceptLanguage =
      fun ?displayName ->
        fun ?description ->
          fun ?providerName ->
            fun ?addTags ->
              fun ?removeTags ->
                fun ~id ->
                  fun () ->
                    {
                      acceptLanguage;
                      displayName;
                      description;
                      providerName;
                      addTags;
                      removeTags;
                      id
                    }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Id", (Some (Id.to_value x.id)));
        ("DisplayName",
          (Option.map x.displayName ~f:PortfolioDisplayName.to_value));
        ("Description",
          (Option.map x.description ~f:PortfolioDescription.to_value));
        ("ProviderName",
          (Option.map x.providerName ~f:ProviderName.to_value));
        ("AddTags", (Option.map x.addTags ~f:AddTags.to_value));
        ("RemoveTags", (Option.map x.removeTags ~f:TagKeys.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let removeTags =
        (Option.map ~f:TagKeys.of_xml) (Xml.child xml_arg0 "RemoveTags") in
      let addTags =
        (Option.map ~f:AddTags.of_xml) (Xml.child xml_arg0 "AddTags") in
      let providerName =
        (Option.map ~f:ProviderName.of_xml)
          (Xml.child xml_arg0 "ProviderName") in
      let description =
        (Option.map ~f:PortfolioDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let displayName =
        (Option.map ~f:PortfolioDisplayName.of_xml)
          (Xml.child xml_arg0 "DisplayName") in
      let id = Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?removeTags ?addTags ?providerName ?description ?displayName ~id
        ?acceptLanguage ()
    let of_json json =
      let removeTags = field_map json "RemoveTags" TagKeys.of_json in
      let addTags = field_map json "AddTags" AddTags.of_json in
      let providerName = field_map json "ProviderName" ProviderName.of_json in
      let description =
        field_map json "Description" PortfolioDescription.of_json in
      let displayName =
        field_map json "DisplayName" PortfolioDisplayName.of_json in
      let id = field_map_exn json "Id" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?removeTags ?addTags ?providerName ?description ?displayName ~id
        ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module UpdateConstraintOutput =
  struct
    type nonrec t =
      {
      constraintDetail: ConstraintDetail.t option ;
      constraintParameters: ConstraintParameters.t option ;
      status: Status.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?constraintDetail =
      fun ?constraintParameters ->
        fun ?status ->
          fun () -> { constraintDetail; constraintParameters; status }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ConstraintDetail",
           (Option.map x.constraintDetail ~f:ConstraintDetail.to_value));
        ("ConstraintParameters",
          (Option.map x.constraintParameters ~f:ConstraintParameters.to_value));
        ("Status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "Status") in
      let constraintParameters =
        (Option.map ~f:ConstraintParameters.of_xml)
          (Xml.child xml_arg0 "ConstraintParameters") in
      let constraintDetail =
        (Option.map ~f:ConstraintDetail.of_xml)
          (Xml.child xml_arg0 "ConstraintDetail") in
      make ?status ?constraintParameters ?constraintDetail ()
    let of_json json =
      let status = field_map json "Status" Status.of_json in
      let constraintParameters =
        field_map json "ConstraintParameters" ConstraintParameters.of_json in
      let constraintDetail =
        field_map json "ConstraintDetail" ConstraintDetail.of_json in
      make ?status ?constraintParameters ?constraintDetail ()
    let to_json v = composed_to_json to_value v
  end
module UpdateConstraintInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      id: Id.t ;
      description: ConstraintDescription.t option ;
      parameters: ConstraintParameters.t option }
    let context_ = "UpdateConstraintInput"
    let make ?acceptLanguage =
      fun ?description ->
        fun ?parameters ->
          fun ~id ->
            fun () -> { acceptLanguage; description; parameters; id }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Id", (Some (Id.to_value x.id)));
        ("Description",
          (Option.map x.description ~f:ConstraintDescription.to_value));
        ("Parameters",
          (Option.map x.parameters ~f:ConstraintParameters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameters =
        (Option.map ~f:ConstraintParameters.of_xml)
          (Xml.child xml_arg0 "Parameters") in
      let description =
        (Option.map ~f:ConstraintDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let id = Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?parameters ?description ~id ?acceptLanguage ()
    let of_json json =
      let parameters =
        field_map json "Parameters" ConstraintParameters.of_json in
      let description =
        field_map json "Description" ConstraintDescription.of_json in
      let id = field_map_exn json "Id" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?parameters ?description ~id ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module TerminateProvisionedProductOutput =
  struct
    type nonrec t = {
      recordDetail: RecordDetail.t option }
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recordDetail = fun () -> { recordDetail }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RecordDetail",
           (Option.map x.recordDetail ~f:RecordDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recordDetail =
        (Option.map ~f:RecordDetail.of_xml)
          (Xml.child xml_arg0 "RecordDetail") in
      make ?recordDetail ()
    let of_json json =
      let recordDetail = field_map json "RecordDetail" RecordDetail.of_json in
      make ?recordDetail ()
    let to_json v = composed_to_json to_value v
  end
module TerminateProvisionedProductInput =
  struct
    type nonrec t =
      {
      provisionedProductName: ProvisionedProductNameOrArn.t option ;
      provisionedProductId: Id.t option ;
      terminateToken: IdempotencyToken.t ;
      ignoreErrors: IgnoreErrors.t option ;
      acceptLanguage: AcceptLanguage.t option ;
      retainPhysicalResources: RetainPhysicalResources.t option }
    let context_ = "TerminateProvisionedProductInput"
    let make ?provisionedProductName =
      fun ?provisionedProductId ->
        fun ?ignoreErrors ->
          fun ?acceptLanguage ->
            fun ?retainPhysicalResources ->
              fun ~terminateToken ->
                fun () ->
                  {
                    provisionedProductName;
                    provisionedProductId;
                    ignoreErrors;
                    acceptLanguage;
                    retainPhysicalResources;
                    terminateToken
                  }
    let to_value x =
      structure_to_value
        [("ProvisionedProductName",
           (Option.map x.provisionedProductName
              ~f:ProvisionedProductNameOrArn.to_value));
        ("ProvisionedProductId",
          (Option.map x.provisionedProductId ~f:Id.to_value));
        ("TerminateToken",
          (Some (IdempotencyToken.to_value x.terminateToken)));
        ("IgnoreErrors",
          (Option.map x.ignoreErrors ~f:IgnoreErrors.to_value));
        ("AcceptLanguage",
          (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("RetainPhysicalResources",
          (Option.map x.retainPhysicalResources
             ~f:RetainPhysicalResources.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retainPhysicalResources =
        (Option.map ~f:RetainPhysicalResources.of_xml)
          (Xml.child xml_arg0 "RetainPhysicalResources") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      let ignoreErrors =
        (Option.map ~f:IgnoreErrors.of_xml)
          (Xml.child xml_arg0 "IgnoreErrors") in
      let terminateToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TerminateToken") in
      let provisionedProductId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProvisionedProductId") in
      let provisionedProductName =
        (Option.map ~f:ProvisionedProductNameOrArn.of_xml)
          (Xml.child xml_arg0 "ProvisionedProductName") in
      make ?retainPhysicalResources ?acceptLanguage ?ignoreErrors
        ~terminateToken ?provisionedProductId ?provisionedProductName ()
    let of_json json =
      let retainPhysicalResources =
        field_map json "RetainPhysicalResources"
          RetainPhysicalResources.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      let ignoreErrors = field_map json "IgnoreErrors" IgnoreErrors.of_json in
      let terminateToken =
        field_map_exn json "TerminateToken" IdempotencyToken.of_json in
      let provisionedProductId =
        field_map json "ProvisionedProductId" Id.of_json in
      let provisionedProductName =
        field_map json "ProvisionedProductName"
          ProvisionedProductNameOrArn.of_json in
      make ?retainPhysicalResources ?acceptLanguage ?ignoreErrors
        ~terminateToken ?provisionedProductId ?provisionedProductName ()
    let to_json v = composed_to_json to_value v
  end
module SearchProvisionedProductsOutput =
  struct
    type nonrec t =
      {
      provisionedProducts: ProvisionedProductAttributes.t option ;
      totalResultsCount: TotalResultsCount.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?provisionedProducts =
      fun ?totalResultsCount ->
        fun ?nextPageToken ->
          fun () -> { provisionedProducts; totalResultsCount; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProvisionedProducts",
           (Option.map x.provisionedProducts
              ~f:ProvisionedProductAttributes.to_value));
        ("TotalResultsCount",
          (Option.map x.totalResultsCount ~f:TotalResultsCount.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let totalResultsCount =
        (Option.map ~f:TotalResultsCount.of_xml)
          (Xml.child xml_arg0 "TotalResultsCount") in
      let provisionedProducts =
        (Option.map ~f:ProvisionedProductAttributes.of_xml)
          (Xml.child xml_arg0 "ProvisionedProducts") in
      make ?nextPageToken ?totalResultsCount ?provisionedProducts ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let totalResultsCount =
        field_map json "TotalResultsCount" TotalResultsCount.of_json in
      let provisionedProducts =
        field_map json "ProvisionedProducts"
          ProvisionedProductAttributes.of_json in
      make ?nextPageToken ?totalResultsCount ?provisionedProducts ()
    let to_json v = composed_to_json to_value v
  end
module SearchProvisionedProductsInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      accessLevelFilter: AccessLevelFilter.t option ;
      filters: ProvisionedProductFilters.t option ;
      sortBy: SortField.t option ;
      sortOrder: SortOrder.t option ;
      pageSize: SearchProvisionedProductsPageSize.t option ;
      pageToken: PageToken.t option }
    let make ?acceptLanguage =
      fun ?accessLevelFilter ->
        fun ?filters ->
          fun ?sortBy ->
            fun ?sortOrder ->
              fun ?pageSize ->
                fun ?pageToken ->
                  fun () ->
                    {
                      acceptLanguage;
                      accessLevelFilter;
                      filters;
                      sortBy;
                      sortOrder;
                      pageSize;
                      pageToken
                    }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("AccessLevelFilter",
          (Option.map x.accessLevelFilter ~f:AccessLevelFilter.to_value));
        ("Filters",
          (Option.map x.filters ~f:ProvisionedProductFilters.to_value));
        ("SortBy", (Option.map x.sortBy ~f:SortField.to_value));
        ("SortOrder", (Option.map x.sortOrder ~f:SortOrder.to_value));
        ("PageSize",
          (Option.map x.pageSize
             ~f:SearchProvisionedProductsPageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:SearchProvisionedProductsPageSize.of_xml)
          (Xml.child xml_arg0 "PageSize") in
      let sortOrder =
        (Option.map ~f:SortOrder.of_xml) (Xml.child xml_arg0 "SortOrder") in
      let sortBy =
        (Option.map ~f:SortField.of_xml) (Xml.child xml_arg0 "SortBy") in
      let filters =
        (Option.map ~f:ProvisionedProductFilters.of_xml)
          (Xml.child xml_arg0 "Filters") in
      let accessLevelFilter =
        (Option.map ~f:AccessLevelFilter.of_xml)
          (Xml.child xml_arg0 "AccessLevelFilter") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageToken ?pageSize ?sortOrder ?sortBy ?filters
        ?accessLevelFilter ?acceptLanguage ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize =
        field_map json "PageSize" SearchProvisionedProductsPageSize.of_json in
      let sortOrder = field_map json "SortOrder" SortOrder.of_json in
      let sortBy = field_map json "SortBy" SortField.of_json in
      let filters =
        field_map json "Filters" ProvisionedProductFilters.of_json in
      let accessLevelFilter =
        field_map json "AccessLevelFilter" AccessLevelFilter.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageToken ?pageSize ?sortOrder ?sortBy ?filters
        ?accessLevelFilter ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module SearchProductsOutput =
  struct
    type nonrec t =
      {
      productViewSummaries: ProductViewSummaries.t option ;
      productViewAggregations: ProductViewAggregations.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?productViewSummaries =
      fun ?productViewAggregations ->
        fun ?nextPageToken ->
          fun () ->
            { productViewSummaries; productViewAggregations; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProductViewSummaries",
           (Option.map x.productViewSummaries
              ~f:ProductViewSummaries.to_value));
        ("ProductViewAggregations",
          (Option.map x.productViewAggregations
             ~f:ProductViewAggregations.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let productViewAggregations =
        (Option.map ~f:ProductViewAggregations.of_xml)
          (Xml.child xml_arg0 "ProductViewAggregations") in
      let productViewSummaries =
        (Option.map ~f:ProductViewSummaries.of_xml)
          (Xml.child xml_arg0 "ProductViewSummaries") in
      make ?nextPageToken ?productViewAggregations ?productViewSummaries ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let productViewAggregations =
        field_map json "ProductViewAggregations"
          ProductViewAggregations.of_json in
      let productViewSummaries =
        field_map json "ProductViewSummaries" ProductViewSummaries.of_json in
      make ?nextPageToken ?productViewAggregations ?productViewSummaries ()
    let to_json v = composed_to_json to_value v
  end
module SearchProductsInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      filters: ProductViewFilters.t option ;
      pageSize: PageSizeMax100.t option ;
      sortBy: ProductViewSortBy.t option ;
      sortOrder: SortOrder.t option ;
      pageToken: PageToken.t option }
    let make ?acceptLanguage =
      fun ?filters ->
        fun ?pageSize ->
          fun ?sortBy ->
            fun ?sortOrder ->
              fun ?pageToken ->
                fun () ->
                  {
                    acceptLanguage;
                    filters;
                    pageSize;
                    sortBy;
                    sortOrder;
                    pageToken
                  }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Filters", (Option.map x.filters ~f:ProductViewFilters.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSizeMax100.to_value));
        ("SortBy", (Option.map x.sortBy ~f:ProductViewSortBy.to_value));
        ("SortOrder", (Option.map x.sortOrder ~f:SortOrder.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let sortOrder =
        (Option.map ~f:SortOrder.of_xml) (Xml.child xml_arg0 "SortOrder") in
      let sortBy =
        (Option.map ~f:ProductViewSortBy.of_xml)
          (Xml.child xml_arg0 "SortBy") in
      let pageSize =
        (Option.map ~f:PageSizeMax100.of_xml) (Xml.child xml_arg0 "PageSize") in
      let filters =
        (Option.map ~f:ProductViewFilters.of_xml)
          (Xml.child xml_arg0 "Filters") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageToken ?sortOrder ?sortBy ?pageSize ?filters ?acceptLanguage
        ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let sortOrder = field_map json "SortOrder" SortOrder.of_json in
      let sortBy = field_map json "SortBy" ProductViewSortBy.of_json in
      let pageSize = field_map json "PageSize" PageSizeMax100.of_json in
      let filters = field_map json "Filters" ProductViewFilters.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageToken ?sortOrder ?sortBy ?pageSize ?filters ?acceptLanguage
        ()
    let to_json v = composed_to_json to_value v
  end
module SearchProductsAsAdminOutput =
  struct
    type nonrec t =
      {
      productViewDetails: ProductViewDetails.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?productViewDetails =
      fun ?nextPageToken -> fun () -> { productViewDetails; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProductViewDetails",
           (Option.map x.productViewDetails ~f:ProductViewDetails.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let productViewDetails =
        (Option.map ~f:ProductViewDetails.of_xml)
          (Xml.child xml_arg0 "ProductViewDetails") in
      make ?nextPageToken ?productViewDetails ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let productViewDetails =
        field_map json "ProductViewDetails" ProductViewDetails.of_json in
      make ?nextPageToken ?productViewDetails ()
    let to_json v = composed_to_json to_value v
  end
module SearchProductsAsAdminInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      portfolioId: Id.t option ;
      filters: ProductViewFilters.t option ;
      sortBy: ProductViewSortBy.t option ;
      sortOrder: SortOrder.t option ;
      pageToken: PageToken.t option ;
      pageSize: PageSize.t option ;
      productSource: ProductSource.t option }
    let make ?acceptLanguage =
      fun ?portfolioId ->
        fun ?filters ->
          fun ?sortBy ->
            fun ?sortOrder ->
              fun ?pageToken ->
                fun ?pageSize ->
                  fun ?productSource ->
                    fun () ->
                      {
                        acceptLanguage;
                        portfolioId;
                        filters;
                        sortBy;
                        sortOrder;
                        pageToken;
                        pageSize;
                        productSource
                      }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PortfolioId", (Option.map x.portfolioId ~f:Id.to_value));
        ("Filters", (Option.map x.filters ~f:ProductViewFilters.to_value));
        ("SortBy", (Option.map x.sortBy ~f:ProductViewSortBy.to_value));
        ("SortOrder", (Option.map x.sortOrder ~f:SortOrder.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("ProductSource",
          (Option.map x.productSource ~f:ProductSource.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let productSource =
        (Option.map ~f:ProductSource.of_xml)
          (Xml.child xml_arg0 "ProductSource") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let sortOrder =
        (Option.map ~f:SortOrder.of_xml) (Xml.child xml_arg0 "SortOrder") in
      let sortBy =
        (Option.map ~f:ProductViewSortBy.of_xml)
          (Xml.child xml_arg0 "SortBy") in
      let filters =
        (Option.map ~f:ProductViewFilters.of_xml)
          (Xml.child xml_arg0 "Filters") in
      let portfolioId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PortfolioId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?productSource ?pageSize ?pageToken ?sortOrder ?sortBy ?filters
        ?portfolioId ?acceptLanguage ()
    let of_json json =
      let productSource =
        field_map json "ProductSource" ProductSource.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let sortOrder = field_map json "SortOrder" SortOrder.of_json in
      let sortBy = field_map json "SortBy" ProductViewSortBy.of_json in
      let filters = field_map json "Filters" ProductViewFilters.of_json in
      let portfolioId = field_map json "PortfolioId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?productSource ?pageSize ?pageToken ?sortOrder ?sortBy ?filters
        ?portfolioId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ScanProvisionedProductsOutput =
  struct
    type nonrec t =
      {
      provisionedProducts: ProvisionedProductDetails.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?provisionedProducts =
      fun ?nextPageToken -> fun () -> { provisionedProducts; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProvisionedProducts",
           (Option.map x.provisionedProducts
              ~f:ProvisionedProductDetails.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let provisionedProducts =
        (Option.map ~f:ProvisionedProductDetails.of_xml)
          (Xml.child xml_arg0 "ProvisionedProducts") in
      make ?nextPageToken ?provisionedProducts ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let provisionedProducts =
        field_map json "ProvisionedProducts"
          ProvisionedProductDetails.of_json in
      make ?nextPageToken ?provisionedProducts ()
    let to_json v = composed_to_json to_value v
  end
module ScanProvisionedProductsInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      accessLevelFilter: AccessLevelFilter.t option ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option }
    let make ?acceptLanguage =
      fun ?accessLevelFilter ->
        fun ?pageSize ->
          fun ?pageToken ->
            fun () ->
              { acceptLanguage; accessLevelFilter; pageSize; pageToken }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("AccessLevelFilter",
          (Option.map x.accessLevelFilter ~f:AccessLevelFilter.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let accessLevelFilter =
        (Option.map ~f:AccessLevelFilter.of_xml)
          (Xml.child xml_arg0 "AccessLevelFilter") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageToken ?pageSize ?accessLevelFilter ?acceptLanguage ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let accessLevelFilter =
        field_map json "AccessLevelFilter" AccessLevelFilter.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageToken ?pageSize ?accessLevelFilter ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module RejectPortfolioShareOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module RejectPortfolioShareInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      portfolioId: Id.t ;
      portfolioShareType: PortfolioShareType.t option }
    let context_ = "RejectPortfolioShareInput"
    let make ?acceptLanguage =
      fun ?portfolioShareType ->
        fun ~portfolioId ->
          fun () -> { acceptLanguage; portfolioShareType; portfolioId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("PortfolioShareType",
          (Option.map x.portfolioShareType ~f:PortfolioShareType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let portfolioShareType =
        (Option.map ~f:PortfolioShareType.of_xml)
          (Xml.child xml_arg0 "PortfolioShareType") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?portfolioShareType ~portfolioId ?acceptLanguage ()
    let of_json json =
      let portfolioShareType =
        field_map json "PortfolioShareType" PortfolioShareType.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?portfolioShareType ~portfolioId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ProvisionProductOutput =
  struct
    type nonrec t = {
      recordDetail: RecordDetail.t option }
    type nonrec error =
      [ `DuplicateResourceException of DuplicateResourceException.t 
      | `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recordDetail = fun () -> { recordDetail }
    let error_of_json name json =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException
            (DuplicateResourceException.of_json json)
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException (DuplicateResourceException.of_xml xml)
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DuplicateResourceException e ->
          `Assoc
            [("error", (`String "DuplicateResourceException"));
            ("details", (DuplicateResourceException.to_json e))]
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RecordDetail",
           (Option.map x.recordDetail ~f:RecordDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recordDetail =
        (Option.map ~f:RecordDetail.of_xml)
          (Xml.child xml_arg0 "RecordDetail") in
      make ?recordDetail ()
    let of_json json =
      let recordDetail = field_map json "RecordDetail" RecordDetail.of_json in
      make ?recordDetail ()
    let to_json v = composed_to_json to_value v
  end
module ProvisionProductInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      productId: Id.t option ;
      productName: ProductViewName.t option ;
      provisioningArtifactId: Id.t option ;
      provisioningArtifactName: ProvisioningArtifactName.t option ;
      pathId: Id.t option ;
      pathName: PortfolioDisplayName.t option ;
      provisionedProductName: ProvisionedProductName.t ;
      provisioningParameters: ProvisioningParameters.t option ;
      provisioningPreferences: ProvisioningPreferences.t option ;
      tags: Tags.t option ;
      notificationArns: NotificationArns.t option ;
      provisionToken: IdempotencyToken.t }
    let context_ = "ProvisionProductInput"
    let make ?acceptLanguage =
      fun ?productId ->
        fun ?productName ->
          fun ?provisioningArtifactId ->
            fun ?provisioningArtifactName ->
              fun ?pathId ->
                fun ?pathName ->
                  fun ?provisioningParameters ->
                    fun ?provisioningPreferences ->
                      fun ?tags ->
                        fun ?notificationArns ->
                          fun ~provisionedProductName ->
                            fun ~provisionToken ->
                              fun () ->
                                {
                                  acceptLanguage;
                                  productId;
                                  productName;
                                  provisioningArtifactId;
                                  provisioningArtifactName;
                                  pathId;
                                  pathName;
                                  provisioningParameters;
                                  provisioningPreferences;
                                  tags;
                                  notificationArns;
                                  provisionedProductName;
                                  provisionToken
                                }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProductId", (Option.map x.productId ~f:Id.to_value));
        ("ProductName",
          (Option.map x.productName ~f:ProductViewName.to_value));
        ("ProvisioningArtifactId",
          (Option.map x.provisioningArtifactId ~f:Id.to_value));
        ("ProvisioningArtifactName",
          (Option.map x.provisioningArtifactName
             ~f:ProvisioningArtifactName.to_value));
        ("PathId", (Option.map x.pathId ~f:Id.to_value));
        ("PathName",
          (Option.map x.pathName ~f:PortfolioDisplayName.to_value));
        ("ProvisionedProductName",
          (Some (ProvisionedProductName.to_value x.provisionedProductName)));
        ("ProvisioningParameters",
          (Option.map x.provisioningParameters
             ~f:ProvisioningParameters.to_value));
        ("ProvisioningPreferences",
          (Option.map x.provisioningPreferences
             ~f:ProvisioningPreferences.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("NotificationArns",
          (Option.map x.notificationArns ~f:NotificationArns.to_value));
        ("ProvisionToken",
          (Some (IdempotencyToken.to_value x.provisionToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let provisionToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisionToken") in
      let notificationArns =
        (Option.map ~f:NotificationArns.of_xml)
          (Xml.child xml_arg0 "NotificationArns") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let provisioningPreferences =
        (Option.map ~f:ProvisioningPreferences.of_xml)
          (Xml.child xml_arg0 "ProvisioningPreferences") in
      let provisioningParameters =
        (Option.map ~f:ProvisioningParameters.of_xml)
          (Xml.child xml_arg0 "ProvisioningParameters") in
      let provisionedProductName =
        ProvisionedProductName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisionedProductName") in
      let pathName =
        (Option.map ~f:PortfolioDisplayName.of_xml)
          (Xml.child xml_arg0 "PathName") in
      let pathId = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PathId") in
      let provisioningArtifactName =
        (Option.map ~f:ProvisioningArtifactName.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactName") in
      let provisioningArtifactId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactId") in
      let productName =
        (Option.map ~f:ProductViewName.of_xml)
          (Xml.child xml_arg0 "ProductName") in
      let productId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~provisionToken ?notificationArns ?tags ?provisioningPreferences
        ?provisioningParameters ~provisionedProductName ?pathName ?pathId
        ?provisioningArtifactName ?provisioningArtifactId ?productName
        ?productId ?acceptLanguage ()
    let of_json json =
      let provisionToken =
        field_map_exn json "ProvisionToken" IdempotencyToken.of_json in
      let notificationArns =
        field_map json "NotificationArns" NotificationArns.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let provisioningPreferences =
        field_map json "ProvisioningPreferences"
          ProvisioningPreferences.of_json in
      let provisioningParameters =
        field_map json "ProvisioningParameters"
          ProvisioningParameters.of_json in
      let provisionedProductName =
        field_map_exn json "ProvisionedProductName"
          ProvisionedProductName.of_json in
      let pathName = field_map json "PathName" PortfolioDisplayName.of_json in
      let pathId = field_map json "PathId" Id.of_json in
      let provisioningArtifactName =
        field_map json "ProvisioningArtifactName"
          ProvisioningArtifactName.of_json in
      let provisioningArtifactId =
        field_map json "ProvisioningArtifactId" Id.of_json in
      let productName = field_map json "ProductName" ProductViewName.of_json in
      let productId = field_map json "ProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~provisionToken ?notificationArns ?tags ?provisioningPreferences
        ?provisioningParameters ~provisionedProductName ?pathName ?pathId
        ?provisioningArtifactName ?provisioningArtifactId ?productName
        ?productId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListTagOptionsOutput =
  struct
    type nonrec t =
      {
      tagOptionDetails: TagOptionDetails.t option ;
      pageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tagOptionDetails =
      fun ?pageToken -> fun () -> { tagOptionDetails; pageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TagOptionDetails",
           (Option.map x.tagOptionDetails ~f:TagOptionDetails.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let tagOptionDetails =
        (Option.map ~f:TagOptionDetails.of_xml)
          (Xml.child xml_arg0 "TagOptionDetails") in
      make ?pageToken ?tagOptionDetails ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let tagOptionDetails =
        field_map json "TagOptionDetails" TagOptionDetails.of_json in
      make ?pageToken ?tagOptionDetails ()
    let to_json v = composed_to_json to_value v
  end
module ListTagOptionsInput =
  struct
    type nonrec t =
      {
      filters: ListTagOptionsFilters.t option ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option }
    let make ?filters =
      fun ?pageSize ->
        fun ?pageToken -> fun () -> { filters; pageSize; pageToken }
    let to_value x =
      structure_to_value
        [("Filters",
           (Option.map x.filters ~f:ListTagOptionsFilters.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let filters =
        (Option.map ~f:ListTagOptionsFilters.of_xml)
          (Xml.child xml_arg0 "Filters") in
      make ?pageToken ?pageSize ?filters ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let filters = field_map json "Filters" ListTagOptionsFilters.of_json in
      make ?pageToken ?pageSize ?filters ()
    let to_json v = composed_to_json to_value v
  end
module ListStackInstancesForProvisionedProductOutput =
  struct
    type nonrec t =
      {
      stackInstances: StackInstances.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?stackInstances =
      fun ?nextPageToken -> fun () -> { stackInstances; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("StackInstances",
           (Option.map x.stackInstances ~f:StackInstances.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let stackInstances =
        (Option.map ~f:StackInstances.of_xml)
          (Xml.child xml_arg0 "StackInstances") in
      make ?nextPageToken ?stackInstances ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let stackInstances =
        field_map json "StackInstances" StackInstances.of_json in
      make ?nextPageToken ?stackInstances ()
    let to_json v = composed_to_json to_value v
  end
module ListStackInstancesForProvisionedProductInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      provisionedProductId: Id.t ;
      pageToken: PageToken.t option ;
      pageSize: PageSize.t option }
    let context_ = "ListStackInstancesForProvisionedProductInput"
    let make ?acceptLanguage =
      fun ?pageToken ->
        fun ?pageSize ->
          fun ~provisionedProductId ->
            fun () ->
              { acceptLanguage; pageToken; pageSize; provisionedProductId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProvisionedProductId", (Some (Id.to_value x.provisionedProductId)));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let provisionedProductId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisionedProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageSize ?pageToken ~provisionedProductId ?acceptLanguage ()
    let of_json json =
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let provisionedProductId =
        field_map_exn json "ProvisionedProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageSize ?pageToken ~provisionedProductId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListServiceActionsOutput =
  struct
    type nonrec t =
      {
      serviceActionSummaries: ServiceActionSummaries.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?serviceActionSummaries =
      fun ?nextPageToken ->
        fun () -> { serviceActionSummaries; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ServiceActionSummaries",
           (Option.map x.serviceActionSummaries
              ~f:ServiceActionSummaries.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let serviceActionSummaries =
        (Option.map ~f:ServiceActionSummaries.of_xml)
          (Xml.child xml_arg0 "ServiceActionSummaries") in
      make ?nextPageToken ?serviceActionSummaries ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let serviceActionSummaries =
        field_map json "ServiceActionSummaries"
          ServiceActionSummaries.of_json in
      make ?nextPageToken ?serviceActionSummaries ()
    let to_json v = composed_to_json to_value v
  end
module ListServiceActionsInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option }
    let make ?acceptLanguage =
      fun ?pageSize ->
        fun ?pageToken -> fun () -> { acceptLanguage; pageSize; pageToken }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageToken ?pageSize ?acceptLanguage ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageToken ?pageSize ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListServiceActionsForProvisioningArtifactOutput =
  struct
    type nonrec t =
      {
      serviceActionSummaries: ServiceActionSummaries.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?serviceActionSummaries =
      fun ?nextPageToken ->
        fun () -> { serviceActionSummaries; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ServiceActionSummaries",
           (Option.map x.serviceActionSummaries
              ~f:ServiceActionSummaries.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let serviceActionSummaries =
        (Option.map ~f:ServiceActionSummaries.of_xml)
          (Xml.child xml_arg0 "ServiceActionSummaries") in
      make ?nextPageToken ?serviceActionSummaries ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let serviceActionSummaries =
        field_map json "ServiceActionSummaries"
          ServiceActionSummaries.of_json in
      make ?nextPageToken ?serviceActionSummaries ()
    let to_json v = composed_to_json to_value v
  end
module ListServiceActionsForProvisioningArtifactInput =
  struct
    type nonrec t =
      {
      productId: Id.t ;
      provisioningArtifactId: Id.t ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option ;
      acceptLanguage: AcceptLanguage.t option }
    let context_ = "ListServiceActionsForProvisioningArtifactInput"
    let make ?pageSize =
      fun ?pageToken ->
        fun ?acceptLanguage ->
          fun ~productId ->
            fun ~provisioningArtifactId ->
              fun () ->
                {
                  pageSize;
                  pageToken;
                  acceptLanguage;
                  productId;
                  provisioningArtifactId
                }
    let to_value x =
      structure_to_value
        [("ProductId", (Some (Id.to_value x.productId)));
        ("ProvisioningArtifactId",
          (Some (Id.to_value x.provisioningArtifactId)));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value));
        ("AcceptLanguage",
          (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let provisioningArtifactId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisioningArtifactId") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      make ?acceptLanguage ?pageToken ?pageSize ~provisioningArtifactId
        ~productId ()
    let of_json json =
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let provisioningArtifactId =
        field_map_exn json "ProvisioningArtifactId" Id.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      make ?acceptLanguage ?pageToken ?pageSize ~provisioningArtifactId
        ~productId ()
    let to_json v = composed_to_json to_value v
  end
module ListResourcesForTagOptionOutput =
  struct
    type nonrec t =
      {
      resourceDetails: ResourceDetails.t option ;
      pageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?resourceDetails =
      fun ?pageToken -> fun () -> { resourceDetails; pageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ResourceDetails",
           (Option.map x.resourceDetails ~f:ResourceDetails.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let resourceDetails =
        (Option.map ~f:ResourceDetails.of_xml)
          (Xml.child xml_arg0 "ResourceDetails") in
      make ?pageToken ?resourceDetails ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let resourceDetails =
        field_map json "ResourceDetails" ResourceDetails.of_json in
      make ?pageToken ?resourceDetails ()
    let to_json v = composed_to_json to_value v
  end
module ListResourcesForTagOptionInput =
  struct
    type nonrec t =
      {
      tagOptionId: TagOptionId.t ;
      resourceType: ResourceType.t option ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option }
    let context_ = "ListResourcesForTagOptionInput"
    let make ?resourceType =
      fun ?pageSize ->
        fun ?pageToken ->
          fun ~tagOptionId ->
            fun () -> { resourceType; pageSize; pageToken; tagOptionId }
    let to_value x =
      structure_to_value
        [("TagOptionId", (Some (TagOptionId.to_value x.tagOptionId)));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let tagOptionId =
        TagOptionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TagOptionId") in
      make ?pageToken ?pageSize ?resourceType ~tagOptionId ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let tagOptionId = field_map_exn json "TagOptionId" TagOptionId.of_json in
      make ?pageToken ?pageSize ?resourceType ~tagOptionId ()
    let to_json v = composed_to_json to_value v
  end
module ListRecordHistoryOutput =
  struct
    type nonrec t =
      {
      recordDetails: RecordDetails.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recordDetails =
      fun ?nextPageToken -> fun () -> { recordDetails; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RecordDetails",
           (Option.map x.recordDetails ~f:RecordDetails.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let recordDetails =
        (Option.map ~f:RecordDetails.of_xml)
          (Xml.child xml_arg0 "RecordDetails") in
      make ?nextPageToken ?recordDetails ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let recordDetails =
        field_map json "RecordDetails" RecordDetails.of_json in
      make ?nextPageToken ?recordDetails ()
    let to_json v = composed_to_json to_value v
  end
module ListRecordHistoryInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      accessLevelFilter: AccessLevelFilter.t option ;
      searchFilter: ListRecordHistorySearchFilter.t option ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option }
    let make ?acceptLanguage =
      fun ?accessLevelFilter ->
        fun ?searchFilter ->
          fun ?pageSize ->
            fun ?pageToken ->
              fun () ->
                {
                  acceptLanguage;
                  accessLevelFilter;
                  searchFilter;
                  pageSize;
                  pageToken
                }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("AccessLevelFilter",
          (Option.map x.accessLevelFilter ~f:AccessLevelFilter.to_value));
        ("SearchFilter",
          (Option.map x.searchFilter
             ~f:ListRecordHistorySearchFilter.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let searchFilter =
        (Option.map ~f:ListRecordHistorySearchFilter.of_xml)
          (Xml.child xml_arg0 "SearchFilter") in
      let accessLevelFilter =
        (Option.map ~f:AccessLevelFilter.of_xml)
          (Xml.child xml_arg0 "AccessLevelFilter") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageToken ?pageSize ?searchFilter ?accessLevelFilter
        ?acceptLanguage ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let searchFilter =
        field_map json "SearchFilter" ListRecordHistorySearchFilter.of_json in
      let accessLevelFilter =
        field_map json "AccessLevelFilter" AccessLevelFilter.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageToken ?pageSize ?searchFilter ?accessLevelFilter
        ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListProvisioningArtifactsOutput =
  struct
    type nonrec t =
      {
      provisioningArtifactDetails: ProvisioningArtifactDetails.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?provisioningArtifactDetails =
      fun ?nextPageToken ->
        fun () -> { provisioningArtifactDetails; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProvisioningArtifactDetails",
           (Option.map x.provisioningArtifactDetails
              ~f:ProvisioningArtifactDetails.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let provisioningArtifactDetails =
        (Option.map ~f:ProvisioningArtifactDetails.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactDetails") in
      make ?nextPageToken ?provisioningArtifactDetails ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let provisioningArtifactDetails =
        field_map json "ProvisioningArtifactDetails"
          ProvisioningArtifactDetails.of_json in
      make ?nextPageToken ?provisioningArtifactDetails ()
    let to_json v = composed_to_json to_value v
  end
module ListProvisioningArtifactsInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      productId: Id.t }
    let context_ = "ListProvisioningArtifactsInput"
    let make ?acceptLanguage =
      fun ~productId -> fun () -> { acceptLanguage; productId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProductId", (Some (Id.to_value x.productId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~productId ?acceptLanguage ()
    let of_json json =
      let productId = field_map_exn json "ProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~productId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListProvisioningArtifactsForServiceActionOutput =
  struct
    type nonrec t =
      {
      provisioningArtifactViews: ProvisioningArtifactViews.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?provisioningArtifactViews =
      fun ?nextPageToken ->
        fun () -> { provisioningArtifactViews; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProvisioningArtifactViews",
           (Option.map x.provisioningArtifactViews
              ~f:ProvisioningArtifactViews.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let provisioningArtifactViews =
        (Option.map ~f:ProvisioningArtifactViews.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactViews") in
      make ?nextPageToken ?provisioningArtifactViews ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let provisioningArtifactViews =
        field_map json "ProvisioningArtifactViews"
          ProvisioningArtifactViews.of_json in
      make ?nextPageToken ?provisioningArtifactViews ()
    let to_json v = composed_to_json to_value v
  end
module ListProvisioningArtifactsForServiceActionInput =
  struct
    type nonrec t =
      {
      serviceActionId: Id.t ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option ;
      acceptLanguage: AcceptLanguage.t option }
    let context_ = "ListProvisioningArtifactsForServiceActionInput"
    let make ?pageSize =
      fun ?pageToken ->
        fun ?acceptLanguage ->
          fun ~serviceActionId ->
            fun () ->
              { pageSize; pageToken; acceptLanguage; serviceActionId }
    let to_value x =
      structure_to_value
        [("ServiceActionId", (Some (Id.to_value x.serviceActionId)));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value));
        ("AcceptLanguage",
          (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let serviceActionId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceActionId") in
      make ?acceptLanguage ?pageToken ?pageSize ~serviceActionId ()
    let of_json json =
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let serviceActionId = field_map_exn json "ServiceActionId" Id.of_json in
      make ?acceptLanguage ?pageToken ?pageSize ~serviceActionId ()
    let to_json v = composed_to_json to_value v
  end
module ListProvisionedProductPlansOutput =
  struct
    type nonrec t =
      {
      provisionedProductPlans: ProvisionedProductPlans.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?provisionedProductPlans =
      fun ?nextPageToken ->
        fun () -> { provisionedProductPlans; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProvisionedProductPlans",
           (Option.map x.provisionedProductPlans
              ~f:ProvisionedProductPlans.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let provisionedProductPlans =
        (Option.map ~f:ProvisionedProductPlans.of_xml)
          (Xml.child xml_arg0 "ProvisionedProductPlans") in
      make ?nextPageToken ?provisionedProductPlans ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let provisionedProductPlans =
        field_map json "ProvisionedProductPlans"
          ProvisionedProductPlans.of_json in
      make ?nextPageToken ?provisionedProductPlans ()
    let to_json v = composed_to_json to_value v
  end
module ListProvisionedProductPlansInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      provisionProductId: Id.t option ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option ;
      accessLevelFilter: AccessLevelFilter.t option }
    let make ?acceptLanguage =
      fun ?provisionProductId ->
        fun ?pageSize ->
          fun ?pageToken ->
            fun ?accessLevelFilter ->
              fun () ->
                {
                  acceptLanguage;
                  provisionProductId;
                  pageSize;
                  pageToken;
                  accessLevelFilter
                }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProvisionProductId",
          (Option.map x.provisionProductId ~f:Id.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value));
        ("AccessLevelFilter",
          (Option.map x.accessLevelFilter ~f:AccessLevelFilter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accessLevelFilter =
        (Option.map ~f:AccessLevelFilter.of_xml)
          (Xml.child xml_arg0 "AccessLevelFilter") in
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let provisionProductId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProvisionProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?accessLevelFilter ?pageToken ?pageSize ?provisionProductId
        ?acceptLanguage ()
    let of_json json =
      let accessLevelFilter =
        field_map json "AccessLevelFilter" AccessLevelFilter.of_json in
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let provisionProductId = field_map json "ProvisionProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?accessLevelFilter ?pageToken ?pageSize ?provisionProductId
        ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListPrincipalsForPortfolioOutput =
  struct
    type nonrec t =
      {
      principals: Principals.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?principals =
      fun ?nextPageToken -> fun () -> { principals; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Principals", (Option.map x.principals ~f:Principals.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let principals =
        (Option.map ~f:Principals.of_xml) (Xml.child xml_arg0 "Principals") in
      make ?nextPageToken ?principals ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let principals = field_map json "Principals" Principals.of_json in
      make ?nextPageToken ?principals ()
    let to_json v = composed_to_json to_value v
  end
module ListPrincipalsForPortfolioInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      portfolioId: Id.t ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option }
    let context_ = "ListPrincipalsForPortfolioInput"
    let make ?acceptLanguage =
      fun ?pageSize ->
        fun ?pageToken ->
          fun ~portfolioId ->
            fun () -> { acceptLanguage; pageSize; pageToken; portfolioId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageToken ?pageSize ~portfolioId ?acceptLanguage ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageToken ?pageSize ~portfolioId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListPortfoliosOutput =
  struct
    type nonrec t =
      {
      portfolioDetails: PortfolioDetails.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?portfolioDetails =
      fun ?nextPageToken -> fun () -> { portfolioDetails; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PortfolioDetails",
           (Option.map x.portfolioDetails ~f:PortfolioDetails.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let portfolioDetails =
        (Option.map ~f:PortfolioDetails.of_xml)
          (Xml.child xml_arg0 "PortfolioDetails") in
      make ?nextPageToken ?portfolioDetails ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let portfolioDetails =
        field_map json "PortfolioDetails" PortfolioDetails.of_json in
      make ?nextPageToken ?portfolioDetails ()
    let to_json v = composed_to_json to_value v
  end
module ListPortfoliosInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      pageToken: PageToken.t option ;
      pageSize: PageSizeMax100.t option }
    let make ?acceptLanguage =
      fun ?pageToken ->
        fun ?pageSize -> fun () -> { acceptLanguage; pageToken; pageSize }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSizeMax100.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageSize =
        (Option.map ~f:PageSizeMax100.of_xml) (Xml.child xml_arg0 "PageSize") in
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageSize ?pageToken ?acceptLanguage ()
    let of_json json =
      let pageSize = field_map json "PageSize" PageSizeMax100.of_json in
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageSize ?pageToken ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListPortfoliosForProductOutput =
  struct
    type nonrec t =
      {
      portfolioDetails: PortfolioDetails.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?portfolioDetails =
      fun ?nextPageToken -> fun () -> { portfolioDetails; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PortfolioDetails",
           (Option.map x.portfolioDetails ~f:PortfolioDetails.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let portfolioDetails =
        (Option.map ~f:PortfolioDetails.of_xml)
          (Xml.child xml_arg0 "PortfolioDetails") in
      make ?nextPageToken ?portfolioDetails ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let portfolioDetails =
        field_map json "PortfolioDetails" PortfolioDetails.of_json in
      make ?nextPageToken ?portfolioDetails ()
    let to_json v = composed_to_json to_value v
  end
module ListPortfoliosForProductInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      productId: Id.t ;
      pageToken: PageToken.t option ;
      pageSize: PageSizeMax100.t option }
    let context_ = "ListPortfoliosForProductInput"
    let make ?acceptLanguage =
      fun ?pageToken ->
        fun ?pageSize ->
          fun ~productId ->
            fun () -> { acceptLanguage; pageToken; pageSize; productId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProductId", (Some (Id.to_value x.productId)));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSizeMax100.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageSize =
        (Option.map ~f:PageSizeMax100.of_xml) (Xml.child xml_arg0 "PageSize") in
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageSize ?pageToken ~productId ?acceptLanguage ()
    let of_json json =
      let pageSize = field_map json "PageSize" PageSizeMax100.of_json in
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageSize ?pageToken ~productId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListPortfolioAccessOutput =
  struct
    type nonrec t =
      {
      accountIds: AccountIds.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?accountIds =
      fun ?nextPageToken -> fun () -> { accountIds; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("AccountIds", (Option.map x.accountIds ~f:AccountIds.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let accountIds =
        (Option.map ~f:AccountIds.of_xml) (Xml.child xml_arg0 "AccountIds") in
      make ?nextPageToken ?accountIds ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let accountIds = field_map json "AccountIds" AccountIds.of_json in
      make ?nextPageToken ?accountIds ()
    let to_json v = composed_to_json to_value v
  end
module ListPortfolioAccessInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      portfolioId: Id.t ;
      organizationParentId: Id.t option ;
      pageToken: PageToken.t option ;
      pageSize: PageSizeMax100.t option }
    let context_ = "ListPortfolioAccessInput"
    let make ?acceptLanguage =
      fun ?organizationParentId ->
        fun ?pageToken ->
          fun ?pageSize ->
            fun ~portfolioId ->
              fun () ->
                {
                  acceptLanguage;
                  organizationParentId;
                  pageToken;
                  pageSize;
                  portfolioId
                }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("OrganizationParentId",
          (Option.map x.organizationParentId ~f:Id.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSizeMax100.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageSize =
        (Option.map ~f:PageSizeMax100.of_xml) (Xml.child xml_arg0 "PageSize") in
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let organizationParentId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "OrganizationParentId") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageSize ?pageToken ?organizationParentId ~portfolioId
        ?acceptLanguage ()
    let of_json json =
      let pageSize = field_map json "PageSize" PageSizeMax100.of_json in
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let organizationParentId =
        field_map json "OrganizationParentId" Id.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageSize ?pageToken ?organizationParentId ~portfolioId
        ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListOrganizationPortfolioAccessOutput =
  struct
    type nonrec t =
      {
      organizationNodes: OrganizationNodes.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `OperationNotSupportedException of OperationNotSupportedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?organizationNodes =
      fun ?nextPageToken -> fun () -> { organizationNodes; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `OperationNotSupportedException e ->
          `Assoc
            [("error", (`String "OperationNotSupportedException"));
            ("details", (OperationNotSupportedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("OrganizationNodes",
           (Option.map x.organizationNodes ~f:OrganizationNodes.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let organizationNodes =
        (Option.map ~f:OrganizationNodes.of_xml)
          (Xml.child xml_arg0 "OrganizationNodes") in
      make ?nextPageToken ?organizationNodes ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let organizationNodes =
        field_map json "OrganizationNodes" OrganizationNodes.of_json in
      make ?nextPageToken ?organizationNodes ()
    let to_json v = composed_to_json to_value v
  end
module ListOrganizationPortfolioAccessInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      portfolioId: Id.t ;
      organizationNodeType: OrganizationNodeType.t ;
      pageToken: PageToken.t option ;
      pageSize: PageSize.t option }
    let context_ = "ListOrganizationPortfolioAccessInput"
    let make ?acceptLanguage =
      fun ?pageToken ->
        fun ?pageSize ->
          fun ~portfolioId ->
            fun ~organizationNodeType ->
              fun () ->
                {
                  acceptLanguage;
                  pageToken;
                  pageSize;
                  portfolioId;
                  organizationNodeType
                }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("OrganizationNodeType",
          (Some (OrganizationNodeType.to_value x.organizationNodeType)));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let organizationNodeType =
        OrganizationNodeType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OrganizationNodeType") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageSize ?pageToken ~organizationNodeType ~portfolioId
        ?acceptLanguage ()
    let of_json json =
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let organizationNodeType =
        field_map_exn json "OrganizationNodeType"
          OrganizationNodeType.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageSize ?pageToken ~organizationNodeType ~portfolioId
        ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListLaunchPathsOutput =
  struct
    type nonrec t =
      {
      launchPathSummaries: LaunchPathSummaries.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?launchPathSummaries =
      fun ?nextPageToken -> fun () -> { launchPathSummaries; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("LaunchPathSummaries",
           (Option.map x.launchPathSummaries ~f:LaunchPathSummaries.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let launchPathSummaries =
        (Option.map ~f:LaunchPathSummaries.of_xml)
          (Xml.child xml_arg0 "LaunchPathSummaries") in
      make ?nextPageToken ?launchPathSummaries ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let launchPathSummaries =
        field_map json "LaunchPathSummaries" LaunchPathSummaries.of_json in
      make ?nextPageToken ?launchPathSummaries ()
    let to_json v = composed_to_json to_value v
  end
module ListLaunchPathsInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      productId: Id.t ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option }
    let context_ = "ListLaunchPathsInput"
    let make ?acceptLanguage =
      fun ?pageSize ->
        fun ?pageToken ->
          fun ~productId ->
            fun () -> { acceptLanguage; pageSize; pageToken; productId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProductId", (Some (Id.to_value x.productId)));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageToken ?pageSize ~productId ?acceptLanguage ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageToken ?pageSize ~productId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListConstraintsForPortfolioOutput =
  struct
    type nonrec t =
      {
      constraintDetails: ConstraintDetails.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?constraintDetails =
      fun ?nextPageToken -> fun () -> { constraintDetails; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ConstraintDetails",
           (Option.map x.constraintDetails ~f:ConstraintDetails.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let constraintDetails =
        (Option.map ~f:ConstraintDetails.of_xml)
          (Xml.child xml_arg0 "ConstraintDetails") in
      make ?nextPageToken ?constraintDetails ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let constraintDetails =
        field_map json "ConstraintDetails" ConstraintDetails.of_json in
      make ?nextPageToken ?constraintDetails ()
    let to_json v = composed_to_json to_value v
  end
module ListConstraintsForPortfolioInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      portfolioId: Id.t ;
      productId: Id.t option ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option }
    let context_ = "ListConstraintsForPortfolioInput"
    let make ?acceptLanguage =
      fun ?productId ->
        fun ?pageSize ->
          fun ?pageToken ->
            fun ~portfolioId ->
              fun () ->
                { acceptLanguage; productId; pageSize; pageToken; portfolioId
                }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("ProductId", (Option.map x.productId ~f:Id.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let productId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProductId") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageToken ?pageSize ?productId ~portfolioId ?acceptLanguage ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let productId = field_map json "ProductId" Id.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageToken ?pageSize ?productId ~portfolioId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListBudgetsForResourceOutput =
  struct
    type nonrec t =
      {
      budgets: Budgets.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?budgets =
      fun ?nextPageToken -> fun () -> { budgets; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Budgets", (Option.map x.budgets ~f:Budgets.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let budgets =
        (Option.map ~f:Budgets.of_xml) (Xml.child xml_arg0 "Budgets") in
      make ?nextPageToken ?budgets ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let budgets = field_map json "Budgets" Budgets.of_json in
      make ?nextPageToken ?budgets ()
    let to_json v = composed_to_json to_value v
  end
module ListBudgetsForResourceInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      resourceId: Id.t ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option }
    let context_ = "ListBudgetsForResourceInput"
    let make ?acceptLanguage =
      fun ?pageSize ->
        fun ?pageToken ->
          fun ~resourceId ->
            fun () -> { acceptLanguage; pageSize; pageToken; resourceId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ResourceId", (Some (Id.to_value x.resourceId)));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let resourceId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageToken ?pageSize ~resourceId ?acceptLanguage ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let resourceId = field_map_exn json "ResourceId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageToken ?pageSize ~resourceId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ListAcceptedPortfolioSharesOutput =
  struct
    type nonrec t =
      {
      portfolioDetails: PortfolioDetails.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `OperationNotSupportedException of OperationNotSupportedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?portfolioDetails =
      fun ?nextPageToken -> fun () -> { portfolioDetails; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `OperationNotSupportedException e ->
          `Assoc
            [("error", (`String "OperationNotSupportedException"));
            ("details", (OperationNotSupportedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PortfolioDetails",
           (Option.map x.portfolioDetails ~f:PortfolioDetails.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let portfolioDetails =
        (Option.map ~f:PortfolioDetails.of_xml)
          (Xml.child xml_arg0 "PortfolioDetails") in
      make ?nextPageToken ?portfolioDetails ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let portfolioDetails =
        field_map json "PortfolioDetails" PortfolioDetails.of_json in
      make ?nextPageToken ?portfolioDetails ()
    let to_json v = composed_to_json to_value v
  end
module ListAcceptedPortfolioSharesInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      pageToken: PageToken.t option ;
      pageSize: PageSizeMax100.t option ;
      portfolioShareType: PortfolioShareType.t option }
    let make ?acceptLanguage =
      fun ?pageToken ->
        fun ?pageSize ->
          fun ?portfolioShareType ->
            fun () ->
              { acceptLanguage; pageToken; pageSize; portfolioShareType }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSizeMax100.to_value));
        ("PortfolioShareType",
          (Option.map x.portfolioShareType ~f:PortfolioShareType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let portfolioShareType =
        (Option.map ~f:PortfolioShareType.of_xml)
          (Xml.child xml_arg0 "PortfolioShareType") in
      let pageSize =
        (Option.map ~f:PageSizeMax100.of_xml) (Xml.child xml_arg0 "PageSize") in
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?portfolioShareType ?pageSize ?pageToken ?acceptLanguage ()
    let of_json json =
      let portfolioShareType =
        field_map json "PortfolioShareType" PortfolioShareType.of_json in
      let pageSize = field_map json "PageSize" PageSizeMax100.of_json in
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?portfolioShareType ?pageSize ?pageToken ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module ImportAsProvisionedProductOutput =
  struct
    type nonrec t = {
      recordDetail: RecordDetail.t option }
    type nonrec error =
      [ `DuplicateResourceException of DuplicateResourceException.t 
      | `InvalidParametersException of InvalidParametersException.t 
      | `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recordDetail = fun () -> { recordDetail }
    let error_of_json name json =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException
            (DuplicateResourceException.of_json json)
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException (DuplicateResourceException.of_xml xml)
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DuplicateResourceException e ->
          `Assoc
            [("error", (`String "DuplicateResourceException"));
            ("details", (DuplicateResourceException.to_json e))]
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RecordDetail",
           (Option.map x.recordDetail ~f:RecordDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recordDetail =
        (Option.map ~f:RecordDetail.of_xml)
          (Xml.child xml_arg0 "RecordDetail") in
      make ?recordDetail ()
    let of_json json =
      let recordDetail = field_map json "RecordDetail" RecordDetail.of_json in
      make ?recordDetail ()
    let to_json v = composed_to_json to_value v
  end
module ImportAsProvisionedProductInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      productId: Id.t ;
      provisioningArtifactId: Id.t ;
      provisionedProductName: ProvisionedProductName.t ;
      physicalId: PhysicalId.t ;
      idempotencyToken: IdempotencyToken.t }
    let context_ = "ImportAsProvisionedProductInput"
    let make ?acceptLanguage =
      fun ~productId ->
        fun ~provisioningArtifactId ->
          fun ~provisionedProductName ->
            fun ~physicalId ->
              fun ~idempotencyToken ->
                fun () ->
                  {
                    acceptLanguage;
                    productId;
                    provisioningArtifactId;
                    provisionedProductName;
                    physicalId;
                    idempotencyToken
                  }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProductId", (Some (Id.to_value x.productId)));
        ("ProvisioningArtifactId",
          (Some (Id.to_value x.provisioningArtifactId)));
        ("ProvisionedProductName",
          (Some (ProvisionedProductName.to_value x.provisionedProductName)));
        ("PhysicalId", (Some (PhysicalId.to_value x.physicalId)));
        ("IdempotencyToken",
          (Some (IdempotencyToken.to_value x.idempotencyToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdempotencyToken") in
      let physicalId =
        PhysicalId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PhysicalId") in
      let provisionedProductName =
        ProvisionedProductName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisionedProductName") in
      let provisioningArtifactId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisioningArtifactId") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~idempotencyToken ~physicalId ~provisionedProductName
        ~provisioningArtifactId ~productId ?acceptLanguage ()
    let of_json json =
      let idempotencyToken =
        field_map_exn json "IdempotencyToken" IdempotencyToken.of_json in
      let physicalId = field_map_exn json "PhysicalId" PhysicalId.of_json in
      let provisionedProductName =
        field_map_exn json "ProvisionedProductName"
          ProvisionedProductName.of_json in
      let provisioningArtifactId =
        field_map_exn json "ProvisioningArtifactId" Id.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~idempotencyToken ~physicalId ~provisionedProductName
        ~provisioningArtifactId ~productId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module GetProvisionedProductOutputsOutput =
  struct
    type nonrec t =
      {
      outputs: RecordOutputs.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?outputs =
      fun ?nextPageToken -> fun () -> { outputs; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Outputs", (Option.map x.outputs ~f:RecordOutputs.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let outputs =
        (Option.map ~f:RecordOutputs.of_xml) (Xml.child xml_arg0 "Outputs") in
      make ?nextPageToken ?outputs ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let outputs = field_map json "Outputs" RecordOutputs.of_json in
      make ?nextPageToken ?outputs ()
    let to_json v = composed_to_json to_value v
  end
module GetProvisionedProductOutputsInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      provisionedProductId: Id.t option ;
      provisionedProductName: ProvisionedProductName.t option ;
      outputKeys: OutputKeys.t option ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option }
    let make ?acceptLanguage =
      fun ?provisionedProductId ->
        fun ?provisionedProductName ->
          fun ?outputKeys ->
            fun ?pageSize ->
              fun ?pageToken ->
                fun () ->
                  {
                    acceptLanguage;
                    provisionedProductId;
                    provisionedProductName;
                    outputKeys;
                    pageSize;
                    pageToken
                  }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProvisionedProductId",
          (Option.map x.provisionedProductId ~f:Id.to_value));
        ("ProvisionedProductName",
          (Option.map x.provisionedProductName
             ~f:ProvisionedProductName.to_value));
        ("OutputKeys", (Option.map x.outputKeys ~f:OutputKeys.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let outputKeys =
        (Option.map ~f:OutputKeys.of_xml) (Xml.child xml_arg0 "OutputKeys") in
      let provisionedProductName =
        (Option.map ~f:ProvisionedProductName.of_xml)
          (Xml.child xml_arg0 "ProvisionedProductName") in
      let provisionedProductId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProvisionedProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageToken ?pageSize ?outputKeys ?provisionedProductName
        ?provisionedProductId ?acceptLanguage ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let outputKeys = field_map json "OutputKeys" OutputKeys.of_json in
      let provisionedProductName =
        field_map json "ProvisionedProductName"
          ProvisionedProductName.of_json in
      let provisionedProductId =
        field_map json "ProvisionedProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageToken ?pageSize ?outputKeys ?provisionedProductName
        ?provisionedProductId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module GetAWSOrganizationsAccessStatusOutput =
  struct
    type nonrec t = {
      accessStatus: AccessStatus.t option }
    type nonrec error =
      [ `OperationNotSupportedException of OperationNotSupportedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?accessStatus = fun () -> { accessStatus }
    let error_of_json name json =
      match name with
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `OperationNotSupportedException e ->
          `Assoc
            [("error", (`String "OperationNotSupportedException"));
            ("details", (OperationNotSupportedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("AccessStatus",
           (Option.map x.accessStatus ~f:AccessStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accessStatus =
        (Option.map ~f:AccessStatus.of_xml)
          (Xml.child xml_arg0 "AccessStatus") in
      make ?accessStatus ()
    let of_json json =
      let accessStatus = field_map json "AccessStatus" AccessStatus.of_json in
      make ?accessStatus ()
    let to_json v = composed_to_json to_value v
  end
module GetAWSOrganizationsAccessStatusInput =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module ExecuteProvisionedProductServiceActionOutput =
  struct
    type nonrec t = {
      recordDetail: RecordDetail.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recordDetail = fun () -> { recordDetail }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RecordDetail",
           (Option.map x.recordDetail ~f:RecordDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recordDetail =
        (Option.map ~f:RecordDetail.of_xml)
          (Xml.child xml_arg0 "RecordDetail") in
      make ?recordDetail ()
    let of_json json =
      let recordDetail = field_map json "RecordDetail" RecordDetail.of_json in
      make ?recordDetail ()
    let to_json v = composed_to_json to_value v
  end
module ExecuteProvisionedProductServiceActionInput =
  struct
    type nonrec t =
      {
      provisionedProductId: Id.t ;
      serviceActionId: Id.t ;
      executeToken: IdempotencyToken.t ;
      acceptLanguage: AcceptLanguage.t option ;
      parameters: ExecutionParameterMap.t option }
    let context_ = "ExecuteProvisionedProductServiceActionInput"
    let make ?acceptLanguage =
      fun ?parameters ->
        fun ~provisionedProductId ->
          fun ~serviceActionId ->
            fun ~executeToken ->
              fun () ->
                {
                  acceptLanguage;
                  parameters;
                  provisionedProductId;
                  serviceActionId;
                  executeToken
                }
    let to_value x =
      structure_to_value
        [("ProvisionedProductId",
           (Some (Id.to_value x.provisionedProductId)));
        ("ServiceActionId", (Some (Id.to_value x.serviceActionId)));
        ("ExecuteToken", (Some (IdempotencyToken.to_value x.executeToken)));
        ("AcceptLanguage",
          (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Parameters",
          (Option.map x.parameters ~f:ExecutionParameterMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameters =
        (Option.map ~f:ExecutionParameterMap.of_xml)
          (Xml.child xml_arg0 "Parameters") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      let executeToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExecuteToken") in
      let serviceActionId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceActionId") in
      let provisionedProductId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisionedProductId") in
      make ?parameters ?acceptLanguage ~executeToken ~serviceActionId
        ~provisionedProductId ()
    let of_json json =
      let parameters =
        field_map json "Parameters" ExecutionParameterMap.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      let executeToken =
        field_map_exn json "ExecuteToken" IdempotencyToken.of_json in
      let serviceActionId = field_map_exn json "ServiceActionId" Id.of_json in
      let provisionedProductId =
        field_map_exn json "ProvisionedProductId" Id.of_json in
      make ?parameters ?acceptLanguage ~executeToken ~serviceActionId
        ~provisionedProductId ()
    let to_json v = composed_to_json to_value v
  end
module ExecuteProvisionedProductPlanOutput =
  struct
    type nonrec t = {
      recordDetail: RecordDetail.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recordDetail = fun () -> { recordDetail }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RecordDetail",
           (Option.map x.recordDetail ~f:RecordDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recordDetail =
        (Option.map ~f:RecordDetail.of_xml)
          (Xml.child xml_arg0 "RecordDetail") in
      make ?recordDetail ()
    let of_json json =
      let recordDetail = field_map json "RecordDetail" RecordDetail.of_json in
      make ?recordDetail ()
    let to_json v = composed_to_json to_value v
  end
module ExecuteProvisionedProductPlanInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      planId: Id.t ;
      idempotencyToken: IdempotencyToken.t }
    let context_ = "ExecuteProvisionedProductPlanInput"
    let make ?acceptLanguage =
      fun ~planId ->
        fun ~idempotencyToken ->
          fun () -> { acceptLanguage; planId; idempotencyToken }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PlanId", (Some (Id.to_value x.planId)));
        ("IdempotencyToken",
          (Some (IdempotencyToken.to_value x.idempotencyToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdempotencyToken") in
      let planId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PlanId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~idempotencyToken ~planId ?acceptLanguage ()
    let of_json json =
      let idempotencyToken =
        field_map_exn json "IdempotencyToken" IdempotencyToken.of_json in
      let planId = field_map_exn json "PlanId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~idempotencyToken ~planId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module EnableAWSOrganizationsAccessOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `OperationNotSupportedException of OperationNotSupportedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `OperationNotSupportedException e ->
          `Assoc
            [("error", (`String "OperationNotSupportedException"));
            ("details", (OperationNotSupportedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module EnableAWSOrganizationsAccessInput =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateTagOptionFromResourceOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateTagOptionFromResourceInput =
  struct
    type nonrec t = {
      resourceId: ResourceId.t ;
      tagOptionId: TagOptionId.t }
    let context_ = "DisassociateTagOptionFromResourceInput"
    let make ~resourceId =
      fun ~tagOptionId -> fun () -> { resourceId; tagOptionId }
    let to_value x =
      structure_to_value
        [("ResourceId", (Some (ResourceId.to_value x.resourceId)));
        ("TagOptionId", (Some (TagOptionId.to_value x.tagOptionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagOptionId =
        TagOptionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TagOptionId") in
      let resourceId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      make ~tagOptionId ~resourceId ()
    let of_json json =
      let tagOptionId = field_map_exn json "TagOptionId" TagOptionId.of_json in
      let resourceId = field_map_exn json "ResourceId" ResourceId.of_json in
      make ~tagOptionId ~resourceId ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateServiceActionFromProvisioningArtifactOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateServiceActionFromProvisioningArtifactInput =
  struct
    type nonrec t =
      {
      productId: Id.t ;
      provisioningArtifactId: Id.t ;
      serviceActionId: Id.t ;
      acceptLanguage: AcceptLanguage.t option }
    let context_ = "DisassociateServiceActionFromProvisioningArtifactInput"
    let make ?acceptLanguage =
      fun ~productId ->
        fun ~provisioningArtifactId ->
          fun ~serviceActionId ->
            fun () ->
              {
                acceptLanguage;
                productId;
                provisioningArtifactId;
                serviceActionId
              }
    let to_value x =
      structure_to_value
        [("ProductId", (Some (Id.to_value x.productId)));
        ("ProvisioningArtifactId",
          (Some (Id.to_value x.provisioningArtifactId)));
        ("ServiceActionId", (Some (Id.to_value x.serviceActionId)));
        ("AcceptLanguage",
          (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      let serviceActionId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceActionId") in
      let provisioningArtifactId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisioningArtifactId") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      make ?acceptLanguage ~serviceActionId ~provisioningArtifactId
        ~productId ()
    let of_json json =
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      let serviceActionId = field_map_exn json "ServiceActionId" Id.of_json in
      let provisioningArtifactId =
        field_map_exn json "ProvisioningArtifactId" Id.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      make ?acceptLanguage ~serviceActionId ~provisioningArtifactId
        ~productId ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateProductFromPortfolioOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateProductFromPortfolioInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      productId: Id.t ;
      portfolioId: Id.t }
    let context_ = "DisassociateProductFromPortfolioInput"
    let make ?acceptLanguage =
      fun ~productId ->
        fun ~portfolioId ->
          fun () -> { acceptLanguage; productId; portfolioId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProductId", (Some (Id.to_value x.productId)));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~portfolioId ~productId ?acceptLanguage ()
    let of_json json =
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~portfolioId ~productId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DisassociatePrincipalFromPortfolioOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DisassociatePrincipalFromPortfolioInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      portfolioId: Id.t ;
      principalARN: PrincipalARN.t }
    let context_ = "DisassociatePrincipalFromPortfolioInput"
    let make ?acceptLanguage =
      fun ~portfolioId ->
        fun ~principalARN ->
          fun () -> { acceptLanguage; portfolioId; principalARN }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("PrincipalARN", (Some (PrincipalARN.to_value x.principalARN)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let principalARN =
        PrincipalARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrincipalARN") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~principalARN ~portfolioId ?acceptLanguage ()
    let of_json json =
      let principalARN =
        field_map_exn json "PrincipalARN" PrincipalARN.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~principalARN ~portfolioId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateBudgetFromResourceOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateBudgetFromResourceInput =
  struct
    type nonrec t = {
      budgetName: BudgetName.t ;
      resourceId: Id.t }
    let context_ = "DisassociateBudgetFromResourceInput"
    let make ~budgetName =
      fun ~resourceId -> fun () -> { budgetName; resourceId }
    let to_value x =
      structure_to_value
        [("BudgetName", (Some (BudgetName.to_value x.budgetName)));
        ("ResourceId", (Some (Id.to_value x.resourceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      let budgetName =
        BudgetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BudgetName") in
      make ~resourceId ~budgetName ()
    let of_json json =
      let resourceId = field_map_exn json "ResourceId" Id.of_json in
      let budgetName = field_map_exn json "BudgetName" BudgetName.of_json in
      make ~resourceId ~budgetName ()
    let to_json v = composed_to_json to_value v
  end
module DisableAWSOrganizationsAccessOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `OperationNotSupportedException of OperationNotSupportedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `OperationNotSupportedException e ->
          `Assoc
            [("error", (`String "OperationNotSupportedException"));
            ("details", (OperationNotSupportedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DisableAWSOrganizationsAccessInput =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTagOptionOutput =
  struct
    type nonrec t = {
      tagOptionDetail: TagOptionDetail.t option }
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tagOptionDetail = fun () -> { tagOptionDetail }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TagOptionDetail",
           (Option.map x.tagOptionDetail ~f:TagOptionDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagOptionDetail =
        (Option.map ~f:TagOptionDetail.of_xml)
          (Xml.child xml_arg0 "TagOptionDetail") in
      make ?tagOptionDetail ()
    let of_json json =
      let tagOptionDetail =
        field_map json "TagOptionDetail" TagOptionDetail.of_json in
      make ?tagOptionDetail ()
    let to_json v = composed_to_json to_value v
  end
module DescribeTagOptionInput =
  struct
    type nonrec t = {
      id: TagOptionId.t }
    let context_ = "DescribeTagOptionInput"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("Id", (Some (TagOptionId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        TagOptionId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~id ()
    let of_json json =
      let id = field_map_exn json "Id" TagOptionId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end
module DescribeServiceActionOutput =
  struct
    type nonrec t = {
      serviceActionDetail: ServiceActionDetail.t option }
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?serviceActionDetail = fun () -> { serviceActionDetail }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ServiceActionDetail",
           (Option.map x.serviceActionDetail ~f:ServiceActionDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceActionDetail =
        (Option.map ~f:ServiceActionDetail.of_xml)
          (Xml.child xml_arg0 "ServiceActionDetail") in
      make ?serviceActionDetail ()
    let of_json json =
      let serviceActionDetail =
        field_map json "ServiceActionDetail" ServiceActionDetail.of_json in
      make ?serviceActionDetail ()
    let to_json v = composed_to_json to_value v
  end
module DescribeServiceActionInput =
  struct
    type nonrec t = {
      id: Id.t ;
      acceptLanguage: AcceptLanguage.t option }
    let context_ = "DescribeServiceActionInput"
    let make ?acceptLanguage = fun ~id -> fun () -> { acceptLanguage; id }
    let to_value x =
      structure_to_value
        [("Id", (Some (Id.to_value x.id)));
        ("AcceptLanguage",
          (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      let id = Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?acceptLanguage ~id ()
    let of_json json =
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      let id = field_map_exn json "Id" Id.of_json in
      make ?acceptLanguage ~id ()
    let to_json v = composed_to_json to_value v
  end
module DescribeServiceActionExecutionParametersOutput =
  struct
    type nonrec t = {
      serviceActionParameters: ExecutionParameters.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?serviceActionParameters = fun () -> { serviceActionParameters }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ServiceActionParameters",
           (Option.map x.serviceActionParameters
              ~f:ExecutionParameters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceActionParameters =
        (Option.map ~f:ExecutionParameters.of_xml)
          (Xml.child xml_arg0 "ServiceActionParameters") in
      make ?serviceActionParameters ()
    let of_json json =
      let serviceActionParameters =
        field_map json "ServiceActionParameters" ExecutionParameters.of_json in
      make ?serviceActionParameters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeServiceActionExecutionParametersInput =
  struct
    type nonrec t =
      {
      provisionedProductId: Id.t ;
      serviceActionId: Id.t ;
      acceptLanguage: AcceptLanguage.t option }
    let context_ = "DescribeServiceActionExecutionParametersInput"
    let make ?acceptLanguage =
      fun ~provisionedProductId ->
        fun ~serviceActionId ->
          fun () -> { acceptLanguage; provisionedProductId; serviceActionId }
    let to_value x =
      structure_to_value
        [("ProvisionedProductId",
           (Some (Id.to_value x.provisionedProductId)));
        ("ServiceActionId", (Some (Id.to_value x.serviceActionId)));
        ("AcceptLanguage",
          (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      let serviceActionId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceActionId") in
      let provisionedProductId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisionedProductId") in
      make ?acceptLanguage ~serviceActionId ~provisionedProductId ()
    let of_json json =
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      let serviceActionId = field_map_exn json "ServiceActionId" Id.of_json in
      let provisionedProductId =
        field_map_exn json "ProvisionedProductId" Id.of_json in
      make ?acceptLanguage ~serviceActionId ~provisionedProductId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeRecordOutput =
  struct
    type nonrec t =
      {
      recordDetail: RecordDetail.t option ;
      recordOutputs: RecordOutputs.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recordDetail =
      fun ?recordOutputs ->
        fun ?nextPageToken ->
          fun () -> { recordDetail; recordOutputs; nextPageToken }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RecordDetail",
           (Option.map x.recordDetail ~f:RecordDetail.to_value));
        ("RecordOutputs",
          (Option.map x.recordOutputs ~f:RecordOutputs.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let recordOutputs =
        (Option.map ~f:RecordOutputs.of_xml)
          (Xml.child xml_arg0 "RecordOutputs") in
      let recordDetail =
        (Option.map ~f:RecordDetail.of_xml)
          (Xml.child xml_arg0 "RecordDetail") in
      make ?nextPageToken ?recordOutputs ?recordDetail ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let recordOutputs =
        field_map json "RecordOutputs" RecordOutputs.of_json in
      let recordDetail = field_map json "RecordDetail" RecordDetail.of_json in
      make ?nextPageToken ?recordOutputs ?recordDetail ()
    let to_json v = composed_to_json to_value v
  end
module DescribeRecordInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      id: Id.t ;
      pageToken: PageToken.t option ;
      pageSize: PageSize.t option }
    let context_ = "DescribeRecordInput"
    let make ?acceptLanguage =
      fun ?pageToken ->
        fun ?pageSize ->
          fun ~id -> fun () -> { acceptLanguage; pageToken; pageSize; id }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Id", (Some (Id.to_value x.id)));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let id = Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageSize ?pageToken ~id ?acceptLanguage ()
    let of_json json =
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let id = field_map_exn json "Id" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageSize ?pageToken ~id ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProvisioningParametersOutput =
  struct
    type nonrec t =
      {
      provisioningArtifactParameters: ProvisioningArtifactParameters.t option ;
      constraintSummaries: ConstraintSummaries.t option ;
      usageInstructions: UsageInstructions.t option ;
      tagOptions: TagOptionSummaries.t option ;
      provisioningArtifactPreferences:
        ProvisioningArtifactPreferences.t option ;
      provisioningArtifactOutputs: ProvisioningArtifactOutputs.t option ;
      provisioningArtifactOutputKeys: ProvisioningArtifactOutputs.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?provisioningArtifactParameters =
      fun ?constraintSummaries ->
        fun ?usageInstructions ->
          fun ?tagOptions ->
            fun ?provisioningArtifactPreferences ->
              fun ?provisioningArtifactOutputs ->
                fun ?provisioningArtifactOutputKeys ->
                  fun () ->
                    {
                      provisioningArtifactParameters;
                      constraintSummaries;
                      usageInstructions;
                      tagOptions;
                      provisioningArtifactPreferences;
                      provisioningArtifactOutputs;
                      provisioningArtifactOutputKeys
                    }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProvisioningArtifactParameters",
           (Option.map x.provisioningArtifactParameters
              ~f:ProvisioningArtifactParameters.to_value));
        ("ConstraintSummaries",
          (Option.map x.constraintSummaries ~f:ConstraintSummaries.to_value));
        ("UsageInstructions",
          (Option.map x.usageInstructions ~f:UsageInstructions.to_value));
        ("TagOptions",
          (Option.map x.tagOptions ~f:TagOptionSummaries.to_value));
        ("ProvisioningArtifactPreferences",
          (Option.map x.provisioningArtifactPreferences
             ~f:ProvisioningArtifactPreferences.to_value));
        ("ProvisioningArtifactOutputs",
          (Option.map x.provisioningArtifactOutputs
             ~f:ProvisioningArtifactOutputs.to_value));
        ("ProvisioningArtifactOutputKeys",
          (Option.map x.provisioningArtifactOutputKeys
             ~f:ProvisioningArtifactOutputs.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let provisioningArtifactOutputKeys =
        (Option.map ~f:ProvisioningArtifactOutputs.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactOutputKeys") in
      let provisioningArtifactOutputs =
        (Option.map ~f:ProvisioningArtifactOutputs.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactOutputs") in
      let provisioningArtifactPreferences =
        (Option.map ~f:ProvisioningArtifactPreferences.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactPreferences") in
      let tagOptions =
        (Option.map ~f:TagOptionSummaries.of_xml)
          (Xml.child xml_arg0 "TagOptions") in
      let usageInstructions =
        (Option.map ~f:UsageInstructions.of_xml)
          (Xml.child xml_arg0 "UsageInstructions") in
      let constraintSummaries =
        (Option.map ~f:ConstraintSummaries.of_xml)
          (Xml.child xml_arg0 "ConstraintSummaries") in
      let provisioningArtifactParameters =
        (Option.map ~f:ProvisioningArtifactParameters.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactParameters") in
      make ?provisioningArtifactOutputKeys ?provisioningArtifactOutputs
        ?provisioningArtifactPreferences ?tagOptions ?usageInstructions
        ?constraintSummaries ?provisioningArtifactParameters ()
    let of_json json =
      let provisioningArtifactOutputKeys =
        field_map json "ProvisioningArtifactOutputKeys"
          ProvisioningArtifactOutputs.of_json in
      let provisioningArtifactOutputs =
        field_map json "ProvisioningArtifactOutputs"
          ProvisioningArtifactOutputs.of_json in
      let provisioningArtifactPreferences =
        field_map json "ProvisioningArtifactPreferences"
          ProvisioningArtifactPreferences.of_json in
      let tagOptions = field_map json "TagOptions" TagOptionSummaries.of_json in
      let usageInstructions =
        field_map json "UsageInstructions" UsageInstructions.of_json in
      let constraintSummaries =
        field_map json "ConstraintSummaries" ConstraintSummaries.of_json in
      let provisioningArtifactParameters =
        field_map json "ProvisioningArtifactParameters"
          ProvisioningArtifactParameters.of_json in
      make ?provisioningArtifactOutputKeys ?provisioningArtifactOutputs
        ?provisioningArtifactPreferences ?tagOptions ?usageInstructions
        ?constraintSummaries ?provisioningArtifactParameters ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProvisioningParametersInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      productId: Id.t option ;
      productName: ProductViewName.t option ;
      provisioningArtifactId: Id.t option ;
      provisioningArtifactName: ProvisioningArtifactName.t option ;
      pathId: Id.t option ;
      pathName: PortfolioDisplayName.t option }
    let make ?acceptLanguage =
      fun ?productId ->
        fun ?productName ->
          fun ?provisioningArtifactId ->
            fun ?provisioningArtifactName ->
              fun ?pathId ->
                fun ?pathName ->
                  fun () ->
                    {
                      acceptLanguage;
                      productId;
                      productName;
                      provisioningArtifactId;
                      provisioningArtifactName;
                      pathId;
                      pathName
                    }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProductId", (Option.map x.productId ~f:Id.to_value));
        ("ProductName",
          (Option.map x.productName ~f:ProductViewName.to_value));
        ("ProvisioningArtifactId",
          (Option.map x.provisioningArtifactId ~f:Id.to_value));
        ("ProvisioningArtifactName",
          (Option.map x.provisioningArtifactName
             ~f:ProvisioningArtifactName.to_value));
        ("PathId", (Option.map x.pathId ~f:Id.to_value));
        ("PathName",
          (Option.map x.pathName ~f:PortfolioDisplayName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pathName =
        (Option.map ~f:PortfolioDisplayName.of_xml)
          (Xml.child xml_arg0 "PathName") in
      let pathId = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PathId") in
      let provisioningArtifactName =
        (Option.map ~f:ProvisioningArtifactName.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactName") in
      let provisioningArtifactId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactId") in
      let productName =
        (Option.map ~f:ProductViewName.of_xml)
          (Xml.child xml_arg0 "ProductName") in
      let productId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pathName ?pathId ?provisioningArtifactName
        ?provisioningArtifactId ?productName ?productId ?acceptLanguage ()
    let of_json json =
      let pathName = field_map json "PathName" PortfolioDisplayName.of_json in
      let pathId = field_map json "PathId" Id.of_json in
      let provisioningArtifactName =
        field_map json "ProvisioningArtifactName"
          ProvisioningArtifactName.of_json in
      let provisioningArtifactId =
        field_map json "ProvisioningArtifactId" Id.of_json in
      let productName = field_map json "ProductName" ProductViewName.of_json in
      let productId = field_map json "ProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pathName ?pathId ?provisioningArtifactName
        ?provisioningArtifactId ?productName ?productId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProvisioningArtifactOutput =
  struct
    type nonrec t =
      {
      provisioningArtifactDetail: ProvisioningArtifactDetail.t option ;
      info: ProvisioningArtifactInfo.t option ;
      status: Status.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?provisioningArtifactDetail =
      fun ?info ->
        fun ?status -> fun () -> { provisioningArtifactDetail; info; status }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProvisioningArtifactDetail",
           (Option.map x.provisioningArtifactDetail
              ~f:ProvisioningArtifactDetail.to_value));
        ("Info", (Option.map x.info ~f:ProvisioningArtifactInfo.to_value));
        ("Status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "Status") in
      let info =
        (Option.map ~f:ProvisioningArtifactInfo.of_xml)
          (Xml.child xml_arg0 "Info") in
      let provisioningArtifactDetail =
        (Option.map ~f:ProvisioningArtifactDetail.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactDetail") in
      make ?status ?info ?provisioningArtifactDetail ()
    let of_json json =
      let status = field_map json "Status" Status.of_json in
      let info = field_map json "Info" ProvisioningArtifactInfo.of_json in
      let provisioningArtifactDetail =
        field_map json "ProvisioningArtifactDetail"
          ProvisioningArtifactDetail.of_json in
      make ?status ?info ?provisioningArtifactDetail ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProvisioningArtifactInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      provisioningArtifactId: Id.t option ;
      productId: Id.t option ;
      provisioningArtifactName: ProvisioningArtifactName.t option ;
      productName: ProductViewName.t option ;
      verbose: Verbose.t option }
    let make ?acceptLanguage =
      fun ?provisioningArtifactId ->
        fun ?productId ->
          fun ?provisioningArtifactName ->
            fun ?productName ->
              fun ?verbose ->
                fun () ->
                  {
                    acceptLanguage;
                    provisioningArtifactId;
                    productId;
                    provisioningArtifactName;
                    productName;
                    verbose
                  }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProvisioningArtifactId",
          (Option.map x.provisioningArtifactId ~f:Id.to_value));
        ("ProductId", (Option.map x.productId ~f:Id.to_value));
        ("ProvisioningArtifactName",
          (Option.map x.provisioningArtifactName
             ~f:ProvisioningArtifactName.to_value));
        ("ProductName",
          (Option.map x.productName ~f:ProductViewName.to_value));
        ("Verbose", (Option.map x.verbose ~f:Verbose.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let verbose =
        (Option.map ~f:Verbose.of_xml) (Xml.child xml_arg0 "Verbose") in
      let productName =
        (Option.map ~f:ProductViewName.of_xml)
          (Xml.child xml_arg0 "ProductName") in
      let provisioningArtifactName =
        (Option.map ~f:ProvisioningArtifactName.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactName") in
      let productId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProductId") in
      let provisioningArtifactId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?verbose ?productName ?provisioningArtifactName ?productId
        ?provisioningArtifactId ?acceptLanguage ()
    let of_json json =
      let verbose = field_map json "Verbose" Verbose.of_json in
      let productName = field_map json "ProductName" ProductViewName.of_json in
      let provisioningArtifactName =
        field_map json "ProvisioningArtifactName"
          ProvisioningArtifactName.of_json in
      let productId = field_map json "ProductId" Id.of_json in
      let provisioningArtifactId =
        field_map json "ProvisioningArtifactId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?verbose ?productName ?provisioningArtifactName ?productId
        ?provisioningArtifactId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProvisionedProductPlanOutput =
  struct
    type nonrec t =
      {
      provisionedProductPlanDetails: ProvisionedProductPlanDetails.t option ;
      resourceChanges: ResourceChanges.t option ;
      nextPageToken: PageToken.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?provisionedProductPlanDetails =
      fun ?resourceChanges ->
        fun ?nextPageToken ->
          fun () ->
            { provisionedProductPlanDetails; resourceChanges; nextPageToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProvisionedProductPlanDetails",
           (Option.map x.provisionedProductPlanDetails
              ~f:ProvisionedProductPlanDetails.to_value));
        ("ResourceChanges",
          (Option.map x.resourceChanges ~f:ResourceChanges.to_value));
        ("NextPageToken", (Option.map x.nextPageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      let resourceChanges =
        (Option.map ~f:ResourceChanges.of_xml)
          (Xml.child xml_arg0 "ResourceChanges") in
      let provisionedProductPlanDetails =
        (Option.map ~f:ProvisionedProductPlanDetails.of_xml)
          (Xml.child xml_arg0 "ProvisionedProductPlanDetails") in
      make ?nextPageToken ?resourceChanges ?provisionedProductPlanDetails ()
    let of_json json =
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      let resourceChanges =
        field_map json "ResourceChanges" ResourceChanges.of_json in
      let provisionedProductPlanDetails =
        field_map json "ProvisionedProductPlanDetails"
          ProvisionedProductPlanDetails.of_json in
      make ?nextPageToken ?resourceChanges ?provisionedProductPlanDetails ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProvisionedProductPlanInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      planId: Id.t ;
      pageSize: PageSize.t option ;
      pageToken: PageToken.t option }
    let context_ = "DescribeProvisionedProductPlanInput"
    let make ?acceptLanguage =
      fun ?pageSize ->
        fun ?pageToken ->
          fun ~planId ->
            fun () -> { acceptLanguage; pageSize; pageToken; planId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PlanId", (Some (Id.to_value x.planId)));
        ("PageSize", (Option.map x.pageSize ~f:PageSize.to_value));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let pageSize =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "PageSize") in
      let planId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PlanId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?pageToken ?pageSize ~planId ?acceptLanguage ()
    let of_json json =
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let pageSize = field_map json "PageSize" PageSize.of_json in
      let planId = field_map_exn json "PlanId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?pageToken ?pageSize ~planId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProvisionedProductOutput =
  struct
    type nonrec t =
      {
      provisionedProductDetail: ProvisionedProductDetail.t option ;
      cloudWatchDashboards: CloudWatchDashboards.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?provisionedProductDetail =
      fun ?cloudWatchDashboards ->
        fun () -> { provisionedProductDetail; cloudWatchDashboards }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProvisionedProductDetail",
           (Option.map x.provisionedProductDetail
              ~f:ProvisionedProductDetail.to_value));
        ("CloudWatchDashboards",
          (Option.map x.cloudWatchDashboards ~f:CloudWatchDashboards.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cloudWatchDashboards =
        (Option.map ~f:CloudWatchDashboards.of_xml)
          (Xml.child xml_arg0 "CloudWatchDashboards") in
      let provisionedProductDetail =
        (Option.map ~f:ProvisionedProductDetail.of_xml)
          (Xml.child xml_arg0 "ProvisionedProductDetail") in
      make ?cloudWatchDashboards ?provisionedProductDetail ()
    let of_json json =
      let cloudWatchDashboards =
        field_map json "CloudWatchDashboards" CloudWatchDashboards.of_json in
      let provisionedProductDetail =
        field_map json "ProvisionedProductDetail"
          ProvisionedProductDetail.of_json in
      make ?cloudWatchDashboards ?provisionedProductDetail ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProvisionedProductInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      id: Id.t option ;
      name: ProvisionedProductName.t option }
    let make ?acceptLanguage =
      fun ?id -> fun ?name -> fun () -> { acceptLanguage; id; name }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Id", (Option.map x.id ~f:Id.to_value));
        ("Name", (Option.map x.name ~f:ProvisionedProductName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:ProvisionedProductName.of_xml)
          (Xml.child xml_arg0 "Name") in
      let id = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "Id") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?name ?id ?acceptLanguage ()
    let of_json json =
      let name = field_map json "Name" ProvisionedProductName.of_json in
      let id = field_map json "Id" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?name ?id ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProductViewOutput =
  struct
    type nonrec t =
      {
      productViewSummary: ProductViewSummary.t option ;
      provisioningArtifacts: ProvisioningArtifacts.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?productViewSummary =
      fun ?provisioningArtifacts ->
        fun () -> { productViewSummary; provisioningArtifacts }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProductViewSummary",
           (Option.map x.productViewSummary ~f:ProductViewSummary.to_value));
        ("ProvisioningArtifacts",
          (Option.map x.provisioningArtifacts
             ~f:ProvisioningArtifacts.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let provisioningArtifacts =
        (Option.map ~f:ProvisioningArtifacts.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifacts") in
      let productViewSummary =
        (Option.map ~f:ProductViewSummary.of_xml)
          (Xml.child xml_arg0 "ProductViewSummary") in
      make ?provisioningArtifacts ?productViewSummary ()
    let of_json json =
      let provisioningArtifacts =
        field_map json "ProvisioningArtifacts" ProvisioningArtifacts.of_json in
      let productViewSummary =
        field_map json "ProductViewSummary" ProductViewSummary.of_json in
      make ?provisioningArtifacts ?productViewSummary ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProductViewInput =
  struct
    type nonrec t = {
      acceptLanguage: AcceptLanguage.t option ;
      id: Id.t }
    let context_ = "DescribeProductViewInput"
    let make ?acceptLanguage = fun ~id -> fun () -> { acceptLanguage; id }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Id", (Some (Id.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~id ?acceptLanguage ()
    let of_json json =
      let id = field_map_exn json "Id" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~id ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProductOutput =
  struct
    type nonrec t =
      {
      productViewSummary: ProductViewSummary.t option ;
      provisioningArtifacts: ProvisioningArtifacts.t option ;
      budgets: Budgets.t option ;
      launchPaths: LaunchPaths.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?productViewSummary =
      fun ?provisioningArtifacts ->
        fun ?budgets ->
          fun ?launchPaths ->
            fun () ->
              {
                productViewSummary;
                provisioningArtifacts;
                budgets;
                launchPaths
              }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProductViewSummary",
           (Option.map x.productViewSummary ~f:ProductViewSummary.to_value));
        ("ProvisioningArtifacts",
          (Option.map x.provisioningArtifacts
             ~f:ProvisioningArtifacts.to_value));
        ("Budgets", (Option.map x.budgets ~f:Budgets.to_value));
        ("LaunchPaths", (Option.map x.launchPaths ~f:LaunchPaths.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchPaths =
        (Option.map ~f:LaunchPaths.of_xml) (Xml.child xml_arg0 "LaunchPaths") in
      let budgets =
        (Option.map ~f:Budgets.of_xml) (Xml.child xml_arg0 "Budgets") in
      let provisioningArtifacts =
        (Option.map ~f:ProvisioningArtifacts.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifacts") in
      let productViewSummary =
        (Option.map ~f:ProductViewSummary.of_xml)
          (Xml.child xml_arg0 "ProductViewSummary") in
      make ?launchPaths ?budgets ?provisioningArtifacts ?productViewSummary
        ()
    let of_json json =
      let launchPaths = field_map json "LaunchPaths" LaunchPaths.of_json in
      let budgets = field_map json "Budgets" Budgets.of_json in
      let provisioningArtifacts =
        field_map json "ProvisioningArtifacts" ProvisioningArtifacts.of_json in
      let productViewSummary =
        field_map json "ProductViewSummary" ProductViewSummary.of_json in
      make ?launchPaths ?budgets ?provisioningArtifacts ?productViewSummary
        ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProductInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      id: Id.t option ;
      name: ProductViewName.t option }
    let make ?acceptLanguage =
      fun ?id -> fun ?name -> fun () -> { acceptLanguage; id; name }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Id", (Option.map x.id ~f:Id.to_value));
        ("Name", (Option.map x.name ~f:ProductViewName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:ProductViewName.of_xml) (Xml.child xml_arg0 "Name") in
      let id = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "Id") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?name ?id ?acceptLanguage ()
    let of_json json =
      let name = field_map json "Name" ProductViewName.of_json in
      let id = field_map json "Id" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?name ?id ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProductAsAdminOutput =
  struct
    type nonrec t =
      {
      productViewDetail: ProductViewDetail.t option ;
      provisioningArtifactSummaries: ProvisioningArtifactSummaries.t option ;
      tags: Tags.t option ;
      tagOptions: TagOptionDetails.t option ;
      budgets: Budgets.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?productViewDetail =
      fun ?provisioningArtifactSummaries ->
        fun ?tags ->
          fun ?tagOptions ->
            fun ?budgets ->
              fun () ->
                {
                  productViewDetail;
                  provisioningArtifactSummaries;
                  tags;
                  tagOptions;
                  budgets
                }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProductViewDetail",
           (Option.map x.productViewDetail ~f:ProductViewDetail.to_value));
        ("ProvisioningArtifactSummaries",
          (Option.map x.provisioningArtifactSummaries
             ~f:ProvisioningArtifactSummaries.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("TagOptions",
          (Option.map x.tagOptions ~f:TagOptionDetails.to_value));
        ("Budgets", (Option.map x.budgets ~f:Budgets.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let budgets =
        (Option.map ~f:Budgets.of_xml) (Xml.child xml_arg0 "Budgets") in
      let tagOptions =
        (Option.map ~f:TagOptionDetails.of_xml)
          (Xml.child xml_arg0 "TagOptions") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let provisioningArtifactSummaries =
        (Option.map ~f:ProvisioningArtifactSummaries.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactSummaries") in
      let productViewDetail =
        (Option.map ~f:ProductViewDetail.of_xml)
          (Xml.child xml_arg0 "ProductViewDetail") in
      make ?budgets ?tagOptions ?tags ?provisioningArtifactSummaries
        ?productViewDetail ()
    let of_json json =
      let budgets = field_map json "Budgets" Budgets.of_json in
      let tagOptions = field_map json "TagOptions" TagOptionDetails.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let provisioningArtifactSummaries =
        field_map json "ProvisioningArtifactSummaries"
          ProvisioningArtifactSummaries.of_json in
      let productViewDetail =
        field_map json "ProductViewDetail" ProductViewDetail.of_json in
      make ?budgets ?tagOptions ?tags ?provisioningArtifactSummaries
        ?productViewDetail ()
    let to_json v = composed_to_json to_value v
  end
module DescribeProductAsAdminInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      id: Id.t option ;
      name: ProductViewName.t option ;
      sourcePortfolioId: Id.t option }
    let make ?acceptLanguage =
      fun ?id ->
        fun ?name ->
          fun ?sourcePortfolioId ->
            fun () -> { acceptLanguage; id; name; sourcePortfolioId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Id", (Option.map x.id ~f:Id.to_value));
        ("Name", (Option.map x.name ~f:ProductViewName.to_value));
        ("SourcePortfolioId",
          (Option.map x.sourcePortfolioId ~f:Id.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourcePortfolioId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "SourcePortfolioId") in
      let name =
        (Option.map ~f:ProductViewName.of_xml) (Xml.child xml_arg0 "Name") in
      let id = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "Id") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?sourcePortfolioId ?name ?id ?acceptLanguage ()
    let of_json json =
      let sourcePortfolioId = field_map json "SourcePortfolioId" Id.of_json in
      let name = field_map json "Name" ProductViewName.of_json in
      let id = field_map json "Id" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?sourcePortfolioId ?name ?id ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DescribePortfolioSharesOutput =
  struct
    type nonrec t =
      {
      nextPageToken: PageToken.t option ;
      portfolioShareDetails: PortfolioShareDetails.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextPageToken =
      fun ?portfolioShareDetails ->
        fun () -> { nextPageToken; portfolioShareDetails }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NextPageToken",
           (Option.map x.nextPageToken ~f:PageToken.to_value));
        ("PortfolioShareDetails",
          (Option.map x.portfolioShareDetails
             ~f:PortfolioShareDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let portfolioShareDetails =
        (Option.map ~f:PortfolioShareDetails.of_xml)
          (Xml.child xml_arg0 "PortfolioShareDetails") in
      let nextPageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextPageToken") in
      make ?portfolioShareDetails ?nextPageToken ()
    let of_json json =
      let portfolioShareDetails =
        field_map json "PortfolioShareDetails" PortfolioShareDetails.of_json in
      let nextPageToken = field_map json "NextPageToken" PageToken.of_json in
      make ?portfolioShareDetails ?nextPageToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribePortfolioSharesInput =
  struct
    type nonrec t =
      {
      portfolioId: Id.t ;
      type_: DescribePortfolioShareType.t ;
      pageToken: PageToken.t option ;
      pageSize: PageSizeMax100.t option }
    let context_ = "DescribePortfolioSharesInput"
    let make ?pageToken =
      fun ?pageSize ->
        fun ~portfolioId ->
          fun ~type_ -> fun () -> { pageToken; pageSize; portfolioId; type_ }
    let to_value x =
      structure_to_value
        [("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("Type", (Some (DescribePortfolioShareType.to_value x.type_)));
        ("PageToken", (Option.map x.pageToken ~f:PageToken.to_value));
        ("PageSize", (Option.map x.pageSize ~f:PageSizeMax100.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageSize =
        (Option.map ~f:PageSizeMax100.of_xml) (Xml.child xml_arg0 "PageSize") in
      let pageToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "PageToken") in
      let type_ =
        DescribePortfolioShareType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      make ?pageSize ?pageToken ~type_ ~portfolioId ()
    let of_json json =
      let pageSize = field_map json "PageSize" PageSizeMax100.of_json in
      let pageToken = field_map json "PageToken" PageToken.of_json in
      let type_ =
        field_map_exn json "Type" DescribePortfolioShareType.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      make ?pageSize ?pageToken ~type_ ~portfolioId ()
    let to_json v = composed_to_json to_value v
  end
module DescribePortfolioShareStatusOutput =
  struct
    type nonrec t =
      {
      portfolioShareToken: Id.t option ;
      portfolioId: Id.t option ;
      organizationNodeValue: OrganizationNodeValue.t option ;
      status: ShareStatus.t option ;
      shareDetails: ShareDetails.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `OperationNotSupportedException of OperationNotSupportedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?portfolioShareToken =
      fun ?portfolioId ->
        fun ?organizationNodeValue ->
          fun ?status ->
            fun ?shareDetails ->
              fun () ->
                {
                  portfolioShareToken;
                  portfolioId;
                  organizationNodeValue;
                  status;
                  shareDetails
                }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `OperationNotSupportedException e ->
          `Assoc
            [("error", (`String "OperationNotSupportedException"));
            ("details", (OperationNotSupportedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PortfolioShareToken",
           (Option.map x.portfolioShareToken ~f:Id.to_value));
        ("PortfolioId", (Option.map x.portfolioId ~f:Id.to_value));
        ("OrganizationNodeValue",
          (Option.map x.organizationNodeValue
             ~f:OrganizationNodeValue.to_value));
        ("Status", (Option.map x.status ~f:ShareStatus.to_value));
        ("ShareDetails",
          (Option.map x.shareDetails ~f:ShareDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shareDetails =
        (Option.map ~f:ShareDetails.of_xml)
          (Xml.child xml_arg0 "ShareDetails") in
      let status =
        (Option.map ~f:ShareStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let organizationNodeValue =
        (Option.map ~f:OrganizationNodeValue.of_xml)
          (Xml.child xml_arg0 "OrganizationNodeValue") in
      let portfolioId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PortfolioId") in
      let portfolioShareToken =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PortfolioShareToken") in
      make ?shareDetails ?status ?organizationNodeValue ?portfolioId
        ?portfolioShareToken ()
    let of_json json =
      let shareDetails = field_map json "ShareDetails" ShareDetails.of_json in
      let status = field_map json "Status" ShareStatus.of_json in
      let organizationNodeValue =
        field_map json "OrganizationNodeValue" OrganizationNodeValue.of_json in
      let portfolioId = field_map json "PortfolioId" Id.of_json in
      let portfolioShareToken =
        field_map json "PortfolioShareToken" Id.of_json in
      make ?shareDetails ?status ?organizationNodeValue ?portfolioId
        ?portfolioShareToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribePortfolioShareStatusInput =
  struct
    type nonrec t = {
      portfolioShareToken: Id.t }
    let context_ = "DescribePortfolioShareStatusInput"
    let make ~portfolioShareToken = fun () -> { portfolioShareToken }
    let to_value x =
      structure_to_value
        [("PortfolioShareToken", (Some (Id.to_value x.portfolioShareToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let portfolioShareToken =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PortfolioShareToken") in
      make ~portfolioShareToken ()
    let of_json json =
      let portfolioShareToken =
        field_map_exn json "PortfolioShareToken" Id.of_json in
      make ~portfolioShareToken ()
    let to_json v = composed_to_json to_value v
  end
module DescribePortfolioOutput =
  struct
    type nonrec t =
      {
      portfolioDetail: PortfolioDetail.t option ;
      tags: Tags.t option ;
      tagOptions: TagOptionDetails.t option ;
      budgets: Budgets.t option }
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?portfolioDetail =
      fun ?tags ->
        fun ?tagOptions ->
          fun ?budgets ->
            fun () -> { portfolioDetail; tags; tagOptions; budgets }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PortfolioDetail",
           (Option.map x.portfolioDetail ~f:PortfolioDetail.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("TagOptions",
          (Option.map x.tagOptions ~f:TagOptionDetails.to_value));
        ("Budgets", (Option.map x.budgets ~f:Budgets.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let budgets =
        (Option.map ~f:Budgets.of_xml) (Xml.child xml_arg0 "Budgets") in
      let tagOptions =
        (Option.map ~f:TagOptionDetails.of_xml)
          (Xml.child xml_arg0 "TagOptions") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let portfolioDetail =
        (Option.map ~f:PortfolioDetail.of_xml)
          (Xml.child xml_arg0 "PortfolioDetail") in
      make ?budgets ?tagOptions ?tags ?portfolioDetail ()
    let of_json json =
      let budgets = field_map json "Budgets" Budgets.of_json in
      let tagOptions = field_map json "TagOptions" TagOptionDetails.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let portfolioDetail =
        field_map json "PortfolioDetail" PortfolioDetail.of_json in
      make ?budgets ?tagOptions ?tags ?portfolioDetail ()
    let to_json v = composed_to_json to_value v
  end
module DescribePortfolioInput =
  struct
    type nonrec t = {
      acceptLanguage: AcceptLanguage.t option ;
      id: Id.t }
    let context_ = "DescribePortfolioInput"
    let make ?acceptLanguage = fun ~id -> fun () -> { acceptLanguage; id }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Id", (Some (Id.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~id ?acceptLanguage ()
    let of_json json =
      let id = field_map_exn json "Id" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~id ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DescribeCopyProductStatusOutput =
  struct
    type nonrec t =
      {
      copyProductStatus: CopyProductStatus.t option ;
      targetProductId: Id.t option ;
      statusDetail: StatusDetail.t option }
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?copyProductStatus =
      fun ?targetProductId ->
        fun ?statusDetail ->
          fun () -> { copyProductStatus; targetProductId; statusDetail }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("CopyProductStatus",
           (Option.map x.copyProductStatus ~f:CopyProductStatus.to_value));
        ("TargetProductId", (Option.map x.targetProductId ~f:Id.to_value));
        ("StatusDetail",
          (Option.map x.statusDetail ~f:StatusDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statusDetail =
        (Option.map ~f:StatusDetail.of_xml)
          (Xml.child xml_arg0 "StatusDetail") in
      let targetProductId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "TargetProductId") in
      let copyProductStatus =
        (Option.map ~f:CopyProductStatus.of_xml)
          (Xml.child xml_arg0 "CopyProductStatus") in
      make ?statusDetail ?targetProductId ?copyProductStatus ()
    let of_json json =
      let statusDetail = field_map json "StatusDetail" StatusDetail.of_json in
      let targetProductId = field_map json "TargetProductId" Id.of_json in
      let copyProductStatus =
        field_map json "CopyProductStatus" CopyProductStatus.of_json in
      make ?statusDetail ?targetProductId ?copyProductStatus ()
    let to_json v = composed_to_json to_value v
  end
module DescribeCopyProductStatusInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      copyProductToken: Id.t }
    let context_ = "DescribeCopyProductStatusInput"
    let make ?acceptLanguage =
      fun ~copyProductToken -> fun () -> { acceptLanguage; copyProductToken }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("CopyProductToken", (Some (Id.to_value x.copyProductToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let copyProductToken =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CopyProductToken") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~copyProductToken ?acceptLanguage ()
    let of_json json =
      let copyProductToken = field_map_exn json "CopyProductToken" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~copyProductToken ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DescribeConstraintOutput =
  struct
    type nonrec t =
      {
      constraintDetail: ConstraintDetail.t option ;
      constraintParameters: ConstraintParameters.t option ;
      status: Status.t option }
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?constraintDetail =
      fun ?constraintParameters ->
        fun ?status ->
          fun () -> { constraintDetail; constraintParameters; status }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ConstraintDetail",
           (Option.map x.constraintDetail ~f:ConstraintDetail.to_value));
        ("ConstraintParameters",
          (Option.map x.constraintParameters ~f:ConstraintParameters.to_value));
        ("Status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "Status") in
      let constraintParameters =
        (Option.map ~f:ConstraintParameters.of_xml)
          (Xml.child xml_arg0 "ConstraintParameters") in
      let constraintDetail =
        (Option.map ~f:ConstraintDetail.of_xml)
          (Xml.child xml_arg0 "ConstraintDetail") in
      make ?status ?constraintParameters ?constraintDetail ()
    let of_json json =
      let status = field_map json "Status" Status.of_json in
      let constraintParameters =
        field_map json "ConstraintParameters" ConstraintParameters.of_json in
      let constraintDetail =
        field_map json "ConstraintDetail" ConstraintDetail.of_json in
      make ?status ?constraintParameters ?constraintDetail ()
    let to_json v = composed_to_json to_value v
  end
module DescribeConstraintInput =
  struct
    type nonrec t = {
      acceptLanguage: AcceptLanguage.t option ;
      id: Id.t }
    let context_ = "DescribeConstraintInput"
    let make ?acceptLanguage = fun ~id -> fun () -> { acceptLanguage; id }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Id", (Some (Id.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~id ?acceptLanguage ()
    let of_json json =
      let id = field_map_exn json "Id" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~id ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTagOptionOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteTagOptionInput =
  struct
    type nonrec t = {
      id: TagOptionId.t }
    let context_ = "DeleteTagOptionInput"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("Id", (Some (TagOptionId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        TagOptionId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~id ()
    let of_json json =
      let id = field_map_exn json "Id" TagOptionId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end
module DeleteServiceActionOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteServiceActionInput =
  struct
    type nonrec t = {
      id: Id.t ;
      acceptLanguage: AcceptLanguage.t option }
    let context_ = "DeleteServiceActionInput"
    let make ?acceptLanguage = fun ~id -> fun () -> { acceptLanguage; id }
    let to_value x =
      structure_to_value
        [("Id", (Some (Id.to_value x.id)));
        ("AcceptLanguage",
          (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      let id = Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?acceptLanguage ~id ()
    let of_json json =
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      let id = field_map_exn json "Id" Id.of_json in
      make ?acceptLanguage ~id ()
    let to_json v = composed_to_json to_value v
  end
module DeleteProvisioningArtifactOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteProvisioningArtifactInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      productId: Id.t ;
      provisioningArtifactId: Id.t }
    let context_ = "DeleteProvisioningArtifactInput"
    let make ?acceptLanguage =
      fun ~productId ->
        fun ~provisioningArtifactId ->
          fun () -> { acceptLanguage; productId; provisioningArtifactId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProductId", (Some (Id.to_value x.productId)));
        ("ProvisioningArtifactId",
          (Some (Id.to_value x.provisioningArtifactId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let provisioningArtifactId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisioningArtifactId") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~provisioningArtifactId ~productId ?acceptLanguage ()
    let of_json json =
      let provisioningArtifactId =
        field_map_exn json "ProvisioningArtifactId" Id.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~provisioningArtifactId ~productId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DeleteProvisionedProductPlanOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteProvisionedProductPlanInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      planId: Id.t ;
      ignoreErrors: IgnoreErrors.t option }
    let context_ = "DeleteProvisionedProductPlanInput"
    let make ?acceptLanguage =
      fun ?ignoreErrors ->
        fun ~planId -> fun () -> { acceptLanguage; ignoreErrors; planId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PlanId", (Some (Id.to_value x.planId)));
        ("IgnoreErrors",
          (Option.map x.ignoreErrors ~f:IgnoreErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ignoreErrors =
        (Option.map ~f:IgnoreErrors.of_xml)
          (Xml.child xml_arg0 "IgnoreErrors") in
      let planId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PlanId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?ignoreErrors ~planId ?acceptLanguage ()
    let of_json json =
      let ignoreErrors = field_map json "IgnoreErrors" IgnoreErrors.of_json in
      let planId = field_map_exn json "PlanId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?ignoreErrors ~planId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DeleteProductOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteProductInput =
  struct
    type nonrec t = {
      acceptLanguage: AcceptLanguage.t option ;
      id: Id.t }
    let context_ = "DeleteProductInput"
    let make ?acceptLanguage = fun ~id -> fun () -> { acceptLanguage; id }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Id", (Some (Id.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~id ?acceptLanguage ()
    let of_json json =
      let id = field_map_exn json "Id" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~id ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DeletePortfolioShareOutput =
  struct
    type nonrec t = {
      portfolioShareToken: Id.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `InvalidStateException of InvalidStateException.t 
      | `OperationNotSupportedException of OperationNotSupportedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?portfolioShareToken = fun () -> { portfolioShareToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `OperationNotSupportedException e ->
          `Assoc
            [("error", (`String "OperationNotSupportedException"));
            ("details", (OperationNotSupportedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PortfolioShareToken",
           (Option.map x.portfolioShareToken ~f:Id.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let portfolioShareToken =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PortfolioShareToken") in
      make ?portfolioShareToken ()
    let of_json json =
      let portfolioShareToken =
        field_map json "PortfolioShareToken" Id.of_json in
      make ?portfolioShareToken ()
    let to_json v = composed_to_json to_value v
  end
module DeletePortfolioShareInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      portfolioId: Id.t ;
      accountId: AccountId.t option ;
      organizationNode: OrganizationNode.t option }
    let context_ = "DeletePortfolioShareInput"
    let make ?acceptLanguage =
      fun ?accountId ->
        fun ?organizationNode ->
          fun ~portfolioId ->
            fun () ->
              { acceptLanguage; accountId; organizationNode; portfolioId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("AccountId", (Option.map x.accountId ~f:AccountId.to_value));
        ("OrganizationNode",
          (Option.map x.organizationNode ~f:OrganizationNode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let organizationNode =
        (Option.map ~f:OrganizationNode.of_xml)
          (Xml.child xml_arg0 "OrganizationNode") in
      let accountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "AccountId") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?organizationNode ?accountId ~portfolioId ?acceptLanguage ()
    let of_json json =
      let organizationNode =
        field_map json "OrganizationNode" OrganizationNode.of_json in
      let accountId = field_map json "AccountId" AccountId.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?organizationNode ?accountId ~portfolioId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DeletePortfolioOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeletePortfolioInput =
  struct
    type nonrec t = {
      acceptLanguage: AcceptLanguage.t option ;
      id: Id.t }
    let context_ = "DeletePortfolioInput"
    let make ?acceptLanguage = fun ~id -> fun () -> { acceptLanguage; id }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Id", (Some (Id.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~id ?acceptLanguage ()
    let of_json json =
      let id = field_map_exn json "Id" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~id ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module DeleteConstraintOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteConstraintInput =
  struct
    type nonrec t = {
      acceptLanguage: AcceptLanguage.t option ;
      id: Id.t }
    let context_ = "DeleteConstraintInput"
    let make ?acceptLanguage = fun ~id -> fun () -> { acceptLanguage; id }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Id", (Some (Id.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~id ?acceptLanguage ()
    let of_json json =
      let id = field_map_exn json "Id" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~id ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module CreateTagOptionOutput =
  struct
    type nonrec t = {
      tagOptionDetail: TagOptionDetail.t option }
    type nonrec error =
      [ `DuplicateResourceException of DuplicateResourceException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tagOptionDetail = fun () -> { tagOptionDetail }
    let error_of_json name json =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException
            (DuplicateResourceException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException (DuplicateResourceException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DuplicateResourceException e ->
          `Assoc
            [("error", (`String "DuplicateResourceException"));
            ("details", (DuplicateResourceException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TagOptionDetail",
           (Option.map x.tagOptionDetail ~f:TagOptionDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagOptionDetail =
        (Option.map ~f:TagOptionDetail.of_xml)
          (Xml.child xml_arg0 "TagOptionDetail") in
      make ?tagOptionDetail ()
    let of_json json =
      let tagOptionDetail =
        field_map json "TagOptionDetail" TagOptionDetail.of_json in
      make ?tagOptionDetail ()
    let to_json v = composed_to_json to_value v
  end
module CreateTagOptionInput =
  struct
    type nonrec t = {
      key: TagOptionKey.t ;
      value: TagOptionValue.t }
    let context_ = "CreateTagOptionInput"
    let make ~key = fun ~value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Some (TagOptionKey.to_value x.key)));
        ("Value", (Some (TagOptionValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        TagOptionValue.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let key =
        TagOptionKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Key") in
      make ~value ~key ()
    let of_json json =
      let value = field_map_exn json "Value" TagOptionValue.of_json in
      let key = field_map_exn json "Key" TagOptionKey.of_json in
      make ~value ~key ()
    let to_json v = composed_to_json to_value v
  end
module CreateServiceActionOutput =
  struct
    type nonrec t = {
      serviceActionDetail: ServiceActionDetail.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?serviceActionDetail = fun () -> { serviceActionDetail }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ServiceActionDetail",
           (Option.map x.serviceActionDetail ~f:ServiceActionDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceActionDetail =
        (Option.map ~f:ServiceActionDetail.of_xml)
          (Xml.child xml_arg0 "ServiceActionDetail") in
      make ?serviceActionDetail ()
    let of_json json =
      let serviceActionDetail =
        field_map json "ServiceActionDetail" ServiceActionDetail.of_json in
      make ?serviceActionDetail ()
    let to_json v = composed_to_json to_value v
  end
module CreateServiceActionInput =
  struct
    type nonrec t =
      {
      name: ServiceActionName.t ;
      definitionType: ServiceActionDefinitionType.t ;
      definition: ServiceActionDefinitionMap.t ;
      description: ServiceActionDescription.t option ;
      acceptLanguage: AcceptLanguage.t option ;
      idempotencyToken: IdempotencyToken.t }
    let context_ = "CreateServiceActionInput"
    let make ?description =
      fun ?acceptLanguage ->
        fun ~name ->
          fun ~definitionType ->
            fun ~definition ->
              fun ~idempotencyToken ->
                fun () ->
                  {
                    description;
                    acceptLanguage;
                    name;
                    definitionType;
                    definition;
                    idempotencyToken
                  }
    let to_value x =
      structure_to_value
        [("Name", (Some (ServiceActionName.to_value x.name)));
        ("DefinitionType",
          (Some (ServiceActionDefinitionType.to_value x.definitionType)));
        ("Definition",
          (Some (ServiceActionDefinitionMap.to_value x.definition)));
        ("Description",
          (Option.map x.description ~f:ServiceActionDescription.to_value));
        ("AcceptLanguage",
          (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("IdempotencyToken",
          (Some (IdempotencyToken.to_value x.idempotencyToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdempotencyToken") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      let description =
        (Option.map ~f:ServiceActionDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let definition =
        ServiceActionDefinitionMap.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Definition") in
      let definitionType =
        ServiceActionDefinitionType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DefinitionType") in
      let name =
        ServiceActionName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~idempotencyToken ?acceptLanguage ?description ~definition
        ~definitionType ~name ()
    let of_json json =
      let idempotencyToken =
        field_map_exn json "IdempotencyToken" IdempotencyToken.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      let description =
        field_map json "Description" ServiceActionDescription.of_json in
      let definition =
        field_map_exn json "Definition" ServiceActionDefinitionMap.of_json in
      let definitionType =
        field_map_exn json "DefinitionType"
          ServiceActionDefinitionType.of_json in
      let name = field_map_exn json "Name" ServiceActionName.of_json in
      make ~idempotencyToken ?acceptLanguage ?description ~definition
        ~definitionType ~name ()
    let to_json v = composed_to_json to_value v
  end
module CreateProvisioningArtifactOutput =
  struct
    type nonrec t =
      {
      provisioningArtifactDetail: ProvisioningArtifactDetail.t option ;
      info: ProvisioningArtifactInfo.t option ;
      status: Status.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?provisioningArtifactDetail =
      fun ?info ->
        fun ?status -> fun () -> { provisioningArtifactDetail; info; status }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProvisioningArtifactDetail",
           (Option.map x.provisioningArtifactDetail
              ~f:ProvisioningArtifactDetail.to_value));
        ("Info", (Option.map x.info ~f:ProvisioningArtifactInfo.to_value));
        ("Status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "Status") in
      let info =
        (Option.map ~f:ProvisioningArtifactInfo.of_xml)
          (Xml.child xml_arg0 "Info") in
      let provisioningArtifactDetail =
        (Option.map ~f:ProvisioningArtifactDetail.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactDetail") in
      make ?status ?info ?provisioningArtifactDetail ()
    let of_json json =
      let status = field_map json "Status" Status.of_json in
      let info = field_map json "Info" ProvisioningArtifactInfo.of_json in
      let provisioningArtifactDetail =
        field_map json "ProvisioningArtifactDetail"
          ProvisioningArtifactDetail.of_json in
      make ?status ?info ?provisioningArtifactDetail ()
    let to_json v = composed_to_json to_value v
  end
module CreateProvisioningArtifactInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      productId: Id.t ;
      parameters: ProvisioningArtifactProperties.t ;
      idempotencyToken: IdempotencyToken.t }
    let context_ = "CreateProvisioningArtifactInput"
    let make ?acceptLanguage =
      fun ~productId ->
        fun ~parameters ->
          fun ~idempotencyToken ->
            fun () ->
              { acceptLanguage; productId; parameters; idempotencyToken }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProductId", (Some (Id.to_value x.productId)));
        ("Parameters",
          (Some (ProvisioningArtifactProperties.to_value x.parameters)));
        ("IdempotencyToken",
          (Some (IdempotencyToken.to_value x.idempotencyToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdempotencyToken") in
      let parameters =
        ProvisioningArtifactProperties.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Parameters") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~idempotencyToken ~parameters ~productId ?acceptLanguage ()
    let of_json json =
      let idempotencyToken =
        field_map_exn json "IdempotencyToken" IdempotencyToken.of_json in
      let parameters =
        field_map_exn json "Parameters"
          ProvisioningArtifactProperties.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~idempotencyToken ~parameters ~productId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module CreateProvisionedProductPlanOutput =
  struct
    type nonrec t =
      {
      planName: ProvisionedProductPlanName.t option ;
      planId: Id.t option ;
      provisionProductId: Id.t option ;
      provisionedProductName: ProvisionedProductName.t option ;
      provisioningArtifactId: Id.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?planName =
      fun ?planId ->
        fun ?provisionProductId ->
          fun ?provisionedProductName ->
            fun ?provisioningArtifactId ->
              fun () ->
                {
                  planName;
                  planId;
                  provisionProductId;
                  provisionedProductName;
                  provisioningArtifactId
                }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PlanName",
           (Option.map x.planName ~f:ProvisionedProductPlanName.to_value));
        ("PlanId", (Option.map x.planId ~f:Id.to_value));
        ("ProvisionProductId",
          (Option.map x.provisionProductId ~f:Id.to_value));
        ("ProvisionedProductName",
          (Option.map x.provisionedProductName
             ~f:ProvisionedProductName.to_value));
        ("ProvisioningArtifactId",
          (Option.map x.provisioningArtifactId ~f:Id.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let provisioningArtifactId =
        (Option.map ~f:Id.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactId") in
      let provisionedProductName =
        (Option.map ~f:ProvisionedProductName.of_xml)
          (Xml.child xml_arg0 "ProvisionedProductName") in
      let provisionProductId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "ProvisionProductId") in
      let planId = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PlanId") in
      let planName =
        (Option.map ~f:ProvisionedProductPlanName.of_xml)
          (Xml.child xml_arg0 "PlanName") in
      make ?provisioningArtifactId ?provisionedProductName
        ?provisionProductId ?planId ?planName ()
    let of_json json =
      let provisioningArtifactId =
        field_map json "ProvisioningArtifactId" Id.of_json in
      let provisionedProductName =
        field_map json "ProvisionedProductName"
          ProvisionedProductName.of_json in
      let provisionProductId = field_map json "ProvisionProductId" Id.of_json in
      let planId = field_map json "PlanId" Id.of_json in
      let planName =
        field_map json "PlanName" ProvisionedProductPlanName.of_json in
      make ?provisioningArtifactId ?provisionedProductName
        ?provisionProductId ?planId ?planName ()
    let to_json v = composed_to_json to_value v
  end
module CreateProvisionedProductPlanInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      planName: ProvisionedProductPlanName.t ;
      planType: ProvisionedProductPlanType.t ;
      notificationArns: NotificationArns.t option ;
      pathId: Id.t option ;
      productId: Id.t ;
      provisionedProductName: ProvisionedProductName.t ;
      provisioningArtifactId: Id.t ;
      provisioningParameters: UpdateProvisioningParameters.t option ;
      idempotencyToken: IdempotencyToken.t ;
      tags: Tags.t option }
    let context_ = "CreateProvisionedProductPlanInput"
    let make ?acceptLanguage =
      fun ?notificationArns ->
        fun ?pathId ->
          fun ?provisioningParameters ->
            fun ?tags ->
              fun ~planName ->
                fun ~planType ->
                  fun ~productId ->
                    fun ~provisionedProductName ->
                      fun ~provisioningArtifactId ->
                        fun ~idempotencyToken ->
                          fun () ->
                            {
                              acceptLanguage;
                              notificationArns;
                              pathId;
                              provisioningParameters;
                              tags;
                              planName;
                              planType;
                              productId;
                              provisionedProductName;
                              provisioningArtifactId;
                              idempotencyToken
                            }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PlanName", (Some (ProvisionedProductPlanName.to_value x.planName)));
        ("PlanType", (Some (ProvisionedProductPlanType.to_value x.planType)));
        ("NotificationArns",
          (Option.map x.notificationArns ~f:NotificationArns.to_value));
        ("PathId", (Option.map x.pathId ~f:Id.to_value));
        ("ProductId", (Some (Id.to_value x.productId)));
        ("ProvisionedProductName",
          (Some (ProvisionedProductName.to_value x.provisionedProductName)));
        ("ProvisioningArtifactId",
          (Some (Id.to_value x.provisioningArtifactId)));
        ("ProvisioningParameters",
          (Option.map x.provisioningParameters
             ~f:UpdateProvisioningParameters.to_value));
        ("IdempotencyToken",
          (Some (IdempotencyToken.to_value x.idempotencyToken)));
        ("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let idempotencyToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdempotencyToken") in
      let provisioningParameters =
        (Option.map ~f:UpdateProvisioningParameters.of_xml)
          (Xml.child xml_arg0 "ProvisioningParameters") in
      let provisioningArtifactId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisioningArtifactId") in
      let provisionedProductName =
        ProvisionedProductName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisionedProductName") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      let pathId = (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PathId") in
      let notificationArns =
        (Option.map ~f:NotificationArns.of_xml)
          (Xml.child xml_arg0 "NotificationArns") in
      let planType =
        ProvisionedProductPlanType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PlanType") in
      let planName =
        ProvisionedProductPlanName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PlanName") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?tags ~idempotencyToken ?provisioningParameters
        ~provisioningArtifactId ~provisionedProductName ~productId ?pathId
        ?notificationArns ~planType ~planName ?acceptLanguage ()
    let of_json json =
      let tags = field_map json "Tags" Tags.of_json in
      let idempotencyToken =
        field_map_exn json "IdempotencyToken" IdempotencyToken.of_json in
      let provisioningParameters =
        field_map json "ProvisioningParameters"
          UpdateProvisioningParameters.of_json in
      let provisioningArtifactId =
        field_map_exn json "ProvisioningArtifactId" Id.of_json in
      let provisionedProductName =
        field_map_exn json "ProvisionedProductName"
          ProvisionedProductName.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      let pathId = field_map json "PathId" Id.of_json in
      let notificationArns =
        field_map json "NotificationArns" NotificationArns.of_json in
      let planType =
        field_map_exn json "PlanType" ProvisionedProductPlanType.of_json in
      let planName =
        field_map_exn json "PlanName" ProvisionedProductPlanName.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?tags ~idempotencyToken ?provisioningParameters
        ~provisioningArtifactId ~provisionedProductName ~productId ?pathId
        ?notificationArns ~planType ~planName ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module CreateProductOutput =
  struct
    type nonrec t =
      {
      productViewDetail: ProductViewDetail.t option ;
      provisioningArtifactDetail: ProvisioningArtifactDetail.t option ;
      tags: Tags.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?productViewDetail =
      fun ?provisioningArtifactDetail ->
        fun ?tags ->
          fun () -> { productViewDetail; provisioningArtifactDetail; tags }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProductViewDetail",
           (Option.map x.productViewDetail ~f:ProductViewDetail.to_value));
        ("ProvisioningArtifactDetail",
          (Option.map x.provisioningArtifactDetail
             ~f:ProvisioningArtifactDetail.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let provisioningArtifactDetail =
        (Option.map ~f:ProvisioningArtifactDetail.of_xml)
          (Xml.child xml_arg0 "ProvisioningArtifactDetail") in
      let productViewDetail =
        (Option.map ~f:ProductViewDetail.of_xml)
          (Xml.child xml_arg0 "ProductViewDetail") in
      make ?tags ?provisioningArtifactDetail ?productViewDetail ()
    let of_json json =
      let tags = field_map json "Tags" Tags.of_json in
      let provisioningArtifactDetail =
        field_map json "ProvisioningArtifactDetail"
          ProvisioningArtifactDetail.of_json in
      let productViewDetail =
        field_map json "ProductViewDetail" ProductViewDetail.of_json in
      make ?tags ?provisioningArtifactDetail ?productViewDetail ()
    let to_json v = composed_to_json to_value v
  end
module CreateProductInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      name: ProductViewName.t ;
      owner: ProductViewOwner.t ;
      description: ProductViewShortDescription.t option ;
      distributor: ProductViewOwner.t option ;
      supportDescription: SupportDescription.t option ;
      supportEmail: SupportEmail.t option ;
      supportUrl: SupportUrl.t option ;
      productType: ProductType.t ;
      tags: AddTags.t option ;
      provisioningArtifactParameters: ProvisioningArtifactProperties.t ;
      idempotencyToken: IdempotencyToken.t }
    let context_ = "CreateProductInput"
    let make ?acceptLanguage =
      fun ?description ->
        fun ?distributor ->
          fun ?supportDescription ->
            fun ?supportEmail ->
              fun ?supportUrl ->
                fun ?tags ->
                  fun ~name ->
                    fun ~owner ->
                      fun ~productType ->
                        fun ~provisioningArtifactParameters ->
                          fun ~idempotencyToken ->
                            fun () ->
                              {
                                acceptLanguage;
                                description;
                                distributor;
                                supportDescription;
                                supportEmail;
                                supportUrl;
                                tags;
                                name;
                                owner;
                                productType;
                                provisioningArtifactParameters;
                                idempotencyToken
                              }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("Name", (Some (ProductViewName.to_value x.name)));
        ("Owner", (Some (ProductViewOwner.to_value x.owner)));
        ("Description",
          (Option.map x.description ~f:ProductViewShortDescription.to_value));
        ("Distributor",
          (Option.map x.distributor ~f:ProductViewOwner.to_value));
        ("SupportDescription",
          (Option.map x.supportDescription ~f:SupportDescription.to_value));
        ("SupportEmail",
          (Option.map x.supportEmail ~f:SupportEmail.to_value));
        ("SupportUrl", (Option.map x.supportUrl ~f:SupportUrl.to_value));
        ("ProductType", (Some (ProductType.to_value x.productType)));
        ("Tags", (Option.map x.tags ~f:AddTags.to_value));
        ("ProvisioningArtifactParameters",
          (Some
             (ProvisioningArtifactProperties.to_value
                x.provisioningArtifactParameters)));
        ("IdempotencyToken",
          (Some (IdempotencyToken.to_value x.idempotencyToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdempotencyToken") in
      let provisioningArtifactParameters =
        ProvisioningArtifactProperties.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ProvisioningArtifactParameters") in
      let tags = (Option.map ~f:AddTags.of_xml) (Xml.child xml_arg0 "Tags") in
      let productType =
        ProductType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProductType") in
      let supportUrl =
        (Option.map ~f:SupportUrl.of_xml) (Xml.child xml_arg0 "SupportUrl") in
      let supportEmail =
        (Option.map ~f:SupportEmail.of_xml)
          (Xml.child xml_arg0 "SupportEmail") in
      let supportDescription =
        (Option.map ~f:SupportDescription.of_xml)
          (Xml.child xml_arg0 "SupportDescription") in
      let distributor =
        (Option.map ~f:ProductViewOwner.of_xml)
          (Xml.child xml_arg0 "Distributor") in
      let description =
        (Option.map ~f:ProductViewShortDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let owner =
        ProductViewOwner.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Owner") in
      let name =
        ProductViewName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~idempotencyToken ~provisioningArtifactParameters ?tags
        ~productType ?supportUrl ?supportEmail ?supportDescription
        ?distributor ?description ~owner ~name ?acceptLanguage ()
    let of_json json =
      let idempotencyToken =
        field_map_exn json "IdempotencyToken" IdempotencyToken.of_json in
      let provisioningArtifactParameters =
        field_map_exn json "ProvisioningArtifactParameters"
          ProvisioningArtifactProperties.of_json in
      let tags = field_map json "Tags" AddTags.of_json in
      let productType = field_map_exn json "ProductType" ProductType.of_json in
      let supportUrl = field_map json "SupportUrl" SupportUrl.of_json in
      let supportEmail = field_map json "SupportEmail" SupportEmail.of_json in
      let supportDescription =
        field_map json "SupportDescription" SupportDescription.of_json in
      let distributor = field_map json "Distributor" ProductViewOwner.of_json in
      let description =
        field_map json "Description" ProductViewShortDescription.of_json in
      let owner = field_map_exn json "Owner" ProductViewOwner.of_json in
      let name = field_map_exn json "Name" ProductViewName.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~idempotencyToken ~provisioningArtifactParameters ?tags
        ~productType ?supportUrl ?supportEmail ?supportDescription
        ?distributor ?description ~owner ~name ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module CreatePortfolioShareOutput =
  struct
    type nonrec t = {
      portfolioShareToken: Id.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `InvalidStateException of InvalidStateException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `OperationNotSupportedException of OperationNotSupportedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?portfolioShareToken = fun () -> { portfolioShareToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "OperationNotSupportedException" ->
          `OperationNotSupportedException
            (OperationNotSupportedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `OperationNotSupportedException e ->
          `Assoc
            [("error", (`String "OperationNotSupportedException"));
            ("details", (OperationNotSupportedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PortfolioShareToken",
           (Option.map x.portfolioShareToken ~f:Id.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let portfolioShareToken =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "PortfolioShareToken") in
      make ?portfolioShareToken ()
    let of_json json =
      let portfolioShareToken =
        field_map json "PortfolioShareToken" Id.of_json in
      make ?portfolioShareToken ()
    let to_json v = composed_to_json to_value v
  end
module CreatePortfolioShareInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      portfolioId: Id.t ;
      accountId: AccountId.t option ;
      organizationNode: OrganizationNode.t option ;
      shareTagOptions: Boolean.t option }
    let context_ = "CreatePortfolioShareInput"
    let make ?acceptLanguage =
      fun ?accountId ->
        fun ?organizationNode ->
          fun ?shareTagOptions ->
            fun ~portfolioId ->
              fun () ->
                {
                  acceptLanguage;
                  accountId;
                  organizationNode;
                  shareTagOptions;
                  portfolioId
                }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("AccountId", (Option.map x.accountId ~f:AccountId.to_value));
        ("OrganizationNode",
          (Option.map x.organizationNode ~f:OrganizationNode.to_value));
        ("ShareTagOptions",
          (Option.map x.shareTagOptions ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shareTagOptions =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ShareTagOptions") in
      let organizationNode =
        (Option.map ~f:OrganizationNode.of_xml)
          (Xml.child xml_arg0 "OrganizationNode") in
      let accountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "AccountId") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?shareTagOptions ?organizationNode ?accountId ~portfolioId
        ?acceptLanguage ()
    let of_json json =
      let shareTagOptions = field_map json "ShareTagOptions" Boolean.of_json in
      let organizationNode =
        field_map json "OrganizationNode" OrganizationNode.of_json in
      let accountId = field_map json "AccountId" AccountId.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?shareTagOptions ?organizationNode ?accountId ~portfolioId
        ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module CreatePortfolioOutput =
  struct
    type nonrec t =
      {
      portfolioDetail: PortfolioDetail.t option ;
      tags: Tags.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?portfolioDetail =
      fun ?tags -> fun () -> { portfolioDetail; tags }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PortfolioDetail",
           (Option.map x.portfolioDetail ~f:PortfolioDetail.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let portfolioDetail =
        (Option.map ~f:PortfolioDetail.of_xml)
          (Xml.child xml_arg0 "PortfolioDetail") in
      make ?tags ?portfolioDetail ()
    let of_json json =
      let tags = field_map json "Tags" Tags.of_json in
      let portfolioDetail =
        field_map json "PortfolioDetail" PortfolioDetail.of_json in
      make ?tags ?portfolioDetail ()
    let to_json v = composed_to_json to_value v
  end
module CreatePortfolioInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      displayName: PortfolioDisplayName.t ;
      description: PortfolioDescription.t option ;
      providerName: ProviderName.t ;
      tags: AddTags.t option ;
      idempotencyToken: IdempotencyToken.t }
    let context_ = "CreatePortfolioInput"
    let make ?acceptLanguage =
      fun ?description ->
        fun ?tags ->
          fun ~displayName ->
            fun ~providerName ->
              fun ~idempotencyToken ->
                fun () ->
                  {
                    acceptLanguage;
                    description;
                    tags;
                    displayName;
                    providerName;
                    idempotencyToken
                  }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("DisplayName", (Some (PortfolioDisplayName.to_value x.displayName)));
        ("Description",
          (Option.map x.description ~f:PortfolioDescription.to_value));
        ("ProviderName", (Some (ProviderName.to_value x.providerName)));
        ("Tags", (Option.map x.tags ~f:AddTags.to_value));
        ("IdempotencyToken",
          (Some (IdempotencyToken.to_value x.idempotencyToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdempotencyToken") in
      let tags = (Option.map ~f:AddTags.of_xml) (Xml.child xml_arg0 "Tags") in
      let providerName =
        ProviderName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProviderName") in
      let description =
        (Option.map ~f:PortfolioDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let displayName =
        PortfolioDisplayName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DisplayName") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~idempotencyToken ?tags ~providerName ?description ~displayName
        ?acceptLanguage ()
    let of_json json =
      let idempotencyToken =
        field_map_exn json "IdempotencyToken" IdempotencyToken.of_json in
      let tags = field_map json "Tags" AddTags.of_json in
      let providerName =
        field_map_exn json "ProviderName" ProviderName.of_json in
      let description =
        field_map json "Description" PortfolioDescription.of_json in
      let displayName =
        field_map_exn json "DisplayName" PortfolioDisplayName.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~idempotencyToken ?tags ~providerName ?description ~displayName
        ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module CreateConstraintOutput =
  struct
    type nonrec t =
      {
      constraintDetail: ConstraintDetail.t option ;
      constraintParameters: ConstraintParameters.t option ;
      status: Status.t option }
    type nonrec error =
      [ `DuplicateResourceException of DuplicateResourceException.t 
      | `InvalidParametersException of InvalidParametersException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?constraintDetail =
      fun ?constraintParameters ->
        fun ?status ->
          fun () -> { constraintDetail; constraintParameters; status }
    let error_of_json name json =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException
            (DuplicateResourceException.of_json json)
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException (DuplicateResourceException.of_xml xml)
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DuplicateResourceException e ->
          `Assoc
            [("error", (`String "DuplicateResourceException"));
            ("details", (DuplicateResourceException.to_json e))]
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ConstraintDetail",
           (Option.map x.constraintDetail ~f:ConstraintDetail.to_value));
        ("ConstraintParameters",
          (Option.map x.constraintParameters ~f:ConstraintParameters.to_value));
        ("Status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "Status") in
      let constraintParameters =
        (Option.map ~f:ConstraintParameters.of_xml)
          (Xml.child xml_arg0 "ConstraintParameters") in
      let constraintDetail =
        (Option.map ~f:ConstraintDetail.of_xml)
          (Xml.child xml_arg0 "ConstraintDetail") in
      make ?status ?constraintParameters ?constraintDetail ()
    let of_json json =
      let status = field_map json "Status" Status.of_json in
      let constraintParameters =
        field_map json "ConstraintParameters" ConstraintParameters.of_json in
      let constraintDetail =
        field_map json "ConstraintDetail" ConstraintDetail.of_json in
      make ?status ?constraintParameters ?constraintDetail ()
    let to_json v = composed_to_json to_value v
  end
module CreateConstraintInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      portfolioId: Id.t ;
      productId: Id.t ;
      parameters: ConstraintParameters.t ;
      type_: ConstraintType.t ;
      description: ConstraintDescription.t option ;
      idempotencyToken: IdempotencyToken.t }
    let context_ = "CreateConstraintInput"
    let make ?acceptLanguage =
      fun ?description ->
        fun ~portfolioId ->
          fun ~productId ->
            fun ~parameters ->
              fun ~type_ ->
                fun ~idempotencyToken ->
                  fun () ->
                    {
                      acceptLanguage;
                      description;
                      portfolioId;
                      productId;
                      parameters;
                      type_;
                      idempotencyToken
                    }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("ProductId", (Some (Id.to_value x.productId)));
        ("Parameters", (Some (ConstraintParameters.to_value x.parameters)));
        ("Type", (Some (ConstraintType.to_value x.type_)));
        ("Description",
          (Option.map x.description ~f:ConstraintDescription.to_value));
        ("IdempotencyToken",
          (Some (IdempotencyToken.to_value x.idempotencyToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdempotencyToken") in
      let description =
        (Option.map ~f:ConstraintDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let type_ =
        ConstraintType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      let parameters =
        ConstraintParameters.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Parameters") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~idempotencyToken ?description ~type_ ~parameters ~productId
        ~portfolioId ?acceptLanguage ()
    let of_json json =
      let idempotencyToken =
        field_map_exn json "IdempotencyToken" IdempotencyToken.of_json in
      let description =
        field_map json "Description" ConstraintDescription.of_json in
      let type_ = field_map_exn json "Type" ConstraintType.of_json in
      let parameters =
        field_map_exn json "Parameters" ConstraintParameters.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~idempotencyToken ?description ~type_ ~parameters ~productId
        ~portfolioId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module CopyProductOutput =
  struct
    type nonrec t = {
      copyProductToken: Id.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?copyProductToken = fun () -> { copyProductToken }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("CopyProductToken", (Option.map x.copyProductToken ~f:Id.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let copyProductToken =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "CopyProductToken") in
      make ?copyProductToken ()
    let of_json json =
      let copyProductToken = field_map json "CopyProductToken" Id.of_json in
      make ?copyProductToken ()
    let to_json v = composed_to_json to_value v
  end
module CopyProductInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      sourceProductArn: ProductArn.t ;
      targetProductId: Id.t option ;
      targetProductName: ProductViewName.t option ;
      sourceProvisioningArtifactIdentifiers:
        SourceProvisioningArtifactProperties.t option ;
      copyOptions: CopyOptions.t option ;
      idempotencyToken: IdempotencyToken.t }
    let context_ = "CopyProductInput"
    let make ?acceptLanguage =
      fun ?targetProductId ->
        fun ?targetProductName ->
          fun ?sourceProvisioningArtifactIdentifiers ->
            fun ?copyOptions ->
              fun ~sourceProductArn ->
                fun ~idempotencyToken ->
                  fun () ->
                    {
                      acceptLanguage;
                      targetProductId;
                      targetProductName;
                      sourceProvisioningArtifactIdentifiers;
                      copyOptions;
                      sourceProductArn;
                      idempotencyToken
                    }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("SourceProductArn", (Some (ProductArn.to_value x.sourceProductArn)));
        ("TargetProductId", (Option.map x.targetProductId ~f:Id.to_value));
        ("TargetProductName",
          (Option.map x.targetProductName ~f:ProductViewName.to_value));
        ("SourceProvisioningArtifactIdentifiers",
          (Option.map x.sourceProvisioningArtifactIdentifiers
             ~f:SourceProvisioningArtifactProperties.to_value));
        ("CopyOptions", (Option.map x.copyOptions ~f:CopyOptions.to_value));
        ("IdempotencyToken",
          (Some (IdempotencyToken.to_value x.idempotencyToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        IdempotencyToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdempotencyToken") in
      let copyOptions =
        (Option.map ~f:CopyOptions.of_xml) (Xml.child xml_arg0 "CopyOptions") in
      let sourceProvisioningArtifactIdentifiers =
        (Option.map ~f:SourceProvisioningArtifactProperties.of_xml)
          (Xml.child xml_arg0 "SourceProvisioningArtifactIdentifiers") in
      let targetProductName =
        (Option.map ~f:ProductViewName.of_xml)
          (Xml.child xml_arg0 "TargetProductName") in
      let targetProductId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "TargetProductId") in
      let sourceProductArn =
        ProductArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceProductArn") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~idempotencyToken ?copyOptions
        ?sourceProvisioningArtifactIdentifiers ?targetProductName
        ?targetProductId ~sourceProductArn ?acceptLanguage ()
    let of_json json =
      let idempotencyToken =
        field_map_exn json "IdempotencyToken" IdempotencyToken.of_json in
      let copyOptions = field_map json "CopyOptions" CopyOptions.of_json in
      let sourceProvisioningArtifactIdentifiers =
        field_map json "SourceProvisioningArtifactIdentifiers"
          SourceProvisioningArtifactProperties.of_json in
      let targetProductName =
        field_map json "TargetProductName" ProductViewName.of_json in
      let targetProductId = field_map json "TargetProductId" Id.of_json in
      let sourceProductArn =
        field_map_exn json "SourceProductArn" ProductArn.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~idempotencyToken ?copyOptions
        ?sourceProvisioningArtifactIdentifiers ?targetProductName
        ?targetProductId ~sourceProductArn ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module BatchDisassociateServiceActionFromProvisioningArtifactOutput =
  struct
    type nonrec t =
      {
      failedServiceActionAssociations:
        FailedServiceActionAssociations.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?failedServiceActionAssociations =
      fun () -> { failedServiceActionAssociations }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FailedServiceActionAssociations",
           (Option.map x.failedServiceActionAssociations
              ~f:FailedServiceActionAssociations.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failedServiceActionAssociations =
        (Option.map ~f:FailedServiceActionAssociations.of_xml)
          (Xml.child xml_arg0 "FailedServiceActionAssociations") in
      make ?failedServiceActionAssociations ()
    let of_json json =
      let failedServiceActionAssociations =
        field_map json "FailedServiceActionAssociations"
          FailedServiceActionAssociations.of_json in
      make ?failedServiceActionAssociations ()
    let to_json v = composed_to_json to_value v
  end
module BatchDisassociateServiceActionFromProvisioningArtifactInput =
  struct
    type nonrec t =
      {
      serviceActionAssociations: ServiceActionAssociations.t ;
      acceptLanguage: AcceptLanguage.t option }
    let context_ =
      "BatchDisassociateServiceActionFromProvisioningArtifactInput"
    let make ?acceptLanguage =
      fun ~serviceActionAssociations ->
        fun () -> { acceptLanguage; serviceActionAssociations }
    let to_value x =
      structure_to_value
        [("ServiceActionAssociations",
           (Some
              (ServiceActionAssociations.to_value x.serviceActionAssociations)));
        ("AcceptLanguage",
          (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      let serviceActionAssociations =
        ServiceActionAssociations.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ServiceActionAssociations") in
      make ?acceptLanguage ~serviceActionAssociations ()
    let of_json json =
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      let serviceActionAssociations =
        field_map_exn json "ServiceActionAssociations"
          ServiceActionAssociations.of_json in
      make ?acceptLanguage ~serviceActionAssociations ()
    let to_json v = composed_to_json to_value v
  end
module BatchAssociateServiceActionWithProvisioningArtifactOutput =
  struct
    type nonrec t =
      {
      failedServiceActionAssociations:
        FailedServiceActionAssociations.t option }
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?failedServiceActionAssociations =
      fun () -> { failedServiceActionAssociations }
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FailedServiceActionAssociations",
           (Option.map x.failedServiceActionAssociations
              ~f:FailedServiceActionAssociations.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failedServiceActionAssociations =
        (Option.map ~f:FailedServiceActionAssociations.of_xml)
          (Xml.child xml_arg0 "FailedServiceActionAssociations") in
      make ?failedServiceActionAssociations ()
    let of_json json =
      let failedServiceActionAssociations =
        field_map json "FailedServiceActionAssociations"
          FailedServiceActionAssociations.of_json in
      make ?failedServiceActionAssociations ()
    let to_json v = composed_to_json to_value v
  end
module BatchAssociateServiceActionWithProvisioningArtifactInput =
  struct
    type nonrec t =
      {
      serviceActionAssociations: ServiceActionAssociations.t ;
      acceptLanguage: AcceptLanguage.t option }
    let context_ = "BatchAssociateServiceActionWithProvisioningArtifactInput"
    let make ?acceptLanguage =
      fun ~serviceActionAssociations ->
        fun () -> { acceptLanguage; serviceActionAssociations }
    let to_value x =
      structure_to_value
        [("ServiceActionAssociations",
           (Some
              (ServiceActionAssociations.to_value x.serviceActionAssociations)));
        ("AcceptLanguage",
          (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      let serviceActionAssociations =
        ServiceActionAssociations.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ServiceActionAssociations") in
      make ?acceptLanguage ~serviceActionAssociations ()
    let of_json json =
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      let serviceActionAssociations =
        field_map_exn json "ServiceActionAssociations"
          ServiceActionAssociations.of_json in
      make ?acceptLanguage ~serviceActionAssociations ()
    let to_json v = composed_to_json to_value v
  end
module AssociateTagOptionWithResourceOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `DuplicateResourceException of DuplicateResourceException.t 
      | `InvalidParametersException of InvalidParametersException.t 
      | `InvalidStateException of InvalidStateException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TagOptionNotMigratedException of TagOptionNotMigratedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException
            (DuplicateResourceException.of_json json)
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException (DuplicateResourceException.of_xml xml)
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TagOptionNotMigratedException" ->
          `TagOptionNotMigratedException
            (TagOptionNotMigratedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DuplicateResourceException e ->
          `Assoc
            [("error", (`String "DuplicateResourceException"));
            ("details", (DuplicateResourceException.to_json e))]
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TagOptionNotMigratedException e ->
          `Assoc
            [("error", (`String "TagOptionNotMigratedException"));
            ("details", (TagOptionNotMigratedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module AssociateTagOptionWithResourceInput =
  struct
    type nonrec t = {
      resourceId: ResourceId.t ;
      tagOptionId: TagOptionId.t }
    let context_ = "AssociateTagOptionWithResourceInput"
    let make ~resourceId =
      fun ~tagOptionId -> fun () -> { resourceId; tagOptionId }
    let to_value x =
      structure_to_value
        [("ResourceId", (Some (ResourceId.to_value x.resourceId)));
        ("TagOptionId", (Some (TagOptionId.to_value x.tagOptionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagOptionId =
        TagOptionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TagOptionId") in
      let resourceId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      make ~tagOptionId ~resourceId ()
    let of_json json =
      let tagOptionId = field_map_exn json "TagOptionId" TagOptionId.of_json in
      let resourceId = field_map_exn json "ResourceId" ResourceId.of_json in
      make ~tagOptionId ~resourceId ()
    let to_json v = composed_to_json to_value v
  end
module AssociateServiceActionWithProvisioningArtifactOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `DuplicateResourceException of DuplicateResourceException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException
            (DuplicateResourceException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException (DuplicateResourceException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DuplicateResourceException e ->
          `Assoc
            [("error", (`String "DuplicateResourceException"));
            ("details", (DuplicateResourceException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module AssociateServiceActionWithProvisioningArtifactInput =
  struct
    type nonrec t =
      {
      productId: Id.t ;
      provisioningArtifactId: Id.t ;
      serviceActionId: Id.t ;
      acceptLanguage: AcceptLanguage.t option }
    let context_ = "AssociateServiceActionWithProvisioningArtifactInput"
    let make ?acceptLanguage =
      fun ~productId ->
        fun ~provisioningArtifactId ->
          fun ~serviceActionId ->
            fun () ->
              {
                acceptLanguage;
                productId;
                provisioningArtifactId;
                serviceActionId
              }
    let to_value x =
      structure_to_value
        [("ProductId", (Some (Id.to_value x.productId)));
        ("ProvisioningArtifactId",
          (Some (Id.to_value x.provisioningArtifactId)));
        ("ServiceActionId", (Some (Id.to_value x.serviceActionId)));
        ("AcceptLanguage",
          (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      let serviceActionId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceActionId") in
      let provisioningArtifactId =
        Id.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProvisioningArtifactId") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      make ?acceptLanguage ~serviceActionId ~provisioningArtifactId
        ~productId ()
    let of_json json =
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      let serviceActionId = field_map_exn json "ServiceActionId" Id.of_json in
      let provisioningArtifactId =
        field_map_exn json "ProvisioningArtifactId" Id.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      make ?acceptLanguage ~serviceActionId ~provisioningArtifactId
        ~productId ()
    let to_json v = composed_to_json to_value v
  end
module AssociateProductWithPortfolioOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module AssociateProductWithPortfolioInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      productId: Id.t ;
      portfolioId: Id.t ;
      sourcePortfolioId: Id.t option }
    let context_ = "AssociateProductWithPortfolioInput"
    let make ?acceptLanguage =
      fun ?sourcePortfolioId ->
        fun ~productId ->
          fun ~portfolioId ->
            fun () ->
              { acceptLanguage; sourcePortfolioId; productId; portfolioId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("ProductId", (Some (Id.to_value x.productId)));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("SourcePortfolioId",
          (Option.map x.sourcePortfolioId ~f:Id.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourcePortfolioId =
        (Option.map ~f:Id.of_xml) (Xml.child xml_arg0 "SourcePortfolioId") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let productId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ProductId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?sourcePortfolioId ~portfolioId ~productId ?acceptLanguage ()
    let of_json json =
      let sourcePortfolioId = field_map json "SourcePortfolioId" Id.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let productId = field_map_exn json "ProductId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?sourcePortfolioId ~portfolioId ~productId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module AssociatePrincipalWithPortfolioOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module AssociatePrincipalWithPortfolioInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      portfolioId: Id.t ;
      principalARN: PrincipalARN.t ;
      principalType: PrincipalType.t }
    let context_ = "AssociatePrincipalWithPortfolioInput"
    let make ?acceptLanguage =
      fun ~portfolioId ->
        fun ~principalARN ->
          fun ~principalType ->
            fun () ->
              { acceptLanguage; portfolioId; principalARN; principalType }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("PrincipalARN", (Some (PrincipalARN.to_value x.principalARN)));
        ("PrincipalType", (Some (PrincipalType.to_value x.principalType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let principalType =
        PrincipalType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrincipalType") in
      let principalARN =
        PrincipalARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PrincipalARN") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ~principalType ~principalARN ~portfolioId ?acceptLanguage ()
    let of_json json =
      let principalType =
        field_map_exn json "PrincipalType" PrincipalType.of_json in
      let principalARN =
        field_map_exn json "PrincipalARN" PrincipalARN.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ~principalType ~principalARN ~portfolioId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
module AssociateBudgetWithResourceOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `DuplicateResourceException of DuplicateResourceException.t 
      | `InvalidParametersException of InvalidParametersException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException
            (DuplicateResourceException.of_json json)
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DuplicateResourceException" ->
          `DuplicateResourceException (DuplicateResourceException.of_xml xml)
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DuplicateResourceException e ->
          `Assoc
            [("error", (`String "DuplicateResourceException"));
            ("details", (DuplicateResourceException.to_json e))]
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module AssociateBudgetWithResourceInput =
  struct
    type nonrec t = {
      budgetName: BudgetName.t ;
      resourceId: Id.t }
    let context_ = "AssociateBudgetWithResourceInput"
    let make ~budgetName =
      fun ~resourceId -> fun () -> { budgetName; resourceId }
    let to_value x =
      structure_to_value
        [("BudgetName", (Some (BudgetName.to_value x.budgetName)));
        ("ResourceId", (Some (Id.to_value x.resourceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      let budgetName =
        BudgetName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BudgetName") in
      make ~resourceId ~budgetName ()
    let of_json json =
      let resourceId = field_map_exn json "ResourceId" Id.of_json in
      let budgetName = field_map_exn json "BudgetName" BudgetName.of_json in
      make ~resourceId ~budgetName ()
    let to_json v = composed_to_json to_value v
  end
module AcceptPortfolioShareOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidParametersException of InvalidParametersException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException
            (InvalidParametersException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParametersException" ->
          `InvalidParametersException (InvalidParametersException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParametersException e ->
          `Assoc
            [("error", (`String "InvalidParametersException"));
            ("details", (InvalidParametersException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module AcceptPortfolioShareInput =
  struct
    type nonrec t =
      {
      acceptLanguage: AcceptLanguage.t option ;
      portfolioId: Id.t ;
      portfolioShareType: PortfolioShareType.t option }
    let context_ = "AcceptPortfolioShareInput"
    let make ?acceptLanguage =
      fun ?portfolioShareType ->
        fun ~portfolioId ->
          fun () -> { acceptLanguage; portfolioShareType; portfolioId }
    let to_value x =
      structure_to_value
        [("AcceptLanguage",
           (Option.map x.acceptLanguage ~f:AcceptLanguage.to_value));
        ("PortfolioId", (Some (Id.to_value x.portfolioId)));
        ("PortfolioShareType",
          (Option.map x.portfolioShareType ~f:PortfolioShareType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let portfolioShareType =
        (Option.map ~f:PortfolioShareType.of_xml)
          (Xml.child xml_arg0 "PortfolioShareType") in
      let portfolioId =
        Id.of_xml (Xml.child_exn ~context:context_ xml_arg0 "PortfolioId") in
      let acceptLanguage =
        (Option.map ~f:AcceptLanguage.of_xml)
          (Xml.child xml_arg0 "AcceptLanguage") in
      make ?portfolioShareType ~portfolioId ?acceptLanguage ()
    let of_json json =
      let portfolioShareType =
        field_map json "PortfolioShareType" PortfolioShareType.of_json in
      let portfolioId = field_map_exn json "PortfolioId" Id.of_json in
      let acceptLanguage =
        field_map json "AcceptLanguage" AcceptLanguage.of_json in
      make ?portfolioShareType ~portfolioId ?acceptLanguage ()
    let to_json v = composed_to_json to_value v
  end
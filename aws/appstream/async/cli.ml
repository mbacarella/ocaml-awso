(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_application_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetName = flag "fleet-name" (required string) ~doc:"STRING Name"
       and applicationArn =
         flag "application-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_application_fleet
           (Values.AssociateApplicationFleetRequest.make ~fleetName
              ~applicationArn ())
           (Some Values.AssociateApplicationFleetResult.to_json)
           (Some Values.AssociateApplicationFleetResult.error_to_json)])
let associate_application_to_entitlement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName = flag "stack-name" (required string) ~doc:"STRING Name"
       and entitlementName =
         flag "entitlement-name" (required string) ~doc:"STRING Name"
       and applicationIdentifier =
         flag "application-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_application_to_entitlement
           (Values.AssociateApplicationToEntitlementRequest.make ~stackName
              ~entitlementName ~applicationIdentifier ())
           (Some Values.AssociateApplicationToEntitlementResult.to_json)
           (Some Values.AssociateApplicationToEntitlementResult.error_to_json)])
let associate_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetName =
         flag "fleet-name" (required string) ~doc:"STRING String"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_fleet
           (Values.AssociateFleetRequest.make ~fleetName ~stackName ())
           (Some Values.AssociateFleetResult.to_json)
           (Some Values.AssociateFleetResult.error_to_json)])
let batch_associate_user_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userStackAssociations =
         flag "user-stack-associations" (required json_arg)
           ~doc:"JSON UserStackAssociationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_associate_user_stack
           (Values.BatchAssociateUserStackRequest.make
              ~userStackAssociations:(Values.UserStackAssociationList.of_json
                                        userStackAssociations) ())
           (Some Values.BatchAssociateUserStackResult.to_json)
           (Some Values.BatchAssociateUserStackResult.error_to_json)])
let batch_disassociate_user_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userStackAssociations =
         flag "user-stack-associations" (required json_arg)
           ~doc:"JSON UserStackAssociationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_disassociate_user_stack
           (Values.BatchDisassociateUserStackRequest.make
              ~userStackAssociations:(Values.UserStackAssociationList.of_json
                                        userStackAssociations) ())
           (Some Values.BatchDisassociateUserStackResult.to_json)
           (Some Values.BatchDisassociateUserStackResult.error_to_json)])
let copy_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationImageDescription =
         flag "destination-image-description" (optional string)
           ~doc:"STRING Description"
       and sourceImageName =
         flag "source-image-name" (required string) ~doc:"STRING Name"
       and destinationImageName =
         flag "destination-image-name" (required string) ~doc:"STRING Name"
       and destinationRegion =
         flag "destination-region" (required string) ~doc:"STRING RegionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_image
           (Values.CopyImageRequest.make ?destinationImageDescription
              ~sourceImageName ~destinationImageName ~destinationRegion ())
           (Some Values.CopyImageResponse.to_json)
           (Some Values.CopyImageResponse.error_to_json)])
let create_app_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and sourceS3Location =
         flag "source-s3-location" (required json_arg) ~doc:"JSON S3Location"
       and setupScriptDetails =
         flag "setup-script-details" (required json_arg)
           ~doc:"JSON ScriptDetails" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app_block
           (Values.CreateAppBlockRequest.make ?description ?displayName
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~sourceS3Location:(Values.S3Location.of_json sourceS3Location)
              ~setupScriptDetails:(Values.ScriptDetails.of_json
                                     setupScriptDetails) ())
           (Some Values.CreateAppBlockResult.to_json)
           (Some Values.CreateAppBlockResult.error_to_json)])
let create_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and workingDirectory =
         flag "working-directory" (optional string) ~doc:"STRING String"
       and launchParameters =
         flag "launch-parameters" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and iconS3Location =
         flag "icon-s3-location" (required json_arg) ~doc:"JSON S3Location"
       and launchPath =
         flag "launch-path" (required string) ~doc:"STRING String"
       and platforms =
         flag "platforms" (required json_arg) ~doc:"JSON Platforms"
       and instanceFamilies =
         flag "instance-families" (required json_arg) ~doc:"JSON StringList"
       and appBlockArn =
         flag "app-block-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application
           (Values.CreateApplicationRequest.make ?displayName ?description
              ?workingDirectory ?launchParameters
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~iconS3Location:(Values.S3Location.of_json iconS3Location)
              ~launchPath ~platforms:(Values.Platforms.of_json platforms)
              ~instanceFamilies:(Values.StringList.of_json instanceFamilies)
              ~appBlockArn ()) (Some Values.CreateApplicationResult.to_json)
           (Some Values.CreateApplicationResult.error_to_json)])
let create_directory_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceAccountCredentials =
         flag "service-account-credentials" (optional json_arg)
           ~doc:"JSON ServiceAccountCredentials"
       and directoryName =
         flag "directory-name" (required string) ~doc:"STRING DirectoryName"
       and organizationalUnitDistinguishedNames =
         flag "organizational-unit-distinguished-names" (required json_arg)
           ~doc:"JSON OrganizationalUnitDistinguishedNamesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_directory_config
           (Values.CreateDirectoryConfigRequest.make
              ?serviceAccountCredentials:(Option.map
                                            ~f:Values.ServiceAccountCredentials.of_json
                                            serviceAccountCredentials)
              ~directoryName
              ~organizationalUnitDistinguishedNames:(Values.OrganizationalUnitDistinguishedNamesList.of_json
                                                       organizationalUnitDistinguishedNames)
              ()) (Some Values.CreateDirectoryConfigResult.to_json)
           (Some Values.CreateDirectoryConfigResult.error_to_json)])
let create_entitlement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and stackName = flag "stack-name" (required string) ~doc:"STRING Name"
       and appVisibility =
         flag "app-visibility" (required json_arg) ~doc:"JSON AppVisibility"
       and attributes =
         flag "attributes" (required json_arg)
           ~doc:"JSON EntitlementAttributeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_entitlement
           (Values.CreateEntitlementRequest.make ?description ~name
              ~stackName
              ~appVisibility:(Values.AppVisibility.of_json appVisibility)
              ~attributes:(Values.EntitlementAttributeList.of_json attributes)
              ()) (Some Values.CreateEntitlementResult.to_json)
           (Some Values.CreateEntitlementResult.error_to_json)])
let create_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and imageName =
         flag "image-name" (optional string) ~doc:"STRING String"
       and imageArn = flag "image-arn" (optional string) ~doc:"STRING Arn"
       and fleetType =
         flag "fleet-type" (optional json_arg) ~doc:"JSON FleetType"
       and computeCapacity =
         flag "compute-capacity" (optional json_arg)
           ~doc:"JSON ComputeCapacity"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and maxUserDurationInSeconds =
         flag "max-user-duration-in-seconds" (optional int)
           ~doc:"INT Integer"
       and disconnectTimeoutInSeconds =
         flag "disconnect-timeout-in-seconds" (optional int)
           ~doc:"INT Integer"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and enableDefaultInternetAccess =
         flag "enable-default-internet-access" (optional bool)
           ~doc:"BOOL BooleanObject"
       and domainJoinInfo =
         flag "domain-join-info" (optional json_arg)
           ~doc:"JSON DomainJoinInfo"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and idleDisconnectTimeoutInSeconds =
         flag "idle-disconnect-timeout-in-seconds" (optional int)
           ~doc:"INT Integer"
       and iamRoleArn =
         flag "iam-role-arn" (optional string) ~doc:"STRING Arn"
       and streamView =
         flag "stream-view" (optional json_arg) ~doc:"JSON StreamView"
       and platform =
         flag "platform" (optional json_arg) ~doc:"JSON PlatformType"
       and maxConcurrentSessions =
         flag "max-concurrent-sessions" (optional int) ~doc:"INT Integer"
       and usbDeviceFilterStrings =
         flag "usb-device-filter-strings" (optional json_arg)
           ~doc:"JSON UsbDeviceFilterStrings"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and instanceType =
         flag "instance-type" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_fleet
           (Values.CreateFleetRequest.make ?imageName ?imageArn
              ?fleetType:(Option.map ~f:Values.FleetType.of_json fleetType)
              ?computeCapacity:(Option.map ~f:Values.ComputeCapacity.of_json
                                  computeCapacity)
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?maxUserDurationInSeconds ?disconnectTimeoutInSeconds
              ?description ?displayName ?enableDefaultInternetAccess
              ?domainJoinInfo:(Option.map ~f:Values.DomainJoinInfo.of_json
                                 domainJoinInfo)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?idleDisconnectTimeoutInSeconds ?iamRoleArn
              ?streamView:(Option.map ~f:Values.StreamView.of_json streamView)
              ?platform:(Option.map ~f:Values.PlatformType.of_json platform)
              ?maxConcurrentSessions
              ?usbDeviceFilterStrings:(Option.map
                                         ~f:Values.UsbDeviceFilterStrings.of_json
                                         usbDeviceFilterStrings) ~name
              ~instanceType ()) (Some Values.CreateFleetResult.to_json)
           (Some Values.CreateFleetResult.error_to_json)])
let create_image_builder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and imageName =
         flag "image-name" (optional string) ~doc:"STRING String"
       and imageArn = flag "image-arn" (optional string) ~doc:"STRING Arn"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and iamRoleArn =
         flag "iam-role-arn" (optional string) ~doc:"STRING Arn"
       and enableDefaultInternetAccess =
         flag "enable-default-internet-access" (optional bool)
           ~doc:"BOOL BooleanObject"
       and domainJoinInfo =
         flag "domain-join-info" (optional json_arg)
           ~doc:"JSON DomainJoinInfo"
       and appstreamAgentVersion =
         flag "appstream-agent-version" (optional string)
           ~doc:"STRING AppstreamAgentVersion"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and accessEndpoints =
         flag "access-endpoints" (optional json_arg)
           ~doc:"JSON AccessEndpointList"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and instanceType =
         flag "instance-type" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_image_builder
           (Values.CreateImageBuilderRequest.make ?imageName ?imageArn
              ?description ?displayName
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?iamRoleArn ?enableDefaultInternetAccess
              ?domainJoinInfo:(Option.map ~f:Values.DomainJoinInfo.of_json
                                 domainJoinInfo) ?appstreamAgentVersion
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?accessEndpoints:(Option.map
                                  ~f:Values.AccessEndpointList.of_json
                                  accessEndpoints) ~name ~instanceType ())
           (Some Values.CreateImageBuilderResult.to_json)
           (Some Values.CreateImageBuilderResult.error_to_json)])
let create_image_builder_streaming_u_r_l =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and validity = flag "validity" (optional json_arg) ~doc:"JSON Long"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_image_builder_streaming_u_r_l
           (Values.CreateImageBuilderStreamingURLRequest.make
              ?validity:(Option.map ~f:Values.Long.of_json validity) ~name ())
           (Some Values.CreateImageBuilderStreamingURLResult.to_json)
           (Some Values.CreateImageBuilderStreamingURLResult.error_to_json)])
let create_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and storageConnectors =
         flag "storage-connectors" (optional json_arg)
           ~doc:"JSON StorageConnectorList"
       and redirectURL =
         flag "redirect-u-r-l" (optional string) ~doc:"STRING RedirectURL"
       and feedbackURL =
         flag "feedback-u-r-l" (optional string) ~doc:"STRING FeedbackURL"
       and userSettings =
         flag "user-settings" (optional json_arg) ~doc:"JSON UserSettingList"
       and applicationSettings =
         flag "application-settings" (optional json_arg)
           ~doc:"JSON ApplicationSettings"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and accessEndpoints =
         flag "access-endpoints" (optional json_arg)
           ~doc:"JSON AccessEndpointList"
       and embedHostDomains =
         flag "embed-host-domains" (optional json_arg)
           ~doc:"JSON EmbedHostDomains"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_stack
           (Values.CreateStackRequest.make ?description ?displayName
              ?storageConnectors:(Option.map
                                    ~f:Values.StorageConnectorList.of_json
                                    storageConnectors) ?redirectURL
              ?feedbackURL
              ?userSettings:(Option.map ~f:Values.UserSettingList.of_json
                               userSettings)
              ?applicationSettings:(Option.map
                                      ~f:Values.ApplicationSettings.of_json
                                      applicationSettings)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?accessEndpoints:(Option.map
                                  ~f:Values.AccessEndpointList.of_json
                                  accessEndpoints)
              ?embedHostDomains:(Option.map
                                   ~f:Values.EmbedHostDomains.of_json
                                   embedHostDomains) ~name ())
           (Some Values.CreateStackResult.to_json)
           (Some Values.CreateStackResult.error_to_json)])
let create_streaming_u_r_l =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (optional string) ~doc:"STRING String"
       and validity = flag "validity" (optional json_arg) ~doc:"JSON Long"
       and sessionContext =
         flag "session-context" (optional string) ~doc:"STRING String"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING String"
       and fleetName =
         flag "fleet-name" (required string) ~doc:"STRING String"
       and userId =
         flag "user-id" (required string) ~doc:"STRING StreamingUrlUserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_streaming_u_r_l
           (Values.CreateStreamingURLRequest.make ?applicationId
              ?validity:(Option.map ~f:Values.Long.of_json validity)
              ?sessionContext ~stackName ~fleetName ~userId ())
           (Some Values.CreateStreamingURLResult.to_json)
           (Some Values.CreateStreamingURLResult.error_to_json)])
let create_updated_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and newImageDescription =
         flag "new-image-description" (optional string)
           ~doc:"STRING Description"
       and newImageDisplayName =
         flag "new-image-display-name" (optional string)
           ~doc:"STRING DisplayName"
       and newImageTags =
         flag "new-image-tags" (optional json_arg) ~doc:"JSON Tags"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and existingImageName =
         flag "existing-image-name" (required string) ~doc:"STRING Name"
       and newImageName =
         flag "new-image-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_updated_image
           (Values.CreateUpdatedImageRequest.make ?newImageDescription
              ?newImageDisplayName
              ?newImageTags:(Option.map ~f:Values.Tags.of_json newImageTags)
              ?dryRun ~existingImageName ~newImageName ())
           (Some Values.CreateUpdatedImageResult.to_json)
           (Some Values.CreateUpdatedImageResult.error_to_json)])
let create_usage_report_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_usage_report_subscription
           (Values.CreateUsageReportSubscriptionRequest.make ())
           (Some Values.CreateUsageReportSubscriptionResult.to_json)
           (Some Values.CreateUsageReportSubscriptionResult.error_to_json)])
let create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and messageAction =
         flag "message-action" (optional json_arg) ~doc:"JSON MessageAction"
       and firstName =
         flag "first-name" (optional string) ~doc:"STRING UserAttributeValue"
       and lastName =
         flag "last-name" (optional string) ~doc:"STRING UserAttributeValue"
       and userName =
         flag "user-name" (required string) ~doc:"STRING Username"
       and authenticationType =
         flag "authentication-type" (required json_arg)
           ~doc:"JSON AuthenticationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user
           (Values.CreateUserRequest.make
              ?messageAction:(Option.map ~f:Values.MessageAction.of_json
                                messageAction) ?firstName ?lastName ~userName
              ~authenticationType:(Values.AuthenticationType.of_json
                                     authenticationType) ())
           (Some Values.CreateUserResult.to_json)
           (Some Values.CreateUserResult.error_to_json)])
let delete_app_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_block (Values.DeleteAppBlockRequest.make ~name ())
           (Some Values.DeleteAppBlockResult.to_json)
           (Some Values.DeleteAppBlockResult.error_to_json)])
let delete_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_application
           (Values.DeleteApplicationRequest.make ~name ())
           (Some Values.DeleteApplicationResult.to_json)
           (Some Values.DeleteApplicationResult.error_to_json)])
let delete_directory_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryName =
         flag "directory-name" (required string) ~doc:"STRING DirectoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_directory_config
           (Values.DeleteDirectoryConfigRequest.make ~directoryName ())
           (Some Values.DeleteDirectoryConfigResult.to_json)
           (Some Values.DeleteDirectoryConfigResult.error_to_json)])
let delete_entitlement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and stackName = flag "stack-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_entitlement
           (Values.DeleteEntitlementRequest.make ~name ~stackName ())
           (Some Values.DeleteEntitlementResult.to_json)
           (Some Values.DeleteEntitlementResult.error_to_json)])
let delete_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_fleet (Values.DeleteFleetRequest.make ~name ())
           (Some Values.DeleteFleetResult.to_json)
           (Some Values.DeleteFleetResult.error_to_json)])
let delete_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_image (Values.DeleteImageRequest.make ~name ())
           (Some Values.DeleteImageResult.to_json)
           (Some Values.DeleteImageResult.error_to_json)])
let delete_image_builder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_image_builder
           (Values.DeleteImageBuilderRequest.make ~name ())
           (Some Values.DeleteImageBuilderResult.to_json)
           (Some Values.DeleteImageBuilderResult.error_to_json)])
let delete_image_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and sharedAccountId =
         flag "shared-account-id" (required string)
           ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_image_permissions
           (Values.DeleteImagePermissionsRequest.make ~name ~sharedAccountId
              ()) (Some Values.DeleteImagePermissionsResult.to_json)
           (Some Values.DeleteImagePermissionsResult.error_to_json)])
let delete_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_stack (Values.DeleteStackRequest.make ~name ())
           (Some Values.DeleteStackResult.to_json)
           (Some Values.DeleteStackResult.error_to_json)])
let delete_usage_report_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_usage_report_subscription
           (Values.DeleteUsageReportSubscriptionRequest.make ())
           (Some Values.DeleteUsageReportSubscriptionResult.to_json)
           (Some Values.DeleteUsageReportSubscriptionResult.error_to_json)])
let delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING Username"
       and authenticationType =
         flag "authentication-type" (required json_arg)
           ~doc:"JSON AuthenticationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user
           (Values.DeleteUserRequest.make ~userName
              ~authenticationType:(Values.AuthenticationType.of_json
                                     authenticationType) ())
           (Some Values.DeleteUserResult.to_json)
           (Some Values.DeleteUserResult.error_to_json)])
let describe_app_blocks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arns = flag "arns" (optional json_arg) ~doc:"JSON ArnList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app_blocks
           (Values.DescribeAppBlocksRequest.make
              ?arns:(Option.map ~f:Values.ArnList.of_json arns) ?nextToken
              ?maxResults ()) (Some Values.DescribeAppBlocksResult.to_json)
           (Some Values.DescribeAppBlocksResult.error_to_json)])
let describe_application_fleet_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetName = flag "fleet-name" (optional string) ~doc:"STRING Name"
       and applicationArn =
         flag "application-arn" (optional string) ~doc:"STRING Arn"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_application_fleet_associations
           (Values.DescribeApplicationFleetAssociationsRequest.make
              ?fleetName ?applicationArn ?maxResults ?nextToken ())
           (Some Values.DescribeApplicationFleetAssociationsResult.to_json)
           (Some
              Values.DescribeApplicationFleetAssociationsResult.error_to_json)])
let describe_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arns = flag "arns" (optional json_arg) ~doc:"JSON ArnList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_applications
           (Values.DescribeApplicationsRequest.make
              ?arns:(Option.map ~f:Values.ArnList.of_json arns) ?nextToken
              ?maxResults ())
           (Some Values.DescribeApplicationsResult.to_json)
           (Some Values.DescribeApplicationsResult.error_to_json)])
let describe_directory_configs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryNames =
         flag "directory-names" (optional json_arg)
           ~doc:"JSON DirectoryNameList"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_directory_configs
           (Values.DescribeDirectoryConfigsRequest.make
              ?directoryNames:(Option.map ~f:Values.DirectoryNameList.of_json
                                 directoryNames) ?maxResults ?nextToken ())
           (Some Values.DescribeDirectoryConfigsResult.to_json)
           (Some Values.DescribeDirectoryConfigsResult.error_to_json)])
let describe_entitlements =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and stackName = flag "stack-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_entitlements
           (Values.DescribeEntitlementsRequest.make ?name ?nextToken
              ?maxResults ~stackName ())
           (Some Values.DescribeEntitlementsResult.to_json)
           (Some Values.DescribeEntitlementsResult.error_to_json)])
let describe_fleets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names = flag "names" (optional json_arg) ~doc:"JSON StringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleets
           (Values.DescribeFleetsRequest.make
              ?names:(Option.map ~f:Values.StringList.of_json names)
              ?nextToken ()) (Some Values.DescribeFleetsResult.to_json)
           (Some Values.DescribeFleetsResult.error_to_json)])
let describe_image_builders =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names = flag "names" (optional json_arg) ~doc:"JSON StringList"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_image_builders
           (Values.DescribeImageBuildersRequest.make
              ?names:(Option.map ~f:Values.StringList.of_json names)
              ?maxResults ?nextToken ())
           (Some Values.DescribeImageBuildersResult.to_json)
           (Some Values.DescribeImageBuildersResult.error_to_json)])
let describe_image_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and sharedAwsAccountIds =
         flag "shared-aws-account-ids" (optional json_arg)
           ~doc:"JSON AwsAccountIdList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_image_permissions
           (Values.DescribeImagePermissionsRequest.make ?maxResults
              ?sharedAwsAccountIds:(Option.map
                                      ~f:Values.AwsAccountIdList.of_json
                                      sharedAwsAccountIds) ?nextToken ~name
              ()) (Some Values.DescribeImagePermissionsResult.to_json)
           (Some Values.DescribeImagePermissionsResult.error_to_json)])
let describe_images =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names = flag "names" (optional json_arg) ~doc:"JSON StringList"
       and arns = flag "arns" (optional json_arg) ~doc:"JSON ArnList"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON VisibilityType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeImagesMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_images
           (Values.DescribeImagesRequest.make
              ?names:(Option.map ~f:Values.StringList.of_json names)
              ?arns:(Option.map ~f:Values.ArnList.of_json arns)
              ?type_:(Option.map ~f:Values.VisibilityType.of_json type_)
              ?nextToken ?maxResults ())
           (Some Values.DescribeImagesResult.to_json)
           (Some Values.DescribeImagesResult.error_to_json)])
let describe_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (optional string) ~doc:"STRING UserId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and limit = flag "limit" (optional int) ~doc:"INT Integer"
       and authenticationType =
         flag "authentication-type" (optional json_arg)
           ~doc:"JSON AuthenticationType"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING String"
       and fleetName =
         flag "fleet-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_sessions
           (Values.DescribeSessionsRequest.make ?userId ?nextToken ?limit
              ?authenticationType:(Option.map
                                     ~f:Values.AuthenticationType.of_json
                                     authenticationType) ~stackName
              ~fleetName ()) (Some Values.DescribeSessionsResult.to_json)
           (Some Values.DescribeSessionsResult.error_to_json)])
let describe_stacks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names = flag "names" (optional json_arg) ~doc:"JSON StringList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stacks
           (Values.DescribeStacksRequest.make
              ?names:(Option.map ~f:Values.StringList.of_json names)
              ?nextToken ()) (Some Values.DescribeStacksResult.to_json)
           (Some Values.DescribeStacksResult.error_to_json)])
let describe_usage_report_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_usage_report_subscriptions
           (Values.DescribeUsageReportSubscriptionsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.DescribeUsageReportSubscriptionsResult.to_json)
           (Some Values.DescribeUsageReportSubscriptionsResult.error_to_json)])
let describe_user_stack_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (optional string) ~doc:"STRING String"
       and userName =
         flag "user-name" (optional string) ~doc:"STRING Username"
       and authenticationType =
         flag "authentication-type" (optional json_arg)
           ~doc:"JSON AuthenticationType"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user_stack_associations
           (Values.DescribeUserStackAssociationsRequest.make ?stackName
              ?userName
              ?authenticationType:(Option.map
                                     ~f:Values.AuthenticationType.of_json
                                     authenticationType) ?maxResults
              ?nextToken ())
           (Some Values.DescribeUserStackAssociationsResult.to_json)
           (Some Values.DescribeUserStackAssociationsResult.error_to_json)])
let describe_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and authenticationType =
         flag "authentication-type" (required json_arg)
           ~doc:"JSON AuthenticationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_users
           (Values.DescribeUsersRequest.make ?maxResults ?nextToken
              ~authenticationType:(Values.AuthenticationType.of_json
                                     authenticationType) ())
           (Some Values.DescribeUsersResult.to_json)
           (Some Values.DescribeUsersResult.error_to_json)])
let disable_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING Username"
       and authenticationType =
         flag "authentication-type" (required json_arg)
           ~doc:"JSON AuthenticationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_user
           (Values.DisableUserRequest.make ~userName
              ~authenticationType:(Values.AuthenticationType.of_json
                                     authenticationType) ())
           (Some Values.DisableUserResult.to_json)
           (Some Values.DisableUserResult.error_to_json)])
let disassociate_application_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetName = flag "fleet-name" (required string) ~doc:"STRING Name"
       and applicationArn =
         flag "application-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_application_fleet
           (Values.DisassociateApplicationFleetRequest.make ~fleetName
              ~applicationArn ())
           (Some Values.DisassociateApplicationFleetResult.to_json)
           (Some Values.DisassociateApplicationFleetResult.error_to_json)])
let disassociate_application_from_entitlement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName = flag "stack-name" (required string) ~doc:"STRING Name"
       and entitlementName =
         flag "entitlement-name" (required string) ~doc:"STRING Name"
       and applicationIdentifier =
         flag "application-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_application_from_entitlement
           (Values.DisassociateApplicationFromEntitlementRequest.make
              ~stackName ~entitlementName ~applicationIdentifier ())
           (Some Values.DisassociateApplicationFromEntitlementResult.to_json)
           (Some
              Values.DisassociateApplicationFromEntitlementResult.error_to_json)])
let disassociate_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetName =
         flag "fleet-name" (required string) ~doc:"STRING String"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_fleet
           (Values.DisassociateFleetRequest.make ~fleetName ~stackName ())
           (Some Values.DisassociateFleetResult.to_json)
           (Some Values.DisassociateFleetResult.error_to_json)])
let enable_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING Username"
       and authenticationType =
         flag "authentication-type" (required json_arg)
           ~doc:"JSON AuthenticationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_user
           (Values.EnableUserRequest.make ~userName
              ~authenticationType:(Values.AuthenticationType.of_json
                                     authenticationType) ())
           (Some Values.EnableUserResult.to_json)
           (Some Values.EnableUserResult.error_to_json)])
let expire_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.expire_session (Values.ExpireSessionRequest.make ~sessionId ())
           (Some Values.ExpireSessionResult.to_json)
           (Some Values.ExpireSessionResult.error_to_json)])
let list_associated_fleets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and stackName =
         flag "stack-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_associated_fleets
           (Values.ListAssociatedFleetsRequest.make ?nextToken ~stackName ())
           (Some Values.ListAssociatedFleetsResult.to_json)
           (Some Values.ListAssociatedFleetsResult.error_to_json)])
let list_associated_stacks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and fleetName =
         flag "fleet-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_associated_stacks
           (Values.ListAssociatedStacksRequest.make ?nextToken ~fleetName ())
           (Some Values.ListAssociatedStacksResult.to_json)
           (Some Values.ListAssociatedStacksResult.error_to_json)])
let list_entitled_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and stackName = flag "stack-name" (required string) ~doc:"STRING Name"
       and entitlementName =
         flag "entitlement-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_entitled_applications
           (Values.ListEntitledApplicationsRequest.make ?nextToken
              ?maxResults ~stackName ~entitlementName ())
           (Some Values.ListEntitledApplicationsResult.to_json)
           (Some Values.ListEntitledApplicationsResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_fleet (Values.StartFleetRequest.make ~name ())
           (Some Values.StartFleetResult.to_json)
           (Some Values.StartFleetResult.error_to_json)])
let start_image_builder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appstreamAgentVersion =
         flag "appstream-agent-version" (optional string)
           ~doc:"STRING AppstreamAgentVersion"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_image_builder
           (Values.StartImageBuilderRequest.make ?appstreamAgentVersion ~name
              ()) (Some Values.StartImageBuilderResult.to_json)
           (Some Values.StartImageBuilderResult.error_to_json)])
let stop_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_fleet (Values.StopFleetRequest.make ~name ())
           (Some Values.StopFleetResult.to_json)
           (Some Values.StopFleetResult.error_to_json)])
let stop_image_builder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_image_builder
           (Values.StopImageBuilderRequest.make ~name ())
           (Some Values.StopImageBuilderResult.to_json)
           (Some Values.StopImageBuilderResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and iconS3Location =
         flag "icon-s3-location" (optional json_arg) ~doc:"JSON S3Location"
       and launchPath =
         flag "launch-path" (optional string) ~doc:"STRING String"
       and workingDirectory =
         flag "working-directory" (optional string) ~doc:"STRING String"
       and launchParameters =
         flag "launch-parameters" (optional string) ~doc:"STRING String"
       and appBlockArn =
         flag "app-block-arn" (optional string) ~doc:"STRING Arn"
       and attributesToDelete =
         flag "attributes-to-delete" (optional json_arg)
           ~doc:"JSON ApplicationAttributes"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application
           (Values.UpdateApplicationRequest.make ?displayName ?description
              ?iconS3Location:(Option.map ~f:Values.S3Location.of_json
                                 iconS3Location) ?launchPath
              ?workingDirectory ?launchParameters ?appBlockArn
              ?attributesToDelete:(Option.map
                                     ~f:Values.ApplicationAttributes.of_json
                                     attributesToDelete) ~name ())
           (Some Values.UpdateApplicationResult.to_json)
           (Some Values.UpdateApplicationResult.error_to_json)])
let update_directory_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationalUnitDistinguishedNames =
         flag "organizational-unit-distinguished-names" (optional json_arg)
           ~doc:"JSON OrganizationalUnitDistinguishedNamesList"
       and serviceAccountCredentials =
         flag "service-account-credentials" (optional json_arg)
           ~doc:"JSON ServiceAccountCredentials"
       and directoryName =
         flag "directory-name" (required string) ~doc:"STRING DirectoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_directory_config
           (Values.UpdateDirectoryConfigRequest.make
              ?organizationalUnitDistinguishedNames:(Option.map
                                                       ~f:Values.OrganizationalUnitDistinguishedNamesList.of_json
                                                       organizationalUnitDistinguishedNames)
              ?serviceAccountCredentials:(Option.map
                                            ~f:Values.ServiceAccountCredentials.of_json
                                            serviceAccountCredentials)
              ~directoryName ())
           (Some Values.UpdateDirectoryConfigResult.to_json)
           (Some Values.UpdateDirectoryConfigResult.error_to_json)])
let update_entitlement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and appVisibility =
         flag "app-visibility" (optional json_arg) ~doc:"JSON AppVisibility"
       and attributes =
         flag "attributes" (optional json_arg)
           ~doc:"JSON EntitlementAttributeList"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and stackName = flag "stack-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_entitlement
           (Values.UpdateEntitlementRequest.make ?description
              ?appVisibility:(Option.map ~f:Values.AppVisibility.of_json
                                appVisibility)
              ?attributes:(Option.map
                             ~f:Values.EntitlementAttributeList.of_json
                             attributes) ~name ~stackName ())
           (Some Values.UpdateEntitlementResult.to_json)
           (Some Values.UpdateEntitlementResult.error_to_json)])
let update_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and imageName =
         flag "image-name" (optional string) ~doc:"STRING String"
       and imageArn = flag "image-arn" (optional string) ~doc:"STRING Arn"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and instanceType =
         flag "instance-type" (optional string) ~doc:"STRING String"
       and computeCapacity =
         flag "compute-capacity" (optional json_arg)
           ~doc:"JSON ComputeCapacity"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and maxUserDurationInSeconds =
         flag "max-user-duration-in-seconds" (optional int)
           ~doc:"INT Integer"
       and disconnectTimeoutInSeconds =
         flag "disconnect-timeout-in-seconds" (optional int)
           ~doc:"INT Integer"
       and deleteVpcConfig =
         flag "delete-vpc-config" (optional bool) ~doc:"BOOL Boolean"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and enableDefaultInternetAccess =
         flag "enable-default-internet-access" (optional bool)
           ~doc:"BOOL BooleanObject"
       and domainJoinInfo =
         flag "domain-join-info" (optional json_arg)
           ~doc:"JSON DomainJoinInfo"
       and idleDisconnectTimeoutInSeconds =
         flag "idle-disconnect-timeout-in-seconds" (optional int)
           ~doc:"INT Integer"
       and attributesToDelete =
         flag "attributes-to-delete" (optional json_arg)
           ~doc:"JSON FleetAttributes"
       and iamRoleArn =
         flag "iam-role-arn" (optional string) ~doc:"STRING Arn"
       and streamView =
         flag "stream-view" (optional json_arg) ~doc:"JSON StreamView"
       and platform =
         flag "platform" (optional json_arg) ~doc:"JSON PlatformType"
       and maxConcurrentSessions =
         flag "max-concurrent-sessions" (optional int) ~doc:"INT Integer"
       and usbDeviceFilterStrings =
         flag "usb-device-filter-strings" (optional json_arg)
           ~doc:"JSON UsbDeviceFilterStrings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_fleet
           (Values.UpdateFleetRequest.make ?imageName ?imageArn ?name
              ?instanceType
              ?computeCapacity:(Option.map ~f:Values.ComputeCapacity.of_json
                                  computeCapacity)
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?maxUserDurationInSeconds ?disconnectTimeoutInSeconds
              ?deleteVpcConfig ?description ?displayName
              ?enableDefaultInternetAccess
              ?domainJoinInfo:(Option.map ~f:Values.DomainJoinInfo.of_json
                                 domainJoinInfo)
              ?idleDisconnectTimeoutInSeconds
              ?attributesToDelete:(Option.map
                                     ~f:Values.FleetAttributes.of_json
                                     attributesToDelete) ?iamRoleArn
              ?streamView:(Option.map ~f:Values.StreamView.of_json streamView)
              ?platform:(Option.map ~f:Values.PlatformType.of_json platform)
              ?maxConcurrentSessions
              ?usbDeviceFilterStrings:(Option.map
                                         ~f:Values.UsbDeviceFilterStrings.of_json
                                         usbDeviceFilterStrings) ())
           (Some Values.UpdateFleetResult.to_json)
           (Some Values.UpdateFleetResult.error_to_json)])
let update_image_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and sharedAccountId =
         flag "shared-account-id" (required string)
           ~doc:"STRING AwsAccountId"
       and imagePermissions =
         flag "image-permissions" (required json_arg)
           ~doc:"JSON ImagePermissions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_image_permissions
           (Values.UpdateImagePermissionsRequest.make ~name ~sharedAccountId
              ~imagePermissions:(Values.ImagePermissions.of_json
                                   imagePermissions) ())
           (Some Values.UpdateImagePermissionsResult.to_json)
           (Some Values.UpdateImagePermissionsResult.error_to_json)])
let update_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and storageConnectors =
         flag "storage-connectors" (optional json_arg)
           ~doc:"JSON StorageConnectorList"
       and deleteStorageConnectors =
         flag "delete-storage-connectors" (optional bool) ~doc:"BOOL Boolean"
       and redirectURL =
         flag "redirect-u-r-l" (optional string) ~doc:"STRING RedirectURL"
       and feedbackURL =
         flag "feedback-u-r-l" (optional string) ~doc:"STRING FeedbackURL"
       and attributesToDelete =
         flag "attributes-to-delete" (optional json_arg)
           ~doc:"JSON StackAttributes"
       and userSettings =
         flag "user-settings" (optional json_arg) ~doc:"JSON UserSettingList"
       and applicationSettings =
         flag "application-settings" (optional json_arg)
           ~doc:"JSON ApplicationSettings"
       and accessEndpoints =
         flag "access-endpoints" (optional json_arg)
           ~doc:"JSON AccessEndpointList"
       and embedHostDomains =
         flag "embed-host-domains" (optional json_arg)
           ~doc:"JSON EmbedHostDomains"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_stack
           (Values.UpdateStackRequest.make ?displayName ?description
              ?storageConnectors:(Option.map
                                    ~f:Values.StorageConnectorList.of_json
                                    storageConnectors)
              ?deleteStorageConnectors ?redirectURL ?feedbackURL
              ?attributesToDelete:(Option.map
                                     ~f:Values.StackAttributes.of_json
                                     attributesToDelete)
              ?userSettings:(Option.map ~f:Values.UserSettingList.of_json
                               userSettings)
              ?applicationSettings:(Option.map
                                      ~f:Values.ApplicationSettings.of_json
                                      applicationSettings)
              ?accessEndpoints:(Option.map
                                  ~f:Values.AccessEndpointList.of_json
                                  accessEndpoints)
              ?embedHostDomains:(Option.map
                                   ~f:Values.EmbedHostDomains.of_json
                                   embedHostDomains) ~name ())
           (Some Values.UpdateStackResult.to_json)
           (Some Values.UpdateStackResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-application-fleet", associate_application_fleet);
    ("associate-application-to-entitlement",
      associate_application_to_entitlement);
    ("associate-fleet", associate_fleet);
    ("batch-associate-user-stack", batch_associate_user_stack);
    ("batch-disassociate-user-stack", batch_disassociate_user_stack);
    ("copy-image", copy_image);
    ("create-app-block", create_app_block);
    ("create-application", create_application);
    ("create-directory-config", create_directory_config);
    ("create-entitlement", create_entitlement);
    ("create-fleet", create_fleet);
    ("create-image-builder", create_image_builder);
    ("create-image-builder-streaming-u-r-l",
      create_image_builder_streaming_u_r_l);
    ("create-stack", create_stack);
    ("create-streaming-u-r-l", create_streaming_u_r_l);
    ("create-updated-image", create_updated_image);
    ("create-usage-report-subscription", create_usage_report_subscription);
    ("create-user", create_user);
    ("delete-app-block", delete_app_block);
    ("delete-application", delete_application);
    ("delete-directory-config", delete_directory_config);
    ("delete-entitlement", delete_entitlement);
    ("delete-fleet", delete_fleet);
    ("delete-image", delete_image);
    ("delete-image-builder", delete_image_builder);
    ("delete-image-permissions", delete_image_permissions);
    ("delete-stack", delete_stack);
    ("delete-usage-report-subscription", delete_usage_report_subscription);
    ("delete-user", delete_user);
    ("describe-app-blocks", describe_app_blocks);
    ("describe-application-fleet-associations",
      describe_application_fleet_associations);
    ("describe-applications", describe_applications);
    ("describe-directory-configs", describe_directory_configs);
    ("describe-entitlements", describe_entitlements);
    ("describe-fleets", describe_fleets);
    ("describe-image-builders", describe_image_builders);
    ("describe-image-permissions", describe_image_permissions);
    ("describe-images", describe_images);
    ("describe-sessions", describe_sessions);
    ("describe-stacks", describe_stacks);
    ("describe-usage-report-subscriptions",
      describe_usage_report_subscriptions);
    ("describe-user-stack-associations", describe_user_stack_associations);
    ("describe-users", describe_users);
    ("disable-user", disable_user);
    ("disassociate-application-fleet", disassociate_application_fleet);
    ("disassociate-application-from-entitlement",
      disassociate_application_from_entitlement);
    ("disassociate-fleet", disassociate_fleet);
    ("enable-user", enable_user);
    ("expire-session", expire_session);
    ("list-associated-fleets", list_associated_fleets);
    ("list-associated-stacks", list_associated_stacks);
    ("list-entitled-applications", list_entitled_applications);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-fleet", start_fleet);
    ("start-image-builder", start_image_builder);
    ("stop-fleet", stop_fleet);
    ("stop-image-builder", stop_image_builder);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-application", update_application);
    ("update-directory-config", update_directory_config);
    ("update-entitlement", update_entitlement);
    ("update-fleet", update_fleet);
    ("update-image-permissions", update_image_permissions);
    ("update-stack", update_stack)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_batch_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterArn = flag "filter-arn" (optional string) ~doc:"STRING Arn"
       and numResults =
         flag "num-results" (optional int) ~doc:"INT NumBatchResults"
       and batchInferenceJobConfig =
         flag "batch-inference-job-config" (optional json_arg)
           ~doc:"JSON BatchInferenceJobConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and jobName = flag "job-name" (required string) ~doc:"STRING Name"
       and solutionVersionArn =
         flag "solution-version-arn" (required string) ~doc:"STRING Arn"
       and jobInput =
         flag "job-input" (required json_arg)
           ~doc:"JSON BatchInferenceJobInput"
       and jobOutput =
         flag "job-output" (required json_arg)
           ~doc:"JSON BatchInferenceJobOutput"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_batch_inference_job
           (Values.CreateBatchInferenceJobRequest.make ?filterArn ?numResults
              ?batchInferenceJobConfig:(Option.map
                                          ~f:Values.BatchInferenceJobConfig.of_json
                                          batchInferenceJobConfig)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~jobName
              ~solutionVersionArn
              ~jobInput:(Values.BatchInferenceJobInput.of_json jobInput)
              ~jobOutput:(Values.BatchInferenceJobOutput.of_json jobOutput)
              ~roleArn ())
           (Some Values.CreateBatchInferenceJobResponse.to_json)
           (Some Values.CreateBatchInferenceJobResponse.error_to_json)])
let create_batch_segment_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterArn = flag "filter-arn" (optional string) ~doc:"STRING Arn"
       and numResults =
         flag "num-results" (optional int) ~doc:"INT NumBatchResults"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and jobName = flag "job-name" (required string) ~doc:"STRING Name"
       and solutionVersionArn =
         flag "solution-version-arn" (required string) ~doc:"STRING Arn"
       and jobInput =
         flag "job-input" (required json_arg)
           ~doc:"JSON BatchSegmentJobInput"
       and jobOutput =
         flag "job-output" (required json_arg)
           ~doc:"JSON BatchSegmentJobOutput"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_batch_segment_job
           (Values.CreateBatchSegmentJobRequest.make ?filterArn ?numResults
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~jobName
              ~solutionVersionArn
              ~jobInput:(Values.BatchSegmentJobInput.of_json jobInput)
              ~jobOutput:(Values.BatchSegmentJobOutput.of_json jobOutput)
              ~roleArn ())
           (Some Values.CreateBatchSegmentJobResponse.to_json)
           (Some Values.CreateBatchSegmentJobResponse.error_to_json)])
let create_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and minProvisionedTPS =
         flag "min-provisioned-t-p-s" (optional int)
           ~doc:"INT TransactionsPerSecond"
       and campaignConfig =
         flag "campaign-config" (optional json_arg)
           ~doc:"JSON CampaignConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and solutionVersionArn =
         flag "solution-version-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_campaign
           (Values.CreateCampaignRequest.make ?minProvisionedTPS
              ?campaignConfig:(Option.map ~f:Values.CampaignConfig.of_json
                                 campaignConfig)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~solutionVersionArn ())
           (Some Values.CreateCampaignResponse.to_json)
           (Some Values.CreateCampaignResponse.error_to_json)])
let create_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn"
       and datasetGroupArn =
         flag "dataset-group-arn" (required string) ~doc:"STRING Arn"
       and datasetType =
         flag "dataset-type" (required string) ~doc:"STRING DatasetType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dataset
           (Values.CreateDatasetRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name ~schemaArn
              ~datasetGroupArn ~datasetType ())
           (Some Values.CreateDatasetResponse.to_json)
           (Some Values.CreateDatasetResponse.error_to_json)])
let create_dataset_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ingestionMode =
         flag "ingestion-mode" (optional json_arg) ~doc:"JSON IngestionMode"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and jobName = flag "job-name" (required string) ~doc:"STRING Name"
       and datasetArn =
         flag "dataset-arn" (required string) ~doc:"STRING Arn"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and jobOutput =
         flag "job-output" (required json_arg)
           ~doc:"JSON DatasetExportJobOutput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dataset_export_job
           (Values.CreateDatasetExportJobRequest.make
              ?ingestionMode:(Option.map ~f:Values.IngestionMode.of_json
                                ingestionMode)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~jobName
              ~datasetArn ~roleArn
              ~jobOutput:(Values.DatasetExportJobOutput.of_json jobOutput) ())
           (Some Values.CreateDatasetExportJobResponse.to_json)
           (Some Values.CreateDatasetExportJobResponse.error_to_json)])
let create_dataset_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and domain = flag "domain" (optional json_arg) ~doc:"JSON Domain"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dataset_group
           (Values.CreateDatasetGroupRequest.make ?roleArn ?kmsKeyArn
              ?domain:(Option.map ~f:Values.Domain.of_json domain)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name ())
           (Some Values.CreateDatasetGroupResponse.to_json)
           (Some Values.CreateDatasetGroupResponse.error_to_json)])
let create_dataset_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and jobName = flag "job-name" (required string) ~doc:"STRING Name"
       and datasetArn =
         flag "dataset-arn" (required string) ~doc:"STRING Arn"
       and dataSource =
         flag "data-source" (required json_arg) ~doc:"JSON DataSource"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dataset_import_job
           (Values.CreateDatasetImportJobRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~jobName
              ~datasetArn ~dataSource:(Values.DataSource.of_json dataSource)
              ~roleArn ())
           (Some Values.CreateDatasetImportJobResponse.to_json)
           (Some Values.CreateDatasetImportJobResponse.error_to_json)])
let create_event_tracker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and datasetGroupArn =
         flag "dataset-group-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_event_tracker
           (Values.CreateEventTrackerRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~datasetGroupArn ())
           (Some Values.CreateEventTrackerResponse.to_json)
           (Some Values.CreateEventTrackerResponse.error_to_json)])
let create_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and datasetGroupArn =
         flag "dataset-group-arn" (required string) ~doc:"STRING Arn"
       and filterExpression =
         flag "filter-expression" (required string)
           ~doc:"STRING FilterExpression" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_filter
           (Values.CreateFilterRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~datasetGroupArn ~filterExpression ())
           (Some Values.CreateFilterResponse.to_json)
           (Some Values.CreateFilterResponse.error_to_json)])
let create_recommender =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recommenderConfig =
         flag "recommender-config" (optional json_arg)
           ~doc:"JSON RecommenderConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and datasetGroupArn =
         flag "dataset-group-arn" (required string) ~doc:"STRING Arn"
       and recipeArn = flag "recipe-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_recommender
           (Values.CreateRecommenderRequest.make
              ?recommenderConfig:(Option.map
                                    ~f:Values.RecommenderConfig.of_json
                                    recommenderConfig)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~datasetGroupArn ~recipeArn ())
           (Some Values.CreateRecommenderResponse.to_json)
           (Some Values.CreateRecommenderResponse.error_to_json)])
let create_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (optional json_arg) ~doc:"JSON Domain"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and schema = flag "schema" (required string) ~doc:"STRING AvroSchema" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_schema
           (Values.CreateSchemaRequest.make
              ?domain:(Option.map ~f:Values.Domain.of_json domain) ~name
              ~schema ()) (Some Values.CreateSchemaResponse.to_json)
           (Some Values.CreateSchemaResponse.error_to_json)])
let create_solution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and performHPO =
         flag "perform-h-p-o" (optional bool) ~doc:"BOOL Boolean"
       and performAutoML =
         flag "perform-auto-m-l" (optional bool) ~doc:"BOOL PerformAutoML"
       and recipeArn = flag "recipe-arn" (optional string) ~doc:"STRING Arn"
       and eventType =
         flag "event-type" (optional string) ~doc:"STRING EventType"
       and solutionConfig =
         flag "solution-config" (optional json_arg)
           ~doc:"JSON SolutionConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and datasetGroupArn =
         flag "dataset-group-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_solution
           (Values.CreateSolutionRequest.make ?performHPO ?performAutoML
              ?recipeArn ?eventType
              ?solutionConfig:(Option.map ~f:Values.SolutionConfig.of_json
                                 solutionConfig)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~datasetGroupArn ())
           (Some Values.CreateSolutionResponse.to_json)
           (Some Values.CreateSolutionResponse.error_to_json)])
let create_solution_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trainingMode =
         flag "training-mode" (optional json_arg) ~doc:"JSON TrainingMode"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and solutionArn =
         flag "solution-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_solution_version
           (Values.CreateSolutionVersionRequest.make
              ?trainingMode:(Option.map ~f:Values.TrainingMode.of_json
                               trainingMode)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~solutionArn ())
           (Some Values.CreateSolutionVersionResponse.to_json)
           (Some Values.CreateSolutionVersionResponse.error_to_json)])
let delete_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and campaignArn =
         flag "campaign-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_campaign
           (Values.DeleteCampaignRequest.make ~campaignArn ()) None None])
let delete_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetArn =
         flag "dataset-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dataset
           (Values.DeleteDatasetRequest.make ~datasetArn ()) None None])
let delete_dataset_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetGroupArn =
         flag "dataset-group-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dataset_group
           (Values.DeleteDatasetGroupRequest.make ~datasetGroupArn ()) None
           None])
let delete_event_tracker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventTrackerArn =
         flag "event-tracker-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event_tracker
           (Values.DeleteEventTrackerRequest.make ~eventTrackerArn ()) None
           None])
let delete_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterArn = flag "filter-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_filter (Values.DeleteFilterRequest.make ~filterArn ())
           None None])
let delete_recommender =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recommenderArn =
         flag "recommender-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_recommender
           (Values.DeleteRecommenderRequest.make ~recommenderArn ()) None
           None])
let delete_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_schema (Values.DeleteSchemaRequest.make ~schemaArn ())
           None None])
let delete_solution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and solutionArn =
         flag "solution-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_solution
           (Values.DeleteSolutionRequest.make ~solutionArn ()) None None])
let describe_algorithm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and algorithmArn =
         flag "algorithm-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_algorithm
           (Values.DescribeAlgorithmRequest.make ~algorithmArn ())
           (Some Values.DescribeAlgorithmResponse.to_json)
           (Some Values.DescribeAlgorithmResponse.error_to_json)])
let describe_batch_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and batchInferenceJobArn =
         flag "batch-inference-job-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_batch_inference_job
           (Values.DescribeBatchInferenceJobRequest.make
              ~batchInferenceJobArn ())
           (Some Values.DescribeBatchInferenceJobResponse.to_json)
           (Some Values.DescribeBatchInferenceJobResponse.error_to_json)])
let describe_batch_segment_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and batchSegmentJobArn =
         flag "batch-segment-job-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_batch_segment_job
           (Values.DescribeBatchSegmentJobRequest.make ~batchSegmentJobArn ())
           (Some Values.DescribeBatchSegmentJobResponse.to_json)
           (Some Values.DescribeBatchSegmentJobResponse.error_to_json)])
let describe_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and campaignArn =
         flag "campaign-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_campaign
           (Values.DescribeCampaignRequest.make ~campaignArn ())
           (Some Values.DescribeCampaignResponse.to_json)
           (Some Values.DescribeCampaignResponse.error_to_json)])
let describe_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetArn =
         flag "dataset-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dataset
           (Values.DescribeDatasetRequest.make ~datasetArn ())
           (Some Values.DescribeDatasetResponse.to_json)
           (Some Values.DescribeDatasetResponse.error_to_json)])
let describe_dataset_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetExportJobArn =
         flag "dataset-export-job-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dataset_export_job
           (Values.DescribeDatasetExportJobRequest.make ~datasetExportJobArn
              ()) (Some Values.DescribeDatasetExportJobResponse.to_json)
           (Some Values.DescribeDatasetExportJobResponse.error_to_json)])
let describe_dataset_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetGroupArn =
         flag "dataset-group-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dataset_group
           (Values.DescribeDatasetGroupRequest.make ~datasetGroupArn ())
           (Some Values.DescribeDatasetGroupResponse.to_json)
           (Some Values.DescribeDatasetGroupResponse.error_to_json)])
let describe_dataset_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetImportJobArn =
         flag "dataset-import-job-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dataset_import_job
           (Values.DescribeDatasetImportJobRequest.make ~datasetImportJobArn
              ()) (Some Values.DescribeDatasetImportJobResponse.to_json)
           (Some Values.DescribeDatasetImportJobResponse.error_to_json)])
let describe_event_tracker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventTrackerArn =
         flag "event-tracker-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_tracker
           (Values.DescribeEventTrackerRequest.make ~eventTrackerArn ())
           (Some Values.DescribeEventTrackerResponse.to_json)
           (Some Values.DescribeEventTrackerResponse.error_to_json)])
let describe_feature_transformation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and featureTransformationArn =
         flag "feature-transformation-arn" (required string)
           ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_feature_transformation
           (Values.DescribeFeatureTransformationRequest.make
              ~featureTransformationArn ())
           (Some Values.DescribeFeatureTransformationResponse.to_json)
           (Some Values.DescribeFeatureTransformationResponse.error_to_json)])
let describe_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterArn = flag "filter-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_filter
           (Values.DescribeFilterRequest.make ~filterArn ())
           (Some Values.DescribeFilterResponse.to_json)
           (Some Values.DescribeFilterResponse.error_to_json)])
let describe_recipe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recipeArn = flag "recipe-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_recipe
           (Values.DescribeRecipeRequest.make ~recipeArn ())
           (Some Values.DescribeRecipeResponse.to_json)
           (Some Values.DescribeRecipeResponse.error_to_json)])
let describe_recommender =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recommenderArn =
         flag "recommender-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_recommender
           (Values.DescribeRecommenderRequest.make ~recommenderArn ())
           (Some Values.DescribeRecommenderResponse.to_json)
           (Some Values.DescribeRecommenderResponse.error_to_json)])
let describe_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaArn = flag "schema-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_schema
           (Values.DescribeSchemaRequest.make ~schemaArn ())
           (Some Values.DescribeSchemaResponse.to_json)
           (Some Values.DescribeSchemaResponse.error_to_json)])
let describe_solution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and solutionArn =
         flag "solution-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_solution
           (Values.DescribeSolutionRequest.make ~solutionArn ())
           (Some Values.DescribeSolutionResponse.to_json)
           (Some Values.DescribeSolutionResponse.error_to_json)])
let describe_solution_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and solutionVersionArn =
         flag "solution-version-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_solution_version
           (Values.DescribeSolutionVersionRequest.make ~solutionVersionArn ())
           (Some Values.DescribeSolutionVersionResponse.to_json)
           (Some Values.DescribeSolutionVersionResponse.error_to_json)])
let get_solution_metrics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and solutionVersionArn =
         flag "solution-version-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_solution_metrics
           (Values.GetSolutionMetricsRequest.make ~solutionVersionArn ())
           (Some Values.GetSolutionMetricsResponse.to_json)
           (Some Values.GetSolutionMetricsResponse.error_to_json)])
let list_batch_inference_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and solutionVersionArn =
         flag "solution-version-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_batch_inference_jobs
           (Values.ListBatchInferenceJobsRequest.make ?solutionVersionArn
              ?nextToken ?maxResults ())
           (Some Values.ListBatchInferenceJobsResponse.to_json)
           (Some Values.ListBatchInferenceJobsResponse.error_to_json)])
let list_batch_segment_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and solutionVersionArn =
         flag "solution-version-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_batch_segment_jobs
           (Values.ListBatchSegmentJobsRequest.make ?solutionVersionArn
              ?nextToken ?maxResults ())
           (Some Values.ListBatchSegmentJobsResponse.to_json)
           (Some Values.ListBatchSegmentJobsResponse.error_to_json)])
let list_campaigns =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and solutionArn =
         flag "solution-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_campaigns
           (Values.ListCampaignsRequest.make ?solutionArn ?nextToken
              ?maxResults ()) (Some Values.ListCampaignsResponse.to_json)
           (Some Values.ListCampaignsResponse.error_to_json)])
let list_dataset_export_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetArn =
         flag "dataset-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dataset_export_jobs
           (Values.ListDatasetExportJobsRequest.make ?datasetArn ?nextToken
              ?maxResults ())
           (Some Values.ListDatasetExportJobsResponse.to_json)
           (Some Values.ListDatasetExportJobsResponse.error_to_json)])
let list_dataset_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dataset_groups
           (Values.ListDatasetGroupsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListDatasetGroupsResponse.to_json)
           (Some Values.ListDatasetGroupsResponse.error_to_json)])
let list_dataset_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetArn =
         flag "dataset-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dataset_import_jobs
           (Values.ListDatasetImportJobsRequest.make ?datasetArn ?nextToken
              ?maxResults ())
           (Some Values.ListDatasetImportJobsResponse.to_json)
           (Some Values.ListDatasetImportJobsResponse.error_to_json)])
let list_datasets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetGroupArn =
         flag "dataset-group-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_datasets
           (Values.ListDatasetsRequest.make ?datasetGroupArn ?nextToken
              ?maxResults ()) (Some Values.ListDatasetsResponse.to_json)
           (Some Values.ListDatasetsResponse.error_to_json)])
let list_event_trackers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetGroupArn =
         flag "dataset-group-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_event_trackers
           (Values.ListEventTrackersRequest.make ?datasetGroupArn ?nextToken
              ?maxResults ()) (Some Values.ListEventTrackersResponse.to_json)
           (Some Values.ListEventTrackersResponse.error_to_json)])
let list_filters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetGroupArn =
         flag "dataset-group-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_filters
           (Values.ListFiltersRequest.make ?datasetGroupArn ?nextToken
              ?maxResults ()) (Some Values.ListFiltersResponse.to_json)
           (Some Values.ListFiltersResponse.error_to_json)])
let list_recipes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recipeProvider =
         flag "recipe-provider" (optional json_arg)
           ~doc:"JSON RecipeProvider"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and domain = flag "domain" (optional json_arg) ~doc:"JSON Domain" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recipes
           (Values.ListRecipesRequest.make
              ?recipeProvider:(Option.map ~f:Values.RecipeProvider.of_json
                                 recipeProvider) ?nextToken ?maxResults
              ?domain:(Option.map ~f:Values.Domain.of_json domain) ())
           (Some Values.ListRecipesResponse.to_json)
           (Some Values.ListRecipesResponse.error_to_json)])
let list_recommenders =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetGroupArn =
         flag "dataset-group-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recommenders
           (Values.ListRecommendersRequest.make ?datasetGroupArn ?nextToken
              ?maxResults ()) (Some Values.ListRecommendersResponse.to_json)
           (Some Values.ListRecommendersResponse.error_to_json)])
let list_schemas =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_schemas
           (Values.ListSchemasRequest.make ?nextToken ?maxResults ())
           (Some Values.ListSchemasResponse.to_json)
           (Some Values.ListSchemasResponse.error_to_json)])
let list_solution_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and solutionArn =
         flag "solution-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_solution_versions
           (Values.ListSolutionVersionsRequest.make ?solutionArn ?nextToken
              ?maxResults ())
           (Some Values.ListSolutionVersionsResponse.to_json)
           (Some Values.ListSolutionVersionsResponse.error_to_json)])
let list_solutions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetGroupArn =
         flag "dataset-group-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_solutions
           (Values.ListSolutionsRequest.make ?datasetGroupArn ?nextToken
              ?maxResults ()) (Some Values.ListSolutionsResponse.to_json)
           (Some Values.ListSolutionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let stop_solution_version_creation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and solutionVersionArn =
         flag "solution-version-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_solution_version_creation
           (Values.StopSolutionVersionCreationRequest.make
              ~solutionVersionArn ()) None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and solutionVersionArn =
         flag "solution-version-arn" (optional string) ~doc:"STRING Arn"
       and minProvisionedTPS =
         flag "min-provisioned-t-p-s" (optional int)
           ~doc:"INT TransactionsPerSecond"
       and campaignConfig =
         flag "campaign-config" (optional json_arg)
           ~doc:"JSON CampaignConfig"
       and campaignArn =
         flag "campaign-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_campaign
           (Values.UpdateCampaignRequest.make ?solutionVersionArn
              ?minProvisionedTPS
              ?campaignConfig:(Option.map ~f:Values.CampaignConfig.of_json
                                 campaignConfig) ~campaignArn ())
           (Some Values.UpdateCampaignResponse.to_json)
           (Some Values.UpdateCampaignResponse.error_to_json)])
let update_recommender =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recommenderArn =
         flag "recommender-arn" (required string) ~doc:"STRING Arn"
       and recommenderConfig =
         flag "recommender-config" (required json_arg)
           ~doc:"JSON RecommenderConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_recommender
           (Values.UpdateRecommenderRequest.make ~recommenderArn
              ~recommenderConfig:(Values.RecommenderConfig.of_json
                                    recommenderConfig) ())
           (Some Values.UpdateRecommenderResponse.to_json)
           (Some Values.UpdateRecommenderResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-batch-inference-job", create_batch_inference_job);
    ("create-batch-segment-job", create_batch_segment_job);
    ("create-campaign", create_campaign);
    ("create-dataset", create_dataset);
    ("create-dataset-export-job", create_dataset_export_job);
    ("create-dataset-group", create_dataset_group);
    ("create-dataset-import-job", create_dataset_import_job);
    ("create-event-tracker", create_event_tracker);
    ("create-filter", create_filter);
    ("create-recommender", create_recommender);
    ("create-schema", create_schema);
    ("create-solution", create_solution);
    ("create-solution-version", create_solution_version);
    ("delete-campaign", delete_campaign);
    ("delete-dataset", delete_dataset);
    ("delete-dataset-group", delete_dataset_group);
    ("delete-event-tracker", delete_event_tracker);
    ("delete-filter", delete_filter);
    ("delete-recommender", delete_recommender);
    ("delete-schema", delete_schema);
    ("delete-solution", delete_solution);
    ("describe-algorithm", describe_algorithm);
    ("describe-batch-inference-job", describe_batch_inference_job);
    ("describe-batch-segment-job", describe_batch_segment_job);
    ("describe-campaign", describe_campaign);
    ("describe-dataset", describe_dataset);
    ("describe-dataset-export-job", describe_dataset_export_job);
    ("describe-dataset-group", describe_dataset_group);
    ("describe-dataset-import-job", describe_dataset_import_job);
    ("describe-event-tracker", describe_event_tracker);
    ("describe-feature-transformation", describe_feature_transformation);
    ("describe-filter", describe_filter);
    ("describe-recipe", describe_recipe);
    ("describe-recommender", describe_recommender);
    ("describe-schema", describe_schema);
    ("describe-solution", describe_solution);
    ("describe-solution-version", describe_solution_version);
    ("get-solution-metrics", get_solution_metrics);
    ("list-batch-inference-jobs", list_batch_inference_jobs);
    ("list-batch-segment-jobs", list_batch_segment_jobs);
    ("list-campaigns", list_campaigns);
    ("list-dataset-export-jobs", list_dataset_export_jobs);
    ("list-dataset-groups", list_dataset_groups);
    ("list-dataset-import-jobs", list_dataset_import_jobs);
    ("list-datasets", list_datasets);
    ("list-event-trackers", list_event_trackers);
    ("list-filters", list_filters);
    ("list-recipes", list_recipes);
    ("list-recommenders", list_recommenders);
    ("list-schemas", list_schemas);
    ("list-solution-versions", list_solution_versions);
    ("list-solutions", list_solutions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("stop-solution-version-creation", stop_solution_version_creation);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-campaign", update_campaign);
    ("update-recommender", update_recommender)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.personalize
let apiVersion = "2018-05-22"
let endpointPrefix = "personalize"
let serviceFullName = "Amazon Personalize"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let targetPrefix = "AmazonPersonalize"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module CategoricalValue =
  struct
    type nonrec t = string
    let context_ = "CategoricalValue"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1000); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CategoricalValue" j
    let to_json = simple_to_json to_value
  end
module CategoricalValues =
  struct
    type nonrec t = CategoricalValue.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:CategoricalValue.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CategoricalValue.of_xml)
    let of_json j =
      list_of_json ~kind:"CategoricalValues"
        ~of_json:CategoricalValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ParameterName =
  struct
    type nonrec t = string
    let context_ = "ParameterName"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ParameterName" j
    let to_json = simple_to_json to_value
  end
module ContinuousMaxValue =
  struct
    type nonrec t = float
    let make i =
      let open Result in
        ok_or_failwith (check_float_min i ~min:(-1000000.)); i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module ContinuousMinValue =
  struct
    type nonrec t = float
    let make i =
      let open Result in
        ok_or_failwith (check_float_min i ~min:(-1000000.)); i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module IntegerMaxValue =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_max i ~max:1000000); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for IntegerMaxValue" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module IntegerMinValue =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:(-1000000)); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for IntegerMinValue" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CategoricalHyperParameterRange =
  struct
    type nonrec t =
      {
      name: ParameterName.t option
        [@ocaml.doc "The name of the hyperparameter."];
      values: CategoricalValues.t option
        [@ocaml.doc "A list of the categories for the hyperparameter."]}
    let make ?name = fun ?values -> fun () -> { name; values }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:ParameterName.to_value));
        ("values", (Option.map x.values ~f:CategoricalValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        (Option.map ~f:CategoricalValues.of_xml)
          (Xml.child xml_arg0 "values") in
      let name =
        (Option.map ~f:ParameterName.of_xml) (Xml.child xml_arg0 "name") in
      make ?values ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let values = field_map json "values" CategoricalValues.of_json in
      let name = field_map json "name" ParameterName.of_json in
      make ?values ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides the name and range of a categorical hyperparameter."]
module ContinuousHyperParameterRange =
  struct
    type nonrec t =
      {
      name: ParameterName.t option
        [@ocaml.doc "The name of the hyperparameter."];
      minValue: ContinuousMinValue.t option
        [@ocaml.doc "The minimum allowable value for the hyperparameter."];
      maxValue: ContinuousMaxValue.t option
        [@ocaml.doc "The maximum allowable value for the hyperparameter."]}
    let make ?name =
      fun ?minValue ->
        fun ?maxValue -> fun () -> { name; minValue; maxValue }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:ParameterName.to_value));
        ("minValue", (Option.map x.minValue ~f:ContinuousMinValue.to_value));
        ("maxValue", (Option.map x.maxValue ~f:ContinuousMaxValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxValue =
        (Option.map ~f:ContinuousMaxValue.of_xml)
          (Xml.child xml_arg0 "maxValue") in
      let minValue =
        (Option.map ~f:ContinuousMinValue.of_xml)
          (Xml.child xml_arg0 "minValue") in
      let name =
        (Option.map ~f:ParameterName.of_xml) (Xml.child xml_arg0 "name") in
      make ?maxValue ?minValue ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxValue = field_map json "maxValue" ContinuousMaxValue.of_json in
      let minValue = field_map json "minValue" ContinuousMinValue.of_json in
      let name = field_map json "name" ParameterName.of_json in
      make ?maxValue ?minValue ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides the name and range of a continuous hyperparameter."]
module IntegerHyperParameterRange =
  struct
    type nonrec t =
      {
      name: ParameterName.t option
        [@ocaml.doc "The name of the hyperparameter."];
      minValue: IntegerMinValue.t option
        [@ocaml.doc "The minimum allowable value for the hyperparameter."];
      maxValue: IntegerMaxValue.t option
        [@ocaml.doc "The maximum allowable value for the hyperparameter."]}
    let make ?name =
      fun ?minValue ->
        fun ?maxValue -> fun () -> { name; minValue; maxValue }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:ParameterName.to_value));
        ("minValue", (Option.map x.minValue ~f:IntegerMinValue.to_value));
        ("maxValue", (Option.map x.maxValue ~f:IntegerMaxValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxValue =
        (Option.map ~f:IntegerMaxValue.of_xml)
          (Xml.child xml_arg0 "maxValue") in
      let minValue =
        (Option.map ~f:IntegerMinValue.of_xml)
          (Xml.child xml_arg0 "minValue") in
      let name =
        (Option.map ~f:ParameterName.of_xml) (Xml.child xml_arg0 "name") in
      make ?maxValue ?minValue ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxValue = field_map json "maxValue" IntegerMaxValue.of_json in
      let minValue = field_map json "minValue" IntegerMinValue.of_json in
      let name = field_map json "name" ParameterName.of_json in
      make ?maxValue ?minValue ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides the name and range of an integer-valued hyperparameter."]
module ParameterValue =
  struct
    type nonrec t = string
    let context_ = "ParameterValue"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1000); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ParameterValue" j
    let to_json = simple_to_json to_value
  end
module Arn =
  struct
    type nonrec t = string
    let context_ = "Arn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"arn:([a-z\\d-]+):personalize:.*:.*:.+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Arn" j
    let to_json = simple_to_json to_value
  end
module HPOObjectiveType =
  struct
    type nonrec t = string
    let context_ = "HPOObjectiveType"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HPOObjectiveType" j
    let to_json = simple_to_json to_value
  end
module MetricName =
  struct
    type nonrec t = string
    let context_ = "MetricName"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MetricName" j
    let to_json = simple_to_json to_value
  end
module MetricRegex =
  struct
    type nonrec t = string
    let context_ = "MetricRegex"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MetricRegex" j
    let to_json = simple_to_json to_value
  end
module HPOResource =
  struct
    type nonrec t = string
    let context_ = "HPOResource"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HPOResource" j
    let to_json = simple_to_json to_value
  end
module CategoricalHyperParameterRanges =
  struct
    type nonrec t = CategoricalHyperParameterRange.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:CategoricalHyperParameterRange.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CategoricalHyperParameterRange.of_xml)
    let of_json j =
      list_of_json ~kind:"CategoricalHyperParameterRanges"
        ~of_json:CategoricalHyperParameterRange.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContinuousHyperParameterRanges =
  struct
    type nonrec t = ContinuousHyperParameterRange.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:ContinuousHyperParameterRange.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContinuousHyperParameterRange.of_xml)
    let of_json j =
      list_of_json ~kind:"ContinuousHyperParameterRanges"
        ~of_json:ContinuousHyperParameterRange.of_json j
    let to_json v = composed_to_json to_value v
  end
module IntegerHyperParameterRanges =
  struct
    type nonrec t = IntegerHyperParameterRange.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:IntegerHyperParameterRange.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:IntegerHyperParameterRange.of_xml)
    let of_json j =
      list_of_json ~kind:"IntegerHyperParameterRanges"
        ~of_json:IntegerHyperParameterRange.of_json j
    let to_json v = composed_to_json to_value v
  end
module Tunable =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module HyperParameters =
  struct
    type nonrec t = (ParameterName.t * ParameterValue.t) list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ParameterName.of_string chopped),
                              (ParameterValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ParameterName.to_value x) |>
                    (fun x ->
                       (ParameterValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ParameterName.of_string
        ~of_json:ParameterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module TransactionsPerSecond =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for TransactionsPerSecond" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ArnList =
  struct
    type nonrec t = Arn.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:Arn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Arn.of_xml)
    let of_json j = list_of_json ~kind:"ArnList" ~of_json:Arn.of_json j
    let to_json v = composed_to_json to_value v
  end
module HPOObjective =
  struct
    type nonrec t =
      {
      type_: HPOObjectiveType.t option
        [@ocaml.doc
          "The type of the metric. Valid values are Maximize and Minimize."];
      metricName: MetricName.t option [@ocaml.doc "The name of the metric."];
      metricRegex: MetricRegex.t option
        [@ocaml.doc
          "A regular expression for finding the metric in the training job logs."]}
    let make ?type_ =
      fun ?metricName ->
        fun ?metricRegex -> fun () -> { type_; metricName; metricRegex }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:HPOObjectiveType.to_value));
        ("metricName", (Option.map x.metricName ~f:MetricName.to_value));
        ("metricRegex", (Option.map x.metricRegex ~f:MetricRegex.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metricRegex =
        (Option.map ~f:MetricRegex.of_xml) (Xml.child xml_arg0 "metricRegex") in
      let metricName =
        (Option.map ~f:MetricName.of_xml) (Xml.child xml_arg0 "metricName") in
      let type_ =
        (Option.map ~f:HPOObjectiveType.of_xml) (Xml.child xml_arg0 "type") in
      make ?metricRegex ?metricName ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let metricRegex = field_map json "metricRegex" MetricRegex.of_json in
      let metricName = field_map json "metricName" MetricName.of_json in
      let type_ = field_map json "type" HPOObjectiveType.of_json in
      make ?metricRegex ?metricName ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The metric to optimize during hyperparameter optimization (HPO). Amazon Personalize doesn't support configuring the hpoObjective at this time."]
module HPOResourceConfig =
  struct
    type nonrec t =
      {
      maxNumberOfTrainingJobs: HPOResource.t option
        [@ocaml.doc
          "The maximum number of training jobs when you create a solution version. The maximum value for maxNumberOfTrainingJobs is 40."];
      maxParallelTrainingJobs: HPOResource.t option
        [@ocaml.doc
          "The maximum number of parallel training jobs when you create a solution version. The maximum value for maxParallelTrainingJobs is 10."]}
    let make ?maxNumberOfTrainingJobs =
      fun ?maxParallelTrainingJobs ->
        fun () -> { maxNumberOfTrainingJobs; maxParallelTrainingJobs }
    let to_value x =
      structure_to_value
        [("maxNumberOfTrainingJobs",
           (Option.map x.maxNumberOfTrainingJobs ~f:HPOResource.to_value));
        ("maxParallelTrainingJobs",
          (Option.map x.maxParallelTrainingJobs ~f:HPOResource.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxParallelTrainingJobs =
        (Option.map ~f:HPOResource.of_xml)
          (Xml.child xml_arg0 "maxParallelTrainingJobs") in
      let maxNumberOfTrainingJobs =
        (Option.map ~f:HPOResource.of_xml)
          (Xml.child xml_arg0 "maxNumberOfTrainingJobs") in
      make ?maxParallelTrainingJobs ?maxNumberOfTrainingJobs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxParallelTrainingJobs =
        field_map json "maxParallelTrainingJobs" HPOResource.of_json in
      let maxNumberOfTrainingJobs =
        field_map json "maxNumberOfTrainingJobs" HPOResource.of_json in
      make ?maxParallelTrainingJobs ?maxNumberOfTrainingJobs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the resource configuration for hyperparameter optimization (HPO)."]
module HyperParameterRanges =
  struct
    type nonrec t =
      {
      integerHyperParameterRanges: IntegerHyperParameterRanges.t option
        [@ocaml.doc "The integer-valued hyperparameters and their ranges."];
      continuousHyperParameterRanges: ContinuousHyperParameterRanges.t option
        [@ocaml.doc "The continuous hyperparameters and their ranges."];
      categoricalHyperParameterRanges:
        CategoricalHyperParameterRanges.t option
        [@ocaml.doc "The categorical hyperparameters and their ranges."]}
    let make ?integerHyperParameterRanges =
      fun ?continuousHyperParameterRanges ->
        fun ?categoricalHyperParameterRanges ->
          fun () ->
            {
              integerHyperParameterRanges;
              continuousHyperParameterRanges;
              categoricalHyperParameterRanges
            }
    let to_value x =
      structure_to_value
        [("integerHyperParameterRanges",
           (Option.map x.integerHyperParameterRanges
              ~f:IntegerHyperParameterRanges.to_value));
        ("continuousHyperParameterRanges",
          (Option.map x.continuousHyperParameterRanges
             ~f:ContinuousHyperParameterRanges.to_value));
        ("categoricalHyperParameterRanges",
          (Option.map x.categoricalHyperParameterRanges
             ~f:CategoricalHyperParameterRanges.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let categoricalHyperParameterRanges =
        (Option.map ~f:CategoricalHyperParameterRanges.of_xml)
          (Xml.child xml_arg0 "categoricalHyperParameterRanges") in
      let continuousHyperParameterRanges =
        (Option.map ~f:ContinuousHyperParameterRanges.of_xml)
          (Xml.child xml_arg0 "continuousHyperParameterRanges") in
      let integerHyperParameterRanges =
        (Option.map ~f:IntegerHyperParameterRanges.of_xml)
          (Xml.child xml_arg0 "integerHyperParameterRanges") in
      make ?categoricalHyperParameterRanges ?continuousHyperParameterRanges
        ?integerHyperParameterRanges ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let categoricalHyperParameterRanges =
        field_map json "categoricalHyperParameterRanges"
          CategoricalHyperParameterRanges.of_json in
      let continuousHyperParameterRanges =
        field_map json "continuousHyperParameterRanges"
          ContinuousHyperParameterRanges.of_json in
      let integerHyperParameterRanges =
        field_map json "integerHyperParameterRanges"
          IntegerHyperParameterRanges.of_json in
      make ?categoricalHyperParameterRanges ?continuousHyperParameterRanges
        ?integerHyperParameterRanges ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the hyperparameters and their ranges. Hyperparameters can be categorical, continuous, or integer-valued."]
module ItemAttribute =
  struct
    type nonrec t = string
    let context_ = "ItemAttribute"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:150) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ItemAttribute" j
    let to_json = simple_to_json to_value
  end
module ObjectiveSensitivity =
  struct
    type nonrec t =
      | LOW 
      | MEDIUM 
      | HIGH 
      | OFF 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | LOW -> "LOW"
      | MEDIUM -> "MEDIUM"
      | HIGH -> "HIGH"
      | OFF -> "OFF"
      | Non_static_id s -> s
    let of_string =
      function
      | "LOW" -> LOW
      | "MEDIUM" -> MEDIUM
      | "HIGH" -> HIGH
      | "OFF" -> OFF
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ObjectiveSensitivity" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ObjectiveSensitivity" j)
    let to_json = simple_to_json to_value
  end
module KmsKeyArn =
  struct
    type nonrec t = string
    let context_ = "KmsKeyArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () ->
                check_pattern i ~pattern:"arn:aws.*:kms:.*:[0-9]{12}:key/.*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KmsKeyArn" j
    let to_json = simple_to_json to_value
  end
module S3Location =
  struct
    type nonrec t = string
    let context_ = "S3Location"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_pattern i ~pattern:"(s3|http|https)://.+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3Location" j
    let to_json = simple_to_json to_value
  end
module DefaultCategoricalHyperParameterRange =
  struct
    type nonrec t =
      {
      name: ParameterName.t option
        [@ocaml.doc "The name of the hyperparameter."];
      values: CategoricalValues.t option
        [@ocaml.doc "A list of the categories for the hyperparameter."];
      isTunable: Tunable.t option
        [@ocaml.doc "Whether the hyperparameter is tunable."]}
    let make ?name =
      fun ?values -> fun ?isTunable -> fun () -> { name; values; isTunable }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:ParameterName.to_value));
        ("values", (Option.map x.values ~f:CategoricalValues.to_value));
        ("isTunable", (Option.map x.isTunable ~f:Tunable.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let isTunable =
        (Option.map ~f:Tunable.of_xml) (Xml.child xml_arg0 "isTunable") in
      let values =
        (Option.map ~f:CategoricalValues.of_xml)
          (Xml.child xml_arg0 "values") in
      let name =
        (Option.map ~f:ParameterName.of_xml) (Xml.child xml_arg0 "name") in
      make ?isTunable ?values ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let isTunable = field_map json "isTunable" Tunable.of_json in
      let values = field_map json "values" CategoricalValues.of_json in
      let name = field_map json "name" ParameterName.of_json in
      make ?isTunable ?values ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides the name and default range of a categorical hyperparameter and whether the hyperparameter is tunable. A tunable hyperparameter can have its value determined during hyperparameter optimization (HPO)."]
module DefaultContinuousHyperParameterRange =
  struct
    type nonrec t =
      {
      name: ParameterName.t option
        [@ocaml.doc "The name of the hyperparameter."];
      minValue: ContinuousMinValue.t option
        [@ocaml.doc "The minimum allowable value for the hyperparameter."];
      maxValue: ContinuousMaxValue.t option
        [@ocaml.doc "The maximum allowable value for the hyperparameter."];
      isTunable: Tunable.t option
        [@ocaml.doc "Whether the hyperparameter is tunable."]}
    let make ?name =
      fun ?minValue ->
        fun ?maxValue ->
          fun ?isTunable -> fun () -> { name; minValue; maxValue; isTunable }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:ParameterName.to_value));
        ("minValue", (Option.map x.minValue ~f:ContinuousMinValue.to_value));
        ("maxValue", (Option.map x.maxValue ~f:ContinuousMaxValue.to_value));
        ("isTunable", (Option.map x.isTunable ~f:Tunable.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let isTunable =
        (Option.map ~f:Tunable.of_xml) (Xml.child xml_arg0 "isTunable") in
      let maxValue =
        (Option.map ~f:ContinuousMaxValue.of_xml)
          (Xml.child xml_arg0 "maxValue") in
      let minValue =
        (Option.map ~f:ContinuousMinValue.of_xml)
          (Xml.child xml_arg0 "minValue") in
      let name =
        (Option.map ~f:ParameterName.of_xml) (Xml.child xml_arg0 "name") in
      make ?isTunable ?maxValue ?minValue ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let isTunable = field_map json "isTunable" Tunable.of_json in
      let maxValue = field_map json "maxValue" ContinuousMaxValue.of_json in
      let minValue = field_map json "minValue" ContinuousMinValue.of_json in
      let name = field_map json "name" ParameterName.of_json in
      make ?isTunable ?maxValue ?minValue ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides the name and default range of a continuous hyperparameter and whether the hyperparameter is tunable. A tunable hyperparameter can have its value determined during hyperparameter optimization (HPO)."]
module DefaultIntegerHyperParameterRange =
  struct
    type nonrec t =
      {
      name: ParameterName.t option
        [@ocaml.doc "The name of the hyperparameter."];
      minValue: IntegerMinValue.t option
        [@ocaml.doc "The minimum allowable value for the hyperparameter."];
      maxValue: IntegerMaxValue.t option
        [@ocaml.doc "The maximum allowable value for the hyperparameter."];
      isTunable: Tunable.t option
        [@ocaml.doc "Indicates whether the hyperparameter is tunable."]}
    let make ?name =
      fun ?minValue ->
        fun ?maxValue ->
          fun ?isTunable -> fun () -> { name; minValue; maxValue; isTunable }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:ParameterName.to_value));
        ("minValue", (Option.map x.minValue ~f:IntegerMinValue.to_value));
        ("maxValue", (Option.map x.maxValue ~f:IntegerMaxValue.to_value));
        ("isTunable", (Option.map x.isTunable ~f:Tunable.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let isTunable =
        (Option.map ~f:Tunable.of_xml) (Xml.child xml_arg0 "isTunable") in
      let maxValue =
        (Option.map ~f:IntegerMaxValue.of_xml)
          (Xml.child xml_arg0 "maxValue") in
      let minValue =
        (Option.map ~f:IntegerMinValue.of_xml)
          (Xml.child xml_arg0 "minValue") in
      let name =
        (Option.map ~f:ParameterName.of_xml) (Xml.child xml_arg0 "name") in
      make ?isTunable ?maxValue ?minValue ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let isTunable = field_map json "isTunable" Tunable.of_json in
      let maxValue = field_map json "maxValue" IntegerMaxValue.of_json in
      let minValue = field_map json "minValue" IntegerMinValue.of_json in
      let name = field_map json "name" ParameterName.of_json in
      make ?isTunable ?maxValue ?minValue ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides the name and default range of a integer-valued hyperparameter and whether the hyperparameter is tunable. A tunable hyperparameter can have its value determined during hyperparameter optimization (HPO)."]
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module Date =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module Name =
  struct
    type nonrec t = string
    let context_ = "Name"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:63) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9][a-zA-Z0-9\\-_]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Name" j
    let to_json = simple_to_json to_value
  end
module Status =
  struct
    type nonrec t = string
    let context_ = "Status"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Status" j
    let to_json = simple_to_json to_value
  end
module FailureReason =
  struct
    type nonrec t = string
    let context_ = "FailureReason"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FailureReason" j
    let to_json = simple_to_json to_value
  end
module Domain =
  struct
    type nonrec t =
      | ECOMMERCE 
      | VIDEO_ON_DEMAND 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ECOMMERCE -> "ECOMMERCE"
      | VIDEO_ON_DEMAND -> "VIDEO_ON_DEMAND"
      | Non_static_id s -> s
    let of_string =
      function
      | "ECOMMERCE" -> ECOMMERCE
      | "VIDEO_ON_DEMAND" -> VIDEO_ON_DEMAND
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Domain" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Domain" j)
    let to_json = simple_to_json to_value
  end
module RecommenderConfig =
  struct
    type nonrec t =
      {
      itemExplorationConfig: HyperParameters.t option
        [@ocaml.doc
          "Specifies the exploration configuration hyperparameters, including explorationWeight and explorationItemAgeCutOff, you want to use to configure the amount of item exploration Amazon Personalize uses when recommending items. Provide itemExplorationConfig data only if your recommenders generate personalized recommendations for a user (not popular items or similar items)."];
      minRecommendationRequestsPerSecond: TransactionsPerSecond.t option
        [@ocaml.doc
          "Specifies the requested minimum provisioned recommendation requests per second that Amazon Personalize will support."]}
    let make ?itemExplorationConfig =
      fun ?minRecommendationRequestsPerSecond ->
        fun () ->
          { itemExplorationConfig; minRecommendationRequestsPerSecond }
    let to_value x =
      structure_to_value
        [("itemExplorationConfig",
           (Option.map x.itemExplorationConfig ~f:HyperParameters.to_value));
        ("minRecommendationRequestsPerSecond",
          (Option.map x.minRecommendationRequestsPerSecond
             ~f:TransactionsPerSecond.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let minRecommendationRequestsPerSecond =
        (Option.map ~f:TransactionsPerSecond.of_xml)
          (Xml.child xml_arg0 "minRecommendationRequestsPerSecond") in
      let itemExplorationConfig =
        (Option.map ~f:HyperParameters.of_xml)
          (Xml.child xml_arg0 "itemExplorationConfig") in
      make ?minRecommendationRequestsPerSecond ?itemExplorationConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let minRecommendationRequestsPerSecond =
        field_map json "minRecommendationRequestsPerSecond"
          TransactionsPerSecond.of_json in
      let itemExplorationConfig =
        field_map json "itemExplorationConfig" HyperParameters.of_json in
      make ?minRecommendationRequestsPerSecond ?itemExplorationConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The configuration details of the recommender."]
module DatasetType =
  struct
    type nonrec t = string
    let context_ = "DatasetType"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DatasetType" j
    let to_json = simple_to_json to_value
  end
module AutoMLConfig =
  struct
    type nonrec t =
      {
      metricName: MetricName.t option [@ocaml.doc "The metric to optimize."];
      recipeList: ArnList.t option
        [@ocaml.doc "The list of candidate recipes."]}
    let make ?metricName =
      fun ?recipeList -> fun () -> { metricName; recipeList }
    let to_value x =
      structure_to_value
        [("metricName", (Option.map x.metricName ~f:MetricName.to_value));
        ("recipeList", (Option.map x.recipeList ~f:ArnList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recipeList =
        (Option.map ~f:ArnList.of_xml) (Xml.child xml_arg0 "recipeList") in
      let metricName =
        (Option.map ~f:MetricName.of_xml) (Xml.child xml_arg0 "metricName") in
      make ?recipeList ?metricName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recipeList = field_map json "recipeList" ArnList.of_json in
      let metricName = field_map json "metricName" MetricName.of_json in
      make ?recipeList ?metricName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "When the solution performs AutoML (performAutoML is true in CreateSolution), Amazon Personalize determines which recipe, from the specified list, optimizes the given metric. Amazon Personalize then uses that recipe for the solution."]
module EventValueThreshold =
  struct
    type nonrec t = string
    let context_ = "EventValueThreshold"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EventValueThreshold" j
    let to_json = simple_to_json to_value
  end
module FeatureTransformationParameters =
  struct
    type nonrec t = (ParameterName.t * ParameterValue.t) list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ParameterName.of_string chopped),
                              (ParameterValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ParameterName.to_value x) |>
                    (fun x ->
                       (ParameterValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ParameterName.of_string
        ~of_json:ParameterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module HPOConfig =
  struct
    type nonrec t =
      {
      hpoObjective: HPOObjective.t option
        [@ocaml.doc
          "The metric to optimize during HPO. Amazon Personalize doesn't support configuring the hpoObjective at this time."];
      hpoResourceConfig: HPOResourceConfig.t option
        [@ocaml.doc "Describes the resource configuration for HPO."];
      algorithmHyperParameterRanges: HyperParameterRanges.t option
        [@ocaml.doc "The hyperparameters and their allowable ranges."]}
    let make ?hpoObjective =
      fun ?hpoResourceConfig ->
        fun ?algorithmHyperParameterRanges ->
          fun () ->
            { hpoObjective; hpoResourceConfig; algorithmHyperParameterRanges
            }
    let to_value x =
      structure_to_value
        [("hpoObjective",
           (Option.map x.hpoObjective ~f:HPOObjective.to_value));
        ("hpoResourceConfig",
          (Option.map x.hpoResourceConfig ~f:HPOResourceConfig.to_value));
        ("algorithmHyperParameterRanges",
          (Option.map x.algorithmHyperParameterRanges
             ~f:HyperParameterRanges.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let algorithmHyperParameterRanges =
        (Option.map ~f:HyperParameterRanges.of_xml)
          (Xml.child xml_arg0 "algorithmHyperParameterRanges") in
      let hpoResourceConfig =
        (Option.map ~f:HPOResourceConfig.of_xml)
          (Xml.child xml_arg0 "hpoResourceConfig") in
      let hpoObjective =
        (Option.map ~f:HPOObjective.of_xml)
          (Xml.child xml_arg0 "hpoObjective") in
      make ?algorithmHyperParameterRanges ?hpoResourceConfig ?hpoObjective ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let algorithmHyperParameterRanges =
        field_map json "algorithmHyperParameterRanges"
          HyperParameterRanges.of_json in
      let hpoResourceConfig =
        field_map json "hpoResourceConfig" HPOResourceConfig.of_json in
      let hpoObjective = field_map json "hpoObjective" HPOObjective.of_json in
      make ?algorithmHyperParameterRanges ?hpoResourceConfig ?hpoObjective ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the properties for hyperparameter optimization (HPO)."]
module OptimizationObjective =
  struct
    type nonrec t =
      {
      itemAttribute: ItemAttribute.t option
        [@ocaml.doc
          "The numerical metadata column in an Items dataset related to the optimization objective. For example, VIDEO_LENGTH (to maximize streaming minutes), or PRICE (to maximize revenue)."];
      objectiveSensitivity: ObjectiveSensitivity.t option
        [@ocaml.doc
          "Specifies how Amazon Personalize balances the importance of your optimization objective versus relevance."]}
    let make ?itemAttribute =
      fun ?objectiveSensitivity ->
        fun () -> { itemAttribute; objectiveSensitivity }
    let to_value x =
      structure_to_value
        [("itemAttribute",
           (Option.map x.itemAttribute ~f:ItemAttribute.to_value));
        ("objectiveSensitivity",
          (Option.map x.objectiveSensitivity ~f:ObjectiveSensitivity.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let objectiveSensitivity =
        (Option.map ~f:ObjectiveSensitivity.of_xml)
          (Xml.child xml_arg0 "objectiveSensitivity") in
      let itemAttribute =
        (Option.map ~f:ItemAttribute.of_xml)
          (Xml.child xml_arg0 "itemAttribute") in
      make ?objectiveSensitivity ?itemAttribute ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let objectiveSensitivity =
        field_map json "objectiveSensitivity" ObjectiveSensitivity.of_json in
      let itemAttribute =
        field_map json "itemAttribute" ItemAttribute.of_json in
      make ?objectiveSensitivity ?itemAttribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the additional objective for the solution, such as maximizing streaming minutes or increasing revenue. For more information see Optimizing a solution."]
module S3DataConfig =
  struct
    type nonrec t =
      {
      path: S3Location.t
        [@ocaml.doc "The file path of the Amazon S3 bucket."];
      kmsKeyArn: KmsKeyArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Key Management Service (KMS) key that Amazon Personalize uses to encrypt or decrypt the input and output files."]}
    let context_ = "S3DataConfig"
    let make ?kmsKeyArn = fun ~path -> fun () -> { kmsKeyArn; path }
    let to_value x =
      structure_to_value
        [("path", (Some (S3Location.to_value x.path)));
        ("kmsKeyArn", (Option.map x.kmsKeyArn ~f:KmsKeyArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKeyArn =
        (Option.map ~f:KmsKeyArn.of_xml) (Xml.child xml_arg0 "kmsKeyArn") in
      let path =
        S3Location.of_xml (Xml.child_exn ~context:context_ xml_arg0 "path") in
      make ?kmsKeyArn ~path ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let kmsKeyArn = field_map json "kmsKeyArn" KmsKeyArn.of_json in
      let path = field_map_exn json "path" S3Location.of_json in
      make ?kmsKeyArn ~path ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration details of an Amazon S3 input or output bucket."]
module CampaignConfig =
  struct
    type nonrec t =
      {
      itemExplorationConfig: HyperParameters.t option
        [@ocaml.doc
          "Specifies the exploration configuration hyperparameters, including explorationWeight and explorationItemAgeCutOff, you want to use to configure the amount of item exploration Amazon Personalize uses when recommending items. Provide itemExplorationConfig data only if your solution uses the User-Personalization recipe."]}
    let make ?itemExplorationConfig = fun () -> { itemExplorationConfig }
    let to_value x =
      structure_to_value
        [("itemExplorationConfig",
           (Option.map x.itemExplorationConfig ~f:HyperParameters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let itemExplorationConfig =
        (Option.map ~f:HyperParameters.of_xml)
          (Xml.child xml_arg0 "itemExplorationConfig") in
      make ?itemExplorationConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let itemExplorationConfig =
        field_map json "itemExplorationConfig" HyperParameters.of_json in
      make ?itemExplorationConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The configuration details of a campaign."]
module DockerURI =
  struct
    type nonrec t = string
    let context_ = "DockerURI"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DockerURI" j
    let to_json = simple_to_json to_value
  end
module DefaultCategoricalHyperParameterRanges =
  struct
    type nonrec t = DefaultCategoricalHyperParameterRange.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:DefaultCategoricalHyperParameterRange.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:DefaultCategoricalHyperParameterRange.of_xml)
    let of_json j =
      list_of_json ~kind:"DefaultCategoricalHyperParameterRanges"
        ~of_json:DefaultCategoricalHyperParameterRange.of_json j
    let to_json v = composed_to_json to_value v
  end
module DefaultContinuousHyperParameterRanges =
  struct
    type nonrec t = DefaultContinuousHyperParameterRange.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:DefaultContinuousHyperParameterRange.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:DefaultContinuousHyperParameterRange.of_xml)
    let of_json j =
      list_of_json ~kind:"DefaultContinuousHyperParameterRanges"
        ~of_json:DefaultContinuousHyperParameterRange.of_json j
    let to_json v = composed_to_json to_value v
  end
module DefaultIntegerHyperParameterRanges =
  struct
    type nonrec t = DefaultIntegerHyperParameterRange.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:DefaultIntegerHyperParameterRange.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:DefaultIntegerHyperParameterRange.of_xml)
    let of_json j =
      list_of_json ~kind:"DefaultIntegerHyperParameterRanges"
        ~of_json:DefaultIntegerHyperParameterRange.of_json j
    let to_json v = composed_to_json to_value v
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t =
      {
      tagKey: TagKey.t
        [@ocaml.doc
          "One part of a key-value pair that makes up a tag. A key is a general label that acts like a category for more specific tag values."];
      tagValue: TagValue.t
        [@ocaml.doc
          "The optional part of a key-value pair that makes up a tag. A value acts as a descriptor within a tag category (key)."]}
    let context_ = "Tag"
    let make ~tagKey = fun ~tagValue -> fun () -> { tagKey; tagValue }
    let to_value x =
      structure_to_value
        [("tagKey", (Some (TagKey.to_value x.tagKey)));
        ("tagValue", (Some (TagValue.to_value x.tagValue)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagValue =
        TagValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tagValue") in
      let tagKey =
        TagKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tagKey") in
      make ~tagValue ~tagKey ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tagValue = field_map_exn json "tagValue" TagValue.of_json in
      let tagKey = field_map_exn json "tagKey" TagKey.of_json in
      make ~tagValue ~tagKey ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The optional metadata that you apply to resources to help you categorize and organize them. Each tag consists of a key and an optional value, both of which you define. For more information see Tagging Personalize resources."]
module SolutionSummary =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the solution."];
      solutionArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the solution."];
      status: Status.t option
        [@ocaml.doc
          "The status of the solution. A solution can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the solution was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the solution was last updated."]}
    let make ?name =
      fun ?solutionArn ->
        fun ?status ->
          fun ?creationDateTime ->
            fun ?lastUpdatedDateTime ->
              fun () ->
                {
                  name;
                  solutionArn;
                  status;
                  creationDateTime;
                  lastUpdatedDateTime
                }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("solutionArn", (Option.map x.solutionArn ~f:Arn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let solutionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?solutionArn ?name
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let solutionArn = field_map json "solutionArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?solutionArn ?name
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a summary of the properties of a solution. For a complete listing, call the DescribeSolution API."]
module SolutionVersionSummary =
  struct
    type nonrec t =
      {
      solutionVersionArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution version."];
      status: Status.t option
        [@ocaml.doc
          "The status of the solution version. A solution version can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that this version of a solution was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the solution version was last updated."];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If a solution version fails, the reason behind the failure."]}
    let make ?solutionVersionArn =
      fun ?status ->
        fun ?creationDateTime ->
          fun ?lastUpdatedDateTime ->
            fun ?failureReason ->
              fun () ->
                {
                  solutionVersionArn;
                  status;
                  creationDateTime;
                  lastUpdatedDateTime;
                  failureReason
                }
    let to_value x =
      structure_to_value
        [("solutionVersionArn",
           (Option.map x.solutionVersionArn ~f:Arn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let solutionVersionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionVersionArn") in
      make ?failureReason ?lastUpdatedDateTime ?creationDateTime ?status
        ?solutionVersionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let solutionVersionArn =
        field_map json "solutionVersionArn" Arn.of_json in
      make ?failureReason ?lastUpdatedDateTime ?creationDateTime ?status
        ?solutionVersionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a summary of the properties of a solution version. For a complete listing, call the DescribeSolutionVersion API."]
module DatasetSchemaSummary =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the schema."];
      schemaArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the schema."];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the schema was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the schema was last updated."];
      domain: Domain.t option
        [@ocaml.doc
          "The domain of a schema that you created for a dataset in a Domain dataset group."]}
    let make ?name =
      fun ?schemaArn ->
        fun ?creationDateTime ->
          fun ?lastUpdatedDateTime ->
            fun ?domain ->
              fun () ->
                {
                  name;
                  schemaArn;
                  creationDateTime;
                  lastUpdatedDateTime;
                  domain
                }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("schemaArn", (Option.map x.schemaArn ~f:Arn.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("domain", (Option.map x.domain ~f:Domain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        (Option.map ~f:Domain.of_xml) (Xml.child xml_arg0 "domain") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let schemaArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "schemaArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?domain ?lastUpdatedDateTime ?creationDateTime ?schemaArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let domain = field_map json "domain" Domain.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let schemaArn = field_map json "schemaArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?domain ?lastUpdatedDateTime ?creationDateTime ?schemaArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a summary of the properties of a dataset schema. For a complete listing, call the DescribeSchema API."]
module RecommenderSummary =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the recommender."];
      recommenderArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the recommender."];
      datasetGroupArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Domain dataset group that contains the recommender."];
      recipeArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the recipe (Domain dataset group use case) that the recommender was created for."];
      recommenderConfig: RecommenderConfig.t option
        [@ocaml.doc "The configuration details of the recommender."];
      status: Status.t option
        [@ocaml.doc
          "The status of the recommender. A recommender can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix format) that the recommender was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix format) that the recommender was last updated."]}
    let make ?name =
      fun ?recommenderArn ->
        fun ?datasetGroupArn ->
          fun ?recipeArn ->
            fun ?recommenderConfig ->
              fun ?status ->
                fun ?creationDateTime ->
                  fun ?lastUpdatedDateTime ->
                    fun () ->
                      {
                        name;
                        recommenderArn;
                        datasetGroupArn;
                        recipeArn;
                        recommenderConfig;
                        status;
                        creationDateTime;
                        lastUpdatedDateTime
                      }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("recommenderArn", (Option.map x.recommenderArn ~f:Arn.to_value));
        ("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("recipeArn", (Option.map x.recipeArn ~f:Arn.to_value));
        ("recommenderConfig",
          (Option.map x.recommenderConfig ~f:RecommenderConfig.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let recommenderConfig =
        (Option.map ~f:RecommenderConfig.of_xml)
          (Xml.child xml_arg0 "recommenderConfig") in
      let recipeArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "recipeArn") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      let recommenderArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "recommenderArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?recommenderConfig
        ?recipeArn ?datasetGroupArn ?recommenderArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let recommenderConfig =
        field_map json "recommenderConfig" RecommenderConfig.of_json in
      let recipeArn = field_map json "recipeArn" Arn.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      let recommenderArn = field_map json "recommenderArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?recommenderConfig
        ?recipeArn ?datasetGroupArn ?recommenderArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Provides a summary of the properties of the recommender."]
module RecipeSummary =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the recipe."];
      recipeArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the recipe."];
      status: Status.t option [@ocaml.doc "The status of the recipe."];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the recipe was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the recipe was last updated."];
      domain: Domain.t option
        [@ocaml.doc
          "The domain of the recipe (if the recipe is a Domain dataset group use case)."]}
    let make ?name =
      fun ?recipeArn ->
        fun ?status ->
          fun ?creationDateTime ->
            fun ?lastUpdatedDateTime ->
              fun ?domain ->
                fun () ->
                  {
                    name;
                    recipeArn;
                    status;
                    creationDateTime;
                    lastUpdatedDateTime;
                    domain
                  }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("recipeArn", (Option.map x.recipeArn ~f:Arn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("domain", (Option.map x.domain ~f:Domain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        (Option.map ~f:Domain.of_xml) (Xml.child xml_arg0 "domain") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let recipeArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "recipeArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?domain ?lastUpdatedDateTime ?creationDateTime ?status ?recipeArn
        ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let domain = field_map json "domain" Domain.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let recipeArn = field_map json "recipeArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?domain ?lastUpdatedDateTime ?creationDateTime ?status ?recipeArn
        ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a summary of the properties of a recipe. For a complete listing, call the DescribeRecipe API."]
module FilterSummary =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the filter."];
      filterArn: Arn.t option [@ocaml.doc "The ARN of the filter."];
      creationDateTime: Date.t option
        [@ocaml.doc "The time at which the filter was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc "The time at which the filter was last updated."];
      datasetGroupArn: Arn.t option
        [@ocaml.doc
          "The ARN of the dataset group to which the filter belongs."];
      failureReason: FailureReason.t option
        [@ocaml.doc "If the filter failed, the reason for the failure."];
      status: Status.t option [@ocaml.doc "The status of the filter."]}
    let make ?name =
      fun ?filterArn ->
        fun ?creationDateTime ->
          fun ?lastUpdatedDateTime ->
            fun ?datasetGroupArn ->
              fun ?failureReason ->
                fun ?status ->
                  fun () ->
                    {
                      name;
                      filterArn;
                      creationDateTime;
                      lastUpdatedDateTime;
                      datasetGroupArn;
                      failureReason;
                      status
                    }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("filterArn", (Option.map x.filterArn ~f:Arn.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let filterArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "filterArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?status ?failureReason ?datasetGroupArn ?lastUpdatedDateTime
        ?creationDateTime ?filterArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let status = field_map json "status" Status.of_json in
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let filterArn = field_map json "filterArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?status ?failureReason ?datasetGroupArn ?lastUpdatedDateTime
        ?creationDateTime ?filterArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A short summary of a filter's attributes."]
module EventTrackerSummary =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the event tracker."];
      eventTrackerArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the event tracker."];
      status: Status.t option
        [@ocaml.doc
          "The status of the event tracker. An event tracker can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the event tracker was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the event tracker was last updated."]}
    let make ?name =
      fun ?eventTrackerArn ->
        fun ?status ->
          fun ?creationDateTime ->
            fun ?lastUpdatedDateTime ->
              fun () ->
                {
                  name;
                  eventTrackerArn;
                  status;
                  creationDateTime;
                  lastUpdatedDateTime
                }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("eventTrackerArn", (Option.map x.eventTrackerArn ~f:Arn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let eventTrackerArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "eventTrackerArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?eventTrackerArn
        ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let eventTrackerArn = field_map json "eventTrackerArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?eventTrackerArn
        ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a summary of the properties of an event tracker. For a complete listing, call the DescribeEventTracker API."]
module DatasetSummary =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the dataset."];
      datasetArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the dataset."];
      datasetType: DatasetType.t option
        [@ocaml.doc
          "The dataset type. One of the following values: Interactions Items Users Event-Interactions"];
      status: Status.t option
        [@ocaml.doc
          "The status of the dataset. A dataset can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the dataset was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the dataset was last updated."]}
    let make ?name =
      fun ?datasetArn ->
        fun ?datasetType ->
          fun ?status ->
            fun ?creationDateTime ->
              fun ?lastUpdatedDateTime ->
                fun () ->
                  {
                    name;
                    datasetArn;
                    datasetType;
                    status;
                    creationDateTime;
                    lastUpdatedDateTime
                  }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("datasetArn", (Option.map x.datasetArn ~f:Arn.to_value));
        ("datasetType", (Option.map x.datasetType ~f:DatasetType.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let datasetType =
        (Option.map ~f:DatasetType.of_xml) (Xml.child xml_arg0 "datasetType") in
      let datasetArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?datasetType
        ?datasetArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let datasetType = field_map json "datasetType" DatasetType.of_json in
      let datasetArn = field_map json "datasetArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?datasetType
        ?datasetArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a summary of the properties of a dataset. For a complete listing, call the DescribeDataset API."]
module DatasetImportJobSummary =
  struct
    type nonrec t =
      {
      datasetImportJobArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset import job."];
      jobName: Name.t option
        [@ocaml.doc "The name of the dataset import job."];
      status: Status.t option
        [@ocaml.doc
          "The status of the dataset import job. A dataset import job can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the dataset import job was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the dataset import job status was last updated."];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If a dataset import job fails, the reason behind the failure."]}
    let make ?datasetImportJobArn =
      fun ?jobName ->
        fun ?status ->
          fun ?creationDateTime ->
            fun ?lastUpdatedDateTime ->
              fun ?failureReason ->
                fun () ->
                  {
                    datasetImportJobArn;
                    jobName;
                    status;
                    creationDateTime;
                    lastUpdatedDateTime;
                    failureReason
                  }
    let to_value x =
      structure_to_value
        [("datasetImportJobArn",
           (Option.map x.datasetImportJobArn ~f:Arn.to_value));
        ("jobName", (Option.map x.jobName ~f:Name.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let jobName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "jobName") in
      let datasetImportJobArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetImportJobArn") in
      make ?failureReason ?lastUpdatedDateTime ?creationDateTime ?status
        ?jobName ?datasetImportJobArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let jobName = field_map json "jobName" Name.of_json in
      let datasetImportJobArn =
        field_map json "datasetImportJobArn" Arn.of_json in
      make ?failureReason ?lastUpdatedDateTime ?creationDateTime ?status
        ?jobName ?datasetImportJobArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a summary of the properties of a dataset import job. For a complete listing, call the DescribeDatasetImportJob API."]
module DatasetGroupSummary =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the dataset group."];
      datasetGroupArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the dataset group."];
      status: Status.t option
        [@ocaml.doc
          "The status of the dataset group. A dataset group can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the dataset group was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the dataset group was last updated."];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If creating a dataset group fails, the reason behind the failure."];
      domain: Domain.t option
        [@ocaml.doc "The domain of a Domain dataset group."]}
    let make ?name =
      fun ?datasetGroupArn ->
        fun ?status ->
          fun ?creationDateTime ->
            fun ?lastUpdatedDateTime ->
              fun ?failureReason ->
                fun ?domain ->
                  fun () ->
                    {
                      name;
                      datasetGroupArn;
                      status;
                      creationDateTime;
                      lastUpdatedDateTime;
                      failureReason;
                      domain
                    }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("domain", (Option.map x.domain ~f:Domain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        (Option.map ~f:Domain.of_xml) (Xml.child xml_arg0 "domain") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?domain ?failureReason ?lastUpdatedDateTime ?creationDateTime
        ?status ?datasetGroupArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let domain = field_map json "domain" Domain.of_json in
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?domain ?failureReason ?lastUpdatedDateTime ?creationDateTime
        ?status ?datasetGroupArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a summary of the properties of a dataset group. For a complete listing, call the DescribeDatasetGroup API."]
module DatasetExportJobSummary =
  struct
    type nonrec t =
      {
      datasetExportJobArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset export job."];
      jobName: Name.t option
        [@ocaml.doc "The name of the dataset export job."];
      status: Status.t option
        [@ocaml.doc
          "The status of the dataset export job. A dataset export job can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the dataset export job was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the dataset export job status was last updated."];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If a dataset export job fails, the reason behind the failure."]}
    let make ?datasetExportJobArn =
      fun ?jobName ->
        fun ?status ->
          fun ?creationDateTime ->
            fun ?lastUpdatedDateTime ->
              fun ?failureReason ->
                fun () ->
                  {
                    datasetExportJobArn;
                    jobName;
                    status;
                    creationDateTime;
                    lastUpdatedDateTime;
                    failureReason
                  }
    let to_value x =
      structure_to_value
        [("datasetExportJobArn",
           (Option.map x.datasetExportJobArn ~f:Arn.to_value));
        ("jobName", (Option.map x.jobName ~f:Name.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let jobName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "jobName") in
      let datasetExportJobArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetExportJobArn") in
      make ?failureReason ?lastUpdatedDateTime ?creationDateTime ?status
        ?jobName ?datasetExportJobArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let jobName = field_map json "jobName" Name.of_json in
      let datasetExportJobArn =
        field_map json "datasetExportJobArn" Arn.of_json in
      make ?failureReason ?lastUpdatedDateTime ?creationDateTime ?status
        ?jobName ?datasetExportJobArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a summary of the properties of a dataset export job. For a complete listing, call the DescribeDatasetExportJob API."]
module CampaignSummary =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the campaign."];
      campaignArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the campaign."];
      status: Status.t option
        [@ocaml.doc
          "The status of the campaign. A campaign can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the campaign was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the campaign was last updated."];
      failureReason: FailureReason.t option
        [@ocaml.doc "If a campaign fails, the reason behind the failure."]}
    let make ?name =
      fun ?campaignArn ->
        fun ?status ->
          fun ?creationDateTime ->
            fun ?lastUpdatedDateTime ->
              fun ?failureReason ->
                fun () ->
                  {
                    name;
                    campaignArn;
                    status;
                    creationDateTime;
                    lastUpdatedDateTime;
                    failureReason
                  }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("campaignArn", (Option.map x.campaignArn ~f:Arn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let campaignArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "campaignArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?failureReason ?lastUpdatedDateTime ?creationDateTime ?status
        ?campaignArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let campaignArn = field_map json "campaignArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?failureReason ?lastUpdatedDateTime ?creationDateTime ?status
        ?campaignArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a summary of the properties of a campaign. For a complete listing, call the DescribeCampaign API."]
module BatchSegmentJobSummary =
  struct
    type nonrec t =
      {
      batchSegmentJobArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the batch segment job."];
      jobName: Name.t option
        [@ocaml.doc "The name of the batch segment job."];
      status: Status.t option
        [@ocaml.doc
          "The status of the batch segment job. The status is one of the following values: PENDING IN PROGRESS ACTIVE CREATE FAILED"];
      creationDateTime: Date.t option
        [@ocaml.doc "The time at which the batch segment job was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The time at which the batch segment job was last updated."];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If the batch segment job failed, the reason for the failure."];
      solutionVersionArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution version used by the batch segment job to generate batch segments."]}
    let make ?batchSegmentJobArn =
      fun ?jobName ->
        fun ?status ->
          fun ?creationDateTime ->
            fun ?lastUpdatedDateTime ->
              fun ?failureReason ->
                fun ?solutionVersionArn ->
                  fun () ->
                    {
                      batchSegmentJobArn;
                      jobName;
                      status;
                      creationDateTime;
                      lastUpdatedDateTime;
                      failureReason;
                      solutionVersionArn
                    }
    let to_value x =
      structure_to_value
        [("batchSegmentJobArn",
           (Option.map x.batchSegmentJobArn ~f:Arn.to_value));
        ("jobName", (Option.map x.jobName ~f:Name.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("solutionVersionArn",
          (Option.map x.solutionVersionArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let solutionVersionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionVersionArn") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let jobName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "jobName") in
      let batchSegmentJobArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "batchSegmentJobArn") in
      make ?solutionVersionArn ?failureReason ?lastUpdatedDateTime
        ?creationDateTime ?status ?jobName ?batchSegmentJobArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let solutionVersionArn =
        field_map json "solutionVersionArn" Arn.of_json in
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let jobName = field_map json "jobName" Name.of_json in
      let batchSegmentJobArn =
        field_map json "batchSegmentJobArn" Arn.of_json in
      make ?solutionVersionArn ?failureReason ?lastUpdatedDateTime
        ?creationDateTime ?status ?jobName ?batchSegmentJobArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A truncated version of the BatchSegmentJob datatype. ListBatchSegmentJobs operation returns a list of batch segment job summaries."]
module BatchInferenceJobSummary =
  struct
    type nonrec t =
      {
      batchInferenceJobArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the batch inference job."];
      jobName: Name.t option
        [@ocaml.doc "The name of the batch inference job."];
      status: Status.t option
        [@ocaml.doc
          "The status of the batch inference job. The status is one of the following values: PENDING IN PROGRESS ACTIVE CREATE FAILED"];
      creationDateTime: Date.t option
        [@ocaml.doc "The time at which the batch inference job was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The time at which the batch inference job was last updated."];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If the batch inference job failed, the reason for the failure."];
      solutionVersionArn: Arn.t option
        [@ocaml.doc
          "The ARN of the solution version used by the batch inference job."]}
    let make ?batchInferenceJobArn =
      fun ?jobName ->
        fun ?status ->
          fun ?creationDateTime ->
            fun ?lastUpdatedDateTime ->
              fun ?failureReason ->
                fun ?solutionVersionArn ->
                  fun () ->
                    {
                      batchInferenceJobArn;
                      jobName;
                      status;
                      creationDateTime;
                      lastUpdatedDateTime;
                      failureReason;
                      solutionVersionArn
                    }
    let to_value x =
      structure_to_value
        [("batchInferenceJobArn",
           (Option.map x.batchInferenceJobArn ~f:Arn.to_value));
        ("jobName", (Option.map x.jobName ~f:Name.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("solutionVersionArn",
          (Option.map x.solutionVersionArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let solutionVersionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionVersionArn") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let jobName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "jobName") in
      let batchInferenceJobArn =
        (Option.map ~f:Arn.of_xml)
          (Xml.child xml_arg0 "batchInferenceJobArn") in
      make ?solutionVersionArn ?failureReason ?lastUpdatedDateTime
        ?creationDateTime ?status ?jobName ?batchInferenceJobArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let solutionVersionArn =
        field_map json "solutionVersionArn" Arn.of_json in
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let jobName = field_map json "jobName" Name.of_json in
      let batchInferenceJobArn =
        field_map json "batchInferenceJobArn" Arn.of_json in
      make ?solutionVersionArn ?failureReason ?lastUpdatedDateTime
        ?creationDateTime ?status ?jobName ?batchInferenceJobArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A truncated version of the BatchInferenceJob. The ListBatchInferenceJobs operation returns a list of batch inference job summaries."]
module MetricValue =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module EventType =
  struct
    type nonrec t = string
    let context_ = "EventType"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EventType" j
    let to_json = simple_to_json to_value
  end
module PerformAutoML =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module PerformHPO =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module SolutionConfig =
  struct
    type nonrec t =
      {
      eventValueThreshold: EventValueThreshold.t option
        [@ocaml.doc
          "Only events with a value greater than or equal to this threshold are used for training a model."];
      hpoConfig: HPOConfig.t option
        [@ocaml.doc
          "Describes the properties for hyperparameter optimization (HPO)."];
      algorithmHyperParameters: HyperParameters.t option
        [@ocaml.doc "Lists the hyperparameter names and ranges."];
      featureTransformationParameters:
        FeatureTransformationParameters.t option
        [@ocaml.doc "Lists the feature transformation parameters."];
      autoMLConfig: AutoMLConfig.t option
        [@ocaml.doc
          "The AutoMLConfig object containing a list of recipes to search when AutoML is performed."];
      optimizationObjective: OptimizationObjective.t option
        [@ocaml.doc
          "Describes the additional objective for the solution, such as maximizing streaming minutes or increasing revenue. For more information see Optimizing a solution."]}
    let make ?eventValueThreshold =
      fun ?hpoConfig ->
        fun ?algorithmHyperParameters ->
          fun ?featureTransformationParameters ->
            fun ?autoMLConfig ->
              fun ?optimizationObjective ->
                fun () ->
                  {
                    eventValueThreshold;
                    hpoConfig;
                    algorithmHyperParameters;
                    featureTransformationParameters;
                    autoMLConfig;
                    optimizationObjective
                  }
    let to_value x =
      structure_to_value
        [("eventValueThreshold",
           (Option.map x.eventValueThreshold ~f:EventValueThreshold.to_value));
        ("hpoConfig", (Option.map x.hpoConfig ~f:HPOConfig.to_value));
        ("algorithmHyperParameters",
          (Option.map x.algorithmHyperParameters ~f:HyperParameters.to_value));
        ("featureTransformationParameters",
          (Option.map x.featureTransformationParameters
             ~f:FeatureTransformationParameters.to_value));
        ("autoMLConfig",
          (Option.map x.autoMLConfig ~f:AutoMLConfig.to_value));
        ("optimizationObjective",
          (Option.map x.optimizationObjective
             ~f:OptimizationObjective.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let optimizationObjective =
        (Option.map ~f:OptimizationObjective.of_xml)
          (Xml.child xml_arg0 "optimizationObjective") in
      let autoMLConfig =
        (Option.map ~f:AutoMLConfig.of_xml)
          (Xml.child xml_arg0 "autoMLConfig") in
      let featureTransformationParameters =
        (Option.map ~f:FeatureTransformationParameters.of_xml)
          (Xml.child xml_arg0 "featureTransformationParameters") in
      let algorithmHyperParameters =
        (Option.map ~f:HyperParameters.of_xml)
          (Xml.child xml_arg0 "algorithmHyperParameters") in
      let hpoConfig =
        (Option.map ~f:HPOConfig.of_xml) (Xml.child xml_arg0 "hpoConfig") in
      let eventValueThreshold =
        (Option.map ~f:EventValueThreshold.of_xml)
          (Xml.child xml_arg0 "eventValueThreshold") in
      make ?optimizationObjective ?autoMLConfig
        ?featureTransformationParameters ?algorithmHyperParameters ?hpoConfig
        ?eventValueThreshold ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let optimizationObjective =
        field_map json "optimizationObjective" OptimizationObjective.of_json in
      let autoMLConfig = field_map json "autoMLConfig" AutoMLConfig.of_json in
      let featureTransformationParameters =
        field_map json "featureTransformationParameters"
          FeatureTransformationParameters.of_json in
      let algorithmHyperParameters =
        field_map json "algorithmHyperParameters" HyperParameters.of_json in
      let hpoConfig = field_map json "hpoConfig" HPOConfig.of_json in
      let eventValueThreshold =
        field_map json "eventValueThreshold" EventValueThreshold.of_json in
      make ?optimizationObjective ?autoMLConfig
        ?featureTransformationParameters ?algorithmHyperParameters ?hpoConfig
        ?eventValueThreshold ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the configuration properties for the solution."]
module TrainingHours =
  struct
    type nonrec t = float
    let make i =
      let open Result in ok_or_failwith (check_float_min i ~min:0.); i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module TrainingMode =
  struct
    type nonrec t =
      | FULL 
      | UPDATE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | FULL -> "FULL" | UPDATE -> "UPDATE" | Non_static_id s -> s
    let of_string =
      function | "FULL" -> FULL | "UPDATE" -> UPDATE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TrainingMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TrainingMode" j)
    let to_json = simple_to_json to_value
  end
module TunedHPOParams =
  struct
    type nonrec t =
      {
      algorithmHyperParameters: HyperParameters.t option
        [@ocaml.doc
          "A list of the hyperparameter values of the best performing model."]}
    let make ?algorithmHyperParameters =
      fun () -> { algorithmHyperParameters }
    let to_value x =
      structure_to_value
        [("algorithmHyperParameters",
           (Option.map x.algorithmHyperParameters ~f:HyperParameters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let algorithmHyperParameters =
        (Option.map ~f:HyperParameters.of_xml)
          (Xml.child xml_arg0 "algorithmHyperParameters") in
      make ?algorithmHyperParameters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let algorithmHyperParameters =
        field_map json "algorithmHyperParameters" HyperParameters.of_json in
      make ?algorithmHyperParameters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "If hyperparameter optimization (HPO) was performed, contains the hyperparameter values of the best performing model."]
module AutoMLResult =
  struct
    type nonrec t =
      {
      bestRecipeArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the best recipe."]}
    let make ?bestRecipeArn = fun () -> { bestRecipeArn }
    let to_value x =
      structure_to_value
        [("bestRecipeArn", (Option.map x.bestRecipeArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bestRecipeArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "bestRecipeArn") in
      make ?bestRecipeArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let bestRecipeArn = field_map json "bestRecipeArn" Arn.of_json in
      make ?bestRecipeArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "When the solution performs AutoML (performAutoML is true in CreateSolution), specifies the recipe that best optimized the specified metric."]
module AvroSchema =
  struct
    type nonrec t = string
    let context_ = "AvroSchema"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:10000); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AvroSchema" j
    let to_json = simple_to_json to_value
  end
module RecommenderUpdateSummary =
  struct
    type nonrec t =
      {
      recommenderConfig: RecommenderConfig.t option
        [@ocaml.doc "The configuration details of the recommender update."];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix format) that the recommender update was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the recommender update was last updated."];
      status: Status.t option
        [@ocaml.doc
          "The status of the recommender update. A recommender can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS"];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If a recommender update fails, the reason behind the failure."]}
    let make ?recommenderConfig =
      fun ?creationDateTime ->
        fun ?lastUpdatedDateTime ->
          fun ?status ->
            fun ?failureReason ->
              fun () ->
                {
                  recommenderConfig;
                  creationDateTime;
                  lastUpdatedDateTime;
                  status;
                  failureReason
                }
    let to_value x =
      structure_to_value
        [("recommenderConfig",
           (Option.map x.recommenderConfig ~f:RecommenderConfig.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let recommenderConfig =
        (Option.map ~f:RecommenderConfig.of_xml)
          (Xml.child xml_arg0 "recommenderConfig") in
      make ?failureReason ?status ?lastUpdatedDateTime ?creationDateTime
        ?recommenderConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let status = field_map json "status" Status.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let recommenderConfig =
        field_map json "recommenderConfig" RecommenderConfig.of_json in
      make ?failureReason ?status ?lastUpdatedDateTime ?creationDateTime
        ?recommenderConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a summary of the properties of a recommender update. For a complete listing, call the DescribeRecommender API."]
module Description =
  struct
    type nonrec t = string
    let context_ = "Description"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Description" j
    let to_json = simple_to_json to_value
  end
module RecipeType =
  struct
    type nonrec t = string
    let context_ = "RecipeType"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RecipeType" j
    let to_json = simple_to_json to_value
  end
module FilterExpression =
  struct
    type nonrec t = string
    let context_ = "FilterExpression"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2500) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FilterExpression" j
    let to_json = simple_to_json to_value
  end
module FeaturizationParameters =
  struct
    type nonrec t = (ParameterName.t * ParameterValue.t) list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ParameterName.of_string chopped),
                              (ParameterValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ParameterName.to_value x) |>
                    (fun x ->
                       (ParameterValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ParameterName.of_string
        ~of_json:ParameterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module AccountId =
  struct
    type nonrec t = string
    let context_ = "AccountId"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AccountId" j
    let to_json = simple_to_json to_value
  end
module TrackingId =
  struct
    type nonrec t = string
    let context_ = "TrackingId"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TrackingId" j
    let to_json = simple_to_json to_value
  end
module DataSource =
  struct
    type nonrec t =
      {
      dataLocation: S3Location.t option
        [@ocaml.doc
          "The path to the Amazon S3 bucket where the data that you want to upload to your dataset is stored. For example: s3://bucket-name/folder-name/"]}
    let make ?dataLocation = fun () -> { dataLocation }
    let to_value x =
      structure_to_value
        [("dataLocation", (Option.map x.dataLocation ~f:S3Location.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dataLocation =
        (Option.map ~f:S3Location.of_xml) (Xml.child xml_arg0 "dataLocation") in
      make ?dataLocation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let dataLocation = field_map json "dataLocation" S3Location.of_json in
      make ?dataLocation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the data source that contains the data to upload to a dataset."]
module RoleArn =
  struct
    type nonrec t = string
    let context_ = "RoleArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"arn:([a-z\\d-]+):iam::\\d{12}:role/?[a-zA-Z_0-9+=,.@\\-_/]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RoleArn" j
    let to_json = simple_to_json to_value
  end
module DatasetExportJobOutput =
  struct
    type nonrec t = {
      s3DataDestination: S3DataConfig.t }
    let context_ = "DatasetExportJobOutput"
    let make ~s3DataDestination = fun () -> { s3DataDestination }
    let to_value x =
      structure_to_value
        [("s3DataDestination",
           (Some (S3DataConfig.to_value x.s3DataDestination)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3DataDestination =
        S3DataConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "s3DataDestination") in
      make ~s3DataDestination ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let s3DataDestination =
        field_map_exn json "s3DataDestination" S3DataConfig.of_json in
      make ~s3DataDestination ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The output configuration parameters of a dataset export job."]
module IngestionMode =
  struct
    type nonrec t =
      | BULK 
      | PUT 
      | ALL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BULK -> "BULK"
      | PUT -> "PUT"
      | ALL -> "ALL"
      | Non_static_id s -> s
    let of_string =
      function
      | "BULK" -> BULK
      | "PUT" -> PUT
      | "ALL" -> ALL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration IngestionMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"IngestionMode" j)
    let to_json = simple_to_json to_value
  end
module CampaignUpdateSummary =
  struct
    type nonrec t =
      {
      solutionVersionArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the deployed solution version."];
      minProvisionedTPS: TransactionsPerSecond.t option
        [@ocaml.doc
          "Specifies the requested minimum provisioned transactions (recommendations) per second that Amazon Personalize will support."];
      campaignConfig: CampaignConfig.t option ;
      status: Status.t option
        [@ocaml.doc
          "The status of the campaign update. A campaign update can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS"];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If a campaign update fails, the reason behind the failure."];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the campaign update was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the campaign update was last updated."]}
    let make ?solutionVersionArn =
      fun ?minProvisionedTPS ->
        fun ?campaignConfig ->
          fun ?status ->
            fun ?failureReason ->
              fun ?creationDateTime ->
                fun ?lastUpdatedDateTime ->
                  fun () ->
                    {
                      solutionVersionArn;
                      minProvisionedTPS;
                      campaignConfig;
                      status;
                      failureReason;
                      creationDateTime;
                      lastUpdatedDateTime
                    }
    let to_value x =
      structure_to_value
        [("solutionVersionArn",
           (Option.map x.solutionVersionArn ~f:Arn.to_value));
        ("minProvisionedTPS",
          (Option.map x.minProvisionedTPS ~f:TransactionsPerSecond.to_value));
        ("campaignConfig",
          (Option.map x.campaignConfig ~f:CampaignConfig.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let campaignConfig =
        (Option.map ~f:CampaignConfig.of_xml)
          (Xml.child xml_arg0 "campaignConfig") in
      let minProvisionedTPS =
        (Option.map ~f:TransactionsPerSecond.of_xml)
          (Xml.child xml_arg0 "minProvisionedTPS") in
      let solutionVersionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionVersionArn") in
      make ?lastUpdatedDateTime ?creationDateTime ?failureReason ?status
        ?campaignConfig ?minProvisionedTPS ?solutionVersionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let status = field_map json "status" Status.of_json in
      let campaignConfig =
        field_map json "campaignConfig" CampaignConfig.of_json in
      let minProvisionedTPS =
        field_map json "minProvisionedTPS" TransactionsPerSecond.of_json in
      let solutionVersionArn =
        field_map json "solutionVersionArn" Arn.of_json in
      make ?lastUpdatedDateTime ?creationDateTime ?failureReason ?status
        ?campaignConfig ?minProvisionedTPS ?solutionVersionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a summary of the properties of a campaign update. For a complete listing, call the DescribeCampaign API."]
module BatchSegmentJobInput =
  struct
    type nonrec t = {
      s3DataSource: S3DataConfig.t }
    let context_ = "BatchSegmentJobInput"
    let make ~s3DataSource = fun () -> { s3DataSource }
    let to_value x =
      structure_to_value
        [("s3DataSource", (Some (S3DataConfig.to_value x.s3DataSource)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3DataSource =
        S3DataConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "s3DataSource") in
      make ~s3DataSource ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let s3DataSource =
        field_map_exn json "s3DataSource" S3DataConfig.of_json in
      make ~s3DataSource ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The input configuration of a batch segment job."]
module BatchSegmentJobOutput =
  struct
    type nonrec t = {
      s3DataDestination: S3DataConfig.t }
    let context_ = "BatchSegmentJobOutput"
    let make ~s3DataDestination = fun () -> { s3DataDestination }
    let to_value x =
      structure_to_value
        [("s3DataDestination",
           (Some (S3DataConfig.to_value x.s3DataDestination)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3DataDestination =
        S3DataConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "s3DataDestination") in
      make ~s3DataDestination ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let s3DataDestination =
        field_map_exn json "s3DataDestination" S3DataConfig.of_json in
      make ~s3DataDestination ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The output configuration parameters of a batch segment job."]
module NumBatchResults =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for NumBatchResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module BatchInferenceJobConfig =
  struct
    type nonrec t =
      {
      itemExplorationConfig: HyperParameters.t option
        [@ocaml.doc
          "A string to string map specifying the exploration configuration hyperparameters, including explorationWeight and explorationItemAgeCutOff, you want to use to configure the amount of item exploration Amazon Personalize uses when recommending items. See User-Personalization."]}
    let make ?itemExplorationConfig = fun () -> { itemExplorationConfig }
    let to_value x =
      structure_to_value
        [("itemExplorationConfig",
           (Option.map x.itemExplorationConfig ~f:HyperParameters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let itemExplorationConfig =
        (Option.map ~f:HyperParameters.of_xml)
          (Xml.child xml_arg0 "itemExplorationConfig") in
      make ?itemExplorationConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let itemExplorationConfig =
        field_map json "itemExplorationConfig" HyperParameters.of_json in
      make ?itemExplorationConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The configuration details of a batch inference job."]
module BatchInferenceJobInput =
  struct
    type nonrec t =
      {
      s3DataSource: S3DataConfig.t
        [@ocaml.doc
          "The URI of the Amazon S3 location that contains your input data. The Amazon S3 bucket must be in the same region as the API endpoint you are calling."]}
    let context_ = "BatchInferenceJobInput"
    let make ~s3DataSource = fun () -> { s3DataSource }
    let to_value x =
      structure_to_value
        [("s3DataSource", (Some (S3DataConfig.to_value x.s3DataSource)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3DataSource =
        S3DataConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "s3DataSource") in
      make ~s3DataSource ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let s3DataSource =
        field_map_exn json "s3DataSource" S3DataConfig.of_json in
      make ~s3DataSource ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The input configuration of a batch inference job."]
module BatchInferenceJobOutput =
  struct
    type nonrec t =
      {
      s3DataDestination: S3DataConfig.t
        [@ocaml.doc
          "Information on the Amazon S3 bucket in which the batch inference job's output is stored."]}
    let context_ = "BatchInferenceJobOutput"
    let make ~s3DataDestination = fun () -> { s3DataDestination }
    let to_value x =
      structure_to_value
        [("s3DataDestination",
           (Some (S3DataConfig.to_value x.s3DataDestination)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3DataDestination =
        S3DataConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "s3DataDestination") in
      make ~s3DataDestination ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let s3DataDestination =
        field_map_exn json "s3DataDestination" S3DataConfig.of_json in
      make ~s3DataDestination ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The output configuration parameters of a batch inference job."]
module AlgorithmImage =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the algorithm image."];
      dockerURI: DockerURI.t
        [@ocaml.doc
          "The URI of the Docker container for the algorithm image."]}
    let context_ = "AlgorithmImage"
    let make ?name = fun ~dockerURI -> fun () -> { name; dockerURI }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("dockerURI", (Some (DockerURI.to_value x.dockerURI)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dockerURI =
        DockerURI.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "dockerURI") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ~dockerURI ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let dockerURI = field_map_exn json "dockerURI" DockerURI.of_json in
      let name = field_map json "name" Name.of_json in
      make ~dockerURI ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes an algorithm image."]
module DefaultHyperParameterRanges =
  struct
    type nonrec t =
      {
      integerHyperParameterRanges:
        DefaultIntegerHyperParameterRanges.t option
        [@ocaml.doc
          "The integer-valued hyperparameters and their default ranges."];
      continuousHyperParameterRanges:
        DefaultContinuousHyperParameterRanges.t option
        [@ocaml.doc
          "The continuous hyperparameters and their default ranges."];
      categoricalHyperParameterRanges:
        DefaultCategoricalHyperParameterRanges.t option
        [@ocaml.doc
          "The categorical hyperparameters and their default ranges."]}
    let make ?integerHyperParameterRanges =
      fun ?continuousHyperParameterRanges ->
        fun ?categoricalHyperParameterRanges ->
          fun () ->
            {
              integerHyperParameterRanges;
              continuousHyperParameterRanges;
              categoricalHyperParameterRanges
            }
    let to_value x =
      structure_to_value
        [("integerHyperParameterRanges",
           (Option.map x.integerHyperParameterRanges
              ~f:DefaultIntegerHyperParameterRanges.to_value));
        ("continuousHyperParameterRanges",
          (Option.map x.continuousHyperParameterRanges
             ~f:DefaultContinuousHyperParameterRanges.to_value));
        ("categoricalHyperParameterRanges",
          (Option.map x.categoricalHyperParameterRanges
             ~f:DefaultCategoricalHyperParameterRanges.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let categoricalHyperParameterRanges =
        (Option.map ~f:DefaultCategoricalHyperParameterRanges.of_xml)
          (Xml.child xml_arg0 "categoricalHyperParameterRanges") in
      let continuousHyperParameterRanges =
        (Option.map ~f:DefaultContinuousHyperParameterRanges.of_xml)
          (Xml.child xml_arg0 "continuousHyperParameterRanges") in
      let integerHyperParameterRanges =
        (Option.map ~f:DefaultIntegerHyperParameterRanges.of_xml)
          (Xml.child xml_arg0 "integerHyperParameterRanges") in
      make ?categoricalHyperParameterRanges ?continuousHyperParameterRanges
        ?integerHyperParameterRanges ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let categoricalHyperParameterRanges =
        field_map json "categoricalHyperParameterRanges"
          DefaultCategoricalHyperParameterRanges.of_json in
      let continuousHyperParameterRanges =
        field_map json "continuousHyperParameterRanges"
          DefaultContinuousHyperParameterRanges.of_json in
      let integerHyperParameterRanges =
        field_map json "integerHyperParameterRanges"
          DefaultIntegerHyperParameterRanges.of_json in
      make ?categoricalHyperParameterRanges ?continuousHyperParameterRanges
        ?integerHyperParameterRanges ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the hyperparameters and their default ranges. Hyperparameters can be categorical, continuous, or integer-valued."]
module ResourceConfig =
  struct
    type nonrec t = (ParameterName.t * ParameterValue.t) list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ParameterName.of_string chopped),
                              (ParameterValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ParameterName.to_value x) |>
                    (fun x ->
                       (ParameterValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ParameterName.of_string
        ~of_json:ParameterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module TrainingInputMode =
  struct
    type nonrec t = string
    let context_ = "TrainingInputMode"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TrainingInputMode" j
    let to_json = simple_to_json to_value
  end
module InvalidInputException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Provide a valid value for the field or parameter."]
module ResourceInUseException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified resource is in use."]
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Could not find the specified resource."]
module TooManyTagKeysException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request contains more tag keys than can be associated with a resource (50 tag keys per resource)."]
module TagKeys =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeys" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module LimitExceededException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The limit on the number of requests per second has been exceeded."]
module TooManyTagsException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You have exceeded the maximum number of tags you can apply to this resource."]
module Tags =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"Tags" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module InvalidNextTokenException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The token is not valid."]
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1500); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module Solutions =
  struct
    type nonrec t = SolutionSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:SolutionSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SolutionSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"Solutions" ~of_json:SolutionSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SolutionVersions =
  struct
    type nonrec t = SolutionVersionSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:SolutionVersionSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SolutionVersionSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"SolutionVersions"
        ~of_json:SolutionVersionSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module Schemas =
  struct
    type nonrec t = DatasetSchemaSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:DatasetSchemaSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DatasetSchemaSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"Schemas" ~of_json:DatasetSchemaSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module Recommenders =
  struct
    type nonrec t = RecommenderSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:RecommenderSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RecommenderSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"Recommenders" ~of_json:RecommenderSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module Recipes =
  struct
    type nonrec t = RecipeSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:RecipeSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RecipeSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"Recipes" ~of_json:RecipeSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module RecipeProvider =
  struct
    type nonrec t =
      | SERVICE 
      | Non_static_id of string 
    let make i = i
    let to_string = function | SERVICE -> "SERVICE" | Non_static_id s -> s
    let of_string = function | "SERVICE" -> SERVICE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration RecipeProvider" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RecipeProvider" j)
    let to_json = simple_to_json to_value
  end
module Filters =
  struct
    type nonrec t = FilterSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:FilterSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FilterSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"Filters" ~of_json:FilterSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventTrackers =
  struct
    type nonrec t = EventTrackerSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:EventTrackerSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EventTrackerSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"EventTrackers" ~of_json:EventTrackerSummary.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module Datasets =
  struct
    type nonrec t = DatasetSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:DatasetSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DatasetSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"Datasets" ~of_json:DatasetSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module DatasetImportJobs =
  struct
    type nonrec t = DatasetImportJobSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:DatasetImportJobSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DatasetImportJobSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"DatasetImportJobs"
        ~of_json:DatasetImportJobSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module DatasetGroups =
  struct
    type nonrec t = DatasetGroupSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:DatasetGroupSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DatasetGroupSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"DatasetGroups" ~of_json:DatasetGroupSummary.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module DatasetExportJobs =
  struct
    type nonrec t = DatasetExportJobSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:DatasetExportJobSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DatasetExportJobSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"DatasetExportJobs"
        ~of_json:DatasetExportJobSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module Campaigns =
  struct
    type nonrec t = CampaignSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:CampaignSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CampaignSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"Campaigns" ~of_json:CampaignSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchSegmentJobs =
  struct
    type nonrec t = BatchSegmentJobSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:BatchSegmentJobSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchSegmentJobSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchSegmentJobs"
        ~of_json:BatchSegmentJobSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchInferenceJobs =
  struct
    type nonrec t = BatchInferenceJobSummary.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:BatchInferenceJobSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchInferenceJobSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchInferenceJobs"
        ~of_json:BatchInferenceJobSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module Metrics =
  struct
    type nonrec t = (MetricName.t * MetricValue.t) list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((MetricName.of_string chopped),
                              (MetricValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (MetricName.to_value x) |>
                    (fun x -> (MetricValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:MetricName.of_string
        ~of_json:MetricValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module SolutionVersion =
  struct
    type nonrec t =
      {
      solutionVersionArn: Arn.t option
        [@ocaml.doc "The ARN of the solution version."];
      solutionArn: Arn.t option [@ocaml.doc "The ARN of the solution."];
      performHPO: PerformHPO.t option
        [@ocaml.doc
          "Whether to perform hyperparameter optimization (HPO) on the chosen recipe. The default is false."];
      performAutoML: PerformAutoML.t option
        [@ocaml.doc
          "When true, Amazon Personalize searches for the most optimal recipe according to the solution configuration. When false (the default), Amazon Personalize uses recipeArn."];
      recipeArn: Arn.t option
        [@ocaml.doc "The ARN of the recipe used in the solution."];
      eventType: EventType.t option
        [@ocaml.doc
          "The event type (for example, 'click' or 'like') that is used for training the model."];
      datasetGroupArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset group providing the training data."];
      solutionConfig: SolutionConfig.t option
        [@ocaml.doc
          "Describes the configuration properties for the solution."];
      trainingHours: TrainingHours.t option
        [@ocaml.doc
          "The time used to train the model. You are billed for the time it takes to train a model. This field is visible only after Amazon Personalize successfully trains a model."];
      trainingMode: TrainingMode.t option
        [@ocaml.doc
          "The scope of training to be performed when creating the solution version. The FULL option trains the solution version based on the entirety of the input solution's training data, while the UPDATE option processes only the data that has changed in comparison to the input solution. Choose UPDATE when you want to incrementally update your solution version instead of creating an entirely new one. The UPDATE option can only be used when you already have an active solution version created from the input solution using the FULL option and the input solution was trained with the User-Personalization recipe or the HRNN-Coldstart recipe."];
      tunedHPOParams: TunedHPOParams.t option
        [@ocaml.doc
          "If hyperparameter optimization was performed, contains the hyperparameter values of the best performing model."];
      status: Status.t option
        [@ocaml.doc
          "The status of the solution version. A solution version can be in one of the following states: CREATE PENDING CREATE IN_PROGRESS ACTIVE CREATE FAILED CREATE STOPPING CREATE STOPPED"];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If training a solution version fails, the reason for the failure."];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that this version of the solution was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the solution was last updated."]}
    let make ?solutionVersionArn =
      fun ?solutionArn ->
        fun ?performHPO ->
          fun ?performAutoML ->
            fun ?recipeArn ->
              fun ?eventType ->
                fun ?datasetGroupArn ->
                  fun ?solutionConfig ->
                    fun ?trainingHours ->
                      fun ?trainingMode ->
                        fun ?tunedHPOParams ->
                          fun ?status ->
                            fun ?failureReason ->
                              fun ?creationDateTime ->
                                fun ?lastUpdatedDateTime ->
                                  fun () ->
                                    {
                                      solutionVersionArn;
                                      solutionArn;
                                      performHPO;
                                      performAutoML;
                                      recipeArn;
                                      eventType;
                                      datasetGroupArn;
                                      solutionConfig;
                                      trainingHours;
                                      trainingMode;
                                      tunedHPOParams;
                                      status;
                                      failureReason;
                                      creationDateTime;
                                      lastUpdatedDateTime
                                    }
    let to_value x =
      structure_to_value
        [("solutionVersionArn",
           (Option.map x.solutionVersionArn ~f:Arn.to_value));
        ("solutionArn", (Option.map x.solutionArn ~f:Arn.to_value));
        ("performHPO", (Option.map x.performHPO ~f:PerformHPO.to_value));
        ("performAutoML",
          (Option.map x.performAutoML ~f:PerformAutoML.to_value));
        ("recipeArn", (Option.map x.recipeArn ~f:Arn.to_value));
        ("eventType", (Option.map x.eventType ~f:EventType.to_value));
        ("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("solutionConfig",
          (Option.map x.solutionConfig ~f:SolutionConfig.to_value));
        ("trainingHours",
          (Option.map x.trainingHours ~f:TrainingHours.to_value));
        ("trainingMode",
          (Option.map x.trainingMode ~f:TrainingMode.to_value));
        ("tunedHPOParams",
          (Option.map x.tunedHPOParams ~f:TunedHPOParams.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let tunedHPOParams =
        (Option.map ~f:TunedHPOParams.of_xml)
          (Xml.child xml_arg0 "tunedHPOParams") in
      let trainingMode =
        (Option.map ~f:TrainingMode.of_xml)
          (Xml.child xml_arg0 "trainingMode") in
      let trainingHours =
        (Option.map ~f:TrainingHours.of_xml)
          (Xml.child xml_arg0 "trainingHours") in
      let solutionConfig =
        (Option.map ~f:SolutionConfig.of_xml)
          (Xml.child xml_arg0 "solutionConfig") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      let eventType =
        (Option.map ~f:EventType.of_xml) (Xml.child xml_arg0 "eventType") in
      let recipeArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "recipeArn") in
      let performAutoML =
        (Option.map ~f:PerformAutoML.of_xml)
          (Xml.child xml_arg0 "performAutoML") in
      let performHPO =
        (Option.map ~f:PerformHPO.of_xml) (Xml.child xml_arg0 "performHPO") in
      let solutionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionArn") in
      let solutionVersionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionVersionArn") in
      make ?lastUpdatedDateTime ?creationDateTime ?failureReason ?status
        ?tunedHPOParams ?trainingMode ?trainingHours ?solutionConfig
        ?datasetGroupArn ?eventType ?recipeArn ?performAutoML ?performHPO
        ?solutionArn ?solutionVersionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let status = field_map json "status" Status.of_json in
      let tunedHPOParams =
        field_map json "tunedHPOParams" TunedHPOParams.of_json in
      let trainingMode = field_map json "trainingMode" TrainingMode.of_json in
      let trainingHours =
        field_map json "trainingHours" TrainingHours.of_json in
      let solutionConfig =
        field_map json "solutionConfig" SolutionConfig.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      let eventType = field_map json "eventType" EventType.of_json in
      let recipeArn = field_map json "recipeArn" Arn.of_json in
      let performAutoML =
        field_map json "performAutoML" PerformAutoML.of_json in
      let performHPO = field_map json "performHPO" PerformHPO.of_json in
      let solutionArn = field_map json "solutionArn" Arn.of_json in
      let solutionVersionArn =
        field_map json "solutionVersionArn" Arn.of_json in
      make ?lastUpdatedDateTime ?creationDateTime ?failureReason ?status
        ?tunedHPOParams ?trainingMode ?trainingHours ?solutionConfig
        ?datasetGroupArn ?eventType ?recipeArn ?performAutoML ?performHPO
        ?solutionArn ?solutionVersionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object that provides information about a specific version of a Solution in a Custom dataset group."]
module Solution =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the solution."];
      solutionArn: Arn.t option [@ocaml.doc "The ARN of the solution."];
      performHPO: PerformHPO.t option
        [@ocaml.doc
          "Whether to perform hyperparameter optimization (HPO) on the chosen recipe. The default is false."];
      performAutoML: PerformAutoML.t option
        [@ocaml.doc
          "When true, Amazon Personalize performs a search for the best USER_PERSONALIZATION recipe from the list specified in the solution configuration (recipeArn must not be specified). When false (the default), Amazon Personalize uses recipeArn for training."];
      recipeArn: Arn.t option
        [@ocaml.doc "The ARN of the recipe used to create the solution."];
      datasetGroupArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset group that provides the training data."];
      eventType: EventType.t option
        [@ocaml.doc
          "The event type (for example, 'click' or 'like') that is used for training the model. If no eventType is provided, Amazon Personalize uses all interactions for training with equal weight regardless of type."];
      solutionConfig: SolutionConfig.t option
        [@ocaml.doc
          "Describes the configuration properties for the solution."];
      autoMLResult: AutoMLResult.t option
        [@ocaml.doc
          "When performAutoML is true, specifies the best recipe found."];
      status: Status.t option
        [@ocaml.doc
          "The status of the solution. A solution can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The creation date and time (in Unix time) of the solution."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the solution was last updated."];
      latestSolutionVersion: SolutionVersionSummary.t option
        [@ocaml.doc
          "Describes the latest version of the solution, including the status and the ARN."]}
    let make ?name =
      fun ?solutionArn ->
        fun ?performHPO ->
          fun ?performAutoML ->
            fun ?recipeArn ->
              fun ?datasetGroupArn ->
                fun ?eventType ->
                  fun ?solutionConfig ->
                    fun ?autoMLResult ->
                      fun ?status ->
                        fun ?creationDateTime ->
                          fun ?lastUpdatedDateTime ->
                            fun ?latestSolutionVersion ->
                              fun () ->
                                {
                                  name;
                                  solutionArn;
                                  performHPO;
                                  performAutoML;
                                  recipeArn;
                                  datasetGroupArn;
                                  eventType;
                                  solutionConfig;
                                  autoMLResult;
                                  status;
                                  creationDateTime;
                                  lastUpdatedDateTime;
                                  latestSolutionVersion
                                }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("solutionArn", (Option.map x.solutionArn ~f:Arn.to_value));
        ("performHPO", (Option.map x.performHPO ~f:PerformHPO.to_value));
        ("performAutoML",
          (Option.map x.performAutoML ~f:PerformAutoML.to_value));
        ("recipeArn", (Option.map x.recipeArn ~f:Arn.to_value));
        ("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("eventType", (Option.map x.eventType ~f:EventType.to_value));
        ("solutionConfig",
          (Option.map x.solutionConfig ~f:SolutionConfig.to_value));
        ("autoMLResult",
          (Option.map x.autoMLResult ~f:AutoMLResult.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("latestSolutionVersion",
          (Option.map x.latestSolutionVersion
             ~f:SolutionVersionSummary.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let latestSolutionVersion =
        (Option.map ~f:SolutionVersionSummary.of_xml)
          (Xml.child xml_arg0 "latestSolutionVersion") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let autoMLResult =
        (Option.map ~f:AutoMLResult.of_xml)
          (Xml.child xml_arg0 "autoMLResult") in
      let solutionConfig =
        (Option.map ~f:SolutionConfig.of_xml)
          (Xml.child xml_arg0 "solutionConfig") in
      let eventType =
        (Option.map ~f:EventType.of_xml) (Xml.child xml_arg0 "eventType") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      let recipeArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "recipeArn") in
      let performAutoML =
        (Option.map ~f:PerformAutoML.of_xml)
          (Xml.child xml_arg0 "performAutoML") in
      let performHPO =
        (Option.map ~f:PerformHPO.of_xml) (Xml.child xml_arg0 "performHPO") in
      let solutionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?latestSolutionVersion ?lastUpdatedDateTime ?creationDateTime
        ?status ?autoMLResult ?solutionConfig ?eventType ?datasetGroupArn
        ?recipeArn ?performAutoML ?performHPO ?solutionArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let latestSolutionVersion =
        field_map json "latestSolutionVersion" SolutionVersionSummary.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let autoMLResult = field_map json "autoMLResult" AutoMLResult.of_json in
      let solutionConfig =
        field_map json "solutionConfig" SolutionConfig.of_json in
      let eventType = field_map json "eventType" EventType.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      let recipeArn = field_map json "recipeArn" Arn.of_json in
      let performAutoML =
        field_map json "performAutoML" PerformAutoML.of_json in
      let performHPO = field_map json "performHPO" PerformHPO.of_json in
      let solutionArn = field_map json "solutionArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?latestSolutionVersion ?lastUpdatedDateTime ?creationDateTime
        ?status ?autoMLResult ?solutionConfig ?eventType ?datasetGroupArn
        ?recipeArn ?performAutoML ?performHPO ?solutionArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object that provides information about a solution. A solution is a trained model that can be deployed as a campaign."]
module DatasetSchema =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the schema."];
      schemaArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the schema."];
      schema: AvroSchema.t option [@ocaml.doc "The schema."];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the schema was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the schema was last updated."];
      domain: Domain.t option
        [@ocaml.doc
          "The domain of a schema that you created for a dataset in a Domain dataset group."]}
    let make ?name =
      fun ?schemaArn ->
        fun ?schema ->
          fun ?creationDateTime ->
            fun ?lastUpdatedDateTime ->
              fun ?domain ->
                fun () ->
                  {
                    name;
                    schemaArn;
                    schema;
                    creationDateTime;
                    lastUpdatedDateTime;
                    domain
                  }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("schemaArn", (Option.map x.schemaArn ~f:Arn.to_value));
        ("schema", (Option.map x.schema ~f:AvroSchema.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("domain", (Option.map x.domain ~f:Domain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        (Option.map ~f:Domain.of_xml) (Xml.child xml_arg0 "domain") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let schema =
        (Option.map ~f:AvroSchema.of_xml) (Xml.child xml_arg0 "schema") in
      let schemaArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "schemaArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?domain ?lastUpdatedDateTime ?creationDateTime ?schema ?schemaArn
        ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let domain = field_map json "domain" Domain.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let schema = field_map json "schema" AvroSchema.of_json in
      let schemaArn = field_map json "schemaArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?domain ?lastUpdatedDateTime ?creationDateTime ?schema ?schemaArn
        ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the schema for a dataset. For more information on schemas, see CreateSchema."]
module Recommender =
  struct
    type nonrec t =
      {
      recommenderArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the recommender."];
      datasetGroupArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Domain dataset group that contains the recommender."];
      name: Name.t option [@ocaml.doc "The name of the recommender."];
      recipeArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the recipe (Domain dataset group use case) that the recommender was created for."];
      recommenderConfig: RecommenderConfig.t option
        [@ocaml.doc "The configuration details of the recommender."];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix format) that the recommender was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix format) that the recommender was last updated."];
      status: Status.t option
        [@ocaml.doc
          "The status of the recommender. A recommender can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS"];
      failureReason: FailureReason.t option
        [@ocaml.doc "If a recommender fails, the reason behind the failure."];
      latestRecommenderUpdate: RecommenderUpdateSummary.t option
        [@ocaml.doc
          "Provides a summary of the latest updates to the recommender."]}
    let make ?recommenderArn =
      fun ?datasetGroupArn ->
        fun ?name ->
          fun ?recipeArn ->
            fun ?recommenderConfig ->
              fun ?creationDateTime ->
                fun ?lastUpdatedDateTime ->
                  fun ?status ->
                    fun ?failureReason ->
                      fun ?latestRecommenderUpdate ->
                        fun () ->
                          {
                            recommenderArn;
                            datasetGroupArn;
                            name;
                            recipeArn;
                            recommenderConfig;
                            creationDateTime;
                            lastUpdatedDateTime;
                            status;
                            failureReason;
                            latestRecommenderUpdate
                          }
    let to_value x =
      structure_to_value
        [("recommenderArn", (Option.map x.recommenderArn ~f:Arn.to_value));
        ("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("recipeArn", (Option.map x.recipeArn ~f:Arn.to_value));
        ("recommenderConfig",
          (Option.map x.recommenderConfig ~f:RecommenderConfig.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("latestRecommenderUpdate",
          (Option.map x.latestRecommenderUpdate
             ~f:RecommenderUpdateSummary.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let latestRecommenderUpdate =
        (Option.map ~f:RecommenderUpdateSummary.of_xml)
          (Xml.child xml_arg0 "latestRecommenderUpdate") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let recommenderConfig =
        (Option.map ~f:RecommenderConfig.of_xml)
          (Xml.child xml_arg0 "recommenderConfig") in
      let recipeArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "recipeArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      let recommenderArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "recommenderArn") in
      make ?latestRecommenderUpdate ?failureReason ?status
        ?lastUpdatedDateTime ?creationDateTime ?recommenderConfig ?recipeArn
        ?name ?datasetGroupArn ?recommenderArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let latestRecommenderUpdate =
        field_map json "latestRecommenderUpdate"
          RecommenderUpdateSummary.of_json in
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let status = field_map json "status" Status.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let recommenderConfig =
        field_map json "recommenderConfig" RecommenderConfig.of_json in
      let recipeArn = field_map json "recipeArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      let recommenderArn = field_map json "recommenderArn" Arn.of_json in
      make ?latestRecommenderUpdate ?failureReason ?status
        ?lastUpdatedDateTime ?creationDateTime ?recommenderConfig ?recipeArn
        ?name ?datasetGroupArn ?recommenderArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a recommendation generator for a Domain dataset group. You create a recommender in a Domain dataset group for a specific domain use case (domain recipe), and specify the recommender in a GetRecommendations request."]
module Recipe =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the recipe."];
      recipeArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the recipe."];
      algorithmArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the algorithm that Amazon Personalize uses to train the model."];
      featureTransformationArn: Arn.t option
        [@ocaml.doc "The ARN of the FeatureTransformation object."];
      status: Status.t option [@ocaml.doc "The status of the recipe."];
      description: Description.t option
        [@ocaml.doc "The description of the recipe."];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix format) that the recipe was created."];
      recipeType: RecipeType.t option
        [@ocaml.doc
          "One of the following values: PERSONALIZED_RANKING RELATED_ITEMS USER_PERSONALIZATION"];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix format) that the recipe was last updated."]}
    let make ?name =
      fun ?recipeArn ->
        fun ?algorithmArn ->
          fun ?featureTransformationArn ->
            fun ?status ->
              fun ?description ->
                fun ?creationDateTime ->
                  fun ?recipeType ->
                    fun ?lastUpdatedDateTime ->
                      fun () ->
                        {
                          name;
                          recipeArn;
                          algorithmArn;
                          featureTransformationArn;
                          status;
                          description;
                          creationDateTime;
                          recipeType;
                          lastUpdatedDateTime
                        }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("recipeArn", (Option.map x.recipeArn ~f:Arn.to_value));
        ("algorithmArn", (Option.map x.algorithmArn ~f:Arn.to_value));
        ("featureTransformationArn",
          (Option.map x.featureTransformationArn ~f:Arn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("description", (Option.map x.description ~f:Description.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("recipeType", (Option.map x.recipeType ~f:RecipeType.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let recipeType =
        (Option.map ~f:RecipeType.of_xml) (Xml.child xml_arg0 "recipeType") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "description") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let featureTransformationArn =
        (Option.map ~f:Arn.of_xml)
          (Xml.child xml_arg0 "featureTransformationArn") in
      let algorithmArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "algorithmArn") in
      let recipeArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "recipeArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?lastUpdatedDateTime ?recipeType ?creationDateTime ?description
        ?status ?featureTransformationArn ?algorithmArn ?recipeArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let recipeType = field_map json "recipeType" RecipeType.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let description = field_map json "description" Description.of_json in
      let status = field_map json "status" Status.of_json in
      let featureTransformationArn =
        field_map json "featureTransformationArn" Arn.of_json in
      let algorithmArn = field_map json "algorithmArn" Arn.of_json in
      let recipeArn = field_map json "recipeArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?lastUpdatedDateTime ?recipeType ?creationDateTime ?description
        ?status ?featureTransformationArn ?algorithmArn ?recipeArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides information about a recipe. Each recipe provides an algorithm that Amazon Personalize uses in model training when you use the CreateSolution operation."]
module Filter =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the filter."];
      filterArn: Arn.t option [@ocaml.doc "The ARN of the filter."];
      creationDateTime: Date.t option
        [@ocaml.doc "The time at which the filter was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc "The time at which the filter was last updated."];
      datasetGroupArn: Arn.t option
        [@ocaml.doc
          "The ARN of the dataset group to which the filter belongs."];
      failureReason: FailureReason.t option
        [@ocaml.doc "If the filter failed, the reason for its failure."];
      filterExpression: FilterExpression.t option
        [@ocaml.doc
          "Specifies the type of item interactions to filter out of recommendation results. The filter expression must follow specific format rules. For information about filter expression structure and syntax, see Filter expressions."];
      status: Status.t option [@ocaml.doc "The status of the filter."]}
    let make ?name =
      fun ?filterArn ->
        fun ?creationDateTime ->
          fun ?lastUpdatedDateTime ->
            fun ?datasetGroupArn ->
              fun ?failureReason ->
                fun ?filterExpression ->
                  fun ?status ->
                    fun () ->
                      {
                        name;
                        filterArn;
                        creationDateTime;
                        lastUpdatedDateTime;
                        datasetGroupArn;
                        failureReason;
                        filterExpression;
                        status
                      }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("filterArn", (Option.map x.filterArn ~f:Arn.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("filterExpression",
          (Option.map x.filterExpression ~f:FilterExpression.to_value));
        ("status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let filterExpression =
        (Option.map ~f:FilterExpression.of_xml)
          (Xml.child xml_arg0 "filterExpression") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let filterArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "filterArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?status ?filterExpression ?failureReason ?datasetGroupArn
        ?lastUpdatedDateTime ?creationDateTime ?filterArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let status = field_map json "status" Status.of_json in
      let filterExpression =
        field_map json "filterExpression" FilterExpression.of_json in
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let filterArn = field_map json "filterArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?status ?filterExpression ?failureReason ?datasetGroupArn
        ?lastUpdatedDateTime ?creationDateTime ?filterArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information on a recommendation filter, including its ARN, status, and filter expression."]
module FeatureTransformation =
  struct
    type nonrec t =
      {
      name: Name.t option
        [@ocaml.doc "The name of the feature transformation."];
      featureTransformationArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the FeatureTransformation object."];
      defaultParameters: FeaturizationParameters.t option
        [@ocaml.doc
          "Provides the default parameters for feature transformation."];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The creation date and time (in Unix time) of the feature transformation."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The last update date and time (in Unix time) of the feature transformation."];
      status: Status.t option
        [@ocaml.doc
          "The status of the feature transformation. A feature transformation can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED"]}
    let make ?name =
      fun ?featureTransformationArn ->
        fun ?defaultParameters ->
          fun ?creationDateTime ->
            fun ?lastUpdatedDateTime ->
              fun ?status ->
                fun () ->
                  {
                    name;
                    featureTransformationArn;
                    defaultParameters;
                    creationDateTime;
                    lastUpdatedDateTime;
                    status
                  }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("featureTransformationArn",
          (Option.map x.featureTransformationArn ~f:Arn.to_value));
        ("defaultParameters",
          (Option.map x.defaultParameters ~f:FeaturizationParameters.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let defaultParameters =
        (Option.map ~f:FeaturizationParameters.of_xml)
          (Xml.child xml_arg0 "defaultParameters") in
      let featureTransformationArn =
        (Option.map ~f:Arn.of_xml)
          (Xml.child xml_arg0 "featureTransformationArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?status ?lastUpdatedDateTime ?creationDateTime ?defaultParameters
        ?featureTransformationArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let status = field_map json "status" Status.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let defaultParameters =
        field_map json "defaultParameters" FeaturizationParameters.of_json in
      let featureTransformationArn =
        field_map json "featureTransformationArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?status ?lastUpdatedDateTime ?creationDateTime ?defaultParameters
        ?featureTransformationArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides feature transformation information. Feature transformation is the process of modifying raw input data into a form more suitable for model training."]
module EventTracker =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the event tracker."];
      eventTrackerArn: Arn.t option
        [@ocaml.doc "The ARN of the event tracker."];
      accountId: AccountId.t option
        [@ocaml.doc
          "The Amazon Web Services account that owns the event tracker."];
      trackingId: TrackingId.t option
        [@ocaml.doc
          "The ID of the event tracker. Include this ID in requests to the PutEvents API."];
      datasetGroupArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset group that receives the event data."];
      status: Status.t option
        [@ocaml.doc
          "The status of the event tracker. An event tracker can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix format) that the event tracker was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the event tracker was last updated."]}
    let make ?name =
      fun ?eventTrackerArn ->
        fun ?accountId ->
          fun ?trackingId ->
            fun ?datasetGroupArn ->
              fun ?status ->
                fun ?creationDateTime ->
                  fun ?lastUpdatedDateTime ->
                    fun () ->
                      {
                        name;
                        eventTrackerArn;
                        accountId;
                        trackingId;
                        datasetGroupArn;
                        status;
                        creationDateTime;
                        lastUpdatedDateTime
                      }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("eventTrackerArn", (Option.map x.eventTrackerArn ~f:Arn.to_value));
        ("accountId", (Option.map x.accountId ~f:AccountId.to_value));
        ("trackingId", (Option.map x.trackingId ~f:TrackingId.to_value));
        ("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      let trackingId =
        (Option.map ~f:TrackingId.of_xml) (Xml.child xml_arg0 "trackingId") in
      let accountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "accountId") in
      let eventTrackerArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "eventTrackerArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?datasetGroupArn
        ?trackingId ?accountId ?eventTrackerArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      let trackingId = field_map json "trackingId" TrackingId.of_json in
      let accountId = field_map json "accountId" AccountId.of_json in
      let eventTrackerArn = field_map json "eventTrackerArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?datasetGroupArn
        ?trackingId ?accountId ?eventTrackerArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Provides information about an event tracker."]
module Dataset =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the dataset."];
      datasetArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset that you want metadata for."];
      datasetGroupArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the dataset group."];
      datasetType: DatasetType.t option
        [@ocaml.doc "One of the following values: Interactions Items Users"];
      schemaArn: Arn.t option
        [@ocaml.doc "The ARN of the associated schema."];
      status: Status.t option
        [@ocaml.doc
          "The status of the dataset. A dataset can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The creation date and time (in Unix time) of the dataset."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc "A time stamp that shows when the dataset was updated."]}
    let make ?name =
      fun ?datasetArn ->
        fun ?datasetGroupArn ->
          fun ?datasetType ->
            fun ?schemaArn ->
              fun ?status ->
                fun ?creationDateTime ->
                  fun ?lastUpdatedDateTime ->
                    fun () ->
                      {
                        name;
                        datasetArn;
                        datasetGroupArn;
                        datasetType;
                        schemaArn;
                        status;
                        creationDateTime;
                        lastUpdatedDateTime
                      }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("datasetArn", (Option.map x.datasetArn ~f:Arn.to_value));
        ("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("datasetType", (Option.map x.datasetType ~f:DatasetType.to_value));
        ("schemaArn", (Option.map x.schemaArn ~f:Arn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let schemaArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "schemaArn") in
      let datasetType =
        (Option.map ~f:DatasetType.of_xml) (Xml.child xml_arg0 "datasetType") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      let datasetArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?schemaArn
        ?datasetType ?datasetGroupArn ?datasetArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let schemaArn = field_map json "schemaArn" Arn.of_json in
      let datasetType = field_map json "datasetType" DatasetType.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      let datasetArn = field_map json "datasetArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?schemaArn
        ?datasetType ?datasetGroupArn ?datasetArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Provides metadata for a dataset."]
module DatasetImportJob =
  struct
    type nonrec t =
      {
      jobName: Name.t option [@ocaml.doc "The name of the import job."];
      datasetImportJobArn: Arn.t option
        [@ocaml.doc "The ARN of the dataset import job."];
      datasetArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset that receives the imported data."];
      dataSource: DataSource.t option
        [@ocaml.doc
          "The Amazon S3 bucket that contains the training data to import."];
      roleArn: Arn.t option
        [@ocaml.doc
          "The ARN of the IAM role that has permissions to read from the Amazon S3 data source."];
      status: Status.t option
        [@ocaml.doc
          "The status of the dataset import job. A dataset import job can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The creation date and time (in Unix time) of the dataset import job."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) the dataset was last updated."];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If a dataset import job fails, provides the reason why."]}
    let make ?jobName =
      fun ?datasetImportJobArn ->
        fun ?datasetArn ->
          fun ?dataSource ->
            fun ?roleArn ->
              fun ?status ->
                fun ?creationDateTime ->
                  fun ?lastUpdatedDateTime ->
                    fun ?failureReason ->
                      fun () ->
                        {
                          jobName;
                          datasetImportJobArn;
                          datasetArn;
                          dataSource;
                          roleArn;
                          status;
                          creationDateTime;
                          lastUpdatedDateTime;
                          failureReason
                        }
    let to_value x =
      structure_to_value
        [("jobName", (Option.map x.jobName ~f:Name.to_value));
        ("datasetImportJobArn",
          (Option.map x.datasetImportJobArn ~f:Arn.to_value));
        ("datasetArn", (Option.map x.datasetArn ~f:Arn.to_value));
        ("dataSource", (Option.map x.dataSource ~f:DataSource.to_value));
        ("roleArn", (Option.map x.roleArn ~f:Arn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let roleArn = (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let dataSource =
        (Option.map ~f:DataSource.of_xml) (Xml.child xml_arg0 "dataSource") in
      let datasetArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetArn") in
      let datasetImportJobArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetImportJobArn") in
      let jobName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "jobName") in
      make ?failureReason ?lastUpdatedDateTime ?creationDateTime ?status
        ?roleArn ?dataSource ?datasetArn ?datasetImportJobArn ?jobName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let roleArn = field_map json "roleArn" Arn.of_json in
      let dataSource = field_map json "dataSource" DataSource.of_json in
      let datasetArn = field_map json "datasetArn" Arn.of_json in
      let datasetImportJobArn =
        field_map json "datasetImportJobArn" Arn.of_json in
      let jobName = field_map json "jobName" Name.of_json in
      make ?failureReason ?lastUpdatedDateTime ?creationDateTime ?status
        ?roleArn ?dataSource ?datasetArn ?datasetImportJobArn ?jobName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a job that imports training data from a data source (Amazon S3 bucket) to an Amazon Personalize dataset. For more information, see CreateDatasetImportJob. A dataset import job can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED"]
module DatasetGroup =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the dataset group."];
      datasetGroupArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the dataset group."];
      status: Status.t option
        [@ocaml.doc
          "The current status of the dataset group. A dataset group can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING"];
      roleArn: RoleArn.t option
        [@ocaml.doc
          "The ARN of the IAM role that has permissions to create the dataset group."];
      kmsKeyArn: KmsKeyArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Key Management Service (KMS) key used to encrypt the datasets."];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The creation date and time (in Unix time) of the dataset group."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The last update date and time (in Unix time) of the dataset group."];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If creating a dataset group fails, provides the reason why."];
      domain: Domain.t option
        [@ocaml.doc "The domain of a Domain dataset group."]}
    let make ?name =
      fun ?datasetGroupArn ->
        fun ?status ->
          fun ?roleArn ->
            fun ?kmsKeyArn ->
              fun ?creationDateTime ->
                fun ?lastUpdatedDateTime ->
                  fun ?failureReason ->
                    fun ?domain ->
                      fun () ->
                        {
                          name;
                          datasetGroupArn;
                          status;
                          roleArn;
                          kmsKeyArn;
                          creationDateTime;
                          lastUpdatedDateTime;
                          failureReason;
                          domain
                        }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("roleArn", (Option.map x.roleArn ~f:RoleArn.to_value));
        ("kmsKeyArn", (Option.map x.kmsKeyArn ~f:KmsKeyArn.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("domain", (Option.map x.domain ~f:Domain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        (Option.map ~f:Domain.of_xml) (Xml.child xml_arg0 "domain") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let kmsKeyArn =
        (Option.map ~f:KmsKeyArn.of_xml) (Xml.child xml_arg0 "kmsKeyArn") in
      let roleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?domain ?failureReason ?lastUpdatedDateTime ?creationDateTime
        ?kmsKeyArn ?roleArn ?status ?datasetGroupArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let domain = field_map json "domain" Domain.of_json in
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let kmsKeyArn = field_map json "kmsKeyArn" KmsKeyArn.of_json in
      let roleArn = field_map json "roleArn" RoleArn.of_json in
      let status = field_map json "status" Status.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?domain ?failureReason ?lastUpdatedDateTime ?creationDateTime
        ?kmsKeyArn ?roleArn ?status ?datasetGroupArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A dataset group is a collection of related datasets (Interactions, User, and Item). You create a dataset group by calling CreateDatasetGroup. You then create a dataset and add it to a dataset group by calling CreateDataset. The dataset group is used to create and train a solution by calling CreateSolution. A dataset group can contain only one of each type of dataset. You can specify an Key Management Service (KMS) key to encrypt the datasets in the group."]
module DatasetExportJob =
  struct
    type nonrec t =
      {
      jobName: Name.t option [@ocaml.doc "The name of the export job."];
      datasetExportJobArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset export job."];
      datasetArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset to export."];
      ingestionMode: IngestionMode.t option
        [@ocaml.doc
          "The data to export, based on how you imported the data. You can choose to export BULK data that you imported using a dataset import job, PUT data that you imported incrementally (using the console, PutEvents, PutUsers and PutItems operations), or ALL for both types. The default value is PUT."];
      roleArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM service role that has permissions to add data to your output Amazon S3 bucket."];
      status: Status.t option
        [@ocaml.doc
          "The status of the dataset export job. A dataset export job can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED"];
      jobOutput: DatasetExportJobOutput.t option
        [@ocaml.doc
          "The path to the Amazon S3 bucket where the job's output is stored. For example: s3://bucket-name/folder-name/"];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The creation date and time (in Unix time) of the dataset export job."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) the status of the dataset export job was last updated."];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If a dataset export job fails, provides the reason why."]}
    let make ?jobName =
      fun ?datasetExportJobArn ->
        fun ?datasetArn ->
          fun ?ingestionMode ->
            fun ?roleArn ->
              fun ?status ->
                fun ?jobOutput ->
                  fun ?creationDateTime ->
                    fun ?lastUpdatedDateTime ->
                      fun ?failureReason ->
                        fun () ->
                          {
                            jobName;
                            datasetExportJobArn;
                            datasetArn;
                            ingestionMode;
                            roleArn;
                            status;
                            jobOutput;
                            creationDateTime;
                            lastUpdatedDateTime;
                            failureReason
                          }
    let to_value x =
      structure_to_value
        [("jobName", (Option.map x.jobName ~f:Name.to_value));
        ("datasetExportJobArn",
          (Option.map x.datasetExportJobArn ~f:Arn.to_value));
        ("datasetArn", (Option.map x.datasetArn ~f:Arn.to_value));
        ("ingestionMode",
          (Option.map x.ingestionMode ~f:IngestionMode.to_value));
        ("roleArn", (Option.map x.roleArn ~f:Arn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("jobOutput",
          (Option.map x.jobOutput ~f:DatasetExportJobOutput.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let jobOutput =
        (Option.map ~f:DatasetExportJobOutput.of_xml)
          (Xml.child xml_arg0 "jobOutput") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let roleArn = (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let ingestionMode =
        (Option.map ~f:IngestionMode.of_xml)
          (Xml.child xml_arg0 "ingestionMode") in
      let datasetArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetArn") in
      let datasetExportJobArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetExportJobArn") in
      let jobName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "jobName") in
      make ?failureReason ?lastUpdatedDateTime ?creationDateTime ?jobOutput
        ?status ?roleArn ?ingestionMode ?datasetArn ?datasetExportJobArn
        ?jobName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let jobOutput =
        field_map json "jobOutput" DatasetExportJobOutput.of_json in
      let status = field_map json "status" Status.of_json in
      let roleArn = field_map json "roleArn" Arn.of_json in
      let ingestionMode =
        field_map json "ingestionMode" IngestionMode.of_json in
      let datasetArn = field_map json "datasetArn" Arn.of_json in
      let datasetExportJobArn =
        field_map json "datasetExportJobArn" Arn.of_json in
      let jobName = field_map json "jobName" Name.of_json in
      make ?failureReason ?lastUpdatedDateTime ?creationDateTime ?jobOutput
        ?status ?roleArn ?ingestionMode ?datasetArn ?datasetExportJobArn
        ?jobName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a job that exports a dataset to an Amazon S3 bucket. For more information, see CreateDatasetExportJob. A dataset export job can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED"]
module Campaign =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the campaign."];
      campaignArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the campaign."];
      solutionVersionArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of a specific version of the solution."];
      minProvisionedTPS: TransactionsPerSecond.t option
        [@ocaml.doc
          "Specifies the requested minimum provisioned transactions (recommendations) per second."];
      campaignConfig: CampaignConfig.t option
        [@ocaml.doc "The configuration details of a campaign."];
      status: Status.t option
        [@ocaml.doc
          "The status of the campaign. A campaign can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS"];
      failureReason: FailureReason.t option
        [@ocaml.doc "If a campaign fails, the reason behind the failure."];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix format) that the campaign was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix format) that the campaign was last updated."];
      latestCampaignUpdate: CampaignUpdateSummary.t option }
    let make ?name =
      fun ?campaignArn ->
        fun ?solutionVersionArn ->
          fun ?minProvisionedTPS ->
            fun ?campaignConfig ->
              fun ?status ->
                fun ?failureReason ->
                  fun ?creationDateTime ->
                    fun ?lastUpdatedDateTime ->
                      fun ?latestCampaignUpdate ->
                        fun () ->
                          {
                            name;
                            campaignArn;
                            solutionVersionArn;
                            minProvisionedTPS;
                            campaignConfig;
                            status;
                            failureReason;
                            creationDateTime;
                            lastUpdatedDateTime;
                            latestCampaignUpdate
                          }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("campaignArn", (Option.map x.campaignArn ~f:Arn.to_value));
        ("solutionVersionArn",
          (Option.map x.solutionVersionArn ~f:Arn.to_value));
        ("minProvisionedTPS",
          (Option.map x.minProvisionedTPS ~f:TransactionsPerSecond.to_value));
        ("campaignConfig",
          (Option.map x.campaignConfig ~f:CampaignConfig.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value));
        ("latestCampaignUpdate",
          (Option.map x.latestCampaignUpdate
             ~f:CampaignUpdateSummary.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let latestCampaignUpdate =
        (Option.map ~f:CampaignUpdateSummary.of_xml)
          (Xml.child xml_arg0 "latestCampaignUpdate") in
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let campaignConfig =
        (Option.map ~f:CampaignConfig.of_xml)
          (Xml.child xml_arg0 "campaignConfig") in
      let minProvisionedTPS =
        (Option.map ~f:TransactionsPerSecond.of_xml)
          (Xml.child xml_arg0 "minProvisionedTPS") in
      let solutionVersionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionVersionArn") in
      let campaignArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "campaignArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?latestCampaignUpdate ?lastUpdatedDateTime ?creationDateTime
        ?failureReason ?status ?campaignConfig ?minProvisionedTPS
        ?solutionVersionArn ?campaignArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let latestCampaignUpdate =
        field_map json "latestCampaignUpdate" CampaignUpdateSummary.of_json in
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let status = field_map json "status" Status.of_json in
      let campaignConfig =
        field_map json "campaignConfig" CampaignConfig.of_json in
      let minProvisionedTPS =
        field_map json "minProvisionedTPS" TransactionsPerSecond.of_json in
      let solutionVersionArn =
        field_map json "solutionVersionArn" Arn.of_json in
      let campaignArn = field_map json "campaignArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?latestCampaignUpdate ?lastUpdatedDateTime ?creationDateTime
        ?failureReason ?status ?campaignConfig ?minProvisionedTPS
        ?solutionVersionArn ?campaignArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object that describes the deployment of a solution version. For more information on campaigns, see CreateCampaign."]
module BatchSegmentJob =
  struct
    type nonrec t =
      {
      jobName: Name.t option
        [@ocaml.doc "The name of the batch segment job."];
      batchSegmentJobArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the batch segment job."];
      filterArn: Arn.t option
        [@ocaml.doc "The ARN of the filter used on the batch segment job."];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If the batch segment job failed, the reason for the failure."];
      solutionVersionArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution version used by the batch segment job to generate batch segments."];
      numResults: NumBatchResults.t option
        [@ocaml.doc
          "The number of predicted users generated by the batch segment job for each line of input data."];
      jobInput: BatchSegmentJobInput.t option
        [@ocaml.doc
          "The Amazon S3 path that leads to the input data used to generate the batch segment job."];
      jobOutput: BatchSegmentJobOutput.t option
        [@ocaml.doc
          "The Amazon S3 bucket that contains the output data generated by the batch segment job."];
      roleArn: RoleArn.t option
        [@ocaml.doc
          "The ARN of the Amazon Identity and Access Management (IAM) role that requested the batch segment job."];
      status: Status.t option
        [@ocaml.doc
          "The status of the batch segment job. The status is one of the following values: PENDING IN PROGRESS ACTIVE CREATE FAILED"];
      creationDateTime: Date.t option
        [@ocaml.doc "The time at which the batch segment job was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc "The time at which the batch segment job last updated."]}
    let make ?jobName =
      fun ?batchSegmentJobArn ->
        fun ?filterArn ->
          fun ?failureReason ->
            fun ?solutionVersionArn ->
              fun ?numResults ->
                fun ?jobInput ->
                  fun ?jobOutput ->
                    fun ?roleArn ->
                      fun ?status ->
                        fun ?creationDateTime ->
                          fun ?lastUpdatedDateTime ->
                            fun () ->
                              {
                                jobName;
                                batchSegmentJobArn;
                                filterArn;
                                failureReason;
                                solutionVersionArn;
                                numResults;
                                jobInput;
                                jobOutput;
                                roleArn;
                                status;
                                creationDateTime;
                                lastUpdatedDateTime
                              }
    let to_value x =
      structure_to_value
        [("jobName", (Option.map x.jobName ~f:Name.to_value));
        ("batchSegmentJobArn",
          (Option.map x.batchSegmentJobArn ~f:Arn.to_value));
        ("filterArn", (Option.map x.filterArn ~f:Arn.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("solutionVersionArn",
          (Option.map x.solutionVersionArn ~f:Arn.to_value));
        ("numResults", (Option.map x.numResults ~f:NumBatchResults.to_value));
        ("jobInput",
          (Option.map x.jobInput ~f:BatchSegmentJobInput.to_value));
        ("jobOutput",
          (Option.map x.jobOutput ~f:BatchSegmentJobOutput.to_value));
        ("roleArn", (Option.map x.roleArn ~f:RoleArn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let roleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let jobOutput =
        (Option.map ~f:BatchSegmentJobOutput.of_xml)
          (Xml.child xml_arg0 "jobOutput") in
      let jobInput =
        (Option.map ~f:BatchSegmentJobInput.of_xml)
          (Xml.child xml_arg0 "jobInput") in
      let numResults =
        (Option.map ~f:NumBatchResults.of_xml)
          (Xml.child xml_arg0 "numResults") in
      let solutionVersionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionVersionArn") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let filterArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "filterArn") in
      let batchSegmentJobArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "batchSegmentJobArn") in
      let jobName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "jobName") in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?roleArn ?jobOutput
        ?jobInput ?numResults ?solutionVersionArn ?failureReason ?filterArn
        ?batchSegmentJobArn ?jobName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let roleArn = field_map json "roleArn" RoleArn.of_json in
      let jobOutput =
        field_map json "jobOutput" BatchSegmentJobOutput.of_json in
      let jobInput = field_map json "jobInput" BatchSegmentJobInput.of_json in
      let numResults = field_map json "numResults" NumBatchResults.of_json in
      let solutionVersionArn =
        field_map json "solutionVersionArn" Arn.of_json in
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let filterArn = field_map json "filterArn" Arn.of_json in
      let batchSegmentJobArn =
        field_map json "batchSegmentJobArn" Arn.of_json in
      let jobName = field_map json "jobName" Name.of_json in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?roleArn ?jobOutput
        ?jobInput ?numResults ?solutionVersionArn ?failureReason ?filterArn
        ?batchSegmentJobArn ?jobName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information on a batch segment job."]
module BatchInferenceJob =
  struct
    type nonrec t =
      {
      jobName: Name.t option
        [@ocaml.doc "The name of the batch inference job."];
      batchInferenceJobArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the batch inference job."];
      filterArn: Arn.t option
        [@ocaml.doc "The ARN of the filter used on the batch inference job."];
      failureReason: FailureReason.t option
        [@ocaml.doc
          "If the batch inference job failed, the reason for the failure."];
      solutionVersionArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution version from which the batch inference job was created."];
      numResults: NumBatchResults.t option
        [@ocaml.doc
          "The number of recommendations generated by the batch inference job. This number includes the error messages generated for failed input records."];
      jobInput: BatchInferenceJobInput.t option
        [@ocaml.doc
          "The Amazon S3 path that leads to the input data used to generate the batch inference job."];
      jobOutput: BatchInferenceJobOutput.t option
        [@ocaml.doc
          "The Amazon S3 bucket that contains the output data generated by the batch inference job."];
      batchInferenceJobConfig: BatchInferenceJobConfig.t option
        [@ocaml.doc
          "A string to string map of the configuration details of a batch inference job."];
      roleArn: RoleArn.t option
        [@ocaml.doc
          "The ARN of the Amazon Identity and Access Management (IAM) role that requested the batch inference job."];
      status: Status.t option
        [@ocaml.doc
          "The status of the batch inference job. The status is one of the following values: PENDING IN PROGRESS ACTIVE CREATE FAILED"];
      creationDateTime: Date.t option
        [@ocaml.doc "The time at which the batch inference job was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The time at which the batch inference job was last updated."]}
    let make ?jobName =
      fun ?batchInferenceJobArn ->
        fun ?filterArn ->
          fun ?failureReason ->
            fun ?solutionVersionArn ->
              fun ?numResults ->
                fun ?jobInput ->
                  fun ?jobOutput ->
                    fun ?batchInferenceJobConfig ->
                      fun ?roleArn ->
                        fun ?status ->
                          fun ?creationDateTime ->
                            fun ?lastUpdatedDateTime ->
                              fun () ->
                                {
                                  jobName;
                                  batchInferenceJobArn;
                                  filterArn;
                                  failureReason;
                                  solutionVersionArn;
                                  numResults;
                                  jobInput;
                                  jobOutput;
                                  batchInferenceJobConfig;
                                  roleArn;
                                  status;
                                  creationDateTime;
                                  lastUpdatedDateTime
                                }
    let to_value x =
      structure_to_value
        [("jobName", (Option.map x.jobName ~f:Name.to_value));
        ("batchInferenceJobArn",
          (Option.map x.batchInferenceJobArn ~f:Arn.to_value));
        ("filterArn", (Option.map x.filterArn ~f:Arn.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReason.to_value));
        ("solutionVersionArn",
          (Option.map x.solutionVersionArn ~f:Arn.to_value));
        ("numResults", (Option.map x.numResults ~f:NumBatchResults.to_value));
        ("jobInput",
          (Option.map x.jobInput ~f:BatchInferenceJobInput.to_value));
        ("jobOutput",
          (Option.map x.jobOutput ~f:BatchInferenceJobOutput.to_value));
        ("batchInferenceJobConfig",
          (Option.map x.batchInferenceJobConfig
             ~f:BatchInferenceJobConfig.to_value));
        ("roleArn", (Option.map x.roleArn ~f:RoleArn.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let roleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let batchInferenceJobConfig =
        (Option.map ~f:BatchInferenceJobConfig.of_xml)
          (Xml.child xml_arg0 "batchInferenceJobConfig") in
      let jobOutput =
        (Option.map ~f:BatchInferenceJobOutput.of_xml)
          (Xml.child xml_arg0 "jobOutput") in
      let jobInput =
        (Option.map ~f:BatchInferenceJobInput.of_xml)
          (Xml.child xml_arg0 "jobInput") in
      let numResults =
        (Option.map ~f:NumBatchResults.of_xml)
          (Xml.child xml_arg0 "numResults") in
      let solutionVersionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionVersionArn") in
      let failureReason =
        (Option.map ~f:FailureReason.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let filterArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "filterArn") in
      let batchInferenceJobArn =
        (Option.map ~f:Arn.of_xml)
          (Xml.child xml_arg0 "batchInferenceJobArn") in
      let jobName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "jobName") in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?roleArn
        ?batchInferenceJobConfig ?jobOutput ?jobInput ?numResults
        ?solutionVersionArn ?failureReason ?filterArn ?batchInferenceJobArn
        ?jobName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let status = field_map json "status" Status.of_json in
      let roleArn = field_map json "roleArn" RoleArn.of_json in
      let batchInferenceJobConfig =
        field_map json "batchInferenceJobConfig"
          BatchInferenceJobConfig.of_json in
      let jobOutput =
        field_map json "jobOutput" BatchInferenceJobOutput.of_json in
      let jobInput = field_map json "jobInput" BatchInferenceJobInput.of_json in
      let numResults = field_map json "numResults" NumBatchResults.of_json in
      let solutionVersionArn =
        field_map json "solutionVersionArn" Arn.of_json in
      let failureReason =
        field_map json "failureReason" FailureReason.of_json in
      let filterArn = field_map json "filterArn" Arn.of_json in
      let batchInferenceJobArn =
        field_map json "batchInferenceJobArn" Arn.of_json in
      let jobName = field_map json "jobName" Name.of_json in
      make ?lastUpdatedDateTime ?creationDateTime ?status ?roleArn
        ?batchInferenceJobConfig ?jobOutput ?jobInput ?numResults
        ?solutionVersionArn ?failureReason ?filterArn ?batchInferenceJobArn
        ?jobName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information on a batch inference job."]
module Algorithm =
  struct
    type nonrec t =
      {
      name: Name.t option [@ocaml.doc "The name of the algorithm."];
      algorithmArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the algorithm."];
      algorithmImage: AlgorithmImage.t option
        [@ocaml.doc
          "The URI of the Docker container for the algorithm image."];
      defaultHyperParameters: HyperParameters.t option
        [@ocaml.doc "Specifies the default hyperparameters."];
      defaultHyperParameterRanges: DefaultHyperParameterRanges.t option
        [@ocaml.doc
          "Specifies the default hyperparameters, their ranges, and whether they are tunable. A tunable hyperparameter can have its value determined during hyperparameter optimization (HPO)."];
      defaultResourceConfig: ResourceConfig.t option
        [@ocaml.doc
          "Specifies the default maximum number of training jobs and parallel training jobs."];
      trainingInputMode: TrainingInputMode.t option
        [@ocaml.doc "The training input mode."];
      roleArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the role."];
      creationDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the algorithm was created."];
      lastUpdatedDateTime: Date.t option
        [@ocaml.doc
          "The date and time (in Unix time) that the algorithm was last updated."]}
    let make ?name =
      fun ?algorithmArn ->
        fun ?algorithmImage ->
          fun ?defaultHyperParameters ->
            fun ?defaultHyperParameterRanges ->
              fun ?defaultResourceConfig ->
                fun ?trainingInputMode ->
                  fun ?roleArn ->
                    fun ?creationDateTime ->
                      fun ?lastUpdatedDateTime ->
                        fun () ->
                          {
                            name;
                            algorithmArn;
                            algorithmImage;
                            defaultHyperParameters;
                            defaultHyperParameterRanges;
                            defaultResourceConfig;
                            trainingInputMode;
                            roleArn;
                            creationDateTime;
                            lastUpdatedDateTime
                          }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:Name.to_value));
        ("algorithmArn", (Option.map x.algorithmArn ~f:Arn.to_value));
        ("algorithmImage",
          (Option.map x.algorithmImage ~f:AlgorithmImage.to_value));
        ("defaultHyperParameters",
          (Option.map x.defaultHyperParameters ~f:HyperParameters.to_value));
        ("defaultHyperParameterRanges",
          (Option.map x.defaultHyperParameterRanges
             ~f:DefaultHyperParameterRanges.to_value));
        ("defaultResourceConfig",
          (Option.map x.defaultResourceConfig ~f:ResourceConfig.to_value));
        ("trainingInputMode",
          (Option.map x.trainingInputMode ~f:TrainingInputMode.to_value));
        ("roleArn", (Option.map x.roleArn ~f:Arn.to_value));
        ("creationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value));
        ("lastUpdatedDateTime",
          (Option.map x.lastUpdatedDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedDateTime =
        (Option.map ~f:Date.of_xml)
          (Xml.child xml_arg0 "lastUpdatedDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "creationDateTime") in
      let roleArn = (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let trainingInputMode =
        (Option.map ~f:TrainingInputMode.of_xml)
          (Xml.child xml_arg0 "trainingInputMode") in
      let defaultResourceConfig =
        (Option.map ~f:ResourceConfig.of_xml)
          (Xml.child xml_arg0 "defaultResourceConfig") in
      let defaultHyperParameterRanges =
        (Option.map ~f:DefaultHyperParameterRanges.of_xml)
          (Xml.child xml_arg0 "defaultHyperParameterRanges") in
      let defaultHyperParameters =
        (Option.map ~f:HyperParameters.of_xml)
          (Xml.child xml_arg0 "defaultHyperParameters") in
      let algorithmImage =
        (Option.map ~f:AlgorithmImage.of_xml)
          (Xml.child xml_arg0 "algorithmImage") in
      let algorithmArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "algorithmArn") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      make ?lastUpdatedDateTime ?creationDateTime ?roleArn ?trainingInputMode
        ?defaultResourceConfig ?defaultHyperParameterRanges
        ?defaultHyperParameters ?algorithmImage ?algorithmArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastUpdatedDateTime =
        field_map json "lastUpdatedDateTime" Date.of_json in
      let creationDateTime = field_map json "creationDateTime" Date.of_json in
      let roleArn = field_map json "roleArn" Arn.of_json in
      let trainingInputMode =
        field_map json "trainingInputMode" TrainingInputMode.of_json in
      let defaultResourceConfig =
        field_map json "defaultResourceConfig" ResourceConfig.of_json in
      let defaultHyperParameterRanges =
        field_map json "defaultHyperParameterRanges"
          DefaultHyperParameterRanges.of_json in
      let defaultHyperParameters =
        field_map json "defaultHyperParameters" HyperParameters.of_json in
      let algorithmImage =
        field_map json "algorithmImage" AlgorithmImage.of_json in
      let algorithmArn = field_map json "algorithmArn" Arn.of_json in
      let name = field_map json "name" Name.of_json in
      make ?lastUpdatedDateTime ?creationDateTime ?roleArn ?trainingInputMode
        ?defaultResourceConfig ?defaultHyperParameterRanges
        ?defaultHyperParameters ?algorithmImage ?algorithmArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a custom algorithm."]
module ResourceAlreadyExistsException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified resource already exists."]
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module UpdateRecommenderResponse =
  struct
    type nonrec t =
      {
      recommenderArn: Arn.t option
        [@ocaml.doc
          "The same recommender Amazon Resource Name (ARN) as given in the request."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recommenderArn = fun () -> { recommenderArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("recommenderArn", (Option.map x.recommenderArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recommenderArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "recommenderArn") in
      make ?recommenderArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recommenderArn = field_map json "recommenderArn" Arn.of_json in
      make ?recommenderArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the recommender to modify the recommender configuration."]
module UpdateRecommenderRequest =
  struct
    type nonrec t =
      {
      recommenderArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the recommender to modify."];
      recommenderConfig: RecommenderConfig.t
        [@ocaml.doc "The configuration details of the recommender."]}
    let context_ = "UpdateRecommenderRequest"
    let make ~recommenderArn =
      fun ~recommenderConfig ->
        fun () -> { recommenderArn; recommenderConfig }
    let to_value x =
      structure_to_value
        [("recommenderArn", (Some (Arn.to_value x.recommenderArn)));
        ("recommenderConfig",
          (Some (RecommenderConfig.to_value x.recommenderConfig)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recommenderConfig =
        RecommenderConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "recommenderConfig") in
      let recommenderArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "recommenderArn") in
      make ~recommenderConfig ~recommenderArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recommenderConfig =
        field_map_exn json "recommenderConfig" RecommenderConfig.of_json in
      let recommenderArn = field_map_exn json "recommenderArn" Arn.of_json in
      make ~recommenderConfig ~recommenderArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the recommender to modify the recommender configuration."]
module UpdateCampaignResponse =
  struct
    type nonrec t =
      {
      campaignArn: Arn.t option
        [@ocaml.doc "The same campaign ARN as given in the request."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?campaignArn = fun () -> { campaignArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("campaignArn", (Option.map x.campaignArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let campaignArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "campaignArn") in
      make ?campaignArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let campaignArn = field_map json "campaignArn" Arn.of_json in
      make ?campaignArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates a campaign by either deploying a new solution or changing the value of the campaign's minProvisionedTPS parameter. To update a campaign, the campaign status must be ACTIVE or CREATE FAILED. Check the campaign status using the DescribeCampaign operation. You must wait until the status of the updated campaign is ACTIVE before asking the campaign for recommendations. For more information on campaigns, see CreateCampaign."]
module UpdateCampaignRequest =
  struct
    type nonrec t =
      {
      campaignArn: Arn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the campaign."];
      solutionVersionArn: Arn.t option
        [@ocaml.doc "The ARN of a new solution version to deploy."];
      minProvisionedTPS: TransactionsPerSecond.t option
        [@ocaml.doc
          "Specifies the requested minimum provisioned transactions (recommendations) per second that Amazon Personalize will support."];
      campaignConfig: CampaignConfig.t option
        [@ocaml.doc "The configuration details of a campaign."]}
    let context_ = "UpdateCampaignRequest"
    let make ?solutionVersionArn =
      fun ?minProvisionedTPS ->
        fun ?campaignConfig ->
          fun ~campaignArn ->
            fun () ->
              {
                solutionVersionArn;
                minProvisionedTPS;
                campaignConfig;
                campaignArn
              }
    let to_value x =
      structure_to_value
        [("campaignArn", (Some (Arn.to_value x.campaignArn)));
        ("solutionVersionArn",
          (Option.map x.solutionVersionArn ~f:Arn.to_value));
        ("minProvisionedTPS",
          (Option.map x.minProvisionedTPS ~f:TransactionsPerSecond.to_value));
        ("campaignConfig",
          (Option.map x.campaignConfig ~f:CampaignConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let campaignConfig =
        (Option.map ~f:CampaignConfig.of_xml)
          (Xml.child xml_arg0 "campaignConfig") in
      let minProvisionedTPS =
        (Option.map ~f:TransactionsPerSecond.of_xml)
          (Xml.child xml_arg0 "minProvisionedTPS") in
      let solutionVersionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionVersionArn") in
      let campaignArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "campaignArn") in
      make ?campaignConfig ?minProvisionedTPS ?solutionVersionArn
        ~campaignArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let campaignConfig =
        field_map json "campaignConfig" CampaignConfig.of_json in
      let minProvisionedTPS =
        field_map json "minProvisionedTPS" TransactionsPerSecond.of_json in
      let solutionVersionArn =
        field_map json "solutionVersionArn" Arn.of_json in
      let campaignArn = field_map_exn json "campaignArn" Arn.of_json in
      make ?campaignConfig ?minProvisionedTPS ?solutionVersionArn
        ~campaignArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates a campaign by either deploying a new solution or changing the value of the campaign's minProvisionedTPS parameter. To update a campaign, the campaign status must be ACTIVE or CREATE FAILED. Check the campaign status using the DescribeCampaign operation. You must wait until the status of the updated campaign is ACTIVE before asking the campaign for recommendations. For more information on campaigns, see CreateCampaign."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagKeysException of TooManyTagKeysException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagKeysException" ->
          `TooManyTagKeysException (TooManyTagKeysException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagKeysException" ->
          `TooManyTagKeysException (TooManyTagKeysException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagKeysException e ->
          `Assoc
            [("error", (`String "TooManyTagKeysException"));
            ("details", (TooManyTagKeysException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Remove tags that are attached to a resource."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: Arn.t
        [@ocaml.doc "The resource's Amazon Resource Name (ARN)."];
      tagKeys: TagKeys.t
        [@ocaml.doc "Keys to remove from the resource's tags."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeys.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeys.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tagKeys = field_map_exn json "tagKeys" TagKeys.of_json in
      let resourceArn = field_map_exn json "resourceArn" Arn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Remove tags that are attached to a resource."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Add a list of tags to a resource."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: Arn.t
        [@ocaml.doc "The resource's Amazon Resource Name (ARN)."];
      tags: Tags.t
        [@ocaml.doc
          "Tags to apply to the resource. For more information see Tagging Personalize resources."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)));
        ("tags", (Some (Tags.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        Tags.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map_exn json "tags" Tags.of_json in
      let resourceArn = field_map_exn json "resourceArn" Arn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Add a list of tags to a resource."]
module StopSolutionVersionCreationRequest =
  struct
    type nonrec t =
      {
      solutionVersionArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution version you want to stop creating."]}
    let context_ = "StopSolutionVersionCreationRequest"
    let make ~solutionVersionArn = fun () -> { solutionVersionArn }
    let to_value x =
      structure_to_value
        [("solutionVersionArn", (Some (Arn.to_value x.solutionVersionArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let solutionVersionArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "solutionVersionArn") in
      make ~solutionVersionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let solutionVersionArn =
        field_map_exn json "solutionVersionArn" Arn.of_json in
      make ~solutionVersionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stops creating a solution version that is in a state of CREATE_PENDING or CREATE IN_PROGRESS. Depending on the current state of the solution version, the solution version state changes as follows: CREATE_PENDING > CREATE_STOPPED or CREATE_IN_PROGRESS > CREATE_STOPPING > CREATE_STOPPED You are billed for all of the training completed up until you stop the solution version creation. You cannot resume creating a solution version once it has been stopped."]
module ListTagsForResourceResponse =
  struct
    type nonrec t = {
      tags: Tags.t option [@ocaml.doc "The resource's tags."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get a list of tags attached to a resource."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: Arn.t [@ocaml.doc "The resource's Amazon Resource Name."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (Arn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceArn = field_map_exn json "resourceArn" Arn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get a list of tags attached to a resource."]
module ListSolutionsResponse =
  struct
    type nonrec t =
      {
      solutions: Solutions.t option
        [@ocaml.doc "A list of the current solutions."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token for getting the next set of solutions (if they exist)."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `InvalidNextTokenException of InvalidNextTokenException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?solutions =
      fun ?nextToken -> fun () -> { solutions; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("solutions", (Option.map x.solutions ~f:Solutions.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let solutions =
        (Option.map ~f:Solutions.of_xml) (Xml.child xml_arg0 "solutions") in
      make ?nextToken ?solutions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let solutions = field_map json "solutions" Solutions.of_json in
      make ?nextToken ?solutions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of solutions that use the given dataset group. When a dataset group is not specified, all the solutions associated with the account are listed. The response provides the properties for each solution, including the Amazon Resource Name (ARN). For more information on solutions, see CreateSolution."]
module ListSolutionsRequest =
  struct
    type nonrec t =
      {
      datasetGroupArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the dataset group."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from the previous call to ListSolutions for getting the next set of solutions (if they exist)."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of solutions to return."]}
    let make ?datasetGroupArn =
      fun ?nextToken ->
        fun ?maxResults ->
          fun () -> { datasetGroupArn; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      make ?maxResults ?nextToken ?datasetGroupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      make ?maxResults ?nextToken ?datasetGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of solutions that use the given dataset group. When a dataset group is not specified, all the solutions associated with the account are listed. The response provides the properties for each solution, including the Amazon Resource Name (ARN). For more information on solutions, see CreateSolution."]
module ListSolutionVersionsResponse =
  struct
    type nonrec t =
      {
      solutionVersions: SolutionVersions.t option
        [@ocaml.doc
          "A list of solution versions describing the version properties."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token for getting the next set of solution versions (if they exist)."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `InvalidNextTokenException of InvalidNextTokenException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?solutionVersions =
      fun ?nextToken -> fun () -> { solutionVersions; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("solutionVersions",
           (Option.map x.solutionVersions ~f:SolutionVersions.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let solutionVersions =
        (Option.map ~f:SolutionVersions.of_xml)
          (Xml.child xml_arg0 "solutionVersions") in
      make ?nextToken ?solutionVersions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let solutionVersions =
        field_map json "solutionVersions" SolutionVersions.of_json in
      make ?nextToken ?solutionVersions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of solution versions for the given solution. When a solution is not specified, all the solution versions associated with the account are listed. The response provides the properties for each solution version, including the Amazon Resource Name (ARN)."]
module ListSolutionVersionsRequest =
  struct
    type nonrec t =
      {
      solutionArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the solution."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from the previous call to ListSolutionVersions for getting the next set of solution versions (if they exist)."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of solution versions to return."]}
    let make ?solutionArn =
      fun ?nextToken ->
        fun ?maxResults -> fun () -> { solutionArn; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("solutionArn", (Option.map x.solutionArn ~f:Arn.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let solutionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionArn") in
      make ?maxResults ?nextToken ?solutionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let solutionArn = field_map json "solutionArn" Arn.of_json in
      make ?maxResults ?nextToken ?solutionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of solution versions for the given solution. When a solution is not specified, all the solution versions associated with the account are listed. The response provides the properties for each solution version, including the Amazon Resource Name (ARN)."]
module ListSchemasResponse =
  struct
    type nonrec t =
      {
      schemas: Schemas.t option [@ocaml.doc "A list of schemas."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token used to get the next set of schemas (if they exist)."]}
    type nonrec error =
      [ `InvalidNextTokenException of InvalidNextTokenException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?schemas = fun ?nextToken -> fun () -> { schemas; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("schemas", (Option.map x.schemas ~f:Schemas.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let schemas =
        (Option.map ~f:Schemas.of_xml) (Xml.child xml_arg0 "schemas") in
      make ?nextToken ?schemas ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let schemas = field_map json "schemas" Schemas.of_json in
      make ?nextToken ?schemas ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the list of schemas associated with the account. The response provides the properties for each schema, including the Amazon Resource Name (ARN). For more information on schemas, see CreateSchema."]
module ListSchemasRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from the previous call to ListSchemas for getting the next set of schemas (if they exist)."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of schemas to return."]}
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the list of schemas associated with the account. The response provides the properties for each schema, including the Amazon Resource Name (ARN). For more information on schemas, see CreateSchema."]
module ListRecommendersResponse =
  struct
    type nonrec t =
      {
      recommenders: Recommenders.t option
        [@ocaml.doc "A list of the recommenders."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token for getting the next set of recommenders (if they exist)."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `InvalidNextTokenException of InvalidNextTokenException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recommenders =
      fun ?nextToken -> fun () -> { recommenders; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("recommenders",
           (Option.map x.recommenders ~f:Recommenders.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let recommenders =
        (Option.map ~f:Recommenders.of_xml)
          (Xml.child xml_arg0 "recommenders") in
      make ?nextToken ?recommenders ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let recommenders = field_map json "recommenders" Recommenders.of_json in
      make ?nextToken ?recommenders ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of recommenders in a given Domain dataset group. When a Domain dataset group is not specified, all the recommenders associated with the account are listed. The response provides the properties for each recommender, including the Amazon Resource Name (ARN). For more information on recommenders, see CreateRecommender."]
module ListRecommendersRequest =
  struct
    type nonrec t =
      {
      datasetGroupArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Domain dataset group to list the recommenders for. When a Domain dataset group is not specified, all the recommenders associated with the account are listed."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from the previous call to ListRecommenders for getting the next set of recommenders (if they exist)."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of recommenders to return."]}
    let make ?datasetGroupArn =
      fun ?nextToken ->
        fun ?maxResults ->
          fun () -> { datasetGroupArn; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      make ?maxResults ?nextToken ?datasetGroupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      make ?maxResults ?nextToken ?datasetGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of recommenders in a given Domain dataset group. When a Domain dataset group is not specified, all the recommenders associated with the account are listed. The response provides the properties for each recommender, including the Amazon Resource Name (ARN). For more information on recommenders, see CreateRecommender."]
module ListRecipesResponse =
  struct
    type nonrec t =
      {
      recipes: Recipes.t option [@ocaml.doc "The list of available recipes."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token for getting the next set of recipes."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `InvalidNextTokenException of InvalidNextTokenException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recipes = fun ?nextToken -> fun () -> { recipes; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("recipes", (Option.map x.recipes ~f:Recipes.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let recipes =
        (Option.map ~f:Recipes.of_xml) (Xml.child xml_arg0 "recipes") in
      make ?nextToken ?recipes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let recipes = field_map json "recipes" Recipes.of_json in
      make ?nextToken ?recipes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of available recipes. The response provides the properties for each recipe, including the recipe's Amazon Resource Name (ARN)."]
module ListRecipesRequest =
  struct
    type nonrec t =
      {
      recipeProvider: RecipeProvider.t option
        [@ocaml.doc "The default is SERVICE."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from the previous call to ListRecipes for getting the next set of recipes (if they exist)."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of recipes to return."];
      domain: Domain.t option
        [@ocaml.doc
          "Filters returned recipes by domain for a Domain dataset group. Only recipes (Domain dataset group use cases) for this domain are included in the response. If you don't specify a domain, all recipes are returned."]}
    let make ?recipeProvider =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?domain ->
            fun () -> { recipeProvider; nextToken; maxResults; domain }
    let to_value x =
      structure_to_value
        [("recipeProvider",
           (Option.map x.recipeProvider ~f:RecipeProvider.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("domain", (Option.map x.domain ~f:Domain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        (Option.map ~f:Domain.of_xml) (Xml.child xml_arg0 "domain") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let recipeProvider =
        (Option.map ~f:RecipeProvider.of_xml)
          (Xml.child xml_arg0 "recipeProvider") in
      make ?domain ?maxResults ?nextToken ?recipeProvider ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let domain = field_map json "domain" Domain.of_json in
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let recipeProvider =
        field_map json "recipeProvider" RecipeProvider.of_json in
      make ?domain ?maxResults ?nextToken ?recipeProvider ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of available recipes. The response provides the properties for each recipe, including the recipe's Amazon Resource Name (ARN)."]
module ListFiltersResponse =
  struct
    type nonrec t =
      {
      filters: Filters.t option [@ocaml.doc "A list of returned filters."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token for getting the next set of filters (if they exist)."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `InvalidNextTokenException of InvalidNextTokenException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?filters = fun ?nextToken -> fun () -> { filters; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Filters", (Option.map x.filters ~f:Filters.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let filters =
        (Option.map ~f:Filters.of_xml) (Xml.child xml_arg0 "Filters") in
      make ?nextToken ?filters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let filters = field_map json "Filters" Filters.of_json in
      make ?nextToken ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists all filters that belong to a given dataset group."]
module ListFiltersRequest =
  struct
    type nonrec t =
      {
      datasetGroupArn: Arn.t option
        [@ocaml.doc
          "The ARN of the dataset group that contains the filters."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from the previous call to ListFilters for getting the next set of filters (if they exist)."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of filters to return."]}
    let make ?datasetGroupArn =
      fun ?nextToken ->
        fun ?maxResults ->
          fun () -> { datasetGroupArn; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      make ?maxResults ?nextToken ?datasetGroupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      make ?maxResults ?nextToken ?datasetGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists all filters that belong to a given dataset group."]
module ListEventTrackersResponse =
  struct
    type nonrec t =
      {
      eventTrackers: EventTrackers.t option
        [@ocaml.doc "A list of event trackers."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token for getting the next set of event trackers (if they exist)."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `InvalidNextTokenException of InvalidNextTokenException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?eventTrackers =
      fun ?nextToken -> fun () -> { eventTrackers; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("eventTrackers",
           (Option.map x.eventTrackers ~f:EventTrackers.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let eventTrackers =
        (Option.map ~f:EventTrackers.of_xml)
          (Xml.child xml_arg0 "eventTrackers") in
      make ?nextToken ?eventTrackers ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let eventTrackers =
        field_map json "eventTrackers" EventTrackers.of_json in
      make ?nextToken ?eventTrackers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the list of event trackers associated with the account. The response provides the properties for each event tracker, including the Amazon Resource Name (ARN) and tracking ID. For more information on event trackers, see CreateEventTracker."]
module ListEventTrackersRequest =
  struct
    type nonrec t =
      {
      datasetGroupArn: Arn.t option
        [@ocaml.doc
          "The ARN of a dataset group used to filter the response."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from the previous call to ListEventTrackers for getting the next set of event trackers (if they exist)."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of event trackers to return."]}
    let make ?datasetGroupArn =
      fun ?nextToken ->
        fun ?maxResults ->
          fun () -> { datasetGroupArn; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      make ?maxResults ?nextToken ?datasetGroupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      make ?maxResults ?nextToken ?datasetGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the list of event trackers associated with the account. The response provides the properties for each event tracker, including the Amazon Resource Name (ARN) and tracking ID. For more information on event trackers, see CreateEventTracker."]
module ListDatasetsResponse =
  struct
    type nonrec t =
      {
      datasets: Datasets.t option
        [@ocaml.doc
          "An array of Dataset objects. Each object provides metadata information."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token for getting the next set of datasets (if they exist)."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `InvalidNextTokenException of InvalidNextTokenException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?datasets = fun ?nextToken -> fun () -> { datasets; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("datasets", (Option.map x.datasets ~f:Datasets.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let datasets =
        (Option.map ~f:Datasets.of_xml) (Xml.child xml_arg0 "datasets") in
      make ?nextToken ?datasets ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let datasets = field_map json "datasets" Datasets.of_json in
      make ?nextToken ?datasets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the list of datasets contained in the given dataset group. The response provides the properties for each dataset, including the Amazon Resource Name (ARN). For more information on datasets, see CreateDataset."]
module ListDatasetsRequest =
  struct
    type nonrec t =
      {
      datasetGroupArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset group that contains the datasets to list."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from the previous call to ListDatasetImportJobs for getting the next set of dataset import jobs (if they exist)."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of datasets to return."]}
    let make ?datasetGroupArn =
      fun ?nextToken ->
        fun ?maxResults ->
          fun () -> { datasetGroupArn; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      make ?maxResults ?nextToken ?datasetGroupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      make ?maxResults ?nextToken ?datasetGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the list of datasets contained in the given dataset group. The response provides the properties for each dataset, including the Amazon Resource Name (ARN). For more information on datasets, see CreateDataset."]
module ListDatasetImportJobsResponse =
  struct
    type nonrec t =
      {
      datasetImportJobs: DatasetImportJobs.t option
        [@ocaml.doc "The list of dataset import jobs."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token for getting the next set of dataset import jobs (if they exist)."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `InvalidNextTokenException of InvalidNextTokenException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?datasetImportJobs =
      fun ?nextToken -> fun () -> { datasetImportJobs; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("datasetImportJobs",
           (Option.map x.datasetImportJobs ~f:DatasetImportJobs.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let datasetImportJobs =
        (Option.map ~f:DatasetImportJobs.of_xml)
          (Xml.child xml_arg0 "datasetImportJobs") in
      make ?nextToken ?datasetImportJobs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let datasetImportJobs =
        field_map json "datasetImportJobs" DatasetImportJobs.of_json in
      make ?nextToken ?datasetImportJobs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of dataset import jobs that use the given dataset. When a dataset is not specified, all the dataset import jobs associated with the account are listed. The response provides the properties for each dataset import job, including the Amazon Resource Name (ARN). For more information on dataset import jobs, see CreateDatasetImportJob. For more information on datasets, see CreateDataset."]
module ListDatasetImportJobsRequest =
  struct
    type nonrec t =
      {
      datasetArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset to list the dataset import jobs for."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from the previous call to ListDatasetImportJobs for getting the next set of dataset import jobs (if they exist)."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of dataset import jobs to return."]}
    let make ?datasetArn =
      fun ?nextToken ->
        fun ?maxResults -> fun () -> { datasetArn; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("datasetArn", (Option.map x.datasetArn ~f:Arn.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let datasetArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetArn") in
      make ?maxResults ?nextToken ?datasetArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let datasetArn = field_map json "datasetArn" Arn.of_json in
      make ?maxResults ?nextToken ?datasetArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of dataset import jobs that use the given dataset. When a dataset is not specified, all the dataset import jobs associated with the account are listed. The response provides the properties for each dataset import job, including the Amazon Resource Name (ARN). For more information on dataset import jobs, see CreateDatasetImportJob. For more information on datasets, see CreateDataset."]
module ListDatasetGroupsResponse =
  struct
    type nonrec t =
      {
      datasetGroups: DatasetGroups.t option
        [@ocaml.doc "The list of your dataset groups."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token for getting the next set of dataset groups (if they exist)."]}
    type nonrec error =
      [ `InvalidNextTokenException of InvalidNextTokenException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?datasetGroups =
      fun ?nextToken -> fun () -> { datasetGroups; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("datasetGroups",
           (Option.map x.datasetGroups ~f:DatasetGroups.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let datasetGroups =
        (Option.map ~f:DatasetGroups.of_xml)
          (Xml.child xml_arg0 "datasetGroups") in
      make ?nextToken ?datasetGroups ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let datasetGroups =
        field_map json "datasetGroups" DatasetGroups.of_json in
      make ?nextToken ?datasetGroups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of dataset groups. The response provides the properties for each dataset group, including the Amazon Resource Name (ARN). For more information on dataset groups, see CreateDatasetGroup."]
module ListDatasetGroupsRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from the previous call to ListDatasetGroups for getting the next set of dataset groups (if they exist)."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of dataset groups to return."]}
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of dataset groups. The response provides the properties for each dataset group, including the Amazon Resource Name (ARN). For more information on dataset groups, see CreateDatasetGroup."]
module ListDatasetExportJobsResponse =
  struct
    type nonrec t =
      {
      datasetExportJobs: DatasetExportJobs.t option
        [@ocaml.doc "The list of dataset export jobs."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token for getting the next set of dataset export jobs (if they exist)."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `InvalidNextTokenException of InvalidNextTokenException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?datasetExportJobs =
      fun ?nextToken -> fun () -> { datasetExportJobs; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("datasetExportJobs",
           (Option.map x.datasetExportJobs ~f:DatasetExportJobs.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let datasetExportJobs =
        (Option.map ~f:DatasetExportJobs.of_xml)
          (Xml.child xml_arg0 "datasetExportJobs") in
      make ?nextToken ?datasetExportJobs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let datasetExportJobs =
        field_map json "datasetExportJobs" DatasetExportJobs.of_json in
      make ?nextToken ?datasetExportJobs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of dataset export jobs that use the given dataset. When a dataset is not specified, all the dataset export jobs associated with the account are listed. The response provides the properties for each dataset export job, including the Amazon Resource Name (ARN). For more information on dataset export jobs, see CreateDatasetExportJob. For more information on datasets, see CreateDataset."]
module ListDatasetExportJobsRequest =
  struct
    type nonrec t =
      {
      datasetArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset to list the dataset export jobs for."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from the previous call to ListDatasetExportJobs for getting the next set of dataset export jobs (if they exist)."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of dataset export jobs to return."]}
    let make ?datasetArn =
      fun ?nextToken ->
        fun ?maxResults -> fun () -> { datasetArn; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("datasetArn", (Option.map x.datasetArn ~f:Arn.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let datasetArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetArn") in
      make ?maxResults ?nextToken ?datasetArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let datasetArn = field_map json "datasetArn" Arn.of_json in
      make ?maxResults ?nextToken ?datasetArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of dataset export jobs that use the given dataset. When a dataset is not specified, all the dataset export jobs associated with the account are listed. The response provides the properties for each dataset export job, including the Amazon Resource Name (ARN). For more information on dataset export jobs, see CreateDatasetExportJob. For more information on datasets, see CreateDataset."]
module ListCampaignsResponse =
  struct
    type nonrec t =
      {
      campaigns: Campaigns.t option [@ocaml.doc "A list of the campaigns."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token for getting the next set of campaigns (if they exist)."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `InvalidNextTokenException of InvalidNextTokenException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?campaigns =
      fun ?nextToken -> fun () -> { campaigns; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("campaigns", (Option.map x.campaigns ~f:Campaigns.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let campaigns =
        (Option.map ~f:Campaigns.of_xml) (Xml.child xml_arg0 "campaigns") in
      make ?nextToken ?campaigns ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let campaigns = field_map json "campaigns" Campaigns.of_json in
      make ?nextToken ?campaigns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of campaigns that use the given solution. When a solution is not specified, all the campaigns associated with the account are listed. The response provides the properties for each campaign, including the Amazon Resource Name (ARN). For more information on campaigns, see CreateCampaign."]
module ListCampaignsRequest =
  struct
    type nonrec t =
      {
      solutionArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution to list the campaigns for. When a solution is not specified, all the campaigns associated with the account are listed."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token returned from the previous call to ListCampaigns for getting the next set of campaigns (if they exist)."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of campaigns to return."]}
    let make ?solutionArn =
      fun ?nextToken ->
        fun ?maxResults -> fun () -> { solutionArn; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("solutionArn", (Option.map x.solutionArn ~f:Arn.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let solutionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionArn") in
      make ?maxResults ?nextToken ?solutionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let solutionArn = field_map json "solutionArn" Arn.of_json in
      make ?maxResults ?nextToken ?solutionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of campaigns that use the given solution. When a solution is not specified, all the campaigns associated with the account are listed. The response provides the properties for each campaign, including the Amazon Resource Name (ARN). For more information on campaigns, see CreateCampaign."]
module ListBatchSegmentJobsResponse =
  struct
    type nonrec t =
      {
      batchSegmentJobs: BatchSegmentJobs.t option
        [@ocaml.doc
          "A list containing information on each job that is returned."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. The value is null when there are no more results to return."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `InvalidNextTokenException of InvalidNextTokenException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?batchSegmentJobs =
      fun ?nextToken -> fun () -> { batchSegmentJobs; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("batchSegmentJobs",
           (Option.map x.batchSegmentJobs ~f:BatchSegmentJobs.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let batchSegmentJobs =
        (Option.map ~f:BatchSegmentJobs.of_xml)
          (Xml.child xml_arg0 "batchSegmentJobs") in
      make ?nextToken ?batchSegmentJobs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let batchSegmentJobs =
        field_map json "batchSegmentJobs" BatchSegmentJobs.of_json in
      make ?nextToken ?batchSegmentJobs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of the batch segment jobs that have been performed off of a solution version that you specify."]
module ListBatchSegmentJobsRequest =
  struct
    type nonrec t =
      {
      solutionVersionArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution version that the batch segment jobs used to generate batch segments."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token to request the next page of results."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of batch segment job results to return in each page. The default value is 100."]}
    let make ?solutionVersionArn =
      fun ?nextToken ->
        fun ?maxResults ->
          fun () -> { solutionVersionArn; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("solutionVersionArn",
           (Option.map x.solutionVersionArn ~f:Arn.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let solutionVersionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionVersionArn") in
      make ?maxResults ?nextToken ?solutionVersionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let solutionVersionArn =
        field_map json "solutionVersionArn" Arn.of_json in
      make ?maxResults ?nextToken ?solutionVersionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of the batch segment jobs that have been performed off of a solution version that you specify."]
module ListBatchInferenceJobsResponse =
  struct
    type nonrec t =
      {
      batchInferenceJobs: BatchInferenceJobs.t option
        [@ocaml.doc
          "A list containing information on each job that is returned."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. The value is null when there are no more results to return."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `InvalidNextTokenException of InvalidNextTokenException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?batchInferenceJobs =
      fun ?nextToken -> fun () -> { batchInferenceJobs; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("batchInferenceJobs",
           (Option.map x.batchInferenceJobs ~f:BatchInferenceJobs.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let batchInferenceJobs =
        (Option.map ~f:BatchInferenceJobs.of_xml)
          (Xml.child xml_arg0 "batchInferenceJobs") in
      make ?nextToken ?batchInferenceJobs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let batchInferenceJobs =
        field_map json "batchInferenceJobs" BatchInferenceJobs.of_json in
      make ?nextToken ?batchInferenceJobs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of the batch inference jobs that have been performed off of a solution version."]
module ListBatchInferenceJobsRequest =
  struct
    type nonrec t =
      {
      solutionVersionArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution version from which the batch inference jobs were created."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token to request the next page of results."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of batch inference job results to return in each page. The default value is 100."]}
    let make ?solutionVersionArn =
      fun ?nextToken ->
        fun ?maxResults ->
          fun () -> { solutionVersionArn; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("solutionVersionArn",
           (Option.map x.solutionVersionArn ~f:Arn.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let solutionVersionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionVersionArn") in
      make ?maxResults ?nextToken ?solutionVersionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let solutionVersionArn =
        field_map json "solutionVersionArn" Arn.of_json in
      make ?maxResults ?nextToken ?solutionVersionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of the batch inference jobs that have been performed off of a solution version."]
module GetSolutionMetricsResponse =
  struct
    type nonrec t =
      {
      solutionVersionArn: Arn.t option
        [@ocaml.doc
          "The same solution version ARN as specified in the request."];
      metrics: Metrics.t option
        [@ocaml.doc "The metrics for the solution version."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?solutionVersionArn =
      fun ?metrics -> fun () -> { solutionVersionArn; metrics }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("solutionVersionArn",
           (Option.map x.solutionVersionArn ~f:Arn.to_value));
        ("metrics", (Option.map x.metrics ~f:Metrics.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metrics =
        (Option.map ~f:Metrics.of_xml) (Xml.child xml_arg0 "metrics") in
      let solutionVersionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionVersionArn") in
      make ?metrics ?solutionVersionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let metrics = field_map json "metrics" Metrics.of_json in
      let solutionVersionArn =
        field_map json "solutionVersionArn" Arn.of_json in
      make ?metrics ?solutionVersionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets the metrics for the specified solution version."]
module GetSolutionMetricsRequest =
  struct
    type nonrec t =
      {
      solutionVersionArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution version for which to get metrics."]}
    let context_ = "GetSolutionMetricsRequest"
    let make ~solutionVersionArn = fun () -> { solutionVersionArn }
    let to_value x =
      structure_to_value
        [("solutionVersionArn", (Some (Arn.to_value x.solutionVersionArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let solutionVersionArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "solutionVersionArn") in
      make ~solutionVersionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let solutionVersionArn =
        field_map_exn json "solutionVersionArn" Arn.of_json in
      make ~solutionVersionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets the metrics for the specified solution version."]
module DescribeSolutionVersionResponse =
  struct
    type nonrec t =
      {
      solutionVersion: SolutionVersion.t option
        [@ocaml.doc "The solution version."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?solutionVersion = fun () -> { solutionVersion }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("solutionVersion",
           (Option.map x.solutionVersion ~f:SolutionVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let solutionVersion =
        (Option.map ~f:SolutionVersion.of_xml)
          (Xml.child xml_arg0 "solutionVersion") in
      make ?solutionVersion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let solutionVersion =
        field_map json "solutionVersion" SolutionVersion.of_json in
      make ?solutionVersion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a specific version of a solution. For more information on solutions, see CreateSolution"]
module DescribeSolutionVersionRequest =
  struct
    type nonrec t =
      {
      solutionVersionArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution version."]}
    let context_ = "DescribeSolutionVersionRequest"
    let make ~solutionVersionArn = fun () -> { solutionVersionArn }
    let to_value x =
      structure_to_value
        [("solutionVersionArn", (Some (Arn.to_value x.solutionVersionArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let solutionVersionArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "solutionVersionArn") in
      make ~solutionVersionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let solutionVersionArn =
        field_map_exn json "solutionVersionArn" Arn.of_json in
      make ~solutionVersionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a specific version of a solution. For more information on solutions, see CreateSolution"]
module DescribeSolutionResponse =
  struct
    type nonrec t =
      {
      solution: Solution.t option
        [@ocaml.doc "An object that describes the solution."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?solution = fun () -> { solution }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("solution", (Option.map x.solution ~f:Solution.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let solution =
        (Option.map ~f:Solution.of_xml) (Xml.child xml_arg0 "solution") in
      make ?solution ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let solution = field_map json "solution" Solution.of_json in
      make ?solution ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a solution. For more information on solutions, see CreateSolution."]
module DescribeSolutionRequest =
  struct
    type nonrec t =
      {
      solutionArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution to describe."]}
    let context_ = "DescribeSolutionRequest"
    let make ~solutionArn = fun () -> { solutionArn }
    let to_value x =
      structure_to_value
        [("solutionArn", (Some (Arn.to_value x.solutionArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let solutionArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "solutionArn") in
      make ~solutionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let solutionArn = field_map_exn json "solutionArn" Arn.of_json in
      make ~solutionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a solution. For more information on solutions, see CreateSolution."]
module DescribeSchemaResponse =
  struct
    type nonrec t =
      {
      schema: DatasetSchema.t option [@ocaml.doc "The requested schema."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?schema = fun () -> { schema }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("schema", (Option.map x.schema ~f:DatasetSchema.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let schema =
        (Option.map ~f:DatasetSchema.of_xml) (Xml.child xml_arg0 "schema") in
      make ?schema ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let schema = field_map json "schema" DatasetSchema.of_json in
      make ?schema ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a schema. For more information on schemas, see CreateSchema."]
module DescribeSchemaRequest =
  struct
    type nonrec t =
      {
      schemaArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the schema to retrieve."]}
    let context_ = "DescribeSchemaRequest"
    let make ~schemaArn = fun () -> { schemaArn }
    let to_value x =
      structure_to_value [("schemaArn", (Some (Arn.to_value x.schemaArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let schemaArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "schemaArn") in
      make ~schemaArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let schemaArn = field_map_exn json "schemaArn" Arn.of_json in
      make ~schemaArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a schema. For more information on schemas, see CreateSchema."]
module DescribeRecommenderResponse =
  struct
    type nonrec t =
      {
      recommender: Recommender.t option
        [@ocaml.doc "The properties of the recommender."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recommender = fun () -> { recommender }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("recommender", (Option.map x.recommender ~f:Recommender.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recommender =
        (Option.map ~f:Recommender.of_xml) (Xml.child xml_arg0 "recommender") in
      make ?recommender ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recommender = field_map json "recommender" Recommender.of_json in
      make ?recommender ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the given recommender, including its status. A recommender can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS When the status is CREATE FAILED, the response includes the failureReason key, which describes why. For more information on recommenders, see CreateRecommender."]
module DescribeRecommenderRequest =
  struct
    type nonrec t =
      {
      recommenderArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the recommender to describe."]}
    let context_ = "DescribeRecommenderRequest"
    let make ~recommenderArn = fun () -> { recommenderArn }
    let to_value x =
      structure_to_value
        [("recommenderArn", (Some (Arn.to_value x.recommenderArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recommenderArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "recommenderArn") in
      make ~recommenderArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recommenderArn = field_map_exn json "recommenderArn" Arn.of_json in
      make ~recommenderArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the given recommender, including its status. A recommender can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS When the status is CREATE FAILED, the response includes the failureReason key, which describes why. For more information on recommenders, see CreateRecommender."]
module DescribeRecipeResponse =
  struct
    type nonrec t =
      {
      recipe: Recipe.t option
        [@ocaml.doc "An object that describes the recipe."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recipe = fun () -> { recipe }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("recipe", (Option.map x.recipe ~f:Recipe.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recipe =
        (Option.map ~f:Recipe.of_xml) (Xml.child xml_arg0 "recipe") in
      make ?recipe ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recipe = field_map json "recipe" Recipe.of_json in make ?recipe ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a recipe. A recipe contains three items: An algorithm that trains a model. Hyperparameters that govern the training. Feature transformation information for modifying the input data before training. Amazon Personalize provides a set of predefined recipes. You specify a recipe when you create a solution with the CreateSolution API. CreateSolution trains a model by using the algorithm in the specified recipe and a training dataset. The solution, when deployed as a campaign, can provide recommendations using the GetRecommendations API."]
module DescribeRecipeRequest =
  struct
    type nonrec t =
      {
      recipeArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the recipe to describe."]}
    let context_ = "DescribeRecipeRequest"
    let make ~recipeArn = fun () -> { recipeArn }
    let to_value x =
      structure_to_value [("recipeArn", (Some (Arn.to_value x.recipeArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recipeArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "recipeArn") in
      make ~recipeArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recipeArn = field_map_exn json "recipeArn" Arn.of_json in
      make ~recipeArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a recipe. A recipe contains three items: An algorithm that trains a model. Hyperparameters that govern the training. Feature transformation information for modifying the input data before training. Amazon Personalize provides a set of predefined recipes. You specify a recipe when you create a solution with the CreateSolution API. CreateSolution trains a model by using the algorithm in the specified recipe and a training dataset. The solution, when deployed as a campaign, can provide recommendations using the GetRecommendations API."]
module DescribeFilterResponse =
  struct
    type nonrec t =
      {
      filter: Filter.t option [@ocaml.doc "The filter's details."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?filter = fun () -> { filter }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("filter", (Option.map x.filter ~f:Filter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filter =
        (Option.map ~f:Filter.of_xml) (Xml.child xml_arg0 "filter") in
      make ?filter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let filter = field_map json "filter" Filter.of_json in make ?filter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a filter's properties."]
module DescribeFilterRequest =
  struct
    type nonrec t =
      {
      filterArn: Arn.t [@ocaml.doc "The ARN of the filter to describe."]}
    let context_ = "DescribeFilterRequest"
    let make ~filterArn = fun () -> { filterArn }
    let to_value x =
      structure_to_value [("filterArn", (Some (Arn.to_value x.filterArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filterArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "filterArn") in
      make ~filterArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let filterArn = field_map_exn json "filterArn" Arn.of_json in
      make ~filterArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a filter's properties."]
module DescribeFeatureTransformationResponse =
  struct
    type nonrec t =
      {
      featureTransformation: FeatureTransformation.t option
        [@ocaml.doc "A listing of the FeatureTransformation properties."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?featureTransformation = fun () -> { featureTransformation }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("featureTransformation",
           (Option.map x.featureTransformation
              ~f:FeatureTransformation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let featureTransformation =
        (Option.map ~f:FeatureTransformation.of_xml)
          (Xml.child xml_arg0 "featureTransformation") in
      make ?featureTransformation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let featureTransformation =
        field_map json "featureTransformation" FeatureTransformation.of_json in
      make ?featureTransformation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the given feature transformation."]
module DescribeFeatureTransformationRequest =
  struct
    type nonrec t =
      {
      featureTransformationArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the feature transformation to describe."]}
    let context_ = "DescribeFeatureTransformationRequest"
    let make ~featureTransformationArn =
      fun () -> { featureTransformationArn }
    let to_value x =
      structure_to_value
        [("featureTransformationArn",
           (Some (Arn.to_value x.featureTransformationArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let featureTransformationArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "featureTransformationArn") in
      make ~featureTransformationArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let featureTransformationArn =
        field_map_exn json "featureTransformationArn" Arn.of_json in
      make ~featureTransformationArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the given feature transformation."]
module DescribeEventTrackerResponse =
  struct
    type nonrec t =
      {
      eventTracker: EventTracker.t option
        [@ocaml.doc "An object that describes the event tracker."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?eventTracker = fun () -> { eventTracker }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("eventTracker",
           (Option.map x.eventTracker ~f:EventTracker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventTracker =
        (Option.map ~f:EventTracker.of_xml)
          (Xml.child xml_arg0 "eventTracker") in
      make ?eventTracker ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let eventTracker = field_map json "eventTracker" EventTracker.of_json in
      make ?eventTracker ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes an event tracker. The response includes the trackingId and status of the event tracker. For more information on event trackers, see CreateEventTracker."]
module DescribeEventTrackerRequest =
  struct
    type nonrec t =
      {
      eventTrackerArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the event tracker to describe."]}
    let context_ = "DescribeEventTrackerRequest"
    let make ~eventTrackerArn = fun () -> { eventTrackerArn }
    let to_value x =
      structure_to_value
        [("eventTrackerArn", (Some (Arn.to_value x.eventTrackerArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventTrackerArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "eventTrackerArn") in
      make ~eventTrackerArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let eventTrackerArn = field_map_exn json "eventTrackerArn" Arn.of_json in
      make ~eventTrackerArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes an event tracker. The response includes the trackingId and status of the event tracker. For more information on event trackers, see CreateEventTracker."]
module DescribeDatasetResponse =
  struct
    type nonrec t =
      {
      dataset: Dataset.t option
        [@ocaml.doc "A listing of the dataset's properties."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?dataset = fun () -> { dataset }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("dataset", (Option.map x.dataset ~f:Dataset.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dataset =
        (Option.map ~f:Dataset.of_xml) (Xml.child xml_arg0 "dataset") in
      make ?dataset ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let dataset = field_map json "dataset" Dataset.of_json in
      make ?dataset ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the given dataset. For more information on datasets, see CreateDataset."]
module DescribeDatasetRequest =
  struct
    type nonrec t =
      {
      datasetArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset to describe."]}
    let context_ = "DescribeDatasetRequest"
    let make ~datasetArn = fun () -> { datasetArn }
    let to_value x =
      structure_to_value [("datasetArn", (Some (Arn.to_value x.datasetArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let datasetArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "datasetArn") in
      make ~datasetArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let datasetArn = field_map_exn json "datasetArn" Arn.of_json in
      make ~datasetArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the given dataset. For more information on datasets, see CreateDataset."]
module DescribeDatasetImportJobResponse =
  struct
    type nonrec t =
      {
      datasetImportJob: DatasetImportJob.t option
        [@ocaml.doc
          "Information about the dataset import job, including the status. The status is one of the following values: CREATE PENDING CREATE IN_PROGRESS ACTIVE CREATE FAILED"]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?datasetImportJob = fun () -> { datasetImportJob }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("datasetImportJob",
           (Option.map x.datasetImportJob ~f:DatasetImportJob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let datasetImportJob =
        (Option.map ~f:DatasetImportJob.of_xml)
          (Xml.child xml_arg0 "datasetImportJob") in
      make ?datasetImportJob ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let datasetImportJob =
        field_map json "datasetImportJob" DatasetImportJob.of_json in
      make ?datasetImportJob ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the dataset import job created by CreateDatasetImportJob, including the import job status."]
module DescribeDatasetImportJobRequest =
  struct
    type nonrec t =
      {
      datasetImportJobArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset import job to describe."]}
    let context_ = "DescribeDatasetImportJobRequest"
    let make ~datasetImportJobArn = fun () -> { datasetImportJobArn }
    let to_value x =
      structure_to_value
        [("datasetImportJobArn", (Some (Arn.to_value x.datasetImportJobArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let datasetImportJobArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "datasetImportJobArn") in
      make ~datasetImportJobArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let datasetImportJobArn =
        field_map_exn json "datasetImportJobArn" Arn.of_json in
      make ~datasetImportJobArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the dataset import job created by CreateDatasetImportJob, including the import job status."]
module DescribeDatasetGroupResponse =
  struct
    type nonrec t =
      {
      datasetGroup: DatasetGroup.t option
        [@ocaml.doc "A listing of the dataset group's properties."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?datasetGroup = fun () -> { datasetGroup }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("datasetGroup",
           (Option.map x.datasetGroup ~f:DatasetGroup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let datasetGroup =
        (Option.map ~f:DatasetGroup.of_xml)
          (Xml.child xml_arg0 "datasetGroup") in
      make ?datasetGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let datasetGroup = field_map json "datasetGroup" DatasetGroup.of_json in
      make ?datasetGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the given dataset group. For more information on dataset groups, see CreateDatasetGroup."]
module DescribeDatasetGroupRequest =
  struct
    type nonrec t =
      {
      datasetGroupArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset group to describe."]}
    let context_ = "DescribeDatasetGroupRequest"
    let make ~datasetGroupArn = fun () -> { datasetGroupArn }
    let to_value x =
      structure_to_value
        [("datasetGroupArn", (Some (Arn.to_value x.datasetGroupArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let datasetGroupArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "datasetGroupArn") in
      make ~datasetGroupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let datasetGroupArn = field_map_exn json "datasetGroupArn" Arn.of_json in
      make ~datasetGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the given dataset group. For more information on dataset groups, see CreateDatasetGroup."]
module DescribeDatasetExportJobResponse =
  struct
    type nonrec t =
      {
      datasetExportJob: DatasetExportJob.t option
        [@ocaml.doc
          "Information about the dataset export job, including the status. The status is one of the following values: CREATE PENDING CREATE IN_PROGRESS ACTIVE CREATE FAILED"]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?datasetExportJob = fun () -> { datasetExportJob }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("datasetExportJob",
           (Option.map x.datasetExportJob ~f:DatasetExportJob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let datasetExportJob =
        (Option.map ~f:DatasetExportJob.of_xml)
          (Xml.child xml_arg0 "datasetExportJob") in
      make ?datasetExportJob ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let datasetExportJob =
        field_map json "datasetExportJob" DatasetExportJob.of_json in
      make ?datasetExportJob ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the dataset export job created by CreateDatasetExportJob, including the export job status."]
module DescribeDatasetExportJobRequest =
  struct
    type nonrec t =
      {
      datasetExportJobArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset export job to describe."]}
    let context_ = "DescribeDatasetExportJobRequest"
    let make ~datasetExportJobArn = fun () -> { datasetExportJobArn }
    let to_value x =
      structure_to_value
        [("datasetExportJobArn", (Some (Arn.to_value x.datasetExportJobArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let datasetExportJobArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "datasetExportJobArn") in
      make ~datasetExportJobArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let datasetExportJobArn =
        field_map_exn json "datasetExportJobArn" Arn.of_json in
      make ~datasetExportJobArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the dataset export job created by CreateDatasetExportJob, including the export job status."]
module DescribeCampaignResponse =
  struct
    type nonrec t =
      {
      campaign: Campaign.t option
        [@ocaml.doc "The properties of the campaign."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?campaign = fun () -> { campaign }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("campaign", (Option.map x.campaign ~f:Campaign.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let campaign =
        (Option.map ~f:Campaign.of_xml) (Xml.child xml_arg0 "campaign") in
      make ?campaign ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let campaign = field_map json "campaign" Campaign.of_json in
      make ?campaign ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the given campaign, including its status. A campaign can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS When the status is CREATE FAILED, the response includes the failureReason key, which describes why. For more information on campaigns, see CreateCampaign."]
module DescribeCampaignRequest =
  struct
    type nonrec t =
      {
      campaignArn: Arn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the campaign."]}
    let context_ = "DescribeCampaignRequest"
    let make ~campaignArn = fun () -> { campaignArn }
    let to_value x =
      structure_to_value
        [("campaignArn", (Some (Arn.to_value x.campaignArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let campaignArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "campaignArn") in
      make ~campaignArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let campaignArn = field_map_exn json "campaignArn" Arn.of_json in
      make ~campaignArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the given campaign, including its status. A campaign can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS When the status is CREATE FAILED, the response includes the failureReason key, which describes why. For more information on campaigns, see CreateCampaign."]
module DescribeBatchSegmentJobResponse =
  struct
    type nonrec t =
      {
      batchSegmentJob: BatchSegmentJob.t option
        [@ocaml.doc "Information on the specified batch segment job."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?batchSegmentJob = fun () -> { batchSegmentJob }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("batchSegmentJob",
           (Option.map x.batchSegmentJob ~f:BatchSegmentJob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let batchSegmentJob =
        (Option.map ~f:BatchSegmentJob.of_xml)
          (Xml.child xml_arg0 "batchSegmentJob") in
      make ?batchSegmentJob ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let batchSegmentJob =
        field_map json "batchSegmentJob" BatchSegmentJob.of_json in
      make ?batchSegmentJob ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the properties of a batch segment job including name, Amazon Resource Name (ARN), status, input and output configurations, and the ARN of the solution version used to generate segments."]
module DescribeBatchSegmentJobRequest =
  struct
    type nonrec t =
      {
      batchSegmentJobArn: Arn.t
        [@ocaml.doc "The ARN of the batch segment job to describe."]}
    let context_ = "DescribeBatchSegmentJobRequest"
    let make ~batchSegmentJobArn = fun () -> { batchSegmentJobArn }
    let to_value x =
      structure_to_value
        [("batchSegmentJobArn", (Some (Arn.to_value x.batchSegmentJobArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let batchSegmentJobArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "batchSegmentJobArn") in
      make ~batchSegmentJobArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let batchSegmentJobArn =
        field_map_exn json "batchSegmentJobArn" Arn.of_json in
      make ~batchSegmentJobArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the properties of a batch segment job including name, Amazon Resource Name (ARN), status, input and output configurations, and the ARN of the solution version used to generate segments."]
module DescribeBatchInferenceJobResponse =
  struct
    type nonrec t =
      {
      batchInferenceJob: BatchInferenceJob.t option
        [@ocaml.doc "Information on the specified batch inference job."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?batchInferenceJob = fun () -> { batchInferenceJob }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("batchInferenceJob",
           (Option.map x.batchInferenceJob ~f:BatchInferenceJob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let batchInferenceJob =
        (Option.map ~f:BatchInferenceJob.of_xml)
          (Xml.child xml_arg0 "batchInferenceJob") in
      make ?batchInferenceJob ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let batchInferenceJob =
        field_map json "batchInferenceJob" BatchInferenceJob.of_json in
      make ?batchInferenceJob ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the properties of a batch inference job including name, Amazon Resource Name (ARN), status, input and output configurations, and the ARN of the solution version used to generate the recommendations."]
module DescribeBatchInferenceJobRequest =
  struct
    type nonrec t =
      {
      batchInferenceJobArn: Arn.t
        [@ocaml.doc "The ARN of the batch inference job to describe."]}
    let context_ = "DescribeBatchInferenceJobRequest"
    let make ~batchInferenceJobArn = fun () -> { batchInferenceJobArn }
    let to_value x =
      structure_to_value
        [("batchInferenceJobArn",
           (Some (Arn.to_value x.batchInferenceJobArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let batchInferenceJobArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "batchInferenceJobArn") in
      make ~batchInferenceJobArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let batchInferenceJobArn =
        field_map_exn json "batchInferenceJobArn" Arn.of_json in
      make ~batchInferenceJobArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the properties of a batch inference job including name, Amazon Resource Name (ARN), status, input and output configurations, and the ARN of the solution version used to generate the recommendations."]
module DescribeAlgorithmResponse =
  struct
    type nonrec t =
      {
      algorithm: Algorithm.t option
        [@ocaml.doc "A listing of the properties of the algorithm."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?algorithm = fun () -> { algorithm }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("algorithm", (Option.map x.algorithm ~f:Algorithm.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let algorithm =
        (Option.map ~f:Algorithm.of_xml) (Xml.child xml_arg0 "algorithm") in
      make ?algorithm ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let algorithm = field_map json "algorithm" Algorithm.of_json in
      make ?algorithm ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the given algorithm."]
module DescribeAlgorithmRequest =
  struct
    type nonrec t =
      {
      algorithmArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the algorithm to describe."]}
    let context_ = "DescribeAlgorithmRequest"
    let make ~algorithmArn = fun () -> { algorithmArn }
    let to_value x =
      structure_to_value
        [("algorithmArn", (Some (Arn.to_value x.algorithmArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let algorithmArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "algorithmArn") in
      make ~algorithmArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let algorithmArn = field_map_exn json "algorithmArn" Arn.of_json in
      make ~algorithmArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the given algorithm."]
module DeleteSolutionRequest =
  struct
    type nonrec t =
      {
      solutionArn: Arn.t [@ocaml.doc "The ARN of the solution to delete."]}
    let context_ = "DeleteSolutionRequest"
    let make ~solutionArn = fun () -> { solutionArn }
    let to_value x =
      structure_to_value
        [("solutionArn", (Some (Arn.to_value x.solutionArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let solutionArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "solutionArn") in
      make ~solutionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let solutionArn = field_map_exn json "solutionArn" Arn.of_json in
      make ~solutionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes all versions of a solution and the Solution object itself. Before deleting a solution, you must delete all campaigns based on the solution. To determine what campaigns are using the solution, call ListCampaigns and supply the Amazon Resource Name (ARN) of the solution. You can't delete a solution if an associated SolutionVersion is in the CREATE PENDING or IN PROGRESS state. For more information on solutions, see CreateSolution."]
module DeleteSchemaRequest =
  struct
    type nonrec t =
      {
      schemaArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the schema to delete."]}
    let context_ = "DeleteSchemaRequest"
    let make ~schemaArn = fun () -> { schemaArn }
    let to_value x =
      structure_to_value [("schemaArn", (Some (Arn.to_value x.schemaArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let schemaArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "schemaArn") in
      make ~schemaArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let schemaArn = field_map_exn json "schemaArn" Arn.of_json in
      make ~schemaArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a schema. Before deleting a schema, you must delete all datasets referencing the schema. For more information on schemas, see CreateSchema."]
module DeleteRecommenderRequest =
  struct
    type nonrec t =
      {
      recommenderArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the recommender to delete."]}
    let context_ = "DeleteRecommenderRequest"
    let make ~recommenderArn = fun () -> { recommenderArn }
    let to_value x =
      structure_to_value
        [("recommenderArn", (Some (Arn.to_value x.recommenderArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recommenderArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "recommenderArn") in
      make ~recommenderArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recommenderArn = field_map_exn json "recommenderArn" Arn.of_json in
      make ~recommenderArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deactivates and removes a recommender. A deleted recommender can no longer be specified in a GetRecommendations request."]
module DeleteFilterRequest =
  struct
    type nonrec t =
      {
      filterArn: Arn.t [@ocaml.doc "The ARN of the filter to delete."]}
    let context_ = "DeleteFilterRequest"
    let make ~filterArn = fun () -> { filterArn }
    let to_value x =
      structure_to_value [("filterArn", (Some (Arn.to_value x.filterArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filterArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "filterArn") in
      make ~filterArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let filterArn = field_map_exn json "filterArn" Arn.of_json in
      make ~filterArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a filter."]
module DeleteEventTrackerRequest =
  struct
    type nonrec t =
      {
      eventTrackerArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the event tracker to delete."]}
    let context_ = "DeleteEventTrackerRequest"
    let make ~eventTrackerArn = fun () -> { eventTrackerArn }
    let to_value x =
      structure_to_value
        [("eventTrackerArn", (Some (Arn.to_value x.eventTrackerArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventTrackerArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "eventTrackerArn") in
      make ~eventTrackerArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let eventTrackerArn = field_map_exn json "eventTrackerArn" Arn.of_json in
      make ~eventTrackerArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the event tracker. Does not delete the event-interactions dataset from the associated dataset group. For more information on event trackers, see CreateEventTracker."]
module DeleteDatasetRequest =
  struct
    type nonrec t =
      {
      datasetArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset to delete."]}
    let context_ = "DeleteDatasetRequest"
    let make ~datasetArn = fun () -> { datasetArn }
    let to_value x =
      structure_to_value [("datasetArn", (Some (Arn.to_value x.datasetArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let datasetArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "datasetArn") in
      make ~datasetArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let datasetArn = field_map_exn json "datasetArn" Arn.of_json in
      make ~datasetArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a dataset. You can't delete a dataset if an associated DatasetImportJob or SolutionVersion is in the CREATE PENDING or IN PROGRESS state. For more information on datasets, see CreateDataset."]
module DeleteDatasetGroupRequest =
  struct
    type nonrec t =
      {
      datasetGroupArn: Arn.t
        [@ocaml.doc "The ARN of the dataset group to delete."]}
    let context_ = "DeleteDatasetGroupRequest"
    let make ~datasetGroupArn = fun () -> { datasetGroupArn }
    let to_value x =
      structure_to_value
        [("datasetGroupArn", (Some (Arn.to_value x.datasetGroupArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let datasetGroupArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "datasetGroupArn") in
      make ~datasetGroupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let datasetGroupArn = field_map_exn json "datasetGroupArn" Arn.of_json in
      make ~datasetGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a dataset group. Before you delete a dataset group, you must delete the following: All associated event trackers. All associated solutions. All datasets in the dataset group."]
module DeleteCampaignRequest =
  struct
    type nonrec t =
      {
      campaignArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the campaign to delete."]}
    let context_ = "DeleteCampaignRequest"
    let make ~campaignArn = fun () -> { campaignArn }
    let to_value x =
      structure_to_value
        [("campaignArn", (Some (Arn.to_value x.campaignArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let campaignArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "campaignArn") in
      make ~campaignArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let campaignArn = field_map_exn json "campaignArn" Arn.of_json in
      make ~campaignArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes a campaign by deleting the solution deployment. The solution that the campaign is based on is not deleted and can be redeployed when needed. A deleted campaign can no longer be specified in a GetRecommendations request. For information on creating campaigns, see CreateCampaign."]
module CreateSolutionVersionResponse =
  struct
    type nonrec t =
      {
      solutionVersionArn: Arn.t option
        [@ocaml.doc "The ARN of the new solution version."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?solutionVersionArn = fun () -> { solutionVersionArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("solutionVersionArn",
           (Option.map x.solutionVersionArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let solutionVersionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionVersionArn") in
      make ?solutionVersionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let solutionVersionArn =
        field_map json "solutionVersionArn" Arn.of_json in
      make ?solutionVersionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Trains or retrains an active solution in a Custom dataset group. A solution is created using the CreateSolution operation and must be in the ACTIVE state before calling CreateSolutionVersion. A new version of the solution is created every time you call this operation. Status A solution version can be in one of the following states: CREATE PENDING CREATE IN_PROGRESS ACTIVE CREATE FAILED CREATE STOPPING CREATE STOPPED To get the status of the version, call DescribeSolutionVersion. Wait until the status shows as ACTIVE before calling CreateCampaign. If the status shows as CREATE FAILED, the response includes a failureReason key, which describes why the job failed. Related APIs ListSolutionVersions DescribeSolutionVersion ListSolutions CreateSolution DescribeSolution DeleteSolution"]
module CreateSolutionVersionRequest =
  struct
    type nonrec t =
      {
      solutionArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution containing the training configuration information."];
      trainingMode: TrainingMode.t option
        [@ocaml.doc
          "The scope of training to be performed when creating the solution version. The FULL option trains the solution version based on the entirety of the input solution's training data, while the UPDATE option processes only the data that has changed in comparison to the input solution. Choose UPDATE when you want to incrementally update your solution version instead of creating an entirely new one. The UPDATE option can only be used when you already have an active solution version created from the input solution using the FULL option and the input solution was trained with the User-Personalization recipe or the HRNN-Coldstart recipe."];
      tags: Tags.t option
        [@ocaml.doc "A list of tags to apply to the solution version."]}
    let context_ = "CreateSolutionVersionRequest"
    let make ?trainingMode =
      fun ?tags ->
        fun ~solutionArn -> fun () -> { trainingMode; tags; solutionArn }
    let to_value x =
      structure_to_value
        [("solutionArn", (Some (Arn.to_value x.solutionArn)));
        ("trainingMode",
          (Option.map x.trainingMode ~f:TrainingMode.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let trainingMode =
        (Option.map ~f:TrainingMode.of_xml)
          (Xml.child xml_arg0 "trainingMode") in
      let solutionArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "solutionArn") in
      make ?tags ?trainingMode ~solutionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let trainingMode = field_map json "trainingMode" TrainingMode.of_json in
      let solutionArn = field_map_exn json "solutionArn" Arn.of_json in
      make ?tags ?trainingMode ~solutionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Trains or retrains an active solution in a Custom dataset group. A solution is created using the CreateSolution operation and must be in the ACTIVE state before calling CreateSolutionVersion. A new version of the solution is created every time you call this operation. Status A solution version can be in one of the following states: CREATE PENDING CREATE IN_PROGRESS ACTIVE CREATE FAILED CREATE STOPPING CREATE STOPPED To get the status of the version, call DescribeSolutionVersion. Wait until the status shows as ACTIVE before calling CreateCampaign. If the status shows as CREATE FAILED, the response includes a failureReason key, which describes why the job failed. Related APIs ListSolutionVersions DescribeSolutionVersion ListSolutions CreateSolution DescribeSolution DeleteSolution"]
module CreateSolutionResponse =
  struct
    type nonrec t =
      {
      solutionArn: Arn.t option [@ocaml.doc "The ARN of the solution."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?solutionArn = fun () -> { solutionArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("solutionArn", (Option.map x.solutionArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let solutionArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "solutionArn") in
      make ?solutionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let solutionArn = field_map json "solutionArn" Arn.of_json in
      make ?solutionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates the configuration for training a model. A trained model is known as a solution. After the configuration is created, you train the model (create a solution) by calling the CreateSolutionVersion operation. Every time you call CreateSolutionVersion, a new version of the solution is created. After creating a solution version, you check its accuracy by calling GetSolutionMetrics. When you are satisfied with the version, you deploy it using CreateCampaign. The campaign provides recommendations to a client through the GetRecommendations API. To train a model, Amazon Personalize requires training data and a recipe. The training data comes from the dataset group that you provide in the request. A recipe specifies the training algorithm and a feature transformation. You can specify one of the predefined recipes provided by Amazon Personalize. Alternatively, you can specify performAutoML and Amazon Personalize will analyze your data and select the optimum USER_PERSONALIZATION recipe for you. Amazon Personalize doesn't support configuring the hpoObjective for solution hyperparameter optimization at this time. Status A solution can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS To get the status of the solution, call DescribeSolution. Wait until the status shows as ACTIVE before calling CreateSolutionVersion. Related APIs ListSolutions CreateSolutionVersion DescribeSolution DeleteSolution ListSolutionVersions DescribeSolutionVersion"]
module CreateSolutionRequest =
  struct
    type nonrec t =
      {
      name: Name.t [@ocaml.doc "The name for the solution."];
      performHPO: Boolean.t option
        [@ocaml.doc
          "Whether to perform hyperparameter optimization (HPO) on the specified or selected recipe. The default is false. When performing AutoML, this parameter is always true and you should not set it to false."];
      performAutoML: PerformAutoML.t option
        [@ocaml.doc
          "Whether to perform automated machine learning (AutoML). The default is false. For this case, you must specify recipeArn. When set to true, Amazon Personalize analyzes your training data and selects the optimal USER_PERSONALIZATION recipe and hyperparameters. In this case, you must omit recipeArn. Amazon Personalize determines the optimal recipe by running tests with different values for the hyperparameters. AutoML lengthens the training process as compared to selecting a specific recipe."];
      recipeArn: Arn.t option
        [@ocaml.doc
          "The ARN of the recipe to use for model training. Only specified when performAutoML is false."];
      datasetGroupArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset group that provides the training data."];
      eventType: EventType.t option
        [@ocaml.doc
          "When your have multiple event types (using an EVENT_TYPE schema field), this parameter specifies which event type (for example, 'click' or 'like') is used for training the model. If you do not provide an eventType, Amazon Personalize will use all interactions for training with equal weight regardless of type."];
      solutionConfig: SolutionConfig.t option
        [@ocaml.doc
          "The configuration to use with the solution. When performAutoML is set to true, Amazon Personalize only evaluates the autoMLConfig section of the solution configuration. Amazon Personalize doesn't support configuring the hpoObjective at this time."];
      tags: Tags.t option
        [@ocaml.doc "A list of tags to apply to the solution."]}
    let context_ = "CreateSolutionRequest"
    let make ?performHPO =
      fun ?performAutoML ->
        fun ?recipeArn ->
          fun ?eventType ->
            fun ?solutionConfig ->
              fun ?tags ->
                fun ~name ->
                  fun ~datasetGroupArn ->
                    fun () ->
                      {
                        performHPO;
                        performAutoML;
                        recipeArn;
                        eventType;
                        solutionConfig;
                        tags;
                        name;
                        datasetGroupArn
                      }
    let to_value x =
      structure_to_value
        [("name", (Some (Name.to_value x.name)));
        ("performHPO", (Option.map x.performHPO ~f:Boolean.to_value));
        ("performAutoML",
          (Option.map x.performAutoML ~f:PerformAutoML.to_value));
        ("recipeArn", (Option.map x.recipeArn ~f:Arn.to_value));
        ("datasetGroupArn", (Some (Arn.to_value x.datasetGroupArn)));
        ("eventType", (Option.map x.eventType ~f:EventType.to_value));
        ("solutionConfig",
          (Option.map x.solutionConfig ~f:SolutionConfig.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let solutionConfig =
        (Option.map ~f:SolutionConfig.of_xml)
          (Xml.child xml_arg0 "solutionConfig") in
      let eventType =
        (Option.map ~f:EventType.of_xml) (Xml.child xml_arg0 "eventType") in
      let datasetGroupArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "datasetGroupArn") in
      let recipeArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "recipeArn") in
      let performAutoML =
        (Option.map ~f:PerformAutoML.of_xml)
          (Xml.child xml_arg0 "performAutoML") in
      let performHPO =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "performHPO") in
      let name =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ?solutionConfig ?eventType ~datasetGroupArn ?recipeArn
        ?performAutoML ?performHPO ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let solutionConfig =
        field_map json "solutionConfig" SolutionConfig.of_json in
      let eventType = field_map json "eventType" EventType.of_json in
      let datasetGroupArn = field_map_exn json "datasetGroupArn" Arn.of_json in
      let recipeArn = field_map json "recipeArn" Arn.of_json in
      let performAutoML =
        field_map json "performAutoML" PerformAutoML.of_json in
      let performHPO = field_map json "performHPO" Boolean.of_json in
      let name = field_map_exn json "name" Name.of_json in
      make ?tags ?solutionConfig ?eventType ~datasetGroupArn ?recipeArn
        ?performAutoML ?performHPO ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates the configuration for training a model. A trained model is known as a solution. After the configuration is created, you train the model (create a solution) by calling the CreateSolutionVersion operation. Every time you call CreateSolutionVersion, a new version of the solution is created. After creating a solution version, you check its accuracy by calling GetSolutionMetrics. When you are satisfied with the version, you deploy it using CreateCampaign. The campaign provides recommendations to a client through the GetRecommendations API. To train a model, Amazon Personalize requires training data and a recipe. The training data comes from the dataset group that you provide in the request. A recipe specifies the training algorithm and a feature transformation. You can specify one of the predefined recipes provided by Amazon Personalize. Alternatively, you can specify performAutoML and Amazon Personalize will analyze your data and select the optimum USER_PERSONALIZATION recipe for you. Amazon Personalize doesn't support configuring the hpoObjective for solution hyperparameter optimization at this time. Status A solution can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS To get the status of the solution, call DescribeSolution. Wait until the status shows as ACTIVE before calling CreateSolutionVersion. Related APIs ListSolutions CreateSolutionVersion DescribeSolution DeleteSolution ListSolutionVersions DescribeSolutionVersion"]
module CreateSchemaResponse =
  struct
    type nonrec t =
      {
      schemaArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the created schema."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?schemaArn = fun () -> { schemaArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("schemaArn", (Option.map x.schemaArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let schemaArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "schemaArn") in
      make ?schemaArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let schemaArn = field_map json "schemaArn" Arn.of_json in
      make ?schemaArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an Amazon Personalize schema from the specified schema string. The schema you create must be in Avro JSON format. Amazon Personalize recognizes three schema variants. Each schema is associated with a dataset type and has a set of required field and keywords. If you are creating a schema for a dataset in a Domain dataset group, you provide the domain of the Domain dataset group. You specify a schema when you call CreateDataset. Related APIs ListSchemas DescribeSchema DeleteSchema"]
module CreateSchemaRequest =
  struct
    type nonrec t =
      {
      name: Name.t [@ocaml.doc "The name for the schema."];
      schema: AvroSchema.t [@ocaml.doc "A schema in Avro JSON format."];
      domain: Domain.t option
        [@ocaml.doc
          "The domain for the schema. If you are creating a schema for a dataset in a Domain dataset group, specify the domain you chose when you created the Domain dataset group."]}
    let context_ = "CreateSchemaRequest"
    let make ?domain =
      fun ~name -> fun ~schema -> fun () -> { domain; name; schema }
    let to_value x =
      structure_to_value
        [("name", (Some (Name.to_value x.name)));
        ("schema", (Some (AvroSchema.to_value x.schema)));
        ("domain", (Option.map x.domain ~f:Domain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        (Option.map ~f:Domain.of_xml) (Xml.child xml_arg0 "domain") in
      let schema =
        AvroSchema.of_xml (Xml.child_exn ~context:context_ xml_arg0 "schema") in
      let name =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?domain ~schema ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let domain = field_map json "domain" Domain.of_json in
      let schema = field_map_exn json "schema" AvroSchema.of_json in
      let name = field_map_exn json "name" Name.of_json in
      make ?domain ~schema ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an Amazon Personalize schema from the specified schema string. The schema you create must be in Avro JSON format. Amazon Personalize recognizes three schema variants. Each schema is associated with a dataset type and has a set of required field and keywords. If you are creating a schema for a dataset in a Domain dataset group, you provide the domain of the Domain dataset group. You specify a schema when you call CreateDataset. Related APIs ListSchemas DescribeSchema DeleteSchema"]
module CreateRecommenderResponse =
  struct
    type nonrec t =
      {
      recommenderArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the recommender."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recommenderArn = fun () -> { recommenderArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("recommenderArn", (Option.map x.recommenderArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recommenderArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "recommenderArn") in
      make ?recommenderArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recommenderArn = field_map json "recommenderArn" Arn.of_json in
      make ?recommenderArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a recommender with the recipe (a Domain dataset group use case) you specify. You create recommenders for a Domain dataset group and specify the recommender's Amazon Resource Name (ARN) when you make a GetRecommendations request. Minimum recommendation requests per second When you create a recommender, you can configure the recommender's minimum recommendation requests per second. The minimum recommendation requests per second (minRecommendationRequestsPerSecond) specifies the baseline recommendation request throughput provisioned by Amazon Personalize. The default minRecommendationRequestsPerSecond is 1. A recommendation request is a single GetRecommendations operation. Request throughput is measured in requests per second and Amazon Personalize uses your requests per second to derive your requests per hour and the price of your recommender usage. If your requests per second increases beyond minRecommendationRequestsPerSecond, Amazon Personalize auto-scales the provisioned capacity up and down, but never below minRecommendationRequestsPerSecond. There's a short time delay while the capacity is increased that might cause loss of requests. Your bill is the greater of either the minimum requests per hour (based on minRecommendationRequestsPerSecond) or the actual number of requests. The actual request throughput used is calculated as the average requests/second within a one-hour window. We recommend starting with the default minRecommendationRequestsPerSecond, track your usage using Amazon CloudWatch metrics, and then increase the minRecommendationRequestsPerSecond as necessary. Status A recommender can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS To get the recommender status, call DescribeRecommender. Wait until the status of the recommender is ACTIVE before asking the recommender for recommendations. Related APIs ListRecommenders DescribeRecommender UpdateRecommender DeleteRecommender"]
module CreateRecommenderRequest =
  struct
    type nonrec t =
      {
      name: Name.t [@ocaml.doc "The name of the recommender."];
      datasetGroupArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the destination domain dataset group for the recommender."];
      recipeArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the recipe that the recommender will use. For a recommender, a recipe is a Domain dataset group use case. Only Domain dataset group use cases can be used to create a recommender. For information about use cases see Choosing recommender use cases."];
      recommenderConfig: RecommenderConfig.t option
        [@ocaml.doc "The configuration details of the recommender."];
      tags: Tags.t option
        [@ocaml.doc "A list of tags to apply to the recommender."]}
    let context_ = "CreateRecommenderRequest"
    let make ?recommenderConfig =
      fun ?tags ->
        fun ~name ->
          fun ~datasetGroupArn ->
            fun ~recipeArn ->
              fun () ->
                { recommenderConfig; tags; name; datasetGroupArn; recipeArn }
    let to_value x =
      structure_to_value
        [("name", (Some (Name.to_value x.name)));
        ("datasetGroupArn", (Some (Arn.to_value x.datasetGroupArn)));
        ("recipeArn", (Some (Arn.to_value x.recipeArn)));
        ("recommenderConfig",
          (Option.map x.recommenderConfig ~f:RecommenderConfig.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let recommenderConfig =
        (Option.map ~f:RecommenderConfig.of_xml)
          (Xml.child xml_arg0 "recommenderConfig") in
      let recipeArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "recipeArn") in
      let datasetGroupArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "datasetGroupArn") in
      let name =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ?recommenderConfig ~recipeArn ~datasetGroupArn ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let recommenderConfig =
        field_map json "recommenderConfig" RecommenderConfig.of_json in
      let recipeArn = field_map_exn json "recipeArn" Arn.of_json in
      let datasetGroupArn = field_map_exn json "datasetGroupArn" Arn.of_json in
      let name = field_map_exn json "name" Name.of_json in
      make ?tags ?recommenderConfig ~recipeArn ~datasetGroupArn ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a recommender with the recipe (a Domain dataset group use case) you specify. You create recommenders for a Domain dataset group and specify the recommender's Amazon Resource Name (ARN) when you make a GetRecommendations request. Minimum recommendation requests per second When you create a recommender, you can configure the recommender's minimum recommendation requests per second. The minimum recommendation requests per second (minRecommendationRequestsPerSecond) specifies the baseline recommendation request throughput provisioned by Amazon Personalize. The default minRecommendationRequestsPerSecond is 1. A recommendation request is a single GetRecommendations operation. Request throughput is measured in requests per second and Amazon Personalize uses your requests per second to derive your requests per hour and the price of your recommender usage. If your requests per second increases beyond minRecommendationRequestsPerSecond, Amazon Personalize auto-scales the provisioned capacity up and down, but never below minRecommendationRequestsPerSecond. There's a short time delay while the capacity is increased that might cause loss of requests. Your bill is the greater of either the minimum requests per hour (based on minRecommendationRequestsPerSecond) or the actual number of requests. The actual request throughput used is calculated as the average requests/second within a one-hour window. We recommend starting with the default minRecommendationRequestsPerSecond, track your usage using Amazon CloudWatch metrics, and then increase the minRecommendationRequestsPerSecond as necessary. Status A recommender can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS To get the recommender status, call DescribeRecommender. Wait until the status of the recommender is ACTIVE before asking the recommender for recommendations. Related APIs ListRecommenders DescribeRecommender UpdateRecommender DeleteRecommender"]
module CreateFilterResponse =
  struct
    type nonrec t =
      {
      filterArn: Arn.t option [@ocaml.doc "The ARN of the new filter."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?filterArn = fun () -> { filterArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("filterArn", (Option.map x.filterArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filterArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "filterArn") in
      make ?filterArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let filterArn = field_map json "filterArn" Arn.of_json in
      make ?filterArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a recommendation filter. For more information, see Filtering recommendations and user segments."]
module CreateFilterRequest =
  struct
    type nonrec t =
      {
      name: Name.t [@ocaml.doc "The name of the filter to create."];
      datasetGroupArn: Arn.t
        [@ocaml.doc
          "The ARN of the dataset group that the filter will belong to."];
      filterExpression: FilterExpression.t
        [@ocaml.doc
          "The filter expression defines which items are included or excluded from recommendations. Filter expression must follow specific format rules. For information about filter expression structure and syntax, see Filter expressions."];
      tags: Tags.t option
        [@ocaml.doc "A list of tags to apply to the filter."]}
    let context_ = "CreateFilterRequest"
    let make ?tags =
      fun ~name ->
        fun ~datasetGroupArn ->
          fun ~filterExpression ->
            fun () -> { tags; name; datasetGroupArn; filterExpression }
    let to_value x =
      structure_to_value
        [("name", (Some (Name.to_value x.name)));
        ("datasetGroupArn", (Some (Arn.to_value x.datasetGroupArn)));
        ("filterExpression",
          (Some (FilterExpression.to_value x.filterExpression)));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let filterExpression =
        FilterExpression.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "filterExpression") in
      let datasetGroupArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "datasetGroupArn") in
      let name =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ~filterExpression ~datasetGroupArn ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let filterExpression =
        field_map_exn json "filterExpression" FilterExpression.of_json in
      let datasetGroupArn = field_map_exn json "datasetGroupArn" Arn.of_json in
      let name = field_map_exn json "name" Name.of_json in
      make ?tags ~filterExpression ~datasetGroupArn ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a recommendation filter. For more information, see Filtering recommendations and user segments."]
module CreateEventTrackerResponse =
  struct
    type nonrec t =
      {
      eventTrackerArn: Arn.t option
        [@ocaml.doc "The ARN of the event tracker."];
      trackingId: TrackingId.t option
        [@ocaml.doc
          "The ID of the event tracker. Include this ID in requests to the PutEvents API."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?eventTrackerArn =
      fun ?trackingId -> fun () -> { eventTrackerArn; trackingId }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("eventTrackerArn", (Option.map x.eventTrackerArn ~f:Arn.to_value));
        ("trackingId", (Option.map x.trackingId ~f:TrackingId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trackingId =
        (Option.map ~f:TrackingId.of_xml) (Xml.child xml_arg0 "trackingId") in
      let eventTrackerArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "eventTrackerArn") in
      make ?trackingId ?eventTrackerArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let trackingId = field_map json "trackingId" TrackingId.of_json in
      let eventTrackerArn = field_map json "eventTrackerArn" Arn.of_json in
      make ?trackingId ?eventTrackerArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an event tracker that you use when adding event data to a specified dataset group using the PutEvents API. Only one event tracker can be associated with a dataset group. You will get an error if you call CreateEventTracker using the same dataset group as an existing event tracker. When you create an event tracker, the response includes a tracking ID, which you pass as a parameter when you use the PutEvents operation. Amazon Personalize then appends the event data to the Interactions dataset of the dataset group you specify in your event tracker. The event tracker can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS To get the status of the event tracker, call DescribeEventTracker. The event tracker must be in the ACTIVE state before using the tracking ID. Related APIs ListEventTrackers DescribeEventTracker DeleteEventTracker"]
module CreateEventTrackerRequest =
  struct
    type nonrec t =
      {
      name: Name.t [@ocaml.doc "The name for the event tracker."];
      datasetGroupArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset group that receives the event data."];
      tags: Tags.t option
        [@ocaml.doc "A list of tags to apply to the event tracker."]}
    let context_ = "CreateEventTrackerRequest"
    let make ?tags =
      fun ~name ->
        fun ~datasetGroupArn -> fun () -> { tags; name; datasetGroupArn }
    let to_value x =
      structure_to_value
        [("name", (Some (Name.to_value x.name)));
        ("datasetGroupArn", (Some (Arn.to_value x.datasetGroupArn)));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let datasetGroupArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "datasetGroupArn") in
      let name =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ~datasetGroupArn ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let datasetGroupArn = field_map_exn json "datasetGroupArn" Arn.of_json in
      let name = field_map_exn json "name" Name.of_json in
      make ?tags ~datasetGroupArn ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an event tracker that you use when adding event data to a specified dataset group using the PutEvents API. Only one event tracker can be associated with a dataset group. You will get an error if you call CreateEventTracker using the same dataset group as an existing event tracker. When you create an event tracker, the response includes a tracking ID, which you pass as a parameter when you use the PutEvents operation. Amazon Personalize then appends the event data to the Interactions dataset of the dataset group you specify in your event tracker. The event tracker can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS To get the status of the event tracker, call DescribeEventTracker. The event tracker must be in the ACTIVE state before using the tracking ID. Related APIs ListEventTrackers DescribeEventTracker DeleteEventTracker"]
module CreateDatasetResponse =
  struct
    type nonrec t =
      {
      datasetArn: Arn.t option [@ocaml.doc "The ARN of the dataset."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?datasetArn = fun () -> { datasetArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("datasetArn", (Option.map x.datasetArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let datasetArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetArn") in
      make ?datasetArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let datasetArn = field_map json "datasetArn" Arn.of_json in
      make ?datasetArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an empty dataset and adds it to the specified dataset group. Use CreateDatasetImportJob to import your training data to a dataset. There are three types of datasets: Interactions Items Users Each dataset type has an associated schema with required field types. Only the Interactions dataset is required in order to train a model (also referred to as creating a solution). A dataset can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS To get the status of the dataset, call DescribeDataset. Related APIs CreateDatasetGroup ListDatasets DescribeDataset DeleteDataset"]
module CreateDatasetRequest =
  struct
    type nonrec t =
      {
      name: Name.t [@ocaml.doc "The name for the dataset."];
      schemaArn: Arn.t
        [@ocaml.doc
          "The ARN of the schema to associate with the dataset. The schema defines the dataset fields."];
      datasetGroupArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset group to add the dataset to."];
      datasetType: DatasetType.t
        [@ocaml.doc
          "The type of dataset. One of the following (case insensitive) values: Interactions Items Users"];
      tags: Tags.t option
        [@ocaml.doc "A list of tags to apply to the dataset."]}
    let context_ = "CreateDatasetRequest"
    let make ?tags =
      fun ~name ->
        fun ~schemaArn ->
          fun ~datasetGroupArn ->
            fun ~datasetType ->
              fun () ->
                { tags; name; schemaArn; datasetGroupArn; datasetType }
    let to_value x =
      structure_to_value
        [("name", (Some (Name.to_value x.name)));
        ("schemaArn", (Some (Arn.to_value x.schemaArn)));
        ("datasetGroupArn", (Some (Arn.to_value x.datasetGroupArn)));
        ("datasetType", (Some (DatasetType.to_value x.datasetType)));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let datasetType =
        DatasetType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "datasetType") in
      let datasetGroupArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "datasetGroupArn") in
      let schemaArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "schemaArn") in
      let name =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ~datasetType ~datasetGroupArn ~schemaArn ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let datasetType = field_map_exn json "datasetType" DatasetType.of_json in
      let datasetGroupArn = field_map_exn json "datasetGroupArn" Arn.of_json in
      let schemaArn = field_map_exn json "schemaArn" Arn.of_json in
      let name = field_map_exn json "name" Name.of_json in
      make ?tags ~datasetType ~datasetGroupArn ~schemaArn ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an empty dataset and adds it to the specified dataset group. Use CreateDatasetImportJob to import your training data to a dataset. There are three types of datasets: Interactions Items Users Each dataset type has an associated schema with required field types. Only the Interactions dataset is required in order to train a model (also referred to as creating a solution). A dataset can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS To get the status of the dataset, call DescribeDataset. Related APIs CreateDatasetGroup ListDatasets DescribeDataset DeleteDataset"]
module CreateDatasetImportJobResponse =
  struct
    type nonrec t =
      {
      datasetImportJobArn: Arn.t option
        [@ocaml.doc "The ARN of the dataset import job."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?datasetImportJobArn = fun () -> { datasetImportJobArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("datasetImportJobArn",
           (Option.map x.datasetImportJobArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let datasetImportJobArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetImportJobArn") in
      make ?datasetImportJobArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let datasetImportJobArn =
        field_map json "datasetImportJobArn" Arn.of_json in
      make ?datasetImportJobArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a job that imports training data from your data source (an Amazon S3 bucket) to an Amazon Personalize dataset. To allow Amazon Personalize to import the training data, you must specify an IAM service role that has permission to read from the data source, as Amazon Personalize makes a copy of your data and processes it internally. For information on granting access to your Amazon S3 bucket, see Giving Amazon Personalize Access to Amazon S3 Resources. The dataset import job replaces any existing data in the dataset that you imported in bulk. Status A dataset import job can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED To get the status of the import job, call DescribeDatasetImportJob, providing the Amazon Resource Name (ARN) of the dataset import job. The dataset import is complete when the status shows as ACTIVE. If the status shows as CREATE FAILED, the response includes a failureReason key, which describes why the job failed. Importing takes time. You must wait until the status shows as ACTIVE before training a model using the dataset. Related APIs ListDatasetImportJobs DescribeDatasetImportJob"]
module CreateDatasetImportJobRequest =
  struct
    type nonrec t =
      {
      jobName: Name.t [@ocaml.doc "The name for the dataset import job."];
      datasetArn: Arn.t
        [@ocaml.doc
          "The ARN of the dataset that receives the imported data."];
      dataSource: DataSource.t
        [@ocaml.doc
          "The Amazon S3 bucket that contains the training data to import."];
      roleArn: RoleArn.t
        [@ocaml.doc
          "The ARN of the IAM role that has permissions to read from the Amazon S3 data source."];
      tags: Tags.t option
        [@ocaml.doc "A list of tags to apply to the dataset import job."]}
    let context_ = "CreateDatasetImportJobRequest"
    let make ?tags =
      fun ~jobName ->
        fun ~datasetArn ->
          fun ~dataSource ->
            fun ~roleArn ->
              fun () -> { tags; jobName; datasetArn; dataSource; roleArn }
    let to_value x =
      structure_to_value
        [("jobName", (Some (Name.to_value x.jobName)));
        ("datasetArn", (Some (Arn.to_value x.datasetArn)));
        ("dataSource", (Some (DataSource.to_value x.dataSource)));
        ("roleArn", (Some (RoleArn.to_value x.roleArn)));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let roleArn =
        RoleArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "roleArn") in
      let dataSource =
        DataSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "dataSource") in
      let datasetArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "datasetArn") in
      let jobName =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "jobName") in
      make ?tags ~roleArn ~dataSource ~datasetArn ~jobName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let roleArn = field_map_exn json "roleArn" RoleArn.of_json in
      let dataSource = field_map_exn json "dataSource" DataSource.of_json in
      let datasetArn = field_map_exn json "datasetArn" Arn.of_json in
      let jobName = field_map_exn json "jobName" Name.of_json in
      make ?tags ~roleArn ~dataSource ~datasetArn ~jobName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a job that imports training data from your data source (an Amazon S3 bucket) to an Amazon Personalize dataset. To allow Amazon Personalize to import the training data, you must specify an IAM service role that has permission to read from the data source, as Amazon Personalize makes a copy of your data and processes it internally. For information on granting access to your Amazon S3 bucket, see Giving Amazon Personalize Access to Amazon S3 Resources. The dataset import job replaces any existing data in the dataset that you imported in bulk. Status A dataset import job can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED To get the status of the import job, call DescribeDatasetImportJob, providing the Amazon Resource Name (ARN) of the dataset import job. The dataset import is complete when the status shows as ACTIVE. If the status shows as CREATE FAILED, the response includes a failureReason key, which describes why the job failed. Importing takes time. You must wait until the status shows as ACTIVE before training a model using the dataset. Related APIs ListDatasetImportJobs DescribeDatasetImportJob"]
module CreateDatasetGroupResponse =
  struct
    type nonrec t =
      {
      datasetGroupArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the new dataset group."];
      domain: Domain.t option
        [@ocaml.doc "The domain for the new Domain dataset group."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?datasetGroupArn =
      fun ?domain -> fun () -> { datasetGroupArn; domain }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("datasetGroupArn", (Option.map x.datasetGroupArn ~f:Arn.to_value));
        ("domain", (Option.map x.domain ~f:Domain.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        (Option.map ~f:Domain.of_xml) (Xml.child xml_arg0 "domain") in
      let datasetGroupArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetGroupArn") in
      make ?domain ?datasetGroupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let domain = field_map json "domain" Domain.of_json in
      let datasetGroupArn = field_map json "datasetGroupArn" Arn.of_json in
      make ?domain ?datasetGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an empty dataset group. A dataset group is a container for Amazon Personalize resources. A dataset group can contain at most three datasets, one for each type of dataset: Interactions Items Users A dataset group can be a Domain dataset group, where you specify a domain and use pre-configured resources like recommenders, or a Custom dataset group, where you use custom resources, such as a solution with a solution version, that you deploy with a campaign. If you start with a Domain dataset group, you can still add custom resources such as solutions and solution versions trained with recipes for custom use cases and deployed with campaigns. A dataset group can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING To get the status of the dataset group, call DescribeDatasetGroup. If the status shows as CREATE FAILED, the response includes a failureReason key, which describes why the creation failed. You must wait until the status of the dataset group is ACTIVE before adding a dataset to the group. You can specify an Key Management Service (KMS) key to encrypt the datasets in the group. If you specify a KMS key, you must also include an Identity and Access Management (IAM) role that has permission to access the key. APIs that require a dataset group ARN in the request CreateDataset CreateEventTracker CreateSolution Related APIs ListDatasetGroups DescribeDatasetGroup DeleteDatasetGroup"]
module CreateDatasetGroupRequest =
  struct
    type nonrec t =
      {
      name: Name.t [@ocaml.doc "The name for the new dataset group."];
      roleArn: RoleArn.t option
        [@ocaml.doc
          "The ARN of the Identity and Access Management (IAM) role that has permissions to access the Key Management Service (KMS) key. Supplying an IAM role is only valid when also specifying a KMS key."];
      kmsKeyArn: KmsKeyArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of a Key Management Service (KMS) key used to encrypt the datasets."];
      domain: Domain.t option
        [@ocaml.doc
          "The domain of the dataset group. Specify a domain to create a Domain dataset group. The domain you specify determines the default schemas for datasets and the use cases available for recommenders. If you don't specify a domain, you create a Custom dataset group with solution versions that you deploy with a campaign."];
      tags: Tags.t option
        [@ocaml.doc "A list of tags to apply to the dataset group."]}
    let context_ = "CreateDatasetGroupRequest"
    let make ?roleArn =
      fun ?kmsKeyArn ->
        fun ?domain ->
          fun ?tags ->
            fun ~name -> fun () -> { roleArn; kmsKeyArn; domain; tags; name }
    let to_value x =
      structure_to_value
        [("name", (Some (Name.to_value x.name)));
        ("roleArn", (Option.map x.roleArn ~f:RoleArn.to_value));
        ("kmsKeyArn", (Option.map x.kmsKeyArn ~f:KmsKeyArn.to_value));
        ("domain", (Option.map x.domain ~f:Domain.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let domain =
        (Option.map ~f:Domain.of_xml) (Xml.child xml_arg0 "domain") in
      let kmsKeyArn =
        (Option.map ~f:KmsKeyArn.of_xml) (Xml.child xml_arg0 "kmsKeyArn") in
      let roleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let name =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ?domain ?kmsKeyArn ?roleArn ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let domain = field_map json "domain" Domain.of_json in
      let kmsKeyArn = field_map json "kmsKeyArn" KmsKeyArn.of_json in
      let roleArn = field_map json "roleArn" RoleArn.of_json in
      let name = field_map_exn json "name" Name.of_json in
      make ?tags ?domain ?kmsKeyArn ?roleArn ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an empty dataset group. A dataset group is a container for Amazon Personalize resources. A dataset group can contain at most three datasets, one for each type of dataset: Interactions Items Users A dataset group can be a Domain dataset group, where you specify a domain and use pre-configured resources like recommenders, or a Custom dataset group, where you use custom resources, such as a solution with a solution version, that you deploy with a campaign. If you start with a Domain dataset group, you can still add custom resources such as solutions and solution versions trained with recipes for custom use cases and deployed with campaigns. A dataset group can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING To get the status of the dataset group, call DescribeDatasetGroup. If the status shows as CREATE FAILED, the response includes a failureReason key, which describes why the creation failed. You must wait until the status of the dataset group is ACTIVE before adding a dataset to the group. You can specify an Key Management Service (KMS) key to encrypt the datasets in the group. If you specify a KMS key, you must also include an Identity and Access Management (IAM) role that has permission to access the key. APIs that require a dataset group ARN in the request CreateDataset CreateEventTracker CreateSolution Related APIs ListDatasetGroups DescribeDatasetGroup DeleteDatasetGroup"]
module CreateDatasetExportJobResponse =
  struct
    type nonrec t =
      {
      datasetExportJobArn: Arn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset export job."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?datasetExportJobArn = fun () -> { datasetExportJobArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("datasetExportJobArn",
           (Option.map x.datasetExportJobArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let datasetExportJobArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "datasetExportJobArn") in
      make ?datasetExportJobArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let datasetExportJobArn =
        field_map json "datasetExportJobArn" Arn.of_json in
      make ?datasetExportJobArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a job that exports data from your dataset to an Amazon S3 bucket. To allow Amazon Personalize to export the training data, you must specify an service-linked IAM role that gives Amazon Personalize PutObject permissions for your Amazon S3 bucket. For information, see Exporting a dataset in the Amazon Personalize developer guide. Status A dataset export job can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED To get the status of the export job, call DescribeDatasetExportJob, and specify the Amazon Resource Name (ARN) of the dataset export job. The dataset export is complete when the status shows as ACTIVE. If the status shows as CREATE FAILED, the response includes a failureReason key, which describes why the job failed."]
module CreateDatasetExportJobRequest =
  struct
    type nonrec t =
      {
      jobName: Name.t [@ocaml.doc "The name for the dataset export job."];
      datasetArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the dataset that contains the data to export."];
      ingestionMode: IngestionMode.t option
        [@ocaml.doc
          "The data to export, based on how you imported the data. You can choose to export only BULK data that you imported using a dataset import job, only PUT data that you imported incrementally (using the console, PutEvents, PutUsers and PutItems operations), or ALL for both types. The default value is PUT."];
      roleArn: RoleArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM service role that has permissions to add data to your output Amazon S3 bucket."];
      jobOutput: DatasetExportJobOutput.t
        [@ocaml.doc
          "The path to the Amazon S3 bucket where the job's output is stored."];
      tags: Tags.t option
        [@ocaml.doc "A list of tags to apply to the dataset export job."]}
    let context_ = "CreateDatasetExportJobRequest"
    let make ?ingestionMode =
      fun ?tags ->
        fun ~jobName ->
          fun ~datasetArn ->
            fun ~roleArn ->
              fun ~jobOutput ->
                fun () ->
                  {
                    ingestionMode;
                    tags;
                    jobName;
                    datasetArn;
                    roleArn;
                    jobOutput
                  }
    let to_value x =
      structure_to_value
        [("jobName", (Some (Name.to_value x.jobName)));
        ("datasetArn", (Some (Arn.to_value x.datasetArn)));
        ("ingestionMode",
          (Option.map x.ingestionMode ~f:IngestionMode.to_value));
        ("roleArn", (Some (RoleArn.to_value x.roleArn)));
        ("jobOutput", (Some (DatasetExportJobOutput.to_value x.jobOutput)));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let jobOutput =
        DatasetExportJobOutput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "jobOutput") in
      let roleArn =
        RoleArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "roleArn") in
      let ingestionMode =
        (Option.map ~f:IngestionMode.of_xml)
          (Xml.child xml_arg0 "ingestionMode") in
      let datasetArn =
        Arn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "datasetArn") in
      let jobName =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "jobName") in
      make ?tags ~jobOutput ~roleArn ?ingestionMode ~datasetArn ~jobName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let jobOutput =
        field_map_exn json "jobOutput" DatasetExportJobOutput.of_json in
      let roleArn = field_map_exn json "roleArn" RoleArn.of_json in
      let ingestionMode =
        field_map json "ingestionMode" IngestionMode.of_json in
      let datasetArn = field_map_exn json "datasetArn" Arn.of_json in
      let jobName = field_map_exn json "jobName" Name.of_json in
      make ?tags ~jobOutput ~roleArn ?ingestionMode ~datasetArn ~jobName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a job that exports data from your dataset to an Amazon S3 bucket. To allow Amazon Personalize to export the training data, you must specify an service-linked IAM role that gives Amazon Personalize PutObject permissions for your Amazon S3 bucket. For information, see Exporting a dataset in the Amazon Personalize developer guide. Status A dataset export job can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED To get the status of the export job, call DescribeDatasetExportJob, and specify the Amazon Resource Name (ARN) of the dataset export job. The dataset export is complete when the status shows as ACTIVE. If the status shows as CREATE FAILED, the response includes a failureReason key, which describes why the job failed."]
module CreateCampaignResponse =
  struct
    type nonrec t =
      {
      campaignArn: Arn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the campaign."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?campaignArn = fun () -> { campaignArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("campaignArn", (Option.map x.campaignArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let campaignArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "campaignArn") in
      make ?campaignArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let campaignArn = field_map json "campaignArn" Arn.of_json in
      make ?campaignArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a campaign that deploys a solution version. When a client calls the GetRecommendations and GetPersonalizedRanking APIs, a campaign is specified in the request. Minimum Provisioned TPS and Auto-Scaling A transaction is a single GetRecommendations or GetPersonalizedRanking call. Transactions per second (TPS) is the throughput and unit of billing for Amazon Personalize. The minimum provisioned TPS (minProvisionedTPS) specifies the baseline throughput provisioned by Amazon Personalize, and thus, the minimum billing charge. If your TPS increases beyond minProvisionedTPS, Amazon Personalize auto-scales the provisioned capacity up and down, but never below minProvisionedTPS. There's a short time delay while the capacity is increased that might cause loss of transactions. The actual TPS used is calculated as the average requests/second within a 5-minute window. You pay for maximum of either the minimum provisioned TPS or the actual TPS. We recommend starting with a low minProvisionedTPS, track your usage using Amazon CloudWatch metrics, and then increase the minProvisionedTPS as necessary. Status A campaign can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS To get the campaign status, call DescribeCampaign. Wait until the status of the campaign is ACTIVE before asking the campaign for recommendations. Related APIs ListCampaigns DescribeCampaign UpdateCampaign DeleteCampaign"]
module CreateCampaignRequest =
  struct
    type nonrec t =
      {
      name: Name.t
        [@ocaml.doc
          "A name for the new campaign. The campaign name must be unique within your account."];
      solutionVersionArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution version to deploy."];
      minProvisionedTPS: TransactionsPerSecond.t option
        [@ocaml.doc
          "Specifies the requested minimum provisioned transactions (recommendations) per second that Amazon Personalize will support."];
      campaignConfig: CampaignConfig.t option
        [@ocaml.doc "The configuration details of a campaign."];
      tags: Tags.t option
        [@ocaml.doc "A list of tags to apply to the campaign."]}
    let context_ = "CreateCampaignRequest"
    let make ?minProvisionedTPS =
      fun ?campaignConfig ->
        fun ?tags ->
          fun ~name ->
            fun ~solutionVersionArn ->
              fun () ->
                {
                  minProvisionedTPS;
                  campaignConfig;
                  tags;
                  name;
                  solutionVersionArn
                }
    let to_value x =
      structure_to_value
        [("name", (Some (Name.to_value x.name)));
        ("solutionVersionArn", (Some (Arn.to_value x.solutionVersionArn)));
        ("minProvisionedTPS",
          (Option.map x.minProvisionedTPS ~f:TransactionsPerSecond.to_value));
        ("campaignConfig",
          (Option.map x.campaignConfig ~f:CampaignConfig.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let campaignConfig =
        (Option.map ~f:CampaignConfig.of_xml)
          (Xml.child xml_arg0 "campaignConfig") in
      let minProvisionedTPS =
        (Option.map ~f:TransactionsPerSecond.of_xml)
          (Xml.child xml_arg0 "minProvisionedTPS") in
      let solutionVersionArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "solutionVersionArn") in
      let name =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ?campaignConfig ?minProvisionedTPS ~solutionVersionArn ~name
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let campaignConfig =
        field_map json "campaignConfig" CampaignConfig.of_json in
      let minProvisionedTPS =
        field_map json "minProvisionedTPS" TransactionsPerSecond.of_json in
      let solutionVersionArn =
        field_map_exn json "solutionVersionArn" Arn.of_json in
      let name = field_map_exn json "name" Name.of_json in
      make ?tags ?campaignConfig ?minProvisionedTPS ~solutionVersionArn ~name
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a campaign that deploys a solution version. When a client calls the GetRecommendations and GetPersonalizedRanking APIs, a campaign is specified in the request. Minimum Provisioned TPS and Auto-Scaling A transaction is a single GetRecommendations or GetPersonalizedRanking call. Transactions per second (TPS) is the throughput and unit of billing for Amazon Personalize. The minimum provisioned TPS (minProvisionedTPS) specifies the baseline throughput provisioned by Amazon Personalize, and thus, the minimum billing charge. If your TPS increases beyond minProvisionedTPS, Amazon Personalize auto-scales the provisioned capacity up and down, but never below minProvisionedTPS. There's a short time delay while the capacity is increased that might cause loss of transactions. The actual TPS used is calculated as the average requests/second within a 5-minute window. You pay for maximum of either the minimum provisioned TPS or the actual TPS. We recommend starting with a low minProvisionedTPS, track your usage using Amazon CloudWatch metrics, and then increase the minProvisionedTPS as necessary. Status A campaign can be in one of the following states: CREATE PENDING > CREATE IN_PROGRESS > ACTIVE -or- CREATE FAILED DELETE PENDING > DELETE IN_PROGRESS To get the campaign status, call DescribeCampaign. Wait until the status of the campaign is ACTIVE before asking the campaign for recommendations. Related APIs ListCampaigns DescribeCampaign UpdateCampaign DeleteCampaign"]
module CreateBatchSegmentJobResponse =
  struct
    type nonrec t =
      {
      batchSegmentJobArn: Arn.t option
        [@ocaml.doc "The ARN of the batch segment job."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?batchSegmentJobArn = fun () -> { batchSegmentJobArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("batchSegmentJobArn",
           (Option.map x.batchSegmentJobArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let batchSegmentJobArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "batchSegmentJobArn") in
      make ?batchSegmentJobArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let batchSegmentJobArn =
        field_map json "batchSegmentJobArn" Arn.of_json in
      make ?batchSegmentJobArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a batch segment job. The operation can handle up to 50 million records and the input file must be in JSON format. For more information, see Getting batch recommendations and user segments."]
module CreateBatchSegmentJobRequest =
  struct
    type nonrec t =
      {
      jobName: Name.t
        [@ocaml.doc "The name of the batch segment job to create."];
      solutionVersionArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution version you want the batch segment job to use to generate batch segments."];
      filterArn: Arn.t option
        [@ocaml.doc
          "The ARN of the filter to apply to the batch segment job. For more information on using filters, see Filtering batch recommendations."];
      numResults: NumBatchResults.t option
        [@ocaml.doc
          "The number of predicted users generated by the batch segment job for each line of input data."];
      jobInput: BatchSegmentJobInput.t
        [@ocaml.doc
          "The Amazon S3 path for the input data used to generate the batch segment job."];
      jobOutput: BatchSegmentJobOutput.t
        [@ocaml.doc
          "The Amazon S3 path for the bucket where the job's output will be stored."];
      roleArn: RoleArn.t
        [@ocaml.doc
          "The ARN of the Amazon Identity and Access Management role that has permissions to read and write to your input and output Amazon S3 buckets respectively."];
      tags: Tags.t option
        [@ocaml.doc "A list of tags to apply to the batch segment job."]}
    let context_ = "CreateBatchSegmentJobRequest"
    let make ?filterArn =
      fun ?numResults ->
        fun ?tags ->
          fun ~jobName ->
            fun ~solutionVersionArn ->
              fun ~jobInput ->
                fun ~jobOutput ->
                  fun ~roleArn ->
                    fun () ->
                      {
                        filterArn;
                        numResults;
                        tags;
                        jobName;
                        solutionVersionArn;
                        jobInput;
                        jobOutput;
                        roleArn
                      }
    let to_value x =
      structure_to_value
        [("jobName", (Some (Name.to_value x.jobName)));
        ("solutionVersionArn", (Some (Arn.to_value x.solutionVersionArn)));
        ("filterArn", (Option.map x.filterArn ~f:Arn.to_value));
        ("numResults", (Option.map x.numResults ~f:NumBatchResults.to_value));
        ("jobInput", (Some (BatchSegmentJobInput.to_value x.jobInput)));
        ("jobOutput", (Some (BatchSegmentJobOutput.to_value x.jobOutput)));
        ("roleArn", (Some (RoleArn.to_value x.roleArn)));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let roleArn =
        RoleArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "roleArn") in
      let jobOutput =
        BatchSegmentJobOutput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "jobOutput") in
      let jobInput =
        BatchSegmentJobInput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "jobInput") in
      let numResults =
        (Option.map ~f:NumBatchResults.of_xml)
          (Xml.child xml_arg0 "numResults") in
      let filterArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "filterArn") in
      let solutionVersionArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "solutionVersionArn") in
      let jobName =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "jobName") in
      make ?tags ~roleArn ~jobOutput ~jobInput ?numResults ?filterArn
        ~solutionVersionArn ~jobName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let roleArn = field_map_exn json "roleArn" RoleArn.of_json in
      let jobOutput =
        field_map_exn json "jobOutput" BatchSegmentJobOutput.of_json in
      let jobInput =
        field_map_exn json "jobInput" BatchSegmentJobInput.of_json in
      let numResults = field_map json "numResults" NumBatchResults.of_json in
      let filterArn = field_map json "filterArn" Arn.of_json in
      let solutionVersionArn =
        field_map_exn json "solutionVersionArn" Arn.of_json in
      let jobName = field_map_exn json "jobName" Name.of_json in
      make ?tags ~roleArn ~jobOutput ~jobInput ?numResults ?filterArn
        ~solutionVersionArn ~jobName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a batch segment job. The operation can handle up to 50 million records and the input file must be in JSON format. For more information, see Getting batch recommendations and user segments."]
module CreateBatchInferenceJobResponse =
  struct
    type nonrec t =
      {
      batchInferenceJobArn: Arn.t option
        [@ocaml.doc "The ARN of the batch inference job."]}
    type nonrec error =
      [ `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceInUseException of ResourceInUseException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?batchInferenceJobArn = fun () -> { batchInferenceJobArn }
    let error_of_json name json =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceInUseException" ->
          `ResourceInUseException (ResourceInUseException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceInUseException e ->
          `Assoc
            [("error", (`String "ResourceInUseException"));
            ("details", (ResourceInUseException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("batchInferenceJobArn",
           (Option.map x.batchInferenceJobArn ~f:Arn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let batchInferenceJobArn =
        (Option.map ~f:Arn.of_xml)
          (Xml.child xml_arg0 "batchInferenceJobArn") in
      make ?batchInferenceJobArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let batchInferenceJobArn =
        field_map json "batchInferenceJobArn" Arn.of_json in
      make ?batchInferenceJobArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a batch inference job. The operation can handle up to 50 million records and the input file must be in JSON format. For more information, see Creating a batch inference job."]
module CreateBatchInferenceJobRequest =
  struct
    type nonrec t =
      {
      jobName: Name.t
        [@ocaml.doc "The name of the batch inference job to create."];
      solutionVersionArn: Arn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the solution version that will be used to generate the batch inference recommendations."];
      filterArn: Arn.t option
        [@ocaml.doc
          "The ARN of the filter to apply to the batch inference job. For more information on using filters, see Filtering batch recommendations."];
      numResults: NumBatchResults.t option
        [@ocaml.doc "The number of recommendations to retrieve."];
      jobInput: BatchInferenceJobInput.t
        [@ocaml.doc
          "The Amazon S3 path that leads to the input file to base your recommendations on. The input material must be in JSON format."];
      jobOutput: BatchInferenceJobOutput.t
        [@ocaml.doc
          "The path to the Amazon S3 bucket where the job's output will be stored."];
      roleArn: RoleArn.t
        [@ocaml.doc
          "The ARN of the Amazon Identity and Access Management role that has permissions to read and write to your input and output Amazon S3 buckets respectively."];
      batchInferenceJobConfig: BatchInferenceJobConfig.t option
        [@ocaml.doc "The configuration details of a batch inference job."];
      tags: Tags.t option
        [@ocaml.doc "A list of tags to apply to the batch inference job."]}
    let context_ = "CreateBatchInferenceJobRequest"
    let make ?filterArn =
      fun ?numResults ->
        fun ?batchInferenceJobConfig ->
          fun ?tags ->
            fun ~jobName ->
              fun ~solutionVersionArn ->
                fun ~jobInput ->
                  fun ~jobOutput ->
                    fun ~roleArn ->
                      fun () ->
                        {
                          filterArn;
                          numResults;
                          batchInferenceJobConfig;
                          tags;
                          jobName;
                          solutionVersionArn;
                          jobInput;
                          jobOutput;
                          roleArn
                        }
    let to_value x =
      structure_to_value
        [("jobName", (Some (Name.to_value x.jobName)));
        ("solutionVersionArn", (Some (Arn.to_value x.solutionVersionArn)));
        ("filterArn", (Option.map x.filterArn ~f:Arn.to_value));
        ("numResults", (Option.map x.numResults ~f:NumBatchResults.to_value));
        ("jobInput", (Some (BatchInferenceJobInput.to_value x.jobInput)));
        ("jobOutput", (Some (BatchInferenceJobOutput.to_value x.jobOutput)));
        ("roleArn", (Some (RoleArn.to_value x.roleArn)));
        ("batchInferenceJobConfig",
          (Option.map x.batchInferenceJobConfig
             ~f:BatchInferenceJobConfig.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let batchInferenceJobConfig =
        (Option.map ~f:BatchInferenceJobConfig.of_xml)
          (Xml.child xml_arg0 "batchInferenceJobConfig") in
      let roleArn =
        RoleArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "roleArn") in
      let jobOutput =
        BatchInferenceJobOutput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "jobOutput") in
      let jobInput =
        BatchInferenceJobInput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "jobInput") in
      let numResults =
        (Option.map ~f:NumBatchResults.of_xml)
          (Xml.child xml_arg0 "numResults") in
      let filterArn =
        (Option.map ~f:Arn.of_xml) (Xml.child xml_arg0 "filterArn") in
      let solutionVersionArn =
        Arn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "solutionVersionArn") in
      let jobName =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "jobName") in
      make ?tags ?batchInferenceJobConfig ~roleArn ~jobOutput ~jobInput
        ?numResults ?filterArn ~solutionVersionArn ~jobName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let batchInferenceJobConfig =
        field_map json "batchInferenceJobConfig"
          BatchInferenceJobConfig.of_json in
      let roleArn = field_map_exn json "roleArn" RoleArn.of_json in
      let jobOutput =
        field_map_exn json "jobOutput" BatchInferenceJobOutput.of_json in
      let jobInput =
        field_map_exn json "jobInput" BatchInferenceJobInput.of_json in
      let numResults = field_map json "numResults" NumBatchResults.of_json in
      let filterArn = field_map json "filterArn" Arn.of_json in
      let solutionVersionArn =
        field_map_exn json "solutionVersionArn" Arn.of_json in
      let jobName = field_map_exn json "jobName" Name.of_json in
      make ?tags ?batchInferenceJobConfig ~roleArn ~jobOutput ~jobInput
        ?numResults ?filterArn ~solutionVersionArn ~jobName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a batch inference job. The operation can handle up to 50 million records and the input file must be in JSON format. For more information, see Creating a batch inference job."]
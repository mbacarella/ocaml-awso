(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_update_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier"
       and listenerIdentifier =
         flag "listener-identifier" (required string)
           ~doc:"STRING ListenerIdentifier"
       and rules =
         flag "rules" (required json_arg) ~doc:"JSON RuleUpdateList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_rule
           (Values.BatchUpdateRuleRequest.make ~serviceIdentifier
              ~listenerIdentifier
              ~rules:(Values.RuleUpdateList.of_json rules) ())
           (Some Values.BatchUpdateRuleResponse.to_json)
           (Some Values.BatchUpdateRuleResponse.error_to_json)])
let create_access_log_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and serviceNetworkLogType =
         flag "service-network-log-type" (optional json_arg)
           ~doc:"JSON ServiceNetworkLogType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier"
       and destinationArn =
         flag "destination-arn" (required string)
           ~doc:"STRING AccessLogDestinationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_log_subscription
           (Values.CreateAccessLogSubscriptionRequest.make ?clientToken
              ?serviceNetworkLogType:(Option.map
                                        ~f:Values.ServiceNetworkLogType.of_json
                                        serviceNetworkLogType)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~resourceIdentifier ~destinationArn ())
           (Some Values.CreateAccessLogSubscriptionResponse.to_json)
           (Some Values.CreateAccessLogSubscriptionResponse.error_to_json)])
let create_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and port = flag "port" (optional int) ~doc:"INT Port"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier"
       and name = flag "name" (required string) ~doc:"STRING ListenerName"
       and protocol =
         flag "protocol" (required json_arg) ~doc:"JSON ListenerProtocol"
       and defaultAction =
         flag "default-action" (required json_arg) ~doc:"JSON RuleAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_listener
           (Values.CreateListenerRequest.make ?port ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~serviceIdentifier ~name
              ~protocol:(Values.ListenerProtocol.of_json protocol)
              ~defaultAction:(Values.RuleAction.of_json defaultAction) ())
           (Some Values.CreateListenerResponse.to_json)
           (Some Values.CreateListenerResponse.error_to_json)])
let create_resource_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portRanges =
         flag "port-ranges" (optional json_arg) ~doc:"JSON PortRangeList"
       and protocol =
         flag "protocol" (optional json_arg) ~doc:"JSON ProtocolType"
       and resourceGatewayIdentifier =
         flag "resource-gateway-identifier" (optional string)
           ~doc:"STRING ResourceGatewayIdentifier"
       and resourceConfigurationGroupIdentifier =
         flag "resource-configuration-group-identifier" (optional string)
           ~doc:"STRING ResourceConfigurationIdentifier"
       and resourceConfigurationDefinition =
         flag "resource-configuration-definition" (optional json_arg)
           ~doc:"JSON ResourceConfigurationDefinition"
       and allowAssociationToShareableServiceNetwork =
         flag "allow-association-to-shareable-service-network"
           (optional bool) ~doc:"BOOL Boolean"
       and customDomainName =
         flag "custom-domain-name" (optional string) ~doc:"STRING DomainName"
       and groupDomain =
         flag "group-domain" (optional string) ~doc:"STRING DomainName"
       and domainVerificationIdentifier =
         flag "domain-verification-identifier" (optional string)
           ~doc:"STRING DomainVerificationIdentifier"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name =
         flag "name" (required string)
           ~doc:"STRING ResourceConfigurationName"
       and type_ =
         flag "type-" (required json_arg)
           ~doc:"JSON ResourceConfigurationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_configuration
           (Values.CreateResourceConfigurationRequest.make
              ?portRanges:(Option.map ~f:Values.PortRangeList.of_json
                             portRanges)
              ?protocol:(Option.map ~f:Values.ProtocolType.of_json protocol)
              ?resourceGatewayIdentifier
              ?resourceConfigurationGroupIdentifier
              ?resourceConfigurationDefinition:(Option.map
                                                  ~f:Values.ResourceConfigurationDefinition.of_json
                                                  resourceConfigurationDefinition)
              ?allowAssociationToShareableServiceNetwork ?customDomainName
              ?groupDomain ?domainVerificationIdentifier ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~type_:(Values.ResourceConfigurationType.of_json type_) ())
           (Some Values.CreateResourceConfigurationResponse.to_json)
           (Some Values.CreateResourceConfigurationResponse.error_to_json)])
let create_resource_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and vpcIdentifier =
         flag "vpc-identifier" (optional string) ~doc:"STRING VpcId"
       and subnetIds =
         flag "subnet-ids" (optional json_arg) ~doc:"JSON SubnetList"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON CreateResourceGatewayRequestSecurityGroupIdsList"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg)
           ~doc:"JSON ResourceGatewayIpAddressType"
       and ipv4AddressesPerEni =
         flag "ipv4-addresses-per-eni" (optional int)
           ~doc:"INT Ipv4AddressesPerEni"
       and resourceConfigDnsResolution =
         flag "resource-config-dns-resolution" (optional json_arg)
           ~doc:"JSON ResourceConfigDnsResolution"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name =
         flag "name" (required string) ~doc:"STRING ResourceGatewayName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_gateway
           (Values.CreateResourceGatewayRequest.make ?clientToken
              ?vpcIdentifier
              ?subnetIds:(Option.map ~f:Values.SubnetList.of_json subnetIds)
              ?securityGroupIds:(Option.map
                                   ~f:Values.CreateResourceGatewayRequestSecurityGroupIdsList.of_json
                                   securityGroupIds)
              ?ipAddressType:(Option.map
                                ~f:Values.ResourceGatewayIpAddressType.of_json
                                ipAddressType) ?ipv4AddressesPerEni
              ?resourceConfigDnsResolution:(Option.map
                                              ~f:Values.ResourceConfigDnsResolution.of_json
                                              resourceConfigDnsResolution)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name ())
           (Some Values.CreateResourceGatewayResponse.to_json)
           (Some Values.CreateResourceGatewayResponse.error_to_json)])
let create_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier"
       and listenerIdentifier =
         flag "listener-identifier" (required string)
           ~doc:"STRING ListenerIdentifier"
       and name = flag "name" (required string) ~doc:"STRING RuleName"
       and match_ = flag "match-" (required json_arg) ~doc:"JSON RuleMatch"
       and priority = flag "priority" (required int) ~doc:"INT RulePriority"
       and action = flag "action" (required json_arg) ~doc:"JSON RuleAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_rule
           (Values.CreateRuleRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~serviceIdentifier ~listenerIdentifier ~name
              ~match_:(Values.RuleMatch.of_json match_) ~priority
              ~action:(Values.RuleAction.of_json action) ())
           (Some Values.CreateRuleResponse.to_json)
           (Some Values.CreateRuleResponse.error_to_json)])
let create_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and customDomainName =
         flag "custom-domain-name" (optional string)
           ~doc:"STRING ServiceCustomDomainName"
       and certificateArn =
         flag "certificate-arn" (optional string)
           ~doc:"STRING CertificateArn"
       and authType =
         flag "auth-type" (optional json_arg) ~doc:"JSON AuthType"
       and name = flag "name" (required string) ~doc:"STRING ServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service
           (Values.CreateServiceRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?customDomainName ?certificateArn
              ?authType:(Option.map ~f:Values.AuthType.of_json authType)
              ~name ()) (Some Values.CreateServiceResponse.to_json)
           (Some Values.CreateServiceResponse.error_to_json)])
let create_service_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and authType =
         flag "auth-type" (optional json_arg) ~doc:"JSON AuthType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and sharingConfig =
         flag "sharing-config" (optional json_arg) ~doc:"JSON SharingConfig"
       and name =
         flag "name" (required string) ~doc:"STRING ServiceNetworkName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service_network
           (Values.CreateServiceNetworkRequest.make ?clientToken
              ?authType:(Option.map ~f:Values.AuthType.of_json authType)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?sharingConfig:(Option.map ~f:Values.SharingConfig.of_json
                                sharingConfig) ~name ())
           (Some Values.CreateServiceNetworkResponse.to_json)
           (Some Values.CreateServiceNetworkResponse.error_to_json)])
let create_service_network_resource_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and privateDnsEnabled =
         flag "private-dns-enabled" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and resourceConfigurationIdentifier =
         flag "resource-configuration-identifier" (required string)
           ~doc:"STRING ResourceConfigurationIdentifier"
       and serviceNetworkIdentifier =
         flag "service-network-identifier" (required string)
           ~doc:"STRING ServiceNetworkIdentifierWithoutRegex" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service_network_resource_association
           (Values.CreateServiceNetworkResourceAssociationRequest.make
              ?clientToken ?privateDnsEnabled
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~resourceConfigurationIdentifier ~serviceNetworkIdentifier ())
           (Some
              Values.CreateServiceNetworkResourceAssociationResponse.to_json)
           (Some
              Values.CreateServiceNetworkResourceAssociationResponse.error_to_json)])
let create_service_network_service_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier"
       and serviceNetworkIdentifier =
         flag "service-network-identifier" (required string)
           ~doc:"STRING ServiceNetworkIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service_network_service_association
           (Values.CreateServiceNetworkServiceAssociationRequest.make
              ?clientToken ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~serviceIdentifier ~serviceNetworkIdentifier ())
           (Some
              Values.CreateServiceNetworkServiceAssociationResponse.to_json)
           (Some
              Values.CreateServiceNetworkServiceAssociationResponse.error_to_json)])
let create_service_network_vpc_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and privateDnsEnabled =
         flag "private-dns-enabled" (optional bool) ~doc:"BOOL Boolean"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON CreateServiceNetworkVpcAssociationRequestSecurityGroupIdsList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and dnsOptions =
         flag "dns-options" (optional json_arg) ~doc:"JSON DnsOptions"
       and serviceNetworkIdentifier =
         flag "service-network-identifier" (required string)
           ~doc:"STRING ServiceNetworkIdentifier"
       and vpcIdentifier =
         flag "vpc-identifier" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service_network_vpc_association
           (Values.CreateServiceNetworkVpcAssociationRequest.make
              ?clientToken ?privateDnsEnabled
              ?securityGroupIds:(Option.map
                                   ~f:Values.CreateServiceNetworkVpcAssociationRequestSecurityGroupIdsList.of_json
                                   securityGroupIds)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?dnsOptions:(Option.map ~f:Values.DnsOptions.of_json dnsOptions)
              ~serviceNetworkIdentifier ~vpcIdentifier ())
           (Some Values.CreateServiceNetworkVpcAssociationResponse.to_json)
           (Some
              Values.CreateServiceNetworkVpcAssociationResponse.error_to_json)])
let create_target_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and config =
         flag "config" (optional json_arg) ~doc:"JSON TargetGroupConfig"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING TargetGroupName"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON TargetGroupType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_target_group
           (Values.CreateTargetGroupRequest.make
              ?config:(Option.map ~f:Values.TargetGroupConfig.of_json config)
              ?clientToken ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~name ~type_:(Values.TargetGroupType.of_json type_) ())
           (Some Values.CreateTargetGroupResponse.to_json)
           (Some Values.CreateTargetGroupResponse.error_to_json)])
let delete_access_log_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessLogSubscriptionIdentifier =
         flag "access-log-subscription-identifier" (required string)
           ~doc:"STRING AccessLogSubscriptionIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_log_subscription
           (Values.DeleteAccessLogSubscriptionRequest.make
              ~accessLogSubscriptionIdentifier ())
           (Some Values.DeleteAccessLogSubscriptionResponse.to_json)
           (Some Values.DeleteAccessLogSubscriptionResponse.error_to_json)])
let delete_auth_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_auth_policy
           (Values.DeleteAuthPolicyRequest.make ~resourceIdentifier ())
           (Some Values.DeleteAuthPolicyResponse.to_json)
           (Some Values.DeleteAuthPolicyResponse.error_to_json)])
let delete_domain_verification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainVerificationIdentifier =
         flag "domain-verification-identifier" (required string)
           ~doc:"STRING DomainVerificationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain_verification
           (Values.DeleteDomainVerificationRequest.make
              ~domainVerificationIdentifier ())
           (Some Values.DeleteDomainVerificationResponse.to_json)
           (Some Values.DeleteDomainVerificationResponse.error_to_json)])
let delete_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier"
       and listenerIdentifier =
         flag "listener-identifier" (required string)
           ~doc:"STRING ListenerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_listener
           (Values.DeleteListenerRequest.make ~serviceIdentifier
              ~listenerIdentifier ())
           (Some Values.DeleteListenerResponse.to_json)
           (Some Values.DeleteListenerResponse.error_to_json)])
let delete_resource_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceConfigurationIdentifier =
         flag "resource-configuration-identifier" (required string)
           ~doc:"STRING ResourceConfigurationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_configuration
           (Values.DeleteResourceConfigurationRequest.make
              ~resourceConfigurationIdentifier ())
           (Some Values.DeleteResourceConfigurationResponse.to_json)
           (Some Values.DeleteResourceConfigurationResponse.error_to_json)])
let delete_resource_endpoint_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceEndpointAssociationIdentifier =
         flag "resource-endpoint-association-identifier" (required string)
           ~doc:"STRING ResourceEndpointAssociationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_endpoint_association
           (Values.DeleteResourceEndpointAssociationRequest.make
              ~resourceEndpointAssociationIdentifier ())
           (Some Values.DeleteResourceEndpointAssociationResponse.to_json)
           (Some
              Values.DeleteResourceEndpointAssociationResponse.error_to_json)])
let delete_resource_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceGatewayIdentifier =
         flag "resource-gateway-identifier" (required string)
           ~doc:"STRING ResourceGatewayIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_gateway
           (Values.DeleteResourceGatewayRequest.make
              ~resourceGatewayIdentifier ())
           (Some Values.DeleteResourceGatewayResponse.to_json)
           (Some Values.DeleteResourceGatewayResponse.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyRequest.make ~resourceArn ())
           (Some Values.DeleteResourcePolicyResponse.to_json)
           (Some Values.DeleteResourcePolicyResponse.error_to_json)])
let delete_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier"
       and listenerIdentifier =
         flag "listener-identifier" (required string)
           ~doc:"STRING ListenerIdentifier"
       and ruleIdentifier =
         flag "rule-identifier" (required string)
           ~doc:"STRING RuleIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_rule
           (Values.DeleteRuleRequest.make ~serviceIdentifier
              ~listenerIdentifier ~ruleIdentifier ())
           (Some Values.DeleteRuleResponse.to_json)
           (Some Values.DeleteRuleResponse.error_to_json)])
let delete_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service
           (Values.DeleteServiceRequest.make ~serviceIdentifier ())
           (Some Values.DeleteServiceResponse.to_json)
           (Some Values.DeleteServiceResponse.error_to_json)])
let delete_service_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNetworkIdentifier =
         flag "service-network-identifier" (required string)
           ~doc:"STRING ServiceNetworkIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service_network
           (Values.DeleteServiceNetworkRequest.make ~serviceNetworkIdentifier
              ()) (Some Values.DeleteServiceNetworkResponse.to_json)
           (Some Values.DeleteServiceNetworkResponse.error_to_json)])
let delete_service_network_resource_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNetworkResourceAssociationIdentifier =
         flag "service-network-resource-association-identifier"
           (required string)
           ~doc:"STRING ServiceNetworkResourceAssociationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service_network_resource_association
           (Values.DeleteServiceNetworkResourceAssociationRequest.make
              ~serviceNetworkResourceAssociationIdentifier ())
           (Some
              Values.DeleteServiceNetworkResourceAssociationResponse.to_json)
           (Some
              Values.DeleteServiceNetworkResourceAssociationResponse.error_to_json)])
let delete_service_network_service_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNetworkServiceAssociationIdentifier =
         flag "service-network-service-association-identifier"
           (required string)
           ~doc:"STRING ServiceNetworkServiceAssociationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service_network_service_association
           (Values.DeleteServiceNetworkServiceAssociationRequest.make
              ~serviceNetworkServiceAssociationIdentifier ())
           (Some
              Values.DeleteServiceNetworkServiceAssociationResponse.to_json)
           (Some
              Values.DeleteServiceNetworkServiceAssociationResponse.error_to_json)])
let delete_service_network_vpc_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNetworkVpcAssociationIdentifier =
         flag "service-network-vpc-association-identifier" (required string)
           ~doc:"STRING ServiceNetworkVpcAssociationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service_network_vpc_association
           (Values.DeleteServiceNetworkVpcAssociationRequest.make
              ~serviceNetworkVpcAssociationIdentifier ())
           (Some Values.DeleteServiceNetworkVpcAssociationResponse.to_json)
           (Some
              Values.DeleteServiceNetworkVpcAssociationResponse.error_to_json)])
let delete_target_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupIdentifier =
         flag "target-group-identifier" (required string)
           ~doc:"STRING TargetGroupIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_target_group
           (Values.DeleteTargetGroupRequest.make ~targetGroupIdentifier ())
           (Some Values.DeleteTargetGroupResponse.to_json)
           (Some Values.DeleteTargetGroupResponse.error_to_json)])
let deregister_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupIdentifier =
         flag "target-group-identifier" (required string)
           ~doc:"STRING TargetGroupIdentifier"
       and targets =
         flag "targets" (required json_arg)
           ~doc:"JSON DeregisterTargetsRequestTargetsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_targets
           (Values.DeregisterTargetsRequest.make ~targetGroupIdentifier
              ~targets:(Values.DeregisterTargetsRequestTargetsList.of_json
                          targets) ())
           (Some Values.DeregisterTargetsResponse.to_json)
           (Some Values.DeregisterTargetsResponse.error_to_json)])
let get_access_log_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessLogSubscriptionIdentifier =
         flag "access-log-subscription-identifier" (required string)
           ~doc:"STRING AccessLogSubscriptionIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_log_subscription
           (Values.GetAccessLogSubscriptionRequest.make
              ~accessLogSubscriptionIdentifier ())
           (Some Values.GetAccessLogSubscriptionResponse.to_json)
           (Some Values.GetAccessLogSubscriptionResponse.error_to_json)])
let get_auth_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_auth_policy
           (Values.GetAuthPolicyRequest.make ~resourceIdentifier ())
           (Some Values.GetAuthPolicyResponse.to_json)
           (Some Values.GetAuthPolicyResponse.error_to_json)])
let get_domain_verification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainVerificationIdentifier =
         flag "domain-verification-identifier" (required string)
           ~doc:"STRING DomainVerificationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_verification
           (Values.GetDomainVerificationRequest.make
              ~domainVerificationIdentifier ())
           (Some Values.GetDomainVerificationResponse.to_json)
           (Some Values.GetDomainVerificationResponse.error_to_json)])
let get_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier"
       and listenerIdentifier =
         flag "listener-identifier" (required string)
           ~doc:"STRING ListenerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_listener
           (Values.GetListenerRequest.make ~serviceIdentifier
              ~listenerIdentifier ())
           (Some Values.GetListenerResponse.to_json)
           (Some Values.GetListenerResponse.error_to_json)])
let get_resource_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceConfigurationIdentifier =
         flag "resource-configuration-identifier" (required string)
           ~doc:"STRING ResourceConfigurationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_configuration
           (Values.GetResourceConfigurationRequest.make
              ~resourceConfigurationIdentifier ())
           (Some Values.GetResourceConfigurationResponse.to_json)
           (Some Values.GetResourceConfigurationResponse.error_to_json)])
let get_resource_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceGatewayIdentifier =
         flag "resource-gateway-identifier" (required string)
           ~doc:"STRING ResourceGatewayIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_gateway
           (Values.GetResourceGatewayRequest.make ~resourceGatewayIdentifier
              ()) (Some Values.GetResourceGatewayResponse.to_json)
           (Some Values.GetResourceGatewayResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ~resourceArn ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let get_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier"
       and listenerIdentifier =
         flag "listener-identifier" (required string)
           ~doc:"STRING ListenerIdentifier"
       and ruleIdentifier =
         flag "rule-identifier" (required string)
           ~doc:"STRING RuleIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_rule
           (Values.GetRuleRequest.make ~serviceIdentifier ~listenerIdentifier
              ~ruleIdentifier ()) (Some Values.GetRuleResponse.to_json)
           (Some Values.GetRuleResponse.error_to_json)])
let get_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service
           (Values.GetServiceRequest.make ~serviceIdentifier ())
           (Some Values.GetServiceResponse.to_json)
           (Some Values.GetServiceResponse.error_to_json)])
let get_service_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNetworkIdentifier =
         flag "service-network-identifier" (required string)
           ~doc:"STRING ServiceNetworkIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_network
           (Values.GetServiceNetworkRequest.make ~serviceNetworkIdentifier ())
           (Some Values.GetServiceNetworkResponse.to_json)
           (Some Values.GetServiceNetworkResponse.error_to_json)])
let get_service_network_resource_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNetworkResourceAssociationIdentifier =
         flag "service-network-resource-association-identifier"
           (required string)
           ~doc:"STRING ServiceNetworkResourceAssociationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_network_resource_association
           (Values.GetServiceNetworkResourceAssociationRequest.make
              ~serviceNetworkResourceAssociationIdentifier ())
           (Some Values.GetServiceNetworkResourceAssociationResponse.to_json)
           (Some
              Values.GetServiceNetworkResourceAssociationResponse.error_to_json)])
let get_service_network_service_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNetworkServiceAssociationIdentifier =
         flag "service-network-service-association-identifier"
           (required string)
           ~doc:"STRING ServiceNetworkServiceAssociationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_network_service_association
           (Values.GetServiceNetworkServiceAssociationRequest.make
              ~serviceNetworkServiceAssociationIdentifier ())
           (Some Values.GetServiceNetworkServiceAssociationResponse.to_json)
           (Some
              Values.GetServiceNetworkServiceAssociationResponse.error_to_json)])
let get_service_network_vpc_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNetworkVpcAssociationIdentifier =
         flag "service-network-vpc-association-identifier" (required string)
           ~doc:"STRING ServiceNetworkVpcAssociationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_network_vpc_association
           (Values.GetServiceNetworkVpcAssociationRequest.make
              ~serviceNetworkVpcAssociationIdentifier ())
           (Some Values.GetServiceNetworkVpcAssociationResponse.to_json)
           (Some Values.GetServiceNetworkVpcAssociationResponse.error_to_json)])
let get_target_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupIdentifier =
         flag "target-group-identifier" (required string)
           ~doc:"STRING TargetGroupIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_target_group
           (Values.GetTargetGroupRequest.make ~targetGroupIdentifier ())
           (Some Values.GetTargetGroupResponse.to_json)
           (Some Values.GetTargetGroupResponse.error_to_json)])
let list_access_log_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_log_subscriptions
           (Values.ListAccessLogSubscriptionsRequest.make ?maxResults
              ?nextToken ~resourceIdentifier ())
           (Some Values.ListAccessLogSubscriptionsResponse.to_json)
           (Some Values.ListAccessLogSubscriptionsResponse.error_to_json)])
let list_domain_verifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domain_verifications
           (Values.ListDomainVerificationsRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListDomainVerificationsResponse.to_json)
           (Some Values.ListDomainVerificationsResponse.error_to_json)])
let list_listeners =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_listeners
           (Values.ListListenersRequest.make ?maxResults ?nextToken
              ~serviceIdentifier ())
           (Some Values.ListListenersResponse.to_json)
           (Some Values.ListListenersResponse.error_to_json)])
let list_resource_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceGatewayIdentifier =
         flag "resource-gateway-identifier" (optional string)
           ~doc:"STRING ResourceGatewayIdentifier"
       and resourceConfigurationGroupIdentifier =
         flag "resource-configuration-group-identifier" (optional string)
           ~doc:"STRING ResourceConfigurationIdentifier"
       and domainVerificationIdentifier =
         flag "domain-verification-identifier" (optional string)
           ~doc:"STRING DomainVerificationIdentifier"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_configurations
           (Values.ListResourceConfigurationsRequest.make
              ?resourceGatewayIdentifier
              ?resourceConfigurationGroupIdentifier
              ?domainVerificationIdentifier ?maxResults ?nextToken ())
           (Some Values.ListResourceConfigurationsResponse.to_json)
           (Some Values.ListResourceConfigurationsResponse.error_to_json)])
let list_resource_endpoint_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceEndpointAssociationIdentifier =
         flag "resource-endpoint-association-identifier" (optional string)
           ~doc:"STRING ResourceEndpointAssociationIdentifier"
       and vpcEndpointId =
         flag "vpc-endpoint-id" (optional string) ~doc:"STRING VpcEndpointId"
       and vpcEndpointOwner =
         flag "vpc-endpoint-owner" (optional string)
           ~doc:"STRING VpcEndpointOwner"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and resourceConfigurationIdentifier =
         flag "resource-configuration-identifier" (required string)
           ~doc:"STRING ResourceConfigurationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_endpoint_associations
           (Values.ListResourceEndpointAssociationsRequest.make
              ?resourceEndpointAssociationIdentifier ?vpcEndpointId
              ?vpcEndpointOwner ?maxResults ?nextToken
              ~resourceConfigurationIdentifier ())
           (Some Values.ListResourceEndpointAssociationsResponse.to_json)
           (Some
              Values.ListResourceEndpointAssociationsResponse.error_to_json)])
let list_resource_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_gateways
           (Values.ListResourceGatewaysRequest.make ?maxResults ?nextToken ())
           (Some Values.ListResourceGatewaysResponse.to_json)
           (Some Values.ListResourceGatewaysResponse.error_to_json)])
let list_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier"
       and listenerIdentifier =
         flag "listener-identifier" (required string)
           ~doc:"STRING ListenerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rules
           (Values.ListRulesRequest.make ?maxResults ?nextToken
              ~serviceIdentifier ~listenerIdentifier ())
           (Some Values.ListRulesResponse.to_json)
           (Some Values.ListRulesResponse.error_to_json)])
let list_service_network_resource_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNetworkIdentifier =
         flag "service-network-identifier" (optional string)
           ~doc:"STRING ServiceNetworkIdentifier"
       and resourceConfigurationIdentifier =
         flag "resource-configuration-identifier" (optional string)
           ~doc:"STRING ResourceConfigurationIdentifier"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and includeChildren =
         flag "include-children" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_network_resource_associations
           (Values.ListServiceNetworkResourceAssociationsRequest.make
              ?serviceNetworkIdentifier ?resourceConfigurationIdentifier
              ?maxResults ?nextToken ?includeChildren ())
           (Some
              Values.ListServiceNetworkResourceAssociationsResponse.to_json)
           (Some
              Values.ListServiceNetworkResourceAssociationsResponse.error_to_json)])
let list_service_network_service_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNetworkIdentifier =
         flag "service-network-identifier" (optional string)
           ~doc:"STRING ServiceNetworkIdentifier"
       and serviceIdentifier =
         flag "service-identifier" (optional string)
           ~doc:"STRING ServiceIdentifier"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_network_service_associations
           (Values.ListServiceNetworkServiceAssociationsRequest.make
              ?serviceNetworkIdentifier ?serviceIdentifier ?maxResults
              ?nextToken ())
           (Some Values.ListServiceNetworkServiceAssociationsResponse.to_json)
           (Some
              Values.ListServiceNetworkServiceAssociationsResponse.error_to_json)])
let list_service_network_vpc_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNetworkIdentifier =
         flag "service-network-identifier" (optional string)
           ~doc:"STRING ServiceNetworkIdentifier"
       and vpcIdentifier =
         flag "vpc-identifier" (optional string) ~doc:"STRING VpcId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_network_vpc_associations
           (Values.ListServiceNetworkVpcAssociationsRequest.make
              ?serviceNetworkIdentifier ?vpcIdentifier ?maxResults ?nextToken
              ())
           (Some Values.ListServiceNetworkVpcAssociationsResponse.to_json)
           (Some
              Values.ListServiceNetworkVpcAssociationsResponse.error_to_json)])
let list_service_network_vpc_endpoint_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and serviceNetworkIdentifier =
         flag "service-network-identifier" (required string)
           ~doc:"STRING ServiceNetworkIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_network_vpc_endpoint_associations
           (Values.ListServiceNetworkVpcEndpointAssociationsRequest.make
              ?maxResults ?nextToken ~serviceNetworkIdentifier ())
           (Some
              Values.ListServiceNetworkVpcEndpointAssociationsResponse.to_json)
           (Some
              Values.ListServiceNetworkVpcEndpointAssociationsResponse.error_to_json)])
let list_service_networks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_networks
           (Values.ListServiceNetworksRequest.make ?maxResults ?nextToken ())
           (Some Values.ListServiceNetworksResponse.to_json)
           (Some Values.ListServiceNetworksResponse.error_to_json)])
let list_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_services
           (Values.ListServicesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListServicesResponse.to_json)
           (Some Values.ListServicesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_target_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and vpcIdentifier =
         flag "vpc-identifier" (optional string) ~doc:"STRING VpcId"
       and targetGroupType =
         flag "target-group-type" (optional json_arg)
           ~doc:"JSON TargetGroupType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_target_groups
           (Values.ListTargetGroupsRequest.make ?maxResults ?nextToken
              ?vpcIdentifier
              ?targetGroupType:(Option.map ~f:Values.TargetGroupType.of_json
                                  targetGroupType) ())
           (Some Values.ListTargetGroupsResponse.to_json)
           (Some Values.ListTargetGroupsResponse.error_to_json)])
let list_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and targets =
         flag "targets" (optional json_arg)
           ~doc:"JSON ListTargetsRequestTargetsList"
       and targetGroupIdentifier =
         flag "target-group-identifier" (required string)
           ~doc:"STRING TargetGroupIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_targets
           (Values.ListTargetsRequest.make ?maxResults ?nextToken
              ?targets:(Option.map
                          ~f:Values.ListTargetsRequestTargetsList.of_json
                          targets) ~targetGroupIdentifier ())
           (Some Values.ListTargetsResponse.to_json)
           (Some Values.ListTargetsResponse.error_to_json)])
let put_auth_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier"
       and policy =
         flag "policy" (required string) ~doc:"STRING AuthPolicyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_auth_policy
           (Values.PutAuthPolicyRequest.make ~resourceIdentifier ~policy ())
           (Some Values.PutAuthPolicyResponse.to_json)
           (Some Values.PutAuthPolicyResponse.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and policy =
         flag "policy" (required string) ~doc:"STRING PolicyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyRequest.make ~resourceArn ~policy ())
           (Some Values.PutResourcePolicyResponse.to_json)
           (Some Values.PutResourcePolicyResponse.error_to_json)])
let register_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupIdentifier =
         flag "target-group-identifier" (required string)
           ~doc:"STRING TargetGroupIdentifier"
       and targets =
         flag "targets" (required json_arg)
           ~doc:"JSON RegisterTargetsRequestTargetsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_targets
           (Values.RegisterTargetsRequest.make ~targetGroupIdentifier
              ~targets:(Values.RegisterTargetsRequestTargetsList.of_json
                          targets) ())
           (Some Values.RegisterTargetsResponse.to_json)
           (Some Values.RegisterTargetsResponse.error_to_json)])
let start_domain_verification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_domain_verification
           (Values.StartDomainVerificationRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~domainName ())
           (Some Values.StartDomainVerificationResponse.to_json)
           (Some Values.StartDomainVerificationResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_access_log_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessLogSubscriptionIdentifier =
         flag "access-log-subscription-identifier" (required string)
           ~doc:"STRING AccessLogSubscriptionIdentifier"
       and destinationArn =
         flag "destination-arn" (required string)
           ~doc:"STRING AccessLogDestinationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_access_log_subscription
           (Values.UpdateAccessLogSubscriptionRequest.make
              ~accessLogSubscriptionIdentifier ~destinationArn ())
           (Some Values.UpdateAccessLogSubscriptionResponse.to_json)
           (Some Values.UpdateAccessLogSubscriptionResponse.error_to_json)])
let update_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier"
       and listenerIdentifier =
         flag "listener-identifier" (required string)
           ~doc:"STRING ListenerIdentifier"
       and defaultAction =
         flag "default-action" (required json_arg) ~doc:"JSON RuleAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_listener
           (Values.UpdateListenerRequest.make ~serviceIdentifier
              ~listenerIdentifier
              ~defaultAction:(Values.RuleAction.of_json defaultAction) ())
           (Some Values.UpdateListenerResponse.to_json)
           (Some Values.UpdateListenerResponse.error_to_json)])
let update_resource_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceConfigurationDefinition =
         flag "resource-configuration-definition" (optional json_arg)
           ~doc:"JSON ResourceConfigurationDefinition"
       and allowAssociationToShareableServiceNetwork =
         flag "allow-association-to-shareable-service-network"
           (optional bool) ~doc:"BOOL Boolean"
       and portRanges =
         flag "port-ranges" (optional json_arg) ~doc:"JSON PortRangeList"
       and resourceConfigurationIdentifier =
         flag "resource-configuration-identifier" (required string)
           ~doc:"STRING ResourceConfigurationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource_configuration
           (Values.UpdateResourceConfigurationRequest.make
              ?resourceConfigurationDefinition:(Option.map
                                                  ~f:Values.ResourceConfigurationDefinition.of_json
                                                  resourceConfigurationDefinition)
              ?allowAssociationToShareableServiceNetwork
              ?portRanges:(Option.map ~f:Values.PortRangeList.of_json
                             portRanges) ~resourceConfigurationIdentifier ())
           (Some Values.UpdateResourceConfigurationResponse.to_json)
           (Some Values.UpdateResourceConfigurationResponse.error_to_json)])
let update_resource_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON UpdateResourceGatewayRequestSecurityGroupIdsList"
       and resourceGatewayIdentifier =
         flag "resource-gateway-identifier" (required string)
           ~doc:"STRING ResourceGatewayIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource_gateway
           (Values.UpdateResourceGatewayRequest.make
              ?securityGroupIds:(Option.map
                                   ~f:Values.UpdateResourceGatewayRequestSecurityGroupIdsList.of_json
                                   securityGroupIds)
              ~resourceGatewayIdentifier ())
           (Some Values.UpdateResourceGatewayResponse.to_json)
           (Some Values.UpdateResourceGatewayResponse.error_to_json)])
let update_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and match_ = flag "match-" (optional json_arg) ~doc:"JSON RuleMatch"
       and priority = flag "priority" (optional int) ~doc:"INT RulePriority"
       and action = flag "action" (optional json_arg) ~doc:"JSON RuleAction"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier"
       and listenerIdentifier =
         flag "listener-identifier" (required string)
           ~doc:"STRING ListenerIdentifier"
       and ruleIdentifier =
         flag "rule-identifier" (required string)
           ~doc:"STRING RuleIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_rule
           (Values.UpdateRuleRequest.make
              ?match_:(Option.map ~f:Values.RuleMatch.of_json match_)
              ?priority
              ?action:(Option.map ~f:Values.RuleAction.of_json action)
              ~serviceIdentifier ~listenerIdentifier ~ruleIdentifier ())
           (Some Values.UpdateRuleResponse.to_json)
           (Some Values.UpdateRuleResponse.error_to_json)])
let update_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (optional string)
           ~doc:"STRING CertificateArn"
       and authType =
         flag "auth-type" (optional json_arg) ~doc:"JSON AuthType"
       and serviceIdentifier =
         flag "service-identifier" (required string)
           ~doc:"STRING ServiceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service
           (Values.UpdateServiceRequest.make ?certificateArn
              ?authType:(Option.map ~f:Values.AuthType.of_json authType)
              ~serviceIdentifier ())
           (Some Values.UpdateServiceResponse.to_json)
           (Some Values.UpdateServiceResponse.error_to_json)])
let update_service_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNetworkIdentifier =
         flag "service-network-identifier" (required string)
           ~doc:"STRING ServiceNetworkIdentifier"
       and authType =
         flag "auth-type" (required json_arg) ~doc:"JSON AuthType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_network
           (Values.UpdateServiceNetworkRequest.make ~serviceNetworkIdentifier
              ~authType:(Values.AuthType.of_json authType) ())
           (Some Values.UpdateServiceNetworkResponse.to_json)
           (Some Values.UpdateServiceNetworkResponse.error_to_json)])
let update_service_network_vpc_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNetworkVpcAssociationIdentifier =
         flag "service-network-vpc-association-identifier" (required string)
           ~doc:"STRING ServiceNetworkVpcAssociationIdentifier"
       and securityGroupIds =
         flag "security-group-ids" (required json_arg)
           ~doc:"JSON UpdateServiceNetworkVpcAssociationRequestSecurityGroupIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_network_vpc_association
           (Values.UpdateServiceNetworkVpcAssociationRequest.make
              ~serviceNetworkVpcAssociationIdentifier
              ~securityGroupIds:(Values.UpdateServiceNetworkVpcAssociationRequestSecurityGroupIdsList.of_json
                                   securityGroupIds) ())
           (Some Values.UpdateServiceNetworkVpcAssociationResponse.to_json)
           (Some
              Values.UpdateServiceNetworkVpcAssociationResponse.error_to_json)])
let update_target_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupIdentifier =
         flag "target-group-identifier" (required string)
           ~doc:"STRING TargetGroupIdentifier"
       and healthCheck =
         flag "health-check" (required json_arg)
           ~doc:"JSON HealthCheckConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_target_group
           (Values.UpdateTargetGroupRequest.make ~targetGroupIdentifier
              ~healthCheck:(Values.HealthCheckConfig.of_json healthCheck) ())
           (Some Values.UpdateTargetGroupResponse.to_json)
           (Some Values.UpdateTargetGroupResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-update-rule", batch_update_rule);
    ("create-access-log-subscription", create_access_log_subscription);
    ("create-listener", create_listener);
    ("create-resource-configuration", create_resource_configuration);
    ("create-resource-gateway", create_resource_gateway);
    ("create-rule", create_rule);
    ("create-service", create_service);
    ("create-service-network", create_service_network);
    ("create-service-network-resource-association",
      create_service_network_resource_association);
    ("create-service-network-service-association",
      create_service_network_service_association);
    ("create-service-network-vpc-association",
      create_service_network_vpc_association);
    ("create-target-group", create_target_group);
    ("delete-access-log-subscription", delete_access_log_subscription);
    ("delete-auth-policy", delete_auth_policy);
    ("delete-domain-verification", delete_domain_verification);
    ("delete-listener", delete_listener);
    ("delete-resource-configuration", delete_resource_configuration);
    ("delete-resource-endpoint-association",
      delete_resource_endpoint_association);
    ("delete-resource-gateway", delete_resource_gateway);
    ("delete-resource-policy", delete_resource_policy);
    ("delete-rule", delete_rule);
    ("delete-service", delete_service);
    ("delete-service-network", delete_service_network);
    ("delete-service-network-resource-association",
      delete_service_network_resource_association);
    ("delete-service-network-service-association",
      delete_service_network_service_association);
    ("delete-service-network-vpc-association",
      delete_service_network_vpc_association);
    ("delete-target-group", delete_target_group);
    ("deregister-targets", deregister_targets);
    ("get-access-log-subscription", get_access_log_subscription);
    ("get-auth-policy", get_auth_policy);
    ("get-domain-verification", get_domain_verification);
    ("get-listener", get_listener);
    ("get-resource-configuration", get_resource_configuration);
    ("get-resource-gateway", get_resource_gateway);
    ("get-resource-policy", get_resource_policy);
    ("get-rule", get_rule);
    ("get-service", get_service);
    ("get-service-network", get_service_network);
    ("get-service-network-resource-association",
      get_service_network_resource_association);
    ("get-service-network-service-association",
      get_service_network_service_association);
    ("get-service-network-vpc-association",
      get_service_network_vpc_association);
    ("get-target-group", get_target_group);
    ("list-access-log-subscriptions", list_access_log_subscriptions);
    ("list-domain-verifications", list_domain_verifications);
    ("list-listeners", list_listeners);
    ("list-resource-configurations", list_resource_configurations);
    ("list-resource-endpoint-associations",
      list_resource_endpoint_associations);
    ("list-resource-gateways", list_resource_gateways);
    ("list-rules", list_rules);
    ("list-service-network-resource-associations",
      list_service_network_resource_associations);
    ("list-service-network-service-associations",
      list_service_network_service_associations);
    ("list-service-network-vpc-associations",
      list_service_network_vpc_associations);
    ("list-service-network-vpc-endpoint-associations",
      list_service_network_vpc_endpoint_associations);
    ("list-service-networks", list_service_networks);
    ("list-services", list_services);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-target-groups", list_target_groups);
    ("list-targets", list_targets);
    ("put-auth-policy", put_auth_policy);
    ("put-resource-policy", put_resource_policy);
    ("register-targets", register_targets);
    ("start-domain-verification", start_domain_verification);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-access-log-subscription", update_access_log_subscription);
    ("update-listener", update_listener);
    ("update-resource-configuration", update_resource_configuration);
    ("update-resource-gateway", update_resource_gateway);
    ("update-rule", update_rule);
    ("update-service", update_service);
    ("update-service-network", update_service_network);
    ("update-service-network-vpc-association",
      update_service_network_vpc_association);
    ("update-target-group", update_target_group)]
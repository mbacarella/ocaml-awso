(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_reserved_node_exchange =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedNodeId =
         flag "reserved-node-id" (required string) ~doc:"STRING String"
       and targetReservedNodeOfferingId =
         flag "target-reserved-node-offering-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_reserved_node_exchange
           (Values.AcceptReservedNodeExchangeInputMessage.make
              ~reservedNodeId ~targetReservedNodeOfferingId ())
           (Some Values.AcceptReservedNodeExchangeOutputMessage.to_json)
           (Some Values.AcceptReservedNodeExchangeOutputMessage.error_to_json)])
let add_partner =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string)
           ~doc:"STRING PartnerIntegrationAccountId"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING PartnerIntegrationClusterIdentifier"
       and databaseName =
         flag "database-name" (required string)
           ~doc:"STRING PartnerIntegrationDatabaseName"
       and partnerName =
         flag "partner-name" (required string)
           ~doc:"STRING PartnerIntegrationPartnerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_partner
           (Values.PartnerIntegrationInputMessage.make ~accountId
              ~clusterIdentifier ~databaseName ~partnerName ())
           (Some Values.PartnerIntegrationOutputMessage.to_json)
           (Some Values.PartnerIntegrationOutputMessage.error_to_json)])
let associate_data_share_consumer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and associateEntireAccount =
         flag "associate-entire-account" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and consumerArn =
         flag "consumer-arn" (optional string) ~doc:"STRING String"
       and consumerRegion =
         flag "consumer-region" (optional string) ~doc:"STRING String"
       and dataShareArn =
         flag "data-share-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_data_share_consumer
           (Values.AssociateDataShareConsumerMessage.make
              ?associateEntireAccount ?consumerArn ?consumerRegion
              ~dataShareArn ()) (Some Values.DataShare.to_json)
           (Some Values.DataShare.error_to_json)])
let authorize_cluster_security_group_ingress =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cIDRIP = flag "c-i-d-r-i-p" (optional string) ~doc:"STRING String"
       and eC2SecurityGroupName =
         flag "e-c2-security-group-name" (optional string)
           ~doc:"STRING String"
       and eC2SecurityGroupOwnerId =
         flag "e-c2-security-group-owner-id" (optional string)
           ~doc:"STRING String"
       and clusterSecurityGroupName =
         flag "cluster-security-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.authorize_cluster_security_group_ingress
           (Values.AuthorizeClusterSecurityGroupIngressMessage.make ?cIDRIP
              ?eC2SecurityGroupName ?eC2SecurityGroupOwnerId
              ~clusterSecurityGroupName ())
           (Some Values.AuthorizeClusterSecurityGroupIngressResult.to_json)
           (Some
              Values.AuthorizeClusterSecurityGroupIngressResult.error_to_json)])
let authorize_data_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataShareArn =
         flag "data-share-arn" (required string) ~doc:"STRING String"
       and consumerIdentifier =
         flag "consumer-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.authorize_data_share
           (Values.AuthorizeDataShareMessage.make ~dataShareArn
              ~consumerIdentifier ()) (Some Values.DataShare.to_json)
           (Some Values.DataShare.error_to_json)])
let authorize_endpoint_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (optional string) ~doc:"STRING String"
       and vpcIds =
         flag "vpc-ids" (optional json_arg) ~doc:"JSON VpcIdentifierList"
       and account = flag "account" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.authorize_endpoint_access
           (Values.AuthorizeEndpointAccessMessage.make ?clusterIdentifier
              ?vpcIds:(Option.map ~f:Values.VpcIdentifierList.of_json vpcIds)
              ~account ()) (Some Values.EndpointAuthorization.to_json)
           (Some Values.EndpointAuthorization.error_to_json)])
let authorize_snapshot_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotClusterIdentifier =
         flag "snapshot-cluster-identifier" (optional string)
           ~doc:"STRING String"
       and snapshotIdentifier =
         flag "snapshot-identifier" (required string) ~doc:"STRING String"
       and accountWithRestoreAccess =
         flag "account-with-restore-access" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.authorize_snapshot_access
           (Values.AuthorizeSnapshotAccessMessage.make
              ?snapshotClusterIdentifier ~snapshotIdentifier
              ~accountWithRestoreAccess ())
           (Some Values.AuthorizeSnapshotAccessResult.to_json)
           (Some Values.AuthorizeSnapshotAccessResult.error_to_json)])
let batch_delete_cluster_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifiers =
         flag "identifiers" (required json_arg)
           ~doc:"JSON DeleteClusterSnapshotMessageList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_cluster_snapshots
           (Values.BatchDeleteClusterSnapshotsRequest.make
              ~identifiers:(Values.DeleteClusterSnapshotMessageList.of_json
                              identifiers) ())
           (Some Values.BatchDeleteClusterSnapshotsResult.to_json)
           (Some Values.BatchDeleteClusterSnapshotsResult.error_to_json)])
let batch_modify_cluster_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and manualSnapshotRetentionPeriod =
         flag "manual-snapshot-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and snapshotIdentifierList =
         flag "snapshot-identifier-list" (required json_arg)
           ~doc:"JSON SnapshotIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_modify_cluster_snapshots
           (Values.BatchModifyClusterSnapshotsMessage.make
              ?manualSnapshotRetentionPeriod ?force
              ~snapshotIdentifierList:(Values.SnapshotIdentifierList.of_json
                                         snapshotIdentifierList) ())
           (Some Values.BatchModifyClusterSnapshotsOutputMessage.to_json)
           (Some
              Values.BatchModifyClusterSnapshotsOutputMessage.error_to_json)])
let cancel_resize =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_resize
           (Values.CancelResizeMessage.make ~clusterIdentifier ())
           (Some Values.ResizeProgressMessage.to_json)
           (Some Values.ResizeProgressMessage.error_to_json)])
let copy_cluster_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceSnapshotClusterIdentifier =
         flag "source-snapshot-cluster-identifier" (optional string)
           ~doc:"STRING String"
       and manualSnapshotRetentionPeriod =
         flag "manual-snapshot-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and sourceSnapshotIdentifier =
         flag "source-snapshot-identifier" (required string)
           ~doc:"STRING String"
       and targetSnapshotIdentifier =
         flag "target-snapshot-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_cluster_snapshot
           (Values.CopyClusterSnapshotMessage.make
              ?sourceSnapshotClusterIdentifier ?manualSnapshotRetentionPeriod
              ~sourceSnapshotIdentifier ~targetSnapshotIdentifier ())
           (Some Values.CopyClusterSnapshotResult.to_json)
           (Some Values.CopyClusterSnapshotResult.error_to_json)])
let create_authentication_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationProfileName =
         flag "authentication-profile-name" (required string)
           ~doc:"STRING AuthenticationProfileNameString"
       and authenticationProfileContent =
         flag "authentication-profile-content" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_authentication_profile
           (Values.CreateAuthenticationProfileMessage.make
              ~authenticationProfileName ~authenticationProfileContent ())
           (Some Values.CreateAuthenticationProfileResult.to_json)
           (Some Values.CreateAuthenticationProfileResult.error_to_json)])
let create_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBName = flag "d-b-name" (optional string) ~doc:"STRING String"
       and clusterType =
         flag "cluster-type" (optional string) ~doc:"STRING String"
       and clusterSecurityGroups =
         flag "cluster-security-groups" (optional json_arg)
           ~doc:"JSON ClusterSecurityGroupNameList"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and clusterSubnetGroupName =
         flag "cluster-subnet-group-name" (optional string)
           ~doc:"STRING String"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and clusterParameterGroupName =
         flag "cluster-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and automatedSnapshotRetentionPeriod =
         flag "automated-snapshot-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and manualSnapshotRetentionPeriod =
         flag "manual-snapshot-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and clusterVersion =
         flag "cluster-version" (optional string) ~doc:"STRING String"
       and allowVersionUpgrade =
         flag "allow-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and numberOfNodes =
         flag "number-of-nodes" (optional int) ~doc:"INT IntegerOptional"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and encrypted =
         flag "encrypted" (optional bool) ~doc:"BOOL BooleanOptional"
       and hsmClientCertificateIdentifier =
         flag "hsm-client-certificate-identifier" (optional string)
           ~doc:"STRING String"
       and hsmConfigurationIdentifier =
         flag "hsm-configuration-identifier" (optional string)
           ~doc:"STRING String"
       and elasticIp =
         flag "elastic-ip" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and enhancedVpcRouting =
         flag "enhanced-vpc-routing" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and additionalInfo =
         flag "additional-info" (optional string) ~doc:"STRING String"
       and iamRoles =
         flag "iam-roles" (optional json_arg) ~doc:"JSON IamRoleArnList"
       and maintenanceTrackName =
         flag "maintenance-track-name" (optional string) ~doc:"STRING String"
       and snapshotScheduleIdentifier =
         flag "snapshot-schedule-identifier" (optional string)
           ~doc:"STRING String"
       and availabilityZoneRelocation =
         flag "availability-zone-relocation" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and aquaConfigurationStatus =
         flag "aqua-configuration-status" (optional json_arg)
           ~doc:"JSON AquaConfigurationStatus"
       and defaultIamRoleArn =
         flag "default-iam-role-arn" (optional string) ~doc:"STRING String"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String"
       and nodeType = flag "node-type" (required string) ~doc:"STRING String"
       and masterUsername =
         flag "master-username" (required string) ~doc:"STRING String"
       and masterUserPassword =
         flag "master-user-password" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster
           (Values.CreateClusterMessage.make ?dBName ?clusterType
              ?clusterSecurityGroups:(Option.map
                                        ~f:Values.ClusterSecurityGroupNameList.of_json
                                        clusterSecurityGroups)
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds)
              ?clusterSubnetGroupName ?availabilityZone
              ?preferredMaintenanceWindow ?clusterParameterGroupName
              ?automatedSnapshotRetentionPeriod
              ?manualSnapshotRetentionPeriod ?port ?clusterVersion
              ?allowVersionUpgrade ?numberOfNodes ?publiclyAccessible
              ?encrypted ?hsmClientCertificateIdentifier
              ?hsmConfigurationIdentifier ?elasticIp
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?kmsKeyId
              ?enhancedVpcRouting ?additionalInfo
              ?iamRoles:(Option.map ~f:Values.IamRoleArnList.of_json iamRoles)
              ?maintenanceTrackName ?snapshotScheduleIdentifier
              ?availabilityZoneRelocation
              ?aquaConfigurationStatus:(Option.map
                                          ~f:Values.AquaConfigurationStatus.of_json
                                          aquaConfigurationStatus)
              ?defaultIamRoleArn ~clusterIdentifier ~nodeType ~masterUsername
              ~masterUserPassword ())
           (Some Values.CreateClusterResult.to_json)
           (Some Values.CreateClusterResult.error_to_json)])
let create_cluster_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and parameterGroupName =
         flag "parameter-group-name" (required string) ~doc:"STRING String"
       and parameterGroupFamily =
         flag "parameter-group-family" (required string) ~doc:"STRING String"
       and description =
         flag "description" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster_parameter_group
           (Values.CreateClusterParameterGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~parameterGroupName ~parameterGroupFamily ~description ())
           (Some Values.CreateClusterParameterGroupResult.to_json)
           (Some Values.CreateClusterParameterGroupResult.error_to_json)])
let create_cluster_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clusterSecurityGroupName =
         flag "cluster-security-group-name" (required string)
           ~doc:"STRING String"
       and description =
         flag "description" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster_security_group
           (Values.CreateClusterSecurityGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~clusterSecurityGroupName ~description ())
           (Some Values.CreateClusterSecurityGroupResult.to_json)
           (Some Values.CreateClusterSecurityGroupResult.error_to_json)])
let create_cluster_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and manualSnapshotRetentionPeriod =
         flag "manual-snapshot-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and snapshotIdentifier =
         flag "snapshot-identifier" (required string) ~doc:"STRING String"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster_snapshot
           (Values.CreateClusterSnapshotMessage.make
              ?manualSnapshotRetentionPeriod
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~snapshotIdentifier ~clusterIdentifier ())
           (Some Values.CreateClusterSnapshotResult.to_json)
           (Some Values.CreateClusterSnapshotResult.error_to_json)])
let create_cluster_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clusterSubnetGroupName =
         flag "cluster-subnet-group-name" (required string)
           ~doc:"STRING String"
       and description =
         flag "description" (required string) ~doc:"STRING String"
       and subnetIds =
         flag "subnet-ids" (required json_arg)
           ~doc:"JSON SubnetIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster_subnet_group
           (Values.CreateClusterSubnetGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~clusterSubnetGroupName ~description
              ~subnetIds:(Values.SubnetIdentifierList.of_json subnetIds) ())
           (Some Values.CreateClusterSubnetGroupResult.to_json)
           (Some Values.CreateClusterSubnetGroupResult.error_to_json)])
let create_endpoint_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (optional string) ~doc:"STRING String"
       and resourceOwner =
         flag "resource-owner" (optional string) ~doc:"STRING String"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING String"
       and subnetGroupName =
         flag "subnet-group-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_endpoint_access
           (Values.CreateEndpointAccessMessage.make ?clusterIdentifier
              ?resourceOwner
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ~endpointName
              ~subnetGroupName ()) (Some Values.EndpointAccess.to_json)
           (Some Values.EndpointAccess.error_to_json)])
let create_event_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceType =
         flag "source-type" (optional string) ~doc:"STRING String"
       and sourceIds =
         flag "source-ids" (optional json_arg) ~doc:"JSON SourceIdsList"
       and eventCategories =
         flag "event-categories" (optional json_arg)
           ~doc:"JSON EventCategoriesList"
       and severity = flag "severity" (optional string) ~doc:"STRING String"
       and enabled =
         flag "enabled" (optional bool) ~doc:"BOOL BooleanOptional"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and subscriptionName =
         flag "subscription-name" (required string) ~doc:"STRING String"
       and snsTopicArn =
         flag "sns-topic-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_event_subscription
           (Values.CreateEventSubscriptionMessage.make ?sourceType
              ?sourceIds:(Option.map ~f:Values.SourceIdsList.of_json
                            sourceIds)
              ?eventCategories:(Option.map
                                  ~f:Values.EventCategoriesList.of_json
                                  eventCategories) ?severity ?enabled
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~subscriptionName ~snsTopicArn ())
           (Some Values.CreateEventSubscriptionResult.to_json)
           (Some Values.CreateEventSubscriptionResult.error_to_json)])
let create_hsm_client_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and hsmClientCertificateIdentifier =
         flag "hsm-client-certificate-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_hsm_client_certificate
           (Values.CreateHsmClientCertificateMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~hsmClientCertificateIdentifier ())
           (Some Values.CreateHsmClientCertificateResult.to_json)
           (Some Values.CreateHsmClientCertificateResult.error_to_json)])
let create_hsm_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and hsmConfigurationIdentifier =
         flag "hsm-configuration-identifier" (required string)
           ~doc:"STRING String"
       and description =
         flag "description" (required string) ~doc:"STRING String"
       and hsmIpAddress =
         flag "hsm-ip-address" (required string) ~doc:"STRING String"
       and hsmPartitionName =
         flag "hsm-partition-name" (required string) ~doc:"STRING String"
       and hsmPartitionPassword =
         flag "hsm-partition-password" (required string) ~doc:"STRING String"
       and hsmServerPublicCertificate =
         flag "hsm-server-public-certificate" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_hsm_configuration
           (Values.CreateHsmConfigurationMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~hsmConfigurationIdentifier ~description ~hsmIpAddress
              ~hsmPartitionName ~hsmPartitionPassword
              ~hsmServerPublicCertificate ())
           (Some Values.CreateHsmConfigurationResult.to_json)
           (Some Values.CreateHsmConfigurationResult.error_to_json)])
let create_scheduled_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduledActionDescription =
         flag "scheduled-action-description" (optional string)
           ~doc:"STRING String"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TStamp"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON TStamp"
       and enable = flag "enable" (optional bool) ~doc:"BOOL BooleanOptional"
       and scheduledActionName =
         flag "scheduled-action-name" (required string) ~doc:"STRING String"
       and targetAction =
         flag "target-action" (required json_arg)
           ~doc:"JSON ScheduledActionType"
       and schedule = flag "schedule" (required string) ~doc:"STRING String"
       and iamRole = flag "iam-role" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_scheduled_action
           (Values.CreateScheduledActionMessage.make
              ?scheduledActionDescription
              ?startTime:(Option.map ~f:Values.TStamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.TStamp.of_json endTime) ?enable
              ~scheduledActionName
              ~targetAction:(Values.ScheduledActionType.of_json targetAction)
              ~schedule ~iamRole ()) (Some Values.ScheduledAction.to_json)
           (Some Values.ScheduledAction.error_to_json)])
let create_snapshot_copy_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and snapshotCopyGrantName =
         flag "snapshot-copy-grant-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_snapshot_copy_grant
           (Values.CreateSnapshotCopyGrantMessage.make ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~snapshotCopyGrantName ())
           (Some Values.CreateSnapshotCopyGrantResult.to_json)
           (Some Values.CreateSnapshotCopyGrantResult.error_to_json)])
let create_snapshot_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduleDefinitions =
         flag "schedule-definitions" (optional json_arg)
           ~doc:"JSON ScheduleDefinitionList"
       and scheduleIdentifier =
         flag "schedule-identifier" (optional string) ~doc:"STRING String"
       and scheduleDescription =
         flag "schedule-description" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dryRun =
         flag "dry-run" (optional bool) ~doc:"BOOL BooleanOptional"
       and nextInvocations =
         flag "next-invocations" (optional int) ~doc:"INT IntegerOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_snapshot_schedule
           (Values.CreateSnapshotScheduleMessage.make
              ?scheduleDefinitions:(Option.map
                                      ~f:Values.ScheduleDefinitionList.of_json
                                      scheduleDefinitions)
              ?scheduleIdentifier ?scheduleDescription
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?dryRun
              ?nextInvocations ()) (Some Values.SnapshotSchedule.to_json)
           (Some Values.SnapshotSchedule.error_to_json)])
let create_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_tags
           (Values.CreateTagsMessage.make ~resourceName
              ~tags:(Values.TagList.of_json tags) ()) None None])
let create_usage_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and period =
         flag "period" (optional json_arg) ~doc:"JSON UsageLimitPeriod"
       and breachAction =
         flag "breach-action" (optional json_arg)
           ~doc:"JSON UsageLimitBreachAction"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String"
       and featureType =
         flag "feature-type" (required json_arg)
           ~doc:"JSON UsageLimitFeatureType"
       and limitType =
         flag "limit-type" (required json_arg)
           ~doc:"JSON UsageLimitLimitType"
       and amount = flag "amount" (required json_arg) ~doc:"JSON Long" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_usage_limit
           (Values.CreateUsageLimitMessage.make
              ?period:(Option.map ~f:Values.UsageLimitPeriod.of_json period)
              ?breachAction:(Option.map
                               ~f:Values.UsageLimitBreachAction.of_json
                               breachAction)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~clusterIdentifier
              ~featureType:(Values.UsageLimitFeatureType.of_json featureType)
              ~limitType:(Values.UsageLimitLimitType.of_json limitType)
              ~amount:(Values.Long.of_json amount) ())
           (Some Values.UsageLimit.to_json)
           (Some Values.UsageLimit.error_to_json)])
let deauthorize_data_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataShareArn =
         flag "data-share-arn" (required string) ~doc:"STRING String"
       and consumerIdentifier =
         flag "consumer-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deauthorize_data_share
           (Values.DeauthorizeDataShareMessage.make ~dataShareArn
              ~consumerIdentifier ()) (Some Values.DataShare.to_json)
           (Some Values.DataShare.error_to_json)])
let delete_authentication_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationProfileName =
         flag "authentication-profile-name" (required string)
           ~doc:"STRING AuthenticationProfileNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_authentication_profile
           (Values.DeleteAuthenticationProfileMessage.make
              ~authenticationProfileName ())
           (Some Values.DeleteAuthenticationProfileResult.to_json)
           (Some Values.DeleteAuthenticationProfileResult.error_to_json)])
let delete_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipFinalClusterSnapshot =
         flag "skip-final-cluster-snapshot" (optional bool)
           ~doc:"BOOL Boolean"
       and finalClusterSnapshotIdentifier =
         flag "final-cluster-snapshot-identifier" (optional string)
           ~doc:"STRING String"
       and finalClusterSnapshotRetentionPeriod =
         flag "final-cluster-snapshot-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster
           (Values.DeleteClusterMessage.make ?skipFinalClusterSnapshot
              ?finalClusterSnapshotIdentifier
              ?finalClusterSnapshotRetentionPeriod ~clusterIdentifier ())
           (Some Values.DeleteClusterResult.to_json)
           (Some Values.DeleteClusterResult.error_to_json)])
let delete_cluster_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameterGroupName =
         flag "parameter-group-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster_parameter_group
           (Values.DeleteClusterParameterGroupMessage.make
              ~parameterGroupName ()) None None])
let delete_cluster_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterSecurityGroupName =
         flag "cluster-security-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster_security_group
           (Values.DeleteClusterSecurityGroupMessage.make
              ~clusterSecurityGroupName ()) None None])
let delete_cluster_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotClusterIdentifier =
         flag "snapshot-cluster-identifier" (optional string)
           ~doc:"STRING String"
       and snapshotIdentifier =
         flag "snapshot-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster_snapshot
           (Values.DeleteClusterSnapshotMessage.make
              ?snapshotClusterIdentifier ~snapshotIdentifier ())
           (Some Values.DeleteClusterSnapshotResult.to_json)
           (Some Values.DeleteClusterSnapshotResult.error_to_json)])
let delete_cluster_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterSubnetGroupName =
         flag "cluster-subnet-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster_subnet_group
           (Values.DeleteClusterSubnetGroupMessage.make
              ~clusterSubnetGroupName ()) None None])
let delete_endpoint_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_endpoint_access
           (Values.DeleteEndpointAccessMessage.make ~endpointName ())
           (Some Values.EndpointAccess.to_json)
           (Some Values.EndpointAccess.error_to_json)])
let delete_event_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionName =
         flag "subscription-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event_subscription
           (Values.DeleteEventSubscriptionMessage.make ~subscriptionName ())
           None None])
let delete_hsm_client_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hsmClientCertificateIdentifier =
         flag "hsm-client-certificate-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_hsm_client_certificate
           (Values.DeleteHsmClientCertificateMessage.make
              ~hsmClientCertificateIdentifier ()) None None])
let delete_hsm_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hsmConfigurationIdentifier =
         flag "hsm-configuration-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_hsm_configuration
           (Values.DeleteHsmConfigurationMessage.make
              ~hsmConfigurationIdentifier ()) None None])
let delete_partner =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string)
           ~doc:"STRING PartnerIntegrationAccountId"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING PartnerIntegrationClusterIdentifier"
       and databaseName =
         flag "database-name" (required string)
           ~doc:"STRING PartnerIntegrationDatabaseName"
       and partnerName =
         flag "partner-name" (required string)
           ~doc:"STRING PartnerIntegrationPartnerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_partner
           (Values.PartnerIntegrationInputMessage.make ~accountId
              ~clusterIdentifier ~databaseName ~partnerName ())
           (Some Values.PartnerIntegrationOutputMessage.to_json)
           (Some Values.PartnerIntegrationOutputMessage.error_to_json)])
let delete_scheduled_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduledActionName =
         flag "scheduled-action-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_scheduled_action
           (Values.DeleteScheduledActionMessage.make ~scheduledActionName ())
           None None])
let delete_snapshot_copy_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotCopyGrantName =
         flag "snapshot-copy-grant-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_snapshot_copy_grant
           (Values.DeleteSnapshotCopyGrantMessage.make ~snapshotCopyGrantName
              ()) None None])
let delete_snapshot_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduleIdentifier =
         flag "schedule-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_snapshot_schedule
           (Values.DeleteSnapshotScheduleMessage.make ~scheduleIdentifier ())
           None None])
let delete_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tags
           (Values.DeleteTagsMessage.make ~resourceName
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let delete_usage_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and usageLimitId =
         flag "usage-limit-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_usage_limit
           (Values.DeleteUsageLimitMessage.make ~usageLimitId ()) None None])
let describe_account_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeNames =
         flag "attribute-names" (optional json_arg)
           ~doc:"JSON AttributeNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_attributes
           (Values.DescribeAccountAttributesMessage.make
              ?attributeNames:(Option.map ~f:Values.AttributeNameList.of_json
                                 attributeNames) ())
           (Some Values.AccountAttributeList.to_json)
           (Some Values.AccountAttributeList.error_to_json)])
let describe_authentication_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationProfileName =
         flag "authentication-profile-name" (optional string)
           ~doc:"STRING AuthenticationProfileNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_authentication_profiles
           (Values.DescribeAuthenticationProfilesMessage.make
              ?authenticationProfileName ())
           (Some Values.DescribeAuthenticationProfilesResult.to_json)
           (Some Values.DescribeAuthenticationProfilesResult.error_to_json)])
let describe_cluster_db_revisions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster_db_revisions
           (Values.DescribeClusterDbRevisionsMessage.make ?clusterIdentifier
              ?maxRecords ?marker ())
           (Some Values.ClusterDbRevisionsMessage.to_json)
           (Some Values.ClusterDbRevisionsMessage.error_to_json)])
let describe_cluster_parameter_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameterGroupName =
         flag "parameter-group-name" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and tagValues =
         flag "tag-values" (optional json_arg) ~doc:"JSON TagValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster_parameter_groups
           (Values.DescribeClusterParameterGroupsMessage.make
              ?parameterGroupName ?maxRecords ?marker
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ?tagValues:(Option.map ~f:Values.TagValueList.of_json tagValues)
              ()) (Some Values.ClusterParameterGroupsMessage.to_json)
           (Some Values.ClusterParameterGroupsMessage.error_to_json)])
let describe_cluster_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and source = flag "source" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and parameterGroupName =
         flag "parameter-group-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster_parameters
           (Values.DescribeClusterParametersMessage.make ?source ?maxRecords
              ?marker ~parameterGroupName ())
           (Some Values.ClusterParameterGroupDetails.to_json)
           (Some Values.ClusterParameterGroupDetails.error_to_json)])
let describe_cluster_security_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterSecurityGroupName =
         flag "cluster-security-group-name" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and tagValues =
         flag "tag-values" (optional json_arg) ~doc:"JSON TagValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster_security_groups
           (Values.DescribeClusterSecurityGroupsMessage.make
              ?clusterSecurityGroupName ?maxRecords ?marker
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ?tagValues:(Option.map ~f:Values.TagValueList.of_json tagValues)
              ()) (Some Values.ClusterSecurityGroupMessage.to_json)
           (Some Values.ClusterSecurityGroupMessage.error_to_json)])
let describe_cluster_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (optional string) ~doc:"STRING String"
       and snapshotIdentifier =
         flag "snapshot-identifier" (optional string) ~doc:"STRING String"
       and snapshotType =
         flag "snapshot-type" (optional string) ~doc:"STRING String"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TStamp"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON TStamp"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and ownerAccount =
         flag "owner-account" (optional string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and tagValues =
         flag "tag-values" (optional json_arg) ~doc:"JSON TagValueList"
       and clusterExists =
         flag "cluster-exists" (optional bool) ~doc:"BOOL BooleanOptional"
       and sortingEntities =
         flag "sorting-entities" (optional json_arg)
           ~doc:"JSON SnapshotSortingEntityList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster_snapshots
           (Values.DescribeClusterSnapshotsMessage.make ?clusterIdentifier
              ?snapshotIdentifier ?snapshotType
              ?startTime:(Option.map ~f:Values.TStamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.TStamp.of_json endTime)
              ?maxRecords ?marker ?ownerAccount
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ?tagValues:(Option.map ~f:Values.TagValueList.of_json tagValues)
              ?clusterExists
              ?sortingEntities:(Option.map
                                  ~f:Values.SnapshotSortingEntityList.of_json
                                  sortingEntities) ())
           (Some Values.SnapshotMessage.to_json)
           (Some Values.SnapshotMessage.error_to_json)])
let describe_cluster_subnet_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterSubnetGroupName =
         flag "cluster-subnet-group-name" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and tagValues =
         flag "tag-values" (optional json_arg) ~doc:"JSON TagValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster_subnet_groups
           (Values.DescribeClusterSubnetGroupsMessage.make
              ?clusterSubnetGroupName ?maxRecords ?marker
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ?tagValues:(Option.map ~f:Values.TagValueList.of_json tagValues)
              ()) (Some Values.ClusterSubnetGroupMessage.to_json)
           (Some Values.ClusterSubnetGroupMessage.error_to_json)])
let describe_cluster_tracks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maintenanceTrackName =
         flag "maintenance-track-name" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster_tracks
           (Values.DescribeClusterTracksMessage.make ?maintenanceTrackName
              ?maxRecords ?marker ()) (Some Values.TrackListMessage.to_json)
           (Some Values.TrackListMessage.error_to_json)])
let describe_cluster_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterVersion =
         flag "cluster-version" (optional string) ~doc:"STRING String"
       and clusterParameterGroupFamily =
         flag "cluster-parameter-group-family" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster_versions
           (Values.DescribeClusterVersionsMessage.make ?clusterVersion
              ?clusterParameterGroupFamily ?maxRecords ?marker ())
           (Some Values.ClusterVersionsMessage.to_json)
           (Some Values.ClusterVersionsMessage.error_to_json)])
let describe_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and tagValues =
         flag "tag-values" (optional json_arg) ~doc:"JSON TagValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_clusters
           (Values.DescribeClustersMessage.make ?clusterIdentifier
              ?maxRecords ?marker
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ?tagValues:(Option.map ~f:Values.TagValueList.of_json tagValues)
              ()) (Some Values.ClustersMessage.to_json)
           (Some Values.ClustersMessage.error_to_json)])
let describe_data_shares =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataShareArn =
         flag "data-share-arn" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_data_shares
           (Values.DescribeDataSharesMessage.make ?dataShareArn ?maxRecords
              ?marker ()) (Some Values.DescribeDataSharesResult.to_json)
           (Some Values.DescribeDataSharesResult.error_to_json)])
let describe_data_shares_for_consumer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and consumerArn =
         flag "consumer-arn" (optional string) ~doc:"STRING String"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON DataShareStatusForConsumer"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_data_shares_for_consumer
           (Values.DescribeDataSharesForConsumerMessage.make ?consumerArn
              ?status:(Option.map
                         ~f:Values.DataShareStatusForConsumer.of_json status)
              ?maxRecords ?marker ())
           (Some Values.DescribeDataSharesForConsumerResult.to_json)
           (Some Values.DescribeDataSharesForConsumerResult.error_to_json)])
let describe_data_shares_for_producer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and producerArn =
         flag "producer-arn" (optional string) ~doc:"STRING String"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON DataShareStatusForProducer"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_data_shares_for_producer
           (Values.DescribeDataSharesForProducerMessage.make ?producerArn
              ?status:(Option.map
                         ~f:Values.DataShareStatusForProducer.of_json status)
              ?maxRecords ?marker ())
           (Some Values.DescribeDataSharesForProducerResult.to_json)
           (Some Values.DescribeDataSharesForProducerResult.error_to_json)])
let describe_default_cluster_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and parameterGroupFamily =
         flag "parameter-group-family" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_default_cluster_parameters
           (Values.DescribeDefaultClusterParametersMessage.make ?maxRecords
              ?marker ~parameterGroupFamily ())
           (Some Values.DescribeDefaultClusterParametersResult.to_json)
           (Some Values.DescribeDefaultClusterParametersResult.error_to_json)])
let describe_endpoint_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (optional string) ~doc:"STRING String"
       and resourceOwner =
         flag "resource-owner" (optional string) ~doc:"STRING String"
       and endpointName =
         flag "endpoint-name" (optional string) ~doc:"STRING String"
       and vpcId = flag "vpc-id" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoint_access
           (Values.DescribeEndpointAccessMessage.make ?clusterIdentifier
              ?resourceOwner ?endpointName ?vpcId ?maxRecords ?marker ())
           (Some Values.EndpointAccessList.to_json)
           (Some Values.EndpointAccessList.error_to_json)])
let describe_endpoint_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (optional string) ~doc:"STRING String"
       and account = flag "account" (optional string) ~doc:"STRING String"
       and grantee =
         flag "grantee" (optional bool) ~doc:"BOOL BooleanOptional"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoint_authorization
           (Values.DescribeEndpointAuthorizationMessage.make
              ?clusterIdentifier ?account ?grantee ?maxRecords ?marker ())
           (Some Values.EndpointAuthorizationList.to_json)
           (Some Values.EndpointAuthorizationList.error_to_json)])
let describe_event_categories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceType =
         flag "source-type" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_categories
           (Values.DescribeEventCategoriesMessage.make ?sourceType ())
           (Some Values.EventCategoriesMessage.to_json)
           (Some Values.EventCategoriesMessage.error_to_json)])
let describe_event_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionName =
         flag "subscription-name" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and tagValues =
         flag "tag-values" (optional json_arg) ~doc:"JSON TagValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_subscriptions
           (Values.DescribeEventSubscriptionsMessage.make ?subscriptionName
              ?maxRecords ?marker
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ?tagValues:(Option.map ~f:Values.TagValueList.of_json tagValues)
              ()) (Some Values.EventSubscriptionsMessage.to_json)
           (Some Values.EventSubscriptionsMessage.error_to_json)])
let describe_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceIdentifier =
         flag "source-identifier" (optional string) ~doc:"STRING String"
       and sourceType =
         flag "source-type" (optional json_arg) ~doc:"JSON SourceType"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TStamp"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON TStamp"
       and duration =
         flag "duration" (optional int) ~doc:"INT IntegerOptional"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_events
           (Values.DescribeEventsMessage.make ?sourceIdentifier
              ?sourceType:(Option.map ~f:Values.SourceType.of_json sourceType)
              ?startTime:(Option.map ~f:Values.TStamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.TStamp.of_json endTime)
              ?duration ?maxRecords ?marker ())
           (Some Values.EventsMessage.to_json)
           (Some Values.EventsMessage.error_to_json)])
let describe_hsm_client_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hsmClientCertificateIdentifier =
         flag "hsm-client-certificate-identifier" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and tagValues =
         flag "tag-values" (optional json_arg) ~doc:"JSON TagValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_hsm_client_certificates
           (Values.DescribeHsmClientCertificatesMessage.make
              ?hsmClientCertificateIdentifier ?maxRecords ?marker
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ?tagValues:(Option.map ~f:Values.TagValueList.of_json tagValues)
              ()) (Some Values.HsmClientCertificateMessage.to_json)
           (Some Values.HsmClientCertificateMessage.error_to_json)])
let describe_hsm_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hsmConfigurationIdentifier =
         flag "hsm-configuration-identifier" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and tagValues =
         flag "tag-values" (optional json_arg) ~doc:"JSON TagValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_hsm_configurations
           (Values.DescribeHsmConfigurationsMessage.make
              ?hsmConfigurationIdentifier ?maxRecords ?marker
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ?tagValues:(Option.map ~f:Values.TagValueList.of_json tagValues)
              ()) (Some Values.HsmConfigurationMessage.to_json)
           (Some Values.HsmConfigurationMessage.error_to_json)])
let describe_logging_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_logging_status
           (Values.DescribeLoggingStatusMessage.make ~clusterIdentifier ())
           (Some Values.LoggingStatus.to_json)
           (Some Values.LoggingStatus.error_to_json)])
let describe_node_configuration_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (optional string) ~doc:"STRING String"
       and snapshotIdentifier =
         flag "snapshot-identifier" (optional string) ~doc:"STRING String"
       and ownerAccount =
         flag "owner-account" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON NodeConfigurationOptionsFilterList"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and actionType =
         flag "action-type" (required json_arg) ~doc:"JSON ActionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_node_configuration_options
           (Values.DescribeNodeConfigurationOptionsMessage.make
              ?clusterIdentifier ?snapshotIdentifier ?ownerAccount
              ?filters:(Option.map
                          ~f:Values.NodeConfigurationOptionsFilterList.of_json
                          filters) ?marker ?maxRecords
              ~actionType:(Values.ActionType.of_json actionType) ())
           (Some Values.NodeConfigurationOptionsMessage.to_json)
           (Some Values.NodeConfigurationOptionsMessage.error_to_json)])
let describe_orderable_cluster_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterVersion =
         flag "cluster-version" (optional string) ~doc:"STRING String"
       and nodeType = flag "node-type" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_orderable_cluster_options
           (Values.DescribeOrderableClusterOptionsMessage.make
              ?clusterVersion ?nodeType ?maxRecords ?marker ())
           (Some Values.OrderableClusterOptionsMessage.to_json)
           (Some Values.OrderableClusterOptionsMessage.error_to_json)])
let describe_partners =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseName =
         flag "database-name" (optional string)
           ~doc:"STRING PartnerIntegrationDatabaseName"
       and partnerName =
         flag "partner-name" (optional string)
           ~doc:"STRING PartnerIntegrationPartnerName"
       and accountId =
         flag "account-id" (required string)
           ~doc:"STRING PartnerIntegrationAccountId"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING PartnerIntegrationClusterIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_partners
           (Values.DescribePartnersInputMessage.make ?databaseName
              ?partnerName ~accountId ~clusterIdentifier ())
           (Some Values.DescribePartnersOutputMessage.to_json)
           (Some Values.DescribePartnersOutputMessage.error_to_json)])
let describe_reserved_node_exchange_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedNodeId =
         flag "reserved-node-id" (optional string) ~doc:"STRING String"
       and reservedNodeExchangeRequestId =
         flag "reserved-node-exchange-request-id" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_node_exchange_status
           (Values.DescribeReservedNodeExchangeStatusInputMessage.make
              ?reservedNodeId ?reservedNodeExchangeRequestId ?maxRecords
              ?marker ())
           (Some
              Values.DescribeReservedNodeExchangeStatusOutputMessage.to_json)
           (Some
              Values.DescribeReservedNodeExchangeStatusOutputMessage.error_to_json)])
let describe_reserved_node_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedNodeOfferingId =
         flag "reserved-node-offering-id" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_node_offerings
           (Values.DescribeReservedNodeOfferingsMessage.make
              ?reservedNodeOfferingId ?maxRecords ?marker ())
           (Some Values.ReservedNodeOfferingsMessage.to_json)
           (Some Values.ReservedNodeOfferingsMessage.error_to_json)])
let describe_reserved_nodes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedNodeId =
         flag "reserved-node-id" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_nodes
           (Values.DescribeReservedNodesMessage.make ?reservedNodeId
              ?maxRecords ?marker ())
           (Some Values.ReservedNodesMessage.to_json)
           (Some Values.ReservedNodesMessage.error_to_json)])
let describe_resize =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_resize
           (Values.DescribeResizeMessage.make ~clusterIdentifier ())
           (Some Values.ResizeProgressMessage.to_json)
           (Some Values.ResizeProgressMessage.error_to_json)])
let describe_scheduled_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduledActionName =
         flag "scheduled-action-name" (optional string) ~doc:"STRING String"
       and targetActionType =
         flag "target-action-type" (optional json_arg)
           ~doc:"JSON ScheduledActionTypeValues"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TStamp"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON TStamp"
       and active = flag "active" (optional bool) ~doc:"BOOL BooleanOptional"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ScheduledActionFilterList"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scheduled_actions
           (Values.DescribeScheduledActionsMessage.make ?scheduledActionName
              ?targetActionType:(Option.map
                                   ~f:Values.ScheduledActionTypeValues.of_json
                                   targetActionType)
              ?startTime:(Option.map ~f:Values.TStamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.TStamp.of_json endTime) ?active
              ?filters:(Option.map
                          ~f:Values.ScheduledActionFilterList.of_json filters)
              ?marker ?maxRecords ())
           (Some Values.ScheduledActionsMessage.to_json)
           (Some Values.ScheduledActionsMessage.error_to_json)])
let describe_snapshot_copy_grants =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotCopyGrantName =
         flag "snapshot-copy-grant-name" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and tagValues =
         flag "tag-values" (optional json_arg) ~doc:"JSON TagValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_snapshot_copy_grants
           (Values.DescribeSnapshotCopyGrantsMessage.make
              ?snapshotCopyGrantName ?maxRecords ?marker
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ?tagValues:(Option.map ~f:Values.TagValueList.of_json tagValues)
              ()) (Some Values.SnapshotCopyGrantMessage.to_json)
           (Some Values.SnapshotCopyGrantMessage.error_to_json)])
let describe_snapshot_schedules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (optional string) ~doc:"STRING String"
       and scheduleIdentifier =
         flag "schedule-identifier" (optional string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and tagValues =
         flag "tag-values" (optional json_arg) ~doc:"JSON TagValueList"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_snapshot_schedules
           (Values.DescribeSnapshotSchedulesMessage.make ?clusterIdentifier
              ?scheduleIdentifier
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ?tagValues:(Option.map ~f:Values.TagValueList.of_json tagValues)
              ?marker ?maxRecords ())
           (Some Values.DescribeSnapshotSchedulesOutputMessage.to_json)
           (Some Values.DescribeSnapshotSchedulesOutputMessage.error_to_json)])
let describe_storage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_storage (Fn.id ())
           (Some Values.CustomerStorageMessage.to_json)
           (Some Values.CustomerStorageMessage.error_to_json)])
let describe_table_restore_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (optional string) ~doc:"STRING String"
       and tableRestoreRequestId =
         flag "table-restore-request-id" (optional string)
           ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_table_restore_status
           (Values.DescribeTableRestoreStatusMessage.make ?clusterIdentifier
              ?tableRestoreRequestId ?maxRecords ?marker ())
           (Some Values.TableRestoreStatusMessage.to_json)
           (Some Values.TableRestoreStatusMessage.error_to_json)])
let describe_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (optional string) ~doc:"STRING String"
       and resourceType =
         flag "resource-type" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and tagValues =
         flag "tag-values" (optional json_arg) ~doc:"JSON TagValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_tags
           (Values.DescribeTagsMessage.make ?resourceName ?resourceType
              ?maxRecords ?marker
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ?tagValues:(Option.map ~f:Values.TagValueList.of_json tagValues)
              ()) (Some Values.TaggedResourceListMessage.to_json)
           (Some Values.TaggedResourceListMessage.error_to_json)])
let describe_usage_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and usageLimitId =
         flag "usage-limit-id" (optional string) ~doc:"STRING String"
       and clusterIdentifier =
         flag "cluster-identifier" (optional string) ~doc:"STRING String"
       and featureType =
         flag "feature-type" (optional json_arg)
           ~doc:"JSON UsageLimitFeatureType"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and tagValues =
         flag "tag-values" (optional json_arg) ~doc:"JSON TagValueList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_usage_limits
           (Values.DescribeUsageLimitsMessage.make ?usageLimitId
              ?clusterIdentifier
              ?featureType:(Option.map
                              ~f:Values.UsageLimitFeatureType.of_json
                              featureType) ?maxRecords ?marker
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ?tagValues:(Option.map ~f:Values.TagValueList.of_json tagValues)
              ()) (Some Values.UsageLimitList.to_json)
           (Some Values.UsageLimitList.error_to_json)])
let disable_logging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_logging
           (Values.DisableLoggingMessage.make ~clusterIdentifier ())
           (Some Values.LoggingStatus.to_json)
           (Some Values.LoggingStatus.error_to_json)])
let disable_snapshot_copy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_snapshot_copy
           (Values.DisableSnapshotCopyMessage.make ~clusterIdentifier ())
           (Some Values.DisableSnapshotCopyResult.to_json)
           (Some Values.DisableSnapshotCopyResult.error_to_json)])
let disassociate_data_share_consumer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and disassociateEntireAccount =
         flag "disassociate-entire-account" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and consumerArn =
         flag "consumer-arn" (optional string) ~doc:"STRING String"
       and consumerRegion =
         flag "consumer-region" (optional string) ~doc:"STRING String"
       and dataShareArn =
         flag "data-share-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_data_share_consumer
           (Values.DisassociateDataShareConsumerMessage.make
              ?disassociateEntireAccount ?consumerArn ?consumerRegion
              ~dataShareArn ()) (Some Values.DataShare.to_json)
           (Some Values.DataShare.error_to_json)])
let enable_logging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3KeyPrefix =
         flag "s3-key-prefix" (optional string) ~doc:"STRING String"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String"
       and bucketName =
         flag "bucket-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_logging
           (Values.EnableLoggingMessage.make ?s3KeyPrefix ~clusterIdentifier
              ~bucketName ()) (Some Values.LoggingStatus.to_json)
           (Some Values.LoggingStatus.error_to_json)])
let enable_snapshot_copy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retentionPeriod =
         flag "retention-period" (optional int) ~doc:"INT IntegerOptional"
       and snapshotCopyGrantName =
         flag "snapshot-copy-grant-name" (optional string)
           ~doc:"STRING String"
       and manualSnapshotRetentionPeriod =
         flag "manual-snapshot-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String"
       and destinationRegion =
         flag "destination-region" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_snapshot_copy
           (Values.EnableSnapshotCopyMessage.make ?retentionPeriod
              ?snapshotCopyGrantName ?manualSnapshotRetentionPeriod
              ~clusterIdentifier ~destinationRegion ())
           (Some Values.EnableSnapshotCopyResult.to_json)
           (Some Values.EnableSnapshotCopyResult.error_to_json)])
let get_cluster_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dbName = flag "db-name" (optional string) ~doc:"STRING String"
       and durationSeconds =
         flag "duration-seconds" (optional int) ~doc:"INT IntegerOptional"
       and autoCreate =
         flag "auto-create" (optional bool) ~doc:"BOOL BooleanOptional"
       and dbGroups =
         flag "db-groups" (optional json_arg) ~doc:"JSON DbGroupList"
       and dbUser = flag "db-user" (required string) ~doc:"STRING String"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cluster_credentials
           (Values.GetClusterCredentialsMessage.make ?dbName ?durationSeconds
              ?autoCreate
              ?dbGroups:(Option.map ~f:Values.DbGroupList.of_json dbGroups)
              ~dbUser ~clusterIdentifier ())
           (Some Values.ClusterCredentials.to_json)
           (Some Values.ClusterCredentials.error_to_json)])
let get_reserved_node_exchange_configuration_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (optional string) ~doc:"STRING String"
       and snapshotIdentifier =
         flag "snapshot-identifier" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and actionType =
         flag "action-type" (required json_arg)
           ~doc:"JSON ReservedNodeExchangeActionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_reserved_node_exchange_configuration_options
           (Values.GetReservedNodeExchangeConfigurationOptionsInputMessage.make
              ?clusterIdentifier ?snapshotIdentifier ?maxRecords ?marker
              ~actionType:(Values.ReservedNodeExchangeActionType.of_json
                             actionType) ())
           (Some
              Values.GetReservedNodeExchangeConfigurationOptionsOutputMessage.to_json)
           (Some
              Values.GetReservedNodeExchangeConfigurationOptionsOutputMessage.error_to_json)])
let get_reserved_node_exchange_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and reservedNodeId =
         flag "reserved-node-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_reserved_node_exchange_offerings
           (Values.GetReservedNodeExchangeOfferingsInputMessage.make
              ?maxRecords ?marker ~reservedNodeId ())
           (Some Values.GetReservedNodeExchangeOfferingsOutputMessage.to_json)
           (Some
              Values.GetReservedNodeExchangeOfferingsOutputMessage.error_to_json)])
let modify_aqua_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aquaConfigurationStatus =
         flag "aqua-configuration-status" (optional json_arg)
           ~doc:"JSON AquaConfigurationStatus"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_aqua_configuration
           (Values.ModifyAquaInputMessage.make
              ?aquaConfigurationStatus:(Option.map
                                          ~f:Values.AquaConfigurationStatus.of_json
                                          aquaConfigurationStatus)
              ~clusterIdentifier ())
           (Some Values.ModifyAquaOutputMessage.to_json)
           (Some Values.ModifyAquaOutputMessage.error_to_json)])
let modify_authentication_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationProfileName =
         flag "authentication-profile-name" (required string)
           ~doc:"STRING AuthenticationProfileNameString"
       and authenticationProfileContent =
         flag "authentication-profile-content" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_authentication_profile
           (Values.ModifyAuthenticationProfileMessage.make
              ~authenticationProfileName ~authenticationProfileContent ())
           (Some Values.ModifyAuthenticationProfileResult.to_json)
           (Some Values.ModifyAuthenticationProfileResult.error_to_json)])
let modify_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterType =
         flag "cluster-type" (optional string) ~doc:"STRING String"
       and nodeType = flag "node-type" (optional string) ~doc:"STRING String"
       and numberOfNodes =
         flag "number-of-nodes" (optional int) ~doc:"INT IntegerOptional"
       and clusterSecurityGroups =
         flag "cluster-security-groups" (optional json_arg)
           ~doc:"JSON ClusterSecurityGroupNameList"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and masterUserPassword =
         flag "master-user-password" (optional string) ~doc:"STRING String"
       and clusterParameterGroupName =
         flag "cluster-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and automatedSnapshotRetentionPeriod =
         flag "automated-snapshot-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and manualSnapshotRetentionPeriod =
         flag "manual-snapshot-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and clusterVersion =
         flag "cluster-version" (optional string) ~doc:"STRING String"
       and allowVersionUpgrade =
         flag "allow-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and hsmClientCertificateIdentifier =
         flag "hsm-client-certificate-identifier" (optional string)
           ~doc:"STRING String"
       and hsmConfigurationIdentifier =
         flag "hsm-configuration-identifier" (optional string)
           ~doc:"STRING String"
       and newClusterIdentifier =
         flag "new-cluster-identifier" (optional string) ~doc:"STRING String"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and elasticIp =
         flag "elastic-ip" (optional string) ~doc:"STRING String"
       and enhancedVpcRouting =
         flag "enhanced-vpc-routing" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and maintenanceTrackName =
         flag "maintenance-track-name" (optional string) ~doc:"STRING String"
       and encrypted =
         flag "encrypted" (optional bool) ~doc:"BOOL BooleanOptional"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and availabilityZoneRelocation =
         flag "availability-zone-relocation" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_cluster
           (Values.ModifyClusterMessage.make ?clusterType ?nodeType
              ?numberOfNodes
              ?clusterSecurityGroups:(Option.map
                                        ~f:Values.ClusterSecurityGroupNameList.of_json
                                        clusterSecurityGroups)
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds)
              ?masterUserPassword ?clusterParameterGroupName
              ?automatedSnapshotRetentionPeriod
              ?manualSnapshotRetentionPeriod ?preferredMaintenanceWindow
              ?clusterVersion ?allowVersionUpgrade
              ?hsmClientCertificateIdentifier ?hsmConfigurationIdentifier
              ?newClusterIdentifier ?publiclyAccessible ?elasticIp
              ?enhancedVpcRouting ?maintenanceTrackName ?encrypted ?kmsKeyId
              ?availabilityZoneRelocation ?availabilityZone ?port
              ~clusterIdentifier ())
           (Some Values.ModifyClusterResult.to_json)
           (Some Values.ModifyClusterResult.error_to_json)])
let modify_cluster_db_revision =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String"
       and revisionTarget =
         flag "revision-target" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_cluster_db_revision
           (Values.ModifyClusterDbRevisionMessage.make ~clusterIdentifier
              ~revisionTarget ())
           (Some Values.ModifyClusterDbRevisionResult.to_json)
           (Some Values.ModifyClusterDbRevisionResult.error_to_json)])
let modify_cluster_iam_roles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addIamRoles =
         flag "add-iam-roles" (optional json_arg) ~doc:"JSON IamRoleArnList"
       and removeIamRoles =
         flag "remove-iam-roles" (optional json_arg)
           ~doc:"JSON IamRoleArnList"
       and defaultIamRoleArn =
         flag "default-iam-role-arn" (optional string) ~doc:"STRING String"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_cluster_iam_roles
           (Values.ModifyClusterIamRolesMessage.make
              ?addIamRoles:(Option.map ~f:Values.IamRoleArnList.of_json
                              addIamRoles)
              ?removeIamRoles:(Option.map ~f:Values.IamRoleArnList.of_json
                                 removeIamRoles) ?defaultIamRoleArn
              ~clusterIdentifier ())
           (Some Values.ModifyClusterIamRolesResult.to_json)
           (Some Values.ModifyClusterIamRolesResult.error_to_json)])
let modify_cluster_maintenance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deferMaintenance =
         flag "defer-maintenance" (optional bool) ~doc:"BOOL BooleanOptional"
       and deferMaintenanceIdentifier =
         flag "defer-maintenance-identifier" (optional string)
           ~doc:"STRING String"
       and deferMaintenanceStartTime =
         flag "defer-maintenance-start-time" (optional json_arg)
           ~doc:"JSON TStamp"
       and deferMaintenanceEndTime =
         flag "defer-maintenance-end-time" (optional json_arg)
           ~doc:"JSON TStamp"
       and deferMaintenanceDuration =
         flag "defer-maintenance-duration" (optional int)
           ~doc:"INT IntegerOptional"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_cluster_maintenance
           (Values.ModifyClusterMaintenanceMessage.make ?deferMaintenance
              ?deferMaintenanceIdentifier
              ?deferMaintenanceStartTime:(Option.map ~f:Values.TStamp.of_json
                                            deferMaintenanceStartTime)
              ?deferMaintenanceEndTime:(Option.map ~f:Values.TStamp.of_json
                                          deferMaintenanceEndTime)
              ?deferMaintenanceDuration ~clusterIdentifier ())
           (Some Values.ModifyClusterMaintenanceResult.to_json)
           (Some Values.ModifyClusterMaintenanceResult.error_to_json)])
let modify_cluster_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameterGroupName =
         flag "parameter-group-name" (required string) ~doc:"STRING String"
       and parameters =
         flag "parameters" (required json_arg) ~doc:"JSON ParametersList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_cluster_parameter_group
           (Values.ModifyClusterParameterGroupMessage.make
              ~parameterGroupName
              ~parameters:(Values.ParametersList.of_json parameters) ())
           (Some Values.ClusterParameterGroupNameMessage.to_json)
           (Some Values.ClusterParameterGroupNameMessage.error_to_json)])
let modify_cluster_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and manualSnapshotRetentionPeriod =
         flag "manual-snapshot-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and snapshotIdentifier =
         flag "snapshot-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_cluster_snapshot
           (Values.ModifyClusterSnapshotMessage.make
              ?manualSnapshotRetentionPeriod ?force ~snapshotIdentifier ())
           (Some Values.ModifyClusterSnapshotResult.to_json)
           (Some Values.ModifyClusterSnapshotResult.error_to_json)])
let modify_cluster_snapshot_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduleIdentifier =
         flag "schedule-identifier" (optional string) ~doc:"STRING String"
       and disassociateSchedule =
         flag "disassociate-schedule" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_cluster_snapshot_schedule
           (Values.ModifyClusterSnapshotScheduleMessage.make
              ?scheduleIdentifier ?disassociateSchedule ~clusterIdentifier ())
           None None])
let modify_cluster_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and clusterSubnetGroupName =
         flag "cluster-subnet-group-name" (required string)
           ~doc:"STRING String"
       and subnetIds =
         flag "subnet-ids" (required json_arg)
           ~doc:"JSON SubnetIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_cluster_subnet_group
           (Values.ModifyClusterSubnetGroupMessage.make ?description
              ~clusterSubnetGroupName
              ~subnetIds:(Values.SubnetIdentifierList.of_json subnetIds) ())
           (Some Values.ModifyClusterSubnetGroupResult.to_json)
           (Some Values.ModifyClusterSubnetGroupResult.error_to_json)])
let modify_endpoint_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_endpoint_access
           (Values.ModifyEndpointAccessMessage.make
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ~endpointName ())
           (Some Values.EndpointAccess.to_json)
           (Some Values.EndpointAccess.error_to_json)])
let modify_event_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snsTopicArn =
         flag "sns-topic-arn" (optional string) ~doc:"STRING String"
       and sourceType =
         flag "source-type" (optional string) ~doc:"STRING String"
       and sourceIds =
         flag "source-ids" (optional json_arg) ~doc:"JSON SourceIdsList"
       and eventCategories =
         flag "event-categories" (optional json_arg)
           ~doc:"JSON EventCategoriesList"
       and severity = flag "severity" (optional string) ~doc:"STRING String"
       and enabled =
         flag "enabled" (optional bool) ~doc:"BOOL BooleanOptional"
       and subscriptionName =
         flag "subscription-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_event_subscription
           (Values.ModifyEventSubscriptionMessage.make ?snsTopicArn
              ?sourceType
              ?sourceIds:(Option.map ~f:Values.SourceIdsList.of_json
                            sourceIds)
              ?eventCategories:(Option.map
                                  ~f:Values.EventCategoriesList.of_json
                                  eventCategories) ?severity ?enabled
              ~subscriptionName ())
           (Some Values.ModifyEventSubscriptionResult.to_json)
           (Some Values.ModifyEventSubscriptionResult.error_to_json)])
let modify_scheduled_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetAction =
         flag "target-action" (optional json_arg)
           ~doc:"JSON ScheduledActionType"
       and schedule = flag "schedule" (optional string) ~doc:"STRING String"
       and iamRole = flag "iam-role" (optional string) ~doc:"STRING String"
       and scheduledActionDescription =
         flag "scheduled-action-description" (optional string)
           ~doc:"STRING String"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TStamp"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON TStamp"
       and enable = flag "enable" (optional bool) ~doc:"BOOL BooleanOptional"
       and scheduledActionName =
         flag "scheduled-action-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_scheduled_action
           (Values.ModifyScheduledActionMessage.make
              ?targetAction:(Option.map ~f:Values.ScheduledActionType.of_json
                               targetAction) ?schedule ?iamRole
              ?scheduledActionDescription
              ?startTime:(Option.map ~f:Values.TStamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.TStamp.of_json endTime) ?enable
              ~scheduledActionName ()) (Some Values.ScheduledAction.to_json)
           (Some Values.ScheduledAction.error_to_json)])
let modify_snapshot_copy_retention_period =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and manual = flag "manual" (optional bool) ~doc:"BOOL Boolean"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String"
       and retentionPeriod =
         flag "retention-period" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_snapshot_copy_retention_period
           (Values.ModifySnapshotCopyRetentionPeriodMessage.make ?manual
              ~clusterIdentifier ~retentionPeriod ())
           (Some Values.ModifySnapshotCopyRetentionPeriodResult.to_json)
           (Some Values.ModifySnapshotCopyRetentionPeriodResult.error_to_json)])
let modify_snapshot_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduleIdentifier =
         flag "schedule-identifier" (required string) ~doc:"STRING String"
       and scheduleDefinitions =
         flag "schedule-definitions" (required json_arg)
           ~doc:"JSON ScheduleDefinitionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_snapshot_schedule
           (Values.ModifySnapshotScheduleMessage.make ~scheduleIdentifier
              ~scheduleDefinitions:(Values.ScheduleDefinitionList.of_json
                                      scheduleDefinitions) ())
           (Some Values.SnapshotSchedule.to_json)
           (Some Values.SnapshotSchedule.error_to_json)])
let modify_usage_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amount =
         flag "amount" (optional json_arg) ~doc:"JSON LongOptional"
       and breachAction =
         flag "breach-action" (optional json_arg)
           ~doc:"JSON UsageLimitBreachAction"
       and usageLimitId =
         flag "usage-limit-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_usage_limit
           (Values.ModifyUsageLimitMessage.make
              ?amount:(Option.map ~f:Values.LongOptional.of_json amount)
              ?breachAction:(Option.map
                               ~f:Values.UsageLimitBreachAction.of_json
                               breachAction) ~usageLimitId ())
           (Some Values.UsageLimit.to_json)
           (Some Values.UsageLimit.error_to_json)])
let pause_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.pause_cluster
           (Values.PauseClusterMessage.make ~clusterIdentifier ())
           (Some Values.PauseClusterResult.to_json)
           (Some Values.PauseClusterResult.error_to_json)])
let purchase_reserved_node_offering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nodeCount =
         flag "node-count" (optional int) ~doc:"INT IntegerOptional"
       and reservedNodeOfferingId =
         flag "reserved-node-offering-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.purchase_reserved_node_offering
           (Values.PurchaseReservedNodeOfferingMessage.make ?nodeCount
              ~reservedNodeOfferingId ())
           (Some Values.PurchaseReservedNodeOfferingResult.to_json)
           (Some Values.PurchaseReservedNodeOfferingResult.error_to_json)])
let reboot_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_cluster
           (Values.RebootClusterMessage.make ~clusterIdentifier ())
           (Some Values.RebootClusterResult.to_json)
           (Some Values.RebootClusterResult.error_to_json)])
let reject_data_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataShareArn =
         flag "data-share-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_data_share
           (Values.RejectDataShareMessage.make ~dataShareArn ())
           (Some Values.DataShare.to_json)
           (Some Values.DataShare.error_to_json)])
let reset_cluster_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resetAllParameters =
         flag "reset-all-parameters" (optional bool) ~doc:"BOOL Boolean"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON ParametersList"
       and parameterGroupName =
         flag "parameter-group-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_cluster_parameter_group
           (Values.ResetClusterParameterGroupMessage.make ?resetAllParameters
              ?parameters:(Option.map ~f:Values.ParametersList.of_json
                             parameters) ~parameterGroupName ())
           (Some Values.ClusterParameterGroupNameMessage.to_json)
           (Some Values.ClusterParameterGroupNameMessage.error_to_json)])
let resize_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterType =
         flag "cluster-type" (optional string) ~doc:"STRING String"
       and nodeType = flag "node-type" (optional string) ~doc:"STRING String"
       and numberOfNodes =
         flag "number-of-nodes" (optional int) ~doc:"INT IntegerOptional"
       and classic =
         flag "classic" (optional bool) ~doc:"BOOL BooleanOptional"
       and reservedNodeId =
         flag "reserved-node-id" (optional string) ~doc:"STRING String"
       and targetReservedNodeOfferingId =
         flag "target-reserved-node-offering-id" (optional string)
           ~doc:"STRING String"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resize_cluster
           (Values.ResizeClusterMessage.make ?clusterType ?nodeType
              ?numberOfNodes ?classic ?reservedNodeId
              ?targetReservedNodeOfferingId ~clusterIdentifier ())
           (Some Values.ResizeClusterResult.to_json)
           (Some Values.ResizeClusterResult.error_to_json)])
let restore_from_cluster_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotClusterIdentifier =
         flag "snapshot-cluster-identifier" (optional string)
           ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and allowVersionUpgrade =
         flag "allow-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and clusterSubnetGroupName =
         flag "cluster-subnet-group-name" (optional string)
           ~doc:"STRING String"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and ownerAccount =
         flag "owner-account" (optional string) ~doc:"STRING String"
       and hsmClientCertificateIdentifier =
         flag "hsm-client-certificate-identifier" (optional string)
           ~doc:"STRING String"
       and hsmConfigurationIdentifier =
         flag "hsm-configuration-identifier" (optional string)
           ~doc:"STRING String"
       and elasticIp =
         flag "elastic-ip" (optional string) ~doc:"STRING String"
       and clusterParameterGroupName =
         flag "cluster-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and clusterSecurityGroups =
         flag "cluster-security-groups" (optional json_arg)
           ~doc:"JSON ClusterSecurityGroupNameList"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and automatedSnapshotRetentionPeriod =
         flag "automated-snapshot-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and manualSnapshotRetentionPeriod =
         flag "manual-snapshot-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and nodeType = flag "node-type" (optional string) ~doc:"STRING String"
       and enhancedVpcRouting =
         flag "enhanced-vpc-routing" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and additionalInfo =
         flag "additional-info" (optional string) ~doc:"STRING String"
       and iamRoles =
         flag "iam-roles" (optional json_arg) ~doc:"JSON IamRoleArnList"
       and maintenanceTrackName =
         flag "maintenance-track-name" (optional string) ~doc:"STRING String"
       and snapshotScheduleIdentifier =
         flag "snapshot-schedule-identifier" (optional string)
           ~doc:"STRING String"
       and numberOfNodes =
         flag "number-of-nodes" (optional int) ~doc:"INT IntegerOptional"
       and availabilityZoneRelocation =
         flag "availability-zone-relocation" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and aquaConfigurationStatus =
         flag "aqua-configuration-status" (optional json_arg)
           ~doc:"JSON AquaConfigurationStatus"
       and defaultIamRoleArn =
         flag "default-iam-role-arn" (optional string) ~doc:"STRING String"
       and reservedNodeId =
         flag "reserved-node-id" (optional string) ~doc:"STRING String"
       and targetReservedNodeOfferingId =
         flag "target-reserved-node-offering-id" (optional string)
           ~doc:"STRING String"
       and encrypted =
         flag "encrypted" (optional bool) ~doc:"BOOL BooleanOptional"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String"
       and snapshotIdentifier =
         flag "snapshot-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_from_cluster_snapshot
           (Values.RestoreFromClusterSnapshotMessage.make
              ?snapshotClusterIdentifier ?port ?availabilityZone
              ?allowVersionUpgrade ?clusterSubnetGroupName
              ?publiclyAccessible ?ownerAccount
              ?hsmClientCertificateIdentifier ?hsmConfigurationIdentifier
              ?elasticIp ?clusterParameterGroupName
              ?clusterSecurityGroups:(Option.map
                                        ~f:Values.ClusterSecurityGroupNameList.of_json
                                        clusterSecurityGroups)
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds)
              ?preferredMaintenanceWindow ?automatedSnapshotRetentionPeriod
              ?manualSnapshotRetentionPeriod ?kmsKeyId ?nodeType
              ?enhancedVpcRouting ?additionalInfo
              ?iamRoles:(Option.map ~f:Values.IamRoleArnList.of_json iamRoles)
              ?maintenanceTrackName ?snapshotScheduleIdentifier
              ?numberOfNodes ?availabilityZoneRelocation
              ?aquaConfigurationStatus:(Option.map
                                          ~f:Values.AquaConfigurationStatus.of_json
                                          aquaConfigurationStatus)
              ?defaultIamRoleArn ?reservedNodeId
              ?targetReservedNodeOfferingId ?encrypted ~clusterIdentifier
              ~snapshotIdentifier ())
           (Some Values.RestoreFromClusterSnapshotResult.to_json)
           (Some Values.RestoreFromClusterSnapshotResult.error_to_json)])
let restore_table_from_cluster_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceSchemaName =
         flag "source-schema-name" (optional string) ~doc:"STRING String"
       and targetDatabaseName =
         flag "target-database-name" (optional string) ~doc:"STRING String"
       and targetSchemaName =
         flag "target-schema-name" (optional string) ~doc:"STRING String"
       and enableCaseSensitiveIdentifier =
         flag "enable-case-sensitive-identifier" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String"
       and snapshotIdentifier =
         flag "snapshot-identifier" (required string) ~doc:"STRING String"
       and sourceDatabaseName =
         flag "source-database-name" (required string) ~doc:"STRING String"
       and sourceTableName =
         flag "source-table-name" (required string) ~doc:"STRING String"
       and newTableName =
         flag "new-table-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_table_from_cluster_snapshot
           (Values.RestoreTableFromClusterSnapshotMessage.make
              ?sourceSchemaName ?targetDatabaseName ?targetSchemaName
              ?enableCaseSensitiveIdentifier ~clusterIdentifier
              ~snapshotIdentifier ~sourceDatabaseName ~sourceTableName
              ~newTableName ())
           (Some Values.RestoreTableFromClusterSnapshotResult.to_json)
           (Some Values.RestoreTableFromClusterSnapshotResult.error_to_json)])
let resume_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resume_cluster
           (Values.ResumeClusterMessage.make ~clusterIdentifier ())
           (Some Values.ResumeClusterResult.to_json)
           (Some Values.ResumeClusterResult.error_to_json)])
let revoke_cluster_security_group_ingress =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cIDRIP = flag "c-i-d-r-i-p" (optional string) ~doc:"STRING String"
       and eC2SecurityGroupName =
         flag "e-c2-security-group-name" (optional string)
           ~doc:"STRING String"
       and eC2SecurityGroupOwnerId =
         flag "e-c2-security-group-owner-id" (optional string)
           ~doc:"STRING String"
       and clusterSecurityGroupName =
         flag "cluster-security-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_cluster_security_group_ingress
           (Values.RevokeClusterSecurityGroupIngressMessage.make ?cIDRIP
              ?eC2SecurityGroupName ?eC2SecurityGroupOwnerId
              ~clusterSecurityGroupName ())
           (Some Values.RevokeClusterSecurityGroupIngressResult.to_json)
           (Some Values.RevokeClusterSecurityGroupIngressResult.error_to_json)])
let revoke_endpoint_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (optional string) ~doc:"STRING String"
       and account = flag "account" (optional string) ~doc:"STRING String"
       and vpcIds =
         flag "vpc-ids" (optional json_arg) ~doc:"JSON VpcIdentifierList"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_endpoint_access
           (Values.RevokeEndpointAccessMessage.make ?clusterIdentifier
              ?account
              ?vpcIds:(Option.map ~f:Values.VpcIdentifierList.of_json vpcIds)
              ?force ()) (Some Values.EndpointAuthorization.to_json)
           (Some Values.EndpointAuthorization.error_to_json)])
let revoke_snapshot_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotClusterIdentifier =
         flag "snapshot-cluster-identifier" (optional string)
           ~doc:"STRING String"
       and snapshotIdentifier =
         flag "snapshot-identifier" (required string) ~doc:"STRING String"
       and accountWithRestoreAccess =
         flag "account-with-restore-access" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_snapshot_access
           (Values.RevokeSnapshotAccessMessage.make
              ?snapshotClusterIdentifier ~snapshotIdentifier
              ~accountWithRestoreAccess ())
           (Some Values.RevokeSnapshotAccessResult.to_json)
           (Some Values.RevokeSnapshotAccessResult.error_to_json)])
let rotate_encryption_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.rotate_encryption_key
           (Values.RotateEncryptionKeyMessage.make ~clusterIdentifier ())
           (Some Values.RotateEncryptionKeyResult.to_json)
           (Some Values.RotateEncryptionKeyResult.error_to_json)])
let update_partner_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statusMessage =
         flag "status-message" (optional string)
           ~doc:"STRING PartnerIntegrationStatusMessage"
       and accountId =
         flag "account-id" (required string)
           ~doc:"STRING PartnerIntegrationAccountId"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING PartnerIntegrationClusterIdentifier"
       and databaseName =
         flag "database-name" (required string)
           ~doc:"STRING PartnerIntegrationDatabaseName"
       and partnerName =
         flag "partner-name" (required string)
           ~doc:"STRING PartnerIntegrationPartnerName"
       and status =
         flag "status" (required json_arg)
           ~doc:"JSON PartnerIntegrationStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_partner_status
           (Values.UpdatePartnerStatusInputMessage.make ?statusMessage
              ~accountId ~clusterIdentifier ~databaseName ~partnerName
              ~status:(Values.PartnerIntegrationStatus.of_json status) ())
           (Some Values.PartnerIntegrationOutputMessage.to_json)
           (Some Values.PartnerIntegrationOutputMessage.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("accept-reserved-node-exchange", accept_reserved_node_exchange);
    ("add-partner", add_partner);
    ("associate-data-share-consumer", associate_data_share_consumer);
    ("authorize-cluster-security-group-ingress",
      authorize_cluster_security_group_ingress);
    ("authorize-data-share", authorize_data_share);
    ("authorize-endpoint-access", authorize_endpoint_access);
    ("authorize-snapshot-access", authorize_snapshot_access);
    ("batch-delete-cluster-snapshots", batch_delete_cluster_snapshots);
    ("batch-modify-cluster-snapshots", batch_modify_cluster_snapshots);
    ("cancel-resize", cancel_resize);
    ("copy-cluster-snapshot", copy_cluster_snapshot);
    ("create-authentication-profile", create_authentication_profile);
    ("create-cluster", create_cluster);
    ("create-cluster-parameter-group", create_cluster_parameter_group);
    ("create-cluster-security-group", create_cluster_security_group);
    ("create-cluster-snapshot", create_cluster_snapshot);
    ("create-cluster-subnet-group", create_cluster_subnet_group);
    ("create-endpoint-access", create_endpoint_access);
    ("create-event-subscription", create_event_subscription);
    ("create-hsm-client-certificate", create_hsm_client_certificate);
    ("create-hsm-configuration", create_hsm_configuration);
    ("create-scheduled-action", create_scheduled_action);
    ("create-snapshot-copy-grant", create_snapshot_copy_grant);
    ("create-snapshot-schedule", create_snapshot_schedule);
    ("create-tags", create_tags);
    ("create-usage-limit", create_usage_limit);
    ("deauthorize-data-share", deauthorize_data_share);
    ("delete-authentication-profile", delete_authentication_profile);
    ("delete-cluster", delete_cluster);
    ("delete-cluster-parameter-group", delete_cluster_parameter_group);
    ("delete-cluster-security-group", delete_cluster_security_group);
    ("delete-cluster-snapshot", delete_cluster_snapshot);
    ("delete-cluster-subnet-group", delete_cluster_subnet_group);
    ("delete-endpoint-access", delete_endpoint_access);
    ("delete-event-subscription", delete_event_subscription);
    ("delete-hsm-client-certificate", delete_hsm_client_certificate);
    ("delete-hsm-configuration", delete_hsm_configuration);
    ("delete-partner", delete_partner);
    ("delete-scheduled-action", delete_scheduled_action);
    ("delete-snapshot-copy-grant", delete_snapshot_copy_grant);
    ("delete-snapshot-schedule", delete_snapshot_schedule);
    ("delete-tags", delete_tags);
    ("delete-usage-limit", delete_usage_limit);
    ("describe-account-attributes", describe_account_attributes);
    ("describe-authentication-profiles", describe_authentication_profiles);
    ("describe-cluster-db-revisions", describe_cluster_db_revisions);
    ("describe-cluster-parameter-groups", describe_cluster_parameter_groups);
    ("describe-cluster-parameters", describe_cluster_parameters);
    ("describe-cluster-security-groups", describe_cluster_security_groups);
    ("describe-cluster-snapshots", describe_cluster_snapshots);
    ("describe-cluster-subnet-groups", describe_cluster_subnet_groups);
    ("describe-cluster-tracks", describe_cluster_tracks);
    ("describe-cluster-versions", describe_cluster_versions);
    ("describe-clusters", describe_clusters);
    ("describe-data-shares", describe_data_shares);
    ("describe-data-shares-for-consumer", describe_data_shares_for_consumer);
    ("describe-data-shares-for-producer", describe_data_shares_for_producer);
    ("describe-default-cluster-parameters",
      describe_default_cluster_parameters);
    ("describe-endpoint-access", describe_endpoint_access);
    ("describe-endpoint-authorization", describe_endpoint_authorization);
    ("describe-event-categories", describe_event_categories);
    ("describe-event-subscriptions", describe_event_subscriptions);
    ("describe-events", describe_events);
    ("describe-hsm-client-certificates", describe_hsm_client_certificates);
    ("describe-hsm-configurations", describe_hsm_configurations);
    ("describe-logging-status", describe_logging_status);
    ("describe-node-configuration-options",
      describe_node_configuration_options);
    ("describe-orderable-cluster-options",
      describe_orderable_cluster_options);
    ("describe-partners", describe_partners);
    ("describe-reserved-node-exchange-status",
      describe_reserved_node_exchange_status);
    ("describe-reserved-node-offerings", describe_reserved_node_offerings);
    ("describe-reserved-nodes", describe_reserved_nodes);
    ("describe-resize", describe_resize);
    ("describe-scheduled-actions", describe_scheduled_actions);
    ("describe-snapshot-copy-grants", describe_snapshot_copy_grants);
    ("describe-snapshot-schedules", describe_snapshot_schedules);
    ("describe-storage", describe_storage);
    ("describe-table-restore-status", describe_table_restore_status);
    ("describe-tags", describe_tags);
    ("describe-usage-limits", describe_usage_limits);
    ("disable-logging", disable_logging);
    ("disable-snapshot-copy", disable_snapshot_copy);
    ("disassociate-data-share-consumer", disassociate_data_share_consumer);
    ("enable-logging", enable_logging);
    ("enable-snapshot-copy", enable_snapshot_copy);
    ("get-cluster-credentials", get_cluster_credentials);
    ("get-reserved-node-exchange-configuration-options",
      get_reserved_node_exchange_configuration_options);
    ("get-reserved-node-exchange-offerings",
      get_reserved_node_exchange_offerings);
    ("modify-aqua-configuration", modify_aqua_configuration);
    ("modify-authentication-profile", modify_authentication_profile);
    ("modify-cluster", modify_cluster);
    ("modify-cluster-db-revision", modify_cluster_db_revision);
    ("modify-cluster-iam-roles", modify_cluster_iam_roles);
    ("modify-cluster-maintenance", modify_cluster_maintenance);
    ("modify-cluster-parameter-group", modify_cluster_parameter_group);
    ("modify-cluster-snapshot", modify_cluster_snapshot);
    ("modify-cluster-snapshot-schedule", modify_cluster_snapshot_schedule);
    ("modify-cluster-subnet-group", modify_cluster_subnet_group);
    ("modify-endpoint-access", modify_endpoint_access);
    ("modify-event-subscription", modify_event_subscription);
    ("modify-scheduled-action", modify_scheduled_action);
    ("modify-snapshot-copy-retention-period",
      modify_snapshot_copy_retention_period);
    ("modify-snapshot-schedule", modify_snapshot_schedule);
    ("modify-usage-limit", modify_usage_limit);
    ("pause-cluster", pause_cluster);
    ("purchase-reserved-node-offering", purchase_reserved_node_offering);
    ("reboot-cluster", reboot_cluster);
    ("reject-data-share", reject_data_share);
    ("reset-cluster-parameter-group", reset_cluster_parameter_group);
    ("resize-cluster", resize_cluster);
    ("restore-from-cluster-snapshot", restore_from_cluster_snapshot);
    ("restore-table-from-cluster-snapshot",
      restore_table_from_cluster_snapshot);
    ("resume-cluster", resume_cluster);
    ("revoke-cluster-security-group-ingress",
      revoke_cluster_security_group_ingress);
    ("revoke-endpoint-access", revoke_endpoint_access);
    ("revoke-snapshot-access", revoke_snapshot_access);
    ("rotate-encryption-key", rotate_encryption_key);
    ("update-partner-status", update_partner_status)]
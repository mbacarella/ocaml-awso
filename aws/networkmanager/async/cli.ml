(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_attachment
           (Values.AcceptAttachmentRequest.make ~attachmentId ())
           (Some Values.AcceptAttachmentResponse.to_json)
           (Some Values.AcceptAttachmentResponse.error_to_json)])
let associate_connect_peer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and linkId = flag "link-id" (optional string) ~doc:"STRING LinkId"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and connectPeerId =
         flag "connect-peer-id" (required string) ~doc:"STRING ConnectPeerId"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_connect_peer
           (Values.AssociateConnectPeerRequest.make ?linkId ~globalNetworkId
              ~connectPeerId ~deviceId ())
           (Some Values.AssociateConnectPeerResponse.to_json)
           (Some Values.AssociateConnectPeerResponse.error_to_json)])
let associate_customer_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and linkId = flag "link-id" (optional string) ~doc:"STRING LinkId"
       and customerGatewayArn =
         flag "customer-gateway-arn" (required string)
           ~doc:"STRING CustomerGatewayArn"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_customer_gateway
           (Values.AssociateCustomerGatewayRequest.make ?linkId
              ~customerGatewayArn ~globalNetworkId ~deviceId ())
           (Some Values.AssociateCustomerGatewayResponse.to_json)
           (Some Values.AssociateCustomerGatewayResponse.error_to_json)])
let associate_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_link
           (Values.AssociateLinkRequest.make ~globalNetworkId ~deviceId
              ~linkId ()) (Some Values.AssociateLinkResponse.to_json)
           (Some Values.AssociateLinkResponse.error_to_json)])
let associate_transit_gateway_connect_peer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and linkId = flag "link-id" (optional string) ~doc:"STRING LinkId"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and transitGatewayConnectPeerArn =
         flag "transit-gateway-connect-peer-arn" (required string)
           ~doc:"STRING TransitGatewayConnectPeerArn"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_transit_gateway_connect_peer
           (Values.AssociateTransitGatewayConnectPeerRequest.make ?linkId
              ~globalNetworkId ~transitGatewayConnectPeerArn ~deviceId ())
           (Some Values.AssociateTransitGatewayConnectPeerResponse.to_json)
           (Some
              Values.AssociateTransitGatewayConnectPeerResponse.error_to_json)])
let create_connect_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and routingPolicyLabel =
         flag "routing-policy-label" (optional string)
           ~doc:"STRING ConstrainedString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and edgeLocation =
         flag "edge-location" (required string)
           ~doc:"STRING ExternalRegionCode"
       and transportAttachmentId =
         flag "transport-attachment-id" (required string)
           ~doc:"STRING AttachmentId"
       and options =
         flag "options" (required json_arg)
           ~doc:"JSON ConnectAttachmentOptions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connect_attachment
           (Values.CreateConnectAttachmentRequest.make ?routingPolicyLabel
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~coreNetworkId ~edgeLocation ~transportAttachmentId
              ~options:(Values.ConnectAttachmentOptions.of_json options) ())
           (Some Values.CreateConnectAttachmentResponse.to_json)
           (Some Values.CreateConnectAttachmentResponse.error_to_json)])
let create_connect_peer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkAddress =
         flag "core-network-address" (optional string)
           ~doc:"STRING IPAddress"
       and bgpOptions =
         flag "bgp-options" (optional json_arg) ~doc:"JSON BgpOptions"
       and insideCidrBlocks =
         flag "inside-cidr-blocks" (optional json_arg)
           ~doc:"JSON ConstrainedStringList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and subnetArn =
         flag "subnet-arn" (optional string) ~doc:"STRING SubnetArn"
       and connectAttachmentId =
         flag "connect-attachment-id" (required string)
           ~doc:"STRING AttachmentId"
       and peerAddress =
         flag "peer-address" (required string) ~doc:"STRING IPAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connect_peer
           (Values.CreateConnectPeerRequest.make ?coreNetworkAddress
              ?bgpOptions:(Option.map ~f:Values.BgpOptions.of_json bgpOptions)
              ?insideCidrBlocks:(Option.map
                                   ~f:Values.ConstrainedStringList.of_json
                                   insideCidrBlocks)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ?subnetArn ~connectAttachmentId ~peerAddress ())
           (Some Values.CreateConnectPeerResponse.to_json)
           (Some Values.CreateConnectPeerResponse.error_to_json)])
let create_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and linkId = flag "link-id" (optional string) ~doc:"STRING LinkId"
       and connectedLinkId =
         flag "connected-link-id" (optional string) ~doc:"STRING LinkId"
       and description =
         flag "description" (optional string) ~doc:"STRING ConstrainedString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId"
       and connectedDeviceId =
         flag "connected-device-id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connection
           (Values.CreateConnectionRequest.make ?linkId ?connectedLinkId
              ?description ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~globalNetworkId ~deviceId ~connectedDeviceId ())
           (Some Values.CreateConnectionResponse.to_json)
           (Some Values.CreateConnectionResponse.error_to_json)])
let create_core_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING ConstrainedString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and policyDocument =
         flag "policy-document" (optional string)
           ~doc:"STRING CoreNetworkPolicyDocument"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_core_network
           (Values.CreateCoreNetworkRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?policyDocument ?clientToken ~globalNetworkId ())
           (Some Values.CreateCoreNetworkResponse.to_json)
           (Some Values.CreateCoreNetworkResponse.error_to_json)])
let create_core_network_prefix_list_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and prefixListArn =
         flag "prefix-list-arn" (required string) ~doc:"STRING PrefixListArn"
       and prefixListAlias =
         flag "prefix-list-alias" (required string)
           ~doc:"STRING ConstrainedString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_core_network_prefix_list_association
           (Values.CreateCoreNetworkPrefixListAssociationRequest.make
              ?clientToken ~coreNetworkId ~prefixListArn ~prefixListAlias ())
           (Some
              Values.CreateCoreNetworkPrefixListAssociationResponse.to_json)
           (Some
              Values.CreateCoreNetworkPrefixListAssociationResponse.error_to_json)])
let create_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aWSLocation =
         flag "a-w-s-location" (optional json_arg) ~doc:"JSON AWSLocation"
       and description =
         flag "description" (optional string) ~doc:"STRING ConstrainedString"
       and type_ =
         flag "type-" (optional string) ~doc:"STRING ConstrainedString"
       and vendor =
         flag "vendor" (optional string) ~doc:"STRING ConstrainedString"
       and model =
         flag "model" (optional string) ~doc:"STRING ConstrainedString"
       and serialNumber =
         flag "serial-number" (optional string)
           ~doc:"STRING ConstrainedString"
       and location =
         flag "location" (optional json_arg) ~doc:"JSON Location"
       and siteId = flag "site-id" (optional string) ~doc:"STRING SiteId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_device
           (Values.CreateDeviceRequest.make
              ?aWSLocation:(Option.map ~f:Values.AWSLocation.of_json
                              aWSLocation) ?description ?type_ ?vendor ?model
              ?serialNumber
              ?location:(Option.map ~f:Values.Location.of_json location)
              ?siteId ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~globalNetworkId ()) (Some Values.CreateDeviceResponse.to_json)
           (Some Values.CreateDeviceResponse.error_to_json)])
let create_direct_connect_gateway_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and routingPolicyLabel =
         flag "routing-policy-label" (optional string)
           ~doc:"STRING ConstrainedString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and directConnectGatewayArn =
         flag "direct-connect-gateway-arn" (required string)
           ~doc:"STRING DirectConnectGatewayArn"
       and edgeLocations =
         flag "edge-locations" (required json_arg)
           ~doc:"JSON ExternalRegionCodeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_direct_connect_gateway_attachment
           (Values.CreateDirectConnectGatewayAttachmentRequest.make
              ?routingPolicyLabel
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~coreNetworkId ~directConnectGatewayArn
              ~edgeLocations:(Values.ExternalRegionCodeList.of_json
                                edgeLocations) ())
           (Some Values.CreateDirectConnectGatewayAttachmentResponse.to_json)
           (Some
              Values.CreateDirectConnectGatewayAttachmentResponse.error_to_json)])
let create_global_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING ConstrainedString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_global_network
           (Values.CreateGlobalNetworkRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ())
           (Some Values.CreateGlobalNetworkResponse.to_json)
           (Some Values.CreateGlobalNetworkResponse.error_to_json)])
let create_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING ConstrainedString"
       and type_ =
         flag "type-" (optional string) ~doc:"STRING ConstrainedString"
       and provider =
         flag "provider" (optional string) ~doc:"STRING ConstrainedString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and bandwidth =
         flag "bandwidth" (required json_arg) ~doc:"JSON Bandwidth"
       and siteId = flag "site-id" (required string) ~doc:"STRING SiteId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_link
           (Values.CreateLinkRequest.make ?description ?type_ ?provider
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~globalNetworkId
              ~bandwidth:(Values.Bandwidth.of_json bandwidth) ~siteId ())
           (Some Values.CreateLinkResponse.to_json)
           (Some Values.CreateLinkResponse.error_to_json)])
let create_site =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING ConstrainedString"
       and location =
         flag "location" (optional json_arg) ~doc:"JSON Location"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_site
           (Values.CreateSiteRequest.make ?description
              ?location:(Option.map ~f:Values.Location.of_json location)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~globalNetworkId ()) (Some Values.CreateSiteResponse.to_json)
           (Some Values.CreateSiteResponse.error_to_json)])
let create_site_to_site_vpn_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and routingPolicyLabel =
         flag "routing-policy-label" (optional string)
           ~doc:"STRING ConstrainedString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and vpnConnectionArn =
         flag "vpn-connection-arn" (required string)
           ~doc:"STRING VpnConnectionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_site_to_site_vpn_attachment
           (Values.CreateSiteToSiteVpnAttachmentRequest.make
              ?routingPolicyLabel
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~coreNetworkId ~vpnConnectionArn ())
           (Some Values.CreateSiteToSiteVpnAttachmentResponse.to_json)
           (Some Values.CreateSiteToSiteVpnAttachmentResponse.error_to_json)])
let create_transit_gateway_peering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and transitGatewayArn =
         flag "transit-gateway-arn" (required string)
           ~doc:"STRING TransitGatewayArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_transit_gateway_peering
           (Values.CreateTransitGatewayPeeringRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~coreNetworkId ~transitGatewayArn ())
           (Some Values.CreateTransitGatewayPeeringResponse.to_json)
           (Some Values.CreateTransitGatewayPeeringResponse.error_to_json)])
let create_transit_gateway_route_table_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and routingPolicyLabel =
         flag "routing-policy-label" (optional string)
           ~doc:"STRING ConstrainedString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and peeringId =
         flag "peering-id" (required string) ~doc:"STRING PeeringId"
       and transitGatewayRouteTableArn =
         flag "transit-gateway-route-table-arn" (required string)
           ~doc:"STRING TransitGatewayRouteTableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_transit_gateway_route_table_attachment
           (Values.CreateTransitGatewayRouteTableAttachmentRequest.make
              ?routingPolicyLabel
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~peeringId ~transitGatewayRouteTableArn ())
           (Some
              Values.CreateTransitGatewayRouteTableAttachmentResponse.to_json)
           (Some
              Values.CreateTransitGatewayRouteTableAttachmentResponse.error_to_json)])
let create_vpc_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and options =
         flag "options" (optional json_arg) ~doc:"JSON VpcOptions"
       and routingPolicyLabel =
         flag "routing-policy-label" (optional string)
           ~doc:"STRING ConstrainedString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and vpcArn = flag "vpc-arn" (required string) ~doc:"STRING VpcArn"
       and subnetArns =
         flag "subnet-arns" (required json_arg) ~doc:"JSON SubnetArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpc_attachment
           (Values.CreateVpcAttachmentRequest.make
              ?options:(Option.map ~f:Values.VpcOptions.of_json options)
              ?routingPolicyLabel
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~coreNetworkId ~vpcArn
              ~subnetArns:(Values.SubnetArnList.of_json subnetArns) ())
           (Some Values.CreateVpcAttachmentResponse.to_json)
           (Some Values.CreateVpcAttachmentResponse.error_to_json)])
let delete_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_attachment
           (Values.DeleteAttachmentRequest.make ~attachmentId ())
           (Some Values.DeleteAttachmentResponse.to_json)
           (Some Values.DeleteAttachmentResponse.error_to_json)])
let delete_connect_peer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectPeerId =
         flag "connect-peer-id" (required string) ~doc:"STRING ConnectPeerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connect_peer
           (Values.DeleteConnectPeerRequest.make ~connectPeerId ())
           (Some Values.DeleteConnectPeerResponse.to_json)
           (Some Values.DeleteConnectPeerResponse.error_to_json)])
let delete_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and connectionId =
         flag "connection-id" (required string) ~doc:"STRING ConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connection
           (Values.DeleteConnectionRequest.make ~globalNetworkId
              ~connectionId ())
           (Some Values.DeleteConnectionResponse.to_json)
           (Some Values.DeleteConnectionResponse.error_to_json)])
let delete_core_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_core_network
           (Values.DeleteCoreNetworkRequest.make ~coreNetworkId ())
           (Some Values.DeleteCoreNetworkResponse.to_json)
           (Some Values.DeleteCoreNetworkResponse.error_to_json)])
let delete_core_network_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and policyVersionId =
         flag "policy-version-id" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_core_network_policy_version
           (Values.DeleteCoreNetworkPolicyVersionRequest.make ~coreNetworkId
              ~policyVersionId ())
           (Some Values.DeleteCoreNetworkPolicyVersionResponse.to_json)
           (Some Values.DeleteCoreNetworkPolicyVersionResponse.error_to_json)])
let delete_core_network_prefix_list_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and prefixListArn =
         flag "prefix-list-arn" (required string) ~doc:"STRING PrefixListArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_core_network_prefix_list_association
           (Values.DeleteCoreNetworkPrefixListAssociationRequest.make
              ~coreNetworkId ~prefixListArn ())
           (Some
              Values.DeleteCoreNetworkPrefixListAssociationResponse.to_json)
           (Some
              Values.DeleteCoreNetworkPrefixListAssociationResponse.error_to_json)])
let delete_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_device
           (Values.DeleteDeviceRequest.make ~globalNetworkId ~deviceId ())
           (Some Values.DeleteDeviceResponse.to_json)
           (Some Values.DeleteDeviceResponse.error_to_json)])
let delete_global_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_global_network
           (Values.DeleteGlobalNetworkRequest.make ~globalNetworkId ())
           (Some Values.DeleteGlobalNetworkResponse.to_json)
           (Some Values.DeleteGlobalNetworkResponse.error_to_json)])
let delete_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_link
           (Values.DeleteLinkRequest.make ~globalNetworkId ~linkId ())
           (Some Values.DeleteLinkResponse.to_json)
           (Some Values.DeleteLinkResponse.error_to_json)])
let delete_peering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and peeringId =
         flag "peering-id" (required string) ~doc:"STRING PeeringId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_peering (Values.DeletePeeringRequest.make ~peeringId ())
           (Some Values.DeletePeeringResponse.to_json)
           (Some Values.DeletePeeringResponse.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyRequest.make ~resourceArn ())
           (Some Values.DeleteResourcePolicyResponse.to_json)
           (Some Values.DeleteResourcePolicyResponse.error_to_json)])
let delete_site =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and siteId = flag "site-id" (required string) ~doc:"STRING SiteId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_site
           (Values.DeleteSiteRequest.make ~globalNetworkId ~siteId ())
           (Some Values.DeleteSiteResponse.to_json)
           (Some Values.DeleteSiteResponse.error_to_json)])
let deregister_transit_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and transitGatewayArn =
         flag "transit-gateway-arn" (required string)
           ~doc:"STRING TransitGatewayArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_transit_gateway
           (Values.DeregisterTransitGatewayRequest.make ~globalNetworkId
              ~transitGatewayArn ())
           (Some Values.DeregisterTransitGatewayResponse.to_json)
           (Some Values.DeregisterTransitGatewayResponse.error_to_json)])
let describe_global_networks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkIds =
         flag "global-network-ids" (optional json_arg)
           ~doc:"JSON GlobalNetworkIdList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_global_networks
           (Values.DescribeGlobalNetworksRequest.make
              ?globalNetworkIds:(Option.map
                                   ~f:Values.GlobalNetworkIdList.of_json
                                   globalNetworkIds) ?maxResults ?nextToken
              ()) (Some Values.DescribeGlobalNetworksResponse.to_json)
           (Some Values.DescribeGlobalNetworksResponse.error_to_json)])
let disassociate_connect_peer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and connectPeerId =
         flag "connect-peer-id" (required string) ~doc:"STRING ConnectPeerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_connect_peer
           (Values.DisassociateConnectPeerRequest.make ~globalNetworkId
              ~connectPeerId ())
           (Some Values.DisassociateConnectPeerResponse.to_json)
           (Some Values.DisassociateConnectPeerResponse.error_to_json)])
let disassociate_customer_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and customerGatewayArn =
         flag "customer-gateway-arn" (required string)
           ~doc:"STRING CustomerGatewayArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_customer_gateway
           (Values.DisassociateCustomerGatewayRequest.make ~globalNetworkId
              ~customerGatewayArn ())
           (Some Values.DisassociateCustomerGatewayResponse.to_json)
           (Some Values.DisassociateCustomerGatewayResponse.error_to_json)])
let disassociate_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_link
           (Values.DisassociateLinkRequest.make ~globalNetworkId ~deviceId
              ~linkId ()) (Some Values.DisassociateLinkResponse.to_json)
           (Some Values.DisassociateLinkResponse.error_to_json)])
let disassociate_transit_gateway_connect_peer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and transitGatewayConnectPeerArn =
         flag "transit-gateway-connect-peer-arn" (required string)
           ~doc:"STRING TransitGatewayConnectPeerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_transit_gateway_connect_peer
           (Values.DisassociateTransitGatewayConnectPeerRequest.make
              ~globalNetworkId ~transitGatewayConnectPeerArn ())
           (Some Values.DisassociateTransitGatewayConnectPeerResponse.to_json)
           (Some
              Values.DisassociateTransitGatewayConnectPeerResponse.error_to_json)])
let execute_core_network_change_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and policyVersionId =
         flag "policy-version-id" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_core_network_change_set
           (Values.ExecuteCoreNetworkChangeSetRequest.make ~coreNetworkId
              ~policyVersionId ())
           (Some Values.ExecuteCoreNetworkChangeSetResponse.to_json)
           (Some Values.ExecuteCoreNetworkChangeSetResponse.error_to_json)])
let get_connect_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connect_attachment
           (Values.GetConnectAttachmentRequest.make ~attachmentId ())
           (Some Values.GetConnectAttachmentResponse.to_json)
           (Some Values.GetConnectAttachmentResponse.error_to_json)])
let get_connect_peer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectPeerId =
         flag "connect-peer-id" (required string) ~doc:"STRING ConnectPeerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connect_peer
           (Values.GetConnectPeerRequest.make ~connectPeerId ())
           (Some Values.GetConnectPeerResponse.to_json)
           (Some Values.GetConnectPeerResponse.error_to_json)])
let get_connect_peer_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectPeerIds =
         flag "connect-peer-ids" (optional json_arg)
           ~doc:"JSON ConnectPeerIdList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connect_peer_associations
           (Values.GetConnectPeerAssociationsRequest.make
              ?connectPeerIds:(Option.map ~f:Values.ConnectPeerIdList.of_json
                                 connectPeerIds) ?maxResults ?nextToken
              ~globalNetworkId ())
           (Some Values.GetConnectPeerAssociationsResponse.to_json)
           (Some Values.GetConnectPeerAssociationsResponse.error_to_json)])
let get_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionIds =
         flag "connection-ids" (optional json_arg)
           ~doc:"JSON ConnectionIdList"
       and deviceId =
         flag "device-id" (optional string) ~doc:"STRING DeviceId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connections
           (Values.GetConnectionsRequest.make
              ?connectionIds:(Option.map ~f:Values.ConnectionIdList.of_json
                                connectionIds) ?deviceId ?maxResults
              ?nextToken ~globalNetworkId ())
           (Some Values.GetConnectionsResponse.to_json)
           (Some Values.GetConnectionsResponse.error_to_json)])
let get_core_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_core_network
           (Values.GetCoreNetworkRequest.make ~coreNetworkId ())
           (Some Values.GetCoreNetworkResponse.to_json)
           (Some Values.GetCoreNetworkResponse.error_to_json)])
let get_core_network_change_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and policyVersionId =
         flag "policy-version-id" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_core_network_change_events
           (Values.GetCoreNetworkChangeEventsRequest.make ?maxResults
              ?nextToken ~coreNetworkId ~policyVersionId ())
           (Some Values.GetCoreNetworkChangeEventsResponse.to_json)
           (Some Values.GetCoreNetworkChangeEventsResponse.error_to_json)])
let get_core_network_change_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and policyVersionId =
         flag "policy-version-id" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_core_network_change_set
           (Values.GetCoreNetworkChangeSetRequest.make ?maxResults ?nextToken
              ~coreNetworkId ~policyVersionId ())
           (Some Values.GetCoreNetworkChangeSetResponse.to_json)
           (Some Values.GetCoreNetworkChangeSetResponse.error_to_json)])
let get_core_network_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyVersionId =
         flag "policy-version-id" (optional int) ~doc:"INT Integer"
       and alias =
         flag "alias" (optional json_arg) ~doc:"JSON CoreNetworkPolicyAlias"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_core_network_policy
           (Values.GetCoreNetworkPolicyRequest.make ?policyVersionId
              ?alias:(Option.map ~f:Values.CoreNetworkPolicyAlias.of_json
                        alias) ~coreNetworkId ())
           (Some Values.GetCoreNetworkPolicyResponse.to_json)
           (Some Values.GetCoreNetworkPolicyResponse.error_to_json)])
let get_customer_gateway_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customerGatewayArns =
         flag "customer-gateway-arns" (optional json_arg)
           ~doc:"JSON CustomerGatewayArnList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_customer_gateway_associations
           (Values.GetCustomerGatewayAssociationsRequest.make
              ?customerGatewayArns:(Option.map
                                      ~f:Values.CustomerGatewayArnList.of_json
                                      customerGatewayArns) ?maxResults
              ?nextToken ~globalNetworkId ())
           (Some Values.GetCustomerGatewayAssociationsResponse.to_json)
           (Some Values.GetCustomerGatewayAssociationsResponse.error_to_json)])
let get_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceIds =
         flag "device-ids" (optional json_arg) ~doc:"JSON DeviceIdList"
       and siteId = flag "site-id" (optional string) ~doc:"STRING SiteId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_devices
           (Values.GetDevicesRequest.make
              ?deviceIds:(Option.map ~f:Values.DeviceIdList.of_json deviceIds)
              ?siteId ?maxResults ?nextToken ~globalNetworkId ())
           (Some Values.GetDevicesResponse.to_json)
           (Some Values.GetDevicesResponse.error_to_json)])
let get_direct_connect_gateway_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_direct_connect_gateway_attachment
           (Values.GetDirectConnectGatewayAttachmentRequest.make
              ~attachmentId ())
           (Some Values.GetDirectConnectGatewayAttachmentResponse.to_json)
           (Some
              Values.GetDirectConnectGatewayAttachmentResponse.error_to_json)])
let get_link_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceId =
         flag "device-id" (optional string) ~doc:"STRING DeviceId"
       and linkId = flag "link-id" (optional string) ~doc:"STRING LinkId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_link_associations
           (Values.GetLinkAssociationsRequest.make ?deviceId ?linkId
              ?maxResults ?nextToken ~globalNetworkId ())
           (Some Values.GetLinkAssociationsResponse.to_json)
           (Some Values.GetLinkAssociationsResponse.error_to_json)])
let get_links =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and linkIds =
         flag "link-ids" (optional json_arg) ~doc:"JSON LinkIdList"
       and siteId = flag "site-id" (optional string) ~doc:"STRING SiteId"
       and type_ =
         flag "type-" (optional string) ~doc:"STRING ConstrainedString"
       and provider =
         flag "provider" (optional string) ~doc:"STRING ConstrainedString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_links
           (Values.GetLinksRequest.make
              ?linkIds:(Option.map ~f:Values.LinkIdList.of_json linkIds)
              ?siteId ?type_ ?provider ?maxResults ?nextToken
              ~globalNetworkId ()) (Some Values.GetLinksResponse.to_json)
           (Some Values.GetLinksResponse.error_to_json)])
let get_network_resource_counts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (optional string)
           ~doc:"STRING ConstrainedString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_network_resource_counts
           (Values.GetNetworkResourceCountsRequest.make ?resourceType
              ?maxResults ?nextToken ~globalNetworkId ())
           (Some Values.GetNetworkResourceCountsResponse.to_json)
           (Some Values.GetNetworkResourceCountsResponse.error_to_json)])
let get_network_resource_relationships =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkId =
         flag "core-network-id" (optional string) ~doc:"STRING CoreNetworkId"
       and registeredGatewayArn =
         flag "registered-gateway-arn" (optional string)
           ~doc:"STRING ResourceArn"
       and awsRegion =
         flag "aws-region" (optional string) ~doc:"STRING ExternalRegionCode"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AWSAccountId"
       and resourceType =
         flag "resource-type" (optional string)
           ~doc:"STRING ConstrainedString"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING ResourceArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_network_resource_relationships
           (Values.GetNetworkResourceRelationshipsRequest.make ?coreNetworkId
              ?registeredGatewayArn ?awsRegion ?accountId ?resourceType
              ?resourceArn ?maxResults ?nextToken ~globalNetworkId ())
           (Some Values.GetNetworkResourceRelationshipsResponse.to_json)
           (Some Values.GetNetworkResourceRelationshipsResponse.error_to_json)])
let get_network_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkId =
         flag "core-network-id" (optional string) ~doc:"STRING CoreNetworkId"
       and registeredGatewayArn =
         flag "registered-gateway-arn" (optional string)
           ~doc:"STRING ResourceArn"
       and awsRegion =
         flag "aws-region" (optional string) ~doc:"STRING ExternalRegionCode"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AWSAccountId"
       and resourceType =
         flag "resource-type" (optional string)
           ~doc:"STRING ConstrainedString"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING ResourceArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_network_resources
           (Values.GetNetworkResourcesRequest.make ?coreNetworkId
              ?registeredGatewayArn ?awsRegion ?accountId ?resourceType
              ?resourceArn ?maxResults ?nextToken ~globalNetworkId ())
           (Some Values.GetNetworkResourcesResponse.to_json)
           (Some Values.GetNetworkResourcesResponse.error_to_json)])
let get_network_routes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exactCidrMatches =
         flag "exact-cidr-matches" (optional json_arg)
           ~doc:"JSON ConstrainedStringList"
       and longestPrefixMatches =
         flag "longest-prefix-matches" (optional json_arg)
           ~doc:"JSON ConstrainedStringList"
       and subnetOfMatches =
         flag "subnet-of-matches" (optional json_arg)
           ~doc:"JSON ConstrainedStringList"
       and supernetOfMatches =
         flag "supernet-of-matches" (optional json_arg)
           ~doc:"JSON ConstrainedStringList"
       and prefixListIds =
         flag "prefix-list-ids" (optional json_arg)
           ~doc:"JSON ConstrainedStringList"
       and states =
         flag "states" (optional json_arg) ~doc:"JSON RouteStateList"
       and types = flag "types" (optional json_arg) ~doc:"JSON RouteTypeList"
       and destinationFilters =
         flag "destination-filters" (optional json_arg) ~doc:"JSON FilterMap"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and routeTableIdentifier =
         flag "route-table-identifier" (required json_arg)
           ~doc:"JSON RouteTableIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_network_routes
           (Values.GetNetworkRoutesRequest.make
              ?exactCidrMatches:(Option.map
                                   ~f:Values.ConstrainedStringList.of_json
                                   exactCidrMatches)
              ?longestPrefixMatches:(Option.map
                                       ~f:Values.ConstrainedStringList.of_json
                                       longestPrefixMatches)
              ?subnetOfMatches:(Option.map
                                  ~f:Values.ConstrainedStringList.of_json
                                  subnetOfMatches)
              ?supernetOfMatches:(Option.map
                                    ~f:Values.ConstrainedStringList.of_json
                                    supernetOfMatches)
              ?prefixListIds:(Option.map
                                ~f:Values.ConstrainedStringList.of_json
                                prefixListIds)
              ?states:(Option.map ~f:Values.RouteStateList.of_json states)
              ?types:(Option.map ~f:Values.RouteTypeList.of_json types)
              ?destinationFilters:(Option.map ~f:Values.FilterMap.of_json
                                     destinationFilters) ~globalNetworkId
              ~routeTableIdentifier:(Values.RouteTableIdentifier.of_json
                                       routeTableIdentifier) ())
           (Some Values.GetNetworkRoutesResponse.to_json)
           (Some Values.GetNetworkRoutesResponse.error_to_json)])
let get_network_telemetry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkId =
         flag "core-network-id" (optional string) ~doc:"STRING CoreNetworkId"
       and registeredGatewayArn =
         flag "registered-gateway-arn" (optional string)
           ~doc:"STRING ResourceArn"
       and awsRegion =
         flag "aws-region" (optional string) ~doc:"STRING ExternalRegionCode"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AWSAccountId"
       and resourceType =
         flag "resource-type" (optional string)
           ~doc:"STRING ConstrainedString"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING ResourceArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_network_telemetry
           (Values.GetNetworkTelemetryRequest.make ?coreNetworkId
              ?registeredGatewayArn ?awsRegion ?accountId ?resourceType
              ?resourceArn ?maxResults ?nextToken ~globalNetworkId ())
           (Some Values.GetNetworkTelemetryResponse.to_json)
           (Some Values.GetNetworkTelemetryResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ~resourceArn ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let get_route_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and routeAnalysisId =
         flag "route-analysis-id" (required string)
           ~doc:"STRING ConstrainedString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_route_analysis
           (Values.GetRouteAnalysisRequest.make ~globalNetworkId
              ~routeAnalysisId ())
           (Some Values.GetRouteAnalysisResponse.to_json)
           (Some Values.GetRouteAnalysisResponse.error_to_json)])
let get_site_to_site_vpn_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_site_to_site_vpn_attachment
           (Values.GetSiteToSiteVpnAttachmentRequest.make ~attachmentId ())
           (Some Values.GetSiteToSiteVpnAttachmentResponse.to_json)
           (Some Values.GetSiteToSiteVpnAttachmentResponse.error_to_json)])
let get_sites =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and siteIds =
         flag "site-ids" (optional json_arg) ~doc:"JSON SiteIdList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sites
           (Values.GetSitesRequest.make
              ?siteIds:(Option.map ~f:Values.SiteIdList.of_json siteIds)
              ?maxResults ?nextToken ~globalNetworkId ())
           (Some Values.GetSitesResponse.to_json)
           (Some Values.GetSitesResponse.error_to_json)])
let get_transit_gateway_connect_peer_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transitGatewayConnectPeerArns =
         flag "transit-gateway-connect-peer-arns" (optional json_arg)
           ~doc:"JSON TransitGatewayConnectPeerArnList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_transit_gateway_connect_peer_associations
           (Values.GetTransitGatewayConnectPeerAssociationsRequest.make
              ?transitGatewayConnectPeerArns:(Option.map
                                                ~f:Values.TransitGatewayConnectPeerArnList.of_json
                                                transitGatewayConnectPeerArns)
              ?maxResults ?nextToken ~globalNetworkId ())
           (Some
              Values.GetTransitGatewayConnectPeerAssociationsResponse.to_json)
           (Some
              Values.GetTransitGatewayConnectPeerAssociationsResponse.error_to_json)])
let get_transit_gateway_peering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and peeringId =
         flag "peering-id" (required string) ~doc:"STRING PeeringId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_transit_gateway_peering
           (Values.GetTransitGatewayPeeringRequest.make ~peeringId ())
           (Some Values.GetTransitGatewayPeeringResponse.to_json)
           (Some Values.GetTransitGatewayPeeringResponse.error_to_json)])
let get_transit_gateway_registrations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transitGatewayArns =
         flag "transit-gateway-arns" (optional json_arg)
           ~doc:"JSON TransitGatewayArnList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_transit_gateway_registrations
           (Values.GetTransitGatewayRegistrationsRequest.make
              ?transitGatewayArns:(Option.map
                                     ~f:Values.TransitGatewayArnList.of_json
                                     transitGatewayArns) ?maxResults
              ?nextToken ~globalNetworkId ())
           (Some Values.GetTransitGatewayRegistrationsResponse.to_json)
           (Some Values.GetTransitGatewayRegistrationsResponse.error_to_json)])
let get_transit_gateway_route_table_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_transit_gateway_route_table_attachment
           (Values.GetTransitGatewayRouteTableAttachmentRequest.make
              ~attachmentId ())
           (Some Values.GetTransitGatewayRouteTableAttachmentResponse.to_json)
           (Some
              Values.GetTransitGatewayRouteTableAttachmentResponse.error_to_json)])
let get_vpc_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vpc_attachment
           (Values.GetVpcAttachmentRequest.make ~attachmentId ())
           (Some Values.GetVpcAttachmentResponse.to_json)
           (Some Values.GetVpcAttachmentResponse.error_to_json)])
let list_attachment_routing_policy_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachmentId =
         flag "attachment-id" (optional string) ~doc:"STRING AttachmentId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attachment_routing_policy_associations
           (Values.ListAttachmentRoutingPolicyAssociationsRequest.make
              ?attachmentId ?maxResults ?nextToken ~coreNetworkId ())
           (Some
              Values.ListAttachmentRoutingPolicyAssociationsResponse.to_json)
           (Some
              Values.ListAttachmentRoutingPolicyAssociationsResponse.error_to_json)])
let list_attachments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkId =
         flag "core-network-id" (optional string) ~doc:"STRING CoreNetworkId"
       and attachmentType =
         flag "attachment-type" (optional json_arg)
           ~doc:"JSON AttachmentType"
       and edgeLocation =
         flag "edge-location" (optional string)
           ~doc:"STRING ExternalRegionCode"
       and state =
         flag "state" (optional json_arg) ~doc:"JSON AttachmentState"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attachments
           (Values.ListAttachmentsRequest.make ?coreNetworkId
              ?attachmentType:(Option.map ~f:Values.AttachmentType.of_json
                                 attachmentType) ?edgeLocation
              ?state:(Option.map ~f:Values.AttachmentState.of_json state)
              ?maxResults ?nextToken ())
           (Some Values.ListAttachmentsResponse.to_json)
           (Some Values.ListAttachmentsResponse.error_to_json)])
let list_connect_peers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkId =
         flag "core-network-id" (optional string) ~doc:"STRING CoreNetworkId"
       and connectAttachmentId =
         flag "connect-attachment-id" (optional string)
           ~doc:"STRING AttachmentId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connect_peers
           (Values.ListConnectPeersRequest.make ?coreNetworkId
              ?connectAttachmentId ?maxResults ?nextToken ())
           (Some Values.ListConnectPeersResponse.to_json)
           (Some Values.ListConnectPeersResponse.error_to_json)])
let list_core_network_policy_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_core_network_policy_versions
           (Values.ListCoreNetworkPolicyVersionsRequest.make ?maxResults
              ?nextToken ~coreNetworkId ())
           (Some Values.ListCoreNetworkPolicyVersionsResponse.to_json)
           (Some Values.ListCoreNetworkPolicyVersionsResponse.error_to_json)])
let list_core_network_prefix_list_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and prefixListArn =
         flag "prefix-list-arn" (optional string) ~doc:"STRING PrefixListArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_core_network_prefix_list_associations
           (Values.ListCoreNetworkPrefixListAssociationsRequest.make
              ?prefixListArn ?maxResults ?nextToken ~coreNetworkId ())
           (Some Values.ListCoreNetworkPrefixListAssociationsResponse.to_json)
           (Some
              Values.ListCoreNetworkPrefixListAssociationsResponse.error_to_json)])
let list_core_network_routing_information =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextHopFilters =
         flag "next-hop-filters" (optional json_arg) ~doc:"JSON FilterMap"
       and localPreferenceMatches =
         flag "local-preference-matches" (optional json_arg)
           ~doc:"JSON ConstrainedStringList"
       and exactAsPathMatches =
         flag "exact-as-path-matches" (optional json_arg)
           ~doc:"JSON ConstrainedStringList"
       and medMatches =
         flag "med-matches" (optional json_arg)
           ~doc:"JSON ConstrainedStringList"
       and communityMatches =
         flag "community-matches" (optional json_arg)
           ~doc:"JSON ConstrainedStringList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and segmentName =
         flag "segment-name" (required string)
           ~doc:"STRING ConstrainedString"
       and edgeLocation =
         flag "edge-location" (required string)
           ~doc:"STRING ExternalRegionCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_core_network_routing_information
           (Values.ListCoreNetworkRoutingInformationRequest.make
              ?nextHopFilters:(Option.map ~f:Values.FilterMap.of_json
                                 nextHopFilters)
              ?localPreferenceMatches:(Option.map
                                         ~f:Values.ConstrainedStringList.of_json
                                         localPreferenceMatches)
              ?exactAsPathMatches:(Option.map
                                     ~f:Values.ConstrainedStringList.of_json
                                     exactAsPathMatches)
              ?medMatches:(Option.map ~f:Values.ConstrainedStringList.of_json
                             medMatches)
              ?communityMatches:(Option.map
                                   ~f:Values.ConstrainedStringList.of_json
                                   communityMatches) ?maxResults ?nextToken
              ~coreNetworkId ~segmentName ~edgeLocation ())
           (Some Values.ListCoreNetworkRoutingInformationResponse.to_json)
           (Some
              Values.ListCoreNetworkRoutingInformationResponse.error_to_json)])
let list_core_networks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_core_networks
           (Values.ListCoreNetworksRequest.make ?maxResults ?nextToken ())
           (Some Values.ListCoreNetworksResponse.to_json)
           (Some Values.ListCoreNetworksResponse.error_to_json)])
let list_organization_service_access_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_organization_service_access_status
           (Values.ListOrganizationServiceAccessStatusRequest.make
              ?maxResults ?nextToken ())
           (Some Values.ListOrganizationServiceAccessStatusResponse.to_json)
           (Some
              Values.ListOrganizationServiceAccessStatusResponse.error_to_json)])
let list_peerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkId =
         flag "core-network-id" (optional string) ~doc:"STRING CoreNetworkId"
       and peeringType =
         flag "peering-type" (optional json_arg) ~doc:"JSON PeeringType"
       and edgeLocation =
         flag "edge-location" (optional string)
           ~doc:"STRING ExternalRegionCode"
       and state = flag "state" (optional json_arg) ~doc:"JSON PeeringState"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_peerings
           (Values.ListPeeringsRequest.make ?coreNetworkId
              ?peeringType:(Option.map ~f:Values.PeeringType.of_json
                              peeringType) ?edgeLocation
              ?state:(Option.map ~f:Values.PeeringState.of_json state)
              ?maxResults ?nextToken ())
           (Some Values.ListPeeringsResponse.to_json)
           (Some Values.ListPeeringsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_attachment_routing_policy_label =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId"
       and routingPolicyLabel =
         flag "routing-policy-label" (required string)
           ~doc:"STRING ConstrainedString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_attachment_routing_policy_label
           (Values.PutAttachmentRoutingPolicyLabelRequest.make ?clientToken
              ~coreNetworkId ~attachmentId ~routingPolicyLabel ())
           (Some Values.PutAttachmentRoutingPolicyLabelResponse.to_json)
           (Some Values.PutAttachmentRoutingPolicyLabelResponse.error_to_json)])
let put_core_network_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING ConstrainedString"
       and latestVersionId =
         flag "latest-version-id" (optional int) ~doc:"INT Integer"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING CoreNetworkPolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_core_network_policy
           (Values.PutCoreNetworkPolicyRequest.make ?description
              ?latestVersionId ?clientToken ~coreNetworkId ~policyDocument ())
           (Some Values.PutCoreNetworkPolicyResponse.to_json)
           (Some Values.PutCoreNetworkPolicyResponse.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING ResourcePolicyDocument"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyRequest.make ~policyDocument ~resourceArn
              ()) (Some Values.PutResourcePolicyResponse.to_json)
           (Some Values.PutResourcePolicyResponse.error_to_json)])
let register_transit_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and transitGatewayArn =
         flag "transit-gateway-arn" (required string)
           ~doc:"STRING TransitGatewayArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_transit_gateway
           (Values.RegisterTransitGatewayRequest.make ~globalNetworkId
              ~transitGatewayArn ())
           (Some Values.RegisterTransitGatewayResponse.to_json)
           (Some Values.RegisterTransitGatewayResponse.error_to_json)])
let reject_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_attachment
           (Values.RejectAttachmentRequest.make ~attachmentId ())
           (Some Values.RejectAttachmentResponse.to_json)
           (Some Values.RejectAttachmentResponse.error_to_json)])
let remove_attachment_routing_policy_label =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_attachment_routing_policy_label
           (Values.RemoveAttachmentRoutingPolicyLabelRequest.make
              ~coreNetworkId ~attachmentId ())
           (Some Values.RemoveAttachmentRoutingPolicyLabelResponse.to_json)
           (Some
              Values.RemoveAttachmentRoutingPolicyLabelResponse.error_to_json)])
let restore_core_network_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId"
       and policyVersionId =
         flag "policy-version-id" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_core_network_policy_version
           (Values.RestoreCoreNetworkPolicyVersionRequest.make ~coreNetworkId
              ~policyVersionId ())
           (Some Values.RestoreCoreNetworkPolicyVersionResponse.to_json)
           (Some Values.RestoreCoreNetworkPolicyVersionResponse.error_to_json)])
let start_organization_service_access_update =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and action = flag "action" (required string) ~doc:"STRING Action" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_organization_service_access_update
           (Values.StartOrganizationServiceAccessUpdateRequest.make ~action
              ())
           (Some Values.StartOrganizationServiceAccessUpdateResponse.to_json)
           (Some
              Values.StartOrganizationServiceAccessUpdateResponse.error_to_json)])
let start_route_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeReturnPath =
         flag "include-return-path" (optional bool) ~doc:"BOOL Boolean"
       and useMiddleboxes =
         flag "use-middleboxes" (optional bool) ~doc:"BOOL Boolean"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and source =
         flag "source" (required json_arg)
           ~doc:"JSON RouteAnalysisEndpointOptionsSpecification"
       and destination =
         flag "destination" (required json_arg)
           ~doc:"JSON RouteAnalysisEndpointOptionsSpecification" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_route_analysis
           (Values.StartRouteAnalysisRequest.make ?includeReturnPath
              ?useMiddleboxes ~globalNetworkId
              ~source:(Values.RouteAnalysisEndpointOptionsSpecification.of_json
                         source)
              ~destination:(Values.RouteAnalysisEndpointOptionsSpecification.of_json
                              destination) ())
           (Some Values.StartRouteAnalysisResponse.to_json)
           (Some Values.StartRouteAnalysisResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and linkId = flag "link-id" (optional string) ~doc:"STRING LinkId"
       and connectedLinkId =
         flag "connected-link-id" (optional string) ~doc:"STRING LinkId"
       and description =
         flag "description" (optional string) ~doc:"STRING ConstrainedString"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and connectionId =
         flag "connection-id" (required string) ~doc:"STRING ConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connection
           (Values.UpdateConnectionRequest.make ?linkId ?connectedLinkId
              ?description ~globalNetworkId ~connectionId ())
           (Some Values.UpdateConnectionResponse.to_json)
           (Some Values.UpdateConnectionResponse.error_to_json)])
let update_core_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING ConstrainedString"
       and coreNetworkId =
         flag "core-network-id" (required string) ~doc:"STRING CoreNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_core_network
           (Values.UpdateCoreNetworkRequest.make ?description ~coreNetworkId
              ()) (Some Values.UpdateCoreNetworkResponse.to_json)
           (Some Values.UpdateCoreNetworkResponse.error_to_json)])
let update_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aWSLocation =
         flag "a-w-s-location" (optional json_arg) ~doc:"JSON AWSLocation"
       and description =
         flag "description" (optional string) ~doc:"STRING ConstrainedString"
       and type_ =
         flag "type-" (optional string) ~doc:"STRING ConstrainedString"
       and vendor =
         flag "vendor" (optional string) ~doc:"STRING ConstrainedString"
       and model =
         flag "model" (optional string) ~doc:"STRING ConstrainedString"
       and serialNumber =
         flag "serial-number" (optional string)
           ~doc:"STRING ConstrainedString"
       and location =
         flag "location" (optional json_arg) ~doc:"JSON Location"
       and siteId = flag "site-id" (optional string) ~doc:"STRING SiteId"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_device
           (Values.UpdateDeviceRequest.make
              ?aWSLocation:(Option.map ~f:Values.AWSLocation.of_json
                              aWSLocation) ?description ?type_ ?vendor ?model
              ?serialNumber
              ?location:(Option.map ~f:Values.Location.of_json location)
              ?siteId ~globalNetworkId ~deviceId ())
           (Some Values.UpdateDeviceResponse.to_json)
           (Some Values.UpdateDeviceResponse.error_to_json)])
let update_direct_connect_gateway_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and edgeLocations =
         flag "edge-locations" (optional json_arg)
           ~doc:"JSON ExternalRegionCodeList"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_direct_connect_gateway_attachment
           (Values.UpdateDirectConnectGatewayAttachmentRequest.make
              ?edgeLocations:(Option.map
                                ~f:Values.ExternalRegionCodeList.of_json
                                edgeLocations) ~attachmentId ())
           (Some Values.UpdateDirectConnectGatewayAttachmentResponse.to_json)
           (Some
              Values.UpdateDirectConnectGatewayAttachmentResponse.error_to_json)])
let update_global_network =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING ConstrainedString"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_global_network
           (Values.UpdateGlobalNetworkRequest.make ?description
              ~globalNetworkId ())
           (Some Values.UpdateGlobalNetworkResponse.to_json)
           (Some Values.UpdateGlobalNetworkResponse.error_to_json)])
let update_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING ConstrainedString"
       and type_ =
         flag "type-" (optional string) ~doc:"STRING ConstrainedString"
       and bandwidth =
         flag "bandwidth" (optional json_arg) ~doc:"JSON Bandwidth"
       and provider =
         flag "provider" (optional string) ~doc:"STRING ConstrainedString"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_link
           (Values.UpdateLinkRequest.make ?description ?type_
              ?bandwidth:(Option.map ~f:Values.Bandwidth.of_json bandwidth)
              ?provider ~globalNetworkId ~linkId ())
           (Some Values.UpdateLinkResponse.to_json)
           (Some Values.UpdateLinkResponse.error_to_json)])
let update_network_resource_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and metadata =
         flag "metadata" (required json_arg)
           ~doc:"JSON NetworkResourceMetadataMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_network_resource_metadata
           (Values.UpdateNetworkResourceMetadataRequest.make ~globalNetworkId
              ~resourceArn
              ~metadata:(Values.NetworkResourceMetadataMap.of_json metadata)
              ()) (Some Values.UpdateNetworkResourceMetadataResponse.to_json)
           (Some Values.UpdateNetworkResourceMetadataResponse.error_to_json)])
let update_site =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING ConstrainedString"
       and location =
         flag "location" (optional json_arg) ~doc:"JSON Location"
       and globalNetworkId =
         flag "global-network-id" (required string)
           ~doc:"STRING GlobalNetworkId"
       and siteId = flag "site-id" (required string) ~doc:"STRING SiteId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_site
           (Values.UpdateSiteRequest.make ?description
              ?location:(Option.map ~f:Values.Location.of_json location)
              ~globalNetworkId ~siteId ())
           (Some Values.UpdateSiteResponse.to_json)
           (Some Values.UpdateSiteResponse.error_to_json)])
let update_vpc_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addSubnetArns =
         flag "add-subnet-arns" (optional json_arg) ~doc:"JSON SubnetArnList"
       and removeSubnetArns =
         flag "remove-subnet-arns" (optional json_arg)
           ~doc:"JSON SubnetArnList"
       and options =
         flag "options" (optional json_arg) ~doc:"JSON VpcOptions"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_vpc_attachment
           (Values.UpdateVpcAttachmentRequest.make
              ?addSubnetArns:(Option.map ~f:Values.SubnetArnList.of_json
                                addSubnetArns)
              ?removeSubnetArns:(Option.map ~f:Values.SubnetArnList.of_json
                                   removeSubnetArns)
              ?options:(Option.map ~f:Values.VpcOptions.of_json options)
              ~attachmentId ())
           (Some Values.UpdateVpcAttachmentResponse.to_json)
           (Some Values.UpdateVpcAttachmentResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-attachment", accept_attachment);
    ("associate-connect-peer", associate_connect_peer);
    ("associate-customer-gateway", associate_customer_gateway);
    ("associate-link", associate_link);
    ("associate-transit-gateway-connect-peer",
      associate_transit_gateway_connect_peer);
    ("create-connect-attachment", create_connect_attachment);
    ("create-connect-peer", create_connect_peer);
    ("create-connection", create_connection);
    ("create-core-network", create_core_network);
    ("create-core-network-prefix-list-association",
      create_core_network_prefix_list_association);
    ("create-device", create_device);
    ("create-direct-connect-gateway-attachment",
      create_direct_connect_gateway_attachment);
    ("create-global-network", create_global_network);
    ("create-link", create_link);
    ("create-site", create_site);
    ("create-site-to-site-vpn-attachment",
      create_site_to_site_vpn_attachment);
    ("create-transit-gateway-peering", create_transit_gateway_peering);
    ("create-transit-gateway-route-table-attachment",
      create_transit_gateway_route_table_attachment);
    ("create-vpc-attachment", create_vpc_attachment);
    ("delete-attachment", delete_attachment);
    ("delete-connect-peer", delete_connect_peer);
    ("delete-connection", delete_connection);
    ("delete-core-network", delete_core_network);
    ("delete-core-network-policy-version",
      delete_core_network_policy_version);
    ("delete-core-network-prefix-list-association",
      delete_core_network_prefix_list_association);
    ("delete-device", delete_device);
    ("delete-global-network", delete_global_network);
    ("delete-link", delete_link);
    ("delete-peering", delete_peering);
    ("delete-resource-policy", delete_resource_policy);
    ("delete-site", delete_site);
    ("deregister-transit-gateway", deregister_transit_gateway);
    ("describe-global-networks", describe_global_networks);
    ("disassociate-connect-peer", disassociate_connect_peer);
    ("disassociate-customer-gateway", disassociate_customer_gateway);
    ("disassociate-link", disassociate_link);
    ("disassociate-transit-gateway-connect-peer",
      disassociate_transit_gateway_connect_peer);
    ("execute-core-network-change-set", execute_core_network_change_set);
    ("get-connect-attachment", get_connect_attachment);
    ("get-connect-peer", get_connect_peer);
    ("get-connect-peer-associations", get_connect_peer_associations);
    ("get-connections", get_connections);
    ("get-core-network", get_core_network);
    ("get-core-network-change-events", get_core_network_change_events);
    ("get-core-network-change-set", get_core_network_change_set);
    ("get-core-network-policy", get_core_network_policy);
    ("get-customer-gateway-associations", get_customer_gateway_associations);
    ("get-devices", get_devices);
    ("get-direct-connect-gateway-attachment",
      get_direct_connect_gateway_attachment);
    ("get-link-associations", get_link_associations);
    ("get-links", get_links);
    ("get-network-resource-counts", get_network_resource_counts);
    ("get-network-resource-relationships",
      get_network_resource_relationships);
    ("get-network-resources", get_network_resources);
    ("get-network-routes", get_network_routes);
    ("get-network-telemetry", get_network_telemetry);
    ("get-resource-policy", get_resource_policy);
    ("get-route-analysis", get_route_analysis);
    ("get-site-to-site-vpn-attachment", get_site_to_site_vpn_attachment);
    ("get-sites", get_sites);
    ("get-transit-gateway-connect-peer-associations",
      get_transit_gateway_connect_peer_associations);
    ("get-transit-gateway-peering", get_transit_gateway_peering);
    ("get-transit-gateway-registrations", get_transit_gateway_registrations);
    ("get-transit-gateway-route-table-attachment",
      get_transit_gateway_route_table_attachment);
    ("get-vpc-attachment", get_vpc_attachment);
    ("list-attachment-routing-policy-associations",
      list_attachment_routing_policy_associations);
    ("list-attachments", list_attachments);
    ("list-connect-peers", list_connect_peers);
    ("list-core-network-policy-versions", list_core_network_policy_versions);
    ("list-core-network-prefix-list-associations",
      list_core_network_prefix_list_associations);
    ("list-core-network-routing-information",
      list_core_network_routing_information);
    ("list-core-networks", list_core_networks);
    ("list-organization-service-access-status",
      list_organization_service_access_status);
    ("list-peerings", list_peerings);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-attachment-routing-policy-label",
      put_attachment_routing_policy_label);
    ("put-core-network-policy", put_core_network_policy);
    ("put-resource-policy", put_resource_policy);
    ("register-transit-gateway", register_transit_gateway);
    ("reject-attachment", reject_attachment);
    ("remove-attachment-routing-policy-label",
      remove_attachment_routing_policy_label);
    ("restore-core-network-policy-version",
      restore_core_network_policy_version);
    ("start-organization-service-access-update",
      start_organization_service_access_update);
    ("start-route-analysis", start_route_analysis);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-connection", update_connection);
    ("update-core-network", update_core_network);
    ("update-device", update_device);
    ("update-direct-connect-gateway-attachment",
      update_direct_connect_gateway_attachment);
    ("update-global-network", update_global_network);
    ("update-link", update_link);
    ("update-network-resource-metadata", update_network_resource_metadata);
    ("update-site", update_site);
    ("update-vpc-attachment", update_vpc_attachment)]
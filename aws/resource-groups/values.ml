(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.resource_groups
let apiVersion = "2017-11-27"
let endpointPrefix = "resource-groups"
let serviceFullName = "AWS Resource Groups"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "Resource Groups"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module GroupConfigurationParameterValue =
  struct
    type nonrec t = string
    let context_ = "GroupConfigurationParameterValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9:_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GroupConfigurationParameterValue" j
    let to_json = simple_to_json to_value
  end
module GroupConfigurationParameterName =
  struct
    type nonrec t = string
    let context_ = "GroupConfigurationParameterName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:80) >>=
                  (fun () -> check_pattern i ~pattern:"[a-z-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GroupConfigurationParameterName" j
    let to_json = simple_to_json to_value
  end
module GroupConfigurationParameterValueList =
  struct
    type nonrec t = GroupConfigurationParameterValue.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:GroupConfigurationParameterValue.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:GroupConfigurationParameterValue.of_xml)
    let of_json j =
      list_of_json ~kind:"GroupConfigurationParameterValueList"
        ~of_json:GroupConfigurationParameterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module GroupConfigurationParameter =
  struct
    type nonrec t =
      {
      name: GroupConfigurationParameterName.t
        [@ocaml.doc
          "The name of the group configuration parameter. For the list of parameters that you can use with each configuration item type, see Supported resource types and parameters."];
      values: GroupConfigurationParameterValueList.t option
        [@ocaml.doc
          "The value or values to be used for the specified parameter. For the list of values you can use with each parameter, see Supported resource types and parameters."]}
    let context_ = "GroupConfigurationParameter"
    let make ?values = fun ~name -> fun () -> { values; name }
    let to_value x =
      structure_to_value
        [("Name", (Some (GroupConfigurationParameterName.to_value x.name)));
        ("Values",
          (Option.map x.values
             ~f:GroupConfigurationParameterValueList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        (Option.map ~f:GroupConfigurationParameterValueList.of_xml)
          (Xml.child xml_arg0 "Values") in
      let name =
        GroupConfigurationParameterName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?values ~name ()
    let of_json json =
      let values =
        field_map json "Values" GroupConfigurationParameterValueList.of_json in
      let name =
        field_map_exn json "Name" GroupConfigurationParameterName.of_json in
      make ?values ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A parameter for a group configuration item. For details about group service configuration syntax, see Service configurations for resource groups."]
module GroupFilterValue =
  struct
    type nonrec t = string
    let context_ = "GroupFilterValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"AWS::(AllSupported|[a-zA-Z0-9]+::[a-zA-Z0-9]+)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GroupFilterValue" j
    let to_json = simple_to_json to_value
  end
module ResourceArn =
  struct
    type nonrec t = string
    let context_ = "ResourceArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"arn:aws(-[a-z]+)*:[a-z0-9\\-]*:([a-z]{2}(-[a-z]+)+-\\d{1})?:([0-9]{12})?:.+");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceArn" j
    let to_json = simple_to_json to_value
  end
module ResourceType =
  struct
    type nonrec t = string
    let context_ = "ResourceType"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"AWS::[a-zA-Z0-9]+::\\w+");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceType" j
    let to_json = simple_to_json to_value
  end
module ResourceStatusValue =
  struct
    type nonrec t =
      | PENDING 
      | Non_static_id of string 
    let make i = i
    let to_string = function | PENDING -> "PENDING" | Non_static_id s -> s
    let of_string = function | "PENDING" -> PENDING | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ResourceStatusValue" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ResourceStatusValue" j)
    let to_json = simple_to_json to_value
  end
module ResourceFilterValue =
  struct
    type nonrec t = string
    let context_ = "ResourceFilterValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"AWS::[a-zA-Z0-9]+::[a-zA-Z0-9]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceFilterValue" j
    let to_json = simple_to_json to_value
  end
module GroupConfigurationType =
  struct
    type nonrec t = string
    let context_ = "GroupConfigurationType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:40) >>=
             (fun () ->
                check_pattern i ~pattern:"AWS::[a-zA-Z0-9]+::[a-zA-Z0-9]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GroupConfigurationType" j
    let to_json = simple_to_json to_value
  end
module GroupParameterList =
  struct
    type nonrec t = GroupConfigurationParameter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:GroupConfigurationParameter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GroupConfigurationParameter.of_xml)
    let of_json j =
      list_of_json ~kind:"GroupParameterList"
        ~of_json:GroupConfigurationParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module Query =
  struct
    type nonrec t = string
    let context_ = "Query"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:4096) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Query" j
    let to_json = simple_to_json to_value
  end
module QueryType =
  struct
    type nonrec t =
      | TAG_FILTERS_1_0 
      | CLOUDFORMATION_STACK_1_0 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TAG_FILTERS_1_0 -> "TAG_FILTERS_1_0"
      | CLOUDFORMATION_STACK_1_0 -> "CLOUDFORMATION_STACK_1_0"
      | Non_static_id s -> s
    let of_string =
      function
      | "TAG_FILTERS_1_0" -> TAG_FILTERS_1_0
      | "CLOUDFORMATION_STACK_1_0" -> CLOUDFORMATION_STACK_1_0
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration QueryType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"QueryType" j)
    let to_json = simple_to_json to_value
  end
module ErrorCode =
  struct
    type nonrec t = string
    let context_ = "ErrorCode"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorCode" j
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module QueryErrorCode =
  struct
    type nonrec t =
      | CLOUDFORMATION_STACK_INACTIVE 
      | CLOUDFORMATION_STACK_NOT_EXISTING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CLOUDFORMATION_STACK_INACTIVE -> "CLOUDFORMATION_STACK_INACTIVE"
      | CLOUDFORMATION_STACK_NOT_EXISTING ->
          "CLOUDFORMATION_STACK_NOT_EXISTING"
      | Non_static_id s -> s
    let of_string =
      function
      | "CLOUDFORMATION_STACK_INACTIVE" -> CLOUDFORMATION_STACK_INACTIVE
      | "CLOUDFORMATION_STACK_NOT_EXISTING" ->
          CLOUDFORMATION_STACK_NOT_EXISTING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration QueryErrorCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"QueryErrorCode" j)
    let to_json = simple_to_json to_value
  end
module QueryErrorMessage =
  struct
    type nonrec t = string
    let context_ = "QueryErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"QueryErrorMessage" j
    let to_json = simple_to_json to_value
  end
module GroupArn =
  struct
    type nonrec t = string
    let context_ = "GroupArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:12) >>=
             (fun () ->
                (check_string_max i ~max:1600) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(-[a-z]+)*:resource-groups:[a-z]{2}(-[a-z]+)+-\\d{1}:[0-9]{12}:group/[a-zA-Z0-9_\\.-]{1,128}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GroupArn" j
    let to_json = simple_to_json to_value
  end
module GroupName =
  struct
    type nonrec t = string
    let context_ = "GroupName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9_\\.-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GroupName" j
    let to_json = simple_to_json to_value
  end
module Description =
  struct
    type nonrec t = string
    let context_ = "Description"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_pattern i ~pattern:"[\\sa-zA-Z0-9_\\.-]*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Description" j
    let to_json = simple_to_json to_value
  end
module GroupFilterName =
  struct
    type nonrec t =
      | Resource_type 
      | Configuration_type 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Resource_type -> "resource-type"
      | Configuration_type -> "configuration-type"
      | Non_static_id s -> s
    let of_string =
      function
      | "resource-type" -> Resource_type
      | "configuration-type" -> Configuration_type
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration GroupFilterName" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GroupFilterName" j)
    let to_json = simple_to_json to_value
  end
module GroupFilterValues =
  struct
    type nonrec t = GroupFilterValue.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:GroupFilterValue.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GroupFilterValue.of_xml)
    let of_json j =
      list_of_json ~kind:"GroupFilterValues"
        ~of_json:GroupFilterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceIdentifier =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t option [@ocaml.doc "The ARN of a resource."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The resource type of a resource, such as AWS::EC2::Instance."]}
    let make ?resourceArn =
      fun ?resourceType -> fun () -> { resourceArn; resourceType }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Option.map x.resourceArn ~f:ResourceArn.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let resourceArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      make ?resourceType ?resourceArn ()
    let of_json json =
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let resourceArn = field_map json "ResourceArn" ResourceArn.of_json in
      make ?resourceType ?resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that contains the ARN of a resource and its resource type."]
module ResourceStatus =
  struct
    type nonrec t =
      {
      name: ResourceStatusValue.t option [@ocaml.doc "The current status."]}
    let make ?name = fun () -> { name }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:ResourceStatusValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:ResourceStatusValue.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?name ()
    let of_json json =
      let name = field_map json "Name" ResourceStatusValue.of_json in
      make ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that identifies the current group membership status for a resource. Adding a resource to a resource group is performed asynchronously as a background task. A PENDING status indicates, for this resource, that the process isn't completed yet."]
module ResourceFilterName =
  struct
    type nonrec t =
      | Resource_type 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Resource_type -> "resource-type" | Non_static_id s -> s
    let of_string =
      function | "resource-type" -> Resource_type | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ResourceFilterName" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ResourceFilterName" j)
    let to_json = simple_to_json to_value
  end
module ResourceFilterValues =
  struct
    type nonrec t = ResourceFilterValue.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:ResourceFilterValue.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceFilterValue.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceFilterValues"
        ~of_json:ResourceFilterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module GroupConfigurationItem =
  struct
    type nonrec t =
      {
      type_: GroupConfigurationType.t
        [@ocaml.doc
          "Specifies the type of group configuration item. Each item must have a unique value for type. For the list of types that you can specify for a configuration item, see Supported resource types and parameters."];
      parameters: GroupParameterList.t option
        [@ocaml.doc
          "A collection of parameters for this group configuration item. For the list of parameters that you can use with each configuration item type, see Supported resource types and parameters."]}
    let context_ = "GroupConfigurationItem"
    let make ?parameters = fun ~type_ -> fun () -> { parameters; type_ }
    let to_value x =
      structure_to_value
        [("Type", (Some (GroupConfigurationType.to_value x.type_)));
        ("Parameters",
          (Option.map x.parameters ~f:GroupParameterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameters =
        (Option.map ~f:GroupParameterList.of_xml)
          (Xml.child xml_arg0 "Parameters") in
      let type_ =
        GroupConfigurationType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      make ?parameters ~type_ ()
    let of_json json =
      let parameters = field_map json "Parameters" GroupParameterList.of_json in
      let type_ = field_map_exn json "Type" GroupConfigurationType.of_json in
      make ?parameters ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An item in a group configuration. A group service configuration can have one or more items. For details about group service configuration syntax, see Service configurations for resource groups."]
module ResourceQuery =
  struct
    type nonrec t =
      {
      type_: QueryType.t
        [@ocaml.doc
          "The type of the query. You can use the following values: CLOUDFORMATION_STACK_1_0: Specifies that the Query contains an ARN for a CloudFormation stack. TAG_FILTERS_1_0: Specifies that the Query parameter contains a JSON string that represents a collection of simple tag filters for resource types and tags. The JSON string uses a syntax similar to the GetResources operation, but uses only the ResourceTypeFilters and TagFilters fields. If you specify more than one tag key, only resources that match all tag keys, and at least one value of each specified tag key, are returned in your query. If you specify more than one value for a tag key, a resource matches the filter if it has a tag key value that matches any of the specified values. For example, consider the following sample query for resources that have two tags, Stage and Version, with two values each: [{\"Stage\":[\"Test\",\"Deploy\"]},{\"Version\":[\"1\",\"2\"]}] The results of this query could include the following. An EC2 instance that has the following two tags: {\"Stage\":\"Deploy\"}, and {\"Version\":\"2\"} An S3 bucket that has the following two tags: {\"Stage\":\"Test\"}, and {\"Version\":\"1\"} The query would not include the following items in the results, however. An EC2 instance that has only the following tag: {\"Stage\":\"Deploy\"}. The instance does not have all of the tag keys specified in the filter, so it is excluded from the results. An RDS database that has the following two tags: {\"Stage\":\"Archived\"} and {\"Version\":\"4\"} The database has all of the tag keys, but none of those keys has an associated value that matches at least one of the specified values in the filter."];
      query: Query.t
        [@ocaml.doc "The query that defines a group or a search."]}
    let context_ = "ResourceQuery"
    let make ~type_ = fun ~query -> fun () -> { type_; query }
    let to_value x =
      structure_to_value
        [("Type", (Some (QueryType.to_value x.type_)));
        ("Query", (Some (Query.to_value x.query)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let query =
        Query.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Query") in
      let type_ =
        QueryType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      make ~query ~type_ ()
    let of_json json =
      let query = field_map_exn json "Query" Query.of_json in
      let type_ = field_map_exn json "Type" QueryType.of_json in
      make ~query ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The query that is used to define a resource group or a search for resources. A query specifies both a query type and a query string as a JSON object. See the examples section for example JSON strings. The examples that follow are shown as standard JSON strings. If you include such a string as a parameter to the AWS CLI or an SDK API, you might need to 'escape' the string into a single line. For example, see the Quoting strings in the AWS CLI User Guide. Example 1 The following generic example shows a resource query JSON string that includes only resources that meet the following criteria: The resource type must be either resource_type1 or resource_type2. The resource must have a tag Key1 with a value of either ValueA or ValueB. The resource must have a tag Key2 with a value of either ValueC or ValueD. { \"Type\": \"TAG_FILTERS_1_0\", \"Query\": { \"ResourceTypeFilters\": [ \"resource_type1\", \"resource_type2\"], \"TagFilters\": [ { \"Key\": \"Key1\", \"Values\": [\"ValueA\",\"ValueB\"] }, { \"Key\":\"Key2\", \"Values\":[\"ValueC\",\"ValueD\"] } ] } } This has the equivalent \"shortcut\" syntax of the following: { \"Type\": \"TAG_FILTERS_1_0\", \"Query\": { \"ResourceTypeFilters\": [ \"resource_type1\", \"resource_type2\"], \"TagFilters\": [ { \"Key1\": [\"ValueA\",\"ValueB\"] }, { \"Key2\": [\"ValueC\",\"ValueD\"] } ] } } Example 2 The following example shows a resource query JSON string that includes only Amazon EC2 instances that are tagged Stage with a value of Test. { \"Type\": \"TAG_FILTERS_1_0\", \"Query\": \"{ \"ResourceTypeFilters\": \"AWS::EC2::Instance\", \"TagFilters\": { \"Stage\": \"Test\" } } } Example 3 The following example shows a resource query JSON string that includes resource of any supported type as long as it is tagged Stage with a value of Prod. { \"Type\": \"TAG_FILTERS_1_0\", \"Query\": { \"ResourceTypeFilters\": \"AWS::AllSupported\", \"TagFilters\": { \"Stage\": \"Prod\" } } } Example 4 The following example shows a resource query JSON string that includes only Amazon EC2 instances and Amazon S3 buckets that are part of the specified AWS CloudFormation stack. { \"Type\": \"CLOUDFORMATION_STACK_1_0\", \"Query\": { \"ResourceTypeFilters\": [ \"AWS::EC2::Instance\", \"AWS::S3::Bucket\" ], \"StackIdentifier\": \"arn:aws:cloudformation:us-west-2:123456789012:stack/AWStestuseraccount/fb0d5000-aba8-00e8-aa9e-50d5cEXAMPLE\" } }"]
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module FailedResource =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t option
        [@ocaml.doc
          "The ARN of the resource that failed to be added or removed."];
      errorMessage: ErrorMessage.t option
        [@ocaml.doc "The error message text associated with the failure."];
      errorCode: ErrorCode.t option
        [@ocaml.doc "The error code associated with the failure."]}
    let make ?resourceArn =
      fun ?errorMessage ->
        fun ?errorCode -> fun () -> { resourceArn; errorMessage; errorCode }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Option.map x.resourceArn ~f:ResourceArn.to_value));
        ("ErrorMessage",
          (Option.map x.errorMessage ~f:ErrorMessage.to_value));
        ("ErrorCode", (Option.map x.errorCode ~f:ErrorCode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "ErrorCode") in
      let errorMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "ErrorMessage") in
      let resourceArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      make ?errorCode ?errorMessage ?resourceArn ()
    let of_json json =
      let errorCode = field_map json "ErrorCode" ErrorCode.of_json in
      let errorMessage = field_map json "ErrorMessage" ErrorMessage.of_json in
      let resourceArn = field_map json "ResourceArn" ResourceArn.of_json in
      make ?errorCode ?errorMessage ?resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A resource that failed to be added to or removed from a group."]
module PendingResource =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon resource name (ARN) of the resource that's in a pending state."]}
    let make ?resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Option.map x.resourceArn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      make ?resourceArn ()
    let of_json json =
      let resourceArn = field_map json "ResourceArn" ResourceArn.of_json in
      make ?resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that identifies a resource that is currently pending addition to the group as a member. Adding a resource to a resource group happens asynchronously as a background task and this one isn't completed yet."]
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module QueryError =
  struct
    type nonrec t =
      {
      errorCode: QueryErrorCode.t option
        [@ocaml.doc
          "Possible values are CLOUDFORMATION_STACK_INACTIVE and CLOUDFORMATION_STACK_NOT_EXISTING."];
      message: QueryErrorMessage.t option
        [@ocaml.doc
          "A message that explains the ErrorCode value. Messages might state that the specified CloudFormation stack does not exist (or no longer exists). For CLOUDFORMATION_STACK_INACTIVE, the message typically states that the CloudFormation stack has a status that is not (or no longer) active, such as CREATE_FAILED."]}
    let make ?errorCode = fun ?message -> fun () -> { errorCode; message }
    let to_value x =
      structure_to_value
        [("ErrorCode", (Option.map x.errorCode ~f:QueryErrorCode.to_value));
        ("Message", (Option.map x.message ~f:QueryErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:QueryErrorMessage.of_xml)
          (Xml.child xml_arg0 "Message") in
      let errorCode =
        (Option.map ~f:QueryErrorCode.of_xml)
          (Xml.child xml_arg0 "ErrorCode") in
      make ?message ?errorCode ()
    let of_json json =
      let message = field_map json "Message" QueryErrorMessage.of_json in
      let errorCode = field_map json "ErrorCode" QueryErrorCode.of_json in
      make ?message ?errorCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A two-part error structure that can occur in ListGroupResources or SearchResources operations on CloudFormation stack-based queries. The error occurs if the CloudFormation stack on which the query is based either does not exist, or has a status that renders the stack inactive. A QueryError occurrence does not necessarily mean that AWS Resource Groups could not complete the operation, but the resulting group might have no member resources."]
module GroupIdentifier =
  struct
    type nonrec t =
      {
      groupName: GroupName.t option
        [@ocaml.doc "The name of the resource group."];
      groupArn: GroupArn.t option
        [@ocaml.doc "The ARN of the resource group."]}
    let make ?groupName = fun ?groupArn -> fun () -> { groupName; groupArn }
    let to_value x =
      structure_to_value
        [("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("GroupArn", (Option.map x.groupArn ~f:GroupArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupArn =
        (Option.map ~f:GroupArn.of_xml) (Xml.child xml_arg0 "GroupArn") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      make ?groupArn ?groupName ()
    let of_json json =
      let groupArn = field_map json "GroupArn" GroupArn.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      make ?groupArn ?groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The unique identifiers for a resource group."]
module Group =
  struct
    type nonrec t =
      {
      groupArn: GroupArn.t [@ocaml.doc "The ARN of the resource group."];
      name: GroupName.t [@ocaml.doc "The name of the resource group."];
      description: Description.t option
        [@ocaml.doc "The description of the resource group."]}
    let context_ = "Group"
    let make ?description =
      fun ~groupArn -> fun ~name -> fun () -> { description; groupArn; name }
    let to_value x =
      structure_to_value
        [("GroupArn", (Some (GroupArn.to_value x.groupArn)));
        ("Name", (Some (GroupName.to_value x.name)));
        ("Description", (Option.map x.description ~f:Description.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      let name =
        GroupName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let groupArn =
        GroupArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "GroupArn") in
      make ?description ~name ~groupArn ()
    let of_json json =
      let description = field_map json "Description" Description.of_json in
      let name = field_map_exn json "Name" GroupName.of_json in
      let groupArn = field_map_exn json "GroupArn" GroupArn.of_json in
      make ?description ~name ~groupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A resource group that contains AWS resources. You can assign resources to the group by associating either of the following elements with the group: ResourceQuery - Use a resource query to specify a set of tag keys and values. All resources in the same AWS Region and AWS account that have those keys with the same values are included in the group. You can add a resource query when you create the group, or later by using the PutGroupConfiguration operation. GroupConfiguration - Use a service configuration to associate the group with an AWS service. The configuration specifies which resource types can be included in the group."]
module GroupFilter =
  struct
    type nonrec t =
      {
      name: GroupFilterName.t
        [@ocaml.doc
          "The name of the filter. Filter names are case-sensitive."];
      values: GroupFilterValues.t
        [@ocaml.doc
          "One or more filter values. Allowed filter values vary by group filter name, and are case-sensitive."]}
    let context_ = "GroupFilter"
    let make ~name = fun ~values -> fun () -> { name; values }
    let to_value x =
      structure_to_value
        [("Name", (Some (GroupFilterName.to_value x.name)));
        ("Values", (Some (GroupFilterValues.to_value x.values)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        GroupFilterValues.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Values") in
      let name =
        GroupFilterName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~values ~name ()
    let of_json json =
      let values = field_map_exn json "Values" GroupFilterValues.of_json in
      let name = field_map_exn json "Name" GroupFilterName.of_json in
      make ~values ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A filter collection that you can use to restrict the results from a List operation to only those you want to include."]
module ListGroupResourcesItem =
  struct
    type nonrec t =
      {
      identifier: ResourceIdentifier.t option ;
      status: ResourceStatus.t option
        [@ocaml.doc
          "A structure that contains the status of this resource's membership in the group. This field is present in the response only if the group is of type AWS::EC2::HostManagement."]}
    let make ?identifier = fun ?status -> fun () -> { identifier; status }
    let to_value x =
      structure_to_value
        [("Identifier",
           (Option.map x.identifier ~f:ResourceIdentifier.to_value));
        ("Status", (Option.map x.status ~f:ResourceStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ResourceStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let identifier =
        (Option.map ~f:ResourceIdentifier.of_xml)
          (Xml.child xml_arg0 "Identifier") in
      make ?status ?identifier ()
    let of_json json =
      let status = field_map json "Status" ResourceStatus.of_json in
      let identifier = field_map json "Identifier" ResourceIdentifier.of_json in
      make ?status ?identifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure returned by the ListGroupResources operation that contains identity and group membership status information for one of the resources in the group."]
module ResourceFilter =
  struct
    type nonrec t =
      {
      name: ResourceFilterName.t
        [@ocaml.doc
          "The name of the filter. Filter names are case-sensitive."];
      values: ResourceFilterValues.t
        [@ocaml.doc
          "One or more filter values. Allowed filter values vary by resource filter name, and are case-sensitive."]}
    let context_ = "ResourceFilter"
    let make ~name = fun ~values -> fun () -> { name; values }
    let to_value x =
      structure_to_value
        [("Name", (Some (ResourceFilterName.to_value x.name)));
        ("Values", (Some (ResourceFilterValues.to_value x.values)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        ResourceFilterValues.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Values") in
      let name =
        ResourceFilterName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~values ~name ()
    let of_json json =
      let values = field_map_exn json "Values" ResourceFilterValues.of_json in
      let name = field_map_exn json "Name" ResourceFilterName.of_json in
      make ~values ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A filter name and value pair that is used to obtain more specific results from a list of resources."]
module GroupConfigurationFailureReason =
  struct
    type nonrec t = string
    let context_ = "GroupConfigurationFailureReason"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GroupConfigurationFailureReason" j
    let to_json = simple_to_json to_value
  end
module GroupConfigurationList =
  struct
    type nonrec t = GroupConfigurationItem.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:2); i
    let to_value xs =
      (xs |> (List.map ~f:GroupConfigurationItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GroupConfigurationItem.of_xml)
    let of_json j =
      list_of_json ~kind:"GroupConfigurationList"
        ~of_json:GroupConfigurationItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module GroupConfigurationStatus =
  struct
    type nonrec t =
      | UPDATING 
      | UPDATE_COMPLETE 
      | UPDATE_FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UPDATING -> "UPDATING"
      | UPDATE_COMPLETE -> "UPDATE_COMPLETE"
      | UPDATE_FAILED -> "UPDATE_FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "UPDATING" -> UPDATING
      | "UPDATE_COMPLETE" -> UPDATE_COMPLETE
      | "UPDATE_FAILED" -> UPDATE_FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GroupConfigurationStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GroupConfigurationStatus" j)
    let to_json = simple_to_json to_value
  end
module BadRequestException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request includes one or more parameters that violate validation rules."]
module ForbiddenException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The caller isn't authorized to make the request. Check permissions."]
module GroupQuery =
  struct
    type nonrec t =
      {
      groupName: GroupName.t
        [@ocaml.doc
          "The name of the resource group that is associated with the specified resource query."];
      resourceQuery: ResourceQuery.t
        [@ocaml.doc
          "The resource query that determines which AWS resources are members of the associated resource group."]}
    let context_ = "GroupQuery"
    let make ~groupName =
      fun ~resourceQuery -> fun () -> { groupName; resourceQuery }
    let to_value x =
      structure_to_value
        [("GroupName", (Some (GroupName.to_value x.groupName)));
        ("ResourceQuery", (Some (ResourceQuery.to_value x.resourceQuery)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceQuery =
        ResourceQuery.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceQuery") in
      let groupName =
        GroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GroupName") in
      make ~resourceQuery ~groupName ()
    let of_json json =
      let resourceQuery =
        field_map_exn json "ResourceQuery" ResourceQuery.of_json in
      let groupName = field_map_exn json "GroupName" GroupName.of_json in
      make ~resourceQuery ~groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A mapping of a query attached to a resource group that determines the AWS resources that are members of the group."]
module InternalServerErrorException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An internal error occurred while processing the request. Try again later."]
module MethodNotAllowedException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request uses an HTTP method that isn't allowed for the specified resource."]
module NotFoundException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "One or more of the specified resources don't exist."]
module TooManyRequestsException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You've exceeded throttling limits by making too many requests in a period of time."]
module GroupString =
  struct
    type nonrec t = string
    let context_ = "GroupString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1600) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(arn:aws(-[a-z]+)*:resource-groups:[a-z]{2}(-[a-z]+)+-\\d{1}:[0-9]{12}:group/)?[a-zA-Z0-9_\\.-]{1,128}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GroupString" j
    let to_json = simple_to_json to_value
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module FailedResourceList =
  struct
    type nonrec t = FailedResource.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:FailedResource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FailedResource.of_xml)
    let of_json j =
      list_of_json ~kind:"FailedResourceList" ~of_json:FailedResource.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module PendingResourceList =
  struct
    type nonrec t = PendingResource.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PendingResource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PendingResource.of_xml)
    let of_json j =
      list_of_json ~kind:"PendingResourceList"
        ~of_json:PendingResource.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceArnList =
  struct
    type nonrec t = ResourceArn.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:ResourceArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceArn.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceArnList" ~of_json:ResourceArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module Tags =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:8192) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9+/]*={0,2}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module QueryErrorList =
  struct
    type nonrec t = QueryError.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:QueryError.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:QueryError.of_xml)
    let of_json j =
      list_of_json ~kind:"QueryErrorList" ~of_json:QueryError.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceIdentifierList =
  struct
    type nonrec t = ResourceIdentifier.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceIdentifier.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceIdentifier.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceIdentifierList"
        ~of_json:ResourceIdentifier.of_json j
    let to_json v = composed_to_json to_value v
  end
module UnauthorizedException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request was rejected because it doesn't have valid credentials for the target resource."]
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GroupIdentifierList =
  struct
    type nonrec t = GroupIdentifier.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:GroupIdentifier.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GroupIdentifier.of_xml)
    let of_json j =
      list_of_json ~kind:"GroupIdentifierList"
        ~of_json:GroupIdentifier.of_json j
    let to_json v = composed_to_json to_value v
  end
module GroupList =
  struct
    type nonrec t = Group.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Group.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Group.of_xml)
    let of_json j = list_of_json ~kind:"GroupList" ~of_json:Group.of_json j
    let to_json v = composed_to_json to_value v
  end
module GroupFilterList =
  struct
    type nonrec t = GroupFilter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:GroupFilter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GroupFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"GroupFilterList" ~of_json:GroupFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListGroupResourcesItemList =
  struct
    type nonrec t = ListGroupResourcesItem.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ListGroupResourcesItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ListGroupResourcesItem.of_xml)
    let of_json j =
      list_of_json ~kind:"ListGroupResourcesItemList"
        ~of_json:ListGroupResourcesItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceFilterList =
  struct
    type nonrec t = ResourceFilter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceFilter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceFilterList" ~of_json:ResourceFilter.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module GroupConfiguration =
  struct
    type nonrec t =
      {
      configuration: GroupConfigurationList.t option
        [@ocaml.doc
          "The configuration currently associated with the group and in effect."];
      proposedConfiguration: GroupConfigurationList.t option
        [@ocaml.doc
          "If present, the new configuration that is in the process of being applied to the group."];
      status: GroupConfigurationStatus.t option
        [@ocaml.doc
          "The current status of an attempt to update the group configuration."];
      failureReason: GroupConfigurationFailureReason.t option
        [@ocaml.doc
          "If present, the reason why a request to update the group configuration failed."]}
    let make ?configuration =
      fun ?proposedConfiguration ->
        fun ?status ->
          fun ?failureReason ->
            fun () ->
              { configuration; proposedConfiguration; status; failureReason }
    let to_value x =
      structure_to_value
        [("Configuration",
           (Option.map x.configuration ~f:GroupConfigurationList.to_value));
        ("ProposedConfiguration",
          (Option.map x.proposedConfiguration
             ~f:GroupConfigurationList.to_value));
        ("Status",
          (Option.map x.status ~f:GroupConfigurationStatus.to_value));
        ("FailureReason",
          (Option.map x.failureReason
             ~f:GroupConfigurationFailureReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failureReason =
        (Option.map ~f:GroupConfigurationFailureReason.of_xml)
          (Xml.child xml_arg0 "FailureReason") in
      let status =
        (Option.map ~f:GroupConfigurationStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let proposedConfiguration =
        (Option.map ~f:GroupConfigurationList.of_xml)
          (Xml.child xml_arg0 "ProposedConfiguration") in
      let configuration =
        (Option.map ~f:GroupConfigurationList.of_xml)
          (Xml.child xml_arg0 "Configuration") in
      make ?failureReason ?status ?proposedConfiguration ?configuration ()
    let of_json json =
      let failureReason =
        field_map json "FailureReason"
          GroupConfigurationFailureReason.of_json in
      let status = field_map json "Status" GroupConfigurationStatus.of_json in
      let proposedConfiguration =
        field_map json "ProposedConfiguration" GroupConfigurationList.of_json in
      let configuration =
        field_map json "Configuration" GroupConfigurationList.of_json in
      make ?failureReason ?status ?proposedConfiguration ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A service configuration associated with a resource group. The configuration options are determined by the AWS service that defines the Type, and specifies which resources can be included in the group. You can add a service configuration when you create the group by using CreateGroup, or later by using the PutGroupConfiguration operation. For details about group service configuration syntax, see Service configurations for resource groups."]
module UpdateGroupQueryOutput =
  struct
    type nonrec t =
      {
      groupQuery: GroupQuery.t option
        [@ocaml.doc
          "The updated resource query associated with the resource group after the update."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `NotFoundException of NotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?groupQuery = fun () -> { groupQuery }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GroupQuery", (Option.map x.groupQuery ~f:GroupQuery.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupQuery =
        (Option.map ~f:GroupQuery.of_xml) (Xml.child xml_arg0 "GroupQuery") in
      make ?groupQuery ()
    let of_json json =
      let groupQuery = field_map json "GroupQuery" GroupQuery.of_json in
      make ?groupQuery ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the resource query of a group. For more information about resource queries, see Create a tag-based group in Resource Groups. Minimum permissions To run this command, you must have the following permissions: resource-groups:UpdateGroupQuery"]
module UpdateGroupQueryInput =
  struct
    type nonrec t =
      {
      groupName: GroupName.t option
        [@ocaml.doc "Don't use this parameter. Use Group instead."];
      group: GroupString.t option
        [@ocaml.doc "The name or the ARN of the resource group to query."];
      resourceQuery: ResourceQuery.t
        [@ocaml.doc
          "The resource query to determine which AWS resources are members of this resource group. A resource group can contain either a Configuration or a ResourceQuery, but not both."]}
    let context_ = "UpdateGroupQueryInput"
    let make ?groupName =
      fun ?group ->
        fun ~resourceQuery -> fun () -> { groupName; group; resourceQuery }
    let to_value x =
      structure_to_value
        [("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("Group", (Option.map x.group ~f:GroupString.to_value));
        ("ResourceQuery", (Some (ResourceQuery.to_value x.resourceQuery)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceQuery =
        ResourceQuery.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceQuery") in
      let group =
        (Option.map ~f:GroupString.of_xml) (Xml.child xml_arg0 "Group") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      make ~resourceQuery ?group ?groupName ()
    let of_json json =
      let resourceQuery =
        field_map_exn json "ResourceQuery" ResourceQuery.of_json in
      let group = field_map json "Group" GroupString.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      make ~resourceQuery ?group ?groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the resource query of a group. For more information about resource queries, see Create a tag-based group in Resource Groups. Minimum permissions To run this command, you must have the following permissions: resource-groups:UpdateGroupQuery"]
module UpdateGroupOutput =
  struct
    type nonrec t =
      {
      group: Group.t option
        [@ocaml.doc "The update description of the resource group."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `NotFoundException of NotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?group = fun () -> { group }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Group", (Option.map x.group ~f:Group.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let group = (Option.map ~f:Group.of_xml) (Xml.child xml_arg0 "Group") in
      make ?group ()
    let of_json json =
      let group = field_map json "Group" Group.of_json in make ?group ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the description for an existing group. You cannot update the name of a resource group. Minimum permissions To run this command, you must have the following permissions: resource-groups:UpdateGroup"]
module UpdateGroupInput =
  struct
    type nonrec t =
      {
      groupName: GroupName.t option
        [@ocaml.doc "Don't use this parameter. Use Group instead."];
      group: GroupString.t option
        [@ocaml.doc "The name or the ARN of the resource group to modify."];
      description: Description.t option
        [@ocaml.doc
          "The new description that you want to update the resource group with. Descriptions can contain letters, numbers, hyphens, underscores, periods, and spaces."]}
    let make ?groupName =
      fun ?group ->
        fun ?description -> fun () -> { groupName; group; description }
    let to_value x =
      structure_to_value
        [("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("Group", (Option.map x.group ~f:GroupString.to_value));
        ("Description", (Option.map x.description ~f:Description.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      let group =
        (Option.map ~f:GroupString.of_xml) (Xml.child xml_arg0 "Group") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      make ?description ?group ?groupName ()
    let of_json json =
      let description = field_map json "Description" Description.of_json in
      let group = field_map json "Group" GroupString.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      make ?description ?group ?groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the description for an existing group. You cannot update the name of a resource group. Minimum permissions To run this command, you must have the following permissions: resource-groups:UpdateGroup"]
module UntagOutput =
  struct
    type nonrec t =
      {
      arn: GroupArn.t option
        [@ocaml.doc
          "The ARN of the resource group from which tags have been removed."];
      keys: TagKeyList.t option
        [@ocaml.doc "The keys of the tags that were removed."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `NotFoundException of NotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn = fun ?keys -> fun () -> { arn; keys }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:GroupArn.to_value));
        ("Keys", (Option.map x.keys ~f:TagKeyList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keys =
        (Option.map ~f:TagKeyList.of_xml) (Xml.child xml_arg0 "Keys") in
      let arn = (Option.map ~f:GroupArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?keys ?arn ()
    let of_json json =
      let keys = field_map json "Keys" TagKeyList.of_json in
      let arn = field_map json "Arn" GroupArn.of_json in make ?keys ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes tags from a specified resource group. Minimum permissions To run this command, you must have the following permissions: resource-groups:Untag"]
module UntagInput =
  struct
    type nonrec t =
      {
      arn: GroupArn.t
        [@ocaml.doc
          "The ARN of the resource group from which to remove tags. The command removed both the specified keys and any values associated with those keys."];
      keys: TagKeyList.t [@ocaml.doc "The keys of the tags to be removed."]}
    let context_ = "UntagInput"
    let make ~arn = fun ~keys -> fun () -> { arn; keys }
    let to_value x =
      structure_to_value
        [("Arn", (Some (GroupArn.to_value x.arn)));
        ("Keys", (Some (TagKeyList.to_value x.keys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keys =
        TagKeyList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Keys") in
      let arn =
        GroupArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Arn") in
      make ~keys ~arn ()
    let of_json json =
      let keys = field_map_exn json "Keys" TagKeyList.of_json in
      let arn = field_map_exn json "Arn" GroupArn.of_json in
      make ~keys ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes tags from a specified resource group. Minimum permissions To run this command, you must have the following permissions: resource-groups:Untag"]
module UngroupResourcesOutput =
  struct
    type nonrec t =
      {
      succeeded: ResourceArnList.t option
        [@ocaml.doc
          "A list of resources that were successfully removed from the group by this operation."];
      failed: FailedResourceList.t option
        [@ocaml.doc
          "A list of any resources that failed to be removed from the group by this operation."];
      pending: PendingResourceList.t option
        [@ocaml.doc
          "A list of any resources that are still in the process of being removed from the group by this operation. These pending removals continue asynchronously. You can check the status of pending removals by using the ListGroupResources operation. After the resource is successfully removed, it no longer appears in the response."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `NotFoundException of NotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?succeeded =
      fun ?failed -> fun ?pending -> fun () -> { succeeded; failed; pending }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Succeeded", (Option.map x.succeeded ~f:ResourceArnList.to_value));
        ("Failed", (Option.map x.failed ~f:FailedResourceList.to_value));
        ("Pending", (Option.map x.pending ~f:PendingResourceList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pending =
        (Option.map ~f:PendingResourceList.of_xml)
          (Xml.child xml_arg0 "Pending") in
      let failed =
        (Option.map ~f:FailedResourceList.of_xml)
          (Xml.child xml_arg0 "Failed") in
      let succeeded =
        (Option.map ~f:ResourceArnList.of_xml)
          (Xml.child xml_arg0 "Succeeded") in
      make ?pending ?failed ?succeeded ()
    let of_json json =
      let pending = field_map json "Pending" PendingResourceList.of_json in
      let failed = field_map json "Failed" FailedResourceList.of_json in
      let succeeded = field_map json "Succeeded" ResourceArnList.of_json in
      make ?pending ?failed ?succeeded ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes the specified resources from the specified group. Minimum permissions To run this command, you must have the following permissions: resource-groups:UngroupResources"]
module UngroupResourcesInput =
  struct
    type nonrec t =
      {
      group: GroupString.t
        [@ocaml.doc
          "The name or the ARN of the resource group from which to remove the resources."];
      resourceArns: ResourceArnList.t
        [@ocaml.doc
          "The ARNs of the resources to be removed from the group."]}
    let context_ = "UngroupResourcesInput"
    let make ~group = fun ~resourceArns -> fun () -> { group; resourceArns }
    let to_value x =
      structure_to_value
        [("Group", (Some (GroupString.to_value x.group)));
        ("ResourceArns", (Some (ResourceArnList.to_value x.resourceArns)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArns =
        ResourceArnList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArns") in
      let group =
        GroupString.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Group") in
      make ~resourceArns ~group ()
    let of_json json =
      let resourceArns =
        field_map_exn json "ResourceArns" ResourceArnList.of_json in
      let group = field_map_exn json "Group" GroupString.of_json in
      make ~resourceArns ~group ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes the specified resources from the specified group. Minimum permissions To run this command, you must have the following permissions: resource-groups:UngroupResources"]
module TagOutput =
  struct
    type nonrec t =
      {
      arn: GroupArn.t option [@ocaml.doc "The ARN of the tagged resource."];
      tags: Tags.t option
        [@ocaml.doc
          "The tags that have been added to the specified resource group."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `NotFoundException of NotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn = fun ?tags -> fun () -> { arn; tags }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:GroupArn.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let arn = (Option.map ~f:GroupArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?tags ?arn ()
    let of_json json =
      let tags = field_map json "Tags" Tags.of_json in
      let arn = field_map json "Arn" GroupArn.of_json in make ?tags ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds tags to a resource group with the specified ARN. Existing tags on a resource group are not changed if they are not specified in the request parameters. Do not store personally identifiable information (PII) or other confidential or sensitive information in tags. We use tags to provide you with billing and administration services. Tags are not intended to be used for private or sensitive data. Minimum permissions To run this command, you must have the following permissions: resource-groups:Tag"]
module TagInput =
  struct
    type nonrec t =
      {
      arn: GroupArn.t
        [@ocaml.doc "The ARN of the resource group to which to add tags."];
      tags: Tags.t
        [@ocaml.doc
          "The tags to add to the specified resource group. A tag is a string-to-string map of key-value pairs."]}
    let context_ = "TagInput"
    let make ~arn = fun ~tags -> fun () -> { arn; tags }
    let to_value x =
      structure_to_value
        [("Arn", (Some (GroupArn.to_value x.arn)));
        ("Tags", (Some (Tags.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        Tags.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let arn =
        GroupArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Arn") in
      make ~tags ~arn ()
    let of_json json =
      let tags = field_map_exn json "Tags" Tags.of_json in
      let arn = field_map_exn json "Arn" GroupArn.of_json in
      make ~tags ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds tags to a resource group with the specified ARN. Existing tags on a resource group are not changed if they are not specified in the request parameters. Do not store personally identifiable information (PII) or other confidential or sensitive information in tags. We use tags to provide you with billing and administration services. Tags are not intended to be used for private or sensitive data. Minimum permissions To run this command, you must have the following permissions: resource-groups:Tag"]
module SearchResourcesOutput =
  struct
    type nonrec t =
      {
      resourceIdentifiers: ResourceIdentifierList.t option
        [@ocaml.doc
          "The ARNs and resource types of resources that are members of the group that you specified."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If present, indicates that more output is available than is included in the current response. Use this value in the NextToken request parameter in a subsequent call to the operation to get the next part of the output. You should repeat this until the NextToken response element comes back as null."];
      queryErrors: QueryErrorList.t option
        [@ocaml.doc
          "A list of QueryError objects. Each error is an object that contains ErrorCode and Message structures. Possible values for ErrorCode are CLOUDFORMATION_STACK_INACTIVE and CLOUDFORMATION_STACK_NOT_EXISTING."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?resourceIdentifiers =
      fun ?nextToken ->
        fun ?queryErrors ->
          fun () -> { resourceIdentifiers; nextToken; queryErrors }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ResourceIdentifiers",
           (Option.map x.resourceIdentifiers
              ~f:ResourceIdentifierList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("QueryErrors",
          (Option.map x.queryErrors ~f:QueryErrorList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let queryErrors =
        (Option.map ~f:QueryErrorList.of_xml)
          (Xml.child xml_arg0 "QueryErrors") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let resourceIdentifiers =
        (Option.map ~f:ResourceIdentifierList.of_xml)
          (Xml.child xml_arg0 "ResourceIdentifiers") in
      make ?queryErrors ?nextToken ?resourceIdentifiers ()
    let of_json json =
      let queryErrors = field_map json "QueryErrors" QueryErrorList.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let resourceIdentifiers =
        field_map json "ResourceIdentifiers" ResourceIdentifierList.of_json in
      make ?queryErrors ?nextToken ?resourceIdentifiers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of AWS resource identifiers that matches the specified query. The query uses the same format as a resource query in a CreateGroup or UpdateGroupQuery operation. Minimum permissions To run this command, you must have the following permissions: resource-groups:SearchResources cloudformation:DescribeStacks cloudformation:ListStackResources tag:GetResources"]
module SearchResourcesInput =
  struct
    type nonrec t =
      {
      resourceQuery: ResourceQuery.t
        [@ocaml.doc
          "The search query, using the same formats that are supported for resource group definition. For more information, see CreateGroup."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The total number of results that you want included on each page of the response. If you do not include this parameter, it defaults to a value that is specific to the operation. If additional items exist beyond the maximum you specify, the NextToken response element is present and has a value (is not null). Include that value as the NextToken request parameter in the next call to the operation to get the next part of the results. Note that the service might return fewer results than the maximum even when there are more results available. You should check NextToken after every operation to ensure that you receive all of the results."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The parameter for receiving additional results if you receive a NextToken response in a previous request. A NextToken response indicates that more output is available. Set this parameter to the value provided by a previous call's NextToken response to indicate where the output should continue from."]}
    let context_ = "SearchResourcesInput"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~resourceQuery ->
          fun () -> { maxResults; nextToken; resourceQuery }
    let to_value x =
      structure_to_value
        [("ResourceQuery", (Some (ResourceQuery.to_value x.resourceQuery)));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let resourceQuery =
        ResourceQuery.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceQuery") in
      make ?nextToken ?maxResults ~resourceQuery ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let resourceQuery =
        field_map_exn json "ResourceQuery" ResourceQuery.of_json in
      make ?nextToken ?maxResults ~resourceQuery ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of AWS resource identifiers that matches the specified query. The query uses the same format as a resource query in a CreateGroup or UpdateGroupQuery operation. Minimum permissions To run this command, you must have the following permissions: resource-groups:SearchResources cloudformation:DescribeStacks cloudformation:ListStackResources tag:GetResources"]
module PutGroupConfigurationOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `NotFoundException of NotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches a service configuration to the specified group. This occurs asynchronously, and can take time to complete. You can use GetGroupConfiguration to check the status of the update. Minimum permissions To run this command, you must have the following permissions: resource-groups:PutGroupConfiguration"]
module PutGroupConfigurationInput =
  struct
    type nonrec t =
      {
      group: GroupString.t option
        [@ocaml.doc
          "The name or ARN of the resource group with the configuration that you want to update."];
      configuration: GroupConfigurationList.t option
        [@ocaml.doc
          "The new configuration to associate with the specified group. A configuration associates the resource group with an AWS service and specifies how the service can interact with the resources in the group. A configuration is an array of GroupConfigurationItem elements. For information about the syntax of a service configuration, see Service configurations for resource groups. A resource group can contain either a Configuration or a ResourceQuery, but not both."]}
    let make ?group =
      fun ?configuration -> fun () -> { group; configuration }
    let to_value x =
      structure_to_value
        [("Group", (Option.map x.group ~f:GroupString.to_value));
        ("Configuration",
          (Option.map x.configuration ~f:GroupConfigurationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuration =
        (Option.map ~f:GroupConfigurationList.of_xml)
          (Xml.child xml_arg0 "Configuration") in
      let group =
        (Option.map ~f:GroupString.of_xml) (Xml.child xml_arg0 "Group") in
      make ?configuration ?group ()
    let of_json json =
      let configuration =
        field_map json "Configuration" GroupConfigurationList.of_json in
      let group = field_map json "Group" GroupString.of_json in
      make ?configuration ?group ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches a service configuration to the specified group. This occurs asynchronously, and can take time to complete. You can use GetGroupConfiguration to check the status of the update. Minimum permissions To run this command, you must have the following permissions: resource-groups:PutGroupConfiguration"]
module ListGroupsOutput =
  struct
    type nonrec t =
      {
      groupIdentifiers: GroupIdentifierList.t option
        [@ocaml.doc
          "A list of GroupIdentifier objects. Each identifier is an object that contains both the Name and the GroupArn."];
      groups: GroupList.t option
        [@ocaml.doc
          "Deprecated - don't use this field. Use the GroupIdentifiers response field instead."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If present, indicates that more output is available than is included in the current response. Use this value in the NextToken request parameter in a subsequent call to the operation to get the next part of the output. You should repeat this until the NextToken response element comes back as null."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?groupIdentifiers =
      fun ?groups ->
        fun ?nextToken -> fun () -> { groupIdentifiers; groups; nextToken }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GroupIdentifiers",
           (Option.map x.groupIdentifiers ~f:GroupIdentifierList.to_value));
        ("Groups", (Option.map x.groups ~f:GroupList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let groups =
        (Option.map ~f:GroupList.of_xml) (Xml.child xml_arg0 "Groups") in
      let groupIdentifiers =
        (Option.map ~f:GroupIdentifierList.of_xml)
          (Xml.child xml_arg0 "GroupIdentifiers") in
      make ?nextToken ?groups ?groupIdentifiers ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let groups = field_map json "Groups" GroupList.of_json in
      let groupIdentifiers =
        field_map json "GroupIdentifiers" GroupIdentifierList.of_json in
      make ?nextToken ?groups ?groupIdentifiers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of existing resource groups in your account. Minimum permissions To run this command, you must have the following permissions: resource-groups:ListGroups"]
module ListGroupsInput =
  struct
    type nonrec t =
      {
      filters: GroupFilterList.t option
        [@ocaml.doc
          "Filters, formatted as GroupFilter objects, that you want to apply to a ListGroups operation. resource-type - Filter the results to include only those of the specified resource types. Specify up to five resource types in the format AWS::ServiceCode::ResourceType . For example, AWS::EC2::Instance, or AWS::S3::Bucket. configuration-type - Filter the results to include only those groups that have the specified configuration types attached. The current supported values are: AWS:EC2::CapacityReservationPool AWS:EC2::HostManagement"];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The total number of results that you want included on each page of the response. If you do not include this parameter, it defaults to a value that is specific to the operation. If additional items exist beyond the maximum you specify, the NextToken response element is present and has a value (is not null). Include that value as the NextToken request parameter in the next call to the operation to get the next part of the results. Note that the service might return fewer results than the maximum even when there are more results available. You should check NextToken after every operation to ensure that you receive all of the results."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The parameter for receiving additional results if you receive a NextToken response in a previous request. A NextToken response indicates that more output is available. Set this parameter to the value provided by a previous call's NextToken response to indicate where the output should continue from."]}
    let make ?filters =
      fun ?maxResults ->
        fun ?nextToken -> fun () -> { filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("Filters", (Option.map x.filters ~f:GroupFilterList.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let filters =
        (Option.map ~f:GroupFilterList.of_xml) (Xml.child xml_arg0 "Filters") in
      make ?nextToken ?maxResults ?filters ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let filters = field_map json "Filters" GroupFilterList.of_json in
      make ?nextToken ?maxResults ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of existing resource groups in your account. Minimum permissions To run this command, you must have the following permissions: resource-groups:ListGroups"]
module ListGroupResourcesOutput =
  struct
    type nonrec t =
      {
      resources: ListGroupResourcesItemList.t option
        [@ocaml.doc
          "An array of resources from which you can determine each resource's identity, type, and group membership status."];
      resourceIdentifiers: ResourceIdentifierList.t option
        [@ocaml.doc
          "Deprecated - don't use this parameter. Use the Resources response field instead."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If present, indicates that more output is available than is included in the current response. Use this value in the NextToken request parameter in a subsequent call to the operation to get the next part of the output. You should repeat this until the NextToken response element comes back as null."];
      queryErrors: QueryErrorList.t option
        [@ocaml.doc
          "A list of QueryError objects. Each error is an object that contains ErrorCode and Message structures. Possible values for ErrorCode are CLOUDFORMATION_STACK_INACTIVE and CLOUDFORMATION_STACK_NOT_EXISTING."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `NotFoundException of NotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?resources =
      fun ?resourceIdentifiers ->
        fun ?nextToken ->
          fun ?queryErrors ->
            fun () ->
              { resources; resourceIdentifiers; nextToken; queryErrors }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Resources",
           (Option.map x.resources ~f:ListGroupResourcesItemList.to_value));
        ("ResourceIdentifiers",
          (Option.map x.resourceIdentifiers
             ~f:ResourceIdentifierList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("QueryErrors",
          (Option.map x.queryErrors ~f:QueryErrorList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let queryErrors =
        (Option.map ~f:QueryErrorList.of_xml)
          (Xml.child xml_arg0 "QueryErrors") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let resourceIdentifiers =
        (Option.map ~f:ResourceIdentifierList.of_xml)
          (Xml.child xml_arg0 "ResourceIdentifiers") in
      let resources =
        (Option.map ~f:ListGroupResourcesItemList.of_xml)
          (Xml.child xml_arg0 "Resources") in
      make ?queryErrors ?nextToken ?resourceIdentifiers ?resources ()
    let of_json json =
      let queryErrors = field_map json "QueryErrors" QueryErrorList.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let resourceIdentifiers =
        field_map json "ResourceIdentifiers" ResourceIdentifierList.of_json in
      let resources =
        field_map json "Resources" ListGroupResourcesItemList.of_json in
      make ?queryErrors ?nextToken ?resourceIdentifiers ?resources ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of ARNs of the resources that are members of a specified resource group. Minimum permissions To run this command, you must have the following permissions: resource-groups:ListGroupResources cloudformation:DescribeStacks cloudformation:ListStackResources tag:GetResources"]
module ListGroupResourcesInput =
  struct
    type nonrec t =
      {
      groupName: GroupName.t option
        [@ocaml.doc
          "Deprecated - don't use this parameter. Use the Group request field instead."];
      group: GroupString.t option
        [@ocaml.doc "The name or the ARN of the resource group"];
      filters: ResourceFilterList.t option
        [@ocaml.doc
          "Filters, formatted as ResourceFilter objects, that you want to apply to a ListGroupResources operation. Filters the results to include only those of the specified resource types. resource-type - Filter resources by their type. Specify up to five resource types in the format AWS::ServiceCode::ResourceType. For example, AWS::EC2::Instance, or AWS::S3::Bucket. When you specify a resource-type filter for ListGroupResources, AWS Resource Groups validates your filter resource types against the types that are defined in the query associated with the group. For example, if a group contains only S3 buckets because its query specifies only that resource type, but your resource-type filter includes EC2 instances, AWS Resource Groups does not filter for EC2 instances. In this case, a ListGroupResources request returns a BadRequestException error with a message similar to the following: The resource types specified as filters in the request are not valid. The error includes a list of resource types that failed the validation because they are not part of the query associated with the group. This validation doesn't occur when the group query specifies AWS::AllSupported, because a group based on such a query can contain any of the allowed resource types for the query type (tag-based or AWS CloudFormation stack-based queries)."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The total number of results that you want included on each page of the response. If you do not include this parameter, it defaults to a value that is specific to the operation. If additional items exist beyond the maximum you specify, the NextToken response element is present and has a value (is not null). Include that value as the NextToken request parameter in the next call to the operation to get the next part of the results. Note that the service might return fewer results than the maximum even when there are more results available. You should check NextToken after every operation to ensure that you receive all of the results."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The parameter for receiving additional results if you receive a NextToken response in a previous request. A NextToken response indicates that more output is available. Set this parameter to the value provided by a previous call's NextToken response to indicate where the output should continue from."]}
    let make ?groupName =
      fun ?group ->
        fun ?filters ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () -> { groupName; group; filters; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("Group", (Option.map x.group ~f:GroupString.to_value));
        ("Filters", (Option.map x.filters ~f:ResourceFilterList.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let filters =
        (Option.map ~f:ResourceFilterList.of_xml)
          (Xml.child xml_arg0 "Filters") in
      let group =
        (Option.map ~f:GroupString.of_xml) (Xml.child xml_arg0 "Group") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      make ?nextToken ?maxResults ?filters ?group ?groupName ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let filters = field_map json "Filters" ResourceFilterList.of_json in
      let group = field_map json "Group" GroupString.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      make ?nextToken ?maxResults ?filters ?group ?groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of ARNs of the resources that are members of a specified resource group. Minimum permissions To run this command, you must have the following permissions: resource-groups:ListGroupResources cloudformation:DescribeStacks cloudformation:ListStackResources tag:GetResources"]
module GroupResourcesOutput =
  struct
    type nonrec t =
      {
      succeeded: ResourceArnList.t option
        [@ocaml.doc
          "A list of ARNs of resources that were successfully added to the group by this operation."];
      failed: FailedResourceList.t option
        [@ocaml.doc
          "A list of ARNs of any resources that failed to be added to the group by this operation."];
      pending: PendingResourceList.t option
        [@ocaml.doc
          "A list of ARNs of any resources that are still in the process of being added to the group by this operation. These pending additions continue asynchronously. You can check the status of pending additions by using the ListGroupResources operation, and checking the Resources array in the response and the Status field of each object in that array."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `NotFoundException of NotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?succeeded =
      fun ?failed -> fun ?pending -> fun () -> { succeeded; failed; pending }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Succeeded", (Option.map x.succeeded ~f:ResourceArnList.to_value));
        ("Failed", (Option.map x.failed ~f:FailedResourceList.to_value));
        ("Pending", (Option.map x.pending ~f:PendingResourceList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pending =
        (Option.map ~f:PendingResourceList.of_xml)
          (Xml.child xml_arg0 "Pending") in
      let failed =
        (Option.map ~f:FailedResourceList.of_xml)
          (Xml.child xml_arg0 "Failed") in
      let succeeded =
        (Option.map ~f:ResourceArnList.of_xml)
          (Xml.child xml_arg0 "Succeeded") in
      make ?pending ?failed ?succeeded ()
    let of_json json =
      let pending = field_map json "Pending" PendingResourceList.of_json in
      let failed = field_map json "Failed" FailedResourceList.of_json in
      let succeeded = field_map json "Succeeded" ResourceArnList.of_json in
      make ?pending ?failed ?succeeded ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds the specified resources to the specified group. Minimum permissions To run this command, you must have the following permissions: resource-groups:GroupResources"]
module GroupResourcesInput =
  struct
    type nonrec t =
      {
      group: GroupString.t
        [@ocaml.doc
          "The name or the ARN of the resource group to add resources to."];
      resourceArns: ResourceArnList.t
        [@ocaml.doc
          "The list of ARNs for resources to be added to the group."]}
    let context_ = "GroupResourcesInput"
    let make ~group = fun ~resourceArns -> fun () -> { group; resourceArns }
    let to_value x =
      structure_to_value
        [("Group", (Some (GroupString.to_value x.group)));
        ("ResourceArns", (Some (ResourceArnList.to_value x.resourceArns)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArns =
        ResourceArnList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArns") in
      let group =
        GroupString.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Group") in
      make ~resourceArns ~group ()
    let of_json json =
      let resourceArns =
        field_map_exn json "ResourceArns" ResourceArnList.of_json in
      let group = field_map_exn json "Group" GroupString.of_json in
      make ~resourceArns ~group ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds the specified resources to the specified group. Minimum permissions To run this command, you must have the following permissions: resource-groups:GroupResources"]
module GetTagsOutput =
  struct
    type nonrec t =
      {
      arn: GroupArn.t option
        [@ocaml.doc "The ARN of the tagged resource group."];
      tags: Tags.t option
        [@ocaml.doc "The tags associated with the specified resource group."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `NotFoundException of NotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn = fun ?tags -> fun () -> { arn; tags }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:GroupArn.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let arn = (Option.map ~f:GroupArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?tags ?arn ()
    let of_json json =
      let tags = field_map json "Tags" Tags.of_json in
      let arn = field_map json "Arn" GroupArn.of_json in make ?tags ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of tags that are associated with a resource group, specified by an ARN. Minimum permissions To run this command, you must have the following permissions: resource-groups:GetTags"]
module GetTagsInput =
  struct
    type nonrec t =
      {
      arn: GroupArn.t
        [@ocaml.doc
          "The ARN of the resource group whose tags you want to retrieve."]}
    let context_ = "GetTagsInput"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value [("Arn", (Some (GroupArn.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        GroupArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "Arn" GroupArn.of_json in make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of tags that are associated with a resource group, specified by an ARN. Minimum permissions To run this command, you must have the following permissions: resource-groups:GetTags"]
module GetGroupQueryOutput =
  struct
    type nonrec t =
      {
      groupQuery: GroupQuery.t option
        [@ocaml.doc
          "The resource query associated with the specified group. For more information about resource queries, see Create a tag-based group in Resource Groups."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `NotFoundException of NotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?groupQuery = fun () -> { groupQuery }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GroupQuery", (Option.map x.groupQuery ~f:GroupQuery.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupQuery =
        (Option.map ~f:GroupQuery.of_xml) (Xml.child xml_arg0 "GroupQuery") in
      make ?groupQuery ()
    let of_json json =
      let groupQuery = field_map json "GroupQuery" GroupQuery.of_json in
      make ?groupQuery ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the resource query associated with the specified resource group. For more information about resource queries, see Create a tag-based group in Resource Groups. Minimum permissions To run this command, you must have the following permissions: resource-groups:GetGroupQuery"]
module GetGroupQueryInput =
  struct
    type nonrec t =
      {
      groupName: GroupName.t option
        [@ocaml.doc "Don't use this parameter. Use Group instead."];
      group: GroupString.t option
        [@ocaml.doc "The name or the ARN of the resource group to query."]}
    let make ?groupName = fun ?group -> fun () -> { groupName; group }
    let to_value x =
      structure_to_value
        [("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("Group", (Option.map x.group ~f:GroupString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let group =
        (Option.map ~f:GroupString.of_xml) (Xml.child xml_arg0 "Group") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      make ?group ?groupName ()
    let of_json json =
      let group = field_map json "Group" GroupString.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      make ?group ?groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the resource query associated with the specified resource group. For more information about resource queries, see Create a tag-based group in Resource Groups. Minimum permissions To run this command, you must have the following permissions: resource-groups:GetGroupQuery"]
module GetGroupOutput =
  struct
    type nonrec t =
      {
      group: Group.t option
        [@ocaml.doc "A full description of the resource group."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `NotFoundException of NotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?group = fun () -> { group }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Group", (Option.map x.group ~f:Group.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let group = (Option.map ~f:Group.of_xml) (Xml.child xml_arg0 "Group") in
      make ?group ()
    let of_json json =
      let group = field_map json "Group" Group.of_json in make ?group ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about a specified resource group. Minimum permissions To run this command, you must have the following permissions: resource-groups:GetGroup"]
module GetGroupInput =
  struct
    type nonrec t =
      {
      groupName: GroupName.t option
        [@ocaml.doc
          "Deprecated - don't use this parameter. Use Group instead."];
      group: GroupString.t option
        [@ocaml.doc "The name or the ARN of the resource group to retrieve."]}
    let make ?groupName = fun ?group -> fun () -> { groupName; group }
    let to_value x =
      structure_to_value
        [("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("Group", (Option.map x.group ~f:GroupString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let group =
        (Option.map ~f:GroupString.of_xml) (Xml.child xml_arg0 "Group") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      make ?group ?groupName ()
    let of_json json =
      let group = field_map json "Group" GroupString.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      make ?group ?groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about a specified resource group. Minimum permissions To run this command, you must have the following permissions: resource-groups:GetGroup"]
module GetGroupConfigurationOutput =
  struct
    type nonrec t =
      {
      groupConfiguration: GroupConfiguration.t option
        [@ocaml.doc
          "The service configuration associated with the specified group. For details about the service configuration syntax, see Service configurations for resource groups."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `NotFoundException of NotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?groupConfiguration = fun () -> { groupConfiguration }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GroupConfiguration",
           (Option.map x.groupConfiguration ~f:GroupConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupConfiguration =
        (Option.map ~f:GroupConfiguration.of_xml)
          (Xml.child xml_arg0 "GroupConfiguration") in
      make ?groupConfiguration ()
    let of_json json =
      let groupConfiguration =
        field_map json "GroupConfiguration" GroupConfiguration.of_json in
      make ?groupConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the service configuration associated with the specified resource group. For details about the service configuration syntax, see Service configurations for resource groups. Minimum permissions To run this command, you must have the following permissions: resource-groups:GetGroupConfiguration"]
module GetGroupConfigurationInput =
  struct
    type nonrec t =
      {
      group: GroupString.t option
        [@ocaml.doc "The name or the ARN of the resource group."]}
    let make ?group = fun () -> { group }
    let to_value x =
      structure_to_value
        [("Group", (Option.map x.group ~f:GroupString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let group =
        (Option.map ~f:GroupString.of_xml) (Xml.child xml_arg0 "Group") in
      make ?group ()
    let of_json json =
      let group = field_map json "Group" GroupString.of_json in
      make ?group ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the service configuration associated with the specified resource group. For details about the service configuration syntax, see Service configurations for resource groups. Minimum permissions To run this command, you must have the following permissions: resource-groups:GetGroupConfiguration"]
module DeleteGroupOutput =
  struct
    type nonrec t =
      {
      group: Group.t option
        [@ocaml.doc "A full description of the deleted resource group."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `NotFoundException of NotFoundException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?group = fun () -> { group }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Group", (Option.map x.group ~f:Group.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let group = (Option.map ~f:Group.of_xml) (Xml.child xml_arg0 "Group") in
      make ?group ()
    let of_json json =
      let group = field_map json "Group" Group.of_json in make ?group ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified resource group. Deleting a resource group does not delete any resources that are members of the group; it only deletes the group structure. Minimum permissions To run this command, you must have the following permissions: resource-groups:DeleteGroup"]
module DeleteGroupInput =
  struct
    type nonrec t =
      {
      groupName: GroupName.t option
        [@ocaml.doc
          "Deprecated - don't use this parameter. Use Group instead."];
      group: GroupString.t option
        [@ocaml.doc "The name or the ARN of the resource group to delete."]}
    let make ?groupName = fun ?group -> fun () -> { groupName; group }
    let to_value x =
      structure_to_value
        [("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("Group", (Option.map x.group ~f:GroupString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let group =
        (Option.map ~f:GroupString.of_xml) (Xml.child xml_arg0 "Group") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      make ?group ?groupName ()
    let of_json json =
      let group = field_map json "Group" GroupString.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      make ?group ?groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified resource group. Deleting a resource group does not delete any resources that are members of the group; it only deletes the group structure. Minimum permissions To run this command, you must have the following permissions: resource-groups:DeleteGroup"]
module CreateGroupOutput =
  struct
    type nonrec t =
      {
      group: Group.t option
        [@ocaml.doc "The description of the resource group."];
      resourceQuery: ResourceQuery.t option
        [@ocaml.doc
          "The resource query associated with the group. For more information about resource queries, see Create a tag-based group in Resource Groups."];
      tags: Tags.t option [@ocaml.doc "The tags associated with the group."];
      groupConfiguration: GroupConfiguration.t option
        [@ocaml.doc
          "The service configuration associated with the resource group. For details about the syntax of a service configuration, see Service configurations for resource groups."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ForbiddenException of ForbiddenException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `MethodNotAllowedException of MethodNotAllowedException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?group =
      fun ?resourceQuery ->
        fun ?tags ->
          fun ?groupConfiguration ->
            fun () -> { group; resourceQuery; tags; groupConfiguration }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ForbiddenException" ->
          `ForbiddenException (ForbiddenException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "MethodNotAllowedException" ->
          `MethodNotAllowedException (MethodNotAllowedException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ForbiddenException e ->
          `Assoc
            [("error", (`String "ForbiddenException"));
            ("details", (ForbiddenException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `MethodNotAllowedException e ->
          `Assoc
            [("error", (`String "MethodNotAllowedException"));
            ("details", (MethodNotAllowedException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Group", (Option.map x.group ~f:Group.to_value));
        ("ResourceQuery",
          (Option.map x.resourceQuery ~f:ResourceQuery.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("GroupConfiguration",
          (Option.map x.groupConfiguration ~f:GroupConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupConfiguration =
        (Option.map ~f:GroupConfiguration.of_xml)
          (Xml.child xml_arg0 "GroupConfiguration") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let resourceQuery =
        (Option.map ~f:ResourceQuery.of_xml)
          (Xml.child xml_arg0 "ResourceQuery") in
      let group = (Option.map ~f:Group.of_xml) (Xml.child xml_arg0 "Group") in
      make ?groupConfiguration ?tags ?resourceQuery ?group ()
    let of_json json =
      let groupConfiguration =
        field_map json "GroupConfiguration" GroupConfiguration.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let resourceQuery =
        field_map json "ResourceQuery" ResourceQuery.of_json in
      let group = field_map json "Group" Group.of_json in
      make ?groupConfiguration ?tags ?resourceQuery ?group ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a resource group with the specified name and description. You can optionally include a resource query, or a service configuration. For more information about constructing a resource query, see Create a tag-based group in Resource Groups. For more information about service configurations, see Service configurations for resource groups. Minimum permissions To run this command, you must have the following permissions: resource-groups:CreateGroup"]
module CreateGroupInput =
  struct
    type nonrec t =
      {
      name: GroupName.t
        [@ocaml.doc
          "The name of the group, which is the identifier of the group in other operations. You can't change the name of a resource group after you create it. A resource group name can consist of letters, numbers, hyphens, periods, and underscores. The name cannot start with AWS or aws; these are reserved. A resource group name must be unique within each AWS Region in your AWS account."];
      description: Description.t option
        [@ocaml.doc
          "The description of the resource group. Descriptions can consist of letters, numbers, hyphens, underscores, periods, and spaces."];
      resourceQuery: ResourceQuery.t option
        [@ocaml.doc
          "The resource query that determines which AWS resources are members of this group. For more information about resource queries, see Create a tag-based group in Resource Groups. A resource group can contain either a ResourceQuery or a Configuration, but not both."];
      tags: Tags.t option
        [@ocaml.doc
          "The tags to add to the group. A tag is key-value pair string."];
      configuration: GroupConfigurationList.t option
        [@ocaml.doc
          "A configuration associates the resource group with an AWS service and specifies how the service can interact with the resources in the group. A configuration is an array of GroupConfigurationItem elements. For details about the syntax of service configurations, see Service configurations for resource groups. A resource group can contain either a Configuration or a ResourceQuery, but not both."]}
    let context_ = "CreateGroupInput"
    let make ?description =
      fun ?resourceQuery ->
        fun ?tags ->
          fun ?configuration ->
            fun ~name ->
              fun () ->
                { description; resourceQuery; tags; configuration; name }
    let to_value x =
      structure_to_value
        [("Name", (Some (GroupName.to_value x.name)));
        ("Description", (Option.map x.description ~f:Description.to_value));
        ("ResourceQuery",
          (Option.map x.resourceQuery ~f:ResourceQuery.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("Configuration",
          (Option.map x.configuration ~f:GroupConfigurationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuration =
        (Option.map ~f:GroupConfigurationList.of_xml)
          (Xml.child xml_arg0 "Configuration") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let resourceQuery =
        (Option.map ~f:ResourceQuery.of_xml)
          (Xml.child xml_arg0 "ResourceQuery") in
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      let name =
        GroupName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?configuration ?tags ?resourceQuery ?description ~name ()
    let of_json json =
      let configuration =
        field_map json "Configuration" GroupConfigurationList.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let resourceQuery =
        field_map json "ResourceQuery" ResourceQuery.of_json in
      let description = field_map json "Description" Description.of_json in
      let name = field_map_exn json "Name" GroupName.of_json in
      make ?configuration ?tags ?resourceQuery ?description ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a resource group with the specified name and description. You can optionally include a resource query, or a service configuration. For more information about constructing a resource query, see Create a tag-based group in Resource Groups. For more information about service configurations, see Service configurations for resource groups. Minimum permissions To run this command, you must have the following permissions: resource-groups:CreateGroup"]